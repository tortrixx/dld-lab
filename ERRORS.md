# ERRORS — 错误记录

> **用途**：记录项目过程中遇到的一切问题——编译报错、仿真波形不符预期、时序违例、上板异常、设计决策失误……
>
> **为什么认真写**：实验报告评分项⑤"故障及问题分析"**占 15 分**，是第三高单项分。
> 得分点不在"我们没出错"，而在**"定位过程"写得清不清楚**——怎么一步步缩小范围、排除了哪些可能、最后怎么锁定根因。
> 所以**发现问题的当下就要写**，事后回忆必然丢失最有价值的排查细节。

---

## 编号规则

`ERR-0001` 递增。每条记录用下面的五段式 + 状态。

| 字段 | 说明 |
|---|---|
| **现象** | 观察到什么。**原始报错信息原文粘贴**，波形异常要描述清楚哪根信号、什么时刻、什么电平 |
| **定位过程** | ⭐ **最重要**。怎么一步步缩小范围的？做了哪些实验？排除了哪些可能？ |
| **根本原因** | 真正的根因，不是表面症状 |
| **修复方法** | 具体改了什么文件、哪一行 |
| **预防措施** | 以后怎么避免同类问题 |
| **状态** | 已解决 / 未解决 / 已规避 |

---

## 问题索引

| 编号 | 日期 | 标题 | 涉及模块 | 状态 |
|---|---|---|---|---|
| [ERR-0001](#err-0001) | 2026-09-17 | 实体名 `exp` 与 Quartus 原语冲突导致综合失败 | 工具链 | 已解决 |
| [ERR-0002](#err-0002) | 2026-09-17 | 内置仿真器无 Tcl 接口、强制要求 `.vwf` 激励，仿真流程一度无法自动化 | 工具链 / 全部模块 | 已解决 |
| [ERR-0003](#err-0003) | 2026-09-17 | 引脚"位序"与板上"网络名"混淆，差点抄到镜像的点阵列序 | `dot_matrix_scan` / `quartus/puzzle.qsf` | 已规避 |
| [ERR-0004](#err-0004) | 2026-09-17 | SVG 的两处格式陷阱：XML 注释含 `--`、presentation attribute 被 CSS 类覆盖 | `docs/图/系统图.html` | 已解决 |
| [ERR-0005](#err-0005) | 2026-09-17 | 校验脚本用硬编码类名白名单，图表改版后反过来误报 | `scripts/check_svg.py` / `docs/图/系统图.html` | 已解决 |
| [ERR-0006](#err-0006) | 2026-09-17 | 第一层框图新画的走线横穿两个方框，且标签压框 | `docs/图/系统图.html` | 已解决 |
| [ERR-0007](#err-0007) | 2026-09-17 | 走线穿框检查把 `fill:none` 的虚线边界框也算作障碍物，产生误报 | `scripts/check_svg.py` / `docs/图/系统图.html` | 已解决 |
| [ERR-0008](#err-0008) | 2026-09-17 | 定稿图上有三处标签压在走线上，而所有校验脚本都是绿的 | `docs/图/系统图.html` | 已解决 |
| [ERR-0009](#err-0009) | 2026-09-17 | `sw7 系统开关` 标签压过容器边界 8 个单位（清单主动抓到） | `docs/图/系统图.html` | 已解决 |
| [ERR-0010](#err-0010) | 2026-09-17 | `docs/01` §5 漏了 `buzzer_ctrl` 的接口定义（框图有框、接口表无表） | `docs/01-系统设计.md` §5 / `buzzer_ctrl` | 已解决 |
| [ERR-0011](#err-0011) | 2026-09-17 | **5 处输入端口没有驱动源** —— 按模块翻永远查不出"缺线" | `puzzle_ctrl` / `piece_rom` / `buzzer_ctrl` / `game_fsm` / `pattern_rom` | 已解决 |
| [ERR-0012](#err-0012) | 2026-09-17 | `docs/02` 6 处代码片段照抄**编译不过**（未定义符号 + 运算符优先级 + VHDL-93 越界） | `docs/02-模块详细设计.md` §10.5/§10.7/§10.8 | 已解决 |
| [ERR-0013](#err-0013) | 2026-09-17 | DWG-02 漏画 3 条连线，`key_press` 标签压掉一个字符 | `docs/图/系统图.html` 的 DWG-01 / DWG-02 | 已解决 |
| [ERR-0014](#err-0014) | 2026-09-17 | `docs/01` 内部 **14 处不一致** —— 单看每节都自洽，交叉引用才暴露 | `docs/01-系统设计.md` | 已解决 |
| [ERR-0015](#err-0015) | 2026-09-17 | 无头 Chrome 截图的两个陷阱：默认暗色方案、CJK 排版错乱 | `.tmp/shot.py` / `.tmp/fullpage.py` | 已解决 |
| [ERR-0016](#err-0016) | 2026-09-18 | `docs/02` §13.4 音效骨架：半周期计数 off-by-one（装载 N 得 N+1 拍）+ 两张查表只有名字没有定义 | `docs/02-模块详细设计.md` §13 / `buzzer_ctrl` | 已解决 |
| [ERR-0017](#err-0017) | 2026-09-18 | 课件合规改到一半就停：**引用悬空**（引 ERR-0018 而它不存在）+ 跨文件自相矛盾 + 接口真值源没动 | `CLAUDE.md` / `docs/01` / `docs/02` / `docs/图` / `ERRORS.md` / `AI_LOG.md` | 已解决 |
| [ERR-0018](#err-0018) | 2026-09-18 | 复位按"文档内部一致性"被删成两源，**违反课件 p45（一）"复位必须用按键"**；后又一度判定"复位无需消抖" | `clk_gen` / `docs/01` §2.1.3 / `docs/02` §3 | 已解决 |
| [ERR-0019](#err-0019) | 2026-09-18 | DWG-01 时钟椭圆副标题**溢出边框**（校验脚本全绿，放大 2.6 倍才现形） | `docs/图/系统图.html` 的 DWG-01 | 已解决 |
| [ERR-0020](#err-0020) | 2026-09-18 | **进代码前审查查出 12 处已验证的严重缺陷** —— 前四根轴结构上看不见的一类：**每处单看都对，合起来跑是错的**（清单见 `docs/审查报告-2026-09-18.md`） | 全项目（`docs/01`/`docs/02`/图/顶层模板） | **已解决**（16 条已复核全修完；80 条 MEDIUM/LOW 复核后 75 条已修） |
| [ERR-0021](#err-0021) | 2026-09-20 | **第 15 次会话跳过收工流程**：`HANDOFF.md` 表头与 `CHANGELOG.md` 双双留空 —— 「表头没更新」这个坑的**第二次复发** | `HANDOFF.md` / `PROGRESS.md` / `CHANGELOG.md` / `AI_LOG.md` | 已解决 |
| [ERR-0022](#err-0022) | 2026-09-20 | 精简 `CLAUDE.md` 前报"减到 ~800 行"，实际只到 **1009** —— **数量可数，我却估了** | `CLAUDE.md` | 已解决 |
| [ERR-0023](#err-0023) | 2026-09-20 | 白板框图留档稿与定稿口径有 3 处冲突，照它出图会与文档自相矛盾 | `docs/01` / 框图 | 已解决 |
| [ERR-0024](#err-0024) | 2026-09-20 | 文档转换链丢失全部图表题注、图片按自然像素插入 | `report/初步设计方案.docx` | 已解决 |
| [ERR-0025](#err-0025) | 2026-09-21 | 审查报告把本机绝对路径写进了入库文档 | 文档规范 / 推送流程 | 已解决 |
| [ERR-0026](#err-0026) | 2026-09-23 | **整机放不进 EPM1270**：3461 个 LE / 只有 1270 个 —— 4 路并行平移逻辑过宽 | `puzzle_ctrl` / `disp_format` | 已解决（1226/1270） |
| [ERR-0027](#err-0027) | 2026-09-23 | **时序不收敛**：Fmax 38.4MHz < 50MHz —— 跨模块组合链没有中间寄存器 | `puzzle_ctrl` / `game_fsm` / `piece_rom` | 已解决（63.66MHz） |
| [ERR-0028](#err-0028) | 2026-09-23 | **Quartus 里中文注释全乱码**（编译却正常）—— 9.1 按 ANSI/GBK 读源文件，而文件是 UTF-8 | `rtl/` 全部 `.vhd` | 已解决（转 GBK + 可切换脚本） |
| [ERR-0029](#err-0029) | 2026-09-24 | **ERR-0023~0028 六条在问题索引表里缺失**（4 条被误追加到 ERR-0025 正文末尾）+ 多份过程文档状态停留在编码前 | `ERRORS.md` / `README.md` / `HANDOFF.md` / `PROGRESS.md` | 已解决 |
| [ERR-0030](#err-0030) | 2026-09-24 | **上板清单说"SW7=0 → LED 全灭"，实现里 `ld` 根本不受门控**（SW7=0 时停在阶段 1 = 只有 LD8 亮）；同一份清单还没交代"**只有 BTN0 接进自检**"。现场两条"异常"由此而来 | `docs/04` §7.1/§7.3 / `docs/01` §8.2 / `docs/06` §6.1 / `docs/05` / `board_test_top` | 已解决（**订文档，不改代码**） |
| [ERR-0031](#err-0031) | 2026-09-24 | **判据让人"看阶段 1"，却没给"怎么知道现在就是阶段 1"** —— 阶段 1 只占 36 秒里的 2 秒（≈5.6%），现场看到"16 个灯都在动"被当成异常；代码里有现成的阶段号读数（**最左数码管 DISP7**）却没被点名 | `docs/06` §6.1 / `docs/01` §8.2 / `docs/05` §1.3·§1.5 | 已解决（**订文档，不改代码**） |
| [ERR-0032](#err-0032) | 2026-09-24 | **「DISP7 = 阶段号」是过度概括**（`when others` 只接住阶段 1/2/3/4/8/9，**5/6/7 数码管另有内容**）；**阶段 6 的读数方向与文档措辞相反**（实物从左到右是 `8 7 6 5 4 3 2 1`） | `docs/06` §6.1 / `docs/01` §8.2 / `HANDOFF.md` §3 | 已解决（**订文档，不改代码**） |
| [ERR-0033](#err-0033) | 2026-09-24 | **按满 16 键后"进不去阶段 7"** —— `r_seen` 只在 `s_rst` 时清零，按满后恒为全 1 → 阶段 7 每圈都在 **0.25 秒**内退出（本应 20 秒） | `rtl/board_test_top.vhd`（`r_seen` 清零条件）/ `docs/01` §8.2 / `docs/05` §1.6 | **已定位 + 已给规避法**（复位即恢复）；**代码修复有意推迟**（只影响自检工具，且板上现跑整机） |
| [ERR-0034](#err-0034) | 2026-09-24 | **「看不到开机自检」** —— 拨 SW7=1 后直接进待机，被当成"要求 1 没实现"。① 自检**只持续 2 秒**（**要求 2 明文**规定），现场判读表里没写这一条；② 第二次回报（按住 BTN0 松开也一样）推翻了"只是错过窗口"；③ 测试 A「按住 BTN0 → 全灭」= 复位通路确认正常 → "跳过自检"被排除 | `docs/06` §4.2·§6.1 / `docs/05` §1.6 / `rtl/clk_gen`·`game_fsm`·`disp_format` | ✅ **已解决** —— 真因是 **ERR-0035**（幻影「开始」把系统踢出自检态），不是自检缺失 |
| [ERR-0035](#err-0035) | 2026-09-24 | ⭐ **键盘扫描极性反了 → 幻影「开始」按键把系统踢出自检态** —— 行无键时就一直读到 `'1'` → 每相误报按键 → 幻影键号 = `phase+1`，整轮锁存 **4 = 开始** → 触发全局边直接进 `S_PREVIEW`（表现为"没有 2 秒自检、直接 5 秒计时 + 点阵亮起"）。`docs/04` §3.3 早记着这处"PDF 低有效 vs 手册高有效"的两源矛盾，**而唯一能验它的阶段 7 被跳过了** | `rtl/keypad_scan.vhd`（**已改**：新增 `KP_ACTIVE` 常量、列驱动改低有效）/ `docs/04` §3.3 / `docs/06` §6.1 / `scripts/encoding.py` | ✅ **已验证**（整机三步全通：2 秒自检 → 待机 → 按「开始」进预览） |
| [ERR-0038](#err-0038) | 2026-09-24 | **`clk_gen` 仿真抓出两处文档与实现不符** —— ① §3.6-1 写「前 10ms」实为 **9.156 ms**（`= T_POR_MS-1` 的 off-by-one，同 ERR-0016 一族）；② §3.6-9 写「`o_rst` 恒为 '1'」不成立 —— POR(9.16ms) 短于消抖(20.09ms)，中间 ~10.9ms 是 `o_rst`='0'。**两处上板都看不出来，只有仿真能抓** | `docs/02` §3.6-1·§3.6-9（已订正）/ `rtl/puzzle_pkg.vhd` 注释（待办） | ✅ 文档已订正 |
| [ERR-0037](#err-0037) | 2026-09-24 | ⚠️ **`set_top.py` 的正则把 16 行 `ld` 引脚约束截断了** —— 正则只匹配到 `-to ld[`，写回时却用它当整行 → 每切一次顶层就吃掉一个 `n]`；截断后的非法节点名又被 Quartus `project_close` 整行丢弃 → 16 行只剩 1 行，**引脚总数 68→53**（15 个 LED 没绑引脚）。旧 `check` 只数行数，**数不出截断** | `scripts/set_top.py`（**已修**）/ `quartus/puzzle.qsf`（**已修**）/ `docs/04` §2 | **已修复并重编译烧录**（68 引脚、0 errors） |
| [ERR-0036](#err-0036) | 2026-09-24 | **切顶层前没说清"板子会变成什么样"** —— 为验位序切到 `board_test_top` 后，现场把**9 阶段轮播**当成"被破坏了"（"没有自检、都乱闪烁"）。**板子完全正常，是板上跑的程序被换掉了** | `docs/06` §5 / `HANDOFF.md` §3 / `scripts/set_top.py` / `docs/05` §1.6.4 | 已解决（**订流程与文档，代码未改**） |

---

## ERR-0001

**实体名 `exp` 与 Quartus 原语冲突导致综合失败**

- **日期**：2026-09-17
- **阶段**：综合（搭建仿真验证环境时的最小实验工程）
- **涉及模块**：工具链
- **状态**：已解决

### 现象

为验证仿真流程，建了一个最小工程，顶层实体取名 `exp`。执行
`quartus_map simtest --generate_functional_sim_netlist` 时报：

```
Warning: Entity "exp" will be ignored because it conflicts with Quartus II primitive name
Info:    Found 2 design units, including 1 entities, in source file exp.vhd
    Info:    Found design unit 1: exp-rtl
Warning: Entity "exp" will be ignored because it conflicts with Quartus II primitive name
Error:   Top-level design entity "exp" is undefined
Error:   Quartus II Functional Simulation Netlist Generation was unsuccessful. 1 error, 2 warnings
```

### 定位过程

1. **第一反应是路径/工程文件问题** —— 因为报错是 "Top-level design entity is undefined"，
   听起来像是 `TOP_LEVEL_ENTITY` 写错或文件没加进工程。
2. 核对 `.qsf`：`set_global_assignment -name TOP_LEVEL_ENTITY exp` 与实体名一致；
   `VHDL_FILE exp.vhd` 也在。**排除配置错误**。
3. **注意到了一个"多余"的信息**：`Found design unit 1: exp-rtl` —— 说明 VHDL 文件
   **被成功解析了**，实体确实存在。那"undefined"就只可能是**被主动丢弃**了。
4. 回头看上面那两条 Warning：`conflicts with Quartus II primitive name`。
   **这才是根因，Error 只是它的后果。**
5. 验证：把实体名改成 `simtest`（同时改文件名、`TOP_LEVEL_ENTITY`、`.qsf` 里的
   `VHDL_FILE`），重新综合 → `0 errors, 0 warnings`。**确认根因**。

### 根本原因

`exp` 是 Quartus 内置的 LPM 原语名（指数运算 megafunction）。Quartus 的综合器
把用户实体匹配到原语库时**优先**认定它是原语，于是把用户这个实体忽略掉，
导致顶层实体找不到。

### 修复方法

- 文件：实验工程 `simtest.vhd`（临时实验，未进仓库）
- 改动：实体名 `exp` → `simtest`，并同步改文件名与 `.qsf` 中
  `TOP_LEVEL_ENTITY` / `VHDL_FILE`

### 预防措施

- **实体命名避开 Quartus 原语名**。高危名单：`exp`、`abs`、`add`、`sub`、`mult`、
  `div`、`lpm_*`、`alt*`、`mux`、`decode`。
- 本项目模块名（`puzzle_pkg` / `clk_gen` / `seg_scan` / `dot_matrix_scan` /
  `keypad_scan` / `pattern_rom` / `piece_rom` / `rng_lfsr` / `puzzle_ctrl` /
  `disp_format` / `game_fsm` / `buzzer_ctrl` / `puzzle_top` / `board_test_top`）
  **均已核对，无冲突**。
- **通用教训**：综合报错时，"Warning" 往往才是根因，"Error" 只是它的下游后果。
  排查时要**从第一条 Warning 读起**，而不是盯着最后的 Error。

---

## ERR-0002

**内置仿真器无 Tcl 接口、强制要求 `.vwf` 激励，仿真流程一度无法自动化**

- **日期**：2026-09-17
- **阶段**：搭建仿真验证环境
- **涉及模块**：工具链 / 全部模块
- **状态**：已解决

### 现象

本机没有 ModelSim / GHDL，只能用 Quartus II 9.1 内置仿真器。计划里写的是
"脚本生成 `.vwf` → 命令行跑仿真 → 脚本解析结果"，但在实施时连续撞墙：

```
Error: No valid vector source file specified and default file
       "...\simtest.cvwf" does not exist
```

即使指定了 `--simulation_results_format=VCD`，仿真"成功"后也**不产生任何 VCD 文件**；
`.sim.rpt` 里关于波形的部分只有一句：

```
Waveform report data cannot be output to ASCII.
Please use Quartus II to view the waveform report data.
```

### 定位过程

1. **先假设有 Tcl 接口**。查 `quartus_sim --help`，发现支持 `-t <script file>`，
   于是写脚本 `package require ::quartus::simulator`。
   结果：**包能加载成功**，但 `info commands simulator_*` 返回空列表。
2. **换 `quartus_sh` 再试**，对比 `package require` 前后的 `info commands`，
   新增的只有 `pkg_mkIndex` / `tclPkgSetup` 之类 Tcl 自举命令。
   → **结论：9.1 的 `::quartus::simulator` 是个空壳包，没有可用的 Tcl API。**
3. **回到"必须提供向量源文件"这条路**。先尝试把格式从 Quartus 自带的帮助/示例里找出来：
   - 整个 Quartus 安装目录里 `find -iname "*.vwf"` → **零个样例文件**
   - 官方 VHDL 入门教程 PDF 只讲概念，不给格式
   - 从 `db_vdb.dll` 里提取字符串，找到 `vdb_vec_lexer.cpp` / `vdb_vec_parser.ypp` /
     "Pattern Section" 等线索 → 确认**存在文本格式解析器**，但**语法仍拼不出来**
4. **转折点：转换思路，去机器上找现成的 `.vwf`**。
   在用户桌面的 `VHDL\` 目录下发现 **9 个此前做过的 Quartus 工程**，
   每个都带 `.vwf` / `.pin` / `.sim.rpt` —— **这些是曾经在开发板上跑通过的**。
   读 `mux4/mux4.vwf` 后，`.vwf` 的文本格式**一次性完全确定**。
5. **验证格式**：按学到的格式手写一个 `.vwf` 喂给 `quartus_sim`，
   仿真成功（`0 errors`），并且发现关键行为——
   **加 `--overwrite_waveform=on` 后，仿真结果会被写回输入 `.vwf` 本身**，
   每个节点都变成可解析的 `TRANSITION_LIST`。

### 根本原因

三个独立的事实叠加，导致"照常规思路自动化"走不通：

1. Quartus II 9.1 的仿真器**故意不提供 Tcl API**（该功能是给 GUI 用的）；
2. 它的输入向量格式**在安装包里既无文档也无样例**；
3. 它的 ASCII 报告**从不包含波形数据**，结果只能通过回写 `.vwf` 拿到。

### 修复方法

- 文件：`scripts/vwf.py`（新增）
- 改动：把 `.vwf` 的读写封装成库——解析嵌套 `NODE`/`REPEAT`/`LEVEL` 结构、
  展开成时间序列、生成激励、保证 `DISPLAY_LINE` 自洽。
  `scripts/vwf.py` 自带自检：`python scripts/vwf.py` 打印 `自检通过 ✓`。
- 确定了可用的命令行：

```bash
quartus_map  <工程> --generate_functional_sim_netlist
quartus_sim  <工程> --mode=functional --overwrite_waveform=on
```

- 做**往返验证**：Python 生成激励 → 仿真 → 解析回写结果 → 与参考模型
  **逐点比对 100 个采样点**，全部通过。至此仿真流程**完全自动化且自校验**。

### 预防措施

- **`.vwf` 会被仿真结果覆盖** → 激励必须可重新生成，永远不手工编辑 `sim/*.vwf`。
- 不要再去找 VCD 输出（`--simulation_results_format=VCD` 实测不产生文件），
  也不用去 `.sim.rpt` 里找波形。
- **通用教训（这条最值钱）**：
  工具链卡住时，**先去本机找"已经跑通过的同类产物"**，比啃文档、逆向二进制快得多。
  这次如果在第 3 步之后继续死磕 `db_vdb.dll`，会白耗大量时间；
  而桌面上的 9 个旧工程一读就通。**"存在可工作的样例"本身就是最权威的格式文档。**
- 副产品：顺带确认了本项目引脚表与旧工程一致（见 ERR-0003）。

---

## ERR-0003

**引脚"位序"与板上"网络名"混淆，差点抄到镜像的点阵列序**

- **日期**：2026-09-17
- **阶段**：引脚约束编写 / 交叉验证
- **涉及模块**：`dot_matrix_scan`、`quartus/puzzle.qsf`
- **状态**：已规避（未造成实际损失）

### 现象

编写 `quartus/puzzle.qsf` 的点阵列引脚时，用桌面上已跑通的旧工程
`VHDL/dot_matrix_pwm/dot_matrix_pwm.qsf` 做交叉验证，发现**两者相反**：

| 信号 | 本项目 `puzzle.qsf` | 旧工程 `dot_matrix_pwm.qsf` |
|---|---|---|
| 红色列 第 0 位 | `PIN_22` | `PIN_11` |
| 红色列 第 7 位 | `PIN_11` | `PIN_22` |

若照着旧工程抄，点阵图案会**左右镜像**——而且因为图案本身大致对称时不易察觉，
很可能拖到上板验收才被发现。

### 定位过程

1. 先怀疑**手册读错了**。回到开发板手册原文重新提取，得到：

   > `COLR0~COLR7 依次使用 EPM1270T144C5 芯片的 22、21、16、15、14、13、12 和 11 脚`

   → 手册明确：**COLR0 = 22，COLR7 = 11**。本项目写法与手册一致。

2. 那旧工程为什么反着写还能跑？去读它的扫描代码，确认它的内部信号 `col_r`
   是**独立命名**的，与板上 `COLRn` 丝印没有绑定关系——它的扫描逻辑按自己的
   `col_r` 顺序写，所以自洽可用。

3. **关键判断**：不能因为"旧工程能跑"就认定"旧工程的位序是权威的"。
   权威的只有**手册里的物理网络名**。旧工程只能用来确认
   "这块板子用到了哪些引脚"（这一项它完全对上了：行 8~1、绿列 38~45 与本项目**逐条一致**），
   但不能用来确定位序。

4. 顺带用同一方法核对了**段码、位选、LED**：

   | 组 | 旧工程（多个工程一致） | 本项目 | 结论 |
   |---|---|---|---|
   | 点阵行 ROW0~7 | 8,7,6,5,4,3,2,1 | 同 | ✅ 一致 |
   | 点阵绿列 COLG0~7 | 38,39,40,41,42,43,44,45 | 同 | ✅ 一致 |
   | 数码管段 | 62=AA,59=AB,…,52=AG,51=AP | 同 | ✅ 一致 |
   | 数码管位选 CAT0~7 | 63,66,67,68,69,70,30,31 | 同 | ✅ 一致 |
   | LED LD0~15 | 80,79,…,73,144,143,…,137 | 同 | ✅ 一致 |
   | 全局时钟 | 18 | 同 | ✅ 一致 |

   六组里五组完全一致，**只有红列一组是旧工程反着写的** —— 这反而印证了
   "旧工程的差异是它自己的命名习惯，不是手册读错"。

### 根本原因

**"信号的第 n 位"和"板上丝印的第 n 号网络"是两个独立的东西。**
旧工程把 `col_r[0]` 接到了物理上的 `COLR7`，只要它的扫描代码按 `col_r` 的顺序写，
功能就正确——但**引脚表本身已经不再对应手册的命名**。
直接复用这样的引脚表，就等于悄悄引入了一次位序反转。

### 修复方法

- 文件：`quartus/puzzle.qsf`（保持不变，它本来就是对的）
- 改动：**无**。本次是**验证**，不是修复。
- 追加防护：在 `CLAUDE.md` §4.2 增加"引脚号 vs 位序"小节，把对照表和这个坑写死。

### 预防措施

- **引脚号只从开发板手册拿。** 旧工程/网上代码只能用来交叉验证
  "用到了哪些引脚"，**不能用来确定位序**。
- 位序约定必须**全项目单点定义**（本项目写在 `rtl/puzzle_pkg.vhd` 与
  `CLAUDE.md` §4.2），不允许各模块各自解释。
- 上板前用 `board_test_top` 自检工程**逐列点亮**，直接肉眼确认
  `dot_colr(0)` 对应的是不是最左列——这是唯一能一锤定音的验证。

---

## ERR-0004

**SVG 的两处格式陷阱：XML 注释含 `--`、presentation attribute 被 CSS 类覆盖**

- **日期**：2026-09-17
- **阶段**：文档（绘制总体框图与状态转移图）
- **涉及模块**：`docs/图/系统图.html`（交付给 draw.io 二次编辑的图表）
- **状态**：已解决

### 现象

在浏览器里打开图表 HTML，**肉眼看着完全正常**，两张图都渲染出来了。
但这个文件的下游用途是**导入 draw.io 继续编辑**，于是用 XML 解析器校验内联 SVG，
暴露出两个问题——它们在浏览器里都看不出来：

```
#1 !! XML 错误：not well-formed (invalid token): line 11, column 15
```

以及状态图上：`S_SELF_TEST` / `S_IDLE` / `S_WIN` / `S_FAIL` 四个状态框
本该分别是灰、灰、绿、红，实际**全部渲染成同一种灰色边框**；
`S_PLAYING → S_WIN`（绿）和 `S_PLAYING → S_FAIL`（红）两条迁移箭头也**都是黑的**。

### 定位过程

**问题一（XML 报错）**：报错行指向 `<!-- ---------- 外部输入 ---------- -->`。
这行在浏览器里毫无异常，一开始怀疑是裸 `&` 未转义，专门写脚本全文扫了一遍
`&(?!(amp|lt|gt|quot|apos|#\d+);)`，**一个都没有**。

回头看报错行本身才发现问题不在内容而在**注释本身**：
XML 规范规定注释体内部**不得出现连续两个连字符 `--`**，因为 `--` 是注释的结束标记。
我用长横线做视觉分隔（`----------`），正好踩中。
HTML5 的解析器对这条很宽容，所以浏览器不报错；draw.io 用的是严格 XML 解析器，会直接拒绝。

**问题二（颜色丢失）**：核查 CSS 时发现 `.dg-box { stroke: var(--paper-rule); }`
这类类规则**会覆盖**元素上的 `stroke="#9E332A"` 属性。
这是 CSS 层叠的基本规则：**presentation attribute 的优先级低于任何作者样式表规则**
（等价于特异性为 0），所以只要类里写了 `stroke`，行内属性就永远不生效。

### 根本原因

两个问题同源：**把"浏览器里能看"当成了"格式正确"。**

- 注释的合法性是 **XML 层面**的约束，与渲染结果无关；
- 颜色的归属是 **CSS 层叠**层面的规则，而 SVG 的属性写法在这里恰好是个陷阱
  （`stroke="..."` 看着像行内样式，实际是特异性最低的 presentation attribute）。

更深一层：**没有验证下游工具能不能吃这个文件。** 浏览器是宽容的解析器，
它渲染成功不能证明文件合法。

### 修复方法

1. 全文 22 处注释里的 `-` 替换为 `·`，SVG 内不再出现任何 `--`；
2. 状态框与两条彩色箭头改用内联 `style="stroke:#9E332A;fill:#FBEDEA"` —— 
   内联 `style` 的优先级高于类规则，能压住它；
3. 补了一个校验脚本，一次性检查三件事：**XML 是否合法**、
   **每个绘制形状是否都有显式填充**（漏了 `fill` 会渲染成黑色实心块）、
   **是否还有会被类覆盖的 `stroke` 属性**。

### 预防措施

- **凡是生成给别的工具消费的文件，必须在那个工具的规则下验证，而不是在浏览器里目测。**
  HTML/SVG 的"能看"和"合法"是两件事。
- **SVG 里给单个元素上色一律用内联 `style`**，不要用 `stroke=` / `fill=` 属性，
  除非确认没有任何 CSS 类会碰这两个属性。
- 图表交付前跑一次 `scripts/check_svg.py`（三合一校验）。

---

## ERR-0005

**校验脚本用硬编码类名白名单，图表改版后反过来误报**

- **日期**：2026-09-17
- **阶段**：文档（按"自顶向下分层"改版总体框图）
- **涉及模块**：`scripts/check_svg.py`、`docs/图/系统图.html`
- **状态**：已解决

### 现象

按"总体框图要分层"的意见把 `系统图.html` 改成两层结构，新增了两种连线样式
`.dg-rail`（节拍总线）和 `.dg-tap`（总线分接），两者都在样式表里明确写了
`fill:none`。改完跑校验脚本，却报：

```
=== docs/图/系统图.html
    内嵌 SVG 3 张，绘制形状 52 个
    ✗ SVG #1 的 <path> 没有显式填充（class=dg-rail）→ 会渲染成黑色实心块
    ✗ SVG #1 的 <path> 没有显式填充（class=dg-tap）→ 会渲染成黑色实心块
```

这两条是**假警报**：`fill:none` 明明写了，而且浏览器渲染出来的图完全正常。

### 定位过程

1. **先怀疑样式表没生效**。去 `系统图.html` 的 `<style>` 块里核对，
   `.dg-rail{stroke:var(--c-time);stroke-width:2.4;fill:none}` 写得没错，
   属性名拼写、分号、花括号都正常。**排除样式表本身的问题**。
2. **转而去看脚本是怎么判"有没有填充"的**。读 `check_svg.py` 发现，
   它不是去读样式表，而是**比对两张硬编码的类名清单**：

   ```python
   FILL_CLASSES = {"dg-box", "dg-time", "dg-driver", ...}    # 有填充的类
   STROKE_ONLY_CLASSES = {"dg-wire", "wire", "link", ...}    # 只描边的类
   has_fill = ("fill" in a) or ("fill" in style) \
       or bool(cls & FILL_CLASSES) or bool(cls & STROKE_ONLY_CLASSES)
   ```

   新加的 `dg-rail` / `dg-tap` **不在任何一张清单里**，于是 `has_fill` 为假。
3. **关键判断**：这不是"漏登记一个类名"的问题，而是**判据选错了**。
   样式表才是"哪个类有没有 fill"的**唯一真值源**，脚本却另外维护了一份副本。
   副本必然滞后——**今天补上 `dg-rail`，下次加新类还会再犯一次**。

### 根本原因

**校验规则与它要校验的对象各维护了一份事实。**
脚本的类名清单是 `系统图.html` 样式表的**手工镜像**，两者没有同步机制。
图表结构一改（本次是为分层而改），镜像就失效，脚本于是从"守门员"变成"报假警"。

> 这和 ERR-0003（引脚位序）是**同一类错误**：把某份副本当成了权威，
> 而真正的权威另有其物。ERR-0003 里副本是旧工程的引脚表，
> 这里副本是脚本的类名白名单。

### 修复方法

- 文件：`scripts/check_svg.py`
- 改动：**删掉两张硬编码清单**，新增 `parse_css_classes()`，
  直接从 `<style>` 块解析出"每个类声明了哪些属性"，再据此判定：

  ```python
  declared = set()
  for c in cls:                    # 元素身上的 class
      declared |= css.get(c, set())
  declared |= {行内 style 里声明的属性}
  if "fill" in declared or "fill" in a:   # ← 判据改成"实际声明过 fill"
      ok
  ```

  - 只取**主体选择器**（逗号分组里最后一段复合选择器）的类名：
    `.canvas svg{width:100%}` 的宽度是加在 `svg` 上的，不能算到 `.canvas` 头上；
  - 顺带补了两个新检查：`marker-end` 引用了不存在的 `id`、同页多张 SVG 的 `id` 重名
    （后者是本页有三张图之后才出现的风险）。

- **重做负向测试**（改动大，不能凭"原来测过"就放过），注入 5 类故障：

  | 注入的故障 | 期望 | 实测 |
  |---|---|---|
  | 注释含 `--` | 报 XML 不合法 | ✅ 检出（且额外点名了具体行） |
  | 有 `stroke` 无 `fill` | 报缺少填充 | ✅ 检出 |
  | 类里有 `fill` 又写了 `fill=` 属性 | 报属性覆盖 | ✅ 检出 |
  | `marker-end` 指向不存在的 id | 报引用错误 | ✅ 检出 |
  | 两张图用了同名 id | 报 id 冲突 | ✅ 检出 |
  | **`.canvas svg{width}` + 元素上 `fill=` 属性** | **不应误报** | ✅ 未误报（主体选择器逻辑正确） |

  最后一行是**反例测试**：只测"该报的报了"不够，还要测"不该报的没报"，
  否则很容易把校验器改成"什么都报"，看起来灵敏、实际没用。

### 预防措施

- **校验规则的判据要从被校验对象本身推导，不要在旁边维护一份副本。**
  写校验脚本时先问一句：这份判据会不会随着被校验内容的正常演进而过期？
- **改动校验脚本后必须重跑负向测试**，且负向测试要**同时包含正例（应报）和反例（不应报）**。
- 脚本里加了一行自检：若一个类都没解析到、但文件确实用了 `class`，
  就打印警告——**校验器要能报告"我可能失效了"**，而不是沉默地一律放行。

---

## ERR-0006

**第一层框图新画的走线横穿两个方框，且标签压框**

- **日期**：2026-09-17
- **阶段**：文档（新画 DWG-01 第一层总体框图）
- **涉及模块**：`docs/图/系统图.html`
- **状态**：已解决

### 现象

新画的 DWG-01（第一层功能子系统框图）里，`游戏控制` → `音效输出` 的 `sound_sel` 走线
原本写成：

```html
<path d="M530,307 H1230 V470" class="dg-wire" marker-end="url(#b1)"/>
```

即"从 `游戏控制` 右边缘出发，在 y=307 处一路向右到 x=1230，再向下拐进 `音效输出`"。

**问题**：第一层方框的 y 范围是 **240~328**，而这条横线在 **y=307**——
正好落在框内。横线自 x=530 拉到 x=1230，于是**从「拼图核心」和「显示子系统」两个方框内部穿了过去**。

同一张图上还有第二处：`pattern_sel` 标签写在 `(578, 286)`，
而「拼图核心」方框的左边框在 **x=610**——标签文字向右延伸约 73px，
**压到了方框的左边缘上**，并且和 `控制` 那条箭头的箭头尖叠在一起，字被切成了 `pat▸rn_sel`。

### 定位过程

1. **校验脚本全绿，所以第一轮是靠"看图"发现的。**
   `scripts/check_svg.py` 此时只查 XML 合法性、显式填充、属性覆盖三类问题——
   这三类**都通过**。也就是说：**图的格式完全正确，但图是错的。**
2. 用无头 Chrome 渲染成 PNG，先看整图，觉得"大致没问题"；
   **改成局部放大后才看出来**：把 y 565~650 一段放大 3 倍，两个方框中间横着一条线，
   旁边 `pattern_sel` 被箭头穿过。**整图缩略时这两处都不明显**——这是本次能发现问题的关键动作。
3. 回到 SVG 源码，把方框的 `y=240 height=88`（即 240~328）与走线的 `y=307` 一对，
   立刻确认穿框；把标签的 `x=578` 与方框的 `x=610`、以及"11 个字符 × 6.6px ≈ 73px"
   一算，确认压框。

### 根本原因

**手工布线时，只在"起点和终点"上做判断，没有对"整条线段经过哪些区域"做检查。**

`M530,307 ...` 里，起点 (530,307) 落在 `游戏控制` 右边缘外侧、终点 (1230,470) 落在
`音效输出` 上方——**两端看着都没问题**，于是写下这条路径。
但中间那段 `H1230` 要横跨 700 个单位，**穿过了两个方框**。

标签同理：写下 `(578,286)` 时想的是"放在这条线附近"，
没有把**文字的宽度**算进去，也没有意识到 y=286 与 y=284 的箭头线只差 2 个单位。

更深一层：**这类错误不是"格式错误"，任何格式校验都查不出来。**
ERR-0004 解决的是"文件合不合法"，本次是"图画得对不对"——**两个不同的问题**。

### 修复方法

- 文件：`docs/图/系统图.html`（DWG-01）
- 改动一 —— `sound_sel` 改走**最下方的空走廊**：

  ```html
  <!-- 改前：y=307 横穿两个方框 -->
  <path d="M530,307 H1230 V470" class="dg-wire" marker-end="url(#b1)"/>

  <!-- 改后：从「游戏控制」下边缘出发，绕到 y=730 再右行 -->
  <path d="M500,328 V730 H1180 V558" class="dg-wire" marker-end="url(#b1)"/>
  <text x="1170" y="724" class="dg-lbl" text-anchor="end">sound_sel</text>
  ```

  选 y=730 而非复用已有的 y=690：y=690 已被 `state·level·cnt` 占用，
  **两条线重合会比穿框更难读**。改后两条走廊上下嵌套、互不相交。

- 改动二 —— `pattern_sel` 标签从 `(578,286)` 移到它自己那条线的**水平段上方** `(574,424)`。

- 改动三 —— **给校验脚本补上第 ④ 类检查：走线是否横穿方框。**
  新增 `_segments()`（把 `<line>` 与轴对齐的 `<path>` 拆成线段）与 `_crosses()`
  （线段是否穿过矩形**内部**）。两个关键设计：
  - **内缩 1 个单位再判定**——否则"箭头正好落在方框边上"这种**正确画法**会被误报；
  - 只判定**轴对齐**线段，斜线一律跳过——避免在无法可靠判定时给出假警报。

- **负向测试**（新增检查必须验证）：

  | 注入的故障 | 期望 | 实测 |
  |---|---|---|
  | 水平线从方框中间穿过 | 报警 | ✅ 检出 |
  | 垂直线从方框中间穿过 | 报警 | ✅ 检出 |
  | **箭头正好落在方框边上** | **不应报警** | ✅ 未误报 |
  | 线从方框外侧经过 | 不应报警 | ✅ 未误报 |

### 预防措施

- **画完图必须局部放大看，不能只看整图缩略。** 本次两处缺陷在缩略图上都不明显，
  都是放大 3 倍后才看清。**"整图看着没问题"是本次最危险的信号**——
  它让人以为已经检查过了。
- **判断走线是否合法，要看整条线段，不能只看两端。** 具体做法：
  写下一条横线/竖线前，先列出它的坐标区间，再与沿线所有方框的坐标区间**求交**。
  本次的 y=307 与方框的 240~328 区间相交，一眼可判。
- **文字标注要把宽度算进去**（中英文混排时按"字符数 × 字号 × 0.6"估）。
  只钉住起点坐标是不够的。
- **校验脚本已能自动查穿框**（第 ④ 类检查），此后新画的线不用再靠肉眼。
  但**标签压框仍查不出来**——需要字体度量，暂未实现，这一项仍要人看。
  **验证器查不到的，要明确知道它查不到，而不是以为它全查了。**

---

## ERR-0007

**刚加的"走线横穿方框"检查，反过来把每一条合法的外部连线都报成了问题**

- **日期**：2026-09-17
- **阶段**：文档（新增第 ④ 类检查后立即自查）
- **涉及模块**：`scripts/check_svg.py`、`docs/图/系统图.html`
- **状态**：已解决

### 现象

ERR-0006 修完之后，给 `check_svg.py` 补上第 ④ 类检查「走线是否横穿方框」，
对新版 DWG-01 立刻跑了一遍，结果**每一条外部连线都被判为穿框**：

```
✗ SVG #1 的 <line> class="dg-wire"> 线段 (90,462)→(170,462) 横穿方框 [dg-region] (150,190 1450×930) —— 连线必须绕开方框
✗ SVG #1 的 <line> class="dg-wire"> 线段 (170,512)→(90,512) 横穿方框 [dg-region] (150,190 1450×930) —— 连线必须绕开方框
✗ SVG #1 的 <line> class="dg-wire"> 线段 (60,245)→(803,245) 横穿方框 [dg-region] (150,190 1450×930) —— 连线必须绕开方框
… 共 6 条
```

被指控的六条线是 `kp_row` / `kp_col` / `sw7` / `clk` / 数码管点阵输出 / 蜂鸣器输出——
**全部是正确画法**：外部信号本来就该从图外穿进顶层的边界框。

### 定位过程

1. 先看被指控的对象：方框是 `dg-region`，即那条 `x=150 y=190 1450×930` 的**虚线边界框**，
   标注写的是"`puzzle_top` 内部 · 7 个功能子系统"。
2. 于是问题立刻明确：**这条框表达的是"范围"，不是"障碍物"。**
   连线穿过它不是缺陷，而是**图的语义要求**——不穿过去，外部信号根本进不来。
3. 反查第 ④ 类检查的判定代码，发现它把所有 `width ≥ 50 且 height ≥ 30` 的 `<rect>`
   一律当障碍物，**根本没看填充**。
4. 对照已有的 `.dg-region` 样式，确认它是 `fill:none`——
   **"有填充的方框才是实体物件，`fill:none` 的是范围"**，这个区分在第 ④ 类检查里漏掉了。

### 根本原因

**新写的检查沿用了"尺寸阈值"当判据，而没有用"图上这个矩形代表什么"当判据。**

第 ④ 类检查的原始意图是"连线不许从**模块方框**里穿过去"。
落到代码里却写成了"连线不许从**任何够大的矩形**里穿过去"——
**判据比意图宽，于是把合法的边界框也算了进去。**

这和 ERR-0005（`check_svg.py` 用硬编码类名白名单）**是同一个病的两种表现**：
ERR-0005 是"判据手工维护、和真实来源脱节"，
本次是"判据的来源（尺寸）不是真正决定性的属性（填充语义）"。
两次都说明同一件事：**写检查时，判据必须取自真正决定行为的那个属性。**

### 修复方法

- 文件：`scripts/check_svg.py`
- 新增 `_fill_of(el, css)`：解析元素**最终生效的 `fill` 值**，
  优先级按 CSS 规则来 —— **行内 `style` > CSS 类 > `fill=` 表现属性**
  （表现属性特异性为 0，会被任何同名类规则压掉，这一点 ERR-0004 已经踩过）。
- 第 ④ 类检查改为**只把"有填充的 rect"当方框**：

  ```python
  fill = _fill_of(el, css)
  if fill == "none" or fill == "transparent":
      continue                 # 边界/区域框，允许被穿过
  boxes.append((x, y, w, h, el.get("class", "—")))
  ```

  **没有**改成"跳过 `dg-region` 类"——那又是加一张类名白名单，会重犯 ERR-0005。
  判据取自 `fill` 这个**决定语义的属性**：以后新加任何一种 `fill:none` 的范围框，
  不用改脚本就自动放行。

- **负向测试**（正例 + 反例都要做）：

  | 注入的用例 | 期望 | 实测 |
  |---|---|---|
  | 线从**有填充**的 `.box` 中间穿过 | 报警 | ✅ 检出 |
  | 线从 `fill:none` 的 `.region` 边界框穿过 | **不应报警** | ✅ 未误报 |

### 预防措施

- **检查项写完后，先拿"已知正确的现有文件"跑一遍。**
  本次正是拿新版 DWG-01 一跑就发现了——**如果只做负向测试（注入故障看能不能查出），
  这个误报反而漏得掉**，因为注入的故障确实被查出来了。
  **"能查出错误"和"不误报正确的东西"是两件事，必须分别验证。**
- **判据要取自"决定语义的那个属性"，不是尺寸、位置、名字这些代理特征。**
  尺寸会变、名字会改，语义属性（这里是 `fill`）才是稳定的。
  这是 ERR-0005 结论的一般化，已并入同一条经验。
- 脚本的文档字符串同步补上了这条说明，避免下次有人"顺手"把尺寸阈值收紧。

---

## ERR-0008

**定稿图上有三处标签压在走线上，而所有校验脚本都是绿的**

- **日期**：2026-09-17
- **阶段**：文档（DWG-01 定稿排版）
- **涉及模块**：`docs/图/系统图.html`
- **状态**：已解决

### 现象

DWG-01 按飞书白板布局重排后，`check_svg.py` **五类检查全绿**：

```
✓ XML 合法 · 填充完整 · 无属性覆盖 · id 无冲突 · 全部通过
```

但把渲染图**放大到 2.5 倍**逐块看时，发现三处文字与线条重叠：

| # | 重叠内容 | 症状 |
|---|---|---|
| 1 | `target_mask · rel_mask` 标签 × 一条竖直走线 | 标签右端（x 到 1237）撞上 x=1240 的竖线 |
| 2 | `rnd_step` 标签 × `pattern_sel` 那条竖线 | 标签横跨 955~1008，而竖线在 x=980，**从字中间穿过去** |
| 3 | `像素 / px_red · px_green` 标签 × 同一条 x=1240 竖线 | 标签居中在 1220，横跨 1160~1280 |

三处的共同点是：**文字被一条线从中划开，字符被切成两半**，读起来像乱码。

### 定位过程

1. 先确认不是格式问题：`check_svg.py` 全绿，且**该脚本的第 ④ 类检查恰好就是查"穿框"**，
   也没有报——说明这三处**既不是穿框，也不是格式错误**。
2. 把渲染图按区域裁切放大（上、中、下三段）逐段看。
   **整图缩略时三处都不明显**，和 ERR-0006 的情况完全一样。
3. 对每一处，把**标签的边界区间**和**附近所有竖线与横线的坐标**列出来求交：
   - 第 1 处：标签 `x=1092` 起向右约 145px → 1092~1237；竖线在 `x=1240` → 差 3px。
   - 第 2 处：标签 `text-anchor="end"` 于 `x=1008`，向左约 53px → 955~1008；竖线 `x=980` → **必穿**。
   - 第 3 处：标签居中于 `x=1215`，左右各约 60px → 1155~1275；竖线 `x=1240` → **必穿**。
4. 追到那条 `x=1240` 的竖线是什么——它是**「图案与随机数据」的节拍分接**。

### 根本原因

**表面的原因**是标签坐标没把文字宽度算进去（与 ERR-0006 同）。

**但真正的根因是那条节拍分接本身就不该存在**（详见 `docs/01` §2.1.1 第 3 处）：
`pattern_rom` / `piece_rom` 是纯组合常量表，`rng_lfsr` 由 `rnd_step` 请求推进——
**「图案与随机数据」不接任何节拍**。为了保持与手绘稿一致而把它留在图上，
结果就是**一条本不该有的线，在 1150~1290 这段本已拥挤的区域里又插了一脚**，
连带制造了两处标签重叠（第 1、3 处）。

第 2 处则是纯粹的排版失误：`rnd_step` 标签放在 `y=658`，
而它旁边那条 `pattern_sel` 竖线从 `y=620` 就开始了，**区间重叠**。

### 修复方法

- 文件：`docs/图/系统图.html`
- **改动一（治本）——删掉「图案与随机数据」的节拍分接。**
  这一条同时消掉了第 1、3 处重叠的肇事竖线，并且**在架构上也是正确的**
  （它接节拍正是 `CLAUDE.md` §10.1 那个"永远不随机"的坑）。
  在删掉的位置留了一条 XML 注释说明**为什么这里没有线**，防止以后有人"补"回来。
- **改动二 —— `rnd_step` 标签从 `y=658` 上移到 `y=576`**，
  移到那条 `pattern_sel` 竖线（起点 `y=620`）的上方，两区间不再相交。
- **改动三 —— `target_mask` 两个标签微调到 `y=600 / y=617`**，`像素` 标签中心从 `x=1220` 移到 `x=1215`，
  留出与箭头的间距。
- 改完**重新渲染并放大复核**，三处全部消除；上、中、下三段逐段确认无新重叠。

### 预防措施

- **"校验全绿"只说明格式对，不说明图对。** 这一条 ERR-0006 已经写过一次，
  **本次是它的复发**——说明当时只写进文档、没有固化成动作。
  现在固化为动作：**每张图定稿前，必须渲染并放大逐段看一遍**，
  且这条已写进 `CLAUDE.md` §12 的图表规范。
- **排版冲突要先问"这条线该不该存在"，再问"标签该挪到哪"。**
  本次如果只挪标签，那条多余的节拍线还是会留在图上，
  下一个人照着图实现就会踩 §10.1 的坑——**挪标签治标，删线才治本。**
- **标签避让要用区间求交，不能靠目测。** 具体做法：
  取标签的 `x`、`text-anchor` 与字符数估出宽度区间，
  再与附近所有竖线的 `x` 比对——**本次三处都是这样一分钟内确认的**。

---

## ERR-0009

**`sw7 系统开关` 标签压过容器边界 8 个单位 —— 这次是清单主动抓到的**

- **日期**：2026-09-17
- **阶段**：文档（DWG-01 飞书白板稿第 2 版同步）
- **涉及模块**：`docs/图/系统图.html`
- **状态**：已解决

### 现象

按用户第 2 版白板稿同步矢量图时，把时钟椭圆的三行文字与 SW7 输入线改完，
按 `CLAUDE.md` §12.4 的五步清单走：`check_svg.py` 五类检查**全绿**，
然后渲染 + 放大 2.5 倍逐段看，在左上角发现：

**`sw7 系统开关` 标签的右端压过了 `puzzle_top` 的虚线边界框。**

放大后测量：标签左对齐在 `x=70`，文字宽约 **79 单位**，
右端落到 `x≈158`；而容器左边界在 **`x=150`** —— **压过去约 8 个单位**。
渲染图上表现为「关」字正好骑在虚线上。

### 定位过程

1. `check_svg.py` **五类检查全绿**。这在意料之中：
   第 ④ 类检查只查"**线** vs **有填充的方框**"，
   而这里是"**文字** vs **无填充的虚线框**"，两个条件都不在它的判定范围内。
2. 按 §12.4 清单第 3 步，渲染整页后放大 2.5 倍、分条带裁切逐段看。
   在左上角条带里看到标签与虚线**挨在一起**。
3. **判定"挨着"还是"压住"，靠估宽不够** —— 于是又裁了一张 250×80 的像素级局部图
   （源区域 `x760..1010 y1340..1420`），看清楚了：
   **确实还有约 10 个渲染像素的缝，没有真重叠，但缩到 1× 就是挨着。**
4. 结论：**这是"看起来像贴着"的排版缺陷，不是几何重叠。**
   但读图的人不会去量，**视觉上贴着 = 图上确实有问题**，照样要修。

### 根本原因

**标签是左对齐的，而它的右边正好是容器边界 —— 文字向右生长，就长到界外去了。**

写 `<text x="70">sw7 系统开关</text>` 时，脑子里锚定的是"起点在 `x=70`"，
和 ERR-0006 / ERR-0008 是同一个病：**只钉起点，不算宽度**。
这一处比前两次更隐蔽的地方在于：**它压的不是别人，是边界**——
而边界是虚线、又是 `fill:none`，**`check_svg.py` 的第 ④ 类检查恰好刚刚被改成"放行 `fill:none`"**
（为了修 ERR-0007 的误报）。两次改动各自都对，合起来就留下了一个盲区。

> ⚠️ **这条值得单独记**：ERR-0007 把虚线框从"障碍物"里豁免掉是正确的
> （外部信号本来就该穿进去），**但豁免之后，"文字压边界"这类问题就再没有任何自动检查兜底了。**
> **修一个误报时，要顺带问一句"豁免掉的东西，还有没有别的问题需要盯"。**

### 修复方法

- 文件：`docs/图/系统图.html`
- 把 `sw7 系统开关` 从**左对齐**改为**右对齐到 `x=132`**：

  ```html
  <!-- 改前：左对齐，文字向右长到 x≈158，压过 x=150 的容器边界 -->
  <text x="70" y="257" class="dg-io">sw7 系统开关</text>

  <!-- 改后：右对齐，向左生长；距边界留 18 单位余量 -->
  <text x="132" y="257" class="dg-io" text-anchor="end">sw7 系统开关</text>
  ```

  **为什么选右对齐而不是"把 `x` 调小"**：`x=70` 已经离画布左边界不远，
  再往左只剩 70 单位的余量，而文字要 79 单位 —— **左对齐这条路根本走不通。**
  右对齐让文字**朝余量大的方向生长**（左侧还有 53 单位），这才是稳的解法。
- 改完**重新渲染并做像素级复核**，确认缝隙清楚可见。
- 顺带在 HTML 里留了注释说明为什么用右对齐，免得以后有人"顺手"改回左对齐。

### 预防措施

- ⭐ **这次的重点不在缺陷本身，而在"它是怎么被发现的"**：
  **是清单第 3 步主动抓到的，不是偶然看到的。**
  ERR-0006 是"缩略图看着没问题、放大才发现"，
  ERR-0008 是"同类问题第二次出现"，而这次 —— **照清单走，第一次放大就抓到了。**
  **说明把经验从"文档叙述"改成"固定动作"（`CLAUDE.md` §12.4）确实生效了。**
  这是本项目流程改进的一个可验证的收益，值得写进报告。
- **靠边界的标签一律右对齐（或居中），不要左对齐。** 判据：
  **文字应当朝"余量大的一侧"生长。** 写标签时先看它右边是什么 ——
  如果右边是边界、是方框、是别的线，就别用左对齐。
- **修一个误报时，要问"豁免掉的东西还有没有别的问题"。**
  本次 ERR-0007 把 `fill:none` 的边界框从障碍物里豁免了（正确），
  但也让"文字压边界"永远失去了自动检查 —— 这类**豁免造成的盲区**要显式记下来。
- **`CLAUDE.md` §12.4 的"查不到什么"表里补一行**：
  「标签压边界 / 压虚线框」也查不出，只能靠放大看。

---

## ERR-0010

**`docs/01` §5 接口定义表漏了 `buzzer_ctrl` —— "唯一真值源"缺了一整节**

- **日期**：2026-09-17
- **阶段**：文档（顶层改用 `component` 语法时核对端口）
- **涉及模块**：`docs/01-系统设计.md` §5、`buzzer_ctrl`
- **状态**：已解决

### 现象

用户要求顶层文件改用 `component` 语法例化。写 `CLAUDE.md` §10.3 的写法模板时，
按习惯写出了 `clk_gen` 的 `component` 声明 —— 回头核对 `docs/01` §5.1，
**发现端口名写错了**（凭印象写成 `i_clk` / `i_rst` / `o_por_rst`，
真实端口是 `clk` / `sys_en` / `o_rst`，也没有 `o_por_rst` 这个端口，
上电复位是内部信号、由 `o_rst` 一并输出）。

顺着这个错往下核，逐个模块对照 §5，才发现更严重的问题：

**`docs/01` §5 的接口定义表只写到 §5.10 `game_fsm`，根本没有 §5.11 ——
`buzzer_ctrl` 一个端口都没定义。**

### 定位过程

1. 写模板时凭印象写端口 → 自查时去 §5.1 核对 → **发现凭印象写的是错的**。
   这一步说明"凭印象写端口"本身就是病根。
2. 于是把 §5 的**小节编号**列出来核对完整性：
   `5.0 puzzle_pkg` / `5.1 clk_gen` / … / `5.10 game_fsm` / **没有 5.11**。
3. `grep -n "buzzer_ctrl" docs/01-系统设计.md` →
   命中 6 处（§1.1 子系统表、§1.2 分类表、§1.1.1 实体表、§2.1 框图、§3.1 模块清单、§7 数据流），
   **全部在 §5 之外** —— 确认 §5 里确实没有它。
4. 好消息是端口定义并非不存在，而是在 `docs/02-模块详细设计.md` §13.2 里，
   内容完整、可直接用。

### 根本原因

**§5 的"覆盖完整性"从来没有被检查过。**

`docs/01` §5 被 `CLAUDE.md` 与 `docs/02` 反复引用为"**接口的唯一真值源**"，
§10 第 1 条也要求"子系统所含实体之和 = 全部 11 个实体，不重不漏"——
**但那条要求是用在框图上的，没有同样施加到 §5 这张表上。**

于是出现了一个**结构性缺口**：11 个实体，§5 只写了 10 个。
漏的那个（`buzzer_ctrl`）恰好是**提高项**（音效）对应的模块，
在 §1.1 / §1.2 / §3.1 各处都露过脸，**唯独在接口表里没有位置**。

> ⚠️ **为什么这个缺口以前没被发现**：因为 §5 一直是"**往里查**"用的
> （想问某个模块的端口，就去翻它那一节）。**没人从外面数过它够不够 11 节。**
> 这正是"**叙述 vs 清单**"那条经验的又一个实例 ——
> "11 个实体不重不漏"写在 §10 第 1 条里是**叙述**，
> 它当时只被应用到了框图上，没有被变成一条**对每张清单都执行的核对动作**。

### 修复方法

- `docs/01` 新增 **§5.11 `buzzer_ctrl`**：端口表（照 `docs/02` §13.2 抄）
  + 指向 `docs/02` §13 的交叉引用，并注明本节是补写的、原因见本记录。
- `CLAUDE.md` §10.3 与 `docs/02` §14.1 的模板里，
  把凭印象写的 `clk_gen` 端口改为照 §5.1 抄的真实端口。
- `docs/02` §14.1 明确写一句：**不要在该节再抄一份端口表** ——
  §5 是唯一真值源，在这里抄一遍就是第三份副本。

### 预防措施

- ⭐ **凡是自称"唯一真值源"的表，必须能回答"它覆盖全了吗"，而且答案要能被数出来。**
  具体到本项目：**§5 的接口表节数必须 = 11 个实体 + `puzzle_pkg`**。
  写代码时每完成一个模块，回到 §5 把它那一节打个勾 —— 缺哪节一眼可见。
- **写端口、写常量、写引脚，一律不许凭印象，必须去真值源抄。**
  本次的病根起于"凭印象写 `clk_gen` 的端口" —— 侥幸的是这次旁边就是 §5.1，
  一核就露馅了。**若当时没去核，这个错的模板会一路带到 `puzzle_top.vhd` 里。**
- **`component` 写法把这个风险放大了**：端口要写两遍，
  而 `port map` 里漏写端口**不报错、静默悬空**（见 `CLAUDE.md` §10.3）。
  所以改用 `component` 的同时，必须接受"**抄完要核对**"这一条附加动作。

> **本次的正面意义**：这个缺口是被"**换一种写法**"逼出来的 ——
> 直接例化时顶层不需要写端口表，缺口可以一直不被触碰；
> 改成 `component` 之后，每个模块的端口都必须在顶层**显式出现一次**，
> 于是"少一个模块"这件事立刻变得可见。**形式改变让隐藏的缺口暴露了。**
> 这与 `CLAUDE.md` §12.4 的经验同源：**让正确的做法变成固定动作，错误才会自己浮上来。**

---

## ERR-0011

**从"线"这一侧数了一遍接口表，查出 5 处"输入端口没有任何驱动源"**

- **日期**：2026-09-17
- **阶段**：文档（阶段 1 封版前的交叉核对）
- **涉及模块**：`puzzle_ctrl`、`piece_rom`、`buzzer_ctrl`、`game_fsm`、`pattern_rom`
- **状态**：已解决

### 现象

上一轮（ERR-0010）把顶层例化方式改成 `component` 语法后，
本会话做了一次"照 `component` 声明去抄端口"的演练 ——
按 `CLAUDE.md` §10.3 的说法，**`port map` 漏写一根线不会报错，那根线会静默悬空**。

演练到 `buzzer_ctrl` 时对不上：它的 `i_trigger`（边沿触发，"给我一个脉冲就播一次音效"）
在全项目**找不到任何输出端口来驱动它**。

顺着往下查，一路查出 5 处同类缺口（明细见 `docs/01` §10.2 的表）：

| # | 缺口 | 后果 |
|---|---|---|
| 1 | `puzzle_ctrl.i_target_mask` 不存在 | `o_solved` 没有比较对象 |
| 2 | `puzzle_ctrl` 的 5 个零片形状输入不存在（`PIECE_MASK(i)` 等符号全项目未定义） | `place()` 无从下手 |
| 3 | `buzzer_ctrl.i_trigger` 无人驱动 | **蜂鸣器永远不响**（提高项①直接失效） |
| 4 | `piece_rom.i_level` 无人驱动 | 第二关的零片形状永远是错的 |
| 5 | `puzzle_ctrl.i_sel / i_conf / i_dir` 无人驱动 | **三个操作键全部失效**，游戏无法进行 |

### 定位过程

1. **起因是一次"默写"**：把 `docs/01` §5 的端口表按模块抄成 `component` 声明。
   抄到第 8 个模块时，手跟不上 —— `buzzer_ctrl` 的 `i_trigger` 写完之后，
   在脑子里找不到"谁给它"。
2. **不信任记忆，改为从另一侧数**：把 §5 里**所有** `i_*` 端口抄成一列（共 60 余项），
   逐个在 §2.1 的连接表里找出它的**发出方**。
   —— 关键在于**换了遍历方向**：以前是"按模块翻表"（往里查，每个模块自己看都完整），
   这次是"按线数表"（从线这一侧数，缺口才会露出来）。
3. 数出 5 处找不到发出方的输入端口。其中第 3 项（`i_trigger`）最刺眼：
   它的**后果与原因完全不成比例** —— 一个 1 位的输入端口没人驱动，
   代价是整项提高要求（音效）静默失效。
4. 反过来又数了一遍 `o_*` 端口（找去处），没有新发现，
   但确认了 `piece_rom.i_level` 这条线在框图上也不存在 —— **图和文档同时缺**。

### 根本原因

**§5 的 12 张表是"按模块"组织的，而"缺线"这个错误只有在"按线"看的时候才存在。**

每个模块的端口表单独看都是自洽的：`buzzer_ctrl` 说自己要一个 `i_trigger`，
这没什么不对 —— 它的**职责描述**（"触发脉冲，边沿触发播放"）也完全成立。
问题在于**没有人在全局问一句"这根线从哪来"**。

> ⭐ **这 5 处的共同点：都不是"写错了"，而是"没人写"。**
> "写错"会被对照检查抓出来（有东西可比）；
> **"没人写"没有任何东西可比 —— 它是空白，而空白不会引人注意。**
> 这正是 ERR-0010 的同一个病根换了个位置：那次是"§5 少了一整节"，
> 这次是"§5 里少了一批端口"。

**为什么 `component` 语法让它暴露了**：直接例化 `entity work.xxx` 时，
端口名在**一个地方**出现（模块自己的实体声明），顶层不必重写；
改成 `component` 后，每个端口都必须在顶层**被写第二遍**，
"这个端口从哪来"这个问题第一次变得**必须回答**。形式改变又一次逼出了隐藏缺口。

### 修复方法

（用户 2026-09-17 拍板，三项均为推荐方案）

| # | 问题 | 决定 |
|---|---|---|
| 1 | `buzzer_ctrl.i_trigger` 的驱动源 | **`game_fsm` 增加 `o_sound_trig`** —— 音效的触发时机（按键/锁定/倒计时/胜负）本来就全是 `game_fsm` 产生的事件 |
| 2 | "键号 → 动作"的译码放哪 | **放 `game_fsm` 内** —— 它是游戏规则；`keypad_scan` 只回答"按了哪个键"，是纯器件驱动 |
| 3 | 修复时机 | **现在一次修完** —— 此时 RTL 一行没写，改文档零成本 |

据此改动：

- **`docs/01` §2.1**：连接表由"11 条"重写为"**2 条系统级 + 13 条子系统间**"，
  每条都写清**方向、信号名、含义**，并补上第 4 条 `rnd_step`（方向极易画反）。
- **`docs/01` §5.6 / §5.8 / §5.9 / §5.10**：补 `piece_rom` 的 4 个输出、
  `puzzle_ctrl` 的 6 个输入 + 1 个输出、`disp_format` 的端口加宽、
  `game_fsm` 的删 6 增 6。
- **`docs/01` §10.2**：新增本节，把这条规则固化成**可数的动作**，并建议脚本化。
- **`docs/02` 同步 18 个小节**（§8.2~§8.5、§10.2~§10.8、§11、§12.2、§12.6~§12.9、§14.1.2）。
- **`docs/图/系统图.html`**：DWG-01 三条连线补标签；DWG-02 补画三条连线（见 ERR-0013）。

### 预防措施

- ⭐ **把"检查端口驱动"变成一条可数的动作，而不是一句"要注意"**：
  遍历方向必须是**从线到模块**（数 §5 里每个 `i_*` 有没有发出方），
  **不能**按模块翻（那样每个模块都自洽，永远查不出缺线）。
- ⭐ **上一条应当脚本化**：`scripts/check_ports.py`，
  从 `docs/01` 解析 §5 与 §2.1 两段做集合相减，**放在阶段 4 写 `puzzle_top` 之前**。
  理由与 ERR-0010 相同 —— **叙述会被读过就忘，能跑的动作不会。**
- **凡是有"事件"语义的输入端口（`i_trigger` / `i_round_start` 这类脉冲），
  在设计文档里就写清"谁产生这个事件"** —— 事件类端口最容易没人产生，
  因为它描述的是"发生了一件事"，而"谁让这件事发生"很容易落在所有人之外。

> **正面意义**：这 5 处缺口是在**RTL 一行没写**的时候发现的。
> 若留到阶段 4 集成，表现是"蜂鸣器不响 / 三个键没反应 / 第二关形状不对"——
> 三个互不相干的症状，排查要从 11 个模块里各找一个原因。
> **一次交叉核对，省掉三次分散排障。**

---

## ERR-0012

**`docs/02` 的 VHDL 片段有 6 处"照着敲会编译不过"，其中一处是运算符优先级**

- **日期**：2026-09-17
- **阶段**：文档（同上，封版前逐段复核代码片段）
- **涉及模块**：`docs/02-模块详细设计.md` §10.5 / §10.7 / §10.8
- **状态**：已解决

### 现象

在用 `docs/02` §10.5（`puzzle_ctrl` 的像素输出进程）作为"照抄源"，
准备在脑内演练一遍综合时，逐行读下去发现有 6 处**不查文档就写不出来**的地方：

| # | `docs/02` 原文 | 问题 |
|---|---|---|
| 1 | `process (all_inputs)` | **`all_inputs` 是 VHDL-2008 的关键字**，本项目用 VHDL-1993 |
| 2 | `PIECE_MASK(i)` | 该符号**全项目从未定义**（与 ERR-0011 第 2 项同源） |
| 3 | `ANCHOR_ROW(i)` / `ANCHOR_COL(i)` | 同上，未定义 |
| 4 | `if r_sel = i then` | `std_logic_vector` **不能与 `integer` 直接比较**（类型错） |
| 5 | `o_union` | **未声明的信号名**（§10.8 里还有第二处） |
| 6 | `r_locked and VALID_MASK = VALID_MASK` | ⭐ **运算符优先级错**，见下 |

### 定位过程

1. 起因是"准备照抄"，于是自然地把每一行都读了一遍 ——
   **"要照着敲"这个动作本身就会让人逐行读**，而"通读一遍"不会。
2. 第 1、2、3、4、5 项是照着 `docs/01` §5 的端口表和 §10.3 的信号表逐个对出来
   （表里没有的名字，代码里出现就是错的）。
3. 第 6 项**光靠对照表看不出来** —— 它每个名字都存在，语法也像对的。
   是读到这里停下来问了一句"**`and` 和 `=` 谁先算**"才发现的：

   ```vhdl
   -- VHDL 的优先级：关系运算符（= /= < <= > >=）比逻辑运算符（and or）**紧**
   -- 所以下面这行实际被解析成：
   r_locked and VALID_MASK = VALID_MASK
   --        ↓
   r_locked and (VALID_MASK = VALID_MASK)   -- 括号里是 boolean
   --        ↓
   -- std_logic_vector and boolean → 类型错误，编译不过
   ```

4. 顺着这条线又查了 §10.7（移动与钳位）与 §10.8（选择/锁定/判定），
   在 §10.7 里发现用了一句**在 VHDL 里根本无法表达**的伪代码：
   `if 减过头 then 补救` —— 下溢**已经发生**了，没法"补救"；
   在 §10.8 里发现一段**死循环**（详见下一节）。

### 根本原因

**这些片段是"讲算法用的伪代码"和"能综合的 VHDL"混在一起写的，
而文风上没有区分这两者。**

读者（包括未来的我）看到 `process (...)` 这种完整骨架，
会默认它可以照抄 —— 但里面混着 `PIECE_MASK(i)` 这种占位符式写法。
**伪代码不是问题，不加标记的伪代码才是问题。**

> ⭐ **运算符优先级那一处的特殊性**：它**不是伪代码**，
> 而是一行看起来完全合法、实际上会被解析成另一个意思的 VHDL。
> 前 5 项照着敲会立刻报错（未定义 / 类型错），**能自己暴露**；
> 这一项**照着敲也报错，但报的错在括号那一层**，
> 很容易被误判成"端口类型不对"而改错地方。
> **它属于"看起来对、其实错"那一类 —— 最贵的一类。**

### 修复方法

- **§10.5 整段重写**：`process` 敏感表改为显式列出信号；
  用 `variable` + `for i in 0 to 3 loop` 展开 4 块（循环界是**常量**）；
  补 `s_valid_mask` / `s_union` 两个内部信号的声明；
  `r_sel = i` 改为 `r_sel = to_unsigned(i, 2)` 的等价写法；
  并把 6 处问题列成表附在代码下方（保留"曾经错在哪"，供报告⑤取材）。
- **§10.7 整段重写**：由"先无条件加减、再修补"改为 **"先判断、再决定改不改"**——
  三个问题一次解决（见下），并补一条 ⚠️ 说明 `v_maxr` / `v_maxc` 的
  `range` 必须到 `0 to 8`（因为 `v_h` 可以是 0）。
- **§10.8 整段重写**：死循环改为
  `for k in 0 to 3 loop … exit when … end loop`（界是常量，最多转 4 圈必停），
  并用 **变量** 做迭代（进程里读信号读到的是旧值，循环体每一轮会算出同一个数）；
  `VALID_MASK` → `s_valid_mask`、`o_union` → `s_union`。
- **新增 ⚠️ 运算符优先级说明**，并写出一条硬规则：
  **凡是要做"全 1 检测"这类表达式，一律加括号。**

### 预防措施

- ⭐ **写"可直接照抄"的代码块时，必须同时满足两件事**：
  ① 用到的每个符号都能在本项目里找到定义；
  ② 语法在本项目的 VHDL 子集内（这里是 **-1993 + `numeric_std`**）。
  **做不到就明确标注「伪代码，不可照抄」**，不要让它混在完整骨架里。
- **凡是出现 `and` / `or` 与 `=` / `/=` 混在一行的表达式，一律加括号。**
  VHDL 的优先级与 C 相反，这一条不能靠"记住了"防住，只能靠"习惯加括号"。
- **文档里的代码片段，在它变成 `.vhd` 文件的那一刻要重新逐行读一遍**——
  本次正是"准备照抄"这个动作把 6 处问题一起逼了出来。
  **这与 ERR-0010/0011 同源：换个动作，错误自己浮上来。**

---

## ERR-0013

**DWG-02 漏画 3 条连线；`key_press` 标签被竖线压掉最后一个字符**

- **日期**：2026-09-17
- **阶段**：文档（图形与接口表交叉核对）
- **涉及模块**：`docs/图/系统图.html` 的 DWG-01 / DWG-02
- **状态**：已解决

### 现象

配合 ERR-0011 的接口改动更新框图时，把图与 `docs/01` §2.1 的连线表
**逐条对了一遍**，发现两个问题：

**① DWG-01 有 3 条连线的标签与 §2.1 对不上**（属于同步遗漏）：

| 连线 | 图上原来写的 | 实际应写 |
|---|---|---|
| 游戏控制 → 拼图核心 | `level · round_start` | `round_start · sel · conf · dir`（**`level` 根本不去 `puzzle_ctrl`**） |
| 游戏控制 → 图案与随机数据 | `pattern_sel · seed` | `pattern_sel · level · seed` |
| 游戏控制 → 音效输出 | `sound_sel` | `sound_sel · sound_trig` |

**② DWG-02 漏画 3 条连线**，而且是在**图上看着完全正常**的情况下漏的：

- `game_fsm → puzzle_ctrl`（`round_start · sel · conf · dir`）—— 图上 `puzzle_ctrl` 只从三个 ROM/LFSR 取数，**没有任何操作键入口**；
- `puzzle_ctrl → rng_lfsr`（`rnd_step`）—— **本项目最危险的那根线，在实体级图上不存在**；
- `game_fsm → piece_rom`（`level`）。

**③ 渲染放大后发现 DWG-02 的 `key_press` 标签被一根竖线压掉了最后一个字符。**

### 定位过程

1. 改完 `docs/01` §2.1 后，按 `CLAUDE.md` §12.3「改了模块接口 → 必须同时改框图」，
   把 §2.1 的 15 条连线**逐条**在图里找对应的一笔 —— 于是①的三处不一致立刻显形。
2. 反过来做：把 DWG-02 图上的每一笔回查 §2.1 —— 发现②的三条在图上**没有对应的一笔**。
   **这一步是关键：只做"表 → 图"会漏掉"图上没有的"，必须再做一遍"图 → 表"。**
3. 走 `CLAUDE.md` §12.4 的五步清单：
   `check_svg.py` **全部通过**（它只查格式），
   于是渲染成 PNG、**放大 2.6 倍分区域看** —— 在 `key_press` 那一处，
   最后一个字符被竖线压住。

### 根本原因

**① 是同步遗漏**：`docs/01` §5 改过好几轮，§2.1 的连线表改过，图没跟上。
**根因是"图与表之间没有一次全量对账"** —— 每次都只改"这次动到的那几条"。

**② 是"看不见的缺口很难发现"**：DWG-02 上已有的连线画得整整齐齐，
**没有任何视觉线索提示"还缺三条"**。
`check_svg.py` 更管不了 —— 它校验的是**格式**，不是**完整性**。

**③ 又是 ERR-0009 的同一类**：`check_svg.py` 第 ④ 类检查（走线穿框）
只查"线 vs **有填充**的方框"，标签压线属于**几何之外**的问题，脚本查不到。
`CLAUDE.md` §12.4 早就写了"必须渲染 + 放大 2.5~3 倍分上中下逐段看"，
**这一步本次照做了，于是抓到了。**

> ⚠️ **但值得记一笔：ERR-0009 的教训是"清单化"，本次能抓到正是因为走了清单。**
> 不过 ① 和 ② 是清单**管不到**的 —— 它们不是"图哪里画错了"，
> 而是"**图与表之间少了什么**"。§12.4 的清单当时只有四步，默认"图是完整的、只是可能有瑕疵"，
> 没有覆盖"完整性"这一维。**这是本次暴露出的清单盲区，已补为第 5 步。**

### 修复方法

- **DWG-01**：三条连线的标签全部按 §2.1 改写；
  「游戏控制 → 拼图核心」由 1 行改为 3 行
  （`操作键转发` / `round_start` / `sel · conf · dir`）以适应走廊宽度；
  「游戏控制 → 显示子系统」的 `cnt` 展开成 `preview_cnt · game_cnt_bcd`。
- **DWG-02**：补画 3 条线。
  - `game_fsm → puzzle_ctrl` 走 **y=272 走廊**（在反馈线 y=240 与 `pattern_rom` 顶边 y=285 之间），
    起点取 x=430 —— 避开 x=450 的反馈竖线与 x=360 的音效竖线。
  - `level → piece_rom` 与 `pattern_sel → pattern_rom` **共用 x=400 这条竖线**
    （185~323 段完全重合、渲染成一根线，在 y=323 与 y=463 各分出一支）——
    这在语义上是对的：**一个源、两个目的地**。
  - `rnd_step` 走 puzzle_ctrl 下方 → 左转进 `rng_lfsr` 右边缘。
- **`key_press` 压线**：把 `game_fsm → buzzer_ctrl` 的竖线**由 x=320 移到 x=360**。
  改标签位置解决不了 —— x=265 与 x=320 之间只有 55 单位余量，
  而 `key_press` 需要 59 单位，**"把标签挪一挪"这条路本来就走不通**，
  只能移动那根线（这与 ERR-0009 里 `sw7 系统开关` 的处境同构）。
- 顺带发现 `rnd_step` 的箭头会压住既有的 `rnd` 标签，
  于是把 `rnd` 标签移到它所标连线的**上方**（y=603 → y=595），
  两条线各占一侧、各自的标签都长在自己那侧。

### 预防措施

- ⭐ **图与连线表对账必须做两遍，方向相反**：
  「表 → 图」（表里的每条线在图上找得到吗）**和**「图 → 表」（图上每笔在表里有出处吗）。
  **只做一遍必漏** —— 本次的 ② 就是只做「表 → 图」时漏掉的。
  （ERR-0011 的端口核对是同一个道理的另一半：**换方向才能看见空白**。）
- ⭐ **`docs/01` §2.1 的连线表要能被数出来**：目前是 **2 + 13 = 15 条**，
  框图上应该恰有 15 组线。这个数字写进本记录，下次改图时直接数。
  （★ 第 10 次会话更正：原记的是"2 + 12 = 14 条"—— **0 号那行也是正儿八经的一条线，漏数了**。）
- **`CLAUDE.md` §12.4 的清单当时只覆盖"画得对不对"，不覆盖"画全了没有"。**
  **已补为第 5 步**：对账做两遍、方向相反，并把连线表的条数与图上的笔数各数一遍。
- **标签放不下一律先量再放**：`dg-lbl` 是 11px 等宽字体，中文按 11 单位/字估、
  西文按 6.6 单位/字估。**先算宽度、再决定放哪、必要时改线的位置而不是标签的位置。**

## ERR-0014 系统设计文档内部的 14 处不一致 —— 单看每一节都自洽，交叉引用才暴露

- **日期**：2026-09-17
- **阶段**：设计文档复查（阶段 1 封版**之后**的一次专门找矛盾）
- **涉及模块**：`docs/01-系统设计.md`（`clk_gen` / `disp_format` / `game_fsm` / `keypad_scan` / `pattern_rom` / `puzzle_ctrl`）
- **状态**：已解决

### 现象

用户要求"最后仔细审查系统设计和交接文档"。分派子代理通读 `docs/01-系统设计.md`（1355 行）后，
一次性报出 **14 处**问题。**没有一处会报错**，全部是"读起来通顺、单独看每节都自洽"的矛盾：

| # | 位置 | 现象 | 性质 |
|---|---|---|---|
| 1 | §1.2 原则三 | 门控代码块里用的是 `dot_row` / `seg` / `buzz` 等**顶层并不存在的信号名** | 符号未定义 |
| 2 | §2.1 系统级连线 A 行 | 断言"`clk` **必须画出来**"，而 §2.1.2 第 1 处写的是"50MHz 并入时钟椭圆，**不再单画**" | **两处直接打架** |
| 3 | §2.1.1 第 2 处 | 同源：说"补回 50MHz 输入线"，而第 2 版白板稿又把它折回了椭圆 | 版本残留 |
| 4 | §4.2 节拍表 | `tick_2hz` 的用途写成"开机自检**闪烁**" | **用途写错**（见"根本原因"②） |
| 5 | §2.2 | 贴着一张 **60 行的 ASCII 框图**，是 §2.1 连接表的**第二份拷贝**，且已漂移 | 陈旧拷贝 |
| 6 | §6.5 | `i_rel_mask` 的类型写成 `std_logic_vector(15 downto 0)`，实为 `mask_arr_t`（4 × 64 位） | **类型错** |
| 7 | §7 小结图 | 树上出现 `└──▶` 之后，**同一列紧跟一个 `│`** | **逻辑矛盾** |
| 8–14 | §1.1.3 / §3.1 / §6.3 / §7 / §9 等 | 漏要求 6、`disp_format` 职责过窄、伪代码循环上界来自信号、小结图漏 4 根线、引脚数未区分两个顶层 | 遗漏 / 过时 |

### 定位过程

1. **先按 `CLAUDE.md` §0 的顺序读**（`HANDOFF` → `PROGRESS` → `CHANGELOG` → `ERRORS`），
   建起"这份文档应该长什么样"的预期，再看它实际长什么样；
2. **分派两个子代理并行**：一个专审 `docs/01`，一个专审 5 份过程文档的**重复与陈旧**。
   分成两个而不是一个，是因为**判据不同** —— 前者查"对不对"，后者查"多不多"；
3. ⭐ **关键动作是"交叉引用对账"，不是"通读"**：
   把 §2.1 / §2.1.1 / §2.1.2 **三处关于 `clk` 的叙述并排摆在一起**，矛盾立刻现形 ——
   通读时它们相隔两百多行，**各自都通顺**。同样的方法抓到
   §4.2 的 `tick_2hz` 用途与 §7 的 `o_blink` 生成方式对不上、
   §6.5 的类型与 §5 接口表对不上；
4. 对 §2.2 那张 ASCII 图，**逐条数它比终稿少了什么**
   （少 `seed/seed_load`、少 `pattern_mask`、把 `puzzle_ctrl` 直连到了 `dot_matrix_scan`）
   → 确认它不是"略有出入"，而是**另一份已经死掉的设计**；
5. §7 的树：逐笔念"这个字符为什么在这儿" —— 该树干有 **4 个分支**，
   第一个却写成 `└`（树干终止），下一行同列又是 `│`（树干继续），**两行自相矛盾**。

### 根本原因

**两类，成因完全不同：**

1. ⭐ **"同一份信息写了两遍，改版时只改了一处"**（第 1 / 2 / 3 / 5 / 7 / 9~14 共 10 处）。
   凡是同一事实存在两个副本（§2.1 的表 ↔ §2.2 的 ASCII 图、§2.1 ↔ §2.1.1/§2.1.2、
   §6.5 的类型 ↔ §5 的表、§7 的小结图 ↔ §2.1 的连接表），
   **改版时只改一处是默认结果，不是疏忽**。
   第 2 处最典型：三处叙述分别写于**三个不同版本**的图上，**每处当时都是对的**。
2. **"把使能当成了电平"**（第 4 处，本次唯一一处真正的技术错）。
   `tick_2hz` 是**单周期使能**（一拍宽的高电平），而"闪烁"要的是**电平**。
   把 `tick_2hz` 当方波直接送出去，每个使能翻转一次 → 实际得到 **1Hz**，不是名义的 2Hz。
   文档写"开机自检闪烁"，等于**把这个错误写进了设计**。

### 修复方法

- `docs/01-系统设计.md` **14 处**由 `.tmp/fix01.py` 一次性批量更正（每条校验命中次数）：
  - 第 1 处 → §1.2 的代码块换成 `puzzle_top` 里**逐字可抄**的六行真实名字（`o_dot_row` / `sw7` / `s_*`）；
    > ⚠️ **本条记录在 2026-09-18 被更正**：`o_dot_row` / `o_seg` / `o_cat` / `o_buzz`
    > **不是** `puzzle_top` 的端口名，而是**子模块**的端口名。
    > 顶层器件端口名是 `dot_row` / `seg` / `cat` / `buzz`（**不带 `o_` 前缀**），
    > 真值源是 `quartus/puzzle.qsf` 的 `-to`。
    > 上面这句把 `o_` 侧钉成"真值"，**正是 ERR-0020 / 报告 `hw-01` 那处缺陷的成因**。
    > 完整裁决见 `docs/01` **§5.12**。**历史记录不重写，只加这条更正。**
  - 第 2 / 3 处 → 确立**分层画法**：DWG-01 **不画** `clk`（折进 `clk_gen` 椭圆），
    DWG-02 用**脚注逐个列出 8 个接收者实体名**；§2.1 的 ⚠️ 改写为解释这个分工，
    §2.1.1 第 2 处标注"**本处只兑现了一半**"；
  - 第 4 处 → §4.2 改为"**2 秒自检计时**"，并新增 ⚠️：`o_blink` 由 `tick_100` ÷25 ÷2 得到；
  - 第 5 处 → §2.2 的 60 行 ASCII 图**整段删除**，改为"要查什么 → 去哪里"的 4 行索引表 + 5 条画法约定；
  - 第 6 处 → §6.5 改为 `mask_arr_t` = `array (0 to 3) of std_logic_vector(63 downto 0)`，
    并订正 ② —— 真正的坑是**整数与位矢量不能直接比较**，不是"位矢量不能作下标"；
  - 第 7 处 → §7 树上 `└` 改 `├`，另把两行续行的 `◀` 左移 1 格对齐；
  - 其余见 `CHANGELOG.md` 第 10 次会话一节。
- **图侧同步 3 处**（`docs/图/系统图.html`）：DWG-01 节拍总线标签去掉 `clk`；
  DWG-02 所有时序实体框内小字一律以 `clk` 开头 + 脚注列 8 个接收者；
  DWG-03 的"任意状态按开始"两条回边改画为**直接回 `S_PREVIEW`**。

### 预防措施

- ⭐ **同一份信息只留一个真值源，其余位置写成"去哪查"的指针。**
  本次删掉的 §2.2 ASCII 图（60 行）就是样板：它是 §2.1 的第二份拷贝，留着只会漂移。
  **判据：同一事实的第二份拷贝 = 下一次改版时必然出错的点。** 删掉后 §2.2 反而更好用。
- ⭐ **凡"使能"与"电平"混用的地方，落笔前先问一句"它是几个 `clk` 宽"。**
  本次唯一的技术错就是这一条（与 ERR-0012 的 `and`/`=` 优先级同属"看着完全合法"那一类）。
- **改版后要按"同一事实的所有副本"逐个回查**，而不是"把改过的地方再看一遍" ——
  14 处里有 10 处属于这一类，**用后者查一个也查不出**。
- ⭐ **"封版"只保证"改完了"，不保证"自洽了"。**
  第 9 次会话沿着**接口轴**核对（文档 ↔ 代码），查出 14 处；
  第 10 次沿着**一致性轴**核对（文档 ↔ 文档），又查出 14 处。
  **换轴才能看见的东西，与换方向才能看见的东西，是同一个道理**
  （后者见 ERR-0011 / ERR-0013 与 `CLAUDE.md` §12.4 第 5 步）。

---

## ERR-0015 无头 Chrome 截图的两个陷阱：默认暗色方案、CJK 排版错乱

- **日期**：2026-09-17
- **阶段**：图表定稿（`CLAUDE.md` §12.4 第 3 步"渲染后放大看"的实施）
- **涉及模块**：`.tmp/shot.py` / `.tmp/fullpage.py` / `docs/图/系统图.html`
- **状态**：已解决（已固化到截图脚本参数里）

### 现象

`CLAUDE.md` §12.4 第 3 步要求"渲染成 PNG，放大 2.5~3 倍分上中下逐段看"。
本次按该步骤搭自动化截图链路时，撞到两个互不相干的渲染问题：

1. **整页截图是黑底**：截出来的 PNG 平均亮度只有 **38.9**（正常应 > 240）。
   图上所有深色文字与走线**全部消失在黑底里** ——
   也就是说，这次"放大看"的任务是在一张**看不清的图**上做的。
2. **HTML 流程文字渲染错乱**：同一页里 `<p>` / `<span>` 段落中的中文出现字宽异常、
   相互重叠、笔画残缺；而**内联 SVG 里的中文完全正常**。

### 定位过程

1. 先怀疑是自己刚改过的内容有语法问题 →
   **对照渲染一段完全没动过的脚注，同样错乱** → **排除"本次改动引入"**，是渲染器行为；
2. 黑底问题：`系统图.html` 里背景只有一个来源 ——
   `@media (prefers-color-scheme: dark) { :root { --bg: #141311 } }`。
   无头 Chrome **默认按 dark 方案渲染**，命中了该媒体查询。
   加 `--blink-settings=preferredColorScheme=1` 强制 light →
   **平均亮度 38.9 → 243.9**，问题消失；
3. CJK 错乱：换过字体回退顺序、试过 `--font-render-hinting=none`，均无效。
   最后确认：**它是无头模式下的字体度量问题，且只影响 HTML 流程文字**；
   **内联 SVG 的 `<text>` 渲染正确**（SVG 文本按显式坐标定位，不参与自动换行度量）；
4. 由此定了本项目的看图方案：**核对图务必走 SVG 裁切 + 缩放**（`.tmp/mkzoom.py`），
   整页截图（`.tmp/fullpage.py`）只用于看**版面关系**，不用来核对文字。

### 根本原因

1. **媒体查询的默认状态不是"浅色"。**
   本项目的文档页都写了 dark 方案（为了用户能用深色浏览器看），
   而无头环境里**没有"用户偏好"这个信息**，渲染器只能取一个默认值 ——
   它取的是 **dark**，于是 `prefers-color-scheme: dark` 在无头下**默认命中**。
2. **CJK 不是等宽的。** 西文字体的 advance width 与 CJK 字形的实际宽度在无头环境下对不齐，
   **自动换行与文本居中因此全部算错**；SVG `<text>` 给的是绝对坐标，不参与自动布局，
   所以不受影响。

### 修复方法

- `.tmp/shot.py` 与 `.tmp/fullpage.py` 的 Chrome 参数固定加
  `--blink-settings=preferredColorScheme=1` 与 `--default-background-color=FFFFFFFF`；
- `.tmp/shot.py` 增加一条容错：`.tmp/render/` 下残留的、不含 `#vp{width:…}` 的旧文件**跳过**
  （否则 `re.search(...).group()` 抛 `'NoneType' object has no attribute 'group'`）；
- **看图一律用 SVG 裁切件 + 放大**（把内联 `<svg>` 的 `viewBox` 改成目标区域、`transform:scale(2.6)`），
  它渲染 CJK 正确。

> 📌 **本条的三个脚本（`shot.py` / `fullpage.py` / `mkzoom.py`）住在 `.tmp/` 里，而 `.tmp/` 是
> `.gitignore` 明列的"临时实验区，随时可删"。** 所以这里记的是**参数与做法**，不是路径 ——
> 换机器 / `.tmp/` 被删掉之后，照上面三条自己重建即可，**不要因为找不到脚本就跳过"放大看"这一步**。

### 预防措施

- ⭐ **"渲染出来看一眼"这件事本身会骗人 —— 先确认那张图是可信的。**
  本次黑底截图里，原先那 6 处压线缺陷**一处也看不出来**；
  若直接拿它当依据，会得出"图上没问题"的结论。
  **这与 `CLAUDE.md` §12.4 表里"脚本没报错 ≠ 已经检查过了"是同一个错误的另一面。**
- ⭐ **凡是自动化产出的中间物（截图、解析结果、日志），第一次用之前先做一次可信性检查**：
  拿一个**已知答案的输入**喂进去，看输出对不对。
  本次的可信性检查就是"渲染一段没改过的脚注看看"。
- **无头浏览器与有头浏览器不是同一个渲染行为**，
  涉及媒体查询 / 字体度量的结论**只在无头下成立**，不要外推到用户的浏览器。

---

## ERR-0016

**`docs/02` §13.4 音效骨架：半周期计数 off-by-one（装载 N 得 N+1 拍）+ 两张查表只有名字没有定义**

- **日期**：2026-09-18
- **阶段**：设计文档审查（阶段 1 封版后的规划评审，第 11 次会话）
- **涉及模块**：`docs/02-模块详细设计.md` §13 / `buzzer_ctrl`
- **状态**：已解决

### 现象

审查 `buzzer_ctrl` 的代码骨架时发现两类缺陷：

1. **频率会全错**：骨架写 `r_toggle_cnt <= HALF_PERIOD(sel)` 装载 **N**，再倒数到 0 翻转 ——
   从 N 数到 0 含 0 共 **N+1 拍**，实际频率是 `8000/(2(N+1))` 而不是表上声称的 `8000/(2N)`
   （2kHz 变成 1.33kHz）。**每种音效全错**，而蜂鸣器音调偏差在仿真与上板时都**不易察觉**。
2. **查表是空名**：`HALF_PERIOD` / `DURATION` 两张表**只有名字、全项目没有定义处**，
   "交替音用 r_dur_cnt 高位切换"也只有一句话、没有可综合的实现 ——
   与 ERR-0012 那批"未定义符号混在完整骨架里"是同族问题。

### 定位过程

1. 按 §13.3 的推导 `f = 8000/(2N)` 反验骨架：装载值与"每 N 拍翻一次"的语义对不上，差一拍；
2. 全项目搜 `HALF_PERIOD` / `DURATION`：只有 §13.4 一处引用，**无定义处**；
3. 顺带检查同节其余内容：交替音（sel 6/7）的段切换逻辑缺失，`r_dur_cnt` 的位宽也未给。

### 根本原因

骨架是按"意图"写的（"每 N 拍翻一次"），没有按"计数器数到 0 含 0"的实际语义核对；
§13 是 2026-09-17 补写的（ERR-0010 的关联修复），补写时只补了"接口存在"，
没有来得及把**内部实现**也写到可综合的完备度 —— 与 ERR-0010/0012 同类：
**补缺口时补了"面"，没补"里"。**

### 修复方法

- 文件：`docs/02-模块详细设计.md` §13.3 / §13.4 / §13.5
- 改动：
  ① 查表改存 **N-1**（`HALF_A` / `HALF_B`），计数器从 N-1 数到 0 恰为 N 拍；
  ② 补全三张查表常量（`HALF_A` / `HALF_B` / `DUR` / `SEG`，模块私有，类型 `int_arr8_t`）
     与全部寄存器声明（含 `range`：`r_dur` 到 4800，13 位）；
  ③ 交替音用独立的段计数器 `r_seg` + 相位位 `r_alt` 实现（非交替音效 `SEG=0` 自动退化）；
  ④ 触发时**锁存** `i_sound_sel` 到 `r_play`，避免播放期间上游选择字变化导致音调跳变。

### 预防措施

- ⭐ **"每 N 拍一次"的计数器，装载值一律写成 N-1，并在注释里写明"数到 0 含 0 共 N 拍"。**
  这类 off-by-one 在"按意图写骨架"时极容易带进来，仿真里又只差 6%~25% 的频率，不易暴露；
- **文档骨架里出现的每一个符号（函数名 / 常量名 / 信号名），都必须能在本项目里找到定义处** ——
  与 `CLAUDE.md` 硬约束 14（伪代码必须显式标注）是同一条，本次是漏标；
- **补一个缺口时，检查它相邻的段落是否也只补了"面"**（ERR-0010 补了接口表，
  却留下了 §13.4 的实现缺口）。

---

## ERR-0017 课件合规改到一半就停了：引用悬空 + 两处自相矛盾 + 接口真值源没动

- **日期**：2026-09-18
- **阶段**：文档（跨文件一致性）
- **涉及模块**：`CLAUDE.md` §4.6/§10.1、`docs/01` §2.1/§4.1/§5.1、`docs/02` §3、`docs/图/系统图.html`、`ERRORS.md`、`AI_LOG.md`
- **状态**：已解决

### 现象

按课程课件《数字系统设计2026》p45（一）"**复位必须用按键来实现**"恢复 BTN0 按键复位时，
做完 `CLAUDE.md` §4.6 / `docs/04` / `.qsf` / `check_pins.py` **四个落点后中断**，
留下一个**处处自相矛盾、且没有任何报错**的中间状态：

| # | 残留 | 为什么危险 |
|---|---|---|
| 1 | `CLAUDE.md` §4.6 写"已回退并记入 **ERR-0018**"，而 `ERRORS.md` **只到 ERR-0016** | **悬空引用**。引用一个不存在的条目，比不引用更糟 —— 读者会以为已经记录在案 |
| 2 | `CLAUDE.md` §4.6 说**三源**复位，§10.1 仍写 `rst = (not SW7) or por_rst`（**两源**） | **同一份文件隔 300 行自相矛盾** |
| 3 | `docs/01`（接口唯一真值源）**一个 `btn` 都没有** —— §2.1 连线表、§4.1、§5.1 `clk_gen` 端口、§8 自检、§9 的 I/O 数 | 新端口 **`i_btn_rst` 在真值源里不存在** |
| 4 | `docs/02` §3 的端口表、结构图、`o_rst` 方程**仍是两源** | 照它抄代码会**漏掉按键复位** |
| 5 | `docs/图/系统图.html` 未改 | 图与表已不一致（违反 `CLAUDE.md` §12.3） |
| 6 | `AI_LOG.md` **没有本次记录** | 评分项⑦ 10 分直接受损 |
| 7 | 顺带发现：`CLAUDE.md` §4.6 写的"**异步断言、同步释放**"是**用词错误** | 见下 |

### 定位过程

1. 按 `CLAUDE.md` §0 读交接文档 → 发现 `HANDOFF.md` / `PROGRESS.md` 里**根本没有这次课件合规工作的记载**；
2. `grep -rn "BTN0"` → 命中 `CLAUDE.md` §4.6 与 `docs/04`，**但 `docs/01` / `docs/02` 零命中** → 异常；
3. `grep -o "ERR-00[0-9][0-9]" ERRORS.md | sort -u` → 最大 **ERR-0016**，
   而 `CLAUDE.md` 引用了 **ERR-0018** → 确认**悬空**；
4. `grep -n "复位 | \*\*同步"` `CLAUDE.md` → §10.1 仍是两源 → 确认**自相矛盾**；
5. 逐条比对"改了哪些文件 / 该改哪些文件"，得出上表 7 项。

### 根本原因

**课件要求命中的是一根"接口轴"，而这根轴上的落点横跨 7 类文件**
（约定手册 / 接口真值源 / 模块详设 / 引脚表 / 图纸 / 过程记录 / 日志）。

前几次会话核对的都是**已有内容的正确性**（第 9 次接口轴、第 10 次一致性轴、第 11 次可执行性轴），
**没有一次是"新增一个端口"** —— 于是从来没有沉淀出"新增端口要动哪些文件"的清单。
改到一半停下来时，**已经改的部分全都自洽，没改的部分也全都自洽**，
**矛盾只存在于"改了的"与"没改的"之间** —— 而这正是任何单文件通读都看不见的地方。

> **这是 ERR-0014「同一份信息写两遍、只改了一处」的第二种形态**：
> ERR-0014 是"**同一个文件**里两处副本只改一处"，
> 本次是"**跨文件**的副本只改一处"，**而且连"该改哪些文件"本身都没有清单**。

### 修复方法

补齐全部落点（详见 `CHANGELOG.md` 第 12 次会话）：

- `CLAUDE.md` §4.6（重写为消抖版）/ §10.1 / §10 模块清单 / §11 考核要求对照；
- `docs/01` §2.1（2 条→3 条系统级连线）、新增 §2.1.3、§4.1、§5.0、§5.1（增 `i_btn_rst`）、§5.10、§8.1/§8.2、§9；
- `docs/02` §2.6（增 `DEBOUNCE_BTN`）、§3.1~§3.6、§12.4；
- `docs/图/系统图.html` 的 DWG-01 与 DWG-02 各补一笔 `btn`；
- 本文件补 ERR-0017 / 0018，`AI_LOG.md` 补本次记录。

⭐ **`scripts/check_ports.py` 抓到了这次修复自身的遗漏**（值得单独记一笔）：
补完 `docs/01` §5.1 的 `i_btn_rst` 之后跑 `check_ports.py`，它报
`✗ 发现 1 个输入端口没有驱动源（悬空）：clk_gen.i_btn_rst（线名 btn_rst）`。
**原因不是接口错了，而是脚本不认识这个新端口** —— `btn` 是板级输入、
且线名与端口名不同名（同 `sw7` → `sys_en` 那种改名）。
按脚本既有机制补两处登记后通过：

```python
TOP_DEVICE_INPUTS = {"kp_row", "btn"}          # btn 是板级输入
ALIAS_IN = { ..., ("clk_gen","btn_rst"): "btn" }  # i_btn_rst 接的是顶层 btn
```

> **这正是把核对动作脚本化的收益**：人做这个核对会漏（ERR-0017 已经漏了 4 处），
> **脚本不会** —— 它连"我自己刚加的端口"也照样拦下来。
> 全绿后 `--selftest` 复跑：两类注入均检出、正例零误报。

**"异步断言、同步释放"的更正**：该说法描述的是
`if rst = '1' then … elsif rising_edge(clk)` 这种**异步复位**写法。
而本设计中 `o_rst` 是组合输出、**只被各模块在 `rising_edge(clk)` 内采样**，
断言与释放**都发生在时钟沿** —— 是纯粹的**同步复位**，`rtl/` 里不会出现任何异步复位分支。
**改为"同步、高有效"，全项目口径统一。**

> ⚠️ **2026-09-18 补注（第二次审查，报告 `clk_gen-01`）**：上面这句"断言与释放都发生在时钟沿"
> **只对三个复位源中的两个成立**。`o_rst` 的三源合并式里，第三源 `(not sys_en)` 是
> **拨动开关 SW7 的组合直通**（`docs/02` §3.5 注②）—— 它与 `clk` 之间**没有任何同步器**，
> 其变化相对时钟沿完全异步。`o_rst` 只作**时钟沿内的条件**使用（不是异步复位端、不引入亚稳态），
> 故实际影响限于"开关沿撞进建立/保持窗口时个别触发器当拍漏采复位值"这一类
> **功能仿真查不出、上板偶发**的边界。本版裁决与可选的根治手段（给 `sys_en` 加 2 级同步器）
> 记在 `docs/02` §3.5 注②。**"同步、高有效"这个总口径不变**，但"发生在时钟沿"
> 这句话不能再当作三源皆准的绝对表述使用。

### 预防措施

- ⭐ **建立"新增/修改一个端口"的固定落点清单**（本次沉淀，
  已写入 **`CLAUDE.md` §7.1**，共 **9 个落点 + 2 遍反向对账 + 1 条收尾 grep**）：
  `docs/01` §2.1 连线表 → §5 接口表 → §2.1.x/§4.x 说明 → §8/§9 →
  `docs/02` 详设四小节 → `docs/图` 两层图 → 顶层 `component` 声明 →
  `.qsf`/`docs/04`（若涉及引脚）→ 过程文档。
  **清单是可数的，叙述不是。**
- ⭐ **引用 `ERR-XXXX` 之前，先确认那个编号存在** ——
  `grep -o "^## ERR-00[0-9][0-9]" ERRORS.md | tail -1` 是一条一秒钟能跑的动作。
  **能跑的计数动作必须是准的**（与 ERR-0014 把模板标题改成 `ERR-XXXX` 同源）。
- **收工前必须跑一次"新事实全库搜索"**：本次新增的关键词是 `btn` / `i_btn_rst`，
  一条 `grep -rn "btn" --include="*.md" --include="*.html" --include="*.qsf"` 就能看出落点齐不齐。
- **中断的工作必须留下"还差什么"的显式标记**，不能只在心里记着 ——
  本次若在 `CLAUDE.md` 写一句"⚠️ `docs/01`/`docs/02`/图 尚未同步"，就不会有人踩。

---

## ERR-0018 复位按"文档内部一致性"被删成两源，违反课件 p45（一）的硬要求

- **日期**：2026-09-18
- **阶段**：文档（需求符合性）
- **涉及模块**：`CLAUDE.md` §4.6 / §10.1、`docs/01` §2.1.3、`docs/02` §3、`clk_gen`
- **状态**：已解决

### 现象

`CLAUDE.md` §4.6 早已写有"BTN0 作为硬件复位键"与 `o_rst <= btn_rst or por_rst or (not sys_en)`，
但**接口定义（`docs/01` §5.1）里 `clk_gen` 没有对应的输入端口**，`docs/02` §3 的实现也是两源。
当日早前为消除这处"文档内部不一致"，**选择删掉 `btn_rst`、保留两源**——
理由是"少一个端口，文档就自洽了"。

### 定位过程

1. 核对 `CLAUDE.md` §4.6 与 `docs/01` §5.1 → 两边对不上；
2. 判断"以哪边为准"时，**没有去查权威来源**（课件 / 手册），而是按"哪边改动小"来裁决
   → 删掉了 `btn_rst`；
3. 通读《数字系统设计2026》**p45（一）**时才发现：
   > *"开机复位：**初值只具有仿真意义**，不能实现对系统的复位，**复位必须用按键来实现**。"*
4. 回看本项目的两源复位 `por_rst or (not SW7)`：
   `por_rst` **正是课件说的"初值"** —— 它靠计数器数 10ms"假装"复位，运行期间再也不动作。

### 根本原因

**"文档自洽"被当成了目标，而它只是手段。**
真正的裁决依据必须是**权威来源**（课件、开发板手册、题目要求），
当"自洽"与"符合要求"冲突时，应当**改文档去满足要求**，而不是**删掉要求去迁就文档**。

> 更深一层：**`por_rst` 这个名字有误导性** —— 它听起来像"复位功能"，
> 实际只是"上电初值"。课件那句话正是来纠正这个直觉的，
> 而我们**在没有读课件的情况下**，按名字的直觉做了相反的裁决。

### 修复方法

- **恢复三源复位**：`o_rst <= r_btn_rst or r_por_rst or (not sys_en)`；
- `clk_gen` 新增输入端口 **`i_btn_rst`**（`docs/01` §5.1 / `docs/02` §3.2）；
- 走完整条链路：连线表 +1（`btn` 系统级）、DWG-01/DWG-02 各 +1 笔、`.qsf` 加 `PIN_61`、
  `check_pins.py` 加 `btn`（68 引脚全过）、自检增阶段 9。

**又一处判断的反转（消抖）**：恢复后先判定"**复位语义对抖动免疫，故不消抖**"。
后按课件 **p54**「**按键要注意防抖处理**」＋手册附图16「按键在使用时需设计防抖动电路」，
改为 **2 级同步器 + 20ms 消抖**：

> **课件要求的是"按键要消抖"，不是"抖动有害的按键才消抖"。**
> 抖动对复位确实无害 —— 但那不是"可以不做"的理由。
> 而且做了之后，答辩时无论问"按键消抖了吗"都答得死。

### 预防措施

- ⭐ **裁决文档冲突之前，先问"权威来源怎么说"** ——
  顺序是：课件/题目 → 开发板手册 → 本项目约定 → 文档内部一致性。
  **"改哪边省事"从来不是判据。**
- ⭐ **新增一条硬约束的口子**：凡遇到"某个设计决定要不要做"的争议，
  先到 `CLAUDE.md` **§11.4 课件要求对照表**里查有没有对应条目 ——
  那张表就是为"把犹豫变成查表"而建的（`docs/00` §1 的"依据来源"一节同源）。
- **名字带着"它像什么"的暗示，要警惕**：`por_rst` 是"上电初值"不是"复位"，
  这类"名字对、含义错"的符号，**判定前必须先读它的定义**。

---

## ERR-0019 DWG-01 时钟椭圆副标题溢出边框（校验脚本全绿，放大后才现形）

- **日期**：2026-09-18
- **阶段**：文档（图表）
- **涉及模块**：`docs/图/系统图.html` 的 DWG-01
- **状态**：已解决

### 现象

给 DWG-01 的时钟椭圆补了"复位产生"之后，副标题变成
`50MHz · 5 档单周期节拍 · 复位产生` 一行。
**文字左右两端同时溢出椭圆边框**（左端"5"压线、右端"产生"完全出框）。

`python scripts/check_svg.py` 对此**全部通过**。

### 定位过程

1. 先跑 `check_svg.py` → 四类检查全绿（这一步**只保证文件合法**）；
2. 按 `CLAUDE.md` §12.4 **第 2 步**用无头 Chrome 把 DWG-01 渲染成 PNG；
3. 按**第 3 步**放大 2.6 倍并裁出椭圆区域 → **一眼可见文字出框**；
4. 量算验证：椭圆 `rx=95`，副标题基线在 `cy+24` 处，
   该高度上半宽只剩 `95 × √(1−(24/50)²) ≈ 83`（可用宽 **166**）；
   而该行 10 个 ASCII + 10 个 CJK 字符在 10.5px 等宽字体下约 **190 单位宽**
   → **文字比椭圆本身还宽**，与观察一致。

### 根本原因

`check_svg.py` 的已知盲区（`CLAUDE.md` §12.4 表格里写着）：
**"标签压线 / 压框"需要字体度量（字符数 × 字号估宽），脚本查不出**。

而本次比 ERR-0009 更进一步：ERR-0009 是**文字压过虚线容器边界**，
本次是**文字压过它所属的图形本体**（椭圆），
**后者更隐蔽** —— 椭圆是 `fill` 有色的，但它不是"方框"，走线穿框检查不覆盖它，
且文字压在色块上时**对比度反而更低**、缩略图里更像"正常的居中文字"。

> ⚠️ **补字的动作要连带量宽度。** 往一个**宽度受限的图形里**加字
> （椭圆/圆角框/固定宽方框），必须**先估一下新总宽**，而不是"看着还能塞"。

### 修复方法

- 拆成两行，并重新分配四条基线使各行都落在可用宽度内：

```
y=244  时钟与节拍              （5 CJK，约 70 单位）
y=263  clk_gen                （7 ASCII，约 46 单位）
y=282  50MHz · 5 档单周期节拍   （约 139 单位 ≤ 178）✅
y=300  复位产生                （约 50 单位 ≤ 136）✅
```

- 在 SVG 源码里**写下量算过程**，防止后人再合并成一行。

### 预防措施

- ⭐ **"往固定宽度的图形里加字"要按 `CLAUDE.md` §12.4 第 3 步单独看一次** ——
  这类溢出**不会**被任何脚本抓到；
- **椭圆/圆形类图形没有"方框边"可参照**，估宽时必须按**文字基线所在的弦长**算，
  而不是按 `rx` 算（本次按 `rx×2=190` 估就会误判为"刚好放得下"）；
- 教训与 ERR-0009 同源，**已第二次复发** → 说明"放大看"这一步的**触发条件**要写清：
  **不只"新画了一张图"要放大看，"往已有的图里改字"同样要**。

---

## ERR-0020 进代码前审查查出 12 处已验证的严重缺陷 —— 前四根轴结构上看不见的一类

- **日期**：2026-09-18
- **阶段**：文档（进代码阶段之前的规划审查）
- **涉及模块**：全项目 —— `docs/01`／`docs/02`／`docs/03`／`docs/04`／图表／顶层 `component` 模板／`CLAUDE.md`
- **状态**：**已解决** —— 12 处已复核缺陷 ＋ 4 条其余已复核项 ＋ 4 条「照抄编译不过」类
  ＋ **80 条 MEDIUM/LOW（复核后 75 条确认）** 全部修完，见下面「进度表」末两节

> **完整报告**：`docs/审查报告-2026-09-18.md`（729 行）。
> 本节只记**根因与预防**，逐条证据不在这里重复（与 §7 文档去重原则一致）。
> 本条与 **ERR-0014** 同类：**一条记录覆盖一批同源缺陷**。

### 现象

用 34 个子 agent 做了自顶向下系统性审查（6 路顶层 ＋ 13 个模块 ＋ 复核／集成／批评），
产出 **96 ＋ 6 ＋ 4 条**发现。其中 12 条 CRITICAL/HIGH 经**对抗性复核**（复核 agent 默认立场是推翻）
确认属实，五类最要命的：

| # | 缺陷 | 后果 |
|---|---|---|
| 1 | **脉冲宽度契约缺失** | `keypad_scan` 出 1ms 宽按键脉冲，下游全按**单周期脉冲**消费 → **一次按键被当成数万拍**，方向键一下把零片顶到边界 |
| 2 | **多重驱动出现在 3 个模块** | `keypad_scan.r_phase`／`game_fsm` 倒计时寄存器／`puzzle_ctrl.r_anchor` 各被两个进程赋值 → **照抄文档骨架编译不过** |
| 3 | **顶层端口名无唯一真值源** | 文档标着"逐字可抄"的六行用 `o_dot_row`／`o_seg`，`.qsf` 的 `-to` 用无前缀名 → `port map` **静默悬空、不报错** |
| 4 | **两处静默失效** | 种子 `or x"01"` 强制 bit0=1，而第二关图案选择**正读这一位**；`i_round_start` 触发时刻有两个互斥说法 |
| 5 | **时序验证手段为零** | 工程无时钟约束、流程无 TAN 步骤，`docs/03` 却已断言"时序裕量极大" |

### 定位过程

1. 前四轮遍历（**接口轴／一致性轴／可执行性轴／符合性轴**）已把"文档互相矛盾"清空，
   所以本轮**换了第五根轴**：「**能不能照着写出能跑的东西**」；
2. 按用户要求**自顶向下**：先 6 路顶层（架构／接口／需求／引脚／仿真／课件符合性），
   其结论带进第 2 阶段的 13 个模块 agent；
3. 12 条 CRITICAL/HIGH 各派一个**独立复核 agent**，指令是"**默认推翻**"；
4. 另设**跨模块集成 agent**（专查单个模块看不见的数据通路）与**完整性批评 agent**（先问"还缺什么"）。

### 根本原因

⭐ **前四轮比的是"两份事实是否一致"，本轮比的是"照它做能不能跑" —— 两者能看见的东西不重叠。**

具体到每一条，根因是三种：

1. **"契约"从来没被写下来** —— 脉冲宽度、`i_seed` 非零、端口名以谁为准，
   这些是**跨模块的约定**，而每一份文档只管自己那一块，**所以每一处单看都对**。
   最典型的是第 3 条：顶层端口名有两个来源（文档模板 vs `.qsf`），
   **谁也没规定哪个是真值源**，于是两边都"有道理"。
2. **"改一处、忘一处"的第二种形态** —— 第 2 条的多重驱动，是**修 P0 缺陷时整段重写引入的**：
   重写的那一段自己自洽，但和**没重写的那一段**加起来就撞车。
   这与 ERR-0014（同一文件两处副本）、ERR-0017（跨文件副本）同族，**这是第三种**：
   **同一模块内两段代码的隐含约定**。
3. **"修正"本身引入了新错误** —— `rng_lfsr` 那一条：一次修正把**本来正确的本原多项式描述**
   改成了"非本原、周期仅 61"。**改完文档更自洽了，但事实错了。**
   **自洽不等于正确**（与 ERR-0014 的"封版 ≠ 自洽"是同一条的加强版）。

### 修复方法

修复顺序见 `docs/审查报告-2026-09-18.md` §6：
「**先冻结契约 → 再修硬伤 → 再对账 → 再补验证**」。

**进度（2026-09-18 第 13~14 次会话）**：

**✅ 已修 —— 报告 §1 的 12 条 CRITICAL/HIGH「已复核」项，全部清零**

`hw-01` 顶层端口名真值源（新增 `docs/01` §5.12/§5.13 + 两处"逐字可抄"改真名 + `CLAUDE.md` §10.1 立规）·
`keypad_scan-01` `r_phase` 多重驱动 ·
`keypad_scan-02`/`INT-1` **脉冲宽度改为 1 个 clk** ·
`pdf-1` 倒计时寄存器多重驱动（改 `r_cnt_load`） ·
`iface-01`/`INT-2` `clk_gen` 两处 `component` 模板补 `i_btn_rst` ·
`pattern_rom-1` 种子 bit0 与图案选择位冲突 ·
`piece_rom-1` 高/宽改位串字面量 ·
`INT-3` 键号 `case` 字面量 4 位→5 位 ·
`iface-02` 掩码"下移一行"方向反 ·
`CRIT-1` **时序约束**（`quartus/puzzle.sdc` + `build.tcl` 打印 Fmax + 更正无据断言） ·
`CRIT-3` SW7 补入硬件事实表与上板清单 ·
`puzzle_ctrl-1`/`game_fsm-05` `i_round_start` 时刻定死 ·
`dot_matrix_scan-1` §6.4 补完整骨架 ·
`arch-01` **DWG-01 补 `pattern_mask` 一笔、删掉指向「拼图核心」的多余节拍分接**（走完 §12.4 五步）

**✅ 已修 —— 已复核的其余 4 条（`sim-03` / `INT-4` / `INT-5` / `CRIT-2`）**

`sim-03` 仿真流程补两个绑定步骤（设顶层、设向量源）·
`INT-4` 散落序列器状态补声明、并写明"已放置并集"不得复用 `s_union` ·
`INT-5` 资源估算改成一张自洽的分解表（~600 LE，原 300 与 450 两处不自洽）·
`CRIT-2` 常量值只留 `docs/02` §2.6 一处，`docs/01` §5.0 改为只列名字

**✅ 已修 —— 从 80 条未复核项里挑出的「照抄编译不过」类（客观可确认，不必等整轮复核）**

`seg_scan-01` `seg_decode` 补出签名与函数体（原先只调用、无定义）·
`buzzer_ctrl-1` `o_buzz <= not o_buzz`（**VHDL-93 不允许读 `out` 端口**）改内部寄存器 ·
`buzzer_ctrl-3` `i_trigger` 的"边沿触发"口径更正为"单周期脉冲 + 电平装载"并写明后果 ·
`puzzle_ctrl-3` 明确 `r_anchor`/`r_locked`/`r_sel` **必须单写者**，散落器要并进同一进程

**✅ 已处理 —— 报告 §4 的 80 条 MEDIUM/LOW**

2026-09-18 第 15 次会话补了一轮**分批复核**（10 个 agent、每批 8 条、默认立场是「怀疑」）：
**80 条 → 75 确认 / 1 推翻 / 4 已修 / 0 存疑**（36 MEDIUM ＋ 39 LOW，**无 HIGH**）。
75 条确认项已按「分组产出编辑指令 → 主循环串行施加并校验唯一性」的方式全部改掉
（241 条编辑指令，226 条一次成功，其余经重新锚定后补完）；
另给 `check_pins.py` 补了 `--selftest`（`docs/04` §6 声称负向测试可复现，此前没有入口）。

> ⚠️ **批量施加暴露出一类新错误：「同一意图被实现了两遍」。**
> 多个 agent 各自独立地修同一条发现的不同侧面，两条编辑会落到同一处 ——
> 三条编辑各改各的、都「成功」了，结果却是**重复内容**：
> `CLAUDE.md` 一行表格出现两次、DWG-02 的 `piece_rom` 标签被两套写法各画一遍
> （且其中一行压住了 x=800/840 的竖线）。
> **防法**：批量施加后必须做一次**重复内容扫描**（相邻重复行 ＋ 同句频次 ＋ 同坐标 `text`），
> 并且**改图后仍要走 `CLAUDE.md` §12.4 第 3 步放大看** —— 那处压框正是渲染后才现形的。

**新增的护栏（防同一类错误再犯）**：

- `scripts/check_ports.py` **新增「顶层端口名 ↔ `.qsf` 的 `-to` 对账」** ——
  报告 `hw-01` 要的那道护栏。**已做负向测试**：把 §5.12 的 `seg` 改成 `o_seg`，
  脚本立刻报 `✗ 声明了顶层端口 o_seg，但 .qsf 里没有对应约束`。
- 同次修复中发现并修掉了该脚本自己的一个**静默误解析**：
  它原先只认 `### 5.N \`实体名\`` 一种标题写法，于是新增的 §5.12/§5.13
  匹配不上、被并进 §5.11 的 `buzzer_ctrl` —— **脚本照旧"通过"，结论却是错的**。
  （与 ERR-0014 "能跑的计数动作必须是准的"同源。）

### 预防措施

- ⭐ **契约要显式写下来，不能靠"每处自洽"** ——
  凡是**跨模块**的量（脉冲宽度、非零前置条件、名字以谁为准），
  必须在 `docs/01` §5 或 `CLAUDE.md` 里有一行**唯一真值源**。
- ⭐ **"默认推翻"的复核要成为审查的固定环节** ——
  本轮 12 条里有被**降级**的（`keypad_scan-01` CRITICAL→HIGH）、
  有**事实被更正**的（`arch-01` 图上实为 14 笔而非 15）。
  **没有复核的审查结论不能直接当行动依据。**
- ⭐ **审查要换轴，不是因为"再查一遍更保险"** ——
  第五根轴查出的问题，**前四根轴上全部不可见**（当时的文档是自洽的、甚至因为太自洽才错）。
  与 ERR-0014 的"不换轴，剩下的错误就是不可见的"完全同源。
- ⚠️ **本轮的两个已知盲区必须在下一轮补上**：
  ① **课件 PDF 当时不在工作树内**（`.gitignore` 的 `/*.pdf` 挡住了），
  "课件符合性"那一路 agent 只能读转述 —— **已把 PDF 复制到位**；
  ② **80 条 MEDIUM/LOW 全部未复核**，若其中藏有被低估的 HIGH，需下一轮补。

> ✅ **两个盲区均已闭合**（第 15 次会话）：① PDF 已复制到位；
> ② 80 条已按「10 个 agent、每批 8 条、默认立场是"怀疑"」复核完毕（75 确认 / 1 推翻 / 4 已修 / 0 存疑）。

---

## ERR-0021 第 15 次会话跳过收工流程：`HANDOFF.md` 表头与 `CHANGELOG.md` 双双留空 —— 同一坑第二次复发

- **日期**：2026-09-20（第 16 次会话开工时发现）
- **阶段**：会话收工
- **涉及模块**：`HANDOFF.md` / `PROGRESS.md` / `CHANGELOG.md` / `AI_LOG.md`
- **状态**：已解决（第 16 次会话代为补齐）

### 现象

第 16 次会话按 `CLAUDE.md` §0 读交接单，读到四处对不上：

1. `HANDOFF.md` 表头写着「**第 14 次会话 · 2026-09-18**」，
   而 `ERRORS.md` 里明写「**2026-09-18 第 15 次会话**补了一轮分批复核」——
   **第 15 次会话确实发生过，交接单却不知道**；
2. `HANDOFF.md` §3 **自相矛盾**：开头说「✅ 修硬伤四步已全部完成……现在可以开始写代码了」，
   正文「第一步 ~ 第四步」**又把那四步列成"你现在该做什么"**；
3. `CHANGELOG.md` 最后一节是第 14 次会话，**第 15 次会话一条记录都没有**；
4. `AI_LOG.md` 同样没有它的条目；`PROGRESS.md`「最后更新」与「上次会话断点」也停在 14。

而第 15 次会话做的是**实打实的工作**：`git show --stat db78064` 显示 **11 个文件、889 增 / 218 删**。

### 定位过程

1. `git log --oneline` 看提交顺序：第 14 次会话的收工提交是 `29db0ed`，
   之后还有 `a642107` / `db78064` / `988edc3` **三个提交**；
2. `git show --stat` 看它们**动了哪些文件**：`db78064` 动了 11 个
   （含 `HANDOFF.md` 7 行、`PROGRESS.md` 6 行、`CLAUDE.md` 37 行），**但没有 `CHANGELOG.md`**；
   `988edc3` 只动 `ERRORS.md`；
3. `git show db78064 -- HANDOFF.md PROGRESS.md` 看**具体改了哪几行**：
   只改了「连线表口径」那一条 ⚠️ 注 ＋ `PROGRESS.md` 的两行文档表 ——
   **`HANDOFF.md` 表头一行没动**，§3 里已完成的修复步骤也没删。

结论：**第 15 次会话改了内容，但没走收工流程。**

### 根本原因

⭐ **它不是"忘了"，是"没把自己当成一次会话"。**

第 15 次会话是接着第 14 次会话的活儿往下干的（第 14 次把 80 条标为"未复核"，
第 15 次就补了那轮复核）。**"续跑"在外观上不像开工**：不开新议题、不重读交接单，
于是收工时也想不起要走收工流程。它在 `ERRORS.md` 里老老实实写了「第 15 次会话」，
说明它**知道自己是第 15 次** —— 只是把"写 `ERRORS.md`"当成了收工的全部。

第 13 次审查的 `CRIT-4` 记的是**同一个坑**（表头连续两次没更新）。
当时的预防措施是「**把"表头"并入 `CLAUDE.md` §0 的收工条目**」——
**这一条改对了，但没生效**：它防的是"记得走收工流程、只是漏了表头"，
而第 15 次会话**整个收工流程都没走**，清单条目再多也够不着它。

### 修复方法

第 16 次会话收工时代为补齐：

- `HANDOFF.md`：表头 → **第 16 次会话 · 2026-09-20**；§2/§3 改为
  「四步已修完 ＋ 80 条已复核」；**删掉 §3 的"第一步 ~ 第四步"**（已完成的修复步骤），
  只留"写代码"那一段。
- `PROGRESS.md`：「最后更新」与「上次会话断点」补第 15、16 次；
  文档表里「审查报告」那一行的"它是下一阶段修复的唯一任务清单"改为已完成。
- `CHANGELOG.md` / `AI_LOG.md`：**补记第 15 次会话一节**
  （据 `ERRORS.md` ERR-0020 的进度表与 `git show db78064`，**不在场的事不编**）。
- `ERRORS.md`：ERR-0020 的**两处过期状态**（索引表「未解决」、条目内「未解决」）改为「已解决」。

### 预防措施

- ⭐ **判据从"哪一次会话"改成"这一轮有没有产生提交"。**
  收工流程的触发条件**不是"新会话"**，而是**"本次对仓库做了改动"** ——
  续跑、补做、临时插进来的一轮，**只要落了盘，就是一轮**，就该走完整收工。
  这条已写进 `CLAUDE.md` §0，**取代**原先"每次会话结束前"这种以"会话"为单位的措辞。
- ⭐ **表头是唯一的"会话存在证明"，所以它必须最先改。**
  本次能查出来，全靠 `CLAUDE.md` §0 第 0(a) 条点名了表头；
  反过来，**若第 15 次会话连 `ERRORS.md` 都没写，这次就完全查不出来**。
- **收工完成与否要有可数判据**：`git status` 干净 **且**
  `HANDOFF.md` / `PROGRESS.md` 两处日期都是今天。
  —— 与 `CLAUDE.md` §10.3「可数动作」同源：**能数的事不要靠记。**

---

## ERR-0022 精简 `CLAUDE.md` 前报了"减到 ~800 行"，实际只到 1009 —— 数量可数，我却估了

- **日期**：2026-09-20
- **阶段**：文档（`CLAUDE.md` 精简化）
- **涉及模块**：`CLAUDE.md`（＋ `docs/01-系统设计.md` 一处反向引用）
- **状态**：已解决（结果如实记录，未向目标凑数）

### 现象

用户要求精简 `CLAUDE.md`。动刀**前**给用户的方案里写着：

| | 动刀前 | 方案承诺 | 实得 |
|---|---|---|---|
| 行数 | 1130 | **~800** | **1009**（精简本身 1003 ＋ 收工时按 ERR-0021 给 §0 补 6 行判据） |
| 字符 | 37 828 | — | 36 481（**−3.6%**） |

偏差 **209 行 / 26%**，且这个数字是**当作"档 2 的收益"写进方案、经用户点头之后才动的手**。

### 定位过程

1. 11 处删改做完，一量：**1009 行**（与承诺差 209 行）——
   说明不是"手软"，是**目标本身错**；
2. 按小节统计剩下的体量，看"胖"在哪：

   | 小节 | 行 | 小节 | 行 |
   |---|---|---|---|
   | §4 硬件速查 | 139 | §10.2 资源约定 | 114 |
   | §5 构建/仿真 | 133 | §11 考核对照 | 98 |
   | §12 图表规范 | 116 | §10.3 顶层例化 | 81 |

3. 逐节问「这一行消失了，会不会有人踩坑」—— 六个大节**全部答"会"**：
   §4.2 的引脚号 vs 位序陷阱、§5.3 的三条实测硬限制、§10.2 六条、§11.4 合规台账、
   §12.4 五步清单（它抓到过 6 处缺陷）。**它们不是冗余，是做过的事。**

### 根本原因

**拿一个不相关的量去外推。**

方案的算法是：「表格占全文 46% 字符，删掉表就能减掉将近一半」。
但**表格 ≠ 冗余**。本文件里的表分两类，与"可删性"无关：

| 类 | 例 | 可删？ |
|---|---|---|
| **事实的副本**（别处另有真值源） | §4.1 引脚号、§10.3 的 `clk_gen` 端口列表 | ✅ 删掉只会**少一个会漂移的第二真值源** |
| **判断本身**（只在本文档有） | §4.2 引脚号 vs 位序、§10.2 六条、§12.4 五步、§11.4 台账 | ❌ 删掉就是**删规则** |

**第一类在前 9 次会话里已经被删过一轮**（第 10 次会话"专门找矛盾、专门删重复"，净减约 40 行）。
剩下的**绝大多数是第二类** —— 而我用"表格占比"去估"可删量"，
等于**拿字符数当可删量的代理**，两者不相关。

### 修复方法

- 按「**删事实副本、留陷阱警示**」改 11 处：**1130 → 1009 行（−10.7%）**、
  **37 828 → 36 481 字符（−3.6%）** —— 其中 6 行是收工时按 ERR-0021 的预防措施给 §0 补的判据；
- 交付时**如实报数，不向 800 凑** —— 差额对应的正是方案里那句
  「**全部规则与 `⚠️` 一字不动**」；再往下减就是减规则，与授权相冲突；
- 不变式逐项核对：`⚠️` **36 个一个没少**、`⭐` 原有 **26 个一个没少**（收工时 +1 → 27）、
  **56 个小节标题全在**、本文件内 `§x` 自引用**零悬空**；
  两个校验脚本 `check_pins.py` / `check_ports.py` 仍为绿。

### 预防措施

- ⭐ **给数量目标之前先量一遍。** 本次是"先写方案、后量体量"，
  正确顺序是**量完再写进方案**。与 ERR-0013「对账要走两遍」、
  `CLAUDE.md` §10.3「可数动作」同源：**能数的事就用数，不要用估。**
- ⭐ **删文档内容前，对每一处问一句「这是事实的副本，还是判断本身」。**
  判据不是"别处有没有"，而是**"这一行消失了，会不会有人踩坑"** ——
  `CLAUDE.md` 每次会话自动进上下文、`docs/*` 是按需读的，
  所以"别处也有"**不构成可删的理由**。
- **改完照做反向对账**：全库 `grep` 一遍 `见 CLAUDE.md §X`，本次查出 **1 处**悬空
  （`docs/01` §5.12「引脚表见 `CLAUDE.md` §4.1」，而 §4.1 已不再列引脚号）
  → 已改指 `docs/04` §2。
- **重复内容扫描照做**（ERR-0020 的预防措施）：相邻重复行 / 长行 / 长句三类**全为零**。

---

## ERR-0023 白板框图留档稿与定稿口径有 3 处冲突，照它出图会与文档自相矛盾

- **日期**：2026-09-20
- **阶段**：设计文档（初步设计方案出图）
- **涉及模块**：`puzzle_ctrl` / `keypad_scan` / `seg_scan` / `dot_matrix_scan`
- **状态**：已解决

### 现象

准备把用户提供的「优化后框图」插入初步设计方案时，逐段放大核对，发现它与 `docs/01` 的定稿口径有 3 处不一致：
①「拼图核心」框上多画了一条红色节拍引线；②「键盘输入」副标题写 `20ms 防抖`；
③「显示子系统」副标题写 `tick_1k`。

### 定位过程

1. 先确认图片来源：与 `docs/图/DWG-01-系统总体框图-飞书原稿.png` 逐像素比对 → 内容一致，
   说明它就是留档稿的重新导出，**不是新版本**。
2. 再对着 `docs/01` §2.1 连线表第 0 条（"S4 拼图核心与 S5 图案与随机数据**不接任何节拍**"）
   与 §5.8（`puzzle_ctrl` 只有 `clk`/`rst`）核 ① —— 图上那一笔**表里没有**，即 ERR-0020 审查已删掉的那条。
3. 对 §4.2 节拍表核 ②③ —— 20 ms 是 **BTN0 复位键**的消抖窗口（在 `clk_gen` 内），
   矩阵键盘是 80 ms；显示扫描在 2026-09-18 审查中由 `tick_1k` 改为 `tick_8k`（否则刷新率仅 62.5 Hz）。

### 根本原因

留档稿停在 2026-09-18 那轮审查**之前**的口径；而 `CLAUDE.md` §12.3 只要求"矢量图与文档同步"，
**没有规定留档 PNG 也要同步**，于是 PNG 成了事实上的第三份真值源。

### 修复方法

- 按定稿口径修正该图并另存，插入文档时用的是修正版：
  `report/图/图2-1-系统总体框图（第一层·功能子系统）.png`、`report/图/DWG-01-白板排版-已按定稿口径修正.png`
- 修正手段：按行取非红像素中位色抹除多画的引线（阈值取 `r-g>5` 才不漏抗锯齿边）；
  按原文字实测宽度反解字号后重绘两条副标题
- 文档正文与图注统一采用定稿口径，并在 CHANGELOG 中登记

### 预防措施

- **插图前先核"这张图是哪一版"**：与留档件逐像素比对，比"看着差不多"可靠。
- `docs/图/DWG-01-系统总体框图-飞书原稿.png` 属留档件，**引用时一律以 `系统图.html` 矢量版为准**；
  需要位图就从矢量版导出，不要直接引用留档稿。

---

## ERR-0024 文档转换链丢失全部图表题注、图片按自然像素插入

- **日期**：2026-09-20
- **阶段**：设计文档（Word 交付物生成）
- **涉及模块**：`report/初步设计方案.docx`（生成链而非 RTL）
- **状态**：已解决

### 现象

HTML 定稿转 DOCX 后逐项复核发现：**12 个表题与 3 个图题在文档里一个字都没有**；
同时点阵框图被插成 **14.0×7.5 cm 之外的另一版**——首轮转换时它宽达 **133.8 cm**，远超 A4 版心。

### 定位过程

1. 解包 `word/document.xml` 逐字符串查找表题文本 → 全部缺失，确认不是"渲染小"而是**没生成**。
2. 读转换器源码 `body_paragraph_styler.py` / `style_mapper.py`：段落级样式只处理
   `border / background / margin / text-indent / line-height / font-size`，
   `<caption>` 与 `<figcaption>` 不在其元素映射范围内 → 被静默丢弃。
3. 图片问题读 `image_handler.py`：宽度取 `<img>` 的 `width` 属性（px ÷ 37.795 折 cm），
   否则回落到"自然像素 ÷ dpi"，而 PNG 无 dpi 信息时按 72 dpi 计 → 3794 px 宽算出 133.8 cm。

### 根本原因

转换器**静默降级**：不认识的元素直接跳过、缺失的属性回落到估算值，两者都不报错不告警。

### 修复方法

- 题注改用独立段落承载：表题 `<p class="tbl-cap">` 放表前、图题 `<p class="fig-cap">` 放图后，
  原 `caption` / `figcaption` 保留（转换时被忽略，不影响输出）
- 给每个 `<img>` 显式写 `width` 属性（图 2-1 560 / 图 2-2 520 / 图 4-1 620 px），
  转换后分别为 14.0×7.5、13.8×16.8、14.0×9.8 cm
- 另修一处：整幅白底 `<rect>` 会被 `check_svg.py` 当作"方框"，导致所有走线被判"横穿方框"，
  并入 `系统图.html` 时已去掉白底（`.canvas` 自带白底）

### 预防措施

- **转换后必须回读产物逐项验数**：题注条数、图片尺寸、文字颜色、表格行列数都要数出来，
  不能只看"转换成功"的返回值。
- 把上面三项写进交付前的复核清单；同类"静默降级"在渲染链里还会再遇到。

---

## ERR-0025 审查报告把本机绝对路径写进了入库文档

- **日期**：2026-09-21
- **阶段**：文档规范化 · 推送前敏感信息检查
- **涉及模块**：`docs/审查报告-2026-09-18.md`（已入库并推送）
- **状态**：已解决（工作区）；**历史提交未清理**，见「预防措施」第 3 条

### 现象

推送前按 check-list 扫描敏感信息，发现该文件里有 **67 处**形如
`/Users/<用户名>/WorkBuddy/Worktrees/dld-lab/<工作树编号>/docs/02-模块详细设计.md:570-581`
的**本机绝对路径**。

泄露面有三层：① 本机用户名；② 本机目录结构（`WorkBuddy/Worktrees/`）；③ 工作树编号。
而且这类路径对任何其他机器**完全无效** —— 引用价值为零，纯负担。

### 定位过程

1. **先定 check-list，再动手扫**（不是"凭感觉找一找"）：
   姓名学号 / 密钥令牌密码 / 邮箱 / 手机号 / 身份证 / **本机绝对路径** / 生成文件的元数据。
2. `grep -rIn '/Users/'` 命中两处：`docs/审查报告-2026-09-18.md`（67 处）与 `.workbuddy/memory/*.md`（1 处）。
3. 逐个确认是否会入库：`.gitignore` 已含 `.workbuddy/` → 后者不上传，**无需处理**。
4. 对前者抽取全部匹配并按前缀归类 → 确认**只有一种形式**
   （`/Users/<user>/WorkBuddy/Worktrees/dld-lab/main-224414b8/`），
   另有 1 处是被截断的残片 `② /Users/…`（某次编辑的遗留）。
5. 顺带核对同类风险源：`report/*.docx` 的 `docProps/core.xml`（`dc:creator` = `python-docx`）、
   `report/*.pdf` 的元数据（仅 `Typst 0.15.1`）—— **均无个人信息**。

### 根本原因

审查报告是**逐条引用原始发现**写成的，而原始发现里带的正是**审查当时本机工作树的绝对路径**；
写文档时直接粘贴，没有问过"这个路径对读者有意义吗、会不会带出本机信息"。
更根本的原因是：**此前从未把"推送前扫敏感信息"作为一步**，这类内容因此一路推了上去。

### 修复方法

- 将 67 处 `/Users/<user>/…/main-224414b8/` 前缀**整体删去**，路径回落为**仓库相对形式**
  （如 `docs/01-系统设计.md:223`）—— 引用价值不变，无意义的泄露面消失
- 清除 1 处截断残片（`；② /Users/…` → `；`）
- 在报告表头补「归档说明」，写明文中路径一律相对仓库根

### 预防措施

1. ⭐ **"推送前扫敏感信息"固定为流程的一步，且先定 check-list 再扫** ——
   凭感觉找必然漏项；check-list 最少覆盖：密钥/令牌/密码、个人身份信息、
   本机绝对路径、生成文件的元数据（docx / pdf）。
2. **文档里引用路径一律写仓库相对路径** —— 绝对路径换台机器就断，对读者零价值，
   却稳定带出本机信息；**写的时候就不该产生它**。
3. ⚠️ **本次只清理了工作区，历史提交里仍带着这些路径。**
   已经推送到公开仓库的历史不会被这次改动抹掉；要彻底清除需重写历史并强推
   （破坏性操作，另行决定）。

---

## ERR-0026 整机设计放不进 EPM1270：3461 个 LE / 只有 1270 个

- **日期**：2026-09-23
- **阶段**：模块编写 → 首次全流程编译（`puzzle_top`）
- **涉及模块**：`puzzle_ctrl`（主犯）· `disp_format`（次犯）
- **状态**：已解决（1226 / 1270，占用 97%）

### 现象

`quartus_sh --flow compile` 在 **fitter** 阶段失败：

```
Error: Design contains 3434 blocks of type logic cell.  However, device contains only 1270.
Error: Can't fit design in device
```

综合报告（map）给出的总量是 **3461 个 LE**，而 EPM1270T144C5 只有 **1270 个** —— 超了 **2.7 倍**。
按实体分解，问题**只在两个模块**上：

| 模块 | LE | 占比 |
|---|---|---|
| `puzzle_ctrl` | **2587** | 75% |
| `disp_format` | 279 | 8% |
| 其余 9 个模块 + 顶层连线 | 595 | 17% |

### 定位过程

1. **先看总量再看分布**：map 报告里有 `Resource Utilization by Entity` 表，一眼就看出
   不是"到处都大"，而是 `puzzle_ctrl` 一个模块占 75%。这一步把排查范围从 11 个模块缩到 1 个。
2. **对着代码数它的面积花在哪**：`puzzle_ctrl` 里只有三处会产生 64 位宽的组合逻辑：
   ① 4 块零片的像素掩码（`place()` 平移 + 配色）；
   ② 散落时的"已放置并集"（又一份 4 路平移）；
   ③ 候选位置的掩码（再一份平移）。
   合计 **9 份 64 位平移逻辑**，而每份按"8 行 × 8 列逐格搬运"展开约 250~300 个 4 输入 LUT
   → 与实测 2587 吻合（估算与实测在此对上，才敢确认是这一处）。
3. **验证"是不是 Quartus 优化不到位"**：把 `OPTIMIZATION_TECHNIQUE` 从 AREA 改成 BALANCED
   试编译一次，数量只差个位数 —— 说明**不是优化器的问题，是写法本身太宽**。
4. 于是按 `CLAUDE.md` §10.2 ④「共享运算资源」给出**面积换时间**方案：
   **同一套平移逻辑分时复用**（4 块 × 8 行 = 32 拍刷一帧 0.64µs），
   并把散落的"掩码求交"换成**只用整数比较的包围盒判据**。

### 根本原因

设计阶段把"每一块零片的掩码**组合地**算出来"当成了唯一写法
（文档原方案），而这只在"面积不敏感"时才成立 —— EPM1270 的 1270 个 LE 是硬约束。
更根本的一点：**原方案的 LE 预估（`docs/01` §9 写 ~600）是拍脑袋相加的**，
既没有区分"组合逻辑宽度"和"触发器个数"，也没有在写下之后立刻用一次编译去证伪它。
**估算没有写进流程，就等于没有估算。**

### 修复方法

- `rtl/puzzle_ctrl.vhd`：删掉 `place()` / `union_placed()` 两个组合函数与
  "4 路并行展开"的像素进程，改为**行扫描引擎**（`r_eng_p` / `r_eng_row` 计数，
  每拍算 1 块零片的 1 个输出行，结果累加进 `r_acc_red/r_acc_grn`，
  行定型时整行移入 `r_px_red/r_px_green`）；
- 散落的重叠判定改为**包围盒不相交**（4 个整数不等式，保守但绝不放过真实重叠）；
- 引擎只在**事件**（开局 / 落子 / 选择 / 移动 / 锁定）时刷新一帧，完成后静止；
- `o_px_red/green` 因此从"纯组合"变成**寄存输出**（功能等价，慢 0.64µs）；
- `disp_format` 的 64 位多路选择被综合器合并，从 279 降到 113，未改代码。

### 预防措施

1. **写宽位逻辑（≥32 位）之前先估面积，并在第一次编译后立刻核对**：
   `docs/01` §9 的 LE 分解表要按"组合宽度"和"触发器数"分别估，再取二者较大者量级。
2. **本设计后续凡是"再来一份同样的运算"的写法，一律先问"能不能分时复用"** ——
   本项目对速度无要求，对面积有要求（`CLAUDE.md` §10.2 的总原则）。
3. 过程记录：`docs/01` §9 的资源预估表已按实测数据订正（见下方"修复方法"的数值）。

---

## ERR-0027 时序不收敛：Fmax 只有 38.4MHz，而时钟是 50MHz

- **日期**：2026-09-23
- **阶段**：布局布线后的时序分析（`quartus_sta`）
- **涉及模块**：`puzzle_ctrl` · `game_fsm` · `piece_rom`
- **状态**：已解决（Fmax = **63.66 MHz**，最差裕量 +4.06ns）

### 现象

LE 超限解决后，全流程编译通过，但 TimeQuest 报：

```
Fmax Summary
; 38.42 MHz ; 38.42 MHz ; clk ;
```

50MHz 要求 20ns 周期，而最差路径的数据延迟 **25.7ns**（裕量 −6.0ns）。最差路径全是：

```
; -6.027 ; game_fsm|r_state[2]            ; puzzle_ctrl|r_mismatch ;
; -5.995 ; puzzle_ctrl|r_anchor[0][3]     ; puzzle_ctrl|r_mismatch ;
```

### 定位过程

1. **读最差路径的两个端点**（TimeQuest 的 `Setup: 'clk'` 表给了 From/To Node），
   立刻锁定：终点只有 `r_mismatch` 一个 —— 说明**不是"到处都慢"，而是一条路径独占**。
2. 对着代码拆这条路径：`r_anchor`（寄存）→ 取相对掩码行 → 列移位 → 配色 → 与行累加器相或
   → **与目标行比较** → `r_mismatch`（寄存）。比较这一级是"用组合值去比"，
   等于把前级所有延迟全串进来了。
3. **第一次修复**：把判定挪到独立的"定型拍"（`r_eng_chk`），比较改成对**已寄存的整幅画面**
   与目标掩码作比较。Fmax 38.4 → **44.2MHz**，裕量 −6.0 → −2.6ns。
4. **再读新的最差路径**：终点变成 `r_try`（散落序列器），起点变成 `game_fsm|r_state`
   —— 路径是 `r_state → o_pattern_sel → piece_rom → 包围盒判据 → 序列器`。
   这说明**问题已经换了一条路**（不是原来那条没修好）。
5. **第二次修复**：把 `o_pattern_sel` 改成**寄存输出**（`game_fsm` 内 1 拍）。
   44.2 → **44.2MHz 附近**（−1.2ns），起点变成 `r_pattern_sel`（已寄存），
   剩下的仍是 `piece_rom → 包围盒比较 → 序列器` 这一段。
6. **第三次修复**：把 `puzzle_ctrl` 用到的图案参数（`i_height` / `i_width` /
   `i_piece_count`）**寄存一份内部副本**，并把**包围盒判据的结果寄存一拍**
   （散落序列器从"两拍一次尝试"改成"三拍一次尝试"）。→ **Fmax 63.66MHz、裕量 +4.06ns** ✓

### 根本原因

**"寄存器 → 组合逻辑 → 寄存器"这条链被跨模块拉长了**：
`game_fsm` 的状态寄存器 → 组合输出的图案选择字 → `piece_rom`（纯组合查找表）
→ `puzzle_ctrl` 的组合判据 → `puzzle_ctrl` 的寄存器。
四个模块里每一段单独看都不长（2~5ns），但**没有任何一级中间寄存器**把它们隔开，
于是一条 20ns 的预算被四段分掉还不够。

### 修复方法

- `rtl/puzzle_ctrl.vhd`：新增 1 个"定型判定拍"（`r_eng_chk`），
  判定改为对着**寄存的整幅画面**比较；
  新增 `r_h` / `r_w` / `r_count` 三个寄存副本（27 个触发器）承载图案参数；
  新增 `r_free`（包围盒判据的寄存结果），序列器改为三拍制（`ST_STEP`/`ST_TRY`/`ST_DEC`）；
- `rtl/game_fsm.vhd`：`o_pattern_sel` 改为寄存输出（`r_pattern_sel`）；
- `rtl/clk_gen.vhd`：上电复位改为数 `tick_1k`（19 位计数器 → 4 位），
  腾出约 25 个 LE 给上面新增的触发器。

### 预防措施

1. ⭐ **跨模块的组合链要主动数"中间有几级寄存器"**：本项目最长的一条链横跨
   `game_fsm → piece_rom → puzzle_ctrl`，四个模块谁都不觉得是自己的问题 ——
   **模块边界处最容易忘记打拍**。
2. **时序不收敛时，看"端点"比看"路径"快**：先确认最差路径的 From/To，
   再看它是"很多条都慢"还是"一条独占"。本次三次迭代都是靠这一步把范围缩到一行代码。
3. **每修一次都要重新读最差路径**：本次修完第一处后，最差路径立刻换了另一条
   —— 若"照着原来的报错改完就收工"，会把剩下的违例留在器件里。
4. 时序报告要留原始数据（`quartus/puzzle.sta.rpt` —— ⚠️ Quartus II 9.1 的产物直接
   写在工程目录 `quartus/` 下，**没有 `output_files/`**），
   报告评分项④要"资源利用情况"，把 Fmax 与 LE 一起写进去。

---

## ERR-0028 Quartus 里中文注释全变乱码（编译却正常）

- **日期**：2026-09-23
- **阶段**：首次在 Quartus 里查看源码（用户打开 `puzzle.qpf` 时发现）
- **涉及模块**：`rtl/` 全部 14 个 `.vhd`
- **状态**：已解决（编码统一为 GBK + 一条命令可切换）

### 现象

用 Quartus II 9.1 打开 `rtl/board_test_top.vhd`：**代码标识符（`entity` / `port` / `clk`…）
显示完全正常，只有中文注释变成一串看不懂的字符**；编译综合却**一个警告都不报、全部通过**。
——"能编译、但看不懂注释"，正是答辩现场打开工程时最尴尬的一类问题，
也让实验报告评分项⑧（"顶层文件的 HDL 代码，有注释"）没法截图。

### 定位过程

1. **先分清"是文件坏了还是显示坏了"**：用 Read 工具（按 UTF-8）读同一个文件，
   中文注释**完全正常** → 说明**文件内容是好的，是 Quartus 的读法不对**。
2. **查 Quartus 的默认解码方式**：搜到权威结论 ——
   **Quartus II 11.0 及以下版本的编辑器固定按系统 ANSI 解码源文件**（12.0 起才改 UTF-8），
   而 9.1 **没有"以指定编码打开"的选项**（那是 Quartus Prime 较新版本的功能）。
3. **量出"系统 ANSI"到底是什么**：读注册表
   `HKLM\SYSTEM\CurrentControlSet\Control\Nls\CodePage` 的 `ACP` = **936**（GBK/CP936）。
   于是确定：Quartus 按 GBK 读，而文件是 **UTF-8 无 BOM** → 中文必然乱码。
   （若 ACP = 65001，则说明系统开了"UTF-8 全球语言支持"，那是另一种情形。）
4. **确认不是"文件本身该用 UTF-8"**：本仓库的 `.md`/`.py` 一直是 UTF-8 且工作正常，
   问题只出在 `.vhd` 被 Quartus 读这一环。

### 根本原因

**同一个坑的两半：写的一方与读的一方编码不一致。**
本项目此前所有文本（含 `.vhd`）统一按 UTF-8 无 BOM 写，
而 **Quartus II 9.1 这个 2009 年的工具按系统 ANSI(GBK) 读**，且无法切换 ——
于是"编译通过、显示乱码"。这与 `CLAUDE.md` §5.4 记录的
"PowerShell 5.1 按 ANSI 解码 UTF-8 文档、静默读出乱码"是**同一类错误、不同软件**：
**静默乱码不报错，因此不会有人去查它。**

### 修复方法

- 新增 `scripts/encoding.py`：`rtl/*.vhd` 的 **UTF-8 ⇄ GBK** 双向转换，带三道保护 ——
  ① **往返安全校验**（目标编码装不下就**拒绝**，绝不静默写 `?`）；
  ② **幂等**（已是目标编码则跳过）；③ **只碰 `rtl/*.vhd`**，不动任何文档/脚本；
- 转换时把 GBK 装不下的装饰符号按固定表替换
  （`⚠️`→`※`、`⭐`→`★`、`µ`→`μ`、`−`→`-`、`↔`→`<->`、`⟺`→`当且仅当`、`⑪⑫`→`(11)(12)`），共 91 处；
- `rtl/*.vhd` 现为 **GBK**；`docs/*.md` `*.py` `*.qsf` 等保持 **UTF-8**；
- 约定写入 `CLAUDE.md` §5.4（含"**AI 改完代码要重跑 `encoding.py gbk`**"）与
  `docs/06-Quartus编译与烧录流程.md` §8。

### 预防措施

1. ⭐ **"编译通过"不等于"在 Quartus 里看得懂"**：源码编码要按**读者工具**来定，
   而不是按最方便的编辑器来定。凡"给某个老工具读的文件"，先确认它按什么编码读。
2. ⭐ **AI 工具（Write/Edit）产出的永远是 UTF-8** —— 每次用 AI 改完 `.vhd`，
   **必须重跑 `python scripts/encoding.py gbk`**。已并入收工动作。
3. **不要打开 Windows 的 "Beta: 使用 Unicode UTF-8 提供全球语言支持"**：
   它会把 ACP 变成 65001，与"源文件用 GBK"这一方案互斥（二选一，别混）。
4. **转换前先关掉 Quartus 里打开的文件**；它提示"文件已被外部修改"时选 **Reload**，
   **不要选保存**（否则用编辑器里的旧内容覆盖转换结果）。
5. 转换后**必须重新编译一次**验证：本次实测两个顶层 LE 与 Fmax **一字不差**
   （`puzzle_top` 1226/1270、63.66MHz；`board_test_top` 439 LE）→
   证明"编码只影响显示、不影响综合"，这条结论也写进了 `docs/06` §8。

---

## ERR-0029 ERRORS 索引表缺 6 行 + 多份过程文档状态停在"编码之前"

- **日期**：2026-09-24
- **阶段**：文档审查（全量改动复核）
- **涉及模块**：`ERRORS.md` / `README.md` / `HANDOFF.md` / `PROGRESS.md` / `CLAUDE.md` / `docs/01-系统设计.md`
- **状态**：已解决

### 现象

按用户要求"审查全部改动、检查是否准确 / 有遗漏 / 有冲突"，逐文件核对后发现两类问题：

① **`ERRORS.md` 的问题索引表缺 6 行**：表里只有 ERR-0001~0022（26 行），而正文有 28 条 ——
ERR-0023~0028 全都不在索引里。进一步看，其中 **ERR-0025~0028 那 4 行被误追加到了
ERR-0025 正文的末尾**（"预防措施"第 3 条之后），ERR-0023 / ERR-0024 则**从未进过索引表**。
另外「新增记录模板」块夹在 ERR-0024 与 ERR-0025 之间，把条目序列切成了两段。

② **多份过程文档的状态停在"编码之前"，且与自身其它小节直接矛盾**：

| 文件 | 陈旧说法 | 实际状态 |
|---|---|---|
| `README.md` | 阶段表 2/3/4/5 全 "⬜ 未开始"；构建节"工程当前不可编译…`rtl/` 仍为空" | 阶段 2/3/4 已完成，`rtl/` 下 14 个文件、两个顶层全流程编译通过 |
| `HANDOFF.md` §7 | "`quartus/` 工程现在还不能编译（`.qsf` 里列的 6 个 VHDL 文件在 `rtl/` 下还不存在）" | 同文件 §2 已写"RTL 全部写出、两个顶层编译通过" —— **自相矛盾** |
| `PROGRESS.md` 顶部 | "⛔ …当前该做的是按 `docs/审查报告` §6 的顺序修，四步完成后再进阶段 2" | 该四步已由第 14/15 次会话执行完毕，此指令已作废 |
| `PROGRESS.md` 文档进度表 | "`ERRORS.md` **25 条记录**（ERR-0001~0025）" | 实为 29 条；且缺 `docs/06` 与第 19 次会话新增的 5 个脚本 |
| `CLAUDE.md` §12.3 | "DWG-01 的权威版本是 `系统图.html` 的矢量版" | 与 §12.1.1"对外提交用用户定稿版 PNG"冲突 |

### 定位过程

1. **先做可数对账，不靠通读**：脚本枚举行首 `| [ERR-…]` 得 26 行、枚举 `^## ERR-` 得 28 条 →
   差额 2，说明有编号缺失（而不是"我看着像少了"）。
2. **打印索引行的行号**：索引集中在第 30~51 行，却有 4 行孤零零出现在第 **2083~2086** 行
   （正文中段）→ 精确定位到"追加时落错了位置"。
3. **两两对照同一文件的不同小节**：`HANDOFF.md` §2（已完成）vs §7（还不能编译）→ 判定 §7 是陈旧副本。
4. **对状态类句子做全仓搜索**：`grep -rn "还不能编译|仍为空|未开始" *.md` → 一次抓出
   README / HANDOFF / PROGRESS 共 5 处，比逐份通读更快也更全。

### 根本原因

- ⭐ **索引表是"手工维护的第二份真值源"**。新增记录的顺手动作是"复制模板 → 写正文 → **追加到文件末尾**"，
  而"回到顶部索引表插一行"是**另一个动作** —— 两个动作就容易只做一个。
  ERR-0021 / ERR-0022 记的两类"漏更新"是同一成因：**该改的地方不止一处，而只改了一处**。
- ⭐ **状态类文字没有被任何"可数动作"触发更新**。收工清单（`CLAUDE.md` §0）只要求改
  `HANDOFF` 的表头与 §2/§3/§4，**没有要求"搜一遍全仓还有没有描述旧状态的句子"** ——
  于是"工程不能编译"这类句子在编码完成后原样留了下来。

### 修复方法

- **`ERRORS.md`**：按编号把 ERR-0023~0028 六行补进顶部「问题索引」表；
  删除第 2083~2086 行那 4 行误植内容；「新增记录模板」块移到**文件末尾**
  （它自带"复制下面的模板"，逻辑上属于文末）；本条（ERR-0029）同步进索引表。
- **`README.md`**：「项目状态」阶段表更新为 2/3/4 已完成；「构建」节改为"已可编译"并给出实测数据；
  目录结构补 `docs/06`；修正「文档索引」里一个**只有表头、没有数据行**的空表。
- **`HANDOFF.md`**：§7 第 1 条改写为"工程已可编译"；§6 工具链列表补入本次新增的脚本。
- **`PROGRESS.md`**：删掉已作废的 ⛔ 段；「文档进度」表把 ERRORS 条数 25→29、
  补 `docs/06` 与第 19 次会话新增的 5 个脚本、补用户定稿版框图。
- **`CLAUDE.md`**：§12.1.1 引言与 §12.3 统一口径（**内部对账版 = `系统图.html`；
  对外提交版 = 用户定稿版 PNG**）；§0 收工清单补入"转码"这一步
  （§5.4 早已写"要并入收工清单"，却一直没并 —— 又一处"声明了但没执行"）。
- **`docs/01`** §2.1 的「图的定稿版」注同步为对外提交版口径。

### 预防措施

1. ⭐ **索引表要有可数校验**：新增一条记录后跑一次对账，两者必须相等 ——
   `grep -c '^| \[ERR-' ERRORS.md` 应等于 `grep -c '^## ERR-[0-9]' ERRORS.md`。
   **能数的事不要靠记**（与 ERR-0014 / ERR-0022 同一条原则）。
2. ⭐ **收工不只"改该改的"，还要"搜不该留的"** ——
   `grep -rn "还不能编译\|仍为空\|未开始" *.md` 这种全仓搜一遍。
   **改状态是"改一处、搜全仓"的动作，不是"改一处"的动作。**
3. **可复制的模板块统一放文末**，别插在条目之间：它会把"记录的序列"切成两段，读起来像缺了一条。
4. ⭐ **"写进清单"与"真的在清单里"是两件事**：`CLAUDE.md` §5.4 声明"要并入收工清单"，
   但 §0 里始终没有这一条 —— **凡是写下"应并入 X"的句子，当场就去 X 里加上**，否则那句话本身就是下一个缺陷。

---

## ERR-0030 上板清单写"SW7=0 → LED 全灭"，实现里 `ld` 根本不受门控

- **日期**：2026-09-24
- **阶段**：上板（首次真正通电，跑 `board_test_top` 硬件自检）
- **涉及模块**：`rtl/board_test_top.vhd`（未改） / `docs/04` §7.1 · §7.3 / `docs/01` §8.2 /
  `docs/06` §4.2 · §6.1 / `docs/05`
- **状态**：已解决（**改文档、不改代码** —— 结论是"实现是对的，清单错了"）。
  ⭐ **13:38 现场回证**：按住**最右边**那个按键 → **16 个 LED 全亮**、时钟档位确认 **50MHz**、
  SW7=1 的 LED 画面"无规则但有周期"（= 阶段号 + 子步号，正常）——
  **30 秒判据四步全过**（`docs/05` §1.5），ERR-0030 里的"按住 BTN0 不亮"悬案就此闭合：
  当时就是**按错了键**。

### 现象

用户在现场报了两条现象，都指向上板清单与实现不符：

```
① 烧录后拨动 SW7 → "不是正常自检现象，而是非常混乱"
② 按住 BTN0 不松（期望 16 个 LED 全亮），但 SW7 为 0 时
   "LD8 这个灯亮黄色，其他灯是灭的"
```

而 `docs/04` §7.1 第 11 条的期望现象写的是：

```
| 11 | SW7 系统开关 | 拨到 0 再拨回 1 | 拨 0 → 数码管/点阵/LED 全灭、蜂鸣器无声；… |
```

第 10 条也没交代"按的是哪个键"：

```
| 10 | BTN0 复位键 | 按住 BTN0 | 16 个 LED 全亮；松开后自检从阶段 1 重来 |
```

### 定位过程

1. **先把"现象"翻译成"哪几个信号应该是什么值"**：SW7=0 时，`clk_gen` 的复位方程是
   `o_rst <= r_btn_rst or r_por_rst or (not sys_en)` —— `sys_en` 就是 `sw7`，
   故 **SW7=0 → `o_rst` 恒为 `'1'`**。`board_test_top` 的流程进程第一句就是
   `if s_rst = '1' then r_stage <= 1; r_step <= 0; …` →
   **整个自检被按在阶段 1、子步 0**。
2. **算阶段 1 的 LED 画面**：`s_ld(15 downto 8) <= to_unsigned(r_stage, 8)` = `"0000_0001"`；
   `s_ld(7 downto 0) <= (others => r_b1)`，而 `r_b1` 同样被复位按住 = `'0'`。
   → `s_ld = "00000001_00000000"`，即 **只有 `ld[8]` 为高**。
   按 `docs/04` §2 的引脚表 `ld[8] = PIN_144 = 板上 LD8` → **只有 LD8 亮**。
   ⭐ **与用户报的现象逐字吻合** —— 现象本身没有异常，是**清单写错了**。
3. **回头读输出级那 7 行**（`board_test_top.vhd` 末尾）：门控的只有
   `seg` / `cat` / `dot_row` / `dot_colr` / `dot_colg` / `buzz` **六项**；
   第 7 行是 `ld <= (others => '1') when btn = '1' else s_ld;` ——
   **既没进 `when sw7 = '1'` 门控，还叠加了一条 BTN0 直读**。
   → 清单里"LED 全灭"这句话**在实现里根本没有对应的代码**。
4. **顺手把"直读"这条也验一遍**：既然 `ld` 的组合越权在复位之上，那么**按住 BTN0 时，
   不管 SW7 是 0 还是 1，都必然是 16 个全亮**。用户按了却没亮 → 只剩两种可能：
   ① 按的不是 BTN0；② `btn`（`PIN_61`）通路坏了。
5. **翻手册定"BTN0 在哪"**（第 8 页 + 附图 16 实物照片）：
   *"实验板上一共有 8 个按键，标示为 BTN0~BTN7，依次使用……61、20、89、91、121、122、123 和
   124 脚……按键平时输出低电平，按下去输出高电平……在 BTN7 左下角有一个波形示意图。"*
   照片上从左到右的丝印是 **`BTN7 BTN6 BTN5 BTN4 BTN3 BTN2 BTN1 BTN0`** ——
   ⭐ **BTN0 = 8 个键里最右边那个**，而**整份 `docs/` 里没有任何一处写过这件事**。
6. **再看"非常混乱"**：阶段 1 之外的 `ld[7:0]` 是**子步号**（`tick_100÷25 = 4Hz`，
   每 0.25 秒 +1），所以整排灯一直在变；而 `ld[15:8]` 每 2 秒才跳一个阶段号。
   一个不知道这两条的人看到的正是"**乱**"。
   —— 这条其实 `docs/01` §8.2 已有 ⚠️ 注，但**只写在文档里、没进"上板判读表"**，
   现场翻不到（见预防措施 3）。

### 根本原因

**两份"真值源"各自正确、但没有对齐：**

- **代码**里 `ld` 的对外接口是 `ld <= (others=>'1') when btn='1' else s_ld;` ——
  **有意**把 BTN0 直读放在门控之外（文件注释写明理由：一次验掉"LED 通路 + 位序"与
  "`btn` 极性 + 复位通路"两件事）；
- **`docs/04` §7.1 那个清单**是按"要求 1 = SW7=0 全部输出关闭"这句话顺手写出来的，
  把 LED 也算了进去 —— 而**要求 1 管的是"显示器件"**（数码管 / 点阵 / 蜂鸣器），
  `ld` 只在自检顶层存在（`puzzle_top` 连 `ld` 端口都没有，`docs/01` §5.12）。

于是同一件事在"需求 → 清单 → 代码"这条翻译链上，**在清单这一环被扩大了范围**。
再叠加一个**完全没写进任何清单的事实**（"只有 BTN0 接进自检、且 BTN0 在最右边"），
现场就表现为"两条异常"。

> ⚠️ 这与 ERR-0014 / ERR-0029 是同一族：**单看好每一处，交叉引用才暴露**。
> 区别是这次错在"**清单**"这一层 —— 它是给人现场照着做的，错了的代价不是编译不过，
> 而是**在实验室里把正常现象当成故障去改代码**。

### 修复方法

**决定：改文档，不改代码。** 三条理由：
① 要求 1 覆盖的是显示器件，`ld` 不在其列；
② BTN0 直读是**唯一能在 SW7=0 下**验证按键通路的观测点，门控掉反而丢了一个测试手段；
③ `puzzle_top` 根本没有 `ld` 端口，整机侧不受影响。

- **`docs/04` §7.1 第 11 条**：期望现象改为"数码管全灭、点阵全灭、蜂鸣器无声"，
  并补 ⚠️ 说明 `ld` 不受门控、SW7=0 时**只有 LD8 亮**是预期现象（回填 ERR-0030）；
- **`docs/04` §7.1 第 10 条**：注明 BTN0 = `PIN_61` = **最右边那个按键**，
  且该路**不受 SW7 门控**，所以 SW7=0 也能验；
- **`docs/04` §7.3（**新增**）「现场位置速查」**：把散在手册第 3/4/7/8/10 页的
  "BTN0 在哪 / SW7 往哪拨 / `FREQ` 指示灯看第 7 格 / LD0 在最右端 / VGA 与 `ld[15:8]` 复用"
  收进一张表 —— 现场够不着电脑时照这张看；
- **`docs/04` §4 手册原文摘录**：补 **第 7 页（拨码）**、**第 8 页（按键）**、**第 10 页（VGA 复用）**
  三段原文 + 第 4 页"**16 个 LED 有 4 种颜色**"一句（"LD8 亮的是黄色"= 编号指纹）；
- **`docs/01` §8.2**：把"实机核对后订正"由两条扩到**三条**（新增第 3 条：SW7=0 时 `ld`
  停在阶段 1 的画面）；并把"**BTN0 直读为何故意不门控**"与"**BTN0 在最右边、
  其余 7 键在自检里无反馈**"补成正式注；
- **`docs/06` §4.2 第 6 步 / §6.1**：订正"阶段 7 无限期"（实为 20 秒超时）、
  把"SW7=0 → LED 也全灭"改为"LED 里只有 LD8 亮"，并**新增判读行**
  （"按住 BTN0 不亮 16 灯"→ 先怀疑按错键；"不按就全亮、一按反而只剩 LD8"→ `btn` 极性反了）
  ＋ 一张 **30 秒判据**；
- **`docs/05`**：把本次现场原话填进 `§1.2 ~ §1.4`，并给出"三条现象"的判读与
  "下次上板 30 秒顺序"（含"8 个键逐个试"这一条排除步骤）。

### 预防措施

1. ⭐ **写完一行输出级代码，回头把"需求清单"当测试用例逐条读一遍**：
   要求 1 说的是"**显示器件**"—— 那么凡是被门控的信号，都要能在需求里找到对应的器件名；
   **找不到的就是清单越权了**。
2. ⭐ **"某功能生效"的清单必须写清"观测点在哪"**：第 10 条写"按住 BTN0"却没写
   **哪个是 BTN0**，就等同于没写。**凡是让人"去按 / 去看 / 去拨"的检查项，
   都要带上板上丝印或物理位置**（本次的做法：全部收进 `docs/04` §7.3）。
3. ⭐ **诊断类结论不能只躺在"技术文档"里**：`docs/01` §8.2 早已写"低 8 个 LED 本来就一直在变"，
   但现场的人不会去翻 §8.2 —— **它必须同时出现在"上板现象判读表"（`docs/06` §6.1）里**。
   规则：**凡是"会让人误判成故障的正常现象"，都要有一行进 §6.1**。
4. **有"直读越权"这类故意的例外时，要在接口处就近标注**：本次代码注释写得很清楚，
   但文档侧没有对应一句 —— **例外一旦存在，它就必须同时出现在代码注释、技术文档、
   上板清单三处**（否则下一个读清单的人一定会当成 bug）。
5. 下次上板按 `docs/05` §1.5 的顺序走，并把**逐阶段结果当场填表**
   （本次是回来才补的，只剩一句"非常混乱"，定位成本因此高了一个数量级）。

---

## ERR-0031 判据让人"看阶段 1"，却没给"怎么知道现在就是阶段 1"

- **日期**：2026-09-24
- **阶段**：上板（第 1 次进实验室，现场照 `docs/06` §6.1 的「30 秒判据」逐条走）
- **涉及模块**：`docs/06` §6.1（判读表 + 30 秒判据） / `docs/01` §8.2 / `docs/05` §1.3 · §1.5
  —— ⭐ **`rtl/` 一行未改：实现是对的**
- **状态**：已解决（**改文档，不改代码**）

### 现象

现场照「30 秒判据」第 3 步（原文：*"不碰键看 3 秒（应 `LD8 常亮 + LD0~LD7` 齐闪 1Hz）"*）去看，
报回：

```
"我看到的现象是所有 16 个灯都闪烁，LD8 也不是常亮"
```

看起来像"阶段 1 的 LED 画面全错"，即像一处真实的逻辑故障。

### 定位过程

1. **先把"期望画面"从代码里重算一遍**（`rtl/board_test_top.vhd` L451~456）：
   阶段 1 时 `s_ld(15 downto 8) <= to_unsigned(1, 8)` = `"0000_0001"` → 只有 `ld[8]` 为高；
   `s_ld(7 downto 0) <= (others => r_b1)` → 低 8 位一起以 1Hz 闪。
   → **期望画面确认无误**：`LD8` 常亮、`LD0~LD7` 齐闪、**`LD9~LD15` 全灭**。**代码这一侧没有错。**
2. **算"阶段 1 占多大比例"**（这一步是关键）：阶段 1~6 各 2 s（12 s）＋ 阶段 7 超时 20 s
   ＋ 阶段 8/9 各 2 s = **完整轮播 36 秒，而阶段 1 只占其中的 2 秒（≈5.6%）**。
   → **"随便看几秒"撞上阶段 1 的概率极低**；看到的绝大多数是阶段 2~9 ——
   那时 `ld[15:8]` 是阶段号（每 2 秒跳）、`ld[7:0]` 是子步号（每 0.25 秒 +1），
   **16 个灯本来就都在动**。⭐ **与现场回报逐字吻合** —— 现象没有异常，是**判据不可执行**。
3. **回头找"有没有办法读出当前是第几阶段"**：有，而且是现成的 ——
   `board_test_top.vhd` L428~431 的 `when others` 分支把 `r_stage` 写进 `s_disp_val(31 downto 28)`，
   而 `seg_scan` 的映射是 `i_disp_val(4k+3..4k) → DISPk`、`o_cat(0)` = DISP0（最右）
   → ⭐ **最左边那位数码管（DISP7）显示的就是当前阶段号**。
   **判据写的时候完全没提这一条** —— 有读数可用，却让人去"猜"。
4. **再找"有没有能精确对准阶段 1 的时刻"**：有，两个 ——
   ① **SW7 由 0 拨到 1 之后**：`o_rst` 释放的那一拍 `r_stage` 从 1 开始；
   ② **松开最右按键（BTN0）之后**：按住期间 `o_rst='1'` 把 `r_stage` 按在 1，松手即从头开始。
   → 判据第 4 步本来就要求"按住→松手"，**"松手后 2 秒"是现成的观测窗口，
   却把"看"这个动作写在了它前面**。

### 根本原因

**判据给的是"该看什么"，没给"什么时候看"。**

- 被观测的量（阶段号）**不是常显的**：它每 2 秒变一次，而阶段 1 只占全周期的 5.6%；
- 而**能直接读出它的那个器件（DISP7）**、以及**能把它按回 1 的那两个动作
  （拨 SW7 / 松 BTN0）**，当时都没写进判据。

这与 **ERR-0030 是同一个坑的第二次发作**：两次都是"清单写得看起来完整，照着做却得不到判据"。
ERR-0030 缺的是"**物理位置**"（BTN0 在哪），这次缺的是"**时间窗口**"（阶段 1 何时才在）。

### 修复方法

**决定：改文档，不改代码。** 三处：

| 文件 | 改什么 |
|---|---|
| `docs/06` §6.1 | 判读表**新增一行**（"16 灯都在动"= 阶段 2~9，不是故障）；「30 秒判据」重写为**用 DISP7 读数**，并列出**三个能精确对准阶段 1 的时刻** + 阶段 1 的准确画面 |
| `docs/01` §8.2 | LED 指示方式的 ⚠️ 注**补一段**：阶段 1 只持续 2 秒、36 秒里占 2 秒，并给出 DISP7 这条定位手段与三个时刻 |
| `docs/05` §1.3 · §1.5 | 阶段 1 那行**补记**本次实测原话与判读；总体结论由"两条异常"改为"三条异常"，并新增一条共性问题说明 |

### 预防措施

1. ⭐ **判据必须包含"何时看"** —— 凡是"看某个显示"的检查项，若该显示**不是常显的**，
   必须同时给出 ① 它的**定位手段**（哪个器件能读出它）、② **触发时刻**（做什么动作能必然落到它）。
   **"看什么"和"何时看"缺一不可。**
2. ⭐ **凡"某个状态只占周期一小段"的，判据里要写明它占多少**（本例：36 秒里占 2 秒）。
   写出来，人就知道"看不到"是概率问题、不是故障。
3. **与 ERR-0030 合看**：上板判据的两类缺口是"**物理位置**"与"**时间窗口**"。
   以后新增任何现场判据，**这两栏各过一遍**。
4. **代码里"现成的读数"要在判据里点名**：本次 DISP7 一直显示阶段号，
   但没有任何一份现场清单提过它 —— **能被直接读出来的量，就不该让人靠数秒表去推。**

---

## ERR-0032 「DISP7 = 阶段号」是过度概括；阶段 6 的读数方向与文档措辞相反

- **日期**：2026-09-24
- **阶段**：上板（在给"逐阶段走查"写步骤时，回头复核 `board_test_top` 的数码管分支）
- **涉及模块**：`docs/06` §6.1 / `docs/01` §8.2 / `HANDOFF.md` §3
  —— ⭐ **`rtl/` 一行未改**
- **状态**：已解决（**改文档，不改代码**）

### 现象

在给现场写"自检阶段 2~8 逐条打勾"的步骤时，复核数码管的期望画面，查出两处**期望现象描述与实现不符**：

```
① 上一轮（ERR-0031）刚写进 docs/06 / docs/01 / HANDOFF 的
   "最左边那位数码管 DISP7 显示的就是当前阶段号" —— 只对 6 个阶段成立；
② docs/01 §8.2 阶段 6 写"数码管依次显示 1 2 3 4 5 6 7 8"，
   而实物从左到右读是 8 7 6 5 4 3 2 1。
```

两条都会在现场造成**误报**：① 在阶段 5/6/7 找不到阶段号 → "数码管坏了"；
② 看到 `87654321` → "段码顺序反了"。

### 定位过程

1. **逐行读数码管的组合进程**（`rtl/board_test_top.vhd` L393~433）：
   `case r_stage is` 下面有 **4 个分支** —— `when 5` / `when 6` / `when 7` / `when others`。
   前三个各自整体覆盖了 `s_disp_val` 与 `s_blank`；**只有 `when others` 才把 `r_stage`
   写进 `s_disp_val(31 downto 28)`**。
   → ⭐ **`when others` 在 VHDL 里接住的是"其余全部取值"，即阶段 1/2/3/4/8/9**
   → **① 成立：阶段 5/6/7 的 DISP7 不显示阶段号。**
2. **把阶段 6 的实物读数算出来**：`s_disp_val <= x"87654321"` 的**最高 nibble = 8**
   送进 `s_disp_val(31 downto 28)`；而 `seg_scan` 的映射是
   `i_disp_val(4k+3..4k) → DISPk`、`o_cat(0)` = DISP0（**最右**）
   → **DISP7（最左）显示 8、DISP0（最右）显示 1**
   → **从左到右读必然是 `8 7 6 5 4 3 2 1`** → **② 成立。**
3. **判断"要不要改代码"**：阶段 6 的目的是**验段码 + 位序**，
   读数方向反过来**不影响这两件事**（反而更狠地测了位序）；
   而改代码要动 `board_test_top`、重编译、重烧录 —— **改文档即可**，与 ERR-0030 同一裁决。

### 根本原因

**同一族错误的第三次发作：把"某个时刻/某个分支为真"的观察，写成了"永远为真"的规则。**

| # | 把什么写成了什么 | 缺的那一维 |
|---|---|---|
| ERR-0030 | "要求 1 管显示器件" → "LED 也全灭" | **范围**（放大） |
| ERR-0031 | "阶段 1 的画面" → "随便看 3 秒" | **时间窗口** |
| **ERR-0032** | "阶段 1/2/3/4/8/9 里 DISP7 = 阶段号" → "**DISP7 就是阶段号**" | **覆盖面**（少列了 3 个分支） |
| **ERR-0032** | "依次显示 1 2 3 4 5 6 7 8" → 未说方向 | **方向** |

> ⚠️ **①这一条是上一轮自己引入的**：ERR-0031 修复时，`when others` 四个字就写在引用里，
> 但**没有回头数"这个 others 到底覆盖哪几个阶段"** —— 引用指向的小节对题了（已修过 ERR-0017 的悬空引用），
> 但**引用的语义边界没核**。

### 修复方法

**决定：改文档，不改代码。** 四处：

| 文件 | 改什么 |
|---|---|
| `docs/06` §6.1 | 判读表**再加一行**（阶段 6 读数是 `8 7 6 5 4 3 2 1`，不是故障）；「30 秒判据」的 DISP7 段补"**只覆盖阶段 1/2/3/4/8/9**" |
| `docs/01` §8.2 | 阶段 6 那一行改为"**从左到右读是 `8 7 6 5 4 3 2 1`**（= DISP7 显 8、DISP0 显 1）"；DISP7 的 ⚠️ 注补覆盖面 |
| `HANDOFF.md` §3 | "看 DISP7"那条补"**阶段 5/6/7 例外**" |
| 本文件 | 本条记录 + 索引 |

### 预防措施

1. ⭐ **写"某信号能读出某个量"时，必须把该信号的赋值分支全部数一遍**：
   凡 `case` / `if` 有多个分支的，**"谁被覆盖、谁落到 `when others`"要逐条列出来**。
   本例：4 个分支里 3 个覆盖了 `s_disp_val`，只有 `when others` 才给阶段号 ——
   **"`when others`"不等于"全部"，它的语义是"其余取值"。**
2. ⭐ **写"从左到右/从上到下看到什么"时，必须把位序算一遍**：
   `x"87654321"` 配"DISP7 最左"→ 读数必然是 `8 7 6 5 4 3 2 1`。
   **"依次显示 1 2 3 4 5 6 7 8"这种不写方向的措辞就是歧义。**
3. ⭐ **上板期望现象一律写成"实物上从左到右 / 从上到下看到什么"**，
   **不写信号值的排列顺序** —— 后者要读者自己在脑子里做位序换算，必然出错。
4. **改完一处"引用代码"的文档，要顺着引用回代码核"语义边界"**：
   ERR-0017 教的是"别引用不存在的小节"，这次补上另一半 ——
   **小节存在、名字对题，还不够；引用的那句话的覆盖范围也要核。**

---

## ERR-0033 按满 16 键之后，阶段 7 变成 0.25 秒一闪而过 —— "进不去阶段 7"

- **日期**：2026-09-24
- **阶段**：上板（第 1 次进实验室，按「30 秒判据」补做阶段 7 的键盘核对时）
- **涉及模块**：`rtl/board_test_top.vhd`（`r_seen` 的清零条件） / `docs/01` §8.2 / `docs/05` §1.6
- **状态**：**已定位、已给规避法；代码修复有意推迟**（见「修复方法」）

### 现象

用户报："**进不去阶段 7**" —— 用"SW7 拨 0 再拨回 1，等 12 秒"的办法也到不了阶段 7。
前一条回报是"按 16 个按键后 LD 灯还是乱闪烁"。

### 定位过程

1. **先算"阶段 7 应该有多长"**：`r_kp_tmr` 是 `integer range 0 to 79`，
   每个子步（0.25 s）加一，加到 79 就往下走 → **80 × 0.25 s = 20 秒**。
   代码逻辑（`board_test_top.vhd` L308~316）本身没问题 —— **阶段 7 应该是能看见的 20 秒**。
2. **再看它"提前结束"的那条条件**：`if r_seen = "1111111111111111" or r_kp_tmr = 79 then`
   → 只要 `r_seen` 全 1，**阶段 7 在进入后的第一个子步就退出**（0.25 秒）。
3. ⭐ **关键：`r_seen` 什么时候清零？** 全文搜 `r_seen`，只有两处写：
   - `if s_rst = '1' then … r_seen <= (others => '0');` ← **只在复位时清零**
   - `if s_key_press = '1' and unsigned(s_key_code) /= 0 then r_seen(…-1) <= '1';` ← **只置位，不复位**
   → **`r_seen` 是累积的，且永不在阶段之间清零。**
4. **于是推出现象**：**一旦按满过 16 个键，`r_seen` 就恒为全 1**，
   此后**每一圈经过阶段 7 都在 0.25 秒内退出** → 肉眼看不见 → 就是"进不去阶段 7"。
   ⭐ **与用户的两条回报逐字吻合**：先"按 16 个键"（把 `r_seen` 填满），
   然后"LD 还是乱闪"（阶段 7 一闪而过、回到轮播）、再"进不去阶段 7"（再也进不去）。
5. **确认"不是别的原因"**：`r_stage` 的推进链（1→…→9→1）无异常；
   `r_kp_tmr` 进入阶段 7 前为 0；复位后 `r_seen` 清空 → **复位一次就能重新看见阶段 7**（实测符合）。

### 根本原因

**`r_seen` 的语义被写成了"本次上电以来按过哪些键"，而阶段 7 需要的是"本轮进阶段 7 之后按过哪些键"。**

两个语义只差一个清零时机，但后果相反：

| 想要的语义 | 需要的清零时机 | 实现里的清零时机 |
|---|---|---|
| 本轮进阶段 7 之后按过哪些键 | **每次进入阶段 7 时清零** | ❌ 只有 `s_rst='1'` 时清零 |

> ⚠️ **这是同一族错误的第四次发作，缺的那一维是「作用域」**：
> ERR-0030 缺**范围**（清单越权）→ ERR-0031 缺**时间窗口** → ERR-0032 缺**覆盖面 / 方向**
> → **ERR-0033 缺"这个状态从什么时候开始算"**。
> 四次都栽在同一件事上：**写代码/写清单时把"某个瞬间成立的观察"当成了"一直成立的规则"。**

### 修复方法

**决定：本次不改代码（有意推迟），只记文档 + 给规避法。** 三条理由：

1. **要改的是 `board_test_top`，而现在板上烧的是 `puzzle_top`** ——
   改完得切回自检顶层、重编译、重烧录，会把正在做的整机演示打断；
2. **它只影响自检工具，不影响整机**（`puzzle_top` 不含 `board_test_top`，`r_seen` 只在这里用）；
3. **`board_test_top` 只有 439/1270 LE，加这一处改动资源上毫无压力** ——
   不存在"来不及改"的问题，纯粹是时机问题。

**⭐ 现场规避法（立刻可用）**：**按 BTN0，或把 SW7 拨到 0 再拨回 1** ——
复位即清空 `r_seen`，阶段 7 恢复成 20 秒。**按满 16 键后想再看一次，必须先复位。**

**⭐ 正确的代码改法（下次动 `board_test_top` 时一并做）**：在阶段推进那一段里，
**从阶段 6 进阶段 7 的那一拍顺手清 `r_seen`** ——

```vhdl
-- 在 r_stage_tmr = 7 的分支内，推进阶段时：
if r_stage = 6 then
    r_seen <= (others => '0');      -- 进阶段 7 前清空"本轮已按键"
end if;
```

### 预防措施

1. ⭐ **凡是"累积型状态"（`seen` / `done` / `count` 这类），必须写清它的"作用域"**：
   **从哪个事件开始累积、到哪个事件清零。** 只写"累加"不写"清零"，等于没定义。
   检查动作：**全文搜这个信号名，看有几处写、分别什么条件** —— 本例一搜就见分晓
   （只有 `s_rst` 一处清零）。
2. ⭐ **"看不到了"要先怀疑"它一闪而过"，而不是"它没发生"** ——
   本例阶段 7 确实发生了，只是只持续 0.25 秒。
   **判据：把"某状态该持续多久"从代码里算出来（本例 80 × 0.25 s = 20 s），
   再和实际看到的比 —— 数字对不上，就是"被提前结束了"，去找那个提前退出的条件。**
3. **"用户先做了某个动作、然后某功能就不见了"这类现象，优先怀疑"那个动作留下了持久状态"。**
   本例：先"按 16 个键" → 留下 `r_seen` 全 1 → 阶段 7 永久失效。**因果链在时间上很明确。**
4. 自检类工具也要按"会被反复使用"来设计：**任何"用完即废"的累积状态，都要在下一轮开始前清零。**

---

## ERR-0034 「看不到开机自检」—— 2 秒的正常画面没进现场判读表

- **日期**：2026-09-24
- **阶段**：上板（首次烧整机 `puzzle_top`，2026-09-24 14:20）
- **涉及模块**：`docs/06` §4.2 · §6.1 / `docs/05` §1.6 —— ⭐ **`rtl/` 一行未改**
- **状态**：已解决（**改文档，不改代码**）

### 现象

用户报：

```
"SW7 拨到 1，没有出现自检现象，点阵没有变黄和闪烁，数码管没有全部显示"8"且闪烁，
 而是直接进行"5"待机状态"
```

看起来像"**要求 1 的开机自检没有实现**"。

### 定位过程

1. **先算自检该持续多久**：`puzzle_pkg` 的 `T_SELFTEST = 2`（秒），
   `game_fsm` 的 `S_SELF_TEST` 分支在 `i_tick_2hz` 上数 `2*T_SELFTEST = 4` 拍
   → **4 × 0.5 s = 2 秒**。**代码这一侧没有错。**
2. **回头对需求原文**（`docs/00` §2）：
   - **要求 1**："SW7='1' 时，**首先进行开机自检**，点阵全部显示黄色并以 2Hz 进行闪烁，
     同时 8 个数码管全部显示"8"且以 2Hz 闪烁。"
   - **要求 2**："**2 秒后**系统进入待机状态，点阵全灭，数码管 DISP7 显示"5"、DISP0 显示"1"，
     其他全灭。"
   → ⭐ **2 秒是需求规定的**（`docs/00` 要求 1 的验收判据里也写着"**SW7=1 后 2 秒内**"），
   **不是实现偷工减料**。
3. **确认"待机画面"也对**：`disp_format` 的 `S_IDLE` 分支给 `DISP7 = "5"`、
   `DISP0 = 关卡号`（`v_lvl = i_level + 1`，第一关 = `"1"`）
   → **与用户看到的"5 待机"逐字吻合** → **自检态与待机态两个画面都是对的**。
4. **结论**：用户**错过了那 2 秒**。但**这不是用户的错** ——
   `docs/06` §4.2 第 6 步（现场判读）**只写"显示要求 1 的开机自检"，没写持续多久**；
   §6.1 判读表里**也没有这一条**。而这两处才是现场唯一会看的地方。

### 根本原因

**已经写下来的预防措施，没有落实到"现场判读表"这一层。**

- `docs/00` 要求 1 的验收判据里**写着**"SW7=1 后 2 秒内" → **知识在文档里，但不在现场会看的那一页**；
- `docs/06` §4.2 没写时长，§6.1 判读表没这一行 → 现场看到"直接待机"，第一反应必然是"自检没实现"。

> ⚠️ **这是同一族错误的第五次发作，缺的那一维仍是「时间窗口」**，
> 而且**与 ERR-0031 是同一个缺口、只是换了个状态**：
> 上次是"**阶段 1 只占 36 秒里的 2 秒**"，这次是"**开机自检只持续 2 秒**"。
>
> ⭐ **更值得记的是：ERR-0030 的预防措施第 3 条早就写过**
> "凡'会让人误判成故障的正常现象'，都要有一行进 `docs/06` §6.1" ——
> **但当时只把它用在 ERR-0030 那一处，没有回头把别的状态也过一遍。**
> **规则写下来 ≠ 规则被执行；执行需要一次"拿规则扫全表"的动作。**

### 修复方法

**决定：改文档，不改代码**（2 秒是需求规定，改代码反而违反要求 2）。三处：

| 文件 | 改什么 |
|---|---|
| `docs/06` §4.2 第 6 步 | `puzzle_top` 那条补"**只持续 2 秒**（要求 2 明文）+ 之后进待机（DISP7 显 5、DISP0 显关卡号）"，并给**三个能看到它的办法** |
| `docs/06` §6.1 | 判读表**新增两行**：① "看不到自检、直接待机" = **正常**；② 整机待机画面 `5 … 1` = **要求 2 原文画面** |
| `docs/05` §1.6 | 把现场现象原话与判读填进实测栏 |

### 预防措施

1. ⭐ **"会一闪而过的正常画面"必须进现场判读表，并写明持续时长**：
   开机自检 **2 秒**、`board_test_top` 阶段 1 占 **36 秒里的 2 秒** ——
   都属于"**看不到 ≠ 没发生**"。
2. ⭐ **写下一类预防措施后，要拿它把"所有同类对象"扫一遍**，而不是只修当前这一处
   （本条就是 ERR-0030 预防措施 3 的补做）。
3. ⭐ **"看不到"与"没实现"要分开判**：判据是**把该现象的应有持续时长从代码/需求里查出来**，
   再和现场观察比 —— **数字对得上就是"错过了"，不是"没做"**。
4. **现场给判据时，凡涉及"看一眼就知道"的画面，都要顺带给出"怎么确保正好看到"的操作**
   （本例：**先盯住板子再拨开关**、**按 BTN0 松开重启自检**、**录视频回放**）。

### ⚠️ 补记（2026-09-24 14:31）—— **结论尚未闭合，现场第二次回报推翻了"只是错过"这一支**

用户按"办法②"再试一次，报：

```
"按住最右那个按键（BTN0）再松开后，还是直接进入 5 待机显示，没有 2 秒的自检！"
```

**这不可能是"错过窗口"** —— 松手是用户自己控制的动作，2 秒内必然在盯着板子。
于是把整条链**逐环节重核一遍**（全部通过）：

| 环节 | 核查结果 |
|---|---|
| `clk_gen` 的 `tick_2hz` | `CNT_8K=6249 → ÷8 → ÷10 → CNT_2HZ=49` → **2 Hz** ✓ |
| `game_fsm` 的 `r_self_cnt` | `integer range 0 to 2*T_SELFTEST-1` = **0~3**，数 4 拍 = 2 秒 ✓ |
| `disp_format` 的自检分支 | 点阵 `(others => i_blink)`、数码管 `x"88888888"` + `o_blank = not i_blink` ✓ |
| `puzzle_top` 的 BTN0 接线 | L310 `i_btn_rst => btn` ✓ |
| `.qsf` 的 `btn` | `PIN_61` **完好**，52 引脚无重复（`check_pins.py`）✓ |

→ **代码与约束侧找不出"跳过自检"的路径**，所以怀疑点转移到**复位信号本身有没有真的送到**。

⭐ **一条此前从未写下来的关键判据（本次补）**：
**按住 BTN0 期间，数码管与点阵应当"全灭"** ——
因为复位期间 `r_state = S_SELF_TEST` 且 `r_blink` 被复位钳在 `'0'`，
于是 `o_blank = not 0 = 全 1`（全灭）、点阵 `px = 0`（全灭）。
**所以"按住时全灭"就是"复位通路真的通了"的直接证据。**

⚠️ **注意 `board_test_top` 上的"按住 BTN0 → 16 灯全亮"证明不了这条**：
那一行走的是 `ld <= (others => '1') when btn = '1' else s_ld;` ——
**组合直读，绕过了同步器、消抖与 `o_rst`**。它只证明**引脚通**，不证明**复位通**。

**待补测（两个 1 秒就能做完的测试）**：

| # | 动作 | 若如此 → 结论 |
|---|---|---|
| **A** | **按住最右按键不松**，看数码管/点阵 | **全灭** → 复位通路正常，松开后必有 2 秒自检 → 回到"窗口"这一支；<br>**仍显示 "5"（不变）** → **BTN0 没有接进复位**，这才是真问题 |
| **B** | **SW7 拨到 0** | 应**数码管/点阵全灭、蜂鸣器无声**（要求 1）—— 验 SW7 门控 |

> **测试 A 是决定性的**：它一次分开"窗口没抓住"与"复位没送到"两种可能。
> 若 A 的答案是"全灭"，则用**录视频**（办法③）复核；若是"不变"，则按 BTN0 通路排查。

#### 测试 A 结果（2026-09-24 14:35）：**全灭** → ⭐ **复位通路确认正常**

| 判据 | 现场 | 推论 |
|---|---|---|
| **按住 BTN0 时数码管/点阵应全灭** | ✅ **全灭** | **`btn` → 2 级同步器 → 20ms 消抖 → `r_btn_rst` → `o_rst` → FSM 全链路工作** |

**推论（这是本次最有价值的一步）**：复位通路既然通了，**FSM 就必然从 `S_SELF_TEST` 起步**；
而 `r_self_cnt` 要数 4 个 `tick_2hz` 才迁到 `S_IDLE` —— **那 2 秒一定存在，不是被跳过**。

**再加两处接线复核（也全对）**：

| 环节 | 结果 |
|---|---|
| `puzzle_top` L315 | `o_tick_2hz => s_tick_2hz`（**没有与 `s_tick_1hz` 接反**）✓ |
| `game_fsm` L224 | `o_blink <= r_blink`，且 blink 进程 `r_blink_div = CNT_BLINK(=24)` 数 25 拍 → **2 Hz** ✓ |

→ **"跳过自检"这一支已被排除。** 剩下唯一的分支是：
**那 2 秒里画面到底是"在闪"还是"黑的"** —— 即 `i_blink` 有没有真的翻转。

**⭐ 决定性观察（5 秒可做，不需要视频）**：
**按住最右按键 → 松开 → 盯住数码管数秒** —— 看 `5` 是"**啪一下立刻出现**"还是"**黑着约 2 秒才出现**"：

| 观察 | 结论 | 下一步 |
|---|---|---|
| **约 2 秒后才出现 "5"** | FSM 正常，那 2 秒画面是**黑的** → **`i_blink` 没翻转**（真问题） | 查 `game_fsm` 的 blink 进程在板上是否真的在跑 |
| **几乎立刻出现 "5"**（< 0.3 s） | 自检真的被跳过了 | 需上板抓 `r_self_cnt`（或退回仿真定位） |
| **看到了闪烁** | 之前只是**没抓住窗口** | 正常，直接进下一步（按「开始」） |

**状态**：仍为「待补测」—— 根因未定，等上面的观察结果。

---

## ERR-0035 键盘扫描极性反了 → 幻影「开始」按键把系统踢出自检态

- **日期**：2026-09-24
- **阶段**：上板（首次烧整机 `puzzle_top`）
- **涉及模块**：`rtl/keypad_scan.vhd`（**已改**：新增 `KP_ACTIVE` 常量 + 列驱动改低有效）
  / `docs/04` §3.3 / `docs/01` §8.2 / `docs/06` §6.1 / `docs/05` §1.6.2 / `scripts/encoding.py`
- **状态**：**已修复并烧录**（2026-09-24 14:41，`puzzle_top` 1226/1270 LE 与改前一致），**待现场验证**

### 现象

`puzzle_top` 烧进板子后：**拨 SW7=1 看不到 2 秒自检**；松开 BTN0 后
"**直接进入 5 秒计时**"，且**点阵会亮起**（用户确认"见过点阵亮起"）。

### 定位过程

1. **先排除"自检被跳过"这一支**（ERR-0034 的续）：测试 A「按住 BTN0 → 数码管与点阵全灭」
   → 复位通路确认正常 → FSM 必然从 `S_SELF_TEST` 起步 → **那 2 秒一定存在**。
2. ⭐ **抓住一个词**：用户说"**5 秒计时**"。
   `disp_format` 里 DISP7 会**从 5 开始往下数**的状态只有 **`S_PREVIEW`（预览 5 秒倒计时）**；
   待机 `S_IDLE` 的 "5" 是**静态**的。**"计时"这个词指向 `S_PREVIEW`。**
3. **找"不按键怎么会进 `S_PREVIEW`"**：`game_fsm` L246 有一条**全局边** ——
   `if i_key_press = '1' and s_act_start = '1' then r_state <= S_PREVIEW;`
   —— 它写在 `case` **之前**，**任何状态（含 `S_SELF_TEST`）都拦不住**。
   → 只要有一次**幻影「开始」**，自检态立刻被踢出。
4. **算"幻影键号是多少"**：`keypad_scan` 用 `if i_kp_row(r) = '1'` 判按下。
   若行在无键时就一直是 `'1'`，则每一相都读到"有键"，而 `v_raw` 在循环里被
   **行号最大的那个**覆盖：`v_raw = (3-3)*4 + phase + 1 = phase + 1`
   → 键号随相号循环 `1→2→3→4`；整轮锁存的是相 3 的值 = **4**。
   查 `game_fsm` L155：`when "00100" => s_act_start <= '1';   -- 键 4 开始`
   → ⭐ **幻影键号恰好就是「开始」**。
5. **核对两条回报**：`S_PREVIEW` = **DISP7 从 5 倒计时**（"5 秒计时" ✓）
   ＋ **点阵显示目标图案**（"见过点阵亮起" ✓）。**两条都吻合。**
6. **回查文档**：`docs/04` §3.3 **早就记着这个矛盾** —— 课程 PDF p55 按**低有效**、
   板手册按**高有效**，项目选了手册那一版，并写着
   "**最终以 §7.2 自检第 7/8 项的上板实测为准**"。
   **而阶段 7 被跳过了**（`docs/05` §1.6）。

### 根本原因

**键盘扫描极性选错了一边，而唯一能验证它的自检阶段 7 被跳过了。**

| 项 | 内容 |
|---|---|
| 代码假设 | 行读到 `'1'` = 按下（**高有效**，按开发板手册） |
| 实际板子 | 行**无按键时就一直读到 `'1'`** |
| 后果 | 每相都误报按键 → 幻影键号 = `phase + 1` → 整轮锁存 **4 = 开始** |
| 传导 | `game_fsm` 全局边「任意状态按开始 → `S_PREVIEW`」→ **自检态被立刻踢出** |

> ⚠️ **这是"跳过自检"的直接代价，而且是可预测的**：
> `PROGRESS.md` 阻塞表第 2 项写的就是"**键盘行列方向与极性需实测确认（自检阶段 7）**" ——
> **它是唯一一项从未实测过的硬件事实，也是唯一一项一旦错了会让整机"看起来像逻辑坏了"的。**
> 跳过它省下几分钟，代价是两轮上板排查。

### 修复方法

**改 `rtl/keypad_scan.vhd`**（采用课程 PDF p55 的**低有效**方案），三处：

1. 新增**全项目唯一的极性翻转点**常量：`constant KP_ACTIVE : std_logic := '0';`
2. 列驱动：选中列驱 `'0'`、其余驱 `'1'` ——
   `o_kp_col <= not (1 sll phase) when KP_ACTIVE = '0' else (1 sll phase);`
3. 行判定：`if i_kp_row(r) = KP_ACTIVE then`

**为什么选低有效**：它对"行外接**上拉**"与"行外接**下拉**"两种板子**都成立** ——
① 行外接下 → 无键时行 = 0，按下时选中列的低电平仍把行压成 0，靠"选中列低、其余列高"区分；
② 行外接上 → 无键时行 = 1，按下被选中列拉低。
**两种情况下"读 0 = 按下"都成立。**

**编译结果**：`puzzle_top` **1226 / 1270 LE（97%）** —— 与改前**完全一致**
（常量比较在综合期折叠，不额外耗 LE）→ **零资源代价**。
**烧录**：Erase → Program → Verify 全过（14:41）。

> ⚠️ **顺带修了 `scripts/encoding.py`**：注释里用了 `✓`/`✗`，GBK 装不下导致转换被拒
> （脚本按设计**拒绝并报位置**，没有静默写 `?`）。已把 `✓ → √`、`✗ → ×` 加进 `GBK_SUBST`。

### 预防措施

1. ⭐ **"文档里记着两源矛盾、并写明'以实测为准'的那一项，就是最不能跳过的那一项"** ——
   `docs/04` §3.3 已写出正确处置方式，**但没人在决定跳过自检时回头读它**。
   **规则写在文档里 ≠ 决策时会想起来。**
2. ⭐ **"跳过某项验证"之前，必须先列出"这项验证能查出什么"** ——
   本次跳过阶段 7 时，`docs/05` §1.6 只写了"点阵/数码管/蜂鸣器 5 项无独立确认"，
   **漏掉了键盘** —— 而键盘恰恰是唯一"错了会让整机完全不可用"的一项。
   **判据：跳过验证前把对应的硬件事实逐条列出来，再看哪一条错了代价最大。**
3. ⭐ **极性/位序这类"两源矛盾"一律做成单点常量**（本次 `KP_ACTIVE`），
   并在常量处写清"两种取值的物理含义 + 为什么选它"。翻转时只改一个字符。
4. ⭐ **"计时"与"静态显示"是两种不同状态**：现场描述里出现"计时 / 倒数"这类词，
   要立刻意识到**那不是待机态**（`S_IDLE` 的 "5" 是静态的）。
   **用户用词里常常藏着状态信息 —— 这一条是本次定位的关键突破口。**

---

## ERR-0036 切顶层前没说清"板子会变成什么样" —— 现场把自检轮播当成"被破坏了"

- **日期**：2026-09-24
- **阶段**：上板（为验位序而切回 `board_test_top`）
- **涉及模块**：`scripts/set_top.py` 的使用流程 / `docs/06` §5 / `docs/05` §1.6.4 / `HANDOFF.md` §3
  —— **`rtl/` 一行未改**
- **状态**：已解决（**订流程与文档**）

### 现象

AI 为验位序，`set_top.py board_test_top` → 重编译 → 烧录，并在回复里写了"已切回自检顶层"。
用户随即报：

```
"你这一次修改后完全破坏了之前的效果，打开 SW7 后没有自检，数码管、点阵、LD 灯都乱闪烁"
```

### 定位过程

1. **逐条对"自检顶层该有什么"**（`docs/01` §8.2）：
   - "**打开 SW7 后没有自检**" → ✅ **正确**：**"2 秒自检"是 `puzzle_top` 的 `S_SELF_TEST`**，
     `board_test_top` **根本没有这个状态**，它进的是 **9 阶段自动轮播**。
   - "**数码管、点阵、LD 都乱闪烁**" → ✅ **正是 9 阶段轮播的正常画面**：
     `ld[7:0]` = **子步号**（每 0.25 秒 +1）、`ld[15:8]` = 阶段号（每 2 秒跳）、
     数码管显示阶段号、点阵逐行/逐列点亮。
2. **结论**：**板子工作正常，是"板上跑的程序被换掉了"** —— 而这一步**在动手前没有被说清**。

### 根本原因

**"切换顶层"是一次会彻底改变板子行为的操作，但它被当成了一次普通的"烧录"来处理。**

- 用户对"烧录"的心智模型是"把修好的程序烧进去"，**默认板子还是那个游戏**；
- 而 `set_top.py board_test_top` 换掉的是**整个顶层实体** ——
  **LED、数码管、点阵、按键的含义全部改变**（`ld` 只在自检顶层存在；
  自检顶层没有 `S_SELF_TEST`、没有游戏逻辑、没有 `puzzle_ctrl`）。
- 回复里虽然写了"已切回自检顶层"，但**没有用一句话说清"板子会变成什么样"** ——
  而"会变成什么样"才是用户判断"是不是坏了"的唯一依据。

> ⚠️ **这是同一族错误的第六次发作，缺的那一维是「操作后的期望画面」**：
> ERR-0030 缺**范围** → ERR-0031 缺**时间窗口** → ERR-0032 缺**覆盖面/方向** →
> ERR-0033 缺**作用域** → ERR-0034 缺**时间窗口（复发）** → **ERR-0036 缺"换程序后的新画面"**。
> **共同点始终是：动手前没有把"接下来会看到什么"写下来。**

### 修复方法

**决定：订流程与文档，不改代码**（板子本身没有任何问题）。三处：

| 文件 | 改什么 |
|---|---|
| `docs/06` §5（切顶层） | **新增醒目警告**："切顶层 = 换掉板上跑的程序，**四个显示器件与按键的含义全部改变**"，并给出**两个顶层各自的期望画面对照表** |
| `HANDOFF.md` §3 | 在"切回 `board_test_top`"那一段**前置一句**："⚠️ 切完板子**不再是游戏**，而是 9 阶段轮播" |
| `docs/05` §1.6.4 | 补记本次现场反应与判读 |

**另**：`set_top.py` 的收尾提示里**补一行**"⚠️ 换顶层后板子行为完全改变，**请先告诉现场的人会看到什么**"。

### 预防措施

1. ⭐ **凡是会改变"板子上跑什么"的操作（切顶层、换 `.pof`、改 `.qsf` 后重编），
   动手前必须先用一句话写清"板子接下来会看到什么"** ——
   不是写"我做了什么"，而是写"**你会看到什么**"。
   **判据：如果现场的人照这句话还不能判断"这是不是坏了"，那这句话就没写完。**
2. ⭐ **"烧录"不等于"还是原来那个程序"** —— 换顶层就是**换了一个设备**。
   两个顶层的期望画面**必须并排写在一起**（本次已补进 `docs/06` §5），
   否则现场只能拿旧画面的记忆去比，必然误判。
3. **报告/文档里凡出现"已切回 / 已换成"这类动作，都要紧跟一句"现在板子上是什么"。**

---

## ERR-0037 ⚠️ **`set_top.py` 的正则把 16 行 `ld` 引脚约束截断了 —— 引脚真值源被破坏**

- **日期**：2026-09-24
- **阶段**：上板（切顶层 / 跑仿真期间）
- **涉及模块**：`scripts/set_top.py`（**已修**） / `quartus/puzzle.qsf`（**已修**） / `docs/04` §2
- **状态**：**已修复并重编译烧录**（`board_test_top` 68 引脚、0 errors）

### 现象

跑完第 1/2 轮仿真后做收工检查，`set_top.py check` 报：

```
ld 引脚约束：共 1 行，其中 1 行生效、0 行已注释
```

而当时板上跑的是 `board_test_top`，**应当有 16 行**。
`check_pins.py` 同时报 `ld ✗`、**引脚总数只有 53**（应为 68）。

`.qsf` 里那一行长这样：

```
set_location_assignment PIN_137 -to ld[
```

—— **`15]` 不见了**，而且另外 15 行整个消失。

### 定位过程

1. **逐提交比对 `.qsf` 里 `ld[` 的行数**：

   | 提交 | ld 行数 | 说明 |
   |---|---|---|
   | `c2c1703` ~ `27a891b` | 16 | 但**每行的 `n]` 已经被吃掉** |
   | `8135549`（14:52） | **1** | 15 行整个消失 |

2. **看"好版本"里那一行长什么样** —— 发现 **16 行全都已经被截断成 `-to ld[`**，
   只是行数还是 16，所以**"数行数"的检查完全看不出来**。
   → 说明截断发生得更早：**第一次跑 `set_top.py`（14:10 切 `puzzle_top`）就发生了**。
3. ⭐ **读 `set_top.py` 的正则，根因一目了然**：

   ```python
   LD_RE = re.compile(r"^(\s*#\s*)?(set_location_assignment\s+PIN_\d+\s+-to\s+ld\[)")
   #                                                                          ^^^^^ 到 `ld[` 就结束
   ...
   body = m2.group(2)      # ← 只拿到"匹配到的那一段"
   ln = body               # ← 把它当整行写回 → 后面的 `0]` 被丢弃
   ```

   **正则只匹配到 `ld[`，而写回时用匹配到的那一段当整行** → 每切一次顶层，
   16 行的 `0]`…`15]` 就被吃掉一次。
4. **那 15 行为什么会"整个消失"**：截断后的 `-to ld[` 是**非法节点名**，
   Quartus 在 `project_close` 重写 `.qsf` 时**把认不出的赋值行直接丢掉** ——
   于是下一次编译/仿真之后，16 行只剩 1 行。
   → 这也解释了为什么"编译还能过"：**fitter 用的内存副本是正确的，落盘的 `.qsf` 才是坏的。**

### 根本原因

**"改写文本"的操作把"匹配到的前缀"当成了"整行"。**

两层叠加放大了后果：

| 层 | 动作 | 后果 |
|---|---|---|
| 1 | `set_top.py` 用 `group(2)`（前缀）当整行写回 | 16 行各丢一个 `n]` |
| 2 | Quartus `project_close` 丢弃非法节点名的赋值行 | 15 行整个消失 |

> ⚠️ **这条最危险的地方在于"检查手段本身失效"**：
> `set_top.py` 的旧 `check` 只**数行数**（16 行 = 通过），
> 而**被截断的行照样能匹配那条正则** → 数行数永远数不出截断。
> **"可数的检查"必须数在"真正会坏的那一维"上** —— 这次坏的是**行内容**，不是行数。

### 修复方法

1. **`scripts/set_top.py` 的正则**改为**捕获整个赋值**（含 `n]`）：
   ```python
   LD_RE = re.compile(r"^(\s*#\s*)?(set_location_assignment\s+PIN_\d+\s+-to\s+ld\[\d+\])\s*$")
   ```
2. **`do_check` 新增"格式检查"**：用 `LD_WELLFORMED_RE` 找出**缺 `n]` 结尾的行**并报错，
   同时断言 ld 行数恰为 16。**补上"数行数"数不出的那一维。**
3. **`quartus/puzzle.qsf` 按 `docs/04` §2 的引脚表补回 16 行完整赋值**
   （`ld[0]`=PIN_80 … `ld[7]`=PIN_73、`ld[8]`=PIN_144 … `ld[15]`=PIN_137）。
4. **重编译 + 重烧**：`board_test_top` **439 LE / 68 引脚**、0 errors；
   `check_pins.py` 全绿、68 引脚无重复。**修复前那个 `.pof` 只有 53 引脚 —— 15 个 LED 没绑上引脚。**

### 预防措施

1. ⭐ **"改写文本"的正则必须捕获整行，不能只捕获前缀** ——
   用 `group(2)` 当整行写回时，**正则的右边界就是数据的右边界**。
   写这类脚本时一律：**要么 `.*` 收尾，要么 `$` 收尾**，绝不留"匹配到一半"的正则。
2. ⭐ **校验器要校验"会坏的那一维"**：旧 `check` 数行数 → 数不出截断。
   **问自己"如果它坏了，会长成什么样"，然后照着那个样子写检查。**
3. ⭐ **`set_top.py` 这类"改写真值源文件"的脚本，必须配一个独立的外部校验**
   （本项目已有 `scripts/check_pins.py`）**，并在改完之后立刻跑**。
   —— 本次是**收工检查时才发现**，已经带病编译烧录过两次。
4. ⭐ **`.qsf` 是引脚号的唯一真值源**（`CLAUDE.md` §2 硬约束 3），
   它被破坏属于**最高级别事故**：编译不会报错、板子会"看起来在工作但引脚是错的"。
   → 收工清单里应固定加一条：**改过 `.qsf` 就跑 `check_pins.py`**。

---

---

## ERR-0038 `clk_gen` 仿真抓出两处「文档与实现不符」—— 仿真第一次真正抓到东西

- **日期**：2026-09-24
- **阶段**：模块级仿真（L3，`clk_gen` 第 1~3 轮）
- **涉及模块**：`docs/02` §3.6-1 / §3.6-9（**已订正**） / `rtl/puzzle_pkg.vhd` 的 `T_POR_MS` 注释（**待一并修正，仅注释**）
- **状态**：**已订正文档**；RTL 注释的 off-by-one 记为待办（改注释不影响网表，但要按闸门流程走）

### 现象

`clk_gen` 的功能仿真（3 轮 / 18 条断言，全绿）**跑出两个与文档不符的实测值**：

```
① §3.6-1 写「前 10ms o_rst='1'」        实测 9.156 ms
② §3.6-9 写「o_rst 恒为 '1'」            实测前 ~10.9 ms 内 o_rst = '0'
```

### 定位过程

1. **① 是 off-by-one**：`r_por_cnt` 的释放条件是 `= T_POR_MS-1`（= 9），
   即只累加了 **9** 个 `tick_1k` → **9 ms**，而常量名 `T_POR_MS := 10` 与注释都写"10ms"。
   **同 ERR-0016（半周期计数 off-by-one）一族** —— 这是本项目第二次出现同一类计数边界错误。
2. **② 是"POR 窗短于消抖窗"**：POR 在 **9.16 ms** 释放，而按键消抖要满 **20.09 ms** 才认定
   → 中间约 **10.9 ms** 是 `o_rst='0'` 的窗口。
   该条**真正要验的是后半句**（"消抖计数器不被 `o_rst` 停住"），那句是成立的。
   → **按字面写 tb 断言必然失败** —— 而这条断言正是课件 p45「复位必须用按键实现」的验证点。

### 根本原因

**文档里的"期望值"是按"设计意图"写的，不是按"实现算出来的"。**

两处都属同一类：**写文档时把"想要的数"当成了"实际的数"**，而没有照着 RTL 把计数边界数一遍。

> ⭐ **这正是"用仿真收敛"的价值所在**：这两个偏差**上板永远看不出来**
> （9 ms 还是 10 ms 人眼看不出；10.9 ms 的 `o_rst` 窗口更看不出来），
> 但**照文档写 tb 会失败、答辩被追问会答错**。**仿真把它们变成了可数的实测值。**

### 修复方法

- **`docs/02` §3.6-1**：改为"前 **9 ms**"，并注明 off-by-one 的成因；
- **`docs/02` §3.6-9**：改为"前 ~10.9 ms 内 `o_rst`='0'"，并说明该条真正要验的是后半句；
- **待办（仅注释，不影响逻辑）**：`puzzle_pkg` 的 `T_POR_MS` 注释写"上电复位时长（ms）"，
  实际是 `T_POR_MS-1` ms。**按闸门规则，改 `rtl/` 要重跑该模块仿真** —— 排到下次动 `rtl/` 时一并做。

### 预防措施

1. ⭐ **文档里的每一个"期望数值"都必须能追溯到 RTL 的算式**，不能按设计意图写。
   **写数值前把计数边界数一遍**（`= N-1` 还是 `= N`？数的是第 N 拍还是第 N+1 拍？）。
2. ⭐ **凡"两个时序窗叠加"的判据（POR × 消抖、扫描 × 消抖、节拍 × 状态机），
   都要先算清谁长谁短** —— 短的那个先结束，中间那段的状态往往与直觉相反。
3. **本项目已两次栽在计数边界上（ERR-0016、本条）** ——
   建议把"边界数一遍"写进 tb 编写清单。

---

<!--
=====================================================================
 复制下面的模板新增记录（**在文件末尾**；加完记得回顶部「问题索引」表补一行）
=====================================================================

## ERR-XXXX <一句话标题>

- **日期**：2026-09-17
- **阶段**：模块编写 / 仿真 / 综合 / 布局布线 / 上板
- **涉及模块**：
- **状态**：未解决

### 现象

（原文粘贴报错信息，或详细描述波形/板级异常）

### 定位过程

1.
2.
3.

### 根本原因

### 修复方法

- 文件：`rtl/xxx.vhd` 第 N 行
- 改动：

### 预防措施

-->
