# PROGRESS — 项目进度状态

> **这是跨会话续接的核心文件。** 每次会话结束前必须更新，尤其是"上次会话断点"。
> 新会话开工按 `CLAUDE.md` §0 的顺序读：`HANDOFF.md` → 本文件 → `CHANGELOG.md` 最后一节
> → `ERRORS.md` 未解决的问题。

**最后更新**：2026-09-20（第 16 次会话结束）

> 📌 **新会话从这里开始：先读 `HANDOFF.md`**（一页纸交接单），再回本文件看详细账本。

---

## 当前状态

- **当前阶段**：阶段 1 · 设计文档 —— **已定稿、已审过五轮**，且
  **第 13 次审查的 16 条已复核缺陷 ＋ 报告 §4 的 80 条 MEDIUM/LOW（复核后 75 条确认）已全部修完**
- **下一个 commit**：`docs(图)` —— 在 `docs/图/系统图.html` 新增
  **DWG-04 逻辑流程图（整机基本工作过程）** 与 **DWG-05 ASM 图（`game_fsm`）**
  （用户已定范围与落点：整机流程图 + `game_fsm` ASM，**并入本文件、编号 DWG-04/05**）
- **下次会话第一件事**：**画 DWG-04 / DWG-05**，画完走 `CLAUDE.md` §12.4 五步清单；
  **之后**才进阶段 2 的第一个模块 `rtl/puzzle_pkg.vhd`（`feat(pkg): …`）
  —— ✅ **修硬伤四步与 80 条复核均已完成，报告里没有"待确认、不要照着改"的东西了**

> ⭐ **阶段 1 到第 9 次会话那次全量接口核对才算「真封版」** ——
> 5 处输入端口无驱动源 / 6 处代码片段照抄编译不过 / DWG-02 漏画 3 条连线（ERR-0011/0012/0013）。
> **完整经过与三条通用结论见 `CHANGELOG.md` 第 9 次会话一节**；其中一条务必记住：
> **"缺"这个错误只有换一个遍历方向才看得见** —— 理由与动作见 `CLAUDE.md` §12.4 第 5 步与 `ERRORS.md` ERR-0013。

> 🔍 **第 10 次会话又做了一次"专门找矛盾、专门删重复"的全项目复查**（用户要求），
> 依据是**另一根轴**：第 9 次比的是"文档 ↔ 代码"（接口轴），这次比的是"文档 ↔ 文档"（一致性轴）。
> 结果：`docs/01` 抓到 **14 处**内部不一致、图侧修 3 处、5 份过程文档去重（**净减约 40 行**）。
> **结论：阶段 1 现在是"复查过的封版"** —— 但请记住 ERR-0014 那句预防措施：
> **不换轴，剩下的错误就是不可见的。** 完整经过见 `CHANGELOG.md` 第 10 次会话一节。

> 🔍 **第 12 次会话走了第四根轴：符合性轴（项目 ↔ 外部权威）。**
> 前三次比的都是"项目内部两份事实是否一致"，这次比的是"**项目与课程课件对不对得上**"。
> 结果：**BTN0 按键复位全链路恢复**（课件 p45（一）："初值只具有仿真意义，复位必须用按键来实现"）、
> `.qsf` 补 **`OPTIMIZATION_TECHNIQUE AREA`**（课件 p50 例4，只在图里）、
> `CLAUDE.md` §11 重写为考核/提交要求对照 + **新增 §11.4 课件要求对照表**。
> **结论：这一轴的价值最高 —— ERR-0018 那处错误在前三根轴上全都看不见**（当时的文档是自洽的）。
> 但同时暴露出一个方法论问题：**第一次解析课件时只提了文字，漏掉 13 页纯图幻灯片，
> 而 4 条实质要求恰在图中** —— 本项目 `AI_LOG.md` 第 1 次会话就记过这条，属于复发。
> 完整经过见 `CHANGELOG.md` 第 12 次会话一节与 `ERRORS.md` ERR-0017/0018/0019。

> **⚠️ 设计方法学口径（写文档、写模块注释都按它来）：**
> 本项目的设计方法是 **自顶向下分层**（需求 → 7 个功能子系统 → 11 个实体），
> 实现顺序才是 **自底向上**，见 `CLAUDE.md` §2.1。
> 模块文件头部必须标"**所属子系统**"（S1 时钟与节拍 / S2 键盘输入 / S3 游戏控制 /
> S4 拼图核心 / S5 图案与随机数据 / S6 显示子系统 / S7 音效输出）。

> ⛔ **距离初步设计方案硬截止 2026-09-24 还剩 7 天，但「现在开始写代码更划算」这句话已作废** ——
> 第 13 次审查查出 **12 处已验证的硬伤**（含 3 处多重驱动、多处照抄编译不过、两处静默失效）。
> **照现在的文档写代码，写出来的东西要么编译不过、要么功能静默不对。**
> 当前该做的是**按 `docs/审查报告-2026-09-18.md` §6 的顺序修**，四步完成后再进阶段 2。

> ⚠️ **写代码前的三条必读硬约束**（细则全在 `CLAUDE.md`，此处只列条目）：
> - §2 第 9 条 + §10.2 ① —— `integer` 必须带 `range`；
> - §10.2 ② —— 分频器**串行级联**，不要五个并行（99 位 → 33 位）；
> - §10.1 + `docs/01` §2.1.1 —— **`rng_lfsr` 不接任何节拍**，由 `puzzle_ctrl` 的 `rnd_step`
>   **请求推进**。写成"每 tick 采一次"会让**同一拍内多次落位尝试读到同一个随机数 →
>   游戏永远不随机，且几乎查不出来**。
>
> 前两条已经引起过一轮文档返工（`docs/02` §3 原写的是并行方案），
> 说明**新规范写下来之后要拿它反查已有文档**，这一步已经固化成习惯（见 `AI_LOG.md`）。

---

## 阶段进度

| 阶段 | 内容 | 状态 | 截止 |
|---|---|---|---|
| 0 | 项目奠基（目录/文档体系/Quartus 工程） | ✅ 完成 | 2026-09-18 |
| 1 | 设计文档（初步设计方案） | 🔨 **已定稿、审出的缺陷全修完**；**只差 DWG-04 / DWG-05 两张图**（4 份主文档 + 5 张图）**← 现在在这里** | **2026-09-24 硬截止** |
| 2 | 基础驱动模块 | ⬜ 未开始（**画完两张图就进这里**） | 2026-09-25 |
| 3 | 数据与逻辑模块 | ⬜ 未开始 | 2026-10-02 |
| 4 | 集成（状态机 + 顶层） | ⬜ 未开始 | 2026-10-09 |
| 5 | 上板调试（实验室） | ⬜ 未开始 | — |
| 6 | 验收准备（报告 + 答辩） | ⬜ 未开始 | 验收前 |

> 图例：⬜ 未开始 / 🔄 进行中 / ✅ 完成 / ⚠️ 有阻塞

---

## 模块进度

| # | 模块 | 编写 | 仿真 | 上板 | 备注 |
|---|---|---|---|---|---|
| 1 | `clk_gen` | ⬜ | ⬜ | ⬜ | 接口已定，见 `docs/02` §3 |
| 2 | `keypad_scan` | ⬜ | ⬜ | ⬜ | 行列极性待上板确认 |
| 3 | `seg_scan` | ⬜ | ⬜ | ⬜ | 需做消隐防鬼影 |
| 4 | `dot_matrix_scan` | ⬜ | ⬜ | ⬜ | 需做消隐防鬼影 |
| 5 | `pattern_rom` | ⬜ | ⬜ | ⬜ | 胜利/失败图案位图已定稿（2026-09-18，见 `docs/02` §2.5/§7.4） |
| 6 | `piece_rom` | ⬜ | ⬜ | ⬜ | 零片常量已实测，见 `docs/02` §8.3 |
| 7 | `rng_lfsr` | ⬜ | ⬜ | ⬜ | ⚠️ 复位不能为全 0 |
| 8 | `puzzle_ctrl` | ⬜ | ⬜ | ⬜ | 算法已完全设计，见 `docs/02` §10 |
| 9 | `disp_format` | ⬜ | ⬜ | ⬜ | 纯组合，倒计时用 BCD |
| 10 | `game_fsm` | ⬜ | ⬜ | ⬜ | 状态迁移表已定，见 `docs/02` §12 |
| 11 | `buzzer_ctrl` | ⬜ | ⬜ | ⬜ | 音效表已定，见 `docs/02` §13 |
| — | `puzzle_top` | ⬜ | ⬜ | ⬜ | 引脚绑定 |
| — | `board_test_top` | ⬜ | ⬜ | ⬜ | **上板前必须先做出来** |

---

## 文档进度

| 文档 | 状态 | 备注 |
|---|---|---|
| `docs/审查报告-2026-09-18.md` | ✅ | **第 13 次会话新增（729 行）**：34 个子 agent 的系统性审查报告。§1 十二条已验证的 CRITICAL/HIGH、§2 六条跨模块集成、§3 四条完整性批评、§4 八十条 MEDIUM/LOW、§5 未复核项与覆盖说明、§6 八步行动清单。**✅ 任务已全部执行完毕**（§4 的 80 条由第 15 次会话复核：75 确认 / 1 推翻 / 4 已修 / 0 存疑，并全部改掉），**此后它转为历史档案，不再是行动清单** |
| `HANDOFF.md` | ✅ | **第 7 次会话新增**：一页纸会话交接单（项目是什么 / 现在到哪 / 下一步做什么 / 铁律索引 / 工具链 / 已知薄弱处）。`CLAUDE.md` §0 已把它列为**开工第一个读**的文件 |
| `CLAUDE.md` | ✅ | **第 16 次会话精简：1130 → 1009 行 / 37 828 → 36 481 字符**。判据「**删事实的副本、留陷阱的警示**」：删掉 §4.1 引脚表、§10 模块表、§10.3 内含端口表的 48 行骨架等**第二真值源**（引脚号真值源仍是 `.qsf` / `docs/04` §2）；`⚠️` **36 个**、56 个小节标题**一个没少**。§0 收工流程补「**触发判据 = 这一轮有没有产生提交**」（ERR-0021 / ERR-0022） |
| `.gitignore` | ✅ | PDF 已排除 |
| `README.md` | ✅ | |
| `quartus/puzzle.qsf` | ✅ | **68 个引脚**已用脚本验证与手册逐条吻合。末次修改：第 12 次会话补入 `btn`(PIN_61) 并按课件 p50 加 `OPTIMIZATION_TECHNIQUE AREA` |
| `scripts/vwf.py` | ✅ | `.vwf` 读写库，自带自检 |
| `scripts/verify_tiling.py` | ✅ | 拼图分解穷举验证 |
| `scripts/check_pins.py` | ✅ | 引脚一致性检查，已做负向测试 |
| `scripts/check_svg.py` | ✅ | **第 5 次会话重写，第 6 次会话修误报**：五类检查 —— ①XML 合法性 ②显式填充 ③属性覆盖 ④**走线横穿方框** + id 冲突/悬空引用。②③ 的判据从 `<style>` 直接解析（不维护硬编码白名单，见 ERR-0005）；④ 只把**有填充**的 rect 当方框（`fill:none` 的边界框是"范围"不是"障碍"，见 ERR-0007）。**每项都做了正例 + 反例负向测试**。⚠️ **查不出"标签压线"**（需字体度量）——**那项仍要人放大看**（ERR-0008 就属这类） |
| `scripts/build.tcl` | ✅ | 编译驱动 + 资源报告打印 |
| `scripts/check_ports.py` | ✅ | **第 11 次会话新增**：接口连通性检查（docs/01 §10.2 的人工动作脚本化）。**第 12 次会话补入 `btn` 的板级输入与别名登记**（它当场抓出了新增端口 `i_btn_rst` 未登记——见 ERR-0017）。当前：11 实体 / **58 输入** / 44 输出全部有驱动源与接收者；`--selftest` 负向测试（正例零误报 + 两类注入均检出） |
| `docs/图/系统图.html` | ✅ | **两层图**：DWG-01 总体框图（7 个功能子系统）+ DWG-02 模块框图（11 实体 + 2 顶层）+ DWG-03 状态转移图。内联 SVG，可导入 draw.io。DWG-01 采用**用户飞书白板稿第 2 版**布局；DWG-02 已补齐与 §2.1 对应的全部连线（ERR-0008 / 0009 / 0013）；第 10 次会话统一了 `clk` 的分层画法并修了两处版面溢出（ERR-0014 / 0015） |
| `ERRORS.md` | ✅ | **22 条记录**（ERR-0001~0022，**全部已解决**），文件开头有索引表。ERR-0020 = 第 13 次审查的发现清单（16 条已复核 ＋ 80 条复核后 75 条确认，**均修完**）；ERR-0021 = 第 15 次会话跳过收工流程；ERR-0022 = 精简 `CLAUDE.md` 的先估后量。⚠️ 文末模板的标题是 `ERR-XXXX`（**不能写成 `ERR-0001`**，否则 `grep '^## ERR'` 数条数会多数出一条） |
| `docs/图/DWG-01-系统总体框图-飞书原稿.png` | ✅ | 用户手绘稿的原始导出（**已更新为第 2 版**），**留档用**。⚠️ **以 `系统图.html` 的矢量版为准**，两者有出入时改矢量版重新导出 |
| `docs/00-需求分析.md` | ✅ | **§0 新增「依据来源」**（第 12 次会话）：题目细则来自**云平台**（课件 p62 明写正文不含）、通用强制要求来自课件 p45~p60、引脚来自板手册；11 条要求逐条拆解 + 需求追踪矩阵 |
| `docs/01-系统设计.md` | ✅ | 总体框图 + 模块划分 + 接口定义。**初步设计方案主体**。**§5 是接口的唯一真值源**（§5.0~§5.11 共 **12 节** = 11 个实体各 1 节 + `puzzle_pkg` 1 节）；**§2.1 连接表 = 4 条系统级 + 14 条子系统出线，共 18 条**（第 12 次会话按课件 p45 补入 `btn`；2026-09-18 审查补入 `kp_row`/`kp_col` 并改分表口径——原口径把这两条漏在两类之外）；§10.2「输入端口必须有驱动源」是可数动作（ERR-0011 的预防措施）。§2.1.1/§2.1.2 记录 DWG-01 两版定稿时的修正；**§2.2 已删掉那张会漂移的 ASCII 拷贝**，改为"要查什么 → 去哪里"索引表（第 10 次会话，ERR-0014） |
| `docs/02-模块详细设计.md` | ✅ | 11 个实体 + `puzzle_pkg` 逐个展开（含算法、时序、省面积写法）。写代码时**照它抄**，不要另发明。⚠️ **§10.5 / §10.7 / §10.8 已整段重写**，其中标 ⚠️ 的段落（运算符优先级、常量折叠、伪代码）**要读，不要跳**；§12.9 共 17 条验证点（含 2 条 ⭐ 残留/契约验证点）。**§12.3.1 声明本状态机为 Moore 型 + 双进程·形式 2**（课件 p30–32 必问；2026-09-18 审查 `pdf-2` 由"形式 1"更正）；§3.4 含按键复位骨架（**消抖计数器不得被 `o_rst` 复位**） |
| `docs/04-引脚分配表.md` | ✅ | 引脚表（**68 个**，第 12 次会话补 `btn`/`PIN_61`）+ 位序说明 + 极性与 p55 分歧裁决 + 自检清单（**10 条**）。§3.3 键号表的更正已用**课件 p55 独立复核** |
| `docs/03-仿真验证方案.md` | 🔄 | **框架已搭**（第 11 次会话）：仿真策略 / 标准循环 / 判读规范定稿，12 张结果记录表留空，随仿真推进逐个模块填充。**第 12 次会话新增 §3.1「波形里必须含中间信号」**（课件 p59 硬要求 + `Node Finder` 用法） |
| `docs/05-硬件调试记录.md` | ⬜ | 上板后 |
| `docs/报告/实验报告.md` | ⬜ | 阶段 6 |
| `PROGRESS.md` / `CHANGELOG.md` / `ERRORS.md` / `AI_LOG.md` | ✅ | 每次会话收工必更新 |

---

## 进行中（未完成的具体工作）

- 🔨 **DWG-04 逻辑流程图（整机）＋ DWG-05 ASM 图（`game_fsm`）尚未画** ——
  用户已定范围与落点（**并入 `docs/图/系统图.html`，编号 DWG-04 / DWG-05**），
  **下次会话的第一件事**。画完必须走 `CLAUDE.md` §12.4 五步清单。
  ⚠️ 三处要同步改：① `系统图.html` 的**页眉写"三张图"、标题栏写"共 3 图"**；
  ② `CLAUDE.md` §12.1 的「当前图集」表；③ `docs/02` §12.3.1 末尾现记着
  「逻辑流程图 / ASM 图**不单独画**」—— 这句要改成"见 DWG-04/05"。
- ⚠️ **`quartus/` 工程现在还不能编译** —— `.qsf` 里列的 6 个 VHDL 文件
  （`puzzle_pkg` / `clk_gen` / `seg_scan` / `dot_matrix_scan` / `keypad_scan` /
  `board_test_top`）在 `rtl/` 下**都还不存在**。这是正常的：
  RTL 要按阶段 2 逐个写出来。**第一个能编译的版本出现在 `board_test_top` 写完时。**
- `scripts/sim.py`（仿真驱动脚本）暂缓 —— 等 `clk_gen` 写出来后**针对真实模块**再写，
  避免凭空设计接口。仿真库 `scripts/vwf.py` 已就绪并验证通过；
  接口检查 `scripts/check_ports.py` 已完成（第 11 次会话，见「文档进度」表）
- ⚠️ **`CLAUDE.md` §11.4 有两项未闭合待办**：① 题目 4 的细则要到云平台取（课件正文没有）；
  **②「工作视频」尚未排期** —— 必须在阶段 5 上板时录（课件 p65 的提交物，出实验室就补不出来了）

---

## 阻塞 / 待确认

| # | 事项 | 影响 | 何时解决 |
|---|---|---|---|
| 1 | 板载时钟档位需确认为 **7 (50MHz)** | 所有分频系数 | 首次上板 |
| 2 | 键盘行列方向与极性需实测确认 | 按键是否可用 | 首次上板 |
| 3 | ~~第二关图案与零片分解~~ | — | ✅ **已解决**：向上箭头 18 格 4 块（6+6+4+2），掩码已实测写入 `docs/02` §2.5 |
| 4 | ~~"确认"键在零片位置错误时的语义~~ | — | ✅ **已解决**：全部锁定后**立即判定**，拼错**立即判失败**（用户 2026-09-17 确认，见下） |

> **第 4 项说明**（`docs/00-需求分析.md` §2 要求 9、`docs/02` §10.8）：
> 按"确认"锁住当前零片，**全部零片都锁定后立即判定**——拼对进下一关，
> 拼错**立即判失败**，不等倒计时耗尽。
>
> **两个失败条件**：① 全部锁定但并集 ≠ 目标图案；② 倒计时归零仍有零片未锁定。
>
> **不再考虑"可解锁重摆"**：要求 8 明确规定确认后零片"**不可再选择及移动**"，
> 允许解锁与该条冲突。**裁决为与要求 8 严格一致。**

---

## 下一步任务清单

> **下一步做什么 → 见 `HANDOFF.md` §3**（唯一清单，避免两处漂移）。

---

## 已打通的工具链

**工具路径与命令 → 见 `CLAUDE.md` §5**（§5.1 路径 / §5.2 命令 / §5.3 仿真方案，含 `.vwf` 的实测结论）。

已端到端跑通的验证闭环：`quartus_map → Python 生成激励 .vwf → quartus_sim →
Python 解析结果 → 参考模型逐点比对`。各脚本的用途与状态见上方「文档进度」表。

---

## 已完成的关键决策（避免重复讨论）

| 议题 | 决策 |
|---|---|
| 实现范围 | 11 条基本要求 + 三项提高（音效 / 多图案随机 / 零片位置随机）；**不做** 90°旋转 |
| **仿真方案** ⭐ | 仅用 **Quartus II 9.1 内置仿真器**（本机无 ModelSim/GHDL）。它**没有任何 Tcl 接口**（实测 `::quartus::simulator` 是空包）→ 所以驱动与判定**全用 Python**：生成 `.vwf` 激励 + 参考模型逐点比对。完整链路见 `CLAUDE.md` §5.3 |
| 文档形态 | 仓库内中文 Markdown 全套 |
| 关卡号显示 | 统一用 **DISP0**；判定要求9 的"DISP2"为原文笔误 |
| 键盘映射 | 方向键居中十字布局（见 `CLAUDE.md` §4.3） |
| 课程 PDF | 已加入 `.gitignore`，不入库 |
| 目标时钟 | **50MHz**，分频常量单点定义 |
| 成功判定 | 零片掩码并集 == 目标掩码（**已穷举验证有 2 种合法铺法**） |
| 引脚号来源 | **只认开发板手册**；旧工程仅用于交叉验证"用到了哪些引脚" |
| **确认键语义** | **锁定语义**：按"确认"锁定零片，全部锁定后才判定（见"阻塞"第 4 项） |
| **时间/空间分离** | `puzzle_ctrl` 只管坐标掩码（无计数器），`game_fsm` 只管状态时间（无坐标运算） |
| **单时钟域** | 全设计一个 50MHz 时钟 + 单周期使能脉冲，**不使用门控时钟** |
| **SW7 门控位置** | 在**输出级**门控（严格零延迟），**不放进状态机** |
| **倒计时数据形式** | **BCD 两位**，避免综合出除法器 |
| **点阵位序** | `bit0` = 左上角；`dot_colr` 与掩码**天然对齐，全链路零次翻转** |
| **零片掩码存储** | **不存储**，由锚点 + 形状组合计算（省 256 个触发器） |
| **设计方法学** | **设计自顶向下**（需求 → 7 个功能子系统 → 11 个实体），**实现自底向上**（先叶子模块后集成）。两者不矛盾，作用在不同阶段（`CLAUDE.md` §2.1） |
| **框图分两层** | DWG-01 = 7 个功能子系统；DWG-02 = 11 个实体（DWG-01 的展开）；DWG-03 = 状态转移图。两层可互查（`docs/01` §2.1 / §2.2 / §3.1） |
| **框图定稿版** | **DWG-01 以用户飞书白板稿的布局为准**（第 6 次会话），矢量版存 `docs/图/系统图.html`，PNG 只作留档。**两者有出入时以矢量版为准** |
| **`rng_lfsr` 不接节拍** ⭐ | 由 `puzzle_ctrl` 的 `rnd_step` **请求推进**，接原始 `clk`（理由见 `CLAUDE.md` §10.1、`docs/01` §2.1.1）。DWG-01 上已删除该节拍分接 |
| **`pattern_rom`/`piece_rom` 纯组合** | 常量表，无时序。所有图案/零片掩码**在编译期折叠**，不是运行时算出来的 |
| **游戏控制是枢纽** | `game_fsm` 除推进 `puzzle_ctrl` 外，还向显示子系统送 `state·level·cnt`、向音效输出送 `sound_sel`、向图案与随机数据送 `pattern_sel·seed·rnd_step`。**管时间的那块天然是分发点** |
| **不合并 RTL 实体** | 用户说"图太细"指的是**图**不是模块。合并会砸掉 `CLAUDE.md` §2 第 11 条的"每模块可独立仿真"与十几处 `docs/02` 小节引用，而仿真波形是评分项③ 的 25 分 |
| **会话交接方式** | **新增 `HANDOFF.md` 一页纸**（第 7 次会话）。`CLAUDE.md` §0 把它列为**开工第一个读**的文件；`PROGRESS.md` 只留"过去做了什么"的账，**"下一步做什么"不在这里重复**——两处都写必然不同步 |
| **图表定稿 = 走清单** | `CLAUDE.md` §12.4 **五步**清单。第 3 步「渲染后放大 2.5~3 倍分上中下逐段看」抓到 ERR-0006(2 处) / ERR-0008(3 处) / ERR-0009(1 处) 共 **6 处**；第 5 步「图→表反向对账」抓到 ERR-0013 的 **3 条漏画线** —— 校验脚本**一处都没报** |
| **验收校验器的标准** | 改任何校验脚本后，**负向测试必须同时有正例与反例**；豁免掉一类对象时，要追问"豁免之后还有什么没人管了"（ERR-0007 / ERR-0009） |
| **顶层一律用 `component` 语法** ⭐ | `puzzle_top.vhd` / `board_test_top.vhd` **不用 `entity work.xxx` 直接例化**，一律 `component` 声明 + `port map`（`CLAUDE.md` §10.3、`docs/01` §10 第 5 条、模板见 `docs/02` §14.1）。**一个 `component` 块 = DWG-02 的一个方框**，图与代码的对应关系肉眼可见，评分项⑧ 10 分直接受益。**代价**：端口写两遍，改接口要同步顶层声明 —— 且 `port map` 漏写端口**不报错、静默悬空** |
| **`docs/01` §5 补 §5.11** | 接口定义表原缺 `buzzer_ctrl`（ERR-0010）。`docs/02` §15 对照表新增「**接口定义**」列，用于核对 §5.0~§5.11 **12 节连续覆盖** |
| **音效触发源** ⭐ | `buzzer_ctrl.i_trigger` 由 **`game_fsm.o_sound_trig`** 驱动（用户 2026-09-17 拍板）。音效的触发时机（按键 / 锁定 / 倒计时 / 胜负）本来就全是 `game_fsm` 产生的事件，**不给 `buzzer_ctrl` 自己数拍子** |
| **键号译码位置** ⭐ | 「键号 → 动作」（上/下/左/右/选择/确认/开始）的译码**放 `game_fsm` 内**（用户 2026-09-17 拍板）。这是**游戏规则**；`keypad_scan` 只回答"按了哪个键"，是纯器件驱动，**不认识"上下左右"** |
| **连线表条数** ⭐ | `docs/01` §2.1 = **4 条系统级 + 14 条子系统出线 = 18 条**（第 12 次会话 +1：`btn`；2026-09-18 审查 +2：`kp_row`/`kp_col`，并把"子系统间"改称"子系统出线"）。**改图时直接数这个数**（ERR-0013 的预防措施） |
| **端口核对方向** ⭐ | 核对"输入端口有没有驱动源"**必须从线到模块**，不能按模块翻；建议脚本化 `scripts/check_ports.py`，放在阶段 4 写 `puzzle_top` 之前（`docs/01` §10.2）。理由见 `CLAUDE.md` §12.4 第 5 步与 ERR-0013 |
| **图与表对账两遍** ⭐ | 「表 → 图」查不一致，「图 → 表」查漏画。**只做一遍必漏** —— ERR-0013 的 3 条漏画线就是只做「表→图」时漏掉的（`CLAUDE.md` §12.4 已补） |
| **文档去重原则** ⭐ | **同一事实只留一个真值源**，其余位置写"去哪查"的指针。第 10 次会话据此删掉 `docs/01` §2.2 那张 60 行 ASCII 图（它是 §2.1 的第二份拷贝，且已经漂移）。**判据：同一事实的第二份拷贝 = 下一次改版时必然出错的点**（ERR-0014） |
| **`clk` 的分层画法** ⭐ | **DWG-01 不画** `clk`（它是 `clk_gen` 子系统的属性，折进椭圆小字）；**DWG-02 也不画 8 条实线**，改用**脚注逐个列出 8 个接收者实体名**（名单是可数的，这是替代画线的等价物）。理由见 `docs/01` §2.1 的 ⚠️ 与 §2.1.2 |
| **封版 ≠ 自洽** ⭐ | 第 9 次会话沿**接口轴**核对（文档 ↔ 代码）查出 14 处；第 10 次沿**一致性轴**核对（文档 ↔ 文档）又查出 14 处。**遍历的轴不同，能看见的东西就不同** —— 不换轴，剩下的错误就是不可见的（ERR-0014 预防措施） |
| **`tick_*` 是使能不是电平** ⭐ | `tick_*` 全是**单周期使能**。**闪烁方波 `o_blink` 由 `tick_100` ÷25 ÷2 得到**，不是把 `tick_2hz` 当方波送（那样实际只有 1Hz）。`tick_2hz` 的真实用途是**开机自检的 2 秒计时**（ERR-0014 第 4 处） |
| **复位三源** ⭐ | `o_rst <= r_btn_rst or r_por_rst or (not sys_en)`。**课件 p45（一）强制"复位必须用按键来实现"** —— `por_rst` 只是"上电初值"，**不能替代按键**。见 ERR-0018 |
| **按键复位的实现** ⭐ | `btn` → `clk_gen.i_btn_rst` → **2 级同步器（消亚稳态）+ 20ms 消抖（数 `tick_1k`）** → `r_btn_rst`。**消抖计数器不得被 `o_rst` 复位**（否则永远退不出复位） |
| **消抖判据 = 器件类型** ⭐ | **SW7 是拨动开关**、要求"即时熄灭" → **不消抖**；**BTN0 与矩阵键盘是机械按键** → **消抖**（20ms / 80ms）。课件 p54「按键要注意防抖处理」＋手册附图16。**不是"抖动有害才消抖"，而是"按键就要消抖"**（ERR-0018 第二处判断反转） |
| **裁决顺序** ⭐ | 文档冲突时按 **课件/题目 → 开发板手册 → 本项目约定 → 文档内部一致性** 的顺序裁决。**"改哪边省事"从来不是判据** —— ERR-0018 就是按"哪边改动小"把课件明文要求删掉的 |
| **Quartus 优化目标 = AREA** ⭐ | `.qsf` 里 `set_global_assignment -name OPTIMIZATION_TECHNIQUE AREA`（课件 p50 例4）。EPM1270 只有 1270 LE 且本设计不要求速度 → 面积优先。**默认是 `Balanced`，必须显式改** |
| **PDF 解析必须看图** ⭐ | 课件 65 页里 **13 页是纯图幻灯片**，`pypdf` 提取为空，而 **4 条实质要求只在图里**。**判据：提取完先数"空页"，空页一律渲染后逐页看**（`AI_LOG.md` 第 1 次会话就记过，第 12 次复发） |
| **端口改动的固定落点** ⭐ | **`CLAUDE.md` §7.1**：9 个落点 + 2 遍反向对账 + 1 条收尾 `grep`。ERR-0017 就是漏了其中 4 处（含接口唯一真值源 `docs/01`） |

---

## 三条"同类陷阱"（实现时必须专门验证）

**三处本质相同的边界回绕 bug**（都是"无符号减法/移位在边界回绕"）：
掩码逐行移位（`docs/02` §2.4）/ 零片上移越界（§10.7）/ 倒计时 `00` 回绕到 `99`（§12.5）。
根除办法见 `CLAUDE.md` §10.1 —— **靠"锚点不变量"根除，不靠移位后修补**；细节全在 `docs/02`。

---

## 上次会话断点

> **2026-09-20 第 16 次会话结束 —— 「下一步做什么」见 `HANDOFF.md` §3（唯一清单）。**
> 各次会话的详细记录（起因 / 做了什么 / 最值得带走的一条）全在 `CHANGELOG.md`，此处只留一行索引。

- **第 16 次**：**精简 `CLAUDE.md`**（用户要求）—— 判据「删事实的副本、留陷阱的警示」，
  **1130 → 1009 行**；`⚠️` 36 个 / 56 个小节标题一个没少，本文件内自引用零悬空。
  另：**开工时撞出 ERR-0021**（第 15 次会话跳过收工流程 —— 表头、`CHANGELOG`、`AI_LOG` 全空），
  本次代为补齐并补记两节；如实记下 ERR-0022（先估后量：承诺 ~800、实得 1009）。
- **第 15 次**：**复核并修完报告 §4 的 80 条 MEDIUM/LOW** —— 10 个 agent × 每批 8 条、
  默认立场"怀疑"：**75 确认 / 1 推翻 / 4 已修 / 0 存疑**；75 条按 241 条编辑指令全部改掉
  （11 个文件 / 889 增 218 删）。⚠️ **本次未走收工流程**（见 ERR-0021）。
- **第 14 次**：按审查报告修缺陷 —— **已复核的 16 条全部修完**（12 CRITICAL/HIGH ＋ `sim-03`/`INT-4`/`INT-5`/`CRIT-2`），
  另清掉 4 条「照抄编译不过」类（`seg_scan-01`/`buzzer_ctrl-1`/`buzzer_ctrl-3`/`puzzle_ctrl-3`）；
  含 DWG-01 的图↔表对账（补 `pattern_mask`、删多余的节拍分接，走完 §12.4 五步）。
- **第 13 次**：进代码前的系统性审查（第五根轴：照着做能不能跑）—— 34 个子 agent 四阶段；
  106 条发现、12 条经"默认推翻"复核确认；产出 `docs/审查报告-2026-09-18.md`（729 行）与 ERR-0020。
  当时的结论是"四步修复完成前不要开始写代码"—— **该结论已由第 14/15 次会话执行完毕**。
- **第 12 次**：课件 PDF 全量解析与合规补全（第四根轴：符合性）—— 65 页完整解析（含 13 页纯图页）、BTN0 按键复位全链路、`.qsf` 加 AREA、`CLAUDE.md` §11 考核/提交对照表、§7.1 端口落点清单（ERR-0017/0018/0019）。
- **第 11 次**：规划审查与完善（第三根轴：可执行性）—— 胜负图案掩码定稿、`docs/03` 框架、`scripts/check_ports.py`（ERR-0016）。

- **第 10 次**：全项目复查（另一根轴：文档 ↔ 文档）—— `docs/01` 的 14 处内部不一致、图侧 3 处、过程文档去重（ERR-0014/0015）。
- **第 9 次**：接口全量核对与修复，阶段 1 真封版 —— 5 处输入端口无驱动源 / 6 处代码片段照抄编译不过 / DWG-02 漏画 3 条连线（ERR-0011/0012/0013）。
- **第 8 次**：顶层例化方式定为 `component` 语法；顺带撞出 `docs/01` §5 漏了 `buzzer_ctrl`（ERR-0010）。
- **第 7 次**：收尾准备 —— 建立 `HANDOFF.md`；DWG-01 同步飞书稿第 2 版（ERR-0009）。
- **第 6 次**：用户手绘总体框图、AI 审图后 DWG-01 定稿（ERR-0007/0008）。
- **第 5 次**：设计方法学表述修正（设计自顶向下、实现自底向上）；总体框图改两层。
- **第 1~4 次**：项目奠基 / 打通自动仿真链路 / 4 份设计文档 / 图表与资源利用率约定。
