# PROGRESS — 项目进度状态

> **这是跨会话续接的核心文件。** 每次会话结束前必须更新，尤其是"上次会话断点"。
> 新会话开工按 `CLAUDE.md` §0 的顺序读：`HANDOFF.md` → 本文件 → `CHANGELOG.md` 最后一节
> → `ERRORS.md` 未解决的问题。

**最后更新**：2026-09-24（第 22 次会话结束）

> 📌 **新会话从这里开始：先读 `HANDOFF.md`**（一页纸交接单），再回本文件看详细账本。

---

## 当前状态

- **当前阶段**：**阶段 2/3/4 的 RTL 全部写完并编译通过**（`rtl/` 下 14 个文件），
  整机 `puzzle_top` **1226 / 1270 LE（97%）**、**Fmax 63.66 MHz ≥ 50 MHz** ✓
- **阶段 5（上板）已第一次开跑**（2026-09-24，第 21 次会话）：
  `board_test_top` 已烧进板子（JTAG ID `0x020A30DD`，Erase→Program→Verify 全过），
  现场报的两条"异常"经判读**都不是硬件故障**：①"非常混乱"= 9 阶段轮播的正常画面
  （`ld[7:0]` 是 4Hz 子步号）；②"SW7=0 时只有 LD8 亮"= `ld` 不受 SW7 门控的**预期**行为
  （→ 见 `ERRORS.md` **ERR-0030**，结论是**清单错了、代码没错**）。
  ✅ **13:38「30 秒判据」四步全过**：时钟档位确认 **50MHz**、SW7=1 的 LED"无规则但有周期"（正常）、
  **按住最右边 BTN0 → 16 灯全亮**（`btn` 通路 + LED 16 位通路 + 极性全通）→
  `docs/06` §6.1 第 1 行（档位不对）与 §7.1 第 10 条（按键）**全部排除**。
  ⚠️ **13:49 第三条"异常"判读完毕**：按"看阶段 1"的判据去看，报"**16 个灯都闪烁、LD8 不是常亮**"
  —— 判读为**判据不可执行**（阶段 1 只占完整轮播 36 秒里的 2 秒 ≈5.6%，看到的是阶段 2~9），
  **代码一行未改**；同时确认**最左数码管 DISP7 显示的就是当前阶段号**
  （`r_stage` → `s_disp_val(31 downto 28)`）→ `ERRORS.md` **ERR-0031**，
  `docs/06` §6.1（判读表 +1 行、30 秒判据重写）与 `docs/01` §8.2 已订正。
  ⭐ **本次上板报回的三条"异常"没有一条是代码错** —— 全是清单/判据的缺口
  （ERR-0030 缺"**物理位置**"、ERR-0031 缺"**时间窗口**"）。
- ⭐ **14:11 已切到整机 `puzzle_top` 并烧进真板**（用户 14:04 决策：**跳过自检阶段 2~8**，
  直接烧整机看效果、之后再完善）：
  顶层 `puzzle_top`、**1226 / 1270 LE（97%）**、52 引脚、0 errors；
  TimeQuest **setup slack +4.292 ns / hold +1.379 ns** → **Fmax ≈ 63.7 MHz ≥ 50 MHz**；
  烧录 **Erase→Program→Verify 全过**（JTAG ID `0x020A30DD`，14:11:18）。
  ⚠️ **代价如实记**：点阵行序/列序/红绿、数码管位序/段码、蜂鸣器这 **5 项至今无独立确认**，
  整机若显示异常**第一嫌疑是它们、不是游戏逻辑**（`docs/05` §1.6）。
  ⭐ **"进不去阶段 7"已定位** → `ERRORS.md` **ERR-0033**：`r_seen` 只在 `s_rst` 时清零，
  **按满 16 键后恒为全 1** → 阶段 7 每圈在 **0.25 秒**内退出（本该 20 秒）；
  **规避法：按 BTN0 或拨 SW7 复位**。**代码修复有意推迟**（只影响自检工具）。
- **下一步**：① **[优先] 趁还在实验室，把自检阶段 2~8 逐条打勾填进 `docs/05` §1.3**
  （点阵行列序/红绿、数码管位序/段序/亮度、键盘行列与极性、蜂鸣器），并**录工作视频**；
  ② 回到**阶段 3 · 仿真验证** —— 逐模块做 `.vwf` 激励并写 `docs/03` 结果表。
  ⚠️ **动任何代码前先看编译报告的 LE 数**：整机只剩约 44 个 LE 余量。
- 阶段 1 的交付物（4 份主文档 + 5 张图 + 初步设计方案）保持封版状态；
  阶段 2~4 的实现与原设计文档有**三处受器件约束的偏离**，
  已记录在 `ERRORS.md` ERR-0026 / ERR-0027 与 `CHANGELOG.md` 第 19 次会话一节。

> ⭐ **阶段 1 到第 9 次会话那次全量接口核对才算「真封版」** ——
> 5 处输入端口无驱动源 / 6 处代码片段照抄编译不过 / DWG-02 漏画 3 条连线（ERR-0011/0012/0013）。
> **完整经过与三条通用结论见 `CHANGELOG.md` 第 9 次会话一节**；其中一条务必记住：
> **"缺"这个错误只有换一个遍历方向才看得见** —— 理由与动作见 `CLAUDE.md` §12.4 第 5 步与 `ERRORS.md` ERR-0013。

> 🔍 **第 10 次会话又做了一次"专门找矛盾、专门删重复"的全项目复查**（用户要求），
> 依据是**另一根轴**：第 9 次比的是"文档 ↔ 代码"（接口轴），这次比的是"文档 ↔ 文档"（一致性轴）。
> 结果：`docs/01` 抓到 **14 处**内部不一致、图侧修 3 处、5 份过程文档去重（**净减约 40 行**）。
> **结论：阶段 1 现在是"复查过的封版"** —— 但请记住 ERR-0014 那句预防措施：
> **不换轴，剩下的错误就是不可见的。** 完整经过见 `CHANGELOG.md` 第 10 次会话一节。

> 🔍 **第 12 次会话走了第四根轴：符合性轴（项目 ↔ 外部权威）。**
> 前三次比的都是"项目内部两份事实是否一致"，这次比的是"**项目与课程课件对不对得上**"。
> 结果：**BTN0 按键复位全链路恢复**（课件 p45（一）："初值只具有仿真意义，复位必须用按键来实现"）、
> `.qsf` 补 **`OPTIMIZATION_TECHNIQUE AREA`**（课件 p50 例4，只在图里）、
> `CLAUDE.md` §11 重写为考核/提交要求对照 + **新增 §11.4 课件要求对照表**。
> **结论：这一轴的价值最高 —— ERR-0018 那处错误在前三根轴上全都看不见**（当时的文档是自洽的）。
> 但同时暴露出一个方法论问题：**第一次解析课件时只提了文字，漏掉 13 页纯图幻灯片，
> 而 4 条实质要求恰在图中** —— 本项目 `AI_LOG.md` 第 1 次会话就记过这条，属于复发。
> 完整经过见 `CHANGELOG.md` 第 12 次会话一节与 `ERRORS.md` ERR-0017/0018/0019。

> **⚠️ 设计方法学口径（写文档、写模块注释都按它来）：**
> 本项目的设计方法是 **自顶向下分层**（需求 → 7 个功能子系统 → 11 个实体），
> 实现顺序才是 **自底向上**，见 `CLAUDE.md` §2.1。
> 模块文件头部必须标"**所属子系统**"（S1 时钟与节拍 / S2 键盘输入 / S3 游戏控制 /
> S4 拼图核心 / S5 图案与随机数据 / S6 显示子系统 / S7 音效输出）。

> ✅ **（历史，已闭环）"先修文档、再写代码"这条指令已执行完毕** ——
> 第 13 次审查查出的缺陷由第 14/15 次会话全部修完（报告 §4 的 80 条 → 75 确认 / 1 推翻 / 4 已修），
> **代码随后写出并通过编译**（第 19 次会话）。
> `docs/审查报告-2026-09-18.md` 自此只是**历史档案，不再是任务清单**。

> ⚠️ **写代码前的三条必读硬约束**（细则全在 `CLAUDE.md`，此处只列条目）：
> - §2 第 9 条 + §10.2 ① —— `integer` 必须带 `range`；
> - §10.2 ② —— 分频器**串行级联**，不要五个并行（99 位 → 33 位）；
> - §10.1 + `docs/01` §2.1.1 —— **`rng_lfsr` 不接任何节拍**，由 `puzzle_ctrl` 的 `rnd_step`
>   **请求推进**。写成"每 tick 采一次"会让**同一拍内多次落位尝试读到同一个随机数 →
>   游戏永远不随机，且几乎查不出来**。
>
> 前两条已经引起过一轮文档返工（`docs/02` §3 原写的是并行方案），
> 说明**新规范写下来之后要拿它反查已有文档**，这一步已经固化成习惯（见 `AI_LOG.md`）。

---

## 阶段进度

| 阶段 | 内容 | 状态 | 截止 |
|---|---|---|---|
| 0 | 项目奠基（目录/文档体系/Quartus 工程） | ✅ 完成 | 2026-09-18 |
| 1 | 设计文档（初步设计方案） | ✅ **完成**（4 份主文档 + 5 张图 + Word 交付物，已审过五轮） | ✅ |
| 2 | 基础驱动模块（`clk_gen`/键盘/两个扫描/两个 ROM/LFSR） | ✅ **完成**（编写 + 编译通过） | 2026-09-25 |
| 3 | 数据与逻辑模块（`puzzle_ctrl`/`disp_format`/`game_fsm`/`buzzer_ctrl`） | 🔄 **仿真进行中**（**9/12 模块已过**：clk_gen 13/13、seg_scan 19/19、dot_matrix_scan 4/4、disp_format 8/8、buzzer_ctrl 14/14、puzzle_ctrl 19/19、game_fsm 27/27、puzzle_top 整机首跑 4/4、board_test_top 14/14；仿真抓出并修复 **ERR-0040/0041 两个真 RTL 缺陷**；剩 `keypad_scan`/`pattern_rom`/`piece_rom`/`rng_lfsr` + 13 场景整机序列） | 2026-10-02 |
| 4 | 集成（`puzzle_top` + `board_test_top`） | ✅ **完成**（两个顶层都全流程编译通过、时序收敛） | 2026-10-09 |
| 5 | 上板调试（实验室） | 🔄 **进行中**（2026-09-24 第一次上板：**自检顶层已验完 4 项、整机 `puzzle_top` 已烧录上板**；**自检阶段 2~8 有意跳过**，逐条结果见 `docs/05` §1.3 / §1.6） | — |
| 6 | 验收准备（报告 + 答辩） | ⬜ 未开始 | 验收前 |

> 图例：⬜ 未开始 / 🔄 进行中 / ✅ 完成 / ⚠️ 有阻塞
>
> ⚠️ **阶段 3 的"仿真"与阶段 2/3/4 的"编写"是两件事**：本表把"编写"记为完成，
> 是因为代码已写出并通过综合/布局布线；**逐模块的 `.vwf` 仿真与 `docs/03` 结果表尚未开始**，
> 那是下一步（评分项③ 25 分）。

---

## 模块进度

| # | 模块 | 编写 | 仿真 | 上板 | 备注 |
|---|---|---|---|---|---|
| 0 | `puzzle_pkg` | ✅ | — | — | 包不是实体，无法独立仿真；常量/掩码/状态编码单点定义 |
| 1 | `clk_gen` | ✅ | ✅ 13/13（r04） | ⬜ | 已实现 2 级同步 + 20ms 消抖；**仿真抓出 ERR-0038**（POR 实为 9.156ms 等两处文档不符） |
| 2 | `keypad_scan` | ✅ | 🔄（2×2 交叉测试已建，ERR-0039） | ⬜ | 行列极性待上板确认；消抖按"轮次"判定（20 轮 = 80ms） |
| 3 | `seg_scan` | ✅ | ✅ 19/19（r02） | ⬜ | 两相消隐防鬼影；刷新率 500Hz（8kHz / 16 拍） |
| 4 | `dot_matrix_scan` | ✅ | ✅ 4/4（r01） | ⬜ | 同上；掩码→引脚的唯一映射点，零位序翻转 |
| 5 | `pattern_rom` | ✅ | ⬜ | ⬜ | 纯组合；5 个图案常量照 `docs/02` §2.5 |
| 6 | `piece_rom` | ✅ | ⬜ | ⬜ | 纯组合；一次输出 4 块；`when others` 走第二关下箭头那套 |
| 7 | `rng_lfsr` | ✅ | ⬜ | ⬜ | 抽头 bit7/5/4/3；**不接节拍**，由 `rnd_step` 请求推进 |
| 8 | `puzzle_ctrl` | ✅ | ✅ 19/19（r03，ERR-0041 复验） | ⬜ | ⭐ **改为时分复用行扫描引擎 + 包围盒判据**（ERR-0026）；**仿真抓出 ERR-0041**（扫描顺序与注释相反） |
| 9 | `disp_format` | ✅ | ✅ 8/8（r02） | ⬜ | 纯组合；点阵像素多路选择的唯一汇合点 |
| 10 | `game_fsm` | ✅ | ✅ 27/27（r03，ERR-0040 复验） | ⬜ | 主状态机 + BCD 倒计时 + 键号译码 + 音效选择；**仿真抓出 ERR-0040**（音效脉冲 2 clk） |
| 11 | `buzzer_ctrl` | ✅ | ✅ 14/14（r04） | ⬜ | 8 种音效；方波由内部寄存器翻转（不可读 out 端口） |
| — | `puzzle_top` | ✅ | 🔄 首跑 4/4（r01，kp_col 时序） | 🔄 | 11 个 `component` + 具名 `port map`；输出级 SW7 门控。**2026-09-24 14:11 已烧上板**（1226/1270 LE、Fmax ≈ 63.7 MHz）；13 场景整机序列待跑 |
| — | `board_test_top` | ✅ | ✅ 14/14（r09，自校准） | 🔄 | 9 阶段自检；**ERR-0042**：仿真采用自校准时间基（子步数 + 内容 + 80:8 比例），绝对速率由 clk_gen 单测背书。⚠️ `r_seen` 不清零 → 按满 16 键后阶段 7 不可见（ERR-0033） |

> 实测：`board_test_top` **439 LE / 68 引脚**；`puzzle_top` **1226 LE / 52 引脚 / Fmax 63.66MHz**。
> 编译验证方法见 `docs/06-Quartus编译与烧录流程.md`。

---

## 文档进度

| 文档 | 状态 | 备注 |
|---|---|---|
| `docs/审查报告-2026-09-18.md` | ✅ | **第 13 次会话新增（729 行）**：34 个子 agent 的系统性审查报告。§1 十二条已验证的 CRITICAL/HIGH、§2 六条跨模块集成、§3 四条完整性批评、§4 八十条 MEDIUM/LOW、§5 未复核项与覆盖说明、§6 八步行动清单。**✅ 任务已全部执行完毕**（§4 的 80 条由第 15 次会话复核：75 确认 / 1 推翻 / 4 已修 / 0 存疑，并全部改掉），**此后它转为历史档案，不再是行动清单** |
| `HANDOFF.md` | ✅ | **第 7 次会话新增**：一页纸会话交接单（项目是什么 / 现在到哪 / 下一步做什么 / 铁律索引 / 工具链 / 已知薄弱处）。`CLAUDE.md` §0 已把它列为**开工第一个读**的文件 |
| `CLAUDE.md` | ✅ | **第 16 次会话精简：1130 → 1009 行 / 37 828 → 36 481 字符**。判据「**删事实的副本、留陷阱的警示**」：删掉 §4.1 引脚表、§10 模块表、§10.3 内含端口表的 48 行骨架等**第二真值源**（引脚号真值源仍是 `.qsf` / `docs/04` §2）；`⚠️` **36 个**、56 个小节标题**一个没少**。§0 收工流程补「**触发判据 = 这一轮有没有产生提交**」（ERR-0021 / ERR-0022） |
| `.gitignore` | ✅ | PDF 已排除 |
| `README.md` | ✅ | 第 18 次会话重写：新增「项目状态」阶段表、补全目录结构、文档索引拆为设计文档／过程记录／交付物三张表、新增「许可与声明」 |
| `quartus/puzzle.qsf` | ✅ | **68 个引脚**已用脚本验证与手册逐条吻合（`python scripts/check_pins.py`）。**第 19 次会话**把源文件表**补全为 14 个**、并清掉 Quartus 自动写入的 `MISC_FILE` 行（带本机绝对路径，与 ERR-0025 同类）；第 12 次会话补入 `btn`(PIN_61) 并按课件 p50 加 `OPTIMIZATION_TECHNIQUE AREA` |
| `scripts/vwf.py` | ✅ | `.vwf` 读写库，自带自检 |
| `scripts/sim.py` | ✅ | **第 21 次会话（续六）新增**：模块级功能仿真驱动（`gen` / `check` / `run`）。`run` 一键完成「绑顶层 → 生成网表 → 仿真 → 比对 → 还原」，`.qsf` 用**整文件备份/还原**保证跑完逐字节相同。自带波形 SVG 渲染与「中间信号清单缺一即报错」断言 |
| `sim/tb_disp_format.py` | ✅ | **已跑 8/8**（第 1 轮）：掩码约定、自检闪烁、待机、预览透传、前导零熄灭（两个方向）、胜负 |
| `sim/tb_dot_matrix_scan.py` | ✅ | **已跑 4/4**（第 2 轮）：掩码→引脚无翻转、行计数器完整循环、两相消隐。判据「扫全程收集状态」与采样时刻无关 |
| `scripts/verify_tiling.py` | ✅ | 拼图分解穷举验证 |
| `scripts/check_pins.py` | ✅ | 引脚一致性检查，已做负向测试 |
| `scripts/check_svg.py` | ✅ | **第 5 次会话重写，第 6 次会话修误报**：五类检查 —— ①XML 合法性 ②显式填充 ③属性覆盖 ④**走线横穿方框** + id 冲突/悬空引用。②③ 的判据从 `<style>` 直接解析（不维护硬编码白名单，见 ERR-0005）；④ 只把**有填充**的 rect 当方框（`fill:none` 的边界框是"范围"不是"障碍"，见 ERR-0007）。**每项都做了正例 + 反例负向测试**。⚠️ **查不出"标签压线"**（需字体度量）——**那项仍要人放大看**（ERR-0008 就属这类） |
| `scripts/build.tcl` | ✅ | 编译驱动 + 资源报告打印（`--top` 可临时换顶层且用完还原） |
| `scripts/encoding.py` | ✅ | **第 19 次会话新增**：`rtl/*.vhd` 的 **UTF-8 ⇄ GBK** 双向转换（`check` / `gbk` / `utf8` / `scan-bad`）。自带**往返安全校验**（装不下的字符拒绝转换并打印码位，绝不静默写 `?`）、幂等、只碰 `rtl/*.vhd`。根因见 ERR-0028；**打开 Quartus 前必跑 `gbk`，用 AI 改完 `.vhd` 后也要再跑一次** |
| `scripts/set_top.py` | ✅ | **第 19 次会话新增**：一条命令切换 `board_test_top` ⇄ `puzzle_top`（同时改 `TOP_LEVEL_ENTITY` 与 16 行 `ld[…]` 约束的注释状态），幂等、不写任何引脚号。手工要改 17 处，容易漏 |
| `scripts/_gen_fitcheck.py` | ✅ | **第 19 次会话新增**：按 `.qsf` 生成临时工程，**不动仓库**地验证任一顶层能否编译（第 19 次会话的面积/时序迭代全靠它） |
| `scripts/gen_dwg01.py` | ✅ | **第 19 次会话新增**：DWG-01 矢量重绘版的生成器（框/线/信号名都是数据结构）。属**备份版**，对外提交用用户定稿版（`CLAUDE.md` §12.1.1） |
| `scripts/annotate_dwg01.py` | ✅ | **第 19 次会话新增**：在原白板 PNG 上叠加小字信号名的加注器（`L` 表可微调）。同上，属**备份版** |
| `scripts/check_ports.py` | ✅ | **第 11 次会话新增**：接口连通性检查（docs/01 §10.2 的人工动作脚本化）。**第 12 次会话补入 `btn` 的板级输入与别名登记**（它当场抓出了新增端口 `i_btn_rst` 未登记——见 ERR-0017）。当前：11 实体 / **58 输入** / 44 输出全部有驱动源与接收者；`--selftest` 负向测试（正例零误报 + 两类注入均检出） |
| `docs/图/系统图.html` | ✅ | **五张图**：DWG-01 总体框图（7 个功能子系统）+ DWG-02 模块框图（11 实体 + 2 顶层）+ DWG-03 状态转移图 + **DWG-04 整机逻辑流程图** + **DWG-05 MDS 助记状态图**。内联 SVG，可导入 draw.io；第 17 次会话把 DWG-04/05 并入本文件（共 5 图、版本 v4），`check_svg.py` 全绿 |
| `ERRORS.md` | ✅ | **35 条记录**（ERR-0001~0035，**34 条已解决 / ERR-0033 已定位待改码**），文件开头是「问题索引」表。ERR-0020 = 第 13 次审查的发现清单（16 条已复核 ＋ 80 条复核后 75 条确认，**均修完**）；ERR-0021 = 第 15 次会话跳过收工流程；ERR-0022 = 精简 `CLAUDE.md` 的先估后量；ERR-0026/0027 = 放不进器件 / 时序不收敛；ERR-0028 = Quartus 中文注释乱码；**ERR-0029 = 索引表缺 6 行 + 多份文档状态陈旧**（第 20 次会话）；**ERR-0030 = 上板清单说"SW7=0 → LED 全灭"而 `ld` 根本不受门控**（第 21 次会话，**订文档、不改代码**）；**ERR-0031 = 判据让人"看阶段 1"却没给"怎么知道现在就是阶段 1"** —— 阶段 1 只占 36 秒里的 2 秒（≈5.6%），代码里现成的阶段号读数 **DISP7** 未被点名（第 21 次会话续二，**订文档、不改代码**）；**ERR-0032 = 「DISP7 = 阶段号」过度概括 + 阶段 6 读数方向与文档措辞相反**（第 21 次会话续二）；**ERR-0033 = 按满 16 键后"进不去阶段 7"** —— `r_seen` 只在 `s_rst` 时清零，按满后恒为全 1 → 阶段 7 每圈 0.25 秒内退出（第 21 次会话续三，**已定位 + 已给规避法，代码修复有意推迟**）；**ERR-0034 = 「看不到开机自检」** —— 自检**只持续 2 秒**（**要求 2 明文**规定），现场判读表没写这一条 → 被当成"要求 1 没实现"（第 21 次会话续四，**订文档、不改代码**）；⭐ **ERR-0035 = 键盘扫描极性反了 → 幻影「开始」把系统踢出自检态**（**改代码**：`keypad_scan` 新增 `KP_ACTIVE` 常量、改低有效；1226/1270 LE 不变；**已烧录待验证**）。⚠️ **索引行数必须等于正文条数**（对账命令见 `CLAUDE.md` §0 收工第 3 条）；文末是记录模板，标题 `ERR-XXXX`（**不能写成 `ERR-0001`**，否则数条数会多数出一条） |
| `docs/图/DWG-01-系统总体框图-飞书原稿.png` | 📦 | 用户更早一版手绘稿的原始导出，**只作留档**（不再作为出图依据）。内部对账以 `系统图.html` 的矢量版为准；**对外提交用用户定稿版**（见下表） |
| `docs/00-需求分析.md` | ✅ | **§0 新增「依据来源」**（第 12 次会话）：题目细则来自**云平台**（课件 p62 明写正文不含）、通用强制要求来自课件 p45~p60、引脚来自板手册；11 条要求逐条拆解 + 需求追踪矩阵 |
| `docs/01-系统设计.md` | ✅ | 总体框图 + 模块划分 + 接口定义。**初步设计方案主体**。**§5 是接口的唯一真值源**（§5.0~§5.11 共 **12 节** = 11 个实体各 1 节 + `puzzle_pkg` 1 节）；**§2.1 连接表 = 4 条系统级 + 14 条子系统出线，共 18 条**（第 12 次会话按课件 p45 补入 `btn`；2026-09-18 审查补入 `kp_row`/`kp_col` 并改分表口径——原口径把这两条漏在两类之外）；§10.2「输入端口必须有驱动源」是可数动作（ERR-0011 的预防措施）。§2.1.1/§2.1.2 记录 DWG-01 两版定稿时的修正；**§2.2 已删掉那张会漂移的 ASCII 拷贝**，改为"要查什么 → 去哪里"索引表（第 10 次会话，ERR-0014） |
| `docs/02-模块详细设计.md` | ✅ | 11 个实体 + `puzzle_pkg` 逐个展开（含算法、时序、省面积写法）。写代码时**照它抄**，不要另发明。⚠️ **§10.5 / §10.7 / §10.8 已整段重写**，其中标 ⚠️ 的段落（运算符优先级、常量折叠、伪代码）**要读，不要跳**；§12.9 共 17 条验证点（含 2 条 ⭐ 残留/契约验证点）。**§12.3.1 声明本状态机为 Moore 型 + 双进程·形式 2**（课件 p30–32 必问；2026-09-18 审查 `pdf-2` 由"形式 1"更正）；§3.4 含按键复位骨架（**消抖计数器不得被 `o_rst` 复位**） |
| `docs/04-引脚分配表.md` | ✅ | 引脚表（**68 个**，第 12 次会话补 `btn`/`PIN_61`）+ 位序说明 + 极性与 p55 分歧裁决 + 上板清单（**11 条**）。§3.3 键号表的更正已用**课件 p55 独立复核**。**第 21 次会话**：§4 摘全手册第 3/4/7/8/10 页原文（LED 4 色/LD0 最右/拨码"往上=开"/**BTN0 在最右**/`ld[15:8]` 与 VGA 复用）；§7.1 第 10/11 条订正（`ld` 不受 SW7 门控）；**新增 §7.3「现场位置速查」** |
| `docs/06-Quartus编译与烧录流程.md` | ✅ | **第 19 次会话新增**：从零建工程 → 编译 → 引脚分配 → 下载烧录 → 首次上板的自检顺序，含**实测资源/时序数据**与常见报错处置表。§3.3 记 9.1 **不生成 `output_files/`**（产物就在 `quartus/` 下）；§3.3.1 记"要看 50MHz 余量必须用 TimeQuest"；§5 是切顶层两步；§8 是中文注释乱码处置 |
| `docs/03-仿真验证方案.md` | 🔄 | **框架已搭**（第 11 次会话）：仿真策略 / 标准循环 / 判读规范定稿，12 张结果记录表留空，随仿真推进逐个模块填充。**第 12 次会话新增 §3.1「波形里必须含中间信号」**（课件 p59 硬要求 + `Node Finder` 用法） |
| `docs/05-硬件调试记录.md` | 🔄 | **第 20 次会话建**（2026-09-24 首次进实验室当天）：现场信息 / 烧录结果 / 自检 **9 阶段**（期望现象指回 `docs/01` §8.2，本文件只填实测）/ 阶段之外的 **3 项目视** / 结论与后续动作；文末留有下次上板的复制模板。**第 21 次会话**填入本次现场原话（§1.2~§1.4）与三条现象的判读、并给出**下次上板的 30 秒顺序**；⚠️ 自检 9 阶段**本次未逐条记录**（只剩一句"非常混乱"），下次必须当场打勾 |
| `docs/报告/实验报告.md` | ⬜ | 阶段 6 |
| `report/初步设计方案.pdf` | ✅ | **第 18 次会话新增**：初步设计方案的 Typst 排版版（12 页，6 章 / 3 图 / 7 表）；已核验图表编号、目录页码与内容完整性 |
| `report/typst/` | ✅ | **第 18 次会话新增**：PDF 排版源 —— `template.typ`（版式模板）/ `main.typ`（正文）/ `covers.typ` + `cover-compare.typ`（5 套封面方案） |
| `report/封面方案对比.pdf` | ✅ | **第 18 次会话新增**：5 套封面各一页，正式版采用「方案 A · 北邮式居中」 |
| `report/图/DWG-01-系统总体框图-含信号传递关系-定稿版.png` | ✅ | **DWG-01 对外提交版（权威）**：用户自己在白板工具里画的，已按老师要求标出模块功能与主要信号名（4034×2054）。**已嵌入 PDF 与 DOCX 的图 2-1**；⛔ **不要再往上叠字**（2026-09-23 叠过又被否，见 ERR-0029 / `CLAUDE.md` §12.1.1）。备份版：`DWG-01-信号传递关系.svg/.png`（矢量重绘）、`DWG-01-白板版-含信号传递关系.png`（原图加注） |
| `.gitattributes` | ✅ | **第 18 次会话新增**：文本统一 LF、显式标注二进制类型、修正 GitHub 语言统计 |
| `PROGRESS.md` / `CHANGELOG.md` / `ERRORS.md` / `AI_LOG.md` | ✅ | 每次会话收工必更新 |

---

## 进行中（未完成的具体工作）

- ✅ **DWG-04 动态流程图 / DWG-05 MDS 图已于第 17 次会话补齐**（并入 `docs/图/系统图.html` 第 4、5 幅，矢量源另存 `report/图/`），`CLAUDE.md` §12.1、`docs/02` §12.3.1、`README.md` 三处已同步。
- ✅ **`quartus/` 工程现在可以编译了** —— 14 个 VHDL 源文件全部就位，两个顶层都实测通过：
  - `board_test_top`（`.qsf` 里的默认顶层）：439 LE / 68 引脚；
  - `puzzle_top`（整机）：1226 LE / 52 引脚 / Fmax 63.66 MHz。
  切换顶层要改两处（见 `docs/06-Quartus编译与烧录流程.md` §5）。
- 🔄 **下一步是阶段 3 的仿真**：`scripts/sim.py` 已就绪（隔离工程版），
  **9/12 模块已全过**（含 board_test_top 整顶层，ERR-0042 见 `ERRORS.md`）；
  剩 `keypad_scan`（2×2 交叉测试已建待跑）/ `pattern_rom` / `piece_rom` / `rng_lfsr`
  四个叶子模块与 `docs/02` §14.3 的 13 个场景整机序列。
- ⚠️ **资源余量只剩 ~44 个 LE（整机 97%）**：任何新增逻辑前先编译一次看 LE 数，
  必要时按 `CLAUDE.md` §10.2 ④ 继续"分时复用"。
- ⚠️ **`CLAUDE.md` §11.4 有两项未闭合待办**：① 题目 4 的细则要到云平台取（课件正文没有）；
  **②「工作视频」尚未排期** —— 必须在阶段 5 上板时录（课件 p65 的提交物，出实验室就补不出来了）

---

## 阻塞 / 待确认

| # | 事项 | 影响 | 何时解决 |
|---|---|---|---|
| 1 | ~~板载时钟档位需确认为 **7 (50MHz)**~~ | — | ✅ **已确认（2026-09-24 13:38）**：50MHz，分频系数全部有效 |
| 2 | 键盘行列方向与极性需实测确认（自检阶段 7） | 按键是否可用 | **现场逐条打勾**（`docs/05` §1.3 阶段 7），只有 BTN0 已确认 |
| 5 | ~~**`btn`（BTN0）是否真的连通**~~ | — | ✅ **已确认（2026-09-24 13:38）**：按住**最右边**的 BTN0 → **16 个 LED 全亮**（`btn` 直读通路 + LED 16 位通路 + 极性全通） |
| 3 | ~~第二关图案与零片分解~~ | — | ✅ **已解决**：向上箭头 18 格 4 块（6+6+4+2），掩码已实测写入 `docs/02` §2.5 |
| 4 | ~~"确认"键在零片位置错误时的语义~~ | — | ✅ **已解决**：全部锁定后**立即判定**，拼错**立即判失败**（用户 2026-09-17 确认，见下） |

> **第 4 项说明**（`docs/00-需求分析.md` §2 要求 9、`docs/02` §10.8）：
> 按"确认"锁住当前零片，**全部零片都锁定后立即判定**——拼对进下一关，
> 拼错**立即判失败**，不等倒计时耗尽。
>
> **两个失败条件**：① 全部锁定但并集 ≠ 目标图案；② 倒计时归零仍有零片未锁定。
>
> **不再考虑"可解锁重摆"**：要求 8 明确规定确认后零片"**不可再选择及移动**"，
> 允许解锁与该条冲突。**裁决为与要求 8 严格一致。**

---

## 下一步任务清单

> **下一步做什么 → 见 `HANDOFF.md` §3**（唯一清单，避免两处漂移）。

---

## 已打通的工具链

**工具路径与命令 → 见 `CLAUDE.md` §5**（§5.1 路径 / §5.2 命令 / §5.3 仿真方案，含 `.vwf` 的实测结论）。

已端到端跑通的验证闭环：`quartus_map → Python 生成激励 .vwf → quartus_sim →
Python 解析结果 → 参考模型逐点比对`。各脚本的用途与状态见上方「文档进度」表。

---

## 已完成的关键决策（避免重复讨论）

| 议题 | 决策 |
|---|---|
| 实现范围 | 11 条基本要求 + 三项提高（音效 / 多图案随机 / 零片位置随机）；**不做** 90°旋转 |
| **仿真方案** ⭐ | 仅用 **Quartus II 9.1 内置仿真器**（本机无 ModelSim/GHDL）。它**没有任何 Tcl 接口**（实测 `::quartus::simulator` 是空包）→ 所以驱动与判定**全用 Python**：生成 `.vwf` 激励 + 参考模型逐点比对。完整链路见 `CLAUDE.md` §5.3 |
| 文档形态 | 仓库内中文 Markdown 全套 |
| 关卡号显示 | 统一用 **DISP0**；判定要求9 的"DISP2"为原文笔误 |
| 键盘映射 | 方向键居中十字布局（见 `CLAUDE.md` §4.3） |
| 课程 PDF | 已加入 `.gitignore`，不入库 |
| 目标时钟 | **50MHz**，分频常量单点定义 |
| 成功判定 | 零片掩码并集 == 目标掩码（**已穷举验证有 2 种合法铺法**） |
| 引脚号来源 | **只认开发板手册**；旧工程仅用于交叉验证"用到了哪些引脚" |
| **确认键语义** | **锁定语义**：按"确认"锁定零片，全部锁定后才判定（见"阻塞"第 4 项） |
| **时间/空间分离** | `puzzle_ctrl` 只管坐标掩码（无计数器），`game_fsm` 只管状态时间（无坐标运算） |
| **单时钟域** | 全设计一个 50MHz 时钟 + 单周期使能脉冲，**不使用门控时钟** |
| **SW7 门控位置** | 在**输出级**门控（严格零延迟），**不放进状态机** |
| **倒计时数据形式** | **BCD 两位**，避免综合出除法器 |
| **点阵位序** | `bit0` = 左上角；`dot_colr` 与掩码**天然对齐，全链路零次翻转** |
| **零片掩码存储** | **不存储**，由锚点 + 形状组合计算（省 256 个触发器） |
| **设计方法学** | **设计自顶向下**（需求 → 7 个功能子系统 → 11 个实体），**实现自底向上**（先叶子模块后集成）。两者不矛盾，作用在不同阶段（`CLAUDE.md` §2.1） |
| **框图分两层** | DWG-01 = 7 个功能子系统；DWG-02 = 11 个实体（DWG-01 的展开）；DWG-03 = 状态转移图。两层可互查（`docs/01` §2.1 / §2.2 / §3.1） |
| **框图定稿版（两个口径，别混）** ⭐ | **内部对账** → `docs/图/系统图.html` 的**矢量版**（可编辑、与代码同步）；**对外提交** → 用户白板定稿版 `report/图/DWG-01-系统总体框图-含信号传递关系-定稿版.png`（第 19 次会话采纳为权威，⛔ AI 不要再往上叠字）。旧手绘 PNG 只作留档。口径统一见 `CLAUDE.md` §12.1.1 / §12.3（2026-09-24 澄清，此前两节互相矛盾） |
| **`rng_lfsr` 不接节拍** ⭐ | 由 `puzzle_ctrl` 的 `rnd_step` **请求推进**，接原始 `clk`（理由见 `CLAUDE.md` §10.1、`docs/01` §2.1.1）。DWG-01 上已删除该节拍分接 |
| **`pattern_rom`/`piece_rom` 纯组合** | 常量表，无时序。所有图案/零片掩码**在编译期折叠**，不是运行时算出来的 |
| **游戏控制是枢纽** | `game_fsm` 除推进 `puzzle_ctrl` 外，还向显示子系统送 `state·level·cnt`、向音效输出送 `sound_sel`、向图案与随机数据送 `pattern_sel·seed·rnd_step`。**管时间的那块天然是分发点** |
| **不合并 RTL 实体** | 用户说"图太细"指的是**图**不是模块。合并会砸掉 `CLAUDE.md` §2 第 11 条的"每模块可独立仿真"与十几处 `docs/02` 小节引用，而仿真波形是评分项③ 的 25 分 |
| **会话交接方式** | **新增 `HANDOFF.md` 一页纸**（第 7 次会话）。`CLAUDE.md` §0 把它列为**开工第一个读**的文件；`PROGRESS.md` 只留"过去做了什么"的账，**"下一步做什么"不在这里重复**——两处都写必然不同步 |
| **图表定稿 = 走清单** | `CLAUDE.md` §12.4 **五步**清单。第 3 步「渲染后放大 2.5~3 倍分上中下逐段看」抓到 ERR-0006(2 处) / ERR-0008(3 处) / ERR-0009(1 处) 共 **6 处**；第 5 步「图→表反向对账」抓到 ERR-0013 的 **3 条漏画线** —— 校验脚本**一处都没报** |
| **验收校验器的标准** | 改任何校验脚本后，**负向测试必须同时有正例与反例**；豁免掉一类对象时，要追问"豁免之后还有什么没人管了"（ERR-0007 / ERR-0009） |
| **顶层一律用 `component` 语法** ⭐ | `puzzle_top.vhd` / `board_test_top.vhd` **不用 `entity work.xxx` 直接例化**，一律 `component` 声明 + `port map`（`CLAUDE.md` §10.3、`docs/01` §10 第 5 条、模板见 `docs/02` §14.1）。**一个 `component` 块 = DWG-02 的一个方框**，图与代码的对应关系肉眼可见，评分项⑧ 10 分直接受益。**代价**：端口写两遍，改接口要同步顶层声明 —— 且 `port map` 漏写端口**不报错、静默悬空** |
| **`docs/01` §5 补 §5.11** | 接口定义表原缺 `buzzer_ctrl`（ERR-0010）。`docs/02` §15 对照表新增「**接口定义**」列，用于核对 §5.0~§5.11 **12 节连续覆盖** |
| **音效触发源** ⭐ | `buzzer_ctrl.i_trigger` 由 **`game_fsm.o_sound_trig`** 驱动（用户 2026-09-17 拍板）。音效的触发时机（按键 / 锁定 / 倒计时 / 胜负）本来就全是 `game_fsm` 产生的事件，**不给 `buzzer_ctrl` 自己数拍子** |
| **键号译码位置** ⭐ | 「键号 → 动作」（上/下/左/右/选择/确认/开始）的译码**放 `game_fsm` 内**（用户 2026-09-17 拍板）。这是**游戏规则**；`keypad_scan` 只回答"按了哪个键"，是纯器件驱动，**不认识"上下左右"** |
| **连线表条数** ⭐ | `docs/01` §2.1 = **4 条系统级 + 14 条子系统出线 = 18 条**（第 12 次会话 +1：`btn`；2026-09-18 审查 +2：`kp_row`/`kp_col`，并把"子系统间"改称"子系统出线"）。**改图时直接数这个数**（ERR-0013 的预防措施） |
| **端口核对方向** ⭐ | 核对"输入端口有没有驱动源"**必须从线到模块**，不能按模块翻；建议脚本化 `scripts/check_ports.py`，放在阶段 4 写 `puzzle_top` 之前（`docs/01` §10.2）。理由见 `CLAUDE.md` §12.4 第 5 步与 ERR-0013 |
| **图与表对账两遍** ⭐ | 「表 → 图」查不一致，「图 → 表」查漏画。**只做一遍必漏** —— ERR-0013 的 3 条漏画线就是只做「表→图」时漏掉的（`CLAUDE.md` §12.4 已补） |
| **文档去重原则** ⭐ | **同一事实只留一个真值源**，其余位置写"去哪查"的指针。第 10 次会话据此删掉 `docs/01` §2.2 那张 60 行 ASCII 图（它是 §2.1 的第二份拷贝，且已经漂移）。**判据：同一事实的第二份拷贝 = 下一次改版时必然出错的点**（ERR-0014） |
| **`clk` 的分层画法** ⭐ | **DWG-01 不画** `clk`（它是 `clk_gen` 子系统的属性，折进椭圆小字）；**DWG-02 也不画 8 条实线**，改用**脚注逐个列出 8 个接收者实体名**（名单是可数的，这是替代画线的等价物）。理由见 `docs/01` §2.1 的 ⚠️ 与 §2.1.2 |
| **封版 ≠ 自洽** ⭐ | 第 9 次会话沿**接口轴**核对（文档 ↔ 代码）查出 14 处；第 10 次沿**一致性轴**核对（文档 ↔ 文档）又查出 14 处。**遍历的轴不同，能看见的东西就不同** —— 不换轴，剩下的错误就是不可见的（ERR-0014 预防措施） |
| **`tick_*` 是使能不是电平** ⭐ | `tick_*` 全是**单周期使能**。**闪烁方波 `o_blink` 由 `tick_100` ÷25 ÷2 得到**，不是把 `tick_2hz` 当方波送（那样实际只有 1Hz）。`tick_2hz` 的真实用途是**开机自检的 2 秒计时**（ERR-0014 第 4 处） |
| **复位三源** ⭐ | `o_rst <= r_btn_rst or r_por_rst or (not sys_en)`。**课件 p45（一）强制"复位必须用按键来实现"** —— `por_rst` 只是"上电初值"，**不能替代按键**。见 ERR-0018 |
| **按键复位的实现** ⭐ | `btn` → `clk_gen.i_btn_rst` → **2 级同步器（消亚稳态）+ 20ms 消抖（数 `tick_1k`）** → `r_btn_rst`。**消抖计数器不得被 `o_rst` 复位**（否则永远退不出复位） |
| **消抖判据 = 器件类型** ⭐ | **SW7 是拨动开关**、要求"即时熄灭" → **不消抖**；**BTN0 与矩阵键盘是机械按键** → **消抖**（20ms / 80ms）。课件 p54「按键要注意防抖处理」＋手册附图16。**不是"抖动有害才消抖"，而是"按键就要消抖"**（ERR-0018 第二处判断反转） |
| **裁决顺序** ⭐ | 文档冲突时按 **课件/题目 → 开发板手册 → 本项目约定 → 文档内部一致性** 的顺序裁决。**"改哪边省事"从来不是判据** —— ERR-0018 就是按"哪边改动小"把课件明文要求删掉的 |
| **Quartus 优化目标 = AREA** ⭐ | `.qsf` 里 `set_global_assignment -name OPTIMIZATION_TECHNIQUE AREA`（课件 p50 例4）。EPM1270 只有 1270 LE 且本设计不要求速度 → 面积优先。**默认是 `Balanced`，必须显式改** |
| **PDF 解析必须看图** ⭐ | 课件 65 页里 **13 页是纯图幻灯片**，`pypdf` 提取为空，而 **4 条实质要求只在图里**。**判据：提取完先数"空页"，空页一律渲染后逐页看**（`AI_LOG.md` 第 1 次会话就记过，第 12 次复发） |
| **端口改动的固定落点** ⭐ | **`CLAUDE.md` §7.1**：9 个落点 + 2 遍反向对账 + 1 条收尾 `grep`。ERR-0017 就是漏了其中 4 处（含接口唯一真值源 `docs/01`） |

---

## 三条"同类陷阱"（实现时必须专门验证）

**三处本质相同的边界回绕 bug**（都是"无符号减法/移位在边界回绕"）：
掩码逐行移位（`docs/02` §2.4）/ 零片上移越界（§10.7）/ 倒计时 `00` 回绕到 `99`（§12.5）。
根除办法见 `CLAUDE.md` §10.1 —— **靠"锚点不变量"根除，不靠移位后修补**；细节全在 `docs/02`。

---

## 上次会话断点

> **2026-09-24 第 22 次会话结束 —— 「下一步做什么」见 `HANDOFF.md` §3（唯一清单）。**
> 各次会话的详细记录（起因 / 做了什么 / 最值得带走的一条）全在 `CHANGELOG.md`，此处只留一行索引。

- **第 22 次**：⭐ **第三波仿真收尾 + board_test_top 九轮拉锯（ERR-0042）** ——
  ① 第三波入库（6 提交）：game_fsm 27/27、puzzle_ctrl 19/19（ERR-0040/0041 复验）、
  puzzle_top 整机首跑 4/4、复验波全过、`docs/03` 结果表回填；
  ② **board_test_top 整顶层仿真闭合（r09，14/14）** —— 四层根因（解析器多块覆盖 /
  陈旧编译数据库 / 1.74 亿跳变触发分块抽稀 / tick_100 工具畸变）逐层剥开，
  `vwf.py` 支持多块串联、`sim.py` 改名让路强制重综合、tb 改**自校准断言**
  （子步数 + 内容 + 80:8 比例），绝对速率由 clk_gen 单测背书；
  ③ ERR-0042 入账（含 ERR-0040 索引 3 行重复的修复），对账 42 == 42；
  ④ 用户已 `git push`（origin/main 同步至 176701f）。
  → **下一步：跑 keypad_scan 交叉测试 + 3 个 ROM/LFSR 模块仿真 + 13 场景整机序列；录工作视频。**

- **第 21 次（续六）**：⭐ **阶段 3 仿真开跑 —— `scripts/sim.py` 写出 + 前 2 个模块全过** ——
  ① 写出 `scripts/sim.py`（`docs/03` §2 里一直缺的驱动脚本）：`gen` / `check` / `run`，
  `run` 一键走完「绑顶层 → 生成功能仿真网表 → 跑仿真 → 解析比对 → 还原 `.qsf`」，
  `.qsf` 用**整文件备份/还原**；自带波形 SVG 渲染；
  **「中间信号清单缺一即报错」**（`docs/03` §3.1 第 5 条的落地）；
  ② ⚠️ **实测订正一处文档错误**：向量源**不是** `.qsf` 的 `VECTOR_SOURCE_FILE`
  （9.1 不认，实测报 `No valid vector source file specified ... puzzle.cvwf`），
  而是 **`quartus_sim` 的命令行选项 `--vector_source=<文件>`**
  → `CLAUDE.md` §5.3 与 `docs/03` §2.1 已订正；
  ③ **第 1 轮 `disp_format` 8/8**：掩码约定、自检闪烁、待机、**预览透传逐位不差**
  （`o_px_red = 0x00001C1C1C1C0000`）、前导零熄灭（**两个方向都测**）、胜负图案；
  ④ **第 2 轮 `dot_matrix_scan` 4/4**：掩码→引脚无翻转、行计数器 0~7 完整、两相消隐；
  ⑤ ⭐ **收敛结论**：**点阵显示链的逻辑整条正确**（状态 → `disp_format` → 掩码 →
  `dot_matrix_scan` → 引脚位）。**剩下唯一未知量是硬件约定**（`dot_colr(0)` = PIN_22 是哪一列、
  `dot_row(0)` = PIN_8 是哪一行）→ 只能靠上板自检阶段 2/3/4 判定，**仿真无能为力**。
  → **边界已划清**：整机若仍镜像/颠倒，改 `.qsf` 位序即可；若选择效果仍奇怪，
  则归 `puzzle_ctrl`（**下一轮仿真目标**，它至今零仿真且经 ERR-0026 大改）。
  → 数据已填 `docs/03` §4.4 / §4.9 与 §1.2 容量登记表。
  → **（续五的内容见 `docs/05` §1.6.4 与 `HANDOFF` §3：族 A/族 B 分析 + ERR-0036）**

- **第 21 次（续四）**：**整机首次上电判读 —— "看不到开机自检"是错过了 2 秒** ——
  现场报"拨 SW7=1 后没有自检画面、直接进 "5" 待机"。定位：`T_SELFTEST = 2`，
  `game_fsm` 在 `tick_2hz` 上数 4 拍 = **2 秒**；而**要求 2 明文写着"2 秒后系统进入待机状态"**
  → **2 秒是需求规定**；待机画面 `DISP7="5"`、`DISP0=关卡号 "1"` 也与 `disp_format` 的
  `S_IDLE` 分支逐字吻合 → **两个画面都对，只是错过了窗口**。
  → 记 **ERR-0034**；订正 `docs/06` §4.2（补"只持续 2 秒" + **三个能看到它的办法**）
  与 §6.1（判读表 +2 行），`docs/05` §1.6.2 填实测。**`rtl/` 一行未改。**
  → **预防措施**：**"会一闪而过的正常画面"必须进现场判读表并写明持续时长**；
  **写下一类预防措施后要拿它把"所有同类对象"扫一遍**（本条是 ERR-0030 预防措施 3 的补做）。
  → **下一步：按 `docs/02` §14.3 的 13 个场景验收（第一个动作 = 按「开始」）+ 录工作视频。**

- **第 21 次（续三）**：**切整机 `puzzle_top` 上板 + 定位"进不去阶段 7"** ——
  ① 用户决策：**跳过自检阶段 2~8**，直接烧整机演示、之后再完善（代价记在 `docs/05` §1.6：
  点阵/数码管/蜂鸣器 5 项至今无独立确认）；
  ② `set_top.py puzzle_top` → 重编译 **0 errors**（1226/1270 LE、52 引脚）→
  TimeQuest **setup slack +4.292 ns / hold +1.379 ns** → 烧录 **全过**（14:11:18）；
  ③ ⭐ **定位"进不去阶段 7"**：`r_seen` **只在 `s_rst` 时清零** → **按满 16 键后恒为全 1** →
  阶段 7 每圈都在 **0.25 秒**内退出（本该 20 秒）→ 记 **ERR-0033**；
  **规避法：按 BTN0 或拨 SW7 复位**；**代码修复有意推迟**（只影响自检工具）。
  → **预防措施**：**累积型状态（`seen`/`done`/`count`）必须写清"从哪个事件开始累积、到哪个事件清零"**；
  "看不到了"要先怀疑"它一闪而过"，**把该状态的应有持续时长从代码里算出来再和实测比**。
  → **下一步：在整机上按 `docs/02` §14.3 的 13 个场景验收 + 录工作视频。**

- **第 21 次（续二）**：**判据被现场推翻 —— "看阶段 1"这条判据不可执行** ——
  用户照「30 秒判据」去看阶段 1，报回"**所有 16 个灯都闪烁，LD8 也不是常亮**"。
  ① 重算 `board_test_top.vhd` L451~456 → 阶段 1 的期望画面（`LD8` 常亮 + `LD0~LD7` 齐闪
  + `LD9~LD15` 全灭）**代码无误**；② 算出**完整轮播 36 秒里阶段 1 只占 2 秒（≈5.6%）**，
  其余 34 秒是阶段 2~9（`ld[15:8]`=阶段号、`ld[7:0]`=子步号 → 16 灯本来就都在动）；
  ③ 找出**代码里现成的阶段号读数**：L428~431 把 `r_stage` 写进 `s_disp_val(31 downto 28)`，
  经 `seg_scan` 的 `i_disp_val(4k+3..4k) → DISPk` 映射 → **最左数码管 DISP7 = 阶段号**；
  ④ 给出**三个能精确对准阶段 1 的时刻**（松开最右按键后 2 秒 / SW7 由 0→1 后 2 秒 / DISP7 显示 "1"）。
  → 记 **ERR-0031**（索引 31 行 == 正文 31 条）；订正 `docs/06` §6.1、`docs/01` §8.2、`docs/05`。
  **`rtl/` 一行未改。**
  → **预防措施**：**判据必须同时给"看什么 / 在哪看 / 何时看"** ——
  凡观测一个"会变的状态"，要写明它占周期多久、哪个器件能读出它、什么动作能必然落到它。
  → **下一步不变：自检阶段 2~8 逐条打勾（用 DISP7 报阶段号）+ 录工作视频。**

- **第 21 次**：**首次上板（`board_test_top` 烧进真板）+ 现场两条"异常"判读 + ERR-0030** ——
  ① 烧录 `quartus/puzzle.pof` 成功（Erase→Program→Verify 全过）；
  ② 现场报"拨 SW7 后非常混乱"与"SW7=0 时只有 LD8 亮、按住 BTN0 不亮 16 灯"，
  判读结论：**前者是 9 阶段轮播的正常画面**（`ld[7:0]` 是 4Hz 子步号、`ld[15:8]` 才是阶段号），
  **后者是 `ld` 不受 SW7 门控的预期行为**（`board_test_top` 只门控 seg/cat/dot_*/buzz 六项）；
  ③ 用 `pypdf` 抽出板手册第 3/4/7/8/10 页原文核对，补进 `docs/04` §4 与**新增 §7.3「现场位置速查」**
  （BTN0 = `PIN_61` = **8 个按键里最右边那个**、SW7 往上拨才是"开"、`FREQ` 指示灯看第 7 格、
  LD0 在最右端且 16 个灯有 4 种颜色、LD8~LD15 与 VGA 复用）；
  ④ 订正 `docs/04` §7.1 第 10/11 条、`docs/06` §4.2/§6.1、`docs/01` §8.1/§8.2、`docs/05`，
  **代码一行未改**（结论是"实现对、清单错"）；
  ⑤ **13:38 现场回证：「30 秒判据」四步全过** —— 时钟确认 50MHz、LED"无规则但有周期"（正常）、
  **按住最右 BTN0 → 16 灯全亮** → 悬案闭合（当时是按错键）。
  → 记 **ERR-0030**；预防措施：**需求清单要能逐条对回代码**、
  **"让人去按/去看/去拨"的检查项必须带物理位置**、
  **会让人误判成故障的正常现象都要有一行进 `docs/06` §6.1**。
  → **下一步：趁还在实验室把阶段 2~8 逐条打勾 + 录工作视频，再回阶段 3 仿真。**

- **第 20 次**：**全量改动审查 + 文档结构与状态订正**（用户要求"审查全部改动、查准确/遗漏/冲突"）——
  ① `ERRORS.md` 问题索引表**缺 6 行**（ERR-0023~0028；其中 4 行被误追加到 ERR-0025 正文末尾）
  → 已补全，**索引 29 行 == 正文 29 条**；② `README.md` / `HANDOFF.md` / `PROGRESS.md`
  多份状态停在"编码之前"，且 `HANDOFF.md` **自身 §2 与 §7 互相矛盾**；③ `CLAUDE.md`
  §12.1.1 与 §12.3 关于"DWG-01 权威版本"的口径冲突已统一。
  → 记 **ERR-0029**；两条通用预防措施：**索引表可数对账**、
  收工时 `grep -rn "还不能编译\|仍为空\|未开始" *.md` **搜不该留的旧状态**。
  → **下一步不变：阶段 3 逐模块仿真（`scripts/sim.py` + `sim/tb_*.py` + `docs/03` 结果表）**。

- **第 19 次**：**阶段 2/3/4 的 RTL 全部写出并编译通过** ——
  `rtl/` 下 14 个文件（11 实体 + 包 + 2 顶层，约 2600 行，全中文注释）；
  首次整机编译 **LE 3461/1270 放不下** → `puzzle_ctrl` 改为**时分复用行扫描引擎**
  （4 块 × 8 行 = 32 拍刷一帧）+ **包围盒不相交判据** → **1226/1270（97%）**；
  时序 **38.4 → 63.66 MHz**（三处打拍，见 ERR-0027）；新增 `docs/06-Quartus编译与烧录流程.md`。
  → **下一步：阶段 3 逐模块仿真（`.vwf` + `docs/03` 结果表）**。
  剩余风险：① 板上时钟档位/键盘极性仍需上板确认；② 整机 LE 余量只剩 ~44 个。

- **第 18 次**：**Typst 重排初步设计方案 + 文档规范化 + 敏感信息清理** ——
  产出 `report/初步设计方案.pdf`（12 页）与排版源 `report/typst/`；封面按北邮毕设论文体例重做
  （另出 5 套方案对比供挑选）；正文去 AI 痕迹（删提示框、破折号 10→0、拆机械列举）；
  标题层级与图表间距按报告通例重定；**清理 `docs/审查报告` 中 67 处本机绝对路径**（ERR-0025）；
  补 `.gitattributes`、重写 `README.md`；按逻辑拆成多个提交并推送。
  → **下一步仍是进阶段 2：写 `rtl/puzzle_pkg.vhd`**（文档侧已无待办）。
- **第 17 次**：交付初步设计方案并补齐图集 —— 白板框图按定稿口径修正（3 处，ERR-0023）、
  补画 DWG-04/05 并入 `docs/图/系统图.html`、产出 `report/初步设计方案.docx`（ERR-0024）；
  **阶段 1 至此齐活，下一步进阶段 2 写 `rtl/puzzle_pkg.vhd`**。
- **第 16 次**：**精简 `CLAUDE.md`**（用户要求）—— 判据「删事实的副本、留陷阱的警示」，
  **1130 → 1009 行**；`⚠️` 36 个 / 56 个小节标题一个没少，本文件内自引用零悬空。
  另：**开工时撞出 ERR-0021**（第 15 次会话跳过收工流程 —— 表头、`CHANGELOG`、`AI_LOG` 全空），
  本次代为补齐并补记两节；如实记下 ERR-0022（先估后量：承诺 ~800、实得 1009）。
- **第 15 次**：**复核并修完报告 §4 的 80 条 MEDIUM/LOW** —— 10 个 agent × 每批 8 条、
  默认立场"怀疑"：**75 确认 / 1 推翻 / 4 已修 / 0 存疑**；75 条按 241 条编辑指令全部改掉
  （11 个文件 / 889 增 218 删）。⚠️ **本次未走收工流程**（见 ERR-0021）。
- **第 14 次**：按审查报告修缺陷 —— **已复核的 16 条全部修完**（12 CRITICAL/HIGH ＋ `sim-03`/`INT-4`/`INT-5`/`CRIT-2`），
  另清掉 4 条「照抄编译不过」类（`seg_scan-01`/`buzzer_ctrl-1`/`buzzer_ctrl-3`/`puzzle_ctrl-3`）；
  含 DWG-01 的图↔表对账（补 `pattern_mask`、删多余的节拍分接，走完 §12.4 五步）。
- **第 13 次**：进代码前的系统性审查（第五根轴：照着做能不能跑）—— 34 个子 agent 四阶段；
  106 条发现、12 条经"默认推翻"复核确认；产出 `docs/审查报告-2026-09-18.md`（729 行）与 ERR-0020。
  当时的结论是"四步修复完成前不要开始写代码"—— **该结论已由第 14/15 次会话执行完毕**。
- **第 12 次**：课件 PDF 全量解析与合规补全（第四根轴：符合性）—— 65 页完整解析（含 13 页纯图页）、BTN0 按键复位全链路、`.qsf` 加 AREA、`CLAUDE.md` §11 考核/提交对照表、§7.1 端口落点清单（ERR-0017/0018/0019）。
- **第 11 次**：规划审查与完善（第三根轴：可执行性）—— 胜负图案掩码定稿、`docs/03` 框架、`scripts/check_ports.py`（ERR-0016）。

- **第 10 次**：全项目复查（另一根轴：文档 ↔ 文档）—— `docs/01` 的 14 处内部不一致、图侧 3 处、过程文档去重（ERR-0014/0015）。
- **第 9 次**：接口全量核对与修复，阶段 1 真封版 —— 5 处输入端口无驱动源 / 6 处代码片段照抄编译不过 / DWG-02 漏画 3 条连线（ERR-0011/0012/0013）。
- **第 8 次**：顶层例化方式定为 `component` 语法；顺带撞出 `docs/01` §5 漏了 `buzzer_ctrl`（ERR-0010）。
- **第 7 次**：收尾准备 —— 建立 `HANDOFF.md`；DWG-01 同步飞书稿第 2 版（ERR-0009）。
- **第 6 次**：用户手绘总体框图、AI 审图后 DWG-01 定稿（ERR-0007/0008）。
- **第 5 次**：设计方法学表述修正（设计自顶向下、实现自底向上）；总体框图改两层。
- **第 1~4 次**：项目奠基 / 打通自动仿真链路 / 4 份设计文档 / 图表与资源利用率约定。
