# 简易拼图游戏的设计与实现

> 《数字电路与逻辑设计实验（下）》综合课题 · **题目 4**
> 目标器件：**Altera MAX II `EPM1270T144C5`** · 开发工具：**Quartus II 9.1** · 设计语言：**VHDL**

---

## 项目状态

> **完整阶段表见 `PROGRESS.md`**（唯一账本，阶段 0~6）。这里只给一句话现状：

- ✅ **阶段 0~4 已完成**：设计文档封版（4 份主文档 + 5 张图 + 初步设计方案）；
  `rtl/` 下 **14 个文件全部写出**（11 实体 + `puzzle_pkg` + 2 顶层），
  **两个顶层都通过 Quartus II 9.1 全流程编译、时序收敛**
- 🔄 **下一步：阶段 3 的逐模块仿真验证**（`.vwf` 激励 + `docs/03` 结果表 —— 报告评分项③ 占 25 分）
- ⬜ **阶段 5 上板调试** 与 **阶段 6 验收准备** 未开始

| 顶层 | 逻辑单元 | 引脚 | Fmax | 说明 |
|---|---|---|---|---|
| `board_test_top` | **439 / 1270（35%）** | 68 | — | 上板硬件自检（`.qsf` 当前默认顶层） |
| `puzzle_top` | **1226 / 1270（97%）** | 52 | **63.66 MHz** ✓ | 整机拼图游戏 |

- **当前交付物**：初步设计方案 —— `report/初步设计方案.docx` 与 `report/初步设计方案.pdf`（Typst 排版，12 页）
- **硬截止**：初步设计方案 **2026-09-24 24:00**；验收答辩在第 9~10 周
- ⚠️ **整机只剩约 44 个 LE 余量（97%）**：改动任何代码后**先看编译报告的 LE 数**
- ⚠️ 板上**时钟档位（应为 7 / 50MHz）**与**键盘行列极性**仍待上板确认 ——
  `board_test_top` 就是为这两项准备的（见 `docs/01` §8.2）

---

## 项目简介

在 MAXII 数字逻辑实验开发板上，用 VHDL 实现一个基于 **8×8 双色点阵**的简易拼图游戏。

玩家在 5 秒内观察完整拼图图案，随后图案被打散成若干块零片随机散落在点阵上；
玩家用 4×4 矩阵键盘选择零片、移动到正确位置并确认，在倒计时结束前还原完整图案即获胜。

**游戏共两关：**

| | 第一关 | 第二关 |
|---|---|---|
| 完整图案 | 4×3 实心矩形（题目指定） | 自拟图案 |
| 零片数量 | 3 块 | 4 块 |
| 预览时间 | 5 秒 | 5 秒 |
| 拼图限时 | 30 秒 | 40 秒 |

---

## 功能要求

### 基本要求（已全部纳入实现范围）

1. **SW7 系统开关** —— 关断时所有显示器件熄灭、蜂鸣器静音；开启后进入开机自检：点阵全黄 2Hz 闪烁，8 位数码管全显"8" 且 2Hz 闪烁
2. **待机状态** —— 2 秒后点阵熄灭，DISP7 显"5"、DISP0 显"1"
3. **两关制** —— 关卡号由数码管 DISP0 显示
4. **图案预览** —— 按"开始"后点阵显示完整图案 5 秒，DISP7 倒计时
5. **零片散落** —— 预览结束后 3 块零片随机不重叠散落，DISP4/DISP3 开始 30 秒倒计时
6. **选择零片** —— "选择"键循环切换，选中的零片变绿
7. **移动零片** —— "上/下/左/右"键移动，不得移出 8×8 区域
8. **确认落位** —— "确认"键后零片变黄并锁定，不可再选、不可再动
9. **第一关判定** —— 30 秒内拼对进入第二关；拼错或超时显示失败图案，游戏结束
10. **第二关** —— 规则同上，图案与零片自拟，限时 40 秒；成功显示胜利图案
11. **重新开始** —— 游戏过程中或结束后按"开始"键可重开一局

### 提高要求（选择性实现）

- ✅ 不同场景播放不同音效（蜂鸣器）
- ✅ 多种拼图图案随机选择
- ✅ 零片初始位置随机
- ⬜ 零片 90°旋转（不实现）
- ⬜ 零片方向随机（不实现）

---

## 系统架构

**11 个功能实体 + 2 个顶层**。`puzzle_top` 例化 11 个子模块：

| # | 模块 | 职能子系统 | 职责 |
|---|---|---|---|
| 0 | `puzzle_pkg` | — | 全局常量、掩码纯函数、位序约定（**唯一真值源**，不是实体） |
| 1 | `clk_gen` | S1 时钟与节拍 | 时钟分频与节拍产生、**复位产生（上电 + 按键 BTN0，后者为课件强制要求）** |
| 2 | `keypad_scan` | S2 键盘输入 | 4×4 矩阵键盘扫描 + 消抖 + 单次按下脉冲 |
| 3 | `seg_scan` | S6 显示 | 8 位数码管动态扫描 + BCD 译码 |
| 4 | `dot_matrix_scan` | S6 显示 | 8×8 双色点阵动态扫描（消隐防鬼影） |
| 5 | `pattern_rom` | S5 图案与随机数据 | 图案掩码 ROM（两关图案 + 胜负图案） |
| 6 | `piece_rom` | S5 图案与随机数据 | 零片形状 ROM（相对行掩码 + 宽高） |
| 7 | `rng_lfsr` | S5 图案与随机数据 | 8 位伪随机源（含全零保护） |
| 8 | `puzzle_ctrl` | S4 拼图核心 | 零片锚点 / 钳位 / 移动 / 重叠 / 锁定 / 判定（**只管空间，不含计数器**） |
| 9 | `disp_format` | S6 显示 | 显示格式化：状态 → 8 位 BCD + 熄灭掩码 + 点阵像素 |
| 10 | `game_fsm` | S3 游戏控制 | 主状态机、倒计时、关卡切换、随机选图、键号译码（**只管时间，不含坐标运算**） |
| 11 | `buzzer_ctrl` | S7 音效输出 | 蜂鸣器音效 |
| — | `puzzle_top` | 顶层 | **唯一做引脚绑定的文件**，用 `component` + `port map` 例化上面 11 个 |
| — | `board_test_top` | 顶层（调试） | **上板前的硬件自检顶层**：验时钟档位/点阵行列序/键盘行列极性与极性，含 `ld[15:0]` |

详细框图与接口定义见 `docs/01-系统设计.md`（§5 接口表是唯一真值源）。

---

## 目录结构

```
dld-lab/
├── README.md          # 本文件：项目总览与文档索引
├── CLAUDE.md          # 工作约定与硬件速查（开发前必读）
├── HANDOFF.md         # 会话交接单（一页纸：现在干什么 / 别踩什么坑）← 开工第一个读
├── PROGRESS.md        # 跨会话进度状态（唯一账本）
├── CHANGELOG.md       # 变更日志（Keep a Changelog 格式）
├── ERRORS.md          # 错误记录（实验报告评分项⑤素材）
├── AI_LOG.md          # AI 使用记录（评分项⑦素材）
├── docs/              # 设计文档
│   ├── 00-需求分析.md
│   ├── 01-系统设计.md              # ← 初步设计方案主体（§5 接口唯一真值源）
│   ├── 02-模块详细设计.md
│   ├── 03-仿真验证方案.md          # 框架已就绪，结果表随仿真逐个填
│   ├── 04-引脚分配表.md
│   ├── 05-硬件调试记录.md          # 上板后写（阶段 5）
│   ├── 06-Quartus编译与烧录流程.md  # 建工程→编译→引脚→烧录→乱码处置
│   ├── 审查报告-2026-09-18.md      # 进代码前系统性审查的原始记录（历史档案）
│   ├── 报告/                       # 正式实验报告（验收前写）
│   └── 图/系统图.html              # DWG-01~05 五张图（内联 SVG，可导入 draw.io）
├── rtl/               # VHDL 源码（14 个文件：11 实体 + puzzle_pkg + 2 顶层）
├── sim/               # 仿真激励与输出（阶段 3 写入；.vwf 会被仿真回写，别手工编辑）
├── quartus/           # Quartus 工程与引脚约束（引脚号唯一真值源）；产物也在这里
├── scripts/           # TCL / Python 自动化脚本（编译驱动、校验、转码、切顶层…）
└── report/            # 交付物
    ├── 初步设计方案.docx / .pdf    # 12 页（Typst 排版）
    ├── 封面方案对比.pdf
    ├── typst/                       # PDF 排版源（template.typ + main.typ）
    └── 图/                          # 报告插图（含 DWG-01 对外提交版）
```

> 根目录共 7 份 Markdown：本文件（项目总览）＋ **6 份过程记录**。过程记录不是交付物本身，
> 但实验报告评分项 ③（仿真波形 25 分）、⑤（故障分析 15 分）、⑦（AI 使用 10 分）
> **共 50 分直接取自它们**，因此与代码同等重要。各文件的分工见下方「文档索引」。

---

## 开发环境

| 项 | 值 |
|---|---|
| 开发板 | MAXII 数字逻辑实验开发板（LCM12864 液晶版） |
| 器件 | EPM1270T144C5（MAX II，144 脚 TQFP，C5 速度等级） |
| 工具 | Quartus II 9.1 |
| 仿真 | Quartus II 内置仿真器（`.vwf` 向量波形激励） |
| 时钟 | 板载可选，本项目按 **50MHz** 设计（板载档位 **7**）—— ⚠️ **该档位尚待上板确认** |

---

## 构建

> ✅ **工程当前可以直接编译**（`quartus/puzzle.qsf` 已登记全部 14 个 VHDL 源文件）。
> ⚠️ `.qsf` 里**默认顶层是 `board_test_top`（硬件自检）**；要编整机拼图，先切顶层：
> `python scripts/set_top.py puzzle_top`（它会同时处理 16 行 `ld[…]` 约束，见 `docs/06` §5）。

```bash
# 推荐：本仓库的编译驱动（自动打印 LE / Fmax；--top 可临时换顶层且用完还原）
"C:/QuartusII91/QuartusII91/quartus/bin/quartus_sh.exe" -t scripts/build.tcl

# 等价的全流程（在 quartus/ 目录下执行）
quartus_sh --flow compile puzzle

# 分步执行（便于定位是哪一步出错）
quartus_map puzzle      # 综合
quartus_fit puzzle      # 布局布线（引脚在这一步落实）
quartus_asm puzzle      # 汇编（生成 .pof）
quartus_sta puzzle      # TimeQuest 时序分析（Fmax；经典 quartus_tan 不读 .sdc）
```

> ⚠️ **Quartus II 9.1 不生成 `output_files/` 目录** —— 编译产物（`puzzle.pof` 等）
> 直接写在 `quartus/` 下，这是 9.1 的正常行为，不是编译失败。

**完整分步操作**（建工程 → 编译 → 引脚分配 → 下载烧录 → 中文注释乱码处置）
见 **`docs/06-Quartus编译与烧录流程.md`**；烧录命令速查见 `CLAUDE.md` §5.2。

---

## 文档索引

### 设计文档（`docs/`）

| 文档 | 内容 | 对应评分项 |
|---|---|---|
| `docs/00-需求分析.md` | 11 条要求逐条拆解 + 需求追踪矩阵 | ① 5 分 |
| `docs/01-系统设计.md` | 总体框图、模块划分、接口定义（**接口唯一真值源**） | ② 20 分 |
| `docs/02-模块详细设计.md` | 各模块内部设计（答辩抽问的主要依据） | ② 20 分 |
| `docs/03-仿真验证方案.md` | 仿真策略与判读规范；各模块结果表随代码逐个填充 | ③ 25 分 |
| `docs/04-引脚分配表.md` | 引脚约束对照（可读版；真值源是 `quartus/puzzle.qsf`） | ④ 5 分 |
| `docs/06-Quartus编译与烧录流程.md` | 建工程 → 编译 → 引脚分配 → 下载烧录 → 乱码处置（含实测数据） | ④ / ⑧ |
| `docs/审查报告-2026-09-18.md` | 进代码前系统性审查的**原始记录**（历史档案，非任务清单） | — |
| `docs/图/系统图.html` | **5 张图**：DWG-01 总体框图 / DWG-02 模块框图 / DWG-03 状态转移图 / DWG-04 整机逻辑流程图 / DWG-05 主状态机 MDS 图 | ② |

### 过程记录（仓库根目录）

| 文档 | 内容 |
|---|---|
| `CLAUDE.md` | 工作约定、硬件速查、构建与仿真命令（开发前必读） |
| `HANDOFF.md` | 会话交接单：现在该干什么、别踩什么坑 |
| `PROGRESS.md` | 阶段表、模块进度表、阻塞项、上次会话断点 |
| `CHANGELOG.md` | 变更日志（Keep a Changelog 格式） |
| `ERRORS.md` | 错误记录，九字段，含定位过程 | 
| `AI_LOG.md` | AI 使用记录：关键提示词、AI 产出、人工修正 |

### 交付物（`report/`）

| 文件 | 内容 |
|---|---|
| `report/初步设计方案.docx` | 初步设计方案（Word 版） |
| `report/初步设计方案.pdf` | 初步设计方案（Typst 排版 PDF，12 页，正文 6 章 + 3 图 + 7 表） |
| `report/typst/` | PDF 的排版源：`template.typ`（版式模板）、`main.typ`（正文）、`covers.typ`（封面方案集） |
| `report/封面方案对比.pdf` | 5 套封面方案（每版一页，供挑选；正式版采用方案 A） |
| `report/图/DWG-01-系统总体框图-含信号传递关系-定稿版.png` | **图 2-1 对外提交版**（用户白板定稿，已按老师要求标出模块功能与主要信号名） |
| `report/图/` | 其余报告插图（DWG-04/05 矢量源、旧版留档等） |

---

## 许可与声明

- 本仓库是《数字电路与逻辑设计实验（下）》综合课题的**课程作业**，仅供学习与评阅使用。
- **课程原始资料（课件、题目说明等 PDF）不入库**：它们受版权保护，已在 `.gitignore` 中排除；
  仓库内容可独立阅读与构建，不依赖这些文件。
- 硬件参数（引脚号、极性、器件规格）**以开发板手册为准**；
  仓库内的真值源是 `quartus/puzzle.qsf`（引脚号）与 `docs/01-系统设计.md` §5（接口定义）。
