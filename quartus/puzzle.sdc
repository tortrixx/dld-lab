# ============================================================
# puzzle.sdc —— 时序约束（Synopsys Design Constraints）
#
# 【为什么需要这个文件】
# 本项目的硬约束之一是「目标时钟 50MHz」，但在 2026-09-18 的进代码前审查之前，
# 全项目**没有任何时序验证手段**：工程里没有时钟约束、编译流程里没有 TAN 步骤、
# docs/04 的 Fmax 行没有数据来源 —— 而 docs/03 却已经断言「时序裕量极大」。
# 那句断言是**没有任何依据的**（ERRORS.md ERR-0020 / 报告 CRIT-1）。
#
# 有了本文件，Quartus 才知道"这个设计要跑多快"，
# 编译后才能在 timing 报告里给出 Fmax，从而**用数据支持或推翻**那句断言。
#
# 【怎么用】
#   quartus_sh -t scripts/build.tcl        # 全流程编译，脚本会打印时序摘要
#   结果回填到 docs/04-引脚分配表.md §5.2 的「最大工作频率 (Fmax)」一行
#
# 【依据】
#   板上时钟档位 7 = 50MHz → 周期 20ns，占空比 50%（上升沿 0ns、下降沿 10ns）
#   档位确认见 docs/04 §7.1 第 1 条（需上板实测）
# ============================================================

# ---- 系统时钟：50MHz，周期 20ns ----
create_clock -name clk -period 20.000 -waveform {0.000 10.000} [get_ports {clk}]

# ---- 输入/输出延迟 ----
# CPLD 上按键与开关都是"人手速度"的慢信号，显示器件也不要求建立/保持窗口，
# 故不额外加 set_input_delay / set_output_delay。
# 真正要验的是**片内逻辑**能否在 20ns 内跑完一环 —— 上面那条 create_clock 就够。
#
# ⚠️ 若编译后 Fmax < 50MHz，第一嫌疑是**最长组合路径**：
#    r_anchor → place() 平移逻辑 → disp_format 多路选择 → dot_matrix_scan → 引脚
#    （三个模块之间**没有中间寄存器**，见报告 CRIT-1）。
#    对策：在 puzzle_ctrl → disp_format 之间打一拍。
