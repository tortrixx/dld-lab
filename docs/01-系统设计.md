# 01 · 系统设计

> 《数字电路与逻辑设计实验（下）》综合课题 · **题目 4 简易拼图游戏的设计与实现**
> 对应实验报告评分项 **②系统设计（设计思路、总体框图、分块设计）（20 分）**
> **本文档即"初步设计方案"主体**（截止 2026-09-24 24:00）

---

## 1. 设计思路

### 1.1 自顶向下的三层分解

**本设计采用自顶向下的方法逐层分解**：从需求出发，先划出系统的功能子系统，
再把每个子系统落到具体的模块（实体）。分三层：

```
第 0 层 · 需求        11 条基本要求 + 3 项提高              → docs/00-需求分析.md
        ↓ 按"要做的几件大事"切分
第 1 层 · 功能子系统   7 个子系统（DWG-01）                 → §2  总体框图
        ↓ 每个子系统再细分为可独立验证的单元
第 2 层 · 模块（实体） 11 个实体 + 2 个顶层（DWG-02）        → §3 模块划分 / §5 接口定义
```

> **为什么必须自顶向下**：题目要求的是一个**交互式游戏**——有画面（点阵）、
> 有数据显示（数码管）、有输入（键盘）、有声音（蜂鸣器）、有规则（拼图判定）、
> 有时间约束（倒计时）。如果反过来从下位模块往上拼（"先有个分频器，再加个扫描……"），
> 最终结构会由模块的便利性决定，而**没人能保证它满足了上面那些需求**。
> 自顶向下则保证每一层都能追溯到需求，见 §1.1.3 的追溯关系。

#### 1.1.1 第 1 层：7 个功能子系统

第 1 层的划分依据是**"这个系统要做哪几件互相独立的大事"**，
每件事在硬件上表现为**一组职责内聚的实体**：

| # | 功能子系统 | 一句话职责 | 所含实体 | 图纸 |
|---|---|---|---|---|
| S1 | **时钟与节拍** | 把 50MHz 变成全系统要用的几档节拍 | `clk_gen` | DWG-01 |
| S2 | **键盘输入** | 把 16 个按键变成"按了哪个键"的事件 | `keypad_scan` | DWG-01 |
| S3 | **游戏控制** | 管状态、关卡、倒计时——**只管时间** | `game_fsm` | DWG-01 |
| S4 | **拼图核心** | 管零片坐标、移动、判定——**只管空间** | `puzzle_ctrl` | DWG-01 |
| S5 | **图案与随机数据** | 提供图案掩码、零片形状、随机数 | `pattern_rom` `piece_rom` `rng_lfsr` | DWG-01 |
| S6 | **显示子系统** | 把游戏状态变成点阵与数码管上的画面 | `disp_format` `seg_scan` `dot_matrix_scan` | DWG-01 |
| S7 | **音效输出** | 按事件发出对应音效 | `buzzer_ctrl` | DWG-01 |

**子系统所含实体之和 = 11 个实体，不重不漏**（`puzzle_pkg` 是包不是实体，见 §5.0）。

#### 1.1.2 第 2 层：每个子系统怎么落到实体

切分时用**"职责类别"**作为细化的判据——它决定了模块的**接口形态**和**仿真方法**：

| 职责类别 | 特征 | 实体 | 对应的仿真验证 |
|---|---|---|---|
| **器件驱动** | 与外部器件电气特性强相关，与游戏规则无关 | `dot_matrix_scan` `seg_scan` `keypad_scan` `buzzer_ctrl` | 极性与时序（给输入看波形） |
| **数据与规则** | 纯逻辑运算，与外部器件无关 | `pattern_rom` `piece_rom` `rng_lfsr` `puzzle_ctrl` `disp_format` | 算法正确性（穷举/参考模型比对） |
| **时序与调度** | 状态、时间、节拍 | `clk_gen` `game_fsm` | 状态迁移与计数（时序图判读） |

> **这张表和上面那张表是两个层次，不要混**：
> 上表回答"系统分成哪几块"（自顶向下的**切分**），
> 本表回答"每块里的实体属于哪一类"（切分时用的**判据**）。
> 例如 S6 显示子系统里，`dot_matrix_scan` / `seg_scan` 是驱动类，
> 而 `disp_format` 是数据类——同一个子系统里可以有两种职责类别。

#### 1.1.3 每一层都能追溯到需求

自顶向下的意义在于**可追溯**：任何一个实体，都能沿"实体 → 子系统 → 需求"一路上溯。
举三个例子：

| 实体 | 所属子系统 | 直接支撑的需求 |
|---|---|---|
| `dot_matrix_scan` | S6 显示子系统 | 要求 6（选择）、要求 7（移动）、要求 9（判定）、要求 10（第二关） |
| `puzzle_ctrl` | S4 拼图核心 | 要求 7（移动）、要求 8（锁定）、要求 9（判定） |
| `buzzer_ctrl` | S7 音效输出 | 提高要求 1（蜂鸣器音效） |

> 上表的需求编号与 `docs/02-模块详细设计.md` §15 对照表**逐条一致**
> （该表是`模块 ↔ 需求`的完整映射，此处只摘三个例子）。

完整的"需求 ↔ 模块"双向对照见 `docs/00-需求分析.md` 的需求追踪矩阵。

> **与实现顺序的关系**：**设计**是自顶向下的（本节），
> **实现**是自底向上的（先写叶子模块 `puzzle_pkg`/`clk_gen`，再逐层往上集成到 `puzzle_top`）。
> 两者不矛盾——它们作用在项目的不同阶段。详见 `CLAUDE.md` §2.1。

### 1.2 三条贯穿全局的设计原则

**原则一：时间与空间分离**

这是本设计最核心的一条结构决策。

- `puzzle_ctrl` 只管**空间**——零片在哪、什么形状、能不能放下、拼对了没有。
  它**没有倒计时、没有秒概念、不知道第几关**。
- `game_fsm` 只管**时间**——现在是什么状态、还剩几秒、该显示什么。
  它**完全不做坐标运算、不碰掩码**。

> **为什么要分离**：如果把"零片移动"和"倒计时"写在一个模块里，会得到一个既有坐标
> 又有计时的巨型进程，任何一个改动都要回归测试全部功能。分离后，
> "零片边界对不对"只看 `puzzle_ctrl`，"30 秒到了没"只看 `game_fsm`，
> 两者的仿真用例完全独立。
> **答辩时的说法**：`puzzle_ctrl` 是纯组合+寄存的"空间引擎"，`game_fsm` 是"时间调度器"，
> 二者通过 `puzzle_ctrl` 的状态输出（`all_locked` / `solved`）单向耦合。

**原则二：单一数据源**

- **零片位置只存锚点** (row, col)，不存 64 位掩码。掩码由锚点 + 形状**组合计算**。
  → 节省 4 × 64 = 256 个触发器；**从结构上杜绝"锚点和掩码不一致"这种最难查的 bug**。
- **引脚号只在 `quartus/puzzle.qsf` 出现一次**，不在任何 VHDL 里写死。
- **位序约定只在 `puzzle_pkg` 定义一次**，各模块引用而不各自解释。

**原则三：输出级统一消隐**

所有显示驱动（点阵、数码管）与蜂鸣器的**输出级**都被 `sys_en`（即 SW7）门控：

```vhdl
dot_row  <= row_reg  when sys_en = '1' else (others => '1');  -- 低有效 → 全灭
dot_colr <= colr_reg when sys_en = '1' else (others => '0');
dot_colg <= colg_reg when sys_en = '1' else (others => '0');
seg      <= seg_reg  when sys_en = '1' else (others => '0');
cat      <= cat_reg  when sys_en = '1' else (others => '1');  -- 低有效 → 全灭
buzz     <= buzz_reg when sys_en = '1' else '0';
```

**这样做的理由**：要求 1 说"SW7='0' 时所有显示器件不显示"。如果让 SW7 走状态机，
从拨动开关到状态机响应、再到输出熄灭，中间会有若干时钟周期的不一致。
在输出级门控可以做到**严格零延迟**，且**只有一个地方需要维护**。
同时，`board_test_top` 也能复用同一套输出级逻辑。

### 1.3 单时钟域 + 单周期使能

**全设计只有一个时钟 `clk`（50MHz），所有时序逻辑都用 `rising_edge(clk)`。**

不使用门控时钟（不用 `clk_div` 去驱动别的触发器），而是由 `clk_gen` 产生
**单周期宽度的使能脉冲**（`tick_1k` / `tick_1hz` / …），各模块用
`if rising_edge(clk) then if tick_1k = '1' then ...` 的结构分频执行。

> **理由**：CPLD 上做时钟分频驱动全局时钟网络会引入偏斜与毛刺风险；
> 单周期使能是 CPLD/FPGA 设计的标准做法，且**所有触发器仍工作在同一个 50MHz 时钟下**，
> 时序分析简单、无跨时钟域问题。

---

## 2. 总体框图

> **本节的框图分两层，对应 §1.1 的两层分解**：
> **DWG-01** 画第 1 层的 7 个功能子系统，**DWG-02**（§2.2）把每个子系统展开到实体。
> 两张图的矢量源文件在 `docs/图/系统图.html`（内联 SVG，可导入 draw.io 编辑）；
> DWG-01 的**手绘定稿原稿**另存为 `docs/图/DWG-01-系统总体框图-飞书原稿.png`。
> **改模块接口时，两张图和 §5 的接口定义表必须同时改。**
>
> ⚠️ **以 `系统图.html` 的矢量版为准**：PNG 是留档，矢量版才可编辑、才与代码同步。
> 两者若有出入，改矢量的、重新导出 PNG，不要反过来。

### 2.1 DWG-01 · 第一层：功能子系统

系统的**对外接口**——即画在最外圈的器件连接：

```
                          ┌─────────────────────────────────────────┐
                          │  外部器件                                │
   4×4 矩阵键盘 ─────────▶│ kp_row[3:0]                 dot_row[7:0]│────────▶ 8×8 双色点阵 行(低有效)
                          │ kp_col[3:0]◀                dot_colr[7:0]│────────▶           红列(高有效)
                          │                             dot_colg[7:0]│────────▶           绿列(高有效)
   SW7 系统开关 ─────────▶│ sw7                                       │
                          │                          seg[7:0]        │────────▶ 8 位数码管 段(高有效)
   50MHz 时钟  ──────────▶│ clk                      cat[7:0]        │────────▶           位选(低有效)
                          │                                           │
                          │                          buzz             │────────▶ 蜂鸣器
                          │                          ld[15:0]        │────────▶ 16 个 LED
                          └─────────────────────────────────────────┘
```

系统的**内部结构**——7 个功能子系统及其互连（**设计分层的第一层**）：

```
                            ┌───────────────────────────────┐
   50 MHz ─────────────────▶│      时钟与节拍   clk_gen       │◀── SW7 系统开关
                            └───────────────┬───────────────┘
                                            │ clk 与节拍总线
   ┌────────────────────────────────────────┼──────────────────────────────────────┐
   │   tick_8k · tick_1k · tick_100 · tick_2hz · tick_1hz                            │
   └──┬────────────┬──────────────┬─────────┴──────┬───────────────┬────────────────┘
      ▼            ▼              ▼                ▼               ▼
┌──────────┐ ┌──────────┐  ┌───────────┐   ┌────────────┐  ┌──────────┐
│ 键盘输入  │ │ 游戏控制  │  │ 拼图核心   │   │ 显示子系统  │  │ 音效输出  │
│keypad_scan│ │ game_fsm │  │puzzle_ctrl│   │disp_format │  │buzzer_ctrl│
│          │ │          │  │           │   │seg_scan    │  │          │
│tick_1k   │ │状态·倒计时│  │锚点·钳位   │   │dot_matrix_ │  │          │
│20ms 消抖  │ │·关卡·选图 │  │·锁定·判定  │   │scan        │  │          │
└────┬─────┘ └────┬─────┘  └─────┬─────┘   └─────┬──────┘  └────┬─────┘
     │            │              │               │              │
     ▼            │              │               ▼              ▼
 4×4 矩阵键盘      │              │       数码管·8×8 点阵       蜂鸣器
 kp_row/kp_col    │              │
                  │◀─────────────┘
                  │   all_locked · solved（全图唯一的反馈回路）
                  │
                  │        ┌────────────────────────────┐
                  └───────▶│      图案与随机数据          │
                           │  pattern_rom · piece_rom   │
                           │  rng_lfsr                  │
                           │  两个 ROM 纯组合；           │
                           │  rng 按 rnd_step 请求推进    │
                           └────────────────────────────┘
```

> **图的定稿版**：`docs/图/DWG-01-系统总体框图-飞书原稿.png`（手绘定稿的原始导出）
> ＋ `docs/图/系统图.html` 的 DWG-01（**可编辑矢量版**，上列 ASCII 是它的文字摘要）。
> **矢量版才是权威版本**——接口有改动时改它，PNG 只作留档。

**全部 11 条连线**（盒子里的连线太密，ASCII 画不下，列成表反而更准）：

| # | 起点 → 终点 | 信号名（框图上去掉方向前缀） | 含义 |
|---|---|---|---|
| 1 | 键盘输入 → 游戏控制 | `key_code` · `key_press` | 键号 + 单次按下脉冲 |
| 2 | 游戏控制 → 拼图核心 | `level` · `round_start` | 第几关、何时开局 |
| 3 | 游戏控制 → 图案与随机数据 | `pattern_sel` · `seed` | 选哪套图案；随机种子 |
| 4 | 游戏控制 → 图案与随机数据 | `rnd_step` | **每次落位尝试请求 LFSR 推进一步** |
| 5 | 图案与随机数据 → 拼图核心 | `target_mask` · `rel_mask` · `rnd` | 目标图案 / 零片形状 / 随机数 |
| 6 | 拼图核心 → 显示子系统 | `px_red` · `px_green` | 64 位红/绿像素掩码 |
| 7 | 游戏控制 → 显示子系统 | `state` · `level` · `cnt` | 状态、关卡号、倒计时 |
| 8 | 游戏控制 → 音效输出 | `sound_sel` | 该响哪一种音效 |
| 9 | **拼图核心 → 游戏控制** | `all_locked` · `solved` | **唯一的反馈回路** |
| 10 | 显示子系统 → 器件 | `seg` · `cat` · `dot_row` · `dot_colr` · `dot_colg` | 数码管与点阵驱动 |
| 11 | 音效输出 → 器件 | `buzz` | 音频方波 |

**这张图怎么读**（答辩讲这张图的四句话）：

1. **纵向是一条数据通路**：键盘 → 游戏控制 → 拼图核心 → 显示子系统 → 器件。
   按键从左边进，画面从右边出，**全程单向**。
2. **横向是一根节拍总线**：`clk_gen` 在最上面（椭圆画法，强调它是**唯一的时钟源**），
   把 5 档单周期使能扇出给需要计时的子系统（对应 §1.3 的单时钟域方案）。
3. **游戏控制是枢纽**：除推进拼图核心外，它还要给显示子系统送 `state`/`level`/`cnt`、
   给音效输出送 `sound_sel`、给图案与随机数据送选择与随机请求——
   **管时间的那块，天然是分发点**。
4. **只有一条反馈**：`puzzle_ctrl` 的 `all_locked` / `solved` 回到 `game_fsm`——
   因为"拼完没有"必须回到管状态的那个子系统去驱动状态迁移。
   **除这一条外，本设计没有任何回环**，这是它能被清晰分层的前提。

其中 **S4 拼图核心 / S6 显示子系统 / S5 图案与随机数据** 三块的关系值得特别注意：
`pattern_rom` 提供**目标图案**（拼成什么样），`piece_rom` + `rng_lfsr` 提供
**零片形状与随机落点**（拿什么拼），两者都汇入 `puzzle_ctrl`
（拼得对不对），结果再交给 `disp_format` 变成画面。

#### 2.1.1 定稿过程中修正的四处 ⭐

框图先画在飞书白板上，再定稿为矢量图。定稿时改了四处，**每一处都是实现前必须澄清的**：

| # | 改动 | 为什么必须改 |
|---|---|---|
| 1 | **补回 `all_locked · solved` 反馈** | 手绘稿漏画这条线。缺了它就解释不了 `S_PLAYING` 凭什么走到 `S_WIN`/`S_FAIL` —— 全设计只有这一条回环，是**装载信息的**，不是装饰 |
| 2 | **补回 `SW7` 与 `50MHz` 两个外部输入** | SW7 是基本要求第 1 条（`SW7='0'` 时全部显示熄灭），**不能不出现在总体框图上**；时钟是子系统的输入，不画就从图上找不到源头 |
| 3 | **删掉「图案与随机数据」的节拍输入** | 见下方专条，这是**最容易埋雷的一处** |
| 4 | **框图名统一为「显示子系统」** | 与 §1.1.1 的子系统命名、§3.1 的模块清单保持一致（原名"显示系统"少一个字，会让两层框图对不上号） |

**第 3 处展开——为什么「图案与随机数据」不接节拍：**

`pattern_rom` 与 `piece_rom` 是**纯组合**的常量表，根本没有时序；
`rng_lfsr` 是一个由 `puzzle_ctrl` 的 `rnd_step` **请求推进**的移位寄存器，
它接的是原始 `clk`，不是任何一档节拍。

若图上画成"接节拍"，实现时就极易写成"每个 `tick` 采一次随机数"——
那样**同一拍内的多次落位尝试会读到同一个随机数**：所有零片落点相同 →
必然重叠 → 用尽尝试次数 → 每次都走回退布局。
**游戏仍然能跑，只是永远不随机**，这种缺陷靠观察现象基本发现不了。
详见 `CLAUDE.md` §10.1 与 `docs/02` §11。

### 2.2 DWG-02 · 第二层：实体级框图（DWG-01 的展开）

```
                                    ┌───────────────┐
  clk ─────────────────────────────▶│   clk_gen     │
  sw7 ──┬──────────────────────────▶│  时钟分频      │
        │                           │  与上电复位    │
        │                           └───┬───────────┘
        │                               │ tick_8k / tick_1k / tick_100
        │                               │ tick_2hz / tick_1hz / rst
        │                               ▼
        │   ┌──────────────────────────────────────────────────────┐
        │   │                    game_fsm                          │
        │   │  游戏主状态机 · 关卡 · 倒计时 · 图案选择 · 音效选择      │
        │   │                                                      │
        │   │  SELF_TEST→IDLE→PREVIEW→PLAYING→(WIN/FAIL)→…         │
        │   └──┬────────┬──────────┬───────────┬──────────┬───────┘
        │      │        │          │           │          │
        │      │        │ pattern_sel  state/level/cnt    │ sound_sel
        │      │        │          │           │          │
        │      │        ▼          │           │          ▼
        │      │  ┌───────────┐    │           │   ┌─────────────┐
        │      │  │pattern_rom│    │           │   │ buzzer_ctrl │──▶ buzz
        │      │  │ 图案掩码   │    │           │   └─────────────┘
        │      │  └─────┬─────┘    │           │
        │      │        │ target_mask          │
        │      │        │          │           ▼
        │      │        │          │   ┌────────────────┐
        │      │        │          │   │  disp_format   │  纯组合
        │      │        │          │   │ 状态→8位BCD+熄灭│
        │      │        │          │   └───┬────────┬───┘
        │      │        │          │       │disp_val│blank
        │      │        ▼          ▼       ▼        ▼
        │      │  ┌──────────────────────────┐  ┌───────────┐
        │      │  │      puzzle_ctrl         │  │ seg_scan  │──▶ seg/cat
        │      │  │  零片位置/选择/移动/锁定   │  │ 数码管扫描 │
        │      │  │  重叠判定 / 正确性判定     │  └───────────┘
        │      │  └───┬──────────────┬───────┘
        │      │      │ px_red/px_green│ all_locked / solved
        │      │      │              └──────────▶ (回 game_fsm)
        │      │      ▼
        │      │  ┌──────────────────┐
        │      │  │ dot_matrix_scan  │──▶ dot_row/dot_colr/dot_colg
        │      │  │  8×8 双色点阵扫描  │
        │      │  └──────────────────┘
        │      │
        │      │  ┌───────────┐   ┌───────────┐
        │      │  │ piece_rom │   │ rng_lfsr  │
        │      │  │ 零片形状   │   │ 伪随机源   │
        │      │  └─────┬─────┘   └─────┬─────┘
        │      │        │ rel_mask      │ rnd
        │      │        └───────┬───────┘
        │      │                ▼
        │      │        (接 puzzle_ctrl)
        │      │
        │      ▼
        │  ┌──────────────┐
        └─▶│ keypad_scan  │──▶ key_code / key_press
           │ 键盘扫描消抖  │        │
           └──────────────┘        ▼
                              (接 game_fsm)
                                   │
   sw7 ────────────────────────▶   │ 输出级 sys_en 门控
                                   ▼
                            （见 §1.2 原则三）
```

**框图与代码的一致性**：

- **本图（DWG-02，第二层）每个方框都对应 `rtl/` 下一个独立文件、独立实体**，
  一一对应，无合并、无拆分。老师按图抽查任意一个方框，都能在代码里找到同名实体。
- **§2.1 的图（DWG-01，第一层）每个方框是一个功能子系统**，
  方框内列出的实体名之和 = §3.1 模块清单里的全部 11 个实体，不重不漏。
- **两张图不是二选一**：DWG-01 回答"系统分成哪几块"，DWG-02 回答"每块由哪些实体实现"。
  报告里两张都要放，**答辩被抽查的是 DWG-02 的实体**（那才是 `.vhd` 文件）。

---

## 3. 模块划分

### 3.1 模块清单

**所属子系统**一列即 §2.1 的 DWG-01 第一层框图 —— 两列对得上，两层图就对得上。

| # | 模块 | **所属子系统**（DWG-01） | 职责类别 | 一句话职责 | 是否有时序逻辑 |
|---|---|---|---|---|---|
| 0 | `puzzle_pkg` | —（包，不属于任何子系统） | 包 | 全局常量、掩码运算纯函数、位序约定 | 否（纯声明） |
| 1 | `clk_gen` | **S1** 时钟与节拍 | 驱动 | 时钟分频、节拍使能、上电复位 | 是 |
| 2 | `keypad_scan` | **S2** 键盘输入 | 驱动 | 4×4 矩阵键盘扫描、消抖、边沿检测 | 是 |
| 3 | `seg_scan` | **S6** 显示子系统 | 驱动 | 8 位数码管动态扫描 + BCD→段码译码 | 是 |
| 4 | `dot_matrix_scan` | **S6** 显示子系统 | 驱动 | 8×8 双色点阵动态扫描（含消隐防鬼影） | 是 |
| 5 | `pattern_rom` | **S5** 图案与随机数据 | 数据 | 图案掩码查找表（目标/胜利/失败图案） | 否（纯组合 ROM） |
| 6 | `piece_rom` | **S5** 图案与随机数据 | 数据 | 零片形状查找表（相对掩码 + 宽高） | 否（纯组合 ROM） |
| 7 | `rng_lfsr` | **S5** 图案与随机数据 | 数据 | LFSR 伪随机数发生器 | 是 |
| 8 | `puzzle_ctrl` | **S4** 拼图核心 | 逻辑 | 零片锚点 / 选择 / 移动 / 钳位 / 锁定 / 正确性判定 | 是 |
| 9 | `disp_format` | **S6** 显示子系统 | 逻辑 | 游戏状态 → 8 位 BCD + 熄灭掩码 | 否（纯组合） |
| 10 | `game_fsm` | **S3** 游戏控制 | 调度 | 游戏主状态机、倒计时、关卡切换、图案与音效选择 | 是 |
| 11 | `buzzer_ctrl` | **S7** 音效输出 | 驱动 | 蜂鸣器音效产生 | 是 |
| — | `puzzle_top` | 顶层 | 顶层 | 例化全部子模块 + **唯一做引脚绑定** | — |
| — | `board_test_top` | 顶层 | 顶层 | **上板硬件自检专用顶层**（见 §8） | — |

> **`puzzle_pkg` 为什么不属于任何子系统**：它是**包（package）不是实体**，
> 不产生任何硬件，只提供常量与纯函数供各模块引用（见 §5.0）。
> 因此它不出现在任何一张框图的方框里，但**每个模块都依赖它**。
> 报告里说明这一点，可以避免"框图上找不到 `puzzle_pkg`"的追问。

### 3.2 为什么这样分（答辩要点）

- **每个模块职责单一，能用一句话说清**——这是"可独立仿真"的前提；
- **驱动与逻辑严格分开**——改点阵扫描频率不会碰到游戏规则；
- **时间与空间分开**（见 §1.2 原则一）——`puzzle_ctrl` 无计数器，`game_fsm` 无坐标；
- **ROM 独立**——图案和零片形状是**数据**不是**逻辑**，改图案不该动状态机；
- **纯组合模块单独成文件**（`disp_format` / `pattern_rom` / `piece_rom`）——
  它们最容易仿真（无时钟，给输入立刻出输出），可以 100% 覆盖。

---

## 4. 全局设计约定

> 这些约定在 `puzzle_pkg` 中集中定义，**全项目引用，不得各自解释**。

### 4.1 时钟与复位

| 项 | 约定 |
|---|---|
| 系统时钟 | `clk` = **50MHz**（板上档位 7），周期 20ns |
| 时钟域 | **全设计单时钟域**，只用 `rising_edge(clk)` |
| 分频方式 | **单周期使能脉冲**（`tick_xxx`），**不使用门控时钟** |
| 复位 | **同步、高有效**。`rst = (not SW7) or por_rst` |
| 上电复位 | `clk_gen` 内部产生，约 10ms 高电平，保证上电后所有寄存器处于已知状态 |
| SW7 消抖 | **不做消抖** —— 要求是"即时熄灭"，消抖会引入延迟，反而违背要求 |

### 4.2 节拍使能清单

| 信号 | 频率 | 用途 |
|---|---|---|
| `tick_8k` | 8 kHz | 蜂鸣器音效时基（4kHz / 2kHz / 1kHz / 500Hz 方波） |
| `tick_1k` | 1 kHz | 点阵行推进、数码管位选推进、键盘扫描 |
| `tick_100` | 100 Hz | 按键消抖计数、慢速逻辑 |
| `tick_2hz` | 2 Hz | 开机自检闪烁（要求 1） |
| `tick_1hz` | 1 Hz | 预览倒计时（要求 4）、拼图倒计时（要求 5） |

> **为什么点阵和数码管共用 `tick_1k`**：两者都是 8 路扫描，1kHz 使每路点亮 1ms，
> 刷新率 125Hz，远高于人眼闪烁阈值（约 60Hz），不会闪。
> 共用一个节拍可以少一个分频器，且两者互不干扰。

### 4.3 点阵位序约定 ⭐

**这是全项目最容易出错的地方，在此单点定义。**

| 项 | 约定 |
|---|---|
| 掩码编码 | `mask(8 × 行 + 列)`，**列号向右递增**，故 **`bit 0` = 左上角** |
| 取第 r 行 | `mask(8*r + 7 downto 8*r)` |
| 行内位序 | **`bit 0` = 该行最左列**，`bit 7` = 该行最右列 |
| 「向右移一列」 | 行向量**左移**：`r(6 downto 0) & '0'` |
| 「向下移一行」 | 整个掩码**右移 8 位**：`"00000000" & m(63 downto 8)` |
| 与驱动的关系 | `dot_matrix_scan` 扫描第 r 行时，直接把 `mask(8*r+7 downto 8*r)` 赋给 `dot_colr` |

**为什么这样定**：因为硬件上 **COLR0 接的物理列就是最左列**（手册：COLR0~COLR7
依次为 22、21、16、15、14、13、12、11 脚），所以"行内 bit0 = 最左列"与
"COLR0 = 最左列"**天然对齐，无需任何位序翻转**。

> ⚠️ **反面教材**：本机桌面上另一个旧工程把 `col_r[0]` 接到了 `PIN_11`（即 COLR7），
> 与手册相反。那个工程自身是自洽的，但**引脚表已不再对应板上丝印**。
> 详见 `ERRORS.md` ERR-0003。**结论：引脚号只从手册拿。**

### 4.4 器件极性一览

| 器件 | 极性 | 备注 |
|---|---|---|
| 点阵行 `dot_row` | **低有效** | 0 = 选中该行 |
| 点阵红列 `dot_colr` | **高有效** | 1 = 该列红色 LED 可导通 |
| 点阵绿列 `dot_colg` | **高有效** | 1 = 该列绿色 LED 可导通 |
| 数码管段 `seg` | **高有效** | `seg(0)`=AA … `seg(6)`=AG, `seg(7)`=AP |
| 数码管位选 `cat` | **低有效** | `cat(0)`=DISP0(最右) … `cat(7)`=DISP7(最左) |
| LED `ld` | **高有效** | |
| 键盘行 `kp_row` | 按下 = **1** | 列有 10k 上拉、行有 4.7k 下拉 |
| 键盘列 `kp_col` | 扫描时**驱动一列为高** | |

**像素点亮条件**：

| 颜色 | 行 | COLR | COLG |
|---|---|---|---|
| 红 | 低 | 高 | 低 |
| 绿 | 低 | 低 | 高 |
| 黄 | 低 | 高 | 高 |
| 灭 | 低 | 低 | 低 |

### 4.5 显示与配色语义

| 显示 | 含义 |
|---|---|
| `DISP7`（最左） | 预览倒计时（要求 4）；待机时固定显示 "5"（要求 2） |
| `DISP4`,`DISP3` | 拼图倒计时两位十进制（要求 5） |
| `DISP0`（最右） | **关卡号**（要求 3；原文要求 9 的 "DISP2" 为笔误，见 `docs/00` §6） |
| 其余位 | 熄灭 |

| 点阵配色 | 零片状态 |
|---|---|
| **红** | 未选中、未锁定 |
| **绿** | 当前选中（要求 6） |
| **黄** | 已锁定（要求 8） |

### 4.6 键盘映射

```
             COL0    COL1    COL2    COL3
ROW3:         —       上       —      开始
ROW2:        左       下      右      选择
ROW1:         —        —       —      确认
ROW0:         —        —       —       —
```

`KEY号 = (3 − ROW) × 4 + COL + 1`；**ROW3 在最上排**（实物丝印确认）。

---

## 5. 模块接口定义

> 端口命名：`i_` 前缀输入、`o_` 前缀输出（VHDL-1993 无 `inout` 用于此场景）。
> 全部模块统一 `clk` / `rst` / `en` 三件套风格。

### 5.0 `puzzle_pkg`（包，不是实体）

```vhdl
package puzzle_pkg is

    -- 位序约定（§4.3）
    function row_of(idx : integer) return integer;          -- 由位号取行号
    function col_of(idx : integer) return integer;          -- 由位号取列号
    function bitidx(r, c : integer) return integer;         -- 行列 → 位号

    -- 掩码纯函数
    function row_mask(m : std_logic_vector(63 downto 0); r : integer)
        return std_logic_vector;                            -- 取第 r 行 8 位
    function shift_right_col(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 右移一列（逐行左移）
    function shift_left_col(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 左移一列（逐行右移）
    function shift_down_row(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 下移一行
    function shift_up_row(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 上移一行
    function popcount(m : std_logic_vector(63 downto 0)) return integer;

    -- 图案常量（由 scripts/verify_tiling.py 生成，勿手工改动）
    constant L1_TARGET_MASK : std_logic_vector(63 downto 0) := "...";
    constant L2_UP_MASK     : std_logic_vector(63 downto 0) := "...";
    constant L2_DOWN_MASK   : std_logic_vector(63 downto 0) := "...";
    constant WIN_MASK       : std_logic_vector(63 downto 0) := "...";
    constant FAIL_MASK      : std_logic_vector(63 downto 0) := "...";

    -- 分频常量（单点定义，禁止散落魔法数字）
    constant CLK_HZ      : integer := 50_000_000;
    constant CNT_8K      : integer := CLK_HZ / 8_000  - 1;
    constant CNT_1K      : integer := CLK_HZ / 1_000  - 1;
    constant CNT_100     : integer := CLK_HZ / 100    - 1;
    constant CNT_2HZ     : integer := CLK_HZ / 2      - 1;
    constant CNT_1HZ     : integer := CLK_HZ / 1      - 1;
    constant CNT_POR     : integer := CLK_HZ / 100    - 1;   -- 上电复位 10ms

    -- 时间常量
    constant T_PREVIEW   : integer := 5;    -- 预览秒数
    constant T_LEVEL1    : integer := 30;   -- 第一关限时
    constant T_LEVEL2    : integer := 40;   -- 第二关限时
    constant T_SELFTEST  : integer := 2;    -- 自检秒数

end package;
```

> **注意**：`shift_right_col` 等函数**必须逐行处理**，不能整体 `sll`——
> 整体移位会让最右列的格回绕到下一行最左列。函数内部按
> `for r in 0 to 7 loop new(8*r+7 downto 8*r) := old(8*r+6 downto 8*r) & '0'; end loop;` 实现。
> **这是要求 7 边界正确性的根本保障。**

### 5.1 `clk_gen`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` | in | 1 | 50MHz 系统时钟 |
| `sys_en` | in | 1 | SW7，用于产生同步复位 |
| `o_rst` | out | 1 | 同步高有效复位（上电复位 或 SW7=0） |
| `o_tick_8k` | out | 1 | 8kHz 单周期使能 |
| `o_tick_1k` | out | 1 | 1kHz 单周期使能 |
| `o_tick_100` | out | 1 | 100Hz 单周期使能 |
| `o_tick_2hz` | out | 1 | 2Hz 单周期使能 |
| `o_tick_1hz` | out | 1 | 1Hz 单周期使能 |

> **上电复位置位**：`por_rst` 由内部计数器从 0 计到 `CNT_POR` 产生，
> 期间保持高电平；**计数器在 `por_rst` 有效期间不停止**，保证一定会退出。

### 5.2 `keypad_scan`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick` | in | 1 | 1kHz（扫描 + 消抖计时） |
| `i_kp_row` | in | 4 | 键盘行输入，按下 = 1 |
| `o_kp_col` | out | 4 | 键盘列扫描输出，逐列为高 |
| `o_key_code` | out | 4 | 键号 1~16；0 = 无键 |
| `o_key_press` | out | 1 | **单次按下脉冲**，宽度 1 个 `i_tick` |

> **扫描方式**：4 个 `i_tick` 完成一轮（逐列拉高读行）；连续 20 轮读到同一个键
> 才认为稳定（20ms 消抖）。`o_key_press` 在"消抖后新按下"时输出一个脉冲，
> **松手后才允许下一次**（避免长按连发——但方向键需要连发，见 §6.6）。

### 5.3 `seg_scan`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick` | in | 1 | 1kHz |
| `i_disp_val` | in | 32 | 8 位 BCD，**`i_disp_val(4*k+3 downto 4*k)` 对应 DISP`k`** |
| `i_blank` | in | 8 | 每位熄灭掩码，`i_blank(k)='1'` 时 DISP`k` 熄灭 |
| `o_seg` | out | 8 | 段码 AA,AB,…,AG,AP（高有效） |
| `o_cat` | out | 8 | 位选（**低有效**），`o_cat(0)`=DISP0(最右) |

> **防鬼影**：切换位选时先输出全灭段码，再切换位选，最后输出新段码。
> 用一个 2 相的小状态机实现（`BLANK → SET_CAT → SHOW`）。

### 5.4 `dot_matrix_scan`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick` | in | 1 | 1kHz |
| `i_px_red` | in | 64 | 红色像素掩码 |
| `i_px_green` | in | 64 | 绿色像素掩码 |
| `o_dot_row` | out | 8 | 行（**低有效**） |
| `o_dot_colr` | out | 8 | 红列（高有效） |
| `o_dot_colg` | out | 8 | 绿列（高有效） |

> **扫描逻辑**：内部 3 位行计数器 `r`，每 `i_tick` 加 1。
> `o_dot_row <= not (1 sll r)`；`o_dot_colr <= i_px_red(8*r+7 downto 8*r)`；
> `o_dot_colg <= i_px_green(8*r+7 downto 8*r)`。
> 因为 §4.3 的位序与硬件天然对齐，**这里没有任何位序翻转**。
> **消隐**：`i_tick` 到来时先拉高所有行（全灭），下一个周期再切到新行。

### 5.5 `pattern_rom`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `i_sel` | in | 3 | 图案选择，见下表 |
| `o_mask` | out | 64 | 图案掩码 |

| `i_sel` | 图案 |
|---|---|
| 0 | 第一关目标（4×3 实心矩形） |
| 1 | 第二关目标 · 向上箭头 |
| 2 | 第二关目标 · 向下箭头 |
| 3 | 胜利图案 |
| 4 | 失败图案 |

> 纯组合，用 `case` 实现。**图案常量来自 `puzzle_pkg`**，不在本模块内重新写常量。

### 5.6 `piece_rom`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `i_level` | in | 1 | 0 = 第一关，1 = 第二关 |
| `i_idx` | in | 2 | 零片序号 0~3 |
| `o_rel_mask` | out | 64 | **相对掩码**（锚点为包围盒左上角，已对齐到 bit0） |
| `o_height` | out | 3 | 包围盒高度 |
| `o_width` | out | 3 | 包围盒宽度 |
| `o_valid` | out | 1 | 该关是否有这一块（第一关只有 3 块，`i_idx=3` 时 `o_valid=0`） |

### 5.7 `rng_lfsr`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_step` | in | 1 | 推进一步（每来一个高电平，LFSR 前进一拍） |
| `i_seed_load` | in | 1 | 装入种子 |
| `i_seed` | in | 8 | 种子值 |
| `o_rnd` | out | 8 | 当前随机数 |

> **LFSR 多项式**：8 位最大长度序列 `x^8 + x^6 + x^5 + x^4 + 1`（抽头 8,6,5,4）。
> 周期 255。**种子由 `game_fsm` 在"开始"键按下时采样自由运行计数器低位得到**，
> 保证每次开局不同。
>
> ⚠️ **必须每次摆放尝试都 `i_step` 一次**，否则同一个零片的所有尝试读到同一个值
> → 所有零片落在同一位置 → 永远重叠 → 永远回退。**这个 bug 不会让游戏崩溃，
> 只会让"位置随机"静默失效**，详见 `CLAUDE.md` §10.1。

### 5.8 `puzzle_ctrl` ⭐ 核心模块

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_level` | in | 1 | 当前关卡 |
| `i_round_start` | in | 1 | 新一局脉冲：清锁定、重新随机散落 |
| `i_piece_count` | in | 3 | 本关零片数（3 或 4） |
| `i_rnd` | in | 8 | 随机数 |
| `i_rnd_step` | out | 1 | **请求随机数前进一步**（每次尝试都步进） |
| — | | | |
| `i_sel` | in | 1 | "选择"键脉冲 |
| `i_conf` | in | 1 | "确认"键脉冲 |
| `i_dir` | in | 4 | 方向键脉冲，`(3)=上 (2)=下 (1)=左 (0)=右` |
| — | | | |
| `o_px_red` | out | 64 | 点阵红色像素 |
| `o_px_green` | out | 64 | 点阵绿色像素 |
| `o_all_locked` | out | 1 | 全部零片已锁定 |
| `o_solved` | out | 1 | 并集 == 目标图案 |
| `o_sel_idx` | out | 2 | 当前选中零片序号（用于音效/调试 LED） |

**内部状态**（唯一的数据源）：

```vhdl
type anchor_t is array (0 to 3) of std_logic_vector(5 downto 0);  -- 每块 {row[2:0], col[2:0]}
signal r_anchor : anchor_t;
signal r_locked : std_logic_vector(3 downto 0);
signal r_sel    : std_logic_vector(1 downto 0);
```

**掩码是组合计算的，不存储**：

```vhdl
-- 单块绝对掩码 = 相对形状按锚点平移（逐行，杜绝回绕）
function place(rel : std_logic_vector(63 downto 0);
               ar, ac : integer range 0 to 7) return std_logic_vector is
    variable res : std_logic_vector(63 downto 0) := (others => '0');
begin
    for r in 0 to 7 loop
        exit when r + ar > 7;
        for c in 0 to 7 loop
            exit when c + ac > 7;
            if rel(8*r + c) = '1' then
                res(8*(r+ar) + (c+ac)) := '1';
            end if;
        end loop;
    end loop;
    return res;
end function;
```

### 5.9 `disp_format`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `i_state` | in | 3 | 当前状态 |
| `i_level` | in | 1 | 关卡 |
| `i_preview_cnt` | in | 3 | 预览倒计时值 |
| `i_game_cnt` | in | 6 | 拼图倒计时值（秒） |
| `o_disp_val` | out | 32 | 8 位 BCD |
| `o_blank` | out | 8 | 熄灭掩码 |

纯组合。按状态决定每一位显示什么：

| 状态 | DISP7 | DISP6..5 | DISP4 | DISP3 | DISP2..1 | DISP0 |
|---|---|---|---|---|---|---|
| 自检 | `8` | `8` | `8` | `8` | `8` | `8` |
| 待机 | `5` | 灭 | 灭 | 灭 | 灭 | `1` |
| 预览 | 预览倒计时 | 灭 | 灭 | 灭 | 灭 | 关卡号 |
| 游戏中 | 灭 | 灭 | 十位 | 个位 | 灭 | 关卡号 |
| 胜负已分 | 灭 | 灭 | 灭 | 灭 | 灭 | 关卡号 |

### 5.10 `game_fsm` ⭐ 主状态机

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick_1hz` `i_tick_2hz` | in | 1 | |
| `i_key_start` `i_key_sel` `i_key_conf` | in | 1 | 功能键脉冲 |
| `i_key_dir` | in | 4 | 方向键脉冲 |
| `i_all_locked` `i_solved` | in | 1 | 来自 `puzzle_ctrl` |
| `i_rnd` | in | 8 | 随机数（选图案用） |
| `o_state` | out | 3 | 当前状态 |
| `o_level` | out | 1 | 当前关卡 |
| `o_preview_cnt` | out | 3 | 预览倒计时 |
| `o_game_cnt` | out | 6 | 拼图倒计时（秒） |
| `o_pattern_sel` | out | 3 | 图案选择 |
| `o_round_start` | out | 1 | 新一局脉冲 |
| `o_sound_sel` | out | 3 | 音效选择 |
| `o_seed_load` `o_seed` | out | 1 / 8 | 随机种子 |

**状态编码**：

| 状态 | 编码 | 含义 |
|---|---|---|
| `S_SELF_TEST` | 0 | 开机自检（2 秒，2Hz 闪烁） |
| `S_IDLE` | 1 | 待机 |
| `S_PREVIEW` | 2 | 图案预览（5 秒倒计时） |
| `S_PLAYING` | 3 | 拼图中（限时倒计时） |
| `S_WIN` | 4 | 胜利图案 |
| `S_FAIL` | 5 | 失败图案 |

**状态迁移**：

```
        ┌──────────────┐  rst（SW7=0 或上电）   ┌───────────┐
        │  S_SELF_TEST │◀──────────────────────│  任意状态  │
        └──────┬───────┘                       └───────────┘
               │ 2 秒到                            ▲
               ▼                                   │ 任意状态按"开始"
        ┌──────────────┐                           │（重新采样种子）
        │    S_IDLE    │───────────────────────────┘
        └──────┬───────┘
               │ 按"开始"
               ▼
        ┌──────────────┐
        │  S_PREVIEW   │  5 秒倒计时（DISP7）
        └──────┬───────┘
               │ 5 秒到 → 零片随机散落
               ▼
        ┌──────────────┐
        │  S_PLAYING   │  DISP4,DISP3 倒计时
        └──┬────────┬──┘
           │        │
   all_locked│      │ 倒计时到 0
           ▼        ▼
     solved? ┌──────────────┐
      ├─是──▶│    S_FAIL    │
      │      └──────────────┘
      │             ▲
      ▼             │ 第二关失败
 ┌─────────┐        │
 │ 第一关？ │────────┘
 └────┬────┘
      │ 是
      ▼
  level←2, pattern_sel←随机, 回 S_PREVIEW
      │
      └─ 第二关成功 → ┌──────────┐
                      │  S_WIN   │
                      └──────────┘
```

---

## 6. 关键算法

### 6.1 掩码代数（`puzzle_pkg`）

零片在任意时刻的绝对掩码由**锚点 + 相对形状**组合产生，**逐行搬运**：

| 操作 | 实现要点 |
|---|---|
| 右移一列 | 对每行：`r(6 downto 0) & '0'`，**逐行做，不整体 `sll`** |
| 左移一列 | 对每行：`'0' & r(7 downto 1)`，**逐行做** |
| 下移一行 | 整体右移 8 位（行与行之间本来就是连续的，安全） |
| 上移一行 | 整体左移 8 位 |

> **只有"跨行"的整体移位是安全的**；任何"跨列"的移位都必须逐行做。

### 6.2 `sys_en` 输出级门控

见 §1.2 原则三。**门控发生在输出级，不在状态机里**。

### 6.3 随机散落（`puzzle_ctrl`）

**目标**：把 `i_piece_count` 块零片放到 8×8 内，位置随机且两两不重叠。

**难点**：不能用"随机试，不行就重试"的 `while` 循环——**硬件里没有"重试到成功"这种结构**，
必须保证**有限步内一定终止**。

**方案：有界尝试 + 确定性回退**

```
for i in 0 to piece_count-1 loop          -- 依次摆放每块零片
    成功 := false
    for try in 0 to N-1 loop              -- 每块最多尝试 N 次
        ① 请求随机数前进一拍  → 取 rnd
        ② 用 rnd(5 downto 3) 作行、rnd(2 downto 0) 作列
        ③ 钳位到合法范围：行 ≤ 8-height, 列 ≤ 8-width
        ④ 算绝对掩码，与已放的零片求交
        ⑤ 若交集为 0 → 记录锚点，成功 := true，退出内循环
    end loop
    若 未成功 → 采用确定性回退位置（预置的一张不与已放零片冲突的锚点表）
end loop
```

- `N` 取 **8**：8 次尝试失败的概率极低（4 块零片在 8×8 上，可用位置很充裕）；
- **回退表保证有限终止**：最坏情况也一定会摆完，不会死锁；
- **每一次尝试都调用 `i_rnd_step`**——这条是 `CLAUDE.md` §10.1 记录的坑。

**为什么钳位而不是"丢弃越界尝试"**：钳位让每次尝试都有效，提高成功率；
且钳位与移动的边界处理复用同一段逻辑，减少重复代码路径。

### 6.4 移动与边界（要求 7 的核心）

**不变量**：**任何时刻，每块零片的锚点都满足 `行 + 高 ≤ 8` 且 `列 + 宽 ≤ 8`。**

- **由构造保证**：散落时钳位保证（§6.3 步骤③）；移动时先算新锚点，
  **只有满足不变量才更新**，否则保持原值（不动）；
- **由此从根本上排除回绕**：既然零片永远完整落在 8×8 内，
  就不存在"最右列的格跑到下一行"的情形；
- **掩码运算的逐行实现是第二道防线**，即使不变量被破坏也不会撕裂。

> **答辩要点**：要求 7 说"零片不能移出 8×8 点阵区域"。
> 我们不是"移出去再拉回来"，而是**让非法移动根本不发生**（新锚点非法则整个更新被忽略）。

### 6.5 正确性判定（要求 9 的核心）⭐

```vhdl
-- 组合计算：所有零片掩码的并集
union := (others => '0');
for i in 0 to 3 loop
    if i < piece_count then
        union := union or place(rel_mask(i), anchor_row(i), anchor_col(i));
    end if;
end loop;

solved <= '1' when union = target_mask else '0';
```

**判定准则：并集 == 目标掩码。**

**为什么不是"逐块比对初始位置"**：见 `docs/00-需求分析.md` 要求 9 ——
零片初始位置是**随机**的，且这三块零片**恰好有 2 种合法铺法**，
逐块比对会**把其中一种正确解误判为失败**。本判定与铺法无关，自动接受全部合法解。

**证据**：`scripts/verify_tiling.py` 穷举枚举了全部铺法，输出 2 种并打印每种铺法的
格子归属。运行 `python scripts/verify_tiling.py` 可复现。

### 6.6 按键处理：单次脉冲 vs 连发

| 键 | 行为 | 理由 |
|---|---|---|
| 开始 / 选择 / 确认 | **单次脉冲**（按下产生 1 个脉冲，长按不重复） | 避免误触发 |
| 上 / 下 / 左 / 右 | **按住连发**：首次立即响应，按住 500ms 后每 100ms 重复一次 | 长距离移动时不必反复点按 |

`keypad_scan` 输出"稳定按下"电平，`game_fsm` 侧做连发计时。

### 6.7 扫描消隐（防鬼影）

数码管与点阵共用"先灭后切"的原则：

```
状态机：BLANK → SWITCH → SHOW → (下一个 i_tick) → BLANK → …
```

- `seg_scan`：切位选前先把 `o_seg` 置全 0；
- `dot_matrix_scan`：切行前先把 `o_dot_row` 置全 1（低有效 → 全灭）。

### 6.8 音效（提高要求 1）

| `i_sound_sel` | 场合 | 频率 | 时长 |
|---|---|---|---|
| 0 | 静音 | — | — |
| 1 | 按键 | 2 kHz | 30 ms |
| 2 | 移动 | 1 kHz | 20 ms |
| 3 | 锁定 | 500 Hz | 80 ms |
| 4 | 预览倒计时每秒 | 1 kHz | 50 ms |
| 5 | 拼图倒计时最后 5 秒 | 4 kHz | 60 ms |
| 6 | 成功 | 2kHz→4kHz 交替 | 400 ms |
| 7 | 失败 | 500Hz→250Hz 交替 | 600 ms |

由 `tick_8k` 计数分频产生，占空比 50%。

---

## 7. 数据流与控制流小结

```
        ┌──────────── 控制流（键 → 状态） ────────────┐
键盘 ──▶ keypad_scan ──▶ game_fsm ──▶ puzzle_ctrl        │
                          │  ▲                            │
                          │  └── all_locked / solved ─────┘
                          │
                          └──▶ disp_format ──▶ seg_scan ──▶ 数码管
                          │
                          └──▶ pattern_rom ──┐
                                             ├──▶ puzzle_ctrl ──▶ dot_matrix_scan ──▶ 点阵
                          piece_rom ─────────┘        ▲
                          rng_lfsr ──────────────────┘

        ┌──────────── 时间流（节拍 → 一切） ────────────┐
clk ──▶ clk_gen ──┬── tick_1k ──▶ keypad_scan / seg_scan / dot_matrix_scan
                  ├── tick_8k ──▶ buzzer_ctrl
                  ├── tick_2hz ─▶ game_fsm（自检闪烁）
                  └── tick_1hz ─▶ game_fsm（倒计时）
```

**唯一的反馈回路**：`puzzle_ctrl` 的 `o_all_locked` / `o_solved` 回到 `game_fsm`，
驱动 `S_PLAYING → S_WIN / S_FAIL` 的转移。这是"空间"向"时间"汇报结果的唯一通道。

---

## 8. 上板前的硬件自检方案（`board_test_top`）

### 8.1 为什么需要它

本设计的正确性依赖 **7 个无法从文档推断、只能实测的硬件事实**：

| # | 待确认事实 | 若判断错误的后果 |
|---|---|---|
| 1 | 板载时钟档位 = 7（50MHz） | 所有倒计时、扫描频率全部错 |
| 2 | 键盘 ROW3 在最上排 | 方向键上下颠倒 |
| 3 | 键盘按下读入为 **1** | 按键完全无响应 |
| 4 | 点阵行**低有效** | 点阵整片点亮或全灭 |
| 5 | COLR/COLG **高有效** | 颜色红绿颠倒 |
| 6 | 数码管位选**低有效** | 数码管全灭或多位同时亮 |
| 7 | COLR0 对应**最左列** | 点阵图案左右镜像 |

### 8.2 自检工程做什么

`board_test_top` 是一个**独立于游戏逻辑的最小顶层**，逐个点亮、逐个读键：

| 阶段 | 现象 | 验证的是 |
|---|---|---|
| 1 | 全部 LED 以 1Hz 闪烁 | 时钟有、档位对（用 1Hz 数秒即可粗判） |
| 2 | 点阵逐行点亮（上→下） | 行序 + 行极性 |
| 3 | 点阵逐列点亮红色（左→右） | 列序 + COLR 极性 + **bit0 是否最左** |
| 4 | 点阵逐列点亮绿色（左→右） | COLG 极性 |
| 5 | 数码管 DISP0→DISP7 逐个显示"8" | 位选顺序 + 极性 |
| 6 | 数码管依次显示 `1 2 3 4 5 6 7 8` 的段码 | 段码顺序（AA..AP） |
| 7 | 按任意键，对应 LED 点亮（键号 = 亮灯序号） | 键盘行列顺序 + 极性 |
| 8 | 蜂鸣器发出 1kHz | 蜂鸣器可用 |

### 8.3 使用方式

```bash
# 编译自检顶层（不改 .qsf，仅本次生效）
quartus_sh -t scripts/build.tcl --top board_test_top
# 烧录后按上表逐条确认，把结果记入 docs/05-硬件调试记录.md
```

> **这是本项目"板子只能在实验室用"这一约束下的关键风险控制手段**：
> 把 7 项硬件不确定性**集中到一次实验室行程里一次性确认**，
> 避免"写完全部代码才发现极性错"的最坏情况。

---

## 9. 资源预估（EPM1270T144C5）

| 资源 | 总量 | 预估占用 | 说明 |
|---|---|---|---|
| 逻辑单元 (LE) | 1270 | ~450 | 主体是掩码加法器/比较器与两个扫描状态机 |
| 用户 I/O | 116 | ~78 | 点阵 24 + 数码管 16 + 键盘 8 + LED 16 + 其他 ~14 |
| 全局时钟 | 4 | 1 | 只用 1 个 |

**主要面积消耗**：
1. 64 位掩码的并集与比较（4 个 64 位或 + 1 个 64 位比较器）；
2. 4 块零片的 `place()` 平移逻辑（组合，可被综合器部分共享）；
3. 两个 8 路扫描状态机。

> 实际资源占用以编译报告为准，编译后截图记入 `docs/04-引脚分配表.md` 与实验报告评分项④。

---

## 10. 本文档与代码的一致性约定

> 实验报告要求"总体框图中的模块要求与实现电路（代码）一致"，且老师会**随机抽一个模块提问**。

1. **DWG-02（§2.2 第二层）一个方框 = 一个 `.vhd` 文件 = 一个实体**，文件名与实体名完全相同；
   **DWG-01（§2.1 第一层）一个方框 = 一个功能子系统**，框内列出的实体名必须与 §3.1 一致，
   且所有子系统所含实体之和 = 全部 11 个实体（不重不漏）；
2. **框图改了必须先改本文档**，不允许代码与图不一致；两层框图要一起改；
3. `puzzle_top.vhd` 的例化顺序与 §2.2 框图从上到下、从左到右一致；
4. 每个模块文件头部注释必须写明：**职责一句话 + 所属功能子系统 + 对应需求编号**
   （例：`-- 所属子系统：S6 显示子系统（DWG-01）`）。

**下一步**：`docs/02-模块详细设计.md` —— 逐个模块给出内部结构、状态机与算法细节。
