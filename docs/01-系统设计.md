# 01 · 系统设计

> 《数字电路与逻辑设计实验（下）》综合课题 · **题目 4 简易拼图游戏的设计与实现**
> 对应实验报告评分项 **②系统设计（设计思路、总体框图、分块设计）（20 分）**
> **本文档即"初步设计方案"主体**（截止 2026-09-24 24:00）

---

## 1. 设计思路

### 1.1 自顶向下的三层分解

**本设计采用自顶向下的方法逐层分解**：从需求出发，先划出系统的功能子系统，
再把每个子系统落到具体的模块（实体）。分三层：

```
第 0 层 · 需求        11 条基本要求 + 3 项提高              → docs/00-需求分析.md
        ↓ 按"要做的几件大事"切分
第 1 层 · 功能子系统   7 个子系统（DWG-01）                 → §2  总体框图
        ↓ 每个子系统再细分为可独立验证的单元
第 2 层 · 模块（实体） 11 个实体 + 2 个顶层（DWG-02）        → §3 模块划分 / §5 接口定义
```

> **为什么必须自顶向下**：题目要求的是一个**交互式游戏**——有画面（点阵）、
> 有数据显示（数码管）、有输入（键盘）、有声音（蜂鸣器）、有规则（拼图判定）、
> 有时间约束（倒计时）。如果反过来从下位模块往上拼（"先有个分频器，再加个扫描……"），
> 最终结构会由模块的便利性决定，而**没人能保证它满足了上面那些需求**。
> 自顶向下则保证每一层都能追溯到需求，见 §1.1.3 的追溯关系。

#### 1.1.1 第 1 层：7 个功能子系统

第 1 层的划分依据是**"这个系统要做哪几件互相独立的大事"**，
每件事在硬件上表现为**一组职责内聚的实体**：

| # | 功能子系统 | 一句话职责 | 所含实体 | 图纸 |
|---|---|---|---|---|
| S1 | **时钟与节拍** | 把 50MHz 变成全系统要用的几档节拍 | `clk_gen` | DWG-01 |
| S2 | **键盘输入** | 把 16 个按键变成"按了哪个键"的事件 | `keypad_scan` | DWG-01 |
| S3 | **游戏控制** | 管状态、关卡、倒计时——**只管时间** | `game_fsm` | DWG-01 |
| S4 | **拼图核心** | 管零片坐标、移动、判定——**只管空间** | `puzzle_ctrl` | DWG-01 |
| S5 | **图案与随机数据** | 提供图案掩码、零片形状、随机数 | `pattern_rom` `piece_rom` `rng_lfsr` | DWG-01 |
| S6 | **显示子系统** | 把游戏状态变成点阵与数码管上的画面 | `disp_format` `seg_scan` `dot_matrix_scan` | DWG-01 |
| S7 | **音效输出** | 按事件发出对应音效 | `buzzer_ctrl` | DWG-01 |

**子系统所含实体之和 = 11 个实体，不重不漏**（`puzzle_pkg` 是包不是实体，见 §5.0）。

#### 1.1.2 第 2 层：每个子系统怎么落到实体

切分时用**"职责类别"**作为细化的判据——它决定了模块的**接口形态**和**仿真方法**：

| 职责类别 | 特征 | 实体 | 对应的仿真验证 |
|---|---|---|---|
| **器件驱动** | 与外部器件电气特性强相关，与游戏规则无关 | `dot_matrix_scan` `seg_scan` `keypad_scan` `buzzer_ctrl` | 极性与时序（给输入看波形） |
| **数据与规则** | 纯逻辑运算，与外部器件无关 | `pattern_rom` `piece_rom` `rng_lfsr` `puzzle_ctrl` `disp_format` | 算法正确性（穷举/参考模型比对） |
| **时序与调度** | 状态、时间、节拍 | `clk_gen` `game_fsm` | 状态迁移与计数（时序图判读） |

> **这张表和上面那张表是两个层次，不要混**：
> 上表回答"系统分成哪几块"（自顶向下的**切分**），
> 本表回答"每块里的实体属于哪一类"（切分时用的**判据**）。
> 例如 S6 显示子系统里，`dot_matrix_scan` / `seg_scan` 是驱动类，
> 而 `disp_format` 是数据类——同一个子系统里可以有两种职责类别。

#### 1.1.3 每一层都能追溯到需求

自顶向下的意义在于**可追溯**：任何一个实体，都能沿"实体 → 子系统 → 需求"一路上溯。
举三个例子：

| 实体 | 所属子系统 | 直接支撑的需求 |
|---|---|---|
| `dot_matrix_scan` | S6 显示子系统 | 要求 6（选择）、要求 7（移动）、要求 9（判定）、要求 10（第二关） |
| `puzzle_ctrl` | S4 拼图核心 | 要求 6（选择）、要求 7（移动）、要求 8（锁定）、要求 9（判定） |
| `buzzer_ctrl` | S7 音效输出 | 提高要求 1（蜂鸣器音效） |

> 上表的需求编号与 `docs/02-模块详细设计.md` §15 对照表**逐条一致**
> （该表是`模块 ↔ 需求`的完整映射，此处只摘三个例子）。

完整的"需求 ↔ 模块"双向对照见 `docs/00-需求分析.md` 的需求追踪矩阵。

> **与实现顺序的关系**：**设计**是自顶向下的（本节），
> **实现**是自底向上的（先写叶子模块 `puzzle_pkg`/`clk_gen`，再逐层往上集成到 `puzzle_top`）。
> 两者不矛盾——它们作用在项目的不同阶段。详见 `CLAUDE.md` §2.1。

### 1.2 三条贯穿全局的设计原则

**原则一：时间与空间分离**

这是本设计最核心的一条结构决策。

- `puzzle_ctrl` 只管**空间**——零片在哪、什么形状、能不能放下、拼对了没有。
  它**没有倒计时、没有秒概念、不知道第几关**。
- `game_fsm` 只管**时间**——现在是什么状态、还剩几秒、该显示什么。
  它**完全不做坐标运算、不碰掩码**。

> **为什么要分离**：如果把"零片移动"和"倒计时"写在一个模块里，会得到一个既有坐标
> 又有计时的巨型进程，任何一个改动都要回归测试全部功能。分离后，
> "零片边界对不对"只看 `puzzle_ctrl`，"30 秒到了没"只看 `game_fsm`，
> 两者的仿真用例完全独立。
> **答辩时的说法**：`puzzle_ctrl` 是纯组合+寄存的"空间引擎"，`game_fsm` 是"时间调度器"，
> 二者通过 `puzzle_ctrl` 的状态输出（`all_locked` / `solved`）单向耦合。

**原则二：单一数据源**

- **零片位置只存锚点** (row, col)，不存 64 位掩码。掩码由锚点 + 形状**组合计算**。
  → 节省 4 × 64 = 256 个触发器；**从结构上杜绝"锚点和掩码不一致"这种最难查的 bug**。
- **引脚号只在 `quartus/puzzle.qsf` 出现一次**，不在任何 VHDL 里写死。
- **位序约定只在 `puzzle_pkg` 定义一次**，各模块引用而不各自解释。

**原则三：输出级统一消隐**

所有显示驱动（点阵、数码管）与蜂鸣器的**输出级**都被 SW7 门控。
下面是 `puzzle_top.vhd` 里**逐字可抄**的六行（`sw7` 是顶层端口，`s_*` 是各驱动送来的内部信号）：

```vhdl
o_dot_row  <= s_dot_row  when sw7 = '1' else (others => '1');  -- 低有效 → 全灭
o_dot_colr <= s_dot_colr when sw7 = '1' else (others => '0');
o_dot_colg <= s_dot_colg when sw7 = '1' else (others => '0');
o_seg      <= s_seg      when sw7 = '1' else (others => '0');
o_cat      <= s_cat      when sw7 = '1' else (others => '1');  -- 低有效 → 全灭
o_buzz     <= s_buzz     when sw7 = '1' else '0';
```

**这样做的理由**：要求 1 说"SW7='0' 时所有显示器件不显示"。如果让 SW7 走状态机，
从拨动开关到状态机响应、再到输出熄灭，中间会有若干时钟周期的不一致。
在输出级门控可以做到**严格零延迟**，且**只有一个地方需要维护**。
同时，`board_test_top` 也能复用同一套输出级逻辑。

### 1.3 单时钟域 + 单周期使能

**全设计只有一个时钟 `clk`（50MHz），所有时序逻辑都用 `rising_edge(clk)`。**

不使用门控时钟（不用 `clk_div` 去驱动别的触发器），而是由 `clk_gen` 产生
**单周期宽度的使能脉冲**（`tick_1k` / `tick_1hz` / …），各模块用
`if rising_edge(clk) then if tick_1k = '1' then ...` 的结构分频执行。

> **理由**：CPLD 上做时钟分频驱动全局时钟网络会引入偏斜与毛刺风险；
> 单周期使能是 CPLD/FPGA 设计的标准做法，且**所有触发器仍工作在同一个 50MHz 时钟下**，
> 时序分析简单、无跨时钟域问题。

---

## 2. 总体框图

> **本节的框图分两层，对应 §1.1 的两层分解**：
> **DWG-01** 画第 1 层的 7 个功能子系统，**DWG-02**（§2.2）把每个子系统展开到实体。
> 两张图的矢量源文件在 `docs/图/系统图.html`（内联 SVG，可导入 draw.io 编辑）；
> DWG-01 的**手绘定稿原稿**另存为 `docs/图/DWG-01-系统总体框图-飞书原稿.png`。
> **改模块接口时，两张图和 §5 的接口定义表必须同时改。**
>
> ⚠️ **以 `系统图.html` 的矢量版为准**：PNG 是留档，矢量版才可编辑、才与代码同步。
> 两者若有出入，改矢量的、重新导出 PNG，不要反过来。

### 2.1 DWG-01 · 第一层：功能子系统

系统的**对外接口**——即画在最外圈的器件连接：

```
                          ┌─────────────────────────────────────────┐
                          │  外部器件                                │
   4×4 矩阵键盘 ─────────▶│ kp_row[3:0]                 dot_row[7:0]│────────▶ 8×8 双色点阵 行(低有效)
                          │ kp_col[3:0]◀                dot_colr[7:0]│────────▶           红列(高有效)
                          │                             dot_colg[7:0]│────────▶           绿列(高有效)
   SW7 系统开关 ─────────▶│ sw7                                       │
                          │                          seg[7:0]        │────────▶ 8 位数码管 段(高有效)
   50MHz 时钟  ──────────▶│ clk                      cat[7:0]        │────────▶           位选(低有效)
                          │                                           │
                          │                          buzz             │────────▶ 蜂鸣器
                          │                          ld[15:0]        │────────▶ 16 个 LED ⚠️仅 board_test_top
                          └─────────────────────────────────────────┘
```

> ⚠️ **`ld[15:0]` 只属于 `board_test_top`（硬件自检顶层），`puzzle_top` 没有这个端口。**
> 它的用途是上板自检时"按哪个键亮哪个 LED"，直接服务 `CLAUDE.md` §9 风险表第 2 条
> （键盘行列方向与极性）。16 个引脚已在 `quartus/puzzle.qsf` 分配好，
> 引脚表见 `CLAUDE.md` §4.1 与 `docs/04` §2。

系统的**内部结构**——7 个功能子系统及其互连（**设计分层的第一层**）：

```
                            ┌───────────────────────────────┐
   SW7 系统开关 ───────────▶│  时钟与节拍  clk_gen            │
                            │  50MHz · 5 档单周期节拍          │
                            └───────────────┬───────────────┘
                                            │ 节拍总线（5 档单周期使能）
   ┌────────────────────────────────────────┼──────────────────────────────────────┐
   │   tick_8k · tick_1k · tick_100 · tick_2hz · tick_1hz                            │
   └──┬────────────┬──────────────┬─────────┴──────┬───────────────┬────────────────┘
      ▼            ▼              ▼                ▼               ▼
┌──────────┐ ┌──────────┐  ┌───────────┐   ┌────────────┐  ┌──────────┐
│ 键盘输入  │ │ 游戏控制  │  │ 拼图核心   │   │ 显示子系统  │  │ 音效输出  │
│keypad_scan│ │ game_fsm │  │puzzle_ctrl│   │disp_format │  │buzzer_ctrl│
│          │ │          │  │           │   │seg_scan    │  │          │
│tick_1k   │ │状态·倒计时│  │锚点·钳位   │   │dot_matrix_ │  │          │
│80ms 消抖  │ │·关卡·选图 │  │·锁定·判定  │   │scan        │  │          │
└────┬─────┘ └────┬─────┘  └─────┬─────┘   └─────┬──────┘  └────┬─────┘
     │            │              │               │              │
     ▼            │              │               ▼              ▼
 4×4 矩阵键盘      │              │       数码管·8×8 点阵       蜂鸣器
 kp_row/kp_col    │              │
                  │◀─────────────┘
                  │   all_locked · solved（全图唯一的反馈回路）
                  │
                  │        ┌────────────────────────────┐
                  └───────▶│      图案与随机数据          │
                           │  pattern_rom · piece_rom   │
                           │  rng_lfsr                  │
                           │  两个 ROM 纯组合；           │
                           │  rng 按 rnd_step 请求推进    │
                           └────────────────────────────┘
```

> **图的定稿版**：`docs/图/DWG-01-系统总体框图-飞书原稿.png`（手绘定稿的原始导出）
> ＋ `docs/图/系统图.html` 的 DWG-01（**可编辑矢量版**，上列 ASCII 是它的文字摘要）。
> **矢量版才是权威版本**——接口有改动时改它，PNG 只作留档。

**全部连线**。盒子里的连线太密，ASCII 画不下，列成表反而更准。
分两张：**系统级**是从图外进系统、不进任何框的；**子系统间**是框与框之间的每一笔。

**① 系统级连线（3 条）**

| # | 来源 → 去向 | 信号名 | 含义 |
|---|---|---|---|
| A | 外部时钟 → **全部 7 个子系统** | `clk` | 50MHz，**全设计唯一的时钟**（§1.3）。收 `clk` 的是 `clk_gen` 自己 ＋ S2~S7 里的 7 个时序实体，共 **8 个**。**本层（DWG-01）不画这条线**，它在 DWG-02 上体现，见下方 ⚠️ |
| B | SW7 开关 → 时钟与节拍 ＋ 顶层输出级 | `sw7` | ① 参与产生同步复位；② 顶层输出级门控（要求 1） |
| C | **BTN0 复位键 → 时钟与节拍** | `btn` | **按键复位**（课件 PDF p45（一）"复位必须用按键来实现"）。进 `clk_gen.i_btn_rst`，经 2 级同步 + 20ms 消抖并入 `o_rst`（§2.1.3 / §5.1） |

> ⚠️ **A 这一条不能画成 `clk_gen` 的输出。**
> `clk_gen` 只负责"把 50MHz 变成几档节拍"，**它不转发 `clk`** ——
> 每个时序模块收的都是**顶层直连的原始 `clk`**。这才是"单时钟域"的字面含义。
> **漏掉这条线的后果是实打实的**：照图建顶层内部信号时，会漏掉 `clk` 的分发，
> 而 `clk_gen` 的 6 个输出里本来就没有 `clk`（见下表第 0 条）。
>
> **那它画在哪张图上？** —— **两层的画法已经分工定了**，不重复画：
> - **DWG-01（本层）不画**：这一层画的是"功能子系统怎么组织"，不是引脚清单；
>   `clk` 是 `clk_gen` 这一子系统的**属性**（分频基准），折在椭圆的小字里（§2.1.2 第 1 处）。
> - **DWG-02（实体层）画**：在那里它是**一条脚注**，逐个列出 8 个接收者的实体名
>   （`clk_gen` · `keypad_scan` · `game_fsm` · `puzzle_ctrl` · `rng_lfsr` · `seg_scan` ·
>   `dot_matrix_scan` · `buzzer_ctrl`），并且每个时序实体的框内小字都以 `clk` 开头。
>
> 之所以不画成 8 条实线：`clk` 要横穿全图 8 次，画出来反而看不清主线。
> **接收者名单本身是可数的**，这是替代画线的等价物。

**② 子系统间连线（13 条）** —— 编号 0~12，**共 13 行**（0 号也是正儿八经的一条线，别漏数）。
**加系统级 3 条，全图共 16 条** —— 改图时直接数这个数（`CLAUDE.md` §12.4 第 5 步）。

| # | 起点 → 终点 | 信号名（框图上去掉方向前缀） | 含义 |
|---|---|---|---|
| 0 | **时钟与节拍 → 6 个子系统**（`rst`）／**→ 4 个子系统**（节拍） | `rst` · `tick_8k` · `tick_1k` · `tick_100` · `tick_2hz` · `tick_1hz` | **节拍总线**：`rst` 送 S2~S7 全部 6 个子系统；5 档单周期使能按需分配 —— S2 `tick_1k`、S3 `tick_100`/`tick_2hz`/`tick_1hz`、S6 `tick_8k`×2、S7 `tick_8k`。**S4 拼图核心与 S5 图案与随机数据不接任何节拍**（§2.1.1 第 3 处） |
| 1 | 键盘输入 → 游戏控制 | `key_code` · `key_press` | 键号 + 单次按下脉冲（**键号译码在 `game_fsm` 内做**，见 §6.6） |
| 2 | 游戏控制 → 拼图核心 | `round_start` · `sel` · `conf` · `dir` | 何时开局 ＋ **转发**的三种操作键脉冲 |
| 3 | 游戏控制 → 图案与随机数据 | `pattern_sel` · `level` · `seed` · `seed_load` | `pattern_sel` 同驱 `pattern_rom` 与 `piece_rom`（**零片永远跟着图案走**，2026-09-18 修正）；`level` 仅供显示（关卡号）；随机种子 ＋ **装入脉冲** |
| 4 | **拼图核心 → 图案与随机数据** | `rnd_step` | **每次落位尝试请求 LFSR 推进一步** |
| 5 | 图案与随机数据 → 拼图核心 | `target_mask` · `rel_mask` · `height` · `width` · `count` · `rnd` | 目标图案 / 4 块零片的**形状、包围盒高宽**与块数 / 随机数 |
| 6 | 拼图核心 → 显示子系统 | `px_red` · `px_green` | 64 位红/绿像素掩码 |
| 7 | 游戏控制 → 显示子系统 | `state` · `level` · `preview_cnt` · `game_cnt_bcd` · `blink` | 状态、关卡号、两个倒计时 ＋ **2Hz 自检闪烁方波**（要求 1） |
| 8 | 游戏控制 → 音效输出 | `sound_sel` · `sound_trig` | 响哪一种 ＋ 何时响 |
| 9 | **拼图核心 → 游戏控制** | `all_locked` · `solved` | **唯一的反馈回路** |
| 10 | 显示子系统 → 器件 | `seg` · `cat` · `dot_row` · `dot_colr` · `dot_colg` | 数码管与点阵驱动 |
| 11 | 音效输出 → 器件 | `buzz` | 音频方波 |
| 12 | 图案与随机数据 → 显示子系统 | `pattern_mask` | `pattern_rom.o_mask` 的**第二去处**：预览（要求 4）与胜负图案直接上点阵 |

> **第 4 条的方向最容易画反，而且画反了不出声、不报错** —— `rnd_step` 是
> **`puzzle_ctrl` 发出**的。因为**只有它知道何时做了一次落位尝试**，
> 而 LFSR 必须每次尝试推进一步（见 §2.1.1 第 3 处、`CLAUDE.md` §10.1）。
> 若画成 `game_fsm` 驱动，它根本无从得知 `puzzle_ctrl` 何时尝试过 ——
> 正好落回那个"**游戏照常跑，只是永远不随机**"的坑。

**这张图怎么读**（答辩讲这张图的四句话）：

1. **纵向是一条数据通路**：键盘 → 游戏控制 → 拼图核心 → 显示子系统 → 器件。
   按键从左边进，画面从右边出，**全程单向**。
2. **横向是一根节拍总线**：`clk_gen` 在最上面（椭圆画法，强调它是**全系统唯一的分频与节拍源**），
   把 5 档单周期使能扇出给需要计时的子系统（对应 §1.3 的单时钟域方案）。
   **注意它扇出的是节拍，不是时钟** —— `clk` 由顶层直连各模块（上表第 A 条）。
3. **游戏控制是枢纽**：除推进拼图核心外，它还要给显示子系统送 `state`/`level`/`preview_cnt`/`game_cnt_bcd`、
   给音效输出送 `sound_sel`/`sound_trig`、给图案与随机数据送选择与关卡——
   **管时间的那块，天然是分发点**。
   > 但**枢纽不等于什么都由它发**：`rnd_step` 就是例外（上表第 4 条），
   > 它必须由"知道何时做了尝试"的 `puzzle_ctrl` 发。**这条例外正好标注了两个子系统的分界**。
4. **只有一条反馈**：`puzzle_ctrl` 的 `all_locked` / `solved` 回到 `game_fsm`——
   因为"拼完没有"必须回到管状态的那个子系统去驱动状态迁移。
   **除这一条外，本设计没有任何回环**，这是它能被清晰分层的前提。

其中 **S4 拼图核心 / S6 显示子系统 / S5 图案与随机数据** 三块的关系值得特别注意：
`pattern_rom` 提供**目标图案**（拼成什么样），`piece_rom` + `rng_lfsr` 提供
**零片形状与随机落点**（拿什么拼），两者都汇入 `puzzle_ctrl`
（拼得对不对），结果再交给 `disp_format` 变成画面。

#### 2.1.1 定稿过程中修正的四处 ⭐

框图先画在飞书白板上，再定稿为矢量图。定稿时改了四处，**每一处都是实现前必须澄清的**：

| # | 改动 | 为什么必须改 |
|---|---|---|
| 1 | **补回 `all_locked · solved` 反馈** | 手绘稿漏画这条线。缺了它就解释不了 `S_PLAYING` 凭什么走到 `S_WIN`/`S_FAIL` —— 全设计只有这一条回环，是**装载信息的**，不是装饰 |
| 2 | **补回 `SW7` 与 `50MHz` 两个外部输入** | SW7 是基本要求第 1 条（`SW7='0'` 时全部显示熄灭），**不能不出现在总体框图上**；时钟是子系统的输入，不画就从图上找不到源头。<br>⚠️ **本处只兑现了一半**：白板稿第 2 版又把 50MHz 折回了椭圆，见 §2.1.2 第 1 处与 §2.1 系统级连线的 ⚠️ |
| 3 | **删掉「图案与随机数据」的节拍输入** | 见下方专条，这是**最容易埋雷的一处** |
| 4 | **框图名统一为「显示子系统」** | 与 §1.1.1 的子系统命名、§3.1 的模块清单保持一致（原名"显示系统"少一个字，会让两层框图对不上号） |

**第 3 处展开——为什么「图案与随机数据」不接节拍：**

`pattern_rom` 与 `piece_rom` 是**纯组合**的常量表，根本没有时序；
`rng_lfsr` 是一个由 `puzzle_ctrl` 的 `rnd_step` **请求推进**的移位寄存器，
它接的是原始 `clk`，不是任何一档节拍。

若图上画成"接节拍"，实现时就极易写成"每个 `tick` 采一次随机数"——
那样**同一拍内的多次落位尝试会读到同一个随机数**：所有零片落点相同 →
必然重叠 → 用尽尝试次数 → 每次都走回退布局。
**游戏仍然能跑，只是永远不随机**，这种缺陷靠观察现象基本发现不了。
详见 `CLAUDE.md` §10.1 与 `docs/02` §9.5。

> 本处原先写作"`docs/02` §11"——那是 `disp_format`。**LFSR 在 §9**，
> 2026-09-17 随连线表核对一并改正。

#### 2.1.2 飞书稿第 2 版的两处调整

白板稿后来又改了一版（2026-09-17），矢量图已同步。本版改了两处：

| # | 调整 | 说明 |
|---|---|---|
| 1 | **50MHz 并入时钟椭圆，不再单画一条输入线** | 50MHz 是这一子系统的**属性**（分频基准），不是第二路控制输入。真正需要从图外进来的只有 `sw7` —— 它参与复位（`rst = btn_rst or (not SW7) or por_rst`），且在要求 1 里要即时熄灭显示。<br>⚠️ **本处原先只提 `sw7`**：`btn_rst` 是本文档 §2.1.3 补进的第三个复位来源 |
| 2 | **反馈连线补上中文说明**（`all_locked · solved（反馈游戏是否结束）`）| 信号名是给实现看的，中文说明是给读者看的，两者都要 |

> **`clk` 仍是 `puzzle_top` 的真实端口**（见 §5 接口定义表），
> 只是在这一层的图上不再单独画一条线：这一层画的是**功能子系统怎么组织**，
> 不是一个引脚清单。引脚清单在 `docs/04-引脚分配表.md`。
> 两条线都画反而会让人以为时钟是"两路输入"。

> ⚠️ **本版同步时又抓到一处贴边缺陷**：`sw7 系统开关` 标签原本左对齐在 `x=70`，
> 文字宽约 79 单位，右端落到 `x≈158` —— **压过容器左边界 `x=150` 约 8 个单位**。
> 缩略图上看不出来（ERR-0006 / ERR-0008 那一类），
> 是按 `CLAUDE.md` §12.4 清单**第 3 步放大 2.5 倍逐段看**时抓到的。
> 改为右对齐到 `x=132`（留 18 单位余量）。
> **这次是清单主动发现的，不是偶然看到的** —— 流程改动生效了，见 `ERRORS.md` ERR-0009。

#### 2.1.3 按课程课件补回按键复位（第三次改动）⭐

**起因**：课程课件《数字系统设计2026》**p45（一）开机复位**原文——

> *"初值只具有仿真意义，不能实现对系统的复位，**复位必须用按键来实现**。"*

**本项目原先的复位只有两源**：`por_rst`（上电计数器）＋ `(not SW7)`。
其中 `por_rst` **恰恰就是课件说的"初值"，只具有仿真意义** ——
它靠一个计数器数 10ms 来"假装"复位，运行期间再也不会动作。**只靠它是不符合要求的。**

| # | 改动 | 落点 |
|---|---|---|
| 1 | **系统级连线由 2 条增为 3 条**，新增 `btn`（BTN0 复位键 → 时钟与节拍） | §2.1 系统级连线表第 C 条 |
| 2 | `clk_gen` 新增输入端口 **`i_btn_rst`** | §5.1 |
| 3 | 复位方程由两源改为**三源或**：`o_rst <= r_btn_rst or r_por_rst or (not sys_en)` | §4.1、`docs/02` §3 |
| 4 | **DWG-01 / DWG-02 各补一笔** `btn`（图与表必须同时改，§12.3） | `docs/图/系统图.html` |

> ⭐ **按键不能直接接进复位方程**，中间必须有两级处理，缺一不可：
>
> | 处理 | 为什么不能省 |
> |---|---|
> | **2 级同步器** | 按键对 `clk` 是**异步输入**。直接采样会引入亚稳态 —— 这是**功能仿真查不出、上板偶发**的一类缺陷 |
> | **20ms 消抖** | 课件 **p54「按键要注意防抖处理」** ＋ 开发板手册附图16「按键在使用时需设计防抖动电路」。抖动对复位功能无害，**但课件要求的是"按键要消抖"**，不是"抖动有害的按键才消抖" |
>
> **与 SW7 的对比**（同一张表里的两种做法，答辩常问）：
> SW7 是**拨动开关**、要求"即时熄灭"，故**不消抖**；BTN0 是**按键**，故**消抖**。
> **判据是"器件类型 + 该信号的功能要求"，不是一刀切。**
>
> ⚠️ **复位本身仍是"同步、高有效"，没有变成异步复位** ——
> 按键先被同步进 `clk` 域，再作为同步复位使用。详见 `docs/02` §3.5。

> ⚠️ **消抖计数器不得被 `o_rst` 复位** —— 与"上电复位计数器不依赖 `o_rst`"是同一个坑：
> 复位期间若把它停住/清零，就**永远退不出复位**。

> ⭐ **课件 p22 的交通灯方框图里，`复位` 就是一个独立的输入框**（箭头标 `reset` 进控制器）
> —— 本次给 DWG-01 补 `btn` 这一笔，与课件的画法一致。

### 2.2 DWG-02 · 第二层：实体级框图（DWG-01 的展开）
```text
⚠️ 本节原先贴了一张 60 行的 ASCII 框图。它已经和定稿的矢量图对不上
（少一条 seed / seed_load、少 pattern_mask、把 puzzle_ctrl 直连到了 dot_matrix_scan），
而且和 §2.1 的连接表是同一份信息的第二份拷贝 —— 两份就会漂移，所以整段删掉。
```

DWG-02 的**权威版本是矢量图**：`docs/图/系统图.html` 里的第 2 张（可导入 draw.io）。
它和 §2.1 的连接表是**同一份信息**，表格负责"数得清"，图负责"看得懂"：

| 要查什么 | 去哪里 |
|---|---|
| 每个实体叫什么、属于哪个子系统、是纯组合还是时序 | §3.1 模块清单 |
| 框与框之间的**每一笔连线**（16 条） | §2.1 的连线表 —— **唯一真值源**，改图前先改它 |
| 每条线的**走向与画法** | `docs/图/系统图.html` 的 DWG-02 |
| 每个实体的**端口逐条定义** | §5 接口定义表 |

> ⚠️ **动手改图前必须走 `CLAUDE.md` §12.4 的**五步清单**。**
> `scripts/check_svg.py` 只保证"文件合法、走线不穿框"，**它查不出漏画了一条线** ——
> ERR-0013 的 3 条漏画线就是这么漏掉的（第 5 步"图→表反向对账"才抓到）。

**DWG-02 的画法约定**（与图里的注释一一对应）：

1. **一个矩形框 = 一个 `.vhd` 文件 = 一个实体**，框内第一行是实体名；
2. **框内第二、三行是该模块的时钟来源**：以 `clk` 开头的是时序实体，
   写"纯组合"的三个（`disp_format` / `pattern_rom` / `piece_rom`）**不接任何时钟**；
3. **橙色虚线**是唯一的反馈回路（`all_locked` / `solved`）；
4. **最右侧灰色虚线区**是 `puzzle_top` 的输出级门控，不是模块；
5. **`px_red` / `px_green` 出现两次不是笔误** —— `disp_format` 只做多路选择、不改名。

**框图与代码的一致性**：

- **本图（DWG-02，第二层）每个方框都对应 `rtl/` 下一个独立文件、独立实体**，
  一一对应，无合并、无拆分。老师按图抽查任意一个方框，都能在代码里找到同名实体。
- **§2.1 的图（DWG-01，第一层）每个方框是一个功能子系统**，
  方框内列出的实体名之和 = §3.1 模块清单里的全部 11 个实体，不重不漏。
- **两张图不是二选一**：DWG-01 回答"系统分成哪几块"，DWG-02 回答"每块由哪些实体实现"。
  报告里两张都要放，**答辩被抽查的是 DWG-02 的实体**（那才是 `.vhd` 文件）。

---

## 3. 模块划分

### 3.1 模块清单

**所属子系统**一列即 §2.1 的 DWG-01 第一层框图 —— 两列对得上，两层图就对得上。

| # | 模块 | **所属子系统**（DWG-01） | 职责类别 | 一句话职责 | 是否有时序逻辑 |
|---|---|---|---|---|---|
| 0 | `puzzle_pkg` | —（包，不属于任何子系统） | 包 | 全局常量、掩码运算纯函数、位序约定 | 否（纯声明） |
| 1 | `clk_gen` | **S1** 时钟与节拍 | 驱动 | 时钟分频、节拍使能、**复位产生（上电 + 按键）** | 是 |
| 2 | `keypad_scan` | **S2** 键盘输入 | 驱动 | 4×4 矩阵键盘扫描、消抖、边沿检测 | 是 |
| 3 | `seg_scan` | **S6** 显示子系统 | 驱动 | 8 位数码管动态扫描 + BCD→段码译码 | 是 |
| 4 | `dot_matrix_scan` | **S6** 显示子系统 | 驱动 | 8×8 双色点阵动态扫描（含消隐防鬼影） | 是 |
| 5 | `pattern_rom` | **S5** 图案与随机数据 | 数据 | 图案掩码查找表（目标/胜利/失败图案） | 否（纯组合 ROM） |
| 6 | `piece_rom` | **S5** 图案与随机数据 | 数据 | 零片形状查找表（相对掩码 + 宽高） | 否（纯组合 ROM） |
| 7 | `rng_lfsr` | **S5** 图案与随机数据 | 数据 | LFSR 伪随机数发生器 | 是 |
| 8 | `puzzle_ctrl` | **S4** 拼图核心 | 逻辑 | 零片锚点 / 选择 / 移动 / 钳位 / 锁定 / 正确性判定 | 是 |
| 9 | `disp_format` | **S6** 显示子系统 | 逻辑 | 显示总入口：状态 → 8 位 BCD + 熄灭掩码 **+ 点阵像素多路选择** | 否（纯组合） |
| 10 | `game_fsm` | **S3** 游戏控制 | 调度 | 游戏主状态机、倒计时、关卡切换、图案与音效选择 | 是 |
| 11 | `buzzer_ctrl` | **S7** 音效输出 | 驱动 | 蜂鸣器音效产生 | 是 |
| — | `puzzle_top` | 顶层 | 顶层 | 例化全部子模块（**`component` + `port map`**，见 §10 第 5 条）+ **唯一做引脚绑定** | — |
| — | `board_test_top` | 顶层 | 顶层 | **上板硬件自检专用顶层**（见 §8），同样用 `component` 例化 | — |

> **`puzzle_pkg` 为什么不属于任何子系统**：它是**包（package）不是实体**，
> 不产生任何硬件，只提供常量与纯函数供各模块引用（见 §5.0）。
> 因此它不出现在任何一张框图的方框里，但**每个模块都依赖它**。
> 报告里说明这一点，可以避免"框图上找不到 `puzzle_pkg`"的追问。

### 3.2 为什么这样分（答辩要点）

- **每个模块职责单一，能用一句话说清**——这是"可独立仿真"的前提；
- **驱动与逻辑严格分开**——改点阵扫描频率不会碰到游戏规则；
- **时间与空间分开**（见 §1.2 原则一）——`puzzle_ctrl` 无计数器，`game_fsm` 无坐标；
- **ROM 独立**——图案和零片形状是**数据**不是**逻辑**，改图案不该动状态机；
- **纯组合模块单独成文件**（`disp_format` / `pattern_rom` / `piece_rom`）——
  它们最容易仿真（无时钟，给输入立刻出输出），可以 100% 覆盖。

---

## 4. 全局设计约定

> 这些约定在 `puzzle_pkg` 中集中定义，**全项目引用，不得各自解释**。

### 4.1 时钟与复位

| 项 | 约定 |
|---|---|
| 系统时钟 | `clk` = **50MHz**（板上档位 7），周期 20ns |
| 时钟域 | **全设计单时钟域**，只用 `rising_edge(clk)` |
| 分频方式 | **单周期使能脉冲**（`tick_xxx`），**不使用门控时钟** |
| 复位 | **同步、高有效**。**三源或**：`rst = btn_rst or por_rst or (not SW7)` |
| **按键复位** ⭐ | **BTN0 → `clk_gen.i_btn_rst`**，经 **2 级同步器 + 20ms 消抖**后并入 `o_rst`。**课件 PDF p45（一）强制要求"复位必须用按键来实现"**（§2.1.3） |
| 上电复位 | `clk_gen` 内部产生，约 10ms 高电平，保证上电后所有寄存器处于已知状态。**它只解决"上电初值"，不能替代按键复位** |
| SW7 消抖 | **不做消抖** —— 要求是"即时熄灭"，消抖会引入延迟，反而违背要求 |
| 按键消抖 | **BTN0 20ms ＋ 矩阵键盘 80ms 都做** —— 课件 p54「按键要注意防抖处理」＋手册附图16。**SW7 是拨动开关，不在此列** |

### 4.2 节拍使能清单

| 信号 | 频率 | 用途 |
|---|---|---|
| `tick_8k` | 8 kHz | 蜂鸣器音效时基（4kHz / 2kHz / 1kHz / 500Hz 方波）、**点阵行推进、数码管位选推进** |
| `tick_1k` | 1 kHz | 键盘扫描 |
| `tick_100` | 100 Hz | 按键消抖计数、慢速逻辑 |
| `tick_2hz` | 2 Hz | 开机自检的 2 秒计时（要求 1）—— ⚠️ **不是拿来直接当闪烁方波用的**，理由见下 |
| `tick_1hz` | 1 Hz | 预览倒计时（要求 4）、拼图倒计时（要求 5） |

> ⚠️ **闪烁方波 `o_blink` 由 `tick_100` 分频得到，不是 `tick_2hz`。**
> `tick_*` 全是**单周期使能**（一拍宽的高电平），而闪烁要的是**电平**。
> 把 `tick_2hz` 直接当方波送出去，得到的实际是 **1Hz** 方波（每个使能翻转一次）。
> 正确做法：`tick_100` 数 25 个数得 4Hz，再二分频得 2Hz 方波（`game_fsm` 内，见 §7 与 `docs/02` §12.7）。
> 这条也是 `board_test_top` 自检时要盯的一项。

> **为什么点阵和数码管改用 `tick_8k`（2026-09-18 审查修正，原设计用 `tick_1k`）**：
> 两者都是 8 路扫描、每路 2 相（消隐 + 显示）。**若用 `tick_1k`，每位 2ms、
> 一帧 16ms，刷新率只有 62.5Hz** —— 勉强高过人眼闪烁阈值（约 60Hz），
> 且每像素点亮占空仅 1/16，**又闪又暗的风险很高**（原文此处按"1 相 1ms"算成 125Hz，
> 与两相消隐的设计自相矛盾）。
> 改用 `tick_8k` 后：每位 0.25ms、一帧 2ms、**刷新率 500Hz**，余量充足；
> 代价为零（`tick_8k` 本来就要为蜂鸣器产生），且与键盘扫描（仍用 `tick_1k`）互不干扰。

### 4.3 点阵位序约定 ⭐

**这是全项目最容易出错的地方，在此单点定义。**

| 项 | 约定 |
|---|---|
| 掩码编码 | `mask(8 × 行 + 列)`，**列号向右递增**，故 **`bit 0` = 左上角** |
| 取第 r 行 | `mask(8*r + 7 downto 8*r)` |
| 行内位序 | **`bit 0` = 该行最左列**，`bit 7` = 该行最右列 |
| 「向右移一列」 | 行向量**左移**：`r(6 downto 0) & '0'` |
| 「向下移一行」 | 整个掩码**右移 8 位**：`"00000000" & m(63 downto 8)` |
| 与驱动的关系 | `dot_matrix_scan` 扫描第 r 行时，直接把 `mask(8*r+7 downto 8*r)` 赋给 `dot_colr` |

**为什么这样定**：因为硬件上 **COLR0 接的物理列就是最左列**（手册：COLR0~COLR7
依次为 22、21、16、15、14、13、12、11 脚），所以"行内 bit0 = 最左列"与
"COLR0 = 最左列"**天然对齐，无需任何位序翻转**。

> ⚠️ **反面教材**：本机桌面上另一个旧工程把 `col_r[0]` 接到了 `PIN_11`（即 COLR7），
> 与手册相反。那个工程自身是自洽的，但**引脚表已不再对应板上丝印**。
> 详见 `ERRORS.md` ERR-0003。**结论：引脚号只从手册拿。**

### 4.4 器件极性一览

| 器件 | 极性 | 备注 |
|---|---|---|
| 点阵行 `dot_row` | **低有效** | 0 = 选中该行 |
| 点阵红列 `dot_colr` | **高有效** | 1 = 该列红色 LED 可导通 |
| 点阵绿列 `dot_colg` | **高有效** | 1 = 该列绿色 LED 可导通 |
| 数码管段 `seg` | **高有效** | `seg(0)`=AA … `seg(6)`=AG, `seg(7)`=AP |
| 数码管位选 `cat` | **低有效** | `cat(0)`=DISP0(最右) … `cat(7)`=DISP7(最左) |
| LED `ld` | **高有效** | |
| 键盘行 `kp_row` | 按下 = **1** | 列有 10k 上拉、行有 4.7k 下拉 |
| 键盘列 `kp_col` | 扫描时**驱动一列为高** | |
| 复位键 `btn` | 按下 = **1** | BTN0，高有效（与手册 BTN0~BTN7 一致） |
| 系统开关 `sw7` | 高 = 开 | **拨动开关**，非按键 |
| 蜂鸣器 `buzz` | 方波输出 | 频率决定音高 |

> ✅ **课件 p45（二）的"共阴极数码管接法"图独立印证了本表的 `seg` 极性**：
> 课件画的是共阴极数码管、段线经 100~300Ω 限流、**高电平点亮** —— 与"`seg` 高有效"一致。

**像素点亮条件**：

| 颜色 | 行 | COLR | COLG |
|---|---|---|---|
| 红 | 低 | 高 | 低 |
| 绿 | 低 | 低 | 高 |
| 黄 | 低 | 高 | 高 |
| 灭 | 低 | 低 | 低 |

### 4.5 显示与配色语义

| 显示 | 含义 |
|---|---|
| `DISP7`（最左） | 预览倒计时（要求 4）；待机时固定显示 "5"（要求 2） |
| `DISP4`,`DISP3` | 拼图倒计时两位十进制（要求 5） |
| `DISP0`（最右） | **关卡号**（要求 3；原文要求 9 的 "DISP2" 为笔误，见 `docs/00` §6） |
| 其余位 | 熄灭 |

| 点阵配色 | 零片状态 |
|---|---|
| **红** | 未选中、未锁定 |
| **绿** | 当前选中（要求 6） |
| **黄** | 已锁定（要求 8） |

### 4.6 键盘映射

```
             COL0    COL1    COL2    COL3
ROW3:         —       上       —      开始
ROW2:        左       下      右      选择
ROW1:         —        —       —      确认
ROW0:         —        —       —       —
```

`KEY号 = (3 − ROW) × 4 + COL + 1`；**ROW3 在最上排**（实物丝印确认）。

> ✅ **本公式已用课件 PDF p55 的示例表独立验证过**（2026-09-18）：
> 课件给出 `KEY1 = COL0×ROW3`、`KEY2 = COL1×ROW3`、`KEY5 = COL0×ROW2`、`KEY13 = COL0×ROW0`，
> 逐条代入本公式全部吻合。
> **这次验证顺带抓出 `docs/04` §3.3 键号布局表上下颠倒的 bug**（见 `ERRORS.md` ERR-0017）。

> ⚠️ **但课件的键盘图是「另一块板」的**：其按键丝印为 `S1~S8 / S9 / S0 / SEL / SET / SA~SD`、
> 扫描线 `SCAN0~3`、读线 `KEY0~3`，且为**低有效**扫描（`SCAN3=0`/`KEY3=0` 表示按下）；
> 本板键盘丝印为 `KEY1~KEY16`，手册描述的上下拉网络（列 10k 上拉、行 4.7k 下拉）
> 对应**高有效**（驱列为高、读行为 1）。
> **裁决：极性以开发板手册为准，最终以 `board_test_top` 上板自检为据**；
> 两者**键位对应关系一致**，差别只在电气极性，可一处翻转。详见 `docs/04` §3.3 与 §4。

---

## 5. 模块接口定义

> 端口命名：`i_` 前缀输入、`o_` 前缀输出。
> **例外**：全局端口 **`clk` / `rst` / `sys_en`（以及各模块的 `i_tick*`）不带方向前缀**
> —— 它们是全设计共享的时基与使能，带前缀反而看不出"这是同一根线"。
> `sys_en` 只在 `clk_gen` 上出现（§5.1），其余模块拿到的是 `rst`，不是 `sys_en`。

### 5.0 `puzzle_pkg`（包，不是实体）

```vhdl
package puzzle_pkg is

    -- ---------- 数组类型（供端口使用：零片是 4 块，形状要成组传）----------
    type mask_arr_t is array (0 to 3) of std_logic_vector(63 downto 0);  -- 4 块零片掩码
    type dim_arr_t  is array (0 to 3) of std_logic_vector(2 downto 0);   -- 4 块的高/宽

    -- ---------- 位序约定（§4.3）----------
    function row_of(idx : integer) return integer;          -- 由位号取行号
    function col_of(idx : integer) return integer;          -- 由位号取列号
    function bitidx(r, c : integer) return integer;         -- 行列 → 位号

    -- ---------- 掩码纯函数 ----------
    function row_mask(m : std_logic_vector(63 downto 0); r : integer)
        return std_logic_vector;                            -- 取第 r 行 8 位
    function shift_right_col(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 右移一列（逐行左移）
    function shift_left_col(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 左移一列（逐行右移）
    function shift_down_row(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 下移一行
    function shift_up_row(m : std_logic_vector(63 downto 0))
        return std_logic_vector;                            -- 上移一行
    function popcount(m : std_logic_vector(63 downto 0)) return integer;

    -- ---------- 图案常量（位图见 docs/02 §2.5，由 verify_tiling.py 生成，勿手工改动）----------
    constant L1_TARGET_MASK : std_logic_vector(63 downto 0) := "...";  -- 4×3 矩形，12 格
    constant L2_TARGET_MASK : std_logic_vector(63 downto 0) := "...";  -- 向上箭头，18 格
    constant L2B_TARGET_MASK: std_logic_vector(63 downto 0) := "...";  -- 向下箭头，18 格
    constant WIN_MASK       : std_logic_vector(63 downto 0) := "...";  -- 对勾
    constant FAIL_MASK      : std_logic_vector(63 downto 0) := "...";  -- 叉

    -- ---------- 零片相对掩码常量（位图见 docs/02 §2.5）----------
    constant L1_P0 : std_logic_vector(63 downto 0) := "...";   -- 6 格 3×3
    constant L1_P1 : std_logic_vector(63 downto 0) := "...";   -- 3 格 2×2
    constant L1_P2 : std_logic_vector(63 downto 0) := "...";   -- 3 格 1×3
    constant L2_P0 : std_logic_vector(63 downto 0) := "...";   -- 6 格 3×3
    constant L2_P1 : std_logic_vector(63 downto 0) := "...";   -- 6 格 3×3
    constant L2_P2 : std_logic_vector(63 downto 0) := "...";   -- 4 格 2×2
    constant L2_P3 : std_logic_vector(63 downto 0) := "...";   -- 2 格 1×2
    constant L2B_P0..L2B_P3 : ...;   -- 下箭头零片 4 块（⭐ 2026-09-18 补，见 docs/02 §2.5）

    -- ---------- 状态编码（game_fsm 按它赋值、disp_format 按它分支，见 docs/02 §2.7）----------
    subtype state_t is std_logic_vector(2 downto 0);
    constant S_SELF_TEST / S_IDLE / S_PREVIEW / S_PLAYING / S_WIN / S_FAIL : state_t;  -- "000"~"101"

    -- ---------- 分频常量（★ 串行级联，单点定义，禁止散落魔法数字）----------
    constant CLK_HZ  : integer := 50_000_000;      -- ★ 板上时钟档位必须为 7
    constant CNT_8K  : integer := CLK_HZ / 8_000 - 1;   -- 50MHz→8kHz，分频比 6250
    constant CNT_1K  : integer := 8_000 / 1_000 - 1;    -- 8kHz→1kHz，  分频比 8
    constant CNT_100 : integer := 1_000 / 100   - 1;    -- 1kHz→100Hz， 分频比 10
    constant CNT_2HZ : integer := 100   / 2     - 1;    -- 100Hz→2Hz，  分频比 50
    constant CNT_1HZ : integer := 100   / 1     - 1;    -- 100Hz→1Hz，  分频比 100
    constant CNT_POR : integer := CLK_HZ / 100  - 1;    -- 上电复位 10ms（直数 clk）

    -- ---------- 时间常量 ----------
    constant T_PREVIEW   : integer := 5;    -- 预览秒数
    constant T_LEVEL1    : integer := 30;   -- 第一关限时
    constant T_LEVEL2    : integer := 40;   -- 第二关限时
    constant T_SELFTEST  : integer := 2;    -- 自检秒数（★ 由 game_fsm 用 tick_2hz 数 2×T_SELFTEST 实现）

    -- ---------- 消抖 / 连发 / 闪烁 / 种子（docs/02 §2.6 的完整清单为准，此处从略）----------
    -- 含 DEBOUNCE_MAX（矩阵键盘 80ms）与 DEBOUNCE_BTN（按键复位 20ms）

end package;
```

> ⭐ **只有 `CNT_8K` 的除数带 `CLK_HZ`** —— 后四级数的是"上一级来了几个脉冲"，
> 是纯比例关系（8、10、50、100），与板上时钟档位无关。
> **万一档位确认下来不是 50MHz，只需改 `CLK_HZ` 一处**（`CNT_POR` 同理，它直数 `clk`）。
> 级联的分频器结构见 `docs/02` §3.3、位宽账见 `CLAUDE.md` §10.2 ②。
>
> ✅ **`WIN_MASK` / `FAIL_MASK` 已定稿**（2026-09-18）：位图与掩码常量见
> `docs/02` §7.4 / §2.5，由 `scripts/verify_tiling.py` 生成并做过往返反查。

> ⚠️ **`mask_arr_t` / `dim_arr_t` 为什么要放进包里**：`piece_rom` 要一次吐出 4 块零片的
> 形状（64 位 × 4），`puzzle_ctrl` 要一次收 4 块。**VHDL-93 的端口不能用匿名数组**，
> 类型必须在包里声明一次，两个模块引用同一个 —— 这样 `component` 声明与实体声明才同型。

> **注意**：`shift_right_col` 等函数**必须逐行处理**，不能整体 `sll`——
> 整体移位会让最右列的格回绕到下一行最左列。函数内部按
> `for r in 0 to 7 loop new(8*r+7 downto 8*r) := old(8*r+6 downto 8*r) & '0'; end loop;` 实现。
> **这是要求 7 边界正确性的根本保障。**

### 5.1 `clk_gen`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` | in | 1 | 50MHz 系统时钟 |
| `sys_en` | in | 1 | SW7，用于产生同步复位 |
| `i_btn_rst` | in | 1 | **BTN0 复位键**，按下 = 1。**课件 PDF p45（一）强制的按键复位入口** |
| `o_rst` | out | 1 | 同步高有效复位（**按键复位 或 上电复位 或 SW7=0**） |
| `o_tick_8k` | out | 1 | 8kHz 单周期使能 |
| `o_tick_1k` | out | 1 | 1kHz 单周期使能 |
| `o_tick_100` | out | 1 | 100Hz 单周期使能 |
| `o_tick_2hz` | out | 1 | 2Hz 单周期使能 |
| `o_tick_1hz` | out | 1 | 1Hz 单周期使能 |

> ⭐ **`i_btn_rst` 是本接口唯一"因课件而增"的端口**（第 11 次会话），
> 起因是课件 PDF p45（一）：*"初值只具有仿真意义……复位必须用按键来实现"*（§2.1.3）。
> **它必须经过"2 级同步 + 20ms 消抖"才能并入 `o_rst`** —— 理由见 §2.1.3 与 `docs/02` §3.5。

> **上电复位置位**：`por_rst` 由内部计数器从 0 计到 `CNT_POR` 产生，
> 期间保持高电平；**计数器在 `por_rst` 有效期间不停止**，保证一定会退出。
> **按键消抖计数器同理不得依赖 `o_rst`**，否则同样退不出复位。

### 5.2 `keypad_scan`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick` | in | 1 | 1kHz（扫描 + 消抖计时） |
| `i_kp_row` | in | 4 | 键盘行输入，按下 = 1 |
| `o_kp_col` | out | 4 | 键盘列扫描输出，逐列为高 |
| `o_key_code` | out | **5** | 键号 1~16；0 = 无键。**非 0 即"有键稳定按住"**。★ 见下方位宽说明 |
| `o_key_press` | out | 1 | **单次按下脉冲**，宽度 1 个 `i_tick` |

> **扫描方式**：4 个 `i_tick` 完成一轮（逐列拉高读行）；连续 **20 轮**读到同一个键
> 才认为稳定。一轮 = 4 个 `i_tick` = 4ms（`i_tick` 是 1kHz），
> 故消抖时长 = **20 × 4ms = 80ms**。`o_key_press` 在"消抖后新按下"时输出一个脉冲，
> **松手后才允许下一次**（避免长按连发——但方向键需要连发，见 §6.6）。
>
> ⚠️ **本节原先写"20ms 消抖"，与"20 轮"自相矛盾**（20 轮明明是 80ms）。
> 2026-09-17 核对时改正为 80ms，与 `docs/02` §4.3 的代码骨架一致。
>
> ⚠️ **`o_key_code` 是 5 位，不是 4 位。** 键号公式是 `KEY号 = (3-ROW)*4 + COL + 1`，
> **最大取到 16**（ROW0/COL3），而 4 位只能表示 0~15 —— **16 会被静默截断成 0，
> 也就是"什么都没按"**：那个键会彻底失去响应，且**仿真不报任何错**。
> 5 位表示 0~31，绰绰有余。多出的这 1 位触发器是必须付的代价。
> **判据很简单：先算出信号的取值范围，再定位宽**（`CLAUDE.md` §10.2 ③）。
>
> **没有 `o_key_down` 端口**：`o_key_code /= 0` 本身就等价于"有键按住"，
> 再单出一个电平端口是**同一事实的第二份副本**（违反 §1.2 原则二）。
> `docs/02` §12.7 的连发计时直接用 `i_key_code` 判断。`keypad_scan` 内部
> 仍有一个 `r_down` 信号做"松手才允许下一次"的闸门，但它是**内部信号，不出端口**。

### 5.3 `seg_scan`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick` | in | 1 | **8kHz**（2026-09-18 由 1kHz 改，见 §4.2 注：两相消隐下刷新率 62.5Hz → 500Hz） |
| `i_disp_val` | in | 32 | 8 位 BCD，**`i_disp_val(4*k+3 downto 4*k)` 对应 DISP`k`** |
| `i_blank` | in | 8 | 每位熄灭掩码，`i_blank(k)='1'` 时 DISP`k` 熄灭 |
| `o_seg` | out | 8 | 段码 AA,AB,…,AG,AP（高有效） |
| `o_cat` | out | 8 | 位选（**低有效**），`o_cat(0)`=DISP0(最右) |

> **防鬼影**：切换位选时先输出全灭段码，再切换位选，最后输出新段码。
> 用一个 **2 相**的小状态机实现（BLANK → SHOW，BLANK 相段码全灭、位选保持，
> SHOW 相同沿切位选与段码）。⚠️ 本节原先写成 3 相（BLANK→SET_CAT→SHOW），
> 与 `docs/02` §5.5 的 2 相实现矛盾，2026-09-18 审查统一为 2 相。

### 5.4 `dot_matrix_scan`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick` | in | 1 | **8kHz**（同 §5.3 的修正） |
| `i_px_red` | in | 64 | 红色像素掩码 |
| `i_px_green` | in | 64 | 绿色像素掩码 |
| `o_dot_row` | out | 8 | 行（**低有效**） |
| `o_dot_colr` | out | 8 | 红列（高有效） |
| `o_dot_colg` | out | 8 | 绿列（高有效） |

> **扫描逻辑**：内部 3 位行计数器 `r`，每 `i_tick` 加 1。
> `o_dot_row <= not std_logic_vector(to_unsigned(1, 8) sll r)`；
> **注意 `1 sll r` 在 VHDL-93 里是类型错**（整数字面量不能直接移位），
> 必须先 `to_unsigned(1, 8)` 变成向量；
> `o_dot_colr <= i_px_red(8*r+7 downto 8*r)`；
> `o_dot_colg <= i_px_green(8*r+7 downto 8*r)`。
> 因为 §4.3 的位序与硬件天然对齐，**这里没有任何位序翻转**。
> **消隐**：`i_tick` 到来时先拉高所有行（全灭），下一个周期再切到新行。

### 5.5 `pattern_rom`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `i_sel` | in | 3 | 图案选择，见下表 |
| `o_mask` | out | 64 | 图案掩码 |

| `i_sel` | 图案 |
|---|---|
| 0 | 第一关目标（4×3 实心矩形） |
| 1 | 第二关目标 · 向上箭头 |
| 2 | 第二关目标 · 向下箭头 |
| 3 | 胜利图案 |
| 4 | 失败图案 |

> 纯组合，用 `case` 实现。**图案常量来自 `puzzle_pkg`**，不在本模块内重新写常量。

### 5.6 `piece_rom`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `i_pattern_sel` | in | 3 | 图案选择（**与 `pattern_rom.i_sel` 同源**，来自 `game_fsm.o_pattern_sel`）：`"000"`=第一关矩形、`"001"`=上箭头、`"010"`=下箭头 ⭐ **2026-09-18 由 `i_level`(1) 改**——原设计下箭头被随机选中时零片不匹配，永远无法通关（`docs/02` §8.2） |
| `o_rel_mask` | out | 4×64 | **4 块的相对掩码**（`mask_arr_t`；锚点 = 包围盒左上角，已对齐到 bit0） |
| `o_height` | out | 4×3 | 4 块的包围盒高度（`dim_arr_t`） |
| `o_width` | out | 4×3 | 4 块的包围盒宽度（`dim_arr_t`） |
| `o_count` | out | 3 | 本关零片数（**3 或 4**） |

> ⭐ **为什么一次输出 4 块，而不是"给序号查一块"**：本设计**顶层例化全部子模块**
> （见 §10 第 5 条与 `CLAUDE.md` §10.3），`piece_rom` 必须是顶层框图中的**一个方框**。
> 若它只支持"按序号查一块"，`puzzle_ctrl` 就得在内部例化 4 份 —— 那样
> **框图上会多出一个藏在 `puzzle_ctrl` 里的方框**，图与代码立刻对不上。
>
> **代价是可控的**：`o_rel_mask` 从 64 位变 256 位，但每位只是在两关的常量之间做 2 选 1，
> 而**两位常量相同的位置（绝大多数是 '0'）会被综合器直接折叠掉**，实际不增加多少 LE。
>
> **`o_count` 也在这里出**：它是"这一关有几块"，与零片形状同源。
> 若改由 `game_fsm` 另发一个 `piece_count`，就是同一事实的第二份副本 ——
> 一旦两者不一致（例如 `count=4` 而第 4 块形状无效），`puzzle_ctrl` 会读到垃圾。
>
> **没有 `o_valid` 端口**：块数由 `o_count` 唯一表达，见上。
> 第一关的数据表里第 4 槽填全 0 即可（`docs/02` §8.3）。

### 5.7 `rng_lfsr`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_step` | in | 1 | 推进一步（每来一个高电平，LFSR 前进一拍） |
| `i_seed_load` | in | 1 | 装入种子 |
| `i_seed` | in | 8 | 种子值 |
| `o_rnd` | out | 8 | 当前随机数 |

> **LFSR 抽头**：8 位最大长度序列，Fibonacci 反馈抽头 **bit 7/5/4/3**（经穷举验证周期 255）。
> ⚠️ 原先此处写的多项式名 `x^8+x^6+x^5+x^4+1` 与抽头不符且非本原（周期仅 61），
> 2026-09-18 审查删正，**以抽头为准**（详见 `docs/02` §9.3）。
> 周期 255。**种子由 `game_fsm` 在"开始"键按下时采样自由运行计数器低位得到**，
> 保证每次开局不同。
>
> ⚠️ **必须每次摆放尝试都 `i_step` 一次**，否则同一个零片的所有尝试读到同一个值
> → 所有零片落在同一位置 → 永远重叠 → 永远回退。**这个 bug 不会让游戏崩溃，
> 只会让"位置随机"静默失效**，详见 `CLAUDE.md` §10.1。
>
> **端口名 ↔ 连线名的别名**：本表的 `i_step` / `i_seed_load` 在 §2.1 连线表里写作
> `rnd_step` / `seed_load`（上游是 `puzzle_ctrl.o_rnd_step` 与 `game_fsm.o_seed_load`）。
> 顶层建内部互连信号时用连线表的名字（`s_rnd_step` / `s_seed_load`）。

### 5.8 `puzzle_ctrl` ⭐ 核心模块

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_round_start` | in | 1 | 新一局脉冲：清锁定、重新随机散落 |
| — | | | **来自 S5 图案与随机数据**（决定"拼成什么样、拿什么拼"） |
| `i_target_mask` | in | 64 | **目标图案掩码**（来自 `pattern_rom.o_mask`） |
| `i_rel_mask` | in | 4×64 | 4 块零片相对掩码（`mask_arr_t`，来自 `piece_rom`） |
| `i_height` | in | 4×3 | 4 块包围盒高（`dim_arr_t`，来自 `piece_rom`） |
| `i_width` | in | 4×3 | 4 块包围盒宽（`dim_arr_t`，来自 `piece_rom`） |
| `i_piece_count` | in | 3 | 本关零片数 3 或 4（来自 `piece_rom.o_count`） |
| `i_rnd` | in | 8 | 随机数（来自 `rng_lfsr`） |
| `o_rnd_step` | out | 1 | **请求随机数前进一步**（每次尝试都步进，见 §6.3） |
| — | | | **来自 S3 游戏控制**（转发来的操作键脉冲） |
| `i_sel` | in | 1 | "选择"键脉冲 |
| `i_conf` | in | 1 | "确认"键脉冲 |
| `i_dir` | in | 4 | 方向键脉冲，`(3)=上 (2)=下 (1)=左 (0)=右`（含连发） |
| — | | | **输出** |
| `o_px_red` | out | 64 | 点阵红色像素 |
| `o_px_green` | out | 64 | 点阵绿色像素 |
| `o_all_locked` | out | 1 | 全部零片已锁定 |
| `o_solved` | out | 1 | 并集 == 目标图案 |

> ⚠️ **本模块没有 `i_level` 端口，这是刻意的** —— §1.2 原则一要求
> `puzzle_ctrl` **"不知道第几关"**。原先的接口表里有 `i_level`，**与本设计自己声明的
> 职责分离原则相矛盾**，2026-09-17 核对时删除。移除后每一处用途都有了替代：
>
> | 原先用 `i_level` 做什么 | 现在由谁承担 |
> |---|---|
> | 选零片形状 | `piece_rom.i_level` 直接接 `game_fsm.o_level`（§2.1 连线表第 3 条） |
> | 知道本关有几块 | `i_piece_count` ← `piece_rom.o_count` |
> | 选回退锚点表 | **不需要分关卡**，两关回退表的前 3 项完全相同（§6.3） |
>
> ⚠️ **`i_rnd_step` 原先写作方向 `out`、却带 `i_` 前缀 —— 自相矛盾**，
> 且被误当成 `game_fsm` 的输出（§2.1 连线表第 4 条曾画反）。现统一为
> **`o_rnd_step`，`puzzle_ctrl` 的输出**。理由见 §6.3 与 `CLAUDE.md` §10.1。

**内部状态**（唯一的数据源）：

```vhdl
type anchor_t is array (0 to 3) of std_logic_vector(5 downto 0);  -- 每块 {row[2:0], col[2:0]}
signal r_anchor : anchor_t;                     -- 4×6 = 24 个触发器
signal r_locked : std_logic_vector(3 downto 0); --  4 个
signal r_sel    : std_logic_vector(1 downto 0); --  2 个
```

**组合计算的中间量**（不是状态，只是为了把 §6.5 的判定写成一行）：

```vhdl
signal s_valid_mask : std_logic_vector(3 downto 0);  -- 本关哪几块存在
signal s_union      : std_logic_vector(63 downto 0); -- 4 块掩码的并集
```

> `s_valid_mask` 由 `i_piece_count` 展开（`k` 是 `for k in 0 to 3` 的循环常量）：
> `s_valid_mask(k) <= '1' when to_integer(unsigned(i_piece_count)) > k else '0';`
> —— **`i_piece_count` 是 `std_logic_vector`，必须转成整数才能和 `k` 比大小**。
> **它存在的唯一理由是让 `o_all_locked` 不必依赖"第 4 块恰好没锁"这种巧合**。
> 用 4 个 1 位比较器，而非 `(others => '1') sll (count-1)` 之类的移位 —— 后者在
> `count = 0` 时会算错，而比较器不会。

**掩码是组合计算的，不存储**（零片只存锚点，不存掩码）：

```
-- 单块绝对掩码 = 相对形状按锚点平移（逐行，越界丢弃 → 杜绝回绕）
function place(rel : std_logic_vector(63 downto 0);
               ar, ac : integer range 0 to 7) return std_logic_vector;
```

> ⚠️ **这里只写函数签名，不抄函数体** —— 函数体属于实现细节，
> **唯一实现见 `docs/02-模块详细设计.md` §10.4**。
> 早期版本在本文档里也抄了一份完整函数体，**与 `docs/02` §10.4 是两份不同的写法**
> （本文档用"外层 `exit when` 提前跳出"，`docs/02` 用"内层 `if` 逐格判断"）。
> 两份都正确，但**两份就是漂移的种子** —— 改一份忘一份，
> 报告与代码、文档与文档之间就会对不上。**函数体一律只留一份。**
>
> 签名的两个形参 `ar` / `ac` **必须带 `range 0 to 7`**：
> 它们参与加法 `r + ar`，裸 `integer` 会让加法器变成 **32 位**（`CLAUDE.md` 硬约束 9）。

### 5.9 `disp_format`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `i_state` | in | 3 | 当前状态 |
| `i_level` | in | 1 | 关卡 |
| `i_preview_cnt` | in | 3 | 预览倒计时值 |
| `i_game_cnt_bcd` | in | 8 | 拼图倒计时，**BCD**：`(7..4)`=十位、`(3..0)`=个位 |
| `i_blink` | in | 1 | **2Hz 方波**，自检闪烁用（要求 1；由 `game_fsm.o_blink` 提供） |
| — | | | **来自 S5 图案与随机数据** |
| `i_pattern_mask` | in | 64 | 图案掩码（`pattern_rom.o_mask`）：预览图案（要求 4）与胜负图案 |
| — | | | **来自 S4 拼图核心** |
| `i_px_red` `i_px_green` | in | 64 | 游戏中的红 / 绿像素掩码 |
| — | | | **输出** |
| `o_disp_val` | out | 32 | 8 位 BCD |
| `o_blank` | out | 8 | 熄灭掩码 |
| `o_px_red` `o_px_green` | out | 64 | **最终**上点阵的红 / 绿像素掩码 |

> ⚠️ **`i_game_cnt` 原先写作 6 位二进制，与 `game_fsm` 实际输出的 8 位 BCD 不符**
> （`o_game_cnt` / `o_game_cnt_bcd` 两个名字、两种位宽并存）。2026-09-17 统一为
> **8 位 BCD、名字带 `_bcd` 后缀**。理由与代价见 `docs/02` §11.4：
> 6 位二进制要转两位十进制，就得在组合逻辑里放一个 `/10` 除法器（约 30~60 LE）；
> 让倒计时状态**天生就是 BCD**，除法器就根本不存在。**名字带 `_bcd` 是为了让
> "这是 BCD 不是二进制"在端口名上就看得见，避免将来有人拿它去参与算术。**

纯组合。按状态决定每一位显示什么：

| 状态 | DISP7 | DISP6..5 | DISP4 | DISP3 | DISP2..1 | DISP0 |
|---|---|---|---|---|---|---|
| 自检 | `8` | `8` | `8` | `8` | `8` | `8` |
| 待机 | `5` | 灭 | 灭 | 灭 | 灭 | `1` |
| 预览 | 预览倒计时 | 灭 | 灭 | 灭 | 灭 | 关卡号 |
| 游戏中 | 灭 | 灭 | 十位 | 个位 | 灭 | 关卡号 |
| 胜负已分 | 灭 | 灭 | 灭 | 灭 | 灭 | 关卡号 |

**点阵显示什么**（同一张状态表，管的是 `o_px_red` / `o_px_green`）：

| 状态 | 点阵上的内容 | 来源 |
|---|---|---|
| 自检 | 64 点**全黄**（红+绿同时亮），受 `i_blink` 通断 | 常量全 1 ＋ `i_blink` |
| 待机 | 全灭 | 常量全 0 |
| 预览 | **完整目标图案**（红） | `i_pattern_mask`（要求 4） |
| 游戏中 | 零片像素 | `i_px_red` / `i_px_green`（透传） |
| 胜负已分 | 胜利 / 失败图案 | `i_pattern_mask` |

> ⚠️ **原先没有这一级** —— `disp_format` 只输出数码管内容，全项目**没有任何模块**
> 能把"自检全黄闪烁"和"预览完整图案"送到点阵上（`dot_matrix_scan` 只有
> `i_px_red/green` 一个来源，而 `puzzle_ctrl` 在预览期间给的是零片初始位置）。
> 2026-09-17 核对时补上（ERR-0014）。
> **这一级放在 `disp_format` 而不是顶层**：它本来就是"显示格式化"，
> 且放在模块里才能**独立仿真**（`CLAUDE.md` §2 硬约束 11）。

### 5.10 `game_fsm` ⭐ 主状态机

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick_1hz` `i_tick_2hz` `i_tick_100` | in | 1 | 节拍（`tick_100` 供方向键连发计时） |
| — | | | **来自 S2 键盘输入**（原始键号，**译码在本模块内做**） |
| `i_key_code` | in | **5** | 键号 1~16；0 = 无键。**非 0 即"有键按住"**。★ 见 §5.2 的位宽说明 |
| `i_key_press` | in | 1 | 单次按下脉冲（消抖后的"新按下"） |
| — | | | **来自 S4 拼图核心** |
| `i_all_locked` `i_solved` | in | 1 | 全部锁定 / 拼对了 |
| — | | | **输出** |
| `o_state` | out | 3 | 当前状态 |
| `o_blink` | out | 1 | **2Hz 方波**（`i_tick_100` ÷ 25 ÷ 2），自检闪烁用（要求 1） |
| `o_level` | out | 1 | 当前关卡 |
| `o_preview_cnt` | out | 3 | 预览倒计时 |
| `o_game_cnt_bcd` | out | 8 | 拼图倒计时，**BCD**（同 §5.9） |
| `o_pattern_sel` | out | 3 | 图案选择 |
| `o_round_start` | out | 1 | 新一局脉冲 |
| `o_sel_out` | out | 1 | 转发给 `puzzle_ctrl` 的"选择"脉冲 |
| `o_conf_out` | out | 1 | 转发给 `puzzle_ctrl` 的"确认"脉冲 |
| `o_dir_out` | out | 4 | 转发给 `puzzle_ctrl` 的方向脉冲（**含连发**） |
| `o_sound_sel` | out | 3 | 音效选择（响哪一种） |
| `o_sound_trig` | out | 1 | **音效触发脉冲**（何时响），驱动 `buzzer_ctrl.i_trigger` |
| `o_seed_load` `o_seed` | out | 1 / 8 | 随机种子 |

**键号译码（本模块内的第一段组合逻辑）**：

键盘的**物理布局**（§4.6）到**游戏动作**的映射属于游戏规则，不属于器件驱动，
因此放在本模块而不是 `keypad_scan` —— 后者只管"按了哪个键"，
是纯粹的**器件驱动**类模块（§1.1.2）。

| 动作 | 键号 | 按键位置（§4.6） |
|---|---|---|
| 上 | **2** | ROW3 · COL1 |
| 开始 | **4** | ROW3 · COL3 |
| 左 | **5** | ROW2 · COL0 |
| 下 | **6** | ROW2 · COL1 |
| 右 | **7** | ROW2 · COL2 |
| 选择 | **8** | ROW2 · COL3 |
| 确认 | **12** | ROW1 · COL3 |

> 键号由 `keypad_scan` 按 `KEY号 = (3 − ROW) × 4 + COL + 1` 算出（`docs/02` §4.4），
> 本模块用 `case i_key_code` 把它译成 7 路动作。
> **译码只写在一处**，`i_key_press` 做"新按下"的门控、`i_key_code` 做"还按着"的判定，
> 两者配合产生单次脉冲与连发（`docs/02` §12.7）。

> ⚠️ **`o_sound_trig` 是 2026-09-17 核对时新增的**：`buzzer_ctrl.i_trigger`
> （§5.11）是**边沿触发**，必须有人给它脉冲，而此前全项目**没有任何模块有这个输出**。
> 这种缺陷最阴险的地方在于 —— **顶层 `port map` 里漏掉它不会报错，那根线会静默悬空**，
> 表现为"代码齐全、仿真网表齐全，蜂鸣器就是不响"。
> 触发时机由 `game_fsm` 决定（按键 / 移动 / 锁定 / 倒计时 / 胜负都是它产生的事件），
> 与已有的 `o_round_start` 是同一种东西。

> ⭐ **本模块没有 `i_rnd` 端口，随机数由模块内部产生**（2026-09-17 核对时删除）。
> `game_fsm` 内部有一个 **8 位自由运行计数器**（除 `rst` 外从不停止，每 `clk` 加 1），
> 在"开始"键按下时**采样**一次：
>
> | 用途 | 取值 | 去向 |
> |---|---|---|
> | 随机种子（提高③） | 采样到的 8 位 | `o_seed` ＋ `o_seed_load` → `rng_lfsr`（§2.1 连线表第 3 条） |
> | 选图案（提高②） | 采样值的某一位 | `o_pattern_sel` |
>
> **为什么不用 `rng_lfsr.o_rnd` 当种子**：`rng_lfsr` 只在 `puzzle_ctrl` 请求时步进
> （连线表第 4 条），复位后的值本身由种子决定 —— **拿它的输出当种子是循环论证**；
> 而且那会在 S3 与 S5 之间造出第二个回环，破坏 §2.1"唯一的反馈回路"那条结论。
> 自由运行计数器则与采样时刻无关：**采样落在哪个值上完全取决于人按"开始"的时机**，
> 代价只有 8 个触发器。
>
> ⚠️ **`o_blink` 由 `i_tick_100` 分频得到**（÷25 得 4Hz，再二分频得 **2Hz 方波**）。
> 用 `i_tick_2hz` 直接翻转只能得到 1Hz 方波 —— 而 `tick_2hz` 是**单周期使能**，
> 不是方波，**"把使能当方波送给显示模块"是这一处最容易犯的错**。

**状态编码**：

| 状态 | 编码 | 含义 |
|---|---|---|
| `S_SELF_TEST` | 0 | 开机自检（2 秒，2Hz 闪烁） |
| `S_IDLE` | 1 | 待机 |
| `S_PREVIEW` | 2 | 图案预览（5 秒倒计时） |
| `S_PLAYING` | 3 | 拼图中（限时倒计时） |
| `S_WIN` | 4 | 胜利图案 |
| `S_FAIL` | 5 | 失败图案 |

**状态迁移**：

**主干（一条直线，不含那两条"任意状态"边）**：

```
   S_SELF_TEST ──2 秒到──▶ S_IDLE ──按「开始」──▶ S_PREVIEW ──5 秒到──▶ S_PLAYING
                                                                          │
                                                       ┌──────────────────┤
                                                       │                  │
                                              all_locked 且 solved   倒计时到 0
                                                       │                  │
                                     ┌─────────────────┴───┐              │
                                     │ 第一关？             │              │
                                     └──┬───────────────┬──┘              │
                                        │ 是            │ 否              │
                                        ▼               ▼                 ▼
                          level←2 后回 S_PREVIEW    S_WIN            S_FAIL
```

**两条"任意状态"边**（画在上图里会把线搅乱，所以单独列）：

| 边 | 从 | 到 | 动作 |
|---|---|---|---|
| **复位** | **任意** 6 个状态 | `S_SELF_TEST` | 所有寄存器清零（`rst = btn_rst or por_rst or (not SW7)`） |
| **重开** ⭐ | **任意** 6 个状态 | `S_PREVIEW` | 重置本局：`level`←第一关、重新采样种子、清锁定、`preview_cnt`←5 |

> **图形版见 `docs/图/系统图.html` 的 DWG-03**（本节只列迁移，不重复画箭头）。

> ⭐ **"任意状态按开始 → 直接进 `S_PREVIEW`"，不是"先回 `S_IDLE`"**（2026-09-17 修正）。
> 依据是 `docs/00` 要求 11 的**验收判据原文**：
> *"在预览中 / 拼图中 / 失败图案显示中按「开始」，均能**立即回到第一关的预览态**"*。
> 若走 `S_IDLE` 中转，按一次键只会停在待机态，**要按两次才回到预览** —— 与判据不符。
> 现在这条边是**一条规则覆盖全部 6 个状态**（`S_SELF_TEST` 也不例外，
> 于是自检期间按键可以跳过自检），**没有特例就不容易写错**。

### 5.11 `buzzer_ctrl`

| 端口 | 方向 | 宽度 | 说明 |
|---|---|---|---|
| `clk` `rst` | in | 1 | |
| `i_tick_8k` | in | 1 | 8kHz 时基 |
| `i_sound_sel` | in | 3 | 音效选择（来自 `game_fsm`） |
| `i_trigger` | in | 1 | 触发脉冲，**边沿触发**播放一次完整音效 |
| `o_buzz` | out | 1 | 方波输出（输出级由 `sys_en` 门控） |

> 音效表、频率推导与内部逻辑见 `docs/02-模块详细设计.md` §13。
> **`i_trigger` 是边沿触发**：一次脉冲播一个完整音效，播完自动静音，不需要外部计时。

> ⚠️ **本节是 2026-09-17 补上的** —— 原文 §5 只写到 §5.10 `game_fsm`，
> **`buzzer_ctrl` 一直没有接口定义**。因为 `component` 声明的端口要照本节抄，
> 这个缺口会让 `buzzer_ctrl` 的顶层声明无据可依，故补齐（见 `ERRORS.md` ERR-0010）。

---

## 6. 关键算法

### 6.1 掩码代数（`puzzle_pkg`）

零片在任意时刻的绝对掩码由**锚点 + 相对形状**组合产生，**逐行搬运**：

| 操作 | 实现要点 |
|---|---|
| 右移一列 | 对每行：`r(6 downto 0) & '0'`，**逐行做，不整体 `sll`** |
| 左移一列 | 对每行：`'0' & r(7 downto 1)`，**逐行做** |
| 下移一行 | 整体右移 8 位（行与行之间本来就是连续的，安全） |
| 上移一行 | 整体左移 8 位 |

> **只有"跨行"的整体移位是安全的**；任何"跨列"的移位都必须逐行做。

### 6.2 `sys_en` 输出级门控

见 §1.2 原则三。**门控发生在输出级，不在状态机里**。

### 6.3 随机散落（`puzzle_ctrl`）

**目标**：把 `i_piece_count` 块零片放到 8×8 内，位置随机且两两不重叠。

**难点**：不能用"随机试，不行就重试"的 `while` 循环——**硬件里没有"重试到成功"这种结构**，
必须保证**有限步内一定终止**。

**方案：有界尝试 + 确定性回退**

```
for i in 0 to piece_count-1 loop          -- 依次摆放每块零片
    成功 := false
    for try in 0 to N-1 loop              -- 每块最多尝试 N 次
        ① 请求随机数前进一拍  → 取 rnd
        ② 用 rnd(5 downto 3) 作行、rnd(2 downto 0) 作列
        ③ 钳位到合法范围：行 ≤ 8-height, 列 ≤ 8-width
        ④ 算绝对掩码，与已放的零片求交
        ⑤ 若交集为 0 → 记录锚点，成功 := true，退出内循环
    end loop
    若 未成功 → 采用确定性回退位置（预置的一张不与已放零片冲突的锚点表）
end loop
```

> ⚠️ **上面是伪代码，不可照抄。** 至少两处不能进综合器：
> ① `for i in 0 to piece_count-1` 与 `for try in 0 to N-1` 的**上界来自信号**——
>    VHDL-93 的循环上界**必须是常量**（`CLAUDE.md` §10.2 ①），
>    否则综合器只能按该信号的完整位宽展开加法器。
>    真实实现用**固定上界** `for i in 0 to 3` / `for try in 0 to 7`，
>    再在循环体里用 `if i < n` 判断"这一块存不存在"。
> ② `成功 := false` / `成功 := true` 是布尔 variable，真实代码里是
>    `r_placed` 这样的信号位。
> **可综合的唯一版本在 `docs/02-模块详细设计.md` §10.6。**

- `N` 取 **8**：8 次尝试失败的概率极低（4 块零片在 8×8 上，可用位置很充裕）；
- **回退表保证有限终止**：最坏情况也一定会摆完，不会死锁；
- **每一次尝试都拉高一次 `o_rnd_step`**——这条是 `CLAUDE.md` §10.1 记录的坑。
  **回退表两关通用**（前 3 项完全相同，第一关只是用不到第 4 项），
  这也是 `puzzle_ctrl` 不需要 `i_level` 的原因之一（详见 §5.8 的端口说明与 `docs/02` §10.6）。

**为什么钳位而不是"丢弃越界尝试"**：钳位让每次尝试都有效，提高成功率；
且钳位与移动的边界处理复用同一段逻辑，减少重复代码路径。

### 6.4 移动与边界（要求 7 的核心）

**不变量**：**任何时刻，每块零片的锚点都满足 `行 + 高 ≤ 8` 且 `列 + 宽 ≤ 8`。**

- **由构造保证**：散落时钳位保证（§6.3 步骤③）；移动时先算新锚点，
  **只有满足不变量才更新**，否则保持原值（不动）；
- **由此从根本上排除回绕**：既然零片永远完整落在 8×8 内，
  就不存在"最右列的格跑到下一行"的情形；
- **掩码运算的逐行实现是第二道防线**，即使不变量被破坏也不会撕裂。

> **答辩要点**：要求 7 说"零片不能移出 8×8 点阵区域"。
> 我们不是"移出去再拉回来"，而是**让非法移动根本不发生**（新锚点非法则整个更新被忽略）。

### 6.5 正确性判定（要求 9 的核心）⭐

```text
⚠️ 伪代码，不可照抄 —— 可综合的实现在 docs/02-模块详细设计.md §10.5

-- 组合计算：所有零片掩码的并集
union := (others => '0');
for i in 0 to 3 loop
    if i < piece_count then
        union := union or place(rel_mask(i), anchor_row(i), anchor_col(i));
    end if;
end loop;

solved <= '1' when union = target_mask else '0';
```

> ⚠️ **上面这段是伪代码，写下的是"判定意图"，不是能编译的 VHDL。**
> 它至少有三处不合法或未定义：
> ① `anchor_row(i)` / `rel_mask(i)` 这种"两个平行数组"在本项目里**不存在** ——
>    锚点只有**一个**数组 `r_anchor`（元素是 6 位的 `{行,列}`），
>    形状来自 `i_rel_mask`（类型是 `mask_arr_t` = **`array (0 to 3) of std_logic_vector(63 downto 0)`**，
>    4 块各一个 64 位相对掩码），两者靠**同一个下标**对齐；
> ② `if i < piece_count` 把**整数 `i`** 和**`std_logic_vector` 的块数**直接比大小 ——
>    位矢量不能隐式转成整数，必须写 `i < to_integer(unsigned(i_piece_count))`。
>    （`i` 在这里是 `for` 循环变量、本来就是整数，不存在"用矢量当下标"的问题；
>    真正卡住的是**同一行里整数和位矢量不能直接比较**。）
> ③ `piece_count` / `target_mask` / `union` / `solved` 都是**本段自造的名字**，
>    真实名字分别是 `i_piece_count` / `i_target_mask` / `s_union` / `o_solved`，
>    而 `s_union` 是内部信号、`o_solved` 是端口。
>
> **可综合的唯一版本在 `docs/02-模块详细设计.md` §10.5。**

**判定准则：并集 == 目标掩码。**

**为什么不是"逐块比对初始位置"**：见 `docs/00-需求分析.md` 要求 9 ——
零片初始位置是**随机**的，且这三块零片**恰好有 2 种合法铺法**，
逐块比对会**把其中一种正确解误判为失败**。本判定与铺法无关，自动接受全部合法解。

**证据**：`scripts/verify_tiling.py` 穷举枚举了全部铺法，输出 2 种并打印每种铺法的
格子归属。运行 `python scripts/verify_tiling.py` 可复现。

### 6.6 按键处理：单次脉冲 vs 连发

| 键 | 行为 | 理由 |
|---|---|---|
| 开始 / 选择 / 确认 | **单次脉冲**（按下产生 1 个脉冲，长按不重复） | 避免误触发 |
| 上 / 下 / 左 / 右 | **按住连发**：首次立即响应，按住 500ms 后每 100ms 重复一次 | 长距离移动时不必反复点按 |

**分工**（三段，各管一件事，不要混）：

| 环节 | 在哪 | 靠什么信号 |
|---|---|---|
| 扫描 + 消抖 + "新按下"脉冲 | `keypad_scan` | `o_key_code` · `o_key_press` |
| **键号 → 动作的译码** | `game_fsm` | `case i_key_code`（表格见 §5.10） |
| 连发计时 | `game_fsm` | `i_key_code /= 0` 判"还按着" + `i_tick_100` 计时 |

> ⚠️ **`keypad_scan` 不输出"按住"电平端口**：`o_key_code /= 0` 已经表达了它（§5.2）。
> 另出一根电平线会造成两个问题：① 同一事实两份副本；
> ② `docs/02` 里那个端口一度写成"1 位出、4 位收"，**自己和自己对不上**。

### 6.7 扫描消隐（防鬼影）

数码管与点阵共用"先灭后切"的原则：

```
状态机：BLANK → SWITCH → SHOW → (下一个 i_tick) → BLANK → …
```

- `seg_scan`：切位选前先把 `o_seg` 置全 0；
- `dot_matrix_scan`：切行前先把 `o_dot_row` 置全 1（低有效 → 全灭）。

### 6.8 音效（提高要求 1）

| `i_sound_sel` | 场合 | 频率 | 时长 |
|---|---|---|---|
| 0 | 静音 | — | — |
| 1 | 按键 | 2 kHz | 30 ms |
| 2 | 移动 | 1 kHz | 20 ms |
| 3 | 锁定 | 500 Hz | 80 ms |
| 4 | 预览倒计时每秒 | 1 kHz | 50 ms |
| 5 | 拼图倒计时最后 5 秒 | 4 kHz | 60 ms |
| 6 | 成功 | 2kHz→4kHz 交替 | 400 ms |
| 7 | 失败 | 500Hz→250Hz 交替 | 600 ms |

由 `tick_8k` 计数分频产生，占空比 50%。

---

## 7. 数据流与控制流小结

```
        ┌──────────── 控制流（键 → 状态） ────────────┐
键盘 ──▶ keypad_scan ──▶ game_fsm ──▶ puzzle_ctrl        │
                          │  ▲                            │
                          │  └── all_locked / solved ─────┘
                          │
                          ├──▶ disp_format ──┬─▶ seg_scan ────────▶ 数码管
                          │                  └─▶ dot_matrix_scan ▶ 点阵
                          ├──▶ pattern_rom ──┬─▶ puzzle_ctrl（target_mask）
                          │                  └─▶ disp_format（pattern_mask · 预览/胜负图案）
                          ├──▶ piece_rom ───────▶ puzzle_ctrl（rel_mask / 高宽 / 块数）
                          └──▶ rng_lfsr ◀── seed · seed_load（开局装一次）
                                        ◀── rnd_step ── puzzle_ctrl（每次落位尝试推进一步）
                                        ──── rnd ───▶ puzzle_ctrl

        ┌──────────── 时间流（节拍 → 一切） ────────────┐
clk ──▶ clk_gen ──┬── tick_1k ──▶ keypad_scan
                  ├── tick_8k ──▶ buzzer_ctrl / seg_scan / dot_matrix_scan
                  ├── tick_100 ─▶ game_fsm（方向键连发 + ÷25÷2 → o_blink 2Hz 方波）
                  ├── tick_2hz ─▶ game_fsm（自检计时）
                  └── tick_1hz ─▶ game_fsm（倒计时）
```

> ⚠️ **`o_blink` 是由 `tick_100` 分频出来的方波，不是把 `tick_2hz` 当方波送出去。**
> `tick_*` 全是**单周期使能**（一拍宽），而闪烁要的是**电平**。

**唯一的反馈回路**：`puzzle_ctrl` 的 `o_all_locked` / `o_solved` 回到 `game_fsm`，
驱动 `S_PLAYING → S_WIN / S_FAIL` 的转移。这是"空间"向"时间"汇报结果的唯一通道。

---

## 8. 上板前的硬件自检方案（`board_test_top`）

### 8.1 为什么需要它

本设计的正确性依赖 **8 个无法从文档推断、只能实测的硬件事实**：

| # | 待确认事实 | 若判断错误的后果 |
|---|---|---|
| 1 | 板载时钟档位 = 7（50MHz） | 所有倒计时、扫描频率全部错 |
| 2 | 键盘 ROW3 在最上排 | 方向键上下颠倒 |
| 3 | 键盘按下读入为 **1** | 按键完全无响应。⚠️ **课件 p55 的示例表是相反的低有效约定**（见 `docs/04` §3.3） |
| 4 | 点阵行**低有效** | 点阵整片点亮或全灭 |
| 5 | COLR/COLG **高有效** | 颜色红绿颠倒 |
| 6 | 数码管位选**低有效** | 数码管全灭或多位同时亮 |
| 7 | COLR0 对应**最左列** | 点阵图案左右镜像 |
| 8 | **BTN0 按下读入为 1**（高有效） | 课件 p45（一）强制的**按键复位失效**，运行中再也回不到初始状态 |

### 8.2 自检工程做什么

`board_test_top` 是一个**独立于游戏逻辑的最小顶层**，逐个点亮、逐个读键：

| 阶段 | 现象 | 验证的是 |
|---|---|---|
| 1 | 全部 LED 以 1Hz 闪烁 | 时钟有、档位对（用 1Hz 数秒即可粗判） |
| 2 | 点阵逐行点亮（上→下） | 行序 + 行极性 |
| 3 | 点阵逐列点亮红色（左→右） | 列序 + COLR 极性 + **bit0 是否最左** |
| 4 | 点阵逐列点亮绿色（左→右） | COLG 极性 |
| 5 | 数码管 DISP0→DISP7 逐个显示"8" | 位选顺序 + 极性 |
| 6 | 数码管依次显示 `1 2 3 4 5 6 7 8` 的段码 | 段码顺序（AA..AP） |
| 7 | 按任意键，对应 LED 点亮（键号 = 亮灯序号） | 键盘行列顺序 + 极性 |
| 8 | 蜂鸣器发出 1kHz | 蜂鸣器可用 |
| 9 | **按住 BTN0：16 个 LED 全亮；松开后自检序列从阶段 1 重新开始** | BTN0 连通性 + 极性 ＋ **按键复位通路（含 2 级同步与 20ms 消抖）** |

> **阶段 9 是"按键复位"这一条要求唯一能被实测的地方** ——
> 功能仿真只能证明逻辑对，证明不了"这只按键真的接在 `i_btn_rst` 上"。
> 按住时全亮（走 `btn` 的**直读通路**，组合越权于复位之上）＋
> 松开后自检重启（走 `clk_gen` 的**复位通路**），
> **一次把"线接对了"和"复位生效了"两件事都验掉**。

> **阶段推进方式**（2026-09-18 审查补，原先未定义）：上电复位后**自动轮播**——
> 每阶段停留约 2 秒（用 `tick_2hz` 计数），`ld[15:8]` 显示当前阶段号（1~9），
> 阶段 7（键盘）停留**无限期**，直到按过全部 16 键或重新上电；
> 阶段 9 播完回到阶段 1 循环。**不依赖任何按键推进**（阶段 7 之前键盘还没验证过，不能拿它当输入）。
>
> ⚠️ **阶段 9 是唯一会"打断"轮播的阶段**：按住 BTN0 会真的把整个自检复位，
> 松开后从阶段 1 重来 —— **这正是它要验的东西**，不是缺陷。

### 8.3 使用方式

```bash
# 编译自检顶层（不改 .qsf，仅本次生效）
quartus_sh -t scripts/build.tcl --top board_test_top
# 烧录后按上表逐条确认，把结果记入 docs/05-硬件调试记录.md
```

> **这是本项目"板子只能在实验室用"这一约束下的关键风险控制手段**：
> 把 8 项硬件不确定性**集中到一次实验室行程里一次性确认**，
> 避免"写完全部代码才发现极性错"的最坏情况。

---

## 9. 资源预估（EPM1270T144C5）

| 资源 | 总量 | 预估占用 | 说明 |
|---|---|---|---|
| 逻辑单元 (LE) | 1270 | ~450 | 主体是掩码加法器/比较器与两个扫描状态机 |
| 用户 I/O | 116 | **52**（`puzzle_top`）／**68**（含 LED，见 `docs/04` §2） | 点阵 24 + 数码管 16 + 键盘 8 + `clk`/`sw7`/**`btn`**/`buzz` 4 = **52**；<br>⚠️ 另外 16 个 LED（`ld[15:0]`）**只属于 `board_test_top`**，两个顶层不会同时存在 |
| 全局时钟 | 4 | 1 | 只用 1 个 |

**主要面积消耗**：
1. 64 位掩码的并集与比较（4 个 64 位或 + 1 个 64 位比较器）；
2. 4 块零片的 `place()` 平移逻辑（组合，可被综合器部分共享）；
3. 两个 8 路扫描状态机。

> 实际资源占用以编译报告为准，编译后截图记入 `docs/04-引脚分配表.md` 与实验报告评分项④。

---

## 10. 本文档与代码的一致性约定

> 实验报告要求"总体框图中的模块要求与实现电路（代码）一致"，且老师会**随机抽一个模块提问**。

1. **DWG-02（§2.2 第二层）一个方框 = 一个 `.vhd` 文件 = 一个实体**，文件名与实体名完全相同；
   **DWG-01（§2.1 第一层）一个方框 = 一个功能子系统**，框内列出的实体名必须与 §3.1 一致，
   且所有子系统所含实体之和 = 全部 11 个实体（不重不漏）；
2. **框图改了必须先改本文档**，不允许代码与图不一致；两层框图要一起改；
3. `puzzle_top.vhd` 的例化顺序与 §2.2 框图从上到下、从左到右一致；
4. 每个模块文件头部注释必须写明：**职责一句话 + 所属功能子系统 + 对应需求编号**
   （例：`-- 所属子系统：S6 显示子系统（DWG-01）`）；
5. ⭐ **顶层文件（`puzzle_top.vhd` / `board_test_top.vhd`）一律用 `component` 声明 + `port map` 例化**，
   不用 `entity work.xxx` 直接例化。写法与**五条**要求见 `CLAUDE.md` §10.3，
   代码骨架见 `docs/02` §14.1；
6. ⭐ **§5 的每一个输入端口，都必须在 §2.1 的连线表里找得到驱动源**（见 §10.2）——
   不允许出现"有输入端口、但谁也没接"的情况。

### 10.1 为什么顶层必须用 `component` 语法

本节第 5 条不是风格偏好，它直接服务于"**框图与代码一致**"这个验收要求：

| # | 理由 |
|---|---|
| 1 | **一个 `component` 声明块 = DWG-02 的一个方框**。顶层文件里数得出的声明块数，就是图上数得出的框数 |
| 2 | **端口名与位宽在顶层文件内全部可见**。老师抽问某个模块时，顶层一个文件就能把该模块的所有连线说清楚，不用翻 11 个子文件 |
| 3 | **`port map` 的具名关联（`名字 => 信号`）本身就是一张连线表**，与 §2.1 的连线清单表（3 条系统级 + 13 条子系统间）可逐条对照 |
| 4 | 直接例化（`entity work.xxx`）在文件里**没有对应的声明块**，图上"框"与代码"块"的对应关系弱一档 |

> ⚠️ **代价要如实认**：端口列表**写两遍**（子模块实体一遍、顶层 `component` 声明一遍），
> **改了子模块端口就必须同步改顶层的 `component` 声明**。
> 不一致时的报错不一定显眼 —— **`port map` 里漏写一个端口，被漏掉的那根线会静默悬空**，
> 综合器不报错、直接照默认值处理。
>
> **防法**：`component` 声明的端口**逐条对照 §5 的接口定义表**（唯一真值源）。
> 这与 `CLAUDE.md` §7「改了模块接口/端口 → 同步更新」是同一条规则，只是多了一个落点。

### 10.2 输入端口必须有驱动源 ⭐

**规则**：§5 里的每一个 **`i_*` 端口**，都要能在 §2.1 的连线表里找到**发出它的那个输出端口**；
每一个 **`o_*` 端口**，都要有**收它的地方**（或者是顶层要引出的器件信号）。

**为什么单列一条**：`port map` 里漏写一根线**不会报错**，综合器按默认值处理。
若漏的是一根**输入**，那根线就是悬空的 —— 模块照常综合、仿真网表照常生成、
**行为却是错的**。这类缺陷在阶段 4 集成时才会以"某个功能莫名不动"的形式浮出水面，
而那时已经写了 11 个模块，排查成本极高。

**核对动作**（**不是读一遍，是数一遍**）：

```
把 §5 里所有 i_* 端口抄成一列  →  逐个在 §2.1 连线表里找出它的驱动源（表里的信号名或端口名）
                            →  找不到的，就是缺一条连线
                            →  同一动作反过来做一遍：每个 o_* 端口找出它的去处
```

> ✅ **这个动作已脚本化**（2026-09-18）：`scripts/check_ports.py` 解析本文档 §5 的
> 12 张接口定义表，做一次集合相减。**当前结果**：11 个实体、57 个输入端口、44 个输出端口，
> **全部有驱动源 / 接收者**；`--selftest` 注入两类假端口均检出（正例零误报）。
> **阶段 4 写 `puzzle_top` 之前、以及每次改完接口之后，跑一遍。**
> 理由与 ERR-0010 相同：**叙述（"要检查端口"）会被读过就忘，能跑的动作不会。**

**本次核对结果（2026-09-17，逐条数过）**：查出 **5 处缺口**，
全部是"有输入端口、无人驱动"或"有输出端口、无人接收"：

| # | 缺口 | 修法 |
|---|---|---|
| 1 | `puzzle_ctrl` 没有 `i_target_mask`，`o_solved` 却要拿它做比较 | 补端口，驱动源为 `pattern_rom.o_mask` |
| 2 | `puzzle_ctrl` 没有零片形状输入（`place()` 用到的 `PIECE_MASK(i)` 等 5 个符号全项目未定义） | 补 `i_rel_mask` / `i_height` / `i_width`，驱动源为 `piece_rom` |
| 3 | `buzzer_ctrl.i_trigger` 无人驱动 → **蜂鸣器永远不响** | `game_fsm` 补 `o_sound_trig` |
| 4 | `piece_rom.i_level` 无人驱动（图上没有这条线） | 并入连线表第 3 条：`game_fsm.o_level` → `pattern_sel · level · seed`（⚠️ 2026-09-18 该端口已改为 `i_pattern_sel`，由 `o_pattern_sel` 驱动，见 §5.6） |
| 5 | `puzzle_ctrl` 的 `i_sel` / `i_conf` / `i_dir` 无人驱动 | 并入连线表第 2 条：`game_fsm.o_sel_out` / `o_conf_out` / `o_dir_out` |

> **这 5 处的共同点**：**都不是"写错了"，而是"没人写"**。
> 端口表是按模块逐个写的，每个模块自己看都完整；
> 只有**把 12 张表摆在一起、从"线"这一侧数一遍**，缺口才显形。
> **这正是本节作为一条固定动作存在的理由。**

**下一步**：`docs/02-模块详细设计.md` —— 逐个模块给出内部结构、状态机与算法细节。
