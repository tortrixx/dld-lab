# 05 · 硬件调试记录（实验室上板）

> **这份文件只记"实测"**：期望现象不在这里重复抄写 —— 它们分别在
> `docs/01` §8.2（自检 9 阶段）与 `docs/04` §7.1（引脚相关 11 条检查）里，
> 本文件**只填实测栏**，避免同一份清单出现两个副本。
>
> **规则**（与 `CLAUDE.md` §0 收工清单一致）：
> 1. 一次实验室行程 = 一个小节（`## 第 N 次上板 · 日期`）。
> 2. 每项填 **✓ / ✗** ＋ **实测现象的原话**（写"逐行点亮但方向反了"，
>    不要写"不正常"—— 后面写 `ERRORS.md` 时要靠它复现）。
> 3. 任何 **✗** → 当场记 `ERRORS.md`（九字段），把编号回填到本表"备注"列。
> 4. 确认下来的硬件事实 → 回填相关文档（引脚极性回填 `docs/04` §3，
>    资源/时序数字回填 `docs/04` §5.2 与 `docs/01` §9）。

---

## 第 1 次上板 · 2026-09-24（首次真正上电：跑硬件自检顶层）

### 1.1 现场信息

| 项 | 实填 |
|---|---|
| 日期 / 时段 | 2026-09-24 |
| 实验室 / 工位 | （填） |
| 板号（同班只有 3 块） | （填） |
| 下载线 | USB-Blaster [USB-0]，Mode = **JTAG** |
| 烧录文件 | `quartus/puzzle.pof`（**Checksum `005DC9F1`**，UserCode `FFFFFFFF`） |
| 识别到的器件 | **EPM1270T144** |
| 当前顶层 | **`board_test_top`**（硬件自检；`439 / 1270 LE`，68 引脚） |
| 板载时钟档位 | （目视确认后填：应为 **7 = 50MHz**） |

> 勾选项按 `docs/06` §4.2 第 4 步那张表：**只留 `Program/Configure` + `Verify`**。

### 1.2 烧录结果

| 项 | 实填 |
|---|---|
| 进度 / 结论 | （应到 100% 并显示 **Successful**） |
| 烧录后 **SW7 拨到 "1"** | （填） |

### 1.3 自检 9 阶段（期望现象见 `docs/01` §8.2；`ld[15:8]` 显示当前阶段号）

| 阶段 | 验的是 | 实测（✓/✗ ＋ 现象原话） | 备注 |
|---|---|---|---|
| 1 | 全部 LED 1Hz 闪烁（时钟有、档位对） | | |
| 2 | 点阵逐行点亮 上→下（行序 + 行极性） | | |
| 3 | 点阵逐列**红** 左→右（列序 + COLR 极性 + bit0 是否最左） | | |
| 4 | 点阵逐列**绿** 左→右（COLG 极性） | | |
| 5 | 数码管 DISP0→DISP7 逐个显示 "8"（位选顺序 + 极性） | | |
| 6 | 数码管依次显示 `1 2 3 4 5 6 7 8` 段码（AA..AP） | | |
| 7 | 按任意键 → 对应 LED 亮（键号 = 亮灯序号；键盘行列 + 极性） | | |
| 8 | 蜂鸣器 **500Hz ↔ 2kHz 各 1 秒**；**进入本阶段之前应完全无声** | | |
| 9 | 按住 **BTN0** → 16 个 LED 全亮；松开 → 自检从阶段 1 重来 | | |

### 1.4 阶段之外必须目视的 3 项

| # | 检查项 | 依据 | 实测（✓/✗ ＋ 现象原话） |
|---|---|---|---|
| A | **数码管亮度可读吗**（两相消隐使每位占空恒为 1/16，**仿真查不出、只能目视**） | `docs/01` §8.2 补充项 | |
| B | **SW7 门控**：拨 0 → 数码管/点阵/LED 全灭、蜂鸣器无声；拨回 1 → 自检重启 | `docs/04` §7.1 第 11 条 | |
| C | **时钟档位 = 7（50MHz）**：若阶段 1 的闪烁明显不是 1Hz，先怀疑档位 | `docs/04` §7.1 第 1 条 | |

### 1.5 结论与后续动作

- 硬件自检总体结论：（填 —— 全绿 / 有哪几项不符）
- 异常清单（→ `ERRORS.md` 编号）：（填，例如 `ERR-0030`）
- ⬜ **顺手核一眼点阵/数码管的驱动与限流元件**（板手册未入库，`docs/04` §4.1 的待办），
  把型号抄回来 —— 它决定"数码管偏暗"时能不能靠减小 `R` 或改扫描时序解决；
  若届时仍有疑问，**把 `CLAUDE.md` §11.4 该行状态由 ✅ 降为 ⚠️**（`docs/04` §4.1 已约定）
- ⬜ 顺手录一段**工作视频**（课件 p65 的提交物，出了实验室补不出来）
- ⬜ 回填：环境光/温度等无关项不记；**点阵镜像/颠倒或数码管偏暗**的处置见 `docs/06` §6 与 `docs/02` §5.6
- ⬜ 自检全绿后：切顶层 `puzzle_top`（`python scripts/set_top.py puzzle_top`）→ **必须重新编译** → 烧录 → 按 `docs/02` §14.3 的 13 个场景验收

---

<!--
=====================================================================
 下一次上板：复制下面这段，改编号与日期
=====================================================================

## 第 N 次上板 · YYYY-MM-DD（本次目的：一句话）

### N.1 现场信息
（照 1.1 的表改：日期 / 板号 / 顶层 / 校验和 / 时钟档位）

### N.2 结果
| 检查项 | 期望依据 | 实测 | 备注 |
|---|---|---|---|
| | | | |

### N.3 结论与后续动作

-->
