# buzzer_ctrl · 第 04 轮仿真记录

- **时间**：2026-09-24 18:04:32
- **结论**：✅ 全部通过（14 / 14 通过）
- **激励时长**：435000.0 ns
- **RTL 补丁**：无（未改任何 RTL）
- **波形图**：`docs/图/SIM-buzzer_ctrl.svg`

| # | 断言 | 结果 | 实测 / 说明 |
|---|---|---|---|
| 1 | ① 中间信号全部有波形（r_play、r_alt、r_buzz、r_half） | ✅ | 全部有 TRANSITION_LIST |
| 2 | ② 静音电平：未触发 / 每种音效播完后 o_buzz 恒 '0'（18 个采样点） | ✅ | 全部为 '0' |
| 3 | ③ i_sound_sel=0（静音档）：给触发脉冲后 o_buzz 全程无翻转（恒 '0'） | ✅ | 触发于 399670 ns；其后 20 拍内翻转次数 = 0（应 0） |
| 4 | ④ 单音 1~5 方波频率：实测半周期与 §13.3 查表 N 一致（±1 拍内） | ✅ | sel1: 半周期实测 2 拍（目标 N=2）→ f≈2000 Hz; sel2: 半周期实测 4 拍（目标 N=4）→ f≈1000 Hz; sel3: 半周期实测 8 拍（目标 N=8）→ f≈500 Hz; sel4: 半周期实测 4 拍（目标 N=4）→ f≈1000 Hz; sel5: 半周期实测 1 拍（目标 N=1）→ f≈4000 Hz |
| 5 | ⭐ ⑤ ERR-0016 回归（off-by-one）：查表存 N-1、计数器数到 0 共 N 拍 → 实测半周期必须**恰为 N 拍**（当年装载 N 得 N+1 拍、每种音效频率全错） | ✅ | sel1: 查表 N-1=1，实测相邻翻转间隔 = 2 拍 = N（✓ 恰 N 拍，off-by-one 未复发）; sel2: 查表 N-1=3，实测相邻翻转间隔 = 4 拍 = N（✓ 恰 N 拍，off-by-one 未复发）; sel3: 查表 N-1=7，实测相邻翻转间隔 = 8 拍 = N（✓ 恰 N 拍，off-by-one 未复发）; sel4: 查表 N-1=3，实测相邻翻转间隔 = 4 拍 = N（✓ 恰 N 拍，off-by-one 未复发）; sel5: 查表 N-1=0，实测相邻翻转间隔 = 1 拍 = N（✓ 恰 N 拍，off-by-one 未复发） |
| 6 | ⑥ 交替音 6/7 的段结构：4 段窗口翻转数 = [400,800,400,800]（sel6，N=2↔1）/ [150,75,150,75]（sel7，N=8↔16） | ✅ | sel6: 4 段（每段 800 拍）翻转数 实测 [400, 800, 400, 800] / 目标 [400, 800, 400, 800]<br>sel7: 4 段（每段 1200 拍）翻转数 实测 [150, 75, 150, 75] / 目标 [150, 75, 150, 75] |
| 7 | ⑦ 每种音效的总时长（拍）与 §13.3 查表一致，播完自动静音 | ✅ | sel0: 实测 0 拍 / 目标 0 拍; sel1: 实测 240 拍 / 目标 240 拍; sel2: 实测 160 拍 / 目标 160 拍; sel3: 实测 640 拍 / 目标 640 拍; sel4: 实测 400 拍 / 目标 400 拍; sel5: 实测 480 拍 / 目标 480 拍; sel6: 实测 3200 拍 / 目标 3200 拍; sel7: 实测 4800 拍 / 目标 4800 拍 |
| 8 | ⑧ 8 种音效（频率, 时长）两两不同（没有两种完全相同） | ✅ | sel0: 静音 Hz, 0 拍; sel1: 2000 Hz, 240 拍; sel2: 1000 Hz, 160 拍; sel3: 500 Hz, 640 拍; sel4: 1000 Hz, 400 拍; sel5: 4000 Hz, 480 拍; sel6: 2000/4000 Hz, 3200 拍; sel7: 500/250 Hz, 4800 拍 |
| 9 | ⑨ i_trigger 方向一：单个 1 拍脉冲 → 恰好触发一次完整播放（sel2：20ms 内 40 次翻转、时长 160 拍 —— §13.6-1/-2） | ✅ | 翻转次数 = 40（应 40 = 20ms/0.5ms）；末次翻转于 +160 拍（应 160） |
| 10 | ⑩ i_trigger 方向二：按住不放（连续 300 拍）→ 期间 o_buzz 恒 '0'，不重复触发（实现是「电平装载、非边沿检测」，见 docs/02 §13.5） | ✅ | 按住 [412070, 424070] ns 内翻转次数 = 0（0 = 一直静音） |
| 11 | ⑪ 按住释放后：立刻开始一次完整播放（sel1：240 拍内翻转 120 次） | ✅ | 释放于 424070 ns；其后翻转次数 = 120（应 120 = DUR/N = 240/2） |
| 12 | ⑫ §13.6-4 连续两个触发脉冲：第二个中断第一个、重新开始计时（sel3 播到第 100 拍被 sel2 打断 → 自第二个脉冲起重新计 160 拍） | ✅ | 第二个脉冲于 405270 ns；其后翻转 40 次（应 40）；末次翻转 +160 拍（应 160） |
| 13 | ⑬ i_sound_sel 覆盖与越界：0..7 全部驱动过；复位后 o_buzz 全程为确定值（0/1，无 X/Z）。注：端口是 3 位、查表恰 8 项（0..7）→ 不存在可表示的越界值 | ✅ | 驱动过的 sel = [0, 1, 2, 3, 4, 5, 6, 7]；复位后 o_buzz 的非 0/1 电平 = 无 |
| 14 | ⑭ 触发那一拍把 i_sound_sel 锁存到 r_play（播放期间上游变化不影响本次播放） | ✅ | 各音效播放中 r_play = sel1→1, sel2→2, sel3→3, sel4→4, sel5→5, sel6→6, sel7→7 |
