# AI_LOG — AI 工具使用记录

> **用途**：记录本项目中每一次使用 AI 工具的情况。
>
> **为什么认真写**：实验报告评分项⑦"AI 工具使用情况（包括使用的工具、提示词）"**占 10 分**。
> 老师要看的是**提示词写得怎么样**、**你对 AI 产出的判断和修正能力**，不是"用了 AI"这件事本身。
> 所以：**提示词要写具体原话**，**人工修正部分必须如实记录**——AI 给的东西哪些是错的、你怎么发现的、怎么改的，这才是得分点。

---

## 记录规范

| 字段 | 说明 |
|---|---|
| **工具** | 使用的 AI 工具与模型 |
| **任务** | 这次让 AI 做什么 |
| **关键提示词** | ⭐ 实际使用的提示词（可精简，但要能反映真实意图和约束条件） |
| **AI 产出** | AI 给出了什么 |
| **人工修正** | ⭐ 你做了哪些核实、推翻、修改？为什么？ |
| **结论** | 采纳 / 部分采纳 / 弃用 |

---

## 使用记录

### 2026-09-17 · 第 1 次会话 · 项目启动与需求解码

**工具**：Claude Code（Claude Fable 5）

**任务 1：解码题目4 的参考图案**

- **关键提示词**：题目 PDF 里的图4-1、图4-2、图4-3 是位图插图，文本提取拿不到图案。要求从 PDF 中渲染页面并将点阵图案**逐格还原成坐标**，而不是"看一下大概是什么形状"。
- **AI 产出**：把 PDF 页面渲染成高分辨率 PNG，用 OpenCV 做连通域检测定位每一个圆点，反推点阵网格坐标，最终得到：
  - 图4-1 = 第2~5行 × 第2~4列的实心 4×3 矩形（12 格）
  - 三块零片 = 6格 `XXX/XX./X..`、3格 `.X/XX`、3格 `XXX`
- **人工修正**：AI 第一次给的行列索引偏移了一行（把图边框也算成一行网格），要求它重新用完整圆点检测标定网格原点后才对。
- **结论**：采纳

**任务 2：验证零片是否唯一铺法**

- **关键提示词**："请程序化穷举验证这三块零片在 4×3 矩形内共有几种合法铺法，并打印每种铺法的格子归属。这决定了成功判定该怎么写。"
- **AI 产出**：穷举得到 **恰好 2 种**合法铺法（零片1 占矩形上三行或下三行）。
- **人工修正**：无。此结论直接改变了设计——**成功判定必须基于"三块并集 == 目标掩码"，而不是"每块回到各自初始位置"**，否则会把其中一种正确铺法误判为失败。
- **结论**：采纳（关键设计依据）

**任务 3：从开发板手册核实硬件参数**

- **关键提示词**："从开发板手册中提取所有引脚分配、有效电平、位序关系；特别确认数码管 DISP0~DISP7 哪个在最左、键盘实物丝印的行列顺序。"
- **AI 产出**：完整引脚表；确认 **DISP7 最左、DISP0 最右**；确认键盘 **ROW3 在最上排**；从开发板实物照片中读出时钟档位丝印 `7:50MHz`。
- **人工修正**：AI 起初把数码管位序按常规假设成 DISP0 最左，要求它去看手册附图9 的实物丝印照片后才纠正为 DISP7 最左。
- **结论**：采纳

**任务 4：制定实施计划与文档体系**

- **关键提示词**：给出实现范围、仿真条件（无 ModelSim/GHDL）、硬件条件（板子只在实验室可用）、9/24 截止期限等约束，要求设计模块划分与文档体系，并**让文档体系直接对应报告评分表的 8 个评分项**。
- **AI 产出**：10 模块划分方案；文档体系与评分项映射表；分阶段实施计划；`CLAUDE.md` + `PROGRESS.md` 跨会话续接机制。
- **人工修正**：AI 最初给出的键盘映射把方向键排成一排，人工改为**十字居中布局**（现场演示更不易按错）；关卡号显示位置按基本要求3 统一到 DISP0。
- **结论**：采纳

**本会话经验**：PDF 里的**图形信息**不能靠文字提取，必须渲染成图后程序化分析；而**中文 PDF 提取文本必须加 `-enc UTF-8`**，否则中文全部丢失（已记入 `CLAUDE.md` §5.4）。

---

### 2026-09-17 · 第 2 次会话 · 打通自动仿真链路

**工具**：Claude Code（Claude Fable 5）

**任务 1：把仿真流程做成全自动的**

- **关键提示词**："本机没有 ModelSim 也没有 GHDL，只能用 Quartus II 9.1 内置仿真器。
  要求仿真必须能**命令行跑、结果能自动判定**，不能靠人工看波形窗口截图。
  请先做个最小实验确认这条路走得通，不要假设。"
- **AI 产出**：写了最小工程做探针实验，依次确认：
  - `quartus_sim --help` 有 `--mode=functional` / `--simulation_results_format` 等选项
  - `package require ::quartus::simulator` **能加载但命令列表为空** → 无 Tcl API
  - 整个 Quartus 安装目录里 **没有任何 `.vwf` 样例**，官方教程也不给格式
  - `.sim.rpt` 里波形部分是 `Waveform report data cannot be output to ASCII`
  - 最终发现 `--overwrite_waveform=on` 会把**仿真结果写回输入 `.vwf`**
- **人工修正**：⭐ **这是本次会话最值得记录的一条**。
  AI 在"逆向 `.vwf` 格式"这条路上钻了很久——去 `db_vdb.dll` 里抽字符串、
  找 `vdb_vec_parser.ypp`、试图拼出语法。我打断了它，让它**先在本机找有没有
  别人已经跑通的 `.vwf`**。结果用户桌面 `Desktop\VHDL\` 下有 9 个此前做过的
  Quartus 工程，每个都带 `.vwf`。**读一个文件就把格式完全确定了**，
  前面啃二进制的时间基本是白花的。
  → 教训已写进 `ERRORS.md` ERR-0002 的"预防措施"：
  **工具链卡住时，先去本机找已经跑通过的同类产物，比啃文档、逆向二进制快得多。**
- **结论**：采纳

**任务 2：验证自动判定真的可靠**

- **关键提示词**："生成的 `.vwf` 喂给仿真器必须真的能跑；仿真结果必须能被解析出来，
  并且要和参考模型比对。请做一个端到端的往返实验，把结果贴出来。"
- **AI 产出**：`scripts/vwf.py`（读写库）+ 往返实验：生成激励 → 仿真 → 回读结果 →
  与参考模型逐点比对 100 个采样点，全部通过。
- **人工修正**：第一版往返实验里，AI 用**手算**的时刻表做期望值，报出 3 项"失败"。
  经核查**是 AI 自己把计数器回绕的时刻算错了**（把 `q` 回绕点算成 110+20×16），
  仿真器给出的值才是对的。改为"**参考模型（记分板）**"——把期望值写成时间的函数、
  全时段逐点比对——之后全部通过。
  → 这条也进了代码注释：**手算时间点极易出错，一律用参考模型。**
- **结论**：采纳（并修正）

**任务 3：交叉验证引脚表**

- **关键提示词**："用桌面上那些旧工程交叉验证引脚表。但注意：先判断旧工程的值不值得信，
  不要直接抄。"
- **AI 产出**：逐条比对后报告：点阵行、绿列、段码、位选、LED、时钟**六组中五组完全一致**；
  **只有红列一组相反**（旧工程 `col_r[0]→PIN_11`，本项目 `dot_colr(0)→PIN_22`）。
- **人工修正**：AI 第一反应是"以旧工程为准"（因为它"跑通过"）。要求它回到开发板手册
  原文核对，手册明确写着
  `COLR0~COLR7 依次使用 …… 22、21、16、15、14、13、12 和 11 脚`。
  → 结论改为：**引脚号只从手册拿；旧工程只能验证"用到了哪些引脚"，不能确定位序。**
  这个坑记入了 `ERRORS.md` ERR-0003。
- **结论**：采纳

**本会话经验**：
1. **先找现成的可工作样例**，再考虑逆向格式文档（省下大量时间）；
2. **不要用"它跑通过"推断"它的写法是规范写法"**——旧工程自洽不等于符合手册；
3. 验证脚本里**手算的期望值本身就是 bug 来源**，要用参考模型。

---

### 2026-09-17 · 第 3 次会话 · 完成初步设计方案（4 份设计文档 + 引脚检查脚本）

**任务 1：把 11 条要求逐条变成"可判定的验收标准"**

- **关键提示词**："读题目原文，把 11 条基本要求逐条拆开。每条都要给出：原文引用、
  我的解读、**验收判据**（怎么算做到了）、涉及哪些模块。遇到前后矛盾的地方不要
  自己圆过去，单独列出来。"
- **AI 产出**：`docs/00-需求分析.md`。11 条要求逐条拆解 + 需求追踪矩阵
  （要求 → 信号 → 模块 → 仿真 → 上板验收）+ **5 处需求冲突**单列。
- **人工修正**：其中要求 9「位置和形状与初始拼图一致」AI 一开始按字面理解成
  "每块零片回到它自己的初始位置"。要求它用 `scripts/verify_tiling.py` 反查——
  穷举结果是三块零片**恰好有 2 种合法铺法**，两种都拼出正确的 4×3 矩形。
  由于要求 5 规定初始位置**随机**，"回到初始位置"这个读法会**把一种正确解判为失败**。
  → 判定准则改为 **"零片掩码并集 == 目标图案掩码"**。
- **结论**：采纳（并修正）

**任务 2：设计架构，重点是把"时间"和"空间"切开**

- **关键提示词**："按行业标准做系统设计：总体框图、模块划分、接口定义。要求每个模块
  职责单一、能独立仿真。特别注意：拼图逻辑和倒计时逻辑如果写在一个模块里会很难测，
  想想怎么切。"
- **AI 产出**：`docs/01-系统设计.md` + `docs/02-模块详细设计.md`。11 个功能实体的划分与
  完整接口定义；确立三条原则（**时间/空间分离**、**单一数据源**、**输出级统一门控**）；
  单时钟域 + 单周期使能；零片掩码**不存储**而由锚点组合计算。
- **人工修正**：
  ① AI 初版把倒计时设计成 6 位二进制，在 `disp_format` 里做 `/10` 和 `mod 10` 转 BCD。
     指出去查综合代价——十进制除法在 CPLD 上要 30~60 个 LE。改为**倒计时器本身就是两位 BCD**，
     `disp_format` 退化成纯接线，零逻辑开销。
  ② AI 初版把关卡号显示位置写成了"待确认"。要求它回到 `docs/00` 的冲突记录，
     明确取 **DISP0** 并在两处文档保持一致。
- **结论**：采纳（并修正）

**任务 3：引脚表不能只写文档，要能被程序检查**

- **关键提示词**："引脚表写成 markdown 没用——抄错了文档也不会报错。写个脚本，
  把手册的权威表和 .qsf 钉在一起比。**但光有检查器不够：一个永远通过的检查器等于没有**，
  你得证明它能抓到错误。"
- **AI 产出**：`scripts/check_pins.py`。解析 `.qsf`，与内嵌的手册权威表逐条比对，
  另做引脚唯一性检查；支持 `--markdown` 直接生成文档表格。
- **人工修正**：AI 第一版只跑了正向检查就报告"✓ 全部一致"。要求它做**负向测试**——
  往 `.qsf` 里注入三类典型误配置（抄错单个引脚号 / 整组位序颠倒 / 漏掉一行），
  确认**三种都能被检出**。结果显示三类全部检出，其中两类还额外触发了引脚冲突告警。
  这段负向测试记录写进了 `docs/04` §6。
- **结论**：采纳（并补做负向测试）

**任务 4：把设计文档里的掩码"占位符"换成实测数据**

- **关键提示词**："`docs/02` 里那些 `"..."` 的掩码常量，用 verify_tiling.py 生成真实值填进去。
  不要手抄，容易错位。"
- **AI 产出**：用脚本的 `normalize` / `cells_to_mask` / `mask_to_vhdl` 生成了全部常量：
  3 个目标图案掩码（L1 / L2 上箭头 / L2B 下箭头）+ 7 个零片相对掩码，并核实格数守恒
  （第一关 6+3+3=12、第二关 6+6+4+2=18）。
- **人工修正**：AI 第一次调用 `mask_to_rows` 时按"返回字符串列表"用，实际返回的是别的结构，
  报 `TypeError`。改为直接从相对坐标集合算包围盒，一次通过。
- **结论**：采纳

**本会话经验**：
1. **模糊的需求描述要用程序去消歧**，而不是靠语义推断——"与初始拼图一致"的两种读法，
   穷举一跑就知道哪种站得住；
2. **一个永远通过的检查器等于没有检查器**——检查脚本必须配负向测试，
   否则它只是给人心理安慰；
3. **文档里的数据要么由脚本生成、要么由脚本校验**，手工抄的数字迟早会和源头脱节。

---

### 2026-09-17 · 第 4 次会话 · 模块化图表 + 资源利用率约定

**任务 1：把用户口述的零散要求归纳进 `CLAUDE.md`，并指出其中不对的地方**

- **关键提示词**："这个项目需要贯彻"模块化"的概念，自底向上的结构……在代码中，要时刻注意
  资源利用率，比如指数类型的比特数需要尽量小（`range 0 to 9`），提高资源利用率。
  对于按钮，按下是复位（1），弹起后是启动（0）……**将上面的要求整理归纳到 claude.md 中，
  如有有不对的地方直接告诉我。**"
- **AI 产出**：`CLAUDE.md` 新增 §4.6（独立按键 BTN0~BTN7）、§10.2（资源利用率，五小节）、
  §12（图表规范）、§13（过程记录要求）；§2 硬约束由 8 条扩充到 11 条
  （新增：`integer` 必须带 `range`、自底向上实现、每个模块必须能独立仿真）
- **人工修正** —— AI 按"如有不对直接告诉我"点名了三处：
  ① **"频率可以使用串行"不能推广到显示扫描**。8 路行扫描若低于 1kHz，
     每行刷新率只有 125Hz 以下，会肉眼可见地闪。这一条**必须有下限**，
     已写进 §10.2 末尾的"三条红线"。
  ② **`range 0 to 9` 的例子对，但不能照搬**。每个计数器要各自的 `range`：
     50MHz→1kHz 的预分频需要 `range 0 to 6249`（13 位）。
     照搬 `0 to 9` 会直接算错分频比。
  ③ **AI 自己写出来的规则过宽**。初稿写"不允许出现没有 `range` 的 `integer`
     信号或变量，循环变量一个都不例外"——但 VHDL-93 的 `for` 循环变量是**隐式声明**的，
     语法上不允许写 `range`；`constant` 在 elaboration 阶段被折叠，**不占任何资源**。
     规则已改为按位置分类的表格，并补上真正要防的情形：**循环上界不得来自信号**。
- **结论**：采纳（含上述三处修正）

**任务 2：用 HTML 画总体框图与状态转移图，供 draw.io 二次编辑**

- **关键提示词**："需要画出的方框图中每个模块是一个矩形框，还要有状态转移图，
  这些图表你可以使用 html 画出，然后我利用 draw.io 等工具进行完善后加入项目文档中。"
- **AI 产出**：`docs/图/系统图.html`。两张图都是**内联 SVG**（不是 canvas、不是位图），
  导入 draw.io 后每个矩形框、每条箭头都是**独立可编辑对象**。
  12 个方框按职责三色分区（第 1 版：当时尚未拆两层），配色取自本项目点阵自己的**红 / 绿 / 黄**三种点亮色。
  框图里画出主数据流 + 反馈路径（橙色虚线）+ 输出级门控区（灰色虚线，
  表示那是代码区而不是模块）；状态图覆盖 6 个状态与全部 9 条迁移边。
- **人工修正**：
  ① AI 用装饰性长横线做注释分隔（`<!-- ---------- 外部输入 ---------- -->`），
     而 **XML 注释里禁止出现 `--`**。HTML 能容忍这一点，但 draw.io 用严格 XML
     解析器读 SVG，会直接解析失败。已全部替换。**已记入 `ERRORS.md` ERR-0004。**
  ② 状态框与两条彩色迁移箭头原本用 `stroke="#..."` 属性着色，但
     **SVG 中 presentation attribute 的优先级低于任何 CSS 类规则**，
     会被 `.dg-wire` 类里的 `stroke` 覆盖掉 → 全部改为内联 `style`。
- **结论**：采纳（含上述两处修正）

**任务 3：改掉设计文档里与新增约束矛盾的地方**

- **关键提示词**：无 —— AI 在归纳资源利用率约定时，**自己发现** `docs/02` §3 写的
  "5 个计数器并行"（需 99 位）与自己刚写进 `CLAUDE.md` §10.2 ② 的串行级联要求直接冲突。
- **AI 产出**：`docs/02` §3.3~§3.5 由"5 个并行计数器"改为"5 级串行级联"，
  **99 位 → 33 位**；并补上一条便于换档位的性质：只有 `CNT_8K` 依赖 `CLK_HZ`，
  后四级是纯比例关系（8 / 10 / 50 / 100），板上时钟若变更**只需改一处**。
  另修正 `place()` 形参（参与 `r + ar` 加法，不加约束会综合出 32 位加法器）
  与 `popcount()` 内变量。
- **人工修正**：无 —— 这是 AI 主动发现并修正的自相矛盾。
- **结论**：采纳

**任务 4：渲染核对**

- **关键提示词**：无
- **AI 产出**：用 Chrome 无头模式截图自查，确认两张图配色分区可读、无越界裁剪。
  同时写脚本校验两个 SVG 的 XML 合法性、以及每个绘制形状是否都有显式填充。
- **人工修正**：Artifact 发布因本会话的鉴权方式被拒（`ANTHROPIC_AUTH_TOKEN` 优先于
  claude.ai 登录），改为**本地 HTML 文件**交付 —— 这对用户其实更合适，
  因为下游本来就是要导入 draw.io，本地文件是必需的那一份。
- **结论**：采纳

**本会话经验**：
1. **让 AI"把要求写进规范"时，必须同时要求它指出规范里的错误。** 用户原话是
   "如有有不对的地方直接告诉我"。这一条触发了两类修正：用户口述中的过宽表述
   （"速度没要求"、"range 0 to 9"），以及 **AI 自己刚写出来的过宽规则**。
   如果只让它"整理归纳"，这三处都会原样进规范，然后在写代码时变成 bug。
2. **"省资源"和"能工作"之间要有明确的红线。** 用户说"对速度没有太大要求"，
   但显示扫描频率是**有下限的**。把这类红线显式写进规范，比指望以后有人记得住可靠。
3. **生成的图表格式要为目标工具选。** 画成 SVG 而不是 canvas，是因为下游是 draw.io；
   而 SVG 的注释规则与 CSS 优先级又和 HTML 不同，这两处**都实际踩到了**，
   说明"看起来只是画个图"的任务同样需要验证，不能写完就交。
4. **AI 自己写下的规范，下一轮要拿它反查已有文档。** 本次的矛盾（并行 vs 级联分频器）
   不是用户发现的，也不是仿真的结果，而是"新规范 × 旧文档"对撞出来的。**这一步应当固化成习惯。**

---

### 2026-09-17 · 第 5 次会话 · 修正设计方法学表述 + 总体框图改为两层

**工具**：Claude Code（Claude Fable 5）

**任务 1：AI 把设计方法说反了，用户纠正**

- **关键提示词**（用户原话，一句话里包含两处纠正）：

  > "总体思路：**自底向上的依赖分层，这里有问题，是"自顶向下"设计分层！**
  > 对于你给出的系统图 html 中的模块太细了吧，进行改进。"

- **背景**：上一轮 AI 汇报进度时，把本项目的总体思路说成"**自底向上的依赖分层**"。
- **人工修正** —— 这一处纠正的价值不在于措辞，而在于**AI 混淆了两个不同层次的概念**：

  | | AI 原本的说法 | 用户纠正后 |
  |---|---|---|
  | **设计方法** | 自底向上 ✗ | **自顶向下**（系统 → 子系统 → 模块 → 实体） |
  | **实现顺序** | 自底向上 | 自底向上（这一半本来就对） |

  AI 之所以说反，是因为 `CLAUDE.md` §2 硬约束第 10 条当时**只写了实现顺序那一半**
  （"实现顺序自底向上"），**没写设计方法**。AI 拿半条规则当成了全部，
  于是把"依赖关系自底向上"（这是对的）说成了"分层方法自底向上"（这是错的）。

  **这不是笔误，是概念混淆**：依赖方向确实自底向上（上层依赖下层），
  但"分层"这个**动作**是从顶开始的——先有"这是个拼图游戏"，才有"要分输入/控制/显示"，
  最后才有 `seg_scan` 这个实体。若设计真自底向上，就会**由下位模块的便利决定系统结构**，
  而需求在顶上，没人能保证拼出来的东西满足需求。

- **AI 产出（修正后）**：
  ① `CLAUDE.md` §2 第 10 条改写为"**设计自顶向下，实现自底向上**（两件事，别混为一谈）"，
     并**新增 §2.1** 专门讲这两件事的区别、为什么必须分清、以及报告里该怎么分别表述；
  ② `docs/01-系统设计.md` §1.1 由"从需求到结构的推导"重写为
     "**自顶向下的三层分解**"（第 0 层需求 / 第 1 层 7 个功能子系统 / 第 2 层 11 个实体），
     并新增 §1.1.3"每一层都能追溯到需求"——**这是自顶向下区别于自底向上的本质特征**；
  ③ 原 §1.1 那张"三类职责"表**没有删掉**，而是重新定位为 §1.1.2
     "**切分时用的判据**"，并明确写出"这张表和上面那张表是两个层次，不要混"
     ——因为同一个子系统里可以有两种职责类别（如显示子系统里
     `seg_scan` 是驱动类、`disp_format` 是数据类）。
- **结论**：采纳

> **这条值得单独记一笔**：用户纠正的是**方法论表述**，看起来是"文字问题"，
> 但它直接决定了两件事——**报告"设计思路"一节怎么写**（评分项② 20 分），
> 以及**后续所有文档和图该按什么结构组织**。AI 在上一轮把"依赖方向"当成了"设计方法"，
> 属于**用一个正确的观察推出了一个错误的结论**。这类错误比事实错误更难自查，
> 因为每一句话单看都没问题，错的是它们之间的**归属关系**。

**任务 2：总体框图从"一张平铺 12 个方框"改为两层结构**

- **关键提示词**：同上——"对于你给出的系统图 html 中的模块太细了吧，进行改进。"
- **AI 产出**：`docs/图/系统图.html` 由 2 张图扩为 **3 张图**，
  按自顶向下的两层分解重新组织：

  | 图号 | 内容 | 粒度 |
  |---|---|---|
  | **DWG-01**（新） | 总体框图 | **7 个功能子系统**：时钟与节拍 / 键盘输入 / 游戏控制 / 拼图核心 / 图案与随机数据 / 显示子系统 / 音效输出。**每框内列出所含实体名** |
  | **DWG-02** | 模块框图（原图重命名） | 11 个实体 + 2 个顶层，即 DWG-01 的展开 |
  | **DWG-03** | 状态转移图（原图重命名） | 6 个状态 |

  DWG-01 的画法：`clk_gen` 置于顶部，向下引一条**节拍总线**再扇出给各子系统
  （把 §1.3 的"单时钟域 + 单周期使能"画成可见的结构）；
  数据流横向贯穿；**全图只有一条反馈**（`all_locked` / `solved` 回到 `game_fsm`），
  用橙色虚线单独标出。

- **人工修正**（AI 在实施中主动做的两个判断）：
  ① **没有把 RTL 实体合并成 7 个大模块**。用户说"模块太细"，但**"图太细"和"模块太细"
     是两回事**——图是给人看的，模块是给综合器和答辩用的。
     合并实体能少画几个框，但代价是：`docs/02` 里 §3.4 / §8.3 / §10.7 / §12.5 等
     十几处小节引用会全部失效；**更重要的是 `CLAUDE.md` §2 第 11 条要求"每个模块必须能独立仿真"**，
     合并后 `seg_scan` 和 `dot_matrix_scan` 就只能一起仿真了。
     而仿真波形正是**评分项③ 的 25 分**。所以裁决是：**改图，不改模块划分**。
  ② **两层图必须能对得上**。在 `docs/01` §3.1 模块清单里加了一列"**所属子系统**"，
     使 DWG-01 ↔ DWG-02 可以互相查；并规定"子系统所含实体之和 = 全部 11 个实体，不重不漏"，
     写进 `docs/01` §10 的一致性约定第 1 条。
     同时补了一条容易被追问的说明：**`puzzle_pkg` 是包不是实体，因此不出现在任何框里，
     但每个模块都依赖它**——先说明白，免得答辩时被问"框图上怎么找不到它"。

- **结论**：采纳

**任务 3：改动校验脚本后重做负向测试，反而查出脚本自己的缺陷**

- **关键提示词**：无 —— 属于流程要求（`CLAUDE.md` §13.1：图表交付前必须跑 `check_svg.py`）
- **AI 产出**：跑校验脚本时**报了两条假警报**，指向本次新加的两个连线样式类。
  追下去发现根因在脚本自己身上：它用两张**硬编码的类名清单**
  （`FILL_CLASSES` / `STROKE_ONLY_CLASSES`）判断"这个类有没有定义 `fill`"，
  新类不在清单里就被误判成"没有填充"。
- **人工修正**：**没有选择"把新类名补进清单"**——那样今天补上了，下次加新类还会再犯。
  改为**删掉两张清单**，新增 `parse_css_classes()` **直接从 `<style>` 块解析**
  "每个类声明了哪些属性"，判据从"查表"变成"读真值源"。
  顺带补了两个检查：`marker-end` 引用不存在的 `id`、同页多张 SVG 的 `id` 重名。
  **改动后重做负向测试**（注入 5 类故障全部检出），并**额外做了一次反例测试**——
  确认"不该报的没报"（否则很容易把校验器改成"什么都报"，看着灵敏、实际没用）。
  **已记入 `ERRORS.md` ERR-0005。**
- **结论**：采纳

**任务 4：渲染核对新图，发现格式校验查不出的图形缺陷**

- **关键提示词**：无 —— 属自查。上一轮把 DWG-01 画完后，
  **三个校验脚本全绿**，于是渲染成 PNG 用眼睛看。
- **AI 产出**：整图缩略看着没问题；**改成局部放大 3 倍后**发现两处缺陷：
  ① `sound_sel` 走线（`M530,307 H1230`）的 **y=307 落在方框的 240~328 区间内**，
     横穿了「拼图核心」与「显示子系统」两个方框；
  ② `pattern_sel` 标签（`x=578`）向右延伸约 73px，**压到「拼图核心」左边框（x=610）上**，
     还和 `控制` 箭头的箭头尖叠在一起，字被切成 `pat▸rn_sel`。
- **人工修正**：
  ① `sound_sel` 改走最下方的空走廊 `M500,328 V730 H1180 V558`。
     **没有复用已有的 y=690 走廊**——那已被 `state·level·cnt` 占用，
     两条线重合比穿框更难读；改后两条走廊上下嵌套、互不相交。
  ② `pattern_sel` 标签移到它自己那条线的水平段上方。
  ③ **给校验脚本补上第 ④ 类检查「走线是否横穿方框」**，
     并做负向测试（横穿/竖穿检出，**箭头贴边落框的正常画法不误报**）。
     **已记入 `ERRORS.md` ERR-0006。**
- **结论**：采纳

> **这一条最值得记的是"发现方式"**：三个校验脚本**全部通过**，
> 而图**确实是错的**。格式校验回答的是"文件合不合法"，不是"图画得对不对"——
> 本次的缺陷属后者，**只能靠看**。而且**整图缩略看不出来，是放大 3 倍才看清的**。
> 所以"缩略图看着没问题"不等于检查过了。

**本会话经验**：

1. **纠正"措辞"时，要先问这是笔误还是概念混淆。** 本次 AI 说的是"自底向上的依赖分层"，
   拆开看每个词都没错——依赖方向确实自底向上——错的是**把这个方向归属给了"设计分层"**。
   这类错误的修法不是改一句话，而是**把缺失的那半条规则补上**
   （`CLAUDE.md` 原来只写了实现顺序，没写设计方法），否则下次还会说反。
2. **反馈里的"太细了"，要先分清说的是图还是模块。** 用户说"模块太细"，
   但结合上下文（"对于**系统图 html 中**的模块"）说的是**图**。
   直接照字面去合并 RTL 实体，会砸掉独立仿真能力和十几处文档引用——
   **改图的成本远低于改模块划分，而两者的收益在这里是等价的。**
   拿不准时选成本低、可逆的那个。
3. **校验规则不要在旁边维护一份副本。** 本次的假警报（ERR-0005）与之前的引脚位序错误
   （ERR-0003）**是同一类**：把某份手工镜像当成了权威。ERR-0003 里的副本是旧工程的引脚表，
   这次是脚本的类名清单，**真值源都在别处**。写校验脚本时先问一句：
   **这份判据会不会随着被校验内容的正常演进而过期？**
4. **改校验脚本后必须重跑负向测试，且要同时有正例和反例。**
   只测"该报的报了"会把脚本越改越神经质；**"不该报的没报"同样是测试项**。
5. **"校验全绿"和"东西是对的"是两件事。** 本次三个脚本全过，图却横穿两个方框。
   格式校验只能回答"文件合不合法"，**回答不了"图画得对不对"**。
   要把这一条变成可执行的：**凡是画/改图，必须渲染出来放大看至少一遍**，
   而且要看**局部**——本次两处缺陷在整图缩略时都不明显。
   同时要清楚**校验器查不到什么**（本次之后：标签压框仍查不出，需要字体度量），
   免得把"脚本没报错"当成"已经检查过了"。

---

### 2026-09-17 · 第 6 次会话 · 用户手绘总体框图，AI 审阅后定稿

**工具**：Claude Code（Claude Fable 5）

**任务 1：用户自己画出总体框图，AI 审阅**

- **关键提示词**（用户原话，附一张飞书白板导出的 PNG）：

  > "**whiteboard_exported_image** 我用飞书画出了一个完善后的总体框图，你看看"

- **背景**：前五次会话的框图都是 AI 画的（ASCII + SVG）。本次**用户自己动手画了一张**
  ——在飞书白板上重排了 DWG-01 的布局。这是一次**角色反转**：
  之前的"人工修正"都是用户纠正 AI 的产出，这次是**用户先产出、AI 来审**。

- **AI 产出（审阅意见）**：图整体布局比 AI 原来那版更清楚（分层更扁、走线更短），
  但发现**四处必须澄清的问题**，按严重程度排列：

  | # | 问题 | 性质 |
  |---|---|---|
  | 1 | **漏画 `all_locked · solved` 反馈** | **功能性缺失** —— 缺了它解释不了 `S_PLAYING` 怎么走到 `S_WIN`/`S_FAIL` |
  | 2 | **漏画 `SW7` 与 `50MHz` 输入** | **需求覆盖缺失** —— SW7 是基本要求第 1 条，框图上找不到它 |
  | 3 | **「图案与随机数据」接了一条节拍输入** | **会诱导实现出错** —— 见下方专条 |
  | 4 | 框名写「显示系统」，与文档里的「显示子系统」不一致 | 一致性问题，两层框图会对不上号 |

  另有若干小项：两条未标注的连线、时钟画成椭圆但 `clk_gen` 本是内部模块、
  中英文信号名混排、缺图号与图例。

- **人工修正（用户的裁决）**：AI 列出四种处理方式让用户选，
  **用户选择"就定你这张图"**——即：
  **保留飞书稿的布局**，由 AI 把上述四处补/改到矢量图里，
  同时同步更新 `docs/01` §2.1 的文字描述。用户没有选择"重画"或"退回 AI 原版"。

- **结论**：采纳

> **第 3 处值得单独记**：它不是"画错了一条线"这么简单。
> `pattern_rom` / `piece_rom` 是**纯组合**的常量表；`rng_lfsr` 是**由 `puzzle_ctrl`
> 的 `rnd_step` 请求推进**的移位寄存器，接的是原始 `clk`，**不接任何一档节拍**。
> 图上画成"接节拍"，实现时就极易写成"每个 `tick` 采一次随机数"——
> 那样同一拍内的多次落位尝试会读到**同一个**随机数：所有零片落点相同 → 必然重叠 →
> 用尽尝试次数 → 每次都走回退布局。
> **游戏仍然能跑，只是永远不随机**——这种缺陷靠观察现象基本发现不了
> （见 `CLAUDE.md` §10.1）。**一张图上的多画一笔，代价可能是一个查不出来的 bug。**

**任务 2：按飞书布局重排 DWG-01 矢量图**

- **关键提示词**：同上 + 用户的选择"就定你这张图"
- **AI 产出**：`docs/图/系统图.html` 的 DWG-01 整块替换：
  - 采用飞书稿的**椭圆时钟 + 顶部节拍总线 + 横向数据流**布局；
  - 补回 `all_locked · solved` 反馈（棕虚线，走总线与方框之间的 y=395 走廊）；
  - 补回 `sw7` / `clk 50 MHz` 两个外部输入；
  - **删掉「图案与随机数据」的节拍分接**，并在原位置留 XML 注释说明"这里为什么没有线"；
  - 「显示系统」改为「**显示子系统**」，与 §1.1.1 的子系统命名对齐；
  - 所有连线加上**端口名标注**（`key_code · key_press` / `target_mask · rel_mask` / …），
    方便对照 §5 的接口定义表。
- **人工修正**：用户在本轮之前对**图的布局**已亲自做过判断（飞书稿），
  AI 只在其上做**正确性补全**，没有推翻布局重画。
- **结论**：采纳

**任务 3：新加的校验规则反过来误报（ERR-0007）**

- **关键提示词**：无 —— 属流程要求（改了校验脚本就要重跑）
- **AI 产出**：上一轮新加的"走线横穿方框"检查，一跑就把**六条合法的外部连线**
  全判成了穿框——因为它把 `puzzle_top` 的虚线边界框也当成了障碍物。
- **人工修正**：**没有选"跳过 `dg-region` 类"**（那又是加一张类名白名单，会重犯 ERR-0005），
  而是新增 `_fill_of()` **解析元素最终生效的 `fill` 值**，
  只把"有填充的 rect"当方框 —— **判据取自"决定语义的属性"，不是类名也不是尺寸**。
  并补了个反例测试：线穿 `fill:none` 的范围框**不应报警**。
  **已记入 `ERRORS.md` ERR-0007。**
- **结论**：采纳

**任务 4：渲染放大后发现三处标签压线（ERR-0008）**

- **关键提示词**：无 —— 属自查（`CLAUDE.md` §12 要求图表交付前渲染核对）
- **AI 产出**：校验**五类全绿**，但放大 2.5 倍看时发现三处文字被走线从中划开。
  追根因：其中两处的肇事竖线，正是任务 2 里那条**本就不该存在的节拍分接**。
- **人工修正**：
  ① **先删线、再挪标签**。三处重叠里有两处是那条多余的线造成的——
     **只挪标签治标，删线才治本**；而且删线在架构上本来就是对的。
  ② 第三处（`rnd_step` 标签 × `pattern_sel` 竖线）是纯排版失误，把标签移到竖线起点上方。
  **已记入 `ERRORS.md` ERR-0008。**
- **结论**：采纳

**本会话经验**：

1. **用户画图、AI 审图，是比"AI 画图、用户点头"更有效的分工。**
   本次用户交来的飞书稿**布局明显优于 AI 原版**（更扁、走线更短、信息密度合适），
   而 AI 补上的是**正确性**（漏画的反馈、漏画的输入、多画的节拍线）。
   两者恰好互补：**人对"清不清楚"敏感，AI 对"自不自洽"敏感。**
   所以拿到用户的图，第一反应不该是"重画一版"，而是**逐条核对拓扑与接口**。
2. **图上的每一笔都要能说出"它是干什么的"。** 本次那条多余的节拍分接，
   是 AI 为了"保持与手绘稿一致"而留下的——**为了像而留，不是因为有用来留**。
   代价是一条查不出来的 bug 的入口。**保真度不能凌驾于正确性之上。**
3. **排版冲突要先问"这条线该不该存在"，再问"标签该挪到哪"。**
   本次三处重叠两处源自同一条多余的线，删掉它比调三个坐标更省事、也更正确。
4. **"能查出错误"和"不误报正确的东西"必须分别验证。**
   本次 ERR-0007 是做负向测试时**查不出来**的——因为注入的故障确实被正确检出了，
   问题是**没注入故障的地方也被报了**。**光测正例，只会把校验器越改越神经质。**
   现在改成：**新检查写完，先拿"已知正确的现有文件"跑一遍**，再看要不要收紧。

---

### 2026-09-17 · 第 7 次会话 · 收尾准备与交接文档

**工具**：Claude Code（Claude Fable 5）

**任务 1：用户对飞书白板稿做第 2 次修订，AI 审阅并同步矢量图**

- **关键提示词**（用户原话，附第 2 版导出的 PNG）：

  > "**图片进行了修改**：[Image #3] 。整理文档，做一份小的收尾准备，
  > 写好交接文档方便下一个会话中agent能清楚理解目标。
  > 审查全部修改内容，确认后提交和推送至github。"

- **AI 产出（审阅意见）**：第 2 版共两处调整，**两处都是正确的**：
  1. **50MHz 并入时钟椭圆**（第 1 版里它是单独一条输入线）
  2. **反馈连线上补了中文说明** `all_locked · solved（反馈游戏是否结束）`
- **人工修正**：**AI 无修正**——这次用户的改动方向与 AI 在第 6 次会话提出的意见一致，
  且第 2 处（中文说明）比 AI 原来的写法**更好**：AI 只标了信号名，读者看不懂那是什么。
  **信号名是给实现看的，中文说明是给读者看的，两者都要** —— 这条已写进 `docs/01` §2.1.2。
- **结论**：采纳（全盘同步）

> **一处 AI 保留了自己判断的地方**：用户第 2 版的框里只写**功能摘要**（不写实体名），
> 而 `CLAUDE.md` §12.1 硬性要求 DWG-01 的框内**要列出所含实体名**，
> 否则 DWG-01 与 DWG-02 无法互查（子系统所含实体之和 = 全部 11 个实体）。
> 处理方式：矢量版**保留「名称 + 实体名 + 功能摘要」三段**——它是**加法不是冲突**，
> 不影响用户要的布局与信息层级。**这一处需要向用户明示**，不能默默改掉。

**任务 2：写会话交接文档（用户点名要的交付物）**

- **关键提示词**：见任务 1 原话（"写好交接文档方便下一个会话中agent能清楚理解目标"）
- **AI 产出**：新增 **`HANDOFF.md`**，一页纸，七节：
  ①这个项目是什么（含用户最看重的两件事）②现在到哪了 ③你现在该做什么（按顺序，三步）
  ④刚做完什么（别重复劳动）⑤**铁律清单**（按"踩了会痛"排序，第 1 条是 `rng_lfsr` 不接节拍；
  现为指向 `CLAUDE.md` §2 的索引）
  ⑥已打通的工具链 + 仿真器三条硬限制 + 两条流程性要求 ⑦**诚实提示**（已知薄弱处）
- **人工修正**：无（用户未提出修改）
- **结论**：采纳

> **为什么单独写一份而不是扩充 `PROGRESS.md`**：`PROGRESS.md` 是**账本**（厚、全、按时间累积），
> 新会话开工时通读一遍成本高；`HANDOFF.md` 是**交接单**（一页、只回答"现在干什么、别踩什么坑"）。
> 两者分工明确："下一步做什么"**只在 `HANDOFF.md` 里写**，
> `PROGRESS.md` 只留"过去做了什么"的账 —— **同一件事写两处，必然不同步**。

**任务 3：按清单核对图表，抓到 ERR-0009**

- **关键提示词**：无 —— 属自查，走 `CLAUDE.md` §12.4 的五步清单
- **AI 产出**：**第 3 步（渲染后放大 2.5~3 倍分段看）第一次放大就抓到缺陷**：
  `sw7 系统开关` 标签左对齐于 `x=70`、文字宽 79，右端落在 `x≈158`，
  越过容器左边界 `x=150` 约 8 个单位。
- **人工修正**：**没选"把 x 调小"** —— 标签左边只剩 70 单位余量而文字要 79，
  **左对齐这条路根本走不通**。改为**右对齐**（`x=132`，留 18 单位余量）。
  由此提炼出一条硬规则写进 `CLAUDE.md` §12.4：
  **文字要朝"余量大的一侧"生长；右边有线/框/边界时用右对齐。**
  **已记入 `ERRORS.md` ERR-0009。**
- **结论**：采纳

**任务 4：关账前审查全部改动并提交推送**

- **关键提示词**：见任务 1 原话（"审查全部修改内容，确认后提交和推送至github"）
- **AI 产出**：`git status` / `git diff --stat` 全量核对，确认 `.gitignore` 仍排除三份课程 PDF，
  然后按 Conventional Commits + 中文描述提交并推送。
- **人工修正**：用户在此轮**显式授权了 push**（平时默认不推送）
- **结论**：采纳

**本会话经验**：

1. ⭐ **把教训从"文档叙述"升级成"固定动作"，是本项目目前最有效的一次流程改进。**
   ERR-0006（标签压线、缩略图看不出）之后，教训被写进 `ERRORS.md`——**那是叙述**，
   结果 ERR-0008 **原样复发**。于是把教训升级成 `CLAUDE.md` §12.4 的**清单**（把叙述变成动作；现为五步），
   本次 ERR-0009 就被清单自己抓到了，而且是**第一次放大就抓到**。
   **叙述会被读过就忘，清单不会。**
2. **修一个误报时，要追问"豁免掉的东西还有没有别的问题需要盯"。**
   ERR-0007 把 `fill:none` 的虚线边界框从"障碍物"里豁免掉 —— 这个修法本身是对的，
   但**豁免之后，"文字压边界"就再没有自动检查兜底了**，这直接导致了 ERR-0009。
   **每一次"放行"都是在自动化的网里剪一个洞，要顺手看看洞里漏掉了什么。**
3. **用户自己改图时，AI 的职责从"产出"变成"核对 + 保护硬约束"。**
   本次用户的改动全部正确，AI 唯一要做的是**守住 §12.1 的实体名要求**，
   并且**明示这处保留**（不能默默改掉用户的图）。
   **用户拍板 ≠ AI 可以放过与规范冲突的地方；正确做法是保留判断并告知。**
4. **收尾和写代码一样需要"交付物"。** 本次的交付物是 `HANDOFF.md` ——
   它把"跨会话续接"从"靠 `PROGRESS.md` 写得好"变成"**有一个专门的、格式固定的入口**"。

---

### 2026-09-17 · 第 8 次会话 · 顶层例化方式定为 `component` 语法

**工具**：Claude Code（Claude Fable 5）

**任务 1：按用户要求，把顶层文件的例化方式定为 `component` 语法**

- **关键提示词**（用户原话）：

  > "对于最上层的那个逻辑组织文件，你需要使用**"component"语法**进行模块化！"

- **背景**：RTL 一行还没写（阶段 2 尚未开始），所以这次是把约定**提前**写进规范，
  不是改代码 —— **在动手之前先把写法定死，避免写完 11 个模块再回头返工顶层**。

- **AI 产出**：把这一条落到四处，各自分工不重复：
  1. `CLAUDE.md` §2 硬约束新增第 12 条（**一句话的硬约束**）
  2. `CLAUDE.md` 新增 §10.3（**为什么 + 代价 + 模板 + 五条写法要求**）
  3. `docs/01` §10 第 5 条 + 新增 §10.1（**与"框图/代码一致"这个验收要求的联系**）
  4. `docs/02` §14.1 重写（**真正要照着写的代码骨架**）

- **人工修正（AI 自己核出来的两处）**：

  | # | 问题 | 处理 |
  |---|---|---|
  | 1 | AI 写 §10.3 模板时**凭印象写 `clk_gen` 的端口**，写成 `i_clk` / `i_rst` / `o_por_rst`；核对 `docs/01` §5.1 发现真实端口是 `clk` / `sys_en` / `o_rst`，**而且根本没有 `o_por_rst`** | 照 §5.1 改正。**没有把错的模板留在文档里** |
  | 2 | 顺着上一条逐个核 `docs/01` §5，发现**它只写到 §5.10，漏了整个 `buzzer_ctrl`** | 补 §5.11；并在 `docs/02` §15 加一列「接口定义」使覆盖度可数。**记入 `ERRORS.md` ERR-0010** |

- **结论**：采纳

> ⭐ **本任务最有价值的一点，是"AI 提议的规范被拿去核对既有文档"这一步。**
> 用户只说了一句"用 `component` 语法"，AI 本可以只写一段规范就交差。
> 但写模板时必须去 `docs/01` §5 抄端口 —— **正是这个"必须去抄"的动作，
> 把 §5 缺了一整节这件事撞了出来。**
> **直接例化时顶层不需要写端口表，这个缺口可以一直不被触碰。**
>
> **换句话说：用户提的是一个写法要求，顺带暴露的是一个文档结构性缺口。**
> 这说明**规范变更不能只写规范** —— 新写法会改变"哪些地方必须互相核对"，
> 必须顺着新写法把相关的既有文档再走一遍。

**任务 2：把"代价"如实写进文档，而不是只写好处**

- **关键提示词**：无 —— 属 AI 自查（本项目的既有习惯：新规范要拿它反查已有文档）
- **AI 产出**：在 `CLAUDE.md` §10.3 与 `docs/01` §10.1 里都写了**代价**一节：
  1. 端口列表**写两遍**，改子模块端口必须同步改顶层 `component` 声明；
  2. **不一致时的报错不一定显眼** —— 端口名对不上会报 Error（能看见），
     **位宽变了可能只出 Warning**，而 **`port map` 里漏写一个端口会静默悬空、根本不报错**。
- **人工修正**：无。但 AI 主动把这条**接进了既有的 §7 文档同步规则**
  （把"改了模块接口"的同步目标由两处扩为三处），**没有新造一套独立规则**。
- **结论**：采纳

> **为什么"代价"这一节不能省**：`component` 是本项目里**唯一一处"同一份信息必须写两遍"**的地方。
> 而 ERR-0005 / ERR-0007 / ERR-0010 的共同教训正是**手工维护的副本一定会漂移**。
> 把代价写出来、并指明"防法是逐条对照 §5 真值源"，
> 才是把这个写法用得安全的方式 —— **只写优点的规范，是把坑留给未来的自己。**

**本会话经验**：

1. ⭐ **换一种写法，会改变"哪些地方必须互相核对"—— 顺着新写法把既有文档再走一遍。**
   用户提的是 `component`，暴露的是 §5 缺一节。**规范变更的影响面不止规范本身。**
2. **凭印象写端口 = 埋雷。** 本次 AI 第一版模板的端口就是错的。
   侥幸在于旁边就是 §5.1，一核就露馅；**若当时没去核，这个错会一路带到 `puzzle_top.vhd`。**
   铁律：**端口、常量、引脚，一律去真值源抄，不许凭印象。**
3. **凡是自称"唯一真值源"的表，必须能回答"它覆盖全了吗"，而且答案要能被数出来。**
   "11 个实体不重不漏"写在 §10 第 1 条里是**叙述**，它只被应用到了框图上。
   变成一条**对清单执行的核对动作**（`docs/02` §15 加一列）之后，缺哪节一眼可见。
   **这是 §12.4 那条经验（叙述 → 清单）的第三次应用。**

---

### 2026-09-17 · 第 9 次会话 · 接口全量核对与修复

**工具**：Claude Code（Claude Fable 5）

**任务 1：演练"照 §5 抄端口"，做一次全量端口核对（用户要求）**

- **关键提示词**（用户原话，来自 AskUserQuestion 的三次选择）：

  > ① `buzzer_ctrl.i_trigger` 无驱动源 → **"game_fsm 增加 o_sound_trig（推荐）"**
  > ② 「键号 → 键脉冲」译码放哪 → **"放 game_fsm 内（推荐）"**
  > ③ 修复时机 → **"现在一次修完，阶段 1 才算真封版（推荐）"**

- **背景**：上一轮（第 8 次会话）定为 `component` 语法后，
  AI 自己提出"既然顶层要逐个端口写一遍，那不如现在就真写一遍试试" ——
  **这是本次全部发现的起点**：`buzzer_ctrl` 的 `i_trigger` 写到一半，找不到谁驱动它。

- **AI 产出**：
  1. 把 `docs/01` §5 里**所有** `i_*` 端口抄成一列（60 余项），逐个在 §2.1 连接表里找发出方
     —— **换了遍历方向**（以前是"按模块往里查"，这次是"从线这一侧数"）
  2. 查出 **5 处缺口**，列成表（`docs/01` §10.2）
  3. 用户拍板后，一次改完 `docs/01`（§2.1 重写 + §5 六节 + §10.2）、
     `docs/02`（18 小节）、`docs/图/系统图.html`（DWG-01 三处标签 + DWG-02 补 3 条线）
  4. 全过程记入 `ERRORS.md` ERR-0011 / 0012 / 0013

- **人工修正**：无。但**三个决策点全部上交用户**（见关键提示词），
  AI 没有自行决定"音效触发放哪"这类**架构归属**问题 ——
  `o_sound_trig` 放 `game_fsm` 还是放 `buzzer_ctrl` 自己数拍子，
  是**两个都说得通**的方案，属于用户的判断范围。

- **结论**：采纳

> ⭐ **本任务最重要的不是查出 5 处，而是"为什么以前查不出来"** ——
> 按模块翻，每张端口表单独看都自洽；只有**换一个遍历方向**（从"线"这一侧数）
> 才看得见"没人写"的缺口。完整论证见 `ERRORS.md` ERR-0011
> （同源现象见 ERR-0010 / ERR-0013，通用动作见 `CLAUDE.md` §12.4 第 5 步与 `docs/01` §10.2）。

**任务 2：复核 `docs/02` 的 VHDL 片段能否照抄（AI 自查）**

- **关键提示词**：无 —— 起因是任务 1 改完文档后，AI 要"照着自己写的 §10.5 在心里过一遍综合"
- **AI 产出**：查出 **6 处照抄编译不过** —— 5 处是符号未定义/类型错（照着敲会立刻报错，
  **能自己暴露**）；第 6 处是**看着完全合法、其实错**的运算符优先级错
  （`r_locked and VALID_MASK = VALID_MASK` 被解析成 `r_locked and (… = …)` → 类型错误）。
  六处明细与修法见 `ERRORS.md` ERR-0012 与 `CLAUDE.md` §2 第 13 条。

- **人工修正（AI 自查出的三处"伪代码混在真代码里"）**：

  | # | 原文 | 问题 |
  |---|---|---|
  | 1 | §10.8 `exit when r_sel < 0;` | **死循环** —— 2 位无符号**永远不可能 < 0**；且循环体内向**信号**赋值、下一轮却期望读到新值（进程里读信号读到的是旧值） |
  | 2 | §10.7 "减过头再补救" | **VHDL 里根本无法表达** —— 下溢**已经发生**了，没法补救。改为"先判断、再决定改不改" |
  | 3 | §10.5 `process (all_inputs)` | **VHDL-2008 关键字**，本项目是 **-1993** |

- **结论**：采纳

> ⭐ **值得记的是"什么动作把它逼出来的"：是"准备照抄"。**
> 通读一遍不会发现这 6 处 —— 它们混在语法完整、文风正常的代码块里，
> **只有打算逐行敲进文件时才会逐行读**（与任务 1 同源：换一个动作，错误自己浮上来）。
> 由此得出的写作规范已落进 `docs/02` 与 `ERRORS.md` ERR-0012 / `CLAUDE.md` §2 第 14 条：
> **写"可直接照抄"的代码块，符号要有定义、语法要在 VHDL-1993 子集内，
> 做不到就明确标注「伪代码，不可照抄」—— 伪代码不是问题，不加标记的伪代码才是问题。**

**任务 3：修图 —— 补 3 条漏画的连线（AI 自查 + 用户 §12.4 清单）**

- **关键提示词**：无 —— 按项目既定规矩走（`CLAUDE.md` §12.3「改了模块接口 → 必须同时改框图」）
- **AI 产出**：先做「表 → 图」（§2.1 的 15 条连线在图上找对应的一笔）发现 DWG-01 三处标签不符；
  **再反过来做「图 → 表」**（图上每一笔回查 §2.1）才发现 DWG-02 **漏画 3 条**
- **人工修正**：
  - 走线全部**先算出方框坐标再定路径**（`game_fsm` 310..490/95..185 等），不凭目测
  - `level → piece_rom` 与 `pattern_sel → pattern_rom` **共用 x=400 那条竖线**
    （语义上正确：**一个源、两个目的地**），不是画重了
  - `rnd_step` 的箭头会压住既有的 `rnd` 标签 → 把 `rnd` 标签移到它所标连线的**上方**
    （这是全图统一的写法），而不是挪那根线
  - `key_press` 被竖线压字：**改标签位置解决不了** —— x=265 与 x=320 之间只有 55 单位，
    而标签需要 59 单位，**只能移动那根线**（x=320 → 360）。与 ERR-0009 里 `sw7 系统开关`
    的处境同构：**标签放不下时，先量再决定动谁**
- **结论**：采纳

> ⚠️ **本次的图缺陷有两条是 `check_svg.py` 结构上就查不到的**：它校验的是**格式**、
> 不是**完整性**（"图上少了三条线"它没有可比对象）；`key_press` 压线属于 `CLAUDE.md` §12.4
> 表里列明的"脚本查不出"那一类，**由第 3 步「渲染放大 2.5~3 倍分段看」抓到**。
> 清单本次也暴露了自己的盲区（当时只有四步，不覆盖"完整性"这一维），
> **已补为第 5 步** —— 明细见 `ERRORS.md` ERR-0013。

**本会话经验**：

1. ⭐⭐ **"缺"这个错误，只有在换一个遍历方向时才看得见 —— 本次三个发现
   （ERR-0010 / 0011 / 0013，明细见 `ERRORS.md`）全是这样来的。**
   **由此升级为一条通用动作（比"要仔细"有用得多）：
   凡是维护一份"清单/表/图"，核对时必须走两遍、方向相反**
   —— 已固化为 `CLAUDE.md` §12.4 第 5 步。
2. ⭐ **"准备照抄"是一个高价值的自查动作，应当被主动使用。**
   本次的 6 处代码缺陷、5 处端口缺口，全都是"打算真写一遍"时才露头的。
   **写代码之前把文档里的片段真抄一遍，成本极低（RTL 一行未写），收益是省掉三次分散排障。**
3. **架构归属问题必须上交用户，不能替用户拍板。**
   `o_sound_trig` 放哪、"键号译码"放哪 —— 都是**两个方案都说得通**的设计判断。
   AI 给出推荐项与理由、附上代价对比，由用户选。
   **AI 该做的是把选项的成本讲清楚，不是替用户承担设计决策。**
4. **自动化校验的"绿灯"必须分清它保证了什么。**
   `check_svg.py` 本次**全部通过**，而图上有 3 条线没画、1 个标签被压。
   **"脚本没报错"≠"已经检查过了"** —— 这条已在 `CLAUDE.md` §12.4 写过，
   本次是它第 N 次被验证。

### 2026-09-17 · 第 10 次会话 · 全项目复查：系统设计与交接文档

**工具**：Claude Code（Claude Fable 5）

**任务 1：分派多个子代理并行核查整个项目（用户要求）**

- **关键提示词**（用户原话）：

  > **"最后仔细审查系统设计和交接文档，可以分派多个 agent 进行整个项目的核查，
  > 文档不要重复堆积而是结构和内容准确清晰，确认无误后提交推送。"**

- **AI 产出**：

  1. **拆成两个判据不同的核查任务，而不是一个"读一遍看看"**：

     | 子代理 | 范围 | 判据（查什么） |
     |---|---|---|
     | A | `docs/01-系统设计.md`（1355 行） | **查"对不对"** —— 同一事实的不同叙述是否一致 |
     | B | 5 份过程文档（HANDOFF/PROGRESS/CHANGELOG/ERRORS/AI_LOG） | **查"多不多"** —— 同一信息是否写了两遍、是否有一份已陈旧 |

     拆成两个的理由：**这两件事用同一个代理做会互相干扰** ——
     读的时候一旦开始挑错，就不再注意"这段是不是多余的"。

  2. 子代理 A 报出 **14 处**；子代理 B 报出去重项 5 处 + 编号 / 条数错误 6 处；
  3. 由主会话统一执行更正（图、`docs/01`、5 份过程文档），并逐条复验。

- **人工修正**：

  - **子代理 A 报的 14 处里，有 1 处是 AI 自己（第 9 次会话）刚写进去的** ——
    §6.5 把 `i_rel_mask` 的类型写成了 `std_logic_vector(15 downto 0)`。
    这说明**上一轮"全量核对"只核了接口对不对，没核文档内部相不相互矛盾**，
    已记入 ERR-0014 的预防措施（"封版 ≠ 自洽"）。
  - 子代理 B 提了一个**它没权限定夺的问题**（`PROGRESS` 关键决策表里"仿真方案"与"仿真路线"两行重叠），
    并且明确标注 **"没动，提请你定夺"** —— 边界意识正确：
    **合并决策表的行 = 在改文档语义**，不属于"去重"的技术执行范围。
    最终由主会话判定：连同"仿真脚本语言"一行，**三行并一行**。
  - **图侧的问题一律由主会话自己动手，没有交给子代理** ——
    改图必须走 `CLAUDE.md` §12.4 的五步清单（含"渲染放大逐段看"），
    子代理没有渲染与看图的能力，交出去只会得到"改完了"但**没人看过**。

- **结论**：采纳（14 处全部更正；图 3 处；过程文档 5 处去重）

> ⭐ **本次最值得记的一条：封版只保证"改完了"，不保证"自洽了"。**
> 第 9 次会话那次"全量核对"是**沿着接口这根轴**走的（端口 ↔ 连接表），
> 所以它查得出"缺线"（ERR-0011）、查得出"符号不存在"（ERR-0012），
> **但查不出"§2.1 说必须画、§2.1.2 说不用画"** ——
> 因为那是**同一份文档横着比**，不是"文档 ↔ 代码"竖着比。
> **遍历的轴不同，能看见的东西就不同**（"换方向"是 ERR-0010/0011/0013 的道理，"换轴"是这一次的）。

**任务 2：图的第三轮修订 —— `clk` 到底该画在哪一层**

- **关键提示词**：无 —— 起因是任务 1 中子代理 A 报出
  "§2.1 系统级连线 A 行说 `clk` 必须画出来，而 §2.1.2 第 1 处说不再单画"**直接打架**
- **AI 产出**：给两层的画法做了明确分工 ——
  **DWG-01（子系统层）不画 `clk`**：这一层画的是"功能子系统怎么组织"，不是引脚清单；
  `clk` 是 `clk_gen` 这一子系统的**属性**（分频基准），折进椭圆小字。
  **DWG-02（实体层）不画 8 条实线**，改为**一条脚注逐个列出 8 个接收者实体名** ——
  8 条线横穿全图会把主线淹掉，而**接收者名单本身是可数的**，这是替代画线的等价物。

- **人工修正（改图时撞出的两处版面缺陷，都是"放大看"才发现的）**：

  | # | 缺陷 | 怎么发现的 | 怎么改 |
  |---|---|---|---|
  | 1 | `game_fsm` 框的副标题加上 `clk` 前缀后**溢出框宽** | `CLAUDE.md` §12.4 第 3 步放大渲染 | 框宽 **180 → 200**，副标题拆两行，文字居中点跟着移到 x=410 |
  | 2 | 两个 `px_red · px_green` 标签**纵向只差 6 单位**，看着像一个 | 同上 | 输出侧那个改**右对齐**锚在 (1030,152)，让文字向左生长 |

- **结论**：采纳

> ⚠️ **本次撞到两个"看图的工具本身不可信"的问题**（详见 `ERRORS.md` ERR-0015）：
> ① 无头 Chrome **默认按 dark 方案渲染**，整页截图出来是黑底（平均亮度 **38.9**）——
> 这样一张图，**原本能看出来的压线缺陷一处也看不出来**；
> ② 无头模式下 **HTML 流程文字里的中文排版错乱**，但**内联 SVG 里的中文正常**。
> **教训：拿自动化产出的图当审查依据之前，先用一个已知答案的输入验一验它可不可信。**

**任务 3：5 份过程文档去重（子代理 B 提出，主会话执行）**

- **关键提示词**：同任务 1（用户原话里的"**文档不要重复堆积**"）
- **AI 产出**：删掉 3 处整段重复 / 过期的拷贝，净减约 40 行：
  `HANDOFF` §5 抄了 14 行的铁律全文（→ 指向 `CLAUDE.md` §2 的指针 + 只留最容易忘的 4 条）、
  `PROGRESS` 的「下一步任务清单」节（→ 指向 `HANDOFF` §3）、
  `PROGRESS` 关键决策表的三行仿真描述（→ 并成一行）
- **人工修正**：订正 6 处编号 / 条数错误（`13 个模块` → `11 个功能实体`、
  `四条写法要求` → `五条`、`11 个小节` → `18/20 小节`、连线表 `2+12=14 条` → `2+13=15 条`）；
  并把 `ERRORS.md` 文末模板的标题由 `## ERR-0001` 改为 `## ERR-XXXX` ——
  **它原先与真正的 ERR-0001 撞名，`grep '^## ERR'` 会多数出一条**（能跑的计数动作必须是准的）
- **结论**：采纳

**本会话经验**：

1. ⭐⭐ **"封版"与"自洽"是两件事，需要两次不同轴的遍历。**
   第 9 次会话沿**接口轴**核对（文档 ↔ 代码），查出 14 处；
   第 10 次沿**一致性轴**核对（文档 ↔ 文档），又查出 14 处。
   **两次都是 14 处是巧合，但"两次都各有 14 处"不巧合** ——
   **只要不换轴，剩下的错误就是不可见的。**
2. ⭐ **"删"比"补"更需要判断力。** 用户这次的要求里"**不要重复堆积**"是关键：
   `docs/01` §2.2 那 60 行 ASCII 图**看起来是内容，实际是负债** ——
   它是 §2.1 的第二份拷贝，且已经漂移（少 2 条线、多 1 条不存在的直连）。
   **删掉后 §2.2 反而更好用**（变成"要查什么 → 去哪里"的索引）。
   判据：**同一事实的第二份拷贝 = 下一次改版时必然出错的点。**
3. **给子代理划范围时，"哪些不许动"比"要做什么"更重要。**
   子代理 B 遇到"该不该合并两行决策表"时回答"**没动，提请你定夺**" ——
   合并是在改文档的**语义**，不是去重的技术执行。这个边界划得对。
4. **工具产出的"证据"要先验可信度。** 本次若直接采信那张黑底截图，
   会得出"图上没问题"的结论 —— 而实际上同一批改动里就有两处版面溢出。
   **凡是自动化产出的中间物，第一次用之前先喂一个已知答案的输入进去。**

### 2026-09-18 · 第 11 次会话 · 规划审查与完善（可执行性轴）

**背景**：用户要求审查前期规划（需求/系统设计/模块详细设计/引脚表）的合理性与行业标准符合度，
对可优化处直接完善。当前环境为 macOS，无 Quartus，审查聚焦规划与文档层面。

**任务 1：规划全面审查（第三根轴：可执行性）**

- **关键提示词**（用户原话，要点）：「审查现有前期规划内容的合理性，评估模块化结构是否清晰、
  接口明确并符合数字电路与 FPGA 行业开发标准。对可进一步优化的部分直接完善补充，
  确保整体规划严谨完整，便于后续直接交由其他 agent 进行开发」
- **AI 产出**：通读全部规划文档与脚本后给出审查结论（模块化 7 子系统 / 11 实体 + 2 顶层清晰、
  接口有唯一真值源与双向核对机制、时钟/复位/编码规范符合行业惯例）；
  并按"可执行性轴"找出 4 处可补缺口：胜负图案未定稿、`docs/02` §13.4 骨架缺陷、
  §12.9 重号、§12.5 装载点缺失
- **人工修正**：审查结论中"符合行业标准"的表述限定为"符合课程项目与 FPGA 开发惯例"，
  避免夸大；胜负图案的位图样式由 AI 设计、用户尚未过目，留待上板前确认
- **结论**：采纳

**任务 2：胜负图案掩码定稿（WIN_MASK / FAIL_MASK）**

- **关键提示词**：基于 `scripts/verify_tiling.py` 的位序约定（`mask(8*行+列)`，bit0 = 左上角）
  设计对勾与叉位图并生成 64 位 VHDL 掩码
- **AI 产出**：对勾 15 格、叉 24 格（笔画 2 格宽），掩码常量写入 `docs/02` §2.5，
  位图与设计依据重写进 §7.4（原节是两套候选混排的编辑残留）
- **人工修正**：要求做"字面量 → 反解 → 位图"的**往返自检**（不只做单向生成），
  复用 `verify_tiling.py` 的 `render()` 确认无上下/左右镜像
- **结论**：采纳

**任务 3：修复 `docs/02` §13.4 音效骨架缺陷（ERR-0016）**

- **AI 产出**：重写 §13.3~§13.5——查表改存 N-1（修 off-by-one，原写法每种音效频率全错）、
  补全 `HALF_A`/`HALF_B`/`DUR`/`SEG` 四张模块私有查表、交替音段计数器 `r_seg`+`r_alt`、
  寄存器声明带 `range`、触发时锁存 `i_sound_sel`
- **人工修正**：确认查表声明在模块内而非 `puzzle_pkg`（§2.6 单点定义管全局常量，
  音效表模块私有，避免不必要的耦合）；并在 ERRORS.md 按五段式登记 ERR-0016
- **结论**：采纳

**任务 4：`docs/03-仿真验证方案.md` 框架 + `scripts/check_ports.py`**

- **AI 产出**：
  ① `docs/03` 框架（策略/循环/判读规范定稿，12 张结果表留空）——
  验证点只索引 `docs/02` 对应小节，**不复制内容**（防漂移，沿用第 10 次会话的去重原则）；
  ② `check_ports.py`：解析 `docs/01` §5 接口表做"悬空输入/无接收者输出"集合相减，
  改名线用 `ALIAS_IN` 登记。**实测**：11 实体 / 57 输入 / 44 输出全部连通
- **人工修正**：要求 `--selftest` 同时含**正例**（未注入零误报）与**反例**（两类注入均检出），
  与项目"校验器必须做负向测试"的既定规范一致；修正脚本初版对照表只显示单个生产者的问题
  （`px_red` 等透传线有两个生产者，改为全部列出）
- **结论**：采纳

**本会话经验**：

1. ⭐ **"能照抄执行"是规划的第三根轴。** 第 9 次比"文档↔代码"（接口轴）、
   第 10 次比"文档↔文档"（一致性轴），这次比"骨架↔可执行"（可执行性轴）——
   ERR-0016 的 off-by-one 与空名查表在前两根轴上**都不可见**：
   它们各自自洽、也没有第二份拷贝可比。**每个符号都要有定义处、每句意图都要落到计数器语义。**
2. **计数器"每 N 拍一次"的装载值一律写 N-1。** 按意图写"装载 N"是 off-by-one 的高发点，
   且频率偏差只有 6%~25%，仿真听感上不易暴露 —— 文档阶段就要盯住。
3. **校验脚本的对照表要显示"全部生产者"。** 同名线被透传（`px_red`：puzzle_ctrl → disp_format →
   dot_matrix_scan）时只显示一个生产者会误导人工抽查。

---

### 2026-09-18 · 第 12 次会话 · 课件 PDF 全量解析与合规补全

> **起因**：用户要求"**严格按照老师的 PDF 上的要求完善文档，只修改必要的内容**"，
> 并指出本会话早前把 BTN0 复位删掉了（改成仅 POR+SW7 两源），需要按课件恢复。
> **本会话最重要的一条教训：第一次解析 PDF 时只提取了文字，漏掉了全部纯图幻灯片。**

**任务 1：完整解析《数字系统设计2026》课件（65 页）**

- **关键提示词（用户原话）**：*"按照 pdf 中的要求来，**你需要完整解析 pdf 中的内容**！"*
- **AI 产出**：
  ① 用 `pypdf` 提取全文 → **65 页里有 13 页提取出来是空的**（p1/5/6/19/21/22/37/41/47/48/50/52/53/57）；
  ② 装 `pymupdf` 把**全部 65 页渲染成 PNG**（110 dpi，15MB）后**逐页读图**，
  把纯图页的内容补齐。**补出的实质要求共有 4 条是文字提取完全拿不到的**：

  | 页 | 只在图里的内容 | 价值 |
  |---|---|---|
  | **p50 例4** | Quartus `Analysis & Synthesis → Optimization Technique` 要选 **Area** | ⭐ **`.qsf` 里此前一条都没有**，直接照做 |
  | **p47 例2** | `1MHz ÷5000→200Hz ÷2→100Hz ÷100→1Hz` **串行级联分频** | 与本项目 `§10.2 ②` 的方案**同构** → PDF 亲自背书了我们的做法 |
  | **p48 例3** | 缺 `ELSE` → "**导致产生 4 位锁存器**" | 是 `§10.2 ⑤红线3` 的出处 |
  | **p46 例1** | 多路选择器共用译码器 = **资源共享** | 是 `§10.2 ④` 的出处 |

  ③ 另有三页是**独立交叉验证**：
  **p45（二）**图里写的是"共阴极数码管接法"、段线经 100~300Ω 限流、**高电平点亮**
  → 与本项目 `seg` **高有效**完全吻合；
  **p55** 的键盘示例表逐条代入 `KEY号=(3−ROW)×4+COL+1` **全部吻合** → 公式得到第三方证实；
  **p22** 交通灯框图里 **`复位` 是一个独立输入框** → 支持给 DWG-01 补 `btn` 一笔。
- **人工修正**：用户明确否掉了 AI 想按"最小改动"处理消抖的倾向 ——
  原 AI 判定"复位语义对抖动免疫，故不消抖"，用户要求
  **"按照 pdf 中的要求来"** → 课件 p54「**按键要注意防抖处理**」是通用表述，BTN0 也是按键，
  故改为 **2 级同步器 + 20ms 消抖**。
- **结论**：采纳（并记入 `ERRORS.md` ERR-0018）

**任务 2：恢复 BTN0 按键复位并补齐全链路**

- **AI 产出**：`btn` → `clk_gen.i_btn_rst` → 2 级同步 + 20ms 消抖 → `r_btn_rst`，
  与 `r_por_rst`、`(not sys_en)` **三源或**得到 `o_rst`。
  落点补齐：`CLAUDE.md` §4.6/§10.1/§10；`docs/01` §2.1（系统级连线 2→3 条）+ 新增 §2.1.3 +
  §4.1/§5.0/§5.1/§5.10/§8/§9；`docs/02` §2.6/§3/§12.4；
  `docs/图/系统图.html` DWG-01 与 DWG-02 各补一笔；`.qsf` 加 `PIN_61`；
  `check_pins.py` 加 `btn`（**68 引脚全过**）；自检工程增阶段 9。
- **人工修正**：无（用户已给出明确方向）
- **结论**：采纳

**任务 3：跨文件"半成品"清查（ERR-0017）**

- **AI 产出**：发现上一次改到一半中断的 7 处残留 —— 最严重的是
  `CLAUDE.md` 引用了**不存在的 `ERR-0018`**，以及 `CLAUDE.md` §4.6 与 §10.1 **自相矛盾**、
  `docs/01`/`docs/02`/图 三处**接口真值源完全没动**。
- **人工修正**：用户指出"你不是要创建 switch 分支，而是直接进 workbuddy 分支继续任务"——
  AI 最初在错误的 worktree 上重复劳动，后把改动全部作废、改到正确的工作树。
  **教训：多工作树/多分支环境下，动手前先确认"哪个目录是活的"。**
- **结论**：采纳（记入 ERR-0017）

**本会话经验**：

1. ⭐ **PDF 里的图形信息不能靠文字提取。** 本项目 `AI_LOG.md` 早在第 1 次会话就记过这条
   （"题目 PDF 里的图 4-1/4-2/4-3 是位图插图，文本提取拿不到图案"），
   **本次又原样犯了一次** —— 65 页里 13 页是纯图，而"课件要求"恰恰有 4 条只存在于图里。
   **判据：提取完先数一下"空页"，空页必须渲染后逐页看。**
2. ⭐ **"没读到"和"没有"是两回事。** 第一轮解析后 AI 判断"课件正文不含题目 4 的细则"，
   这个结论**只对文字层成立**；图层的四条要求是后来才补上的。
   **报告"某文档没写 X"之前，必须先确认自己读全了。**
3. ⭐ **判据是"器件类型 + 功能要求"，不是一刀切。**
   SW7 是拨动开关、要求即时熄灭 → 不消抖；BTN0 与矩阵键盘是机械按键 → 消抖。
   这条对比本身就是答辩素材。
4. **裁决文档冲突前先问权威来源。** 上一次把 `btn_rst` 删掉，是按"哪边改动小"选的，
   而不是按"课件怎么说" —— 结果违反课件 p45（一）。**"改哪边省事"不是判据**（ERR-0018）。
