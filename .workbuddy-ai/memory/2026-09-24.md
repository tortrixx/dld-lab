# 2026-09-24 工作日志

## 上板三现象的判读复述（13:44 咨询）

用户现场复述三条现象（时钟 50MHz / SW7=1 后 LD 无规律闪烁有周期 / 按住最右按键 16 灯全亮），
要求逐条给硬件或逻辑原因、相关性、排查与修复。

结论：**三条全是 `board_test_top` 的预期行为，无硬件故障**，与 `ERRORS.md` ERR-0030、
`docs/05` §1.5「30 秒判据四步全过」一致。逐条代码依据：

| 现象 | 代码依据（`rtl/board_test_top.vhd`） |
|---|---|
| 时钟 50MHz | 前提；全部分频系数按 50MHz 定死，档位 7 才有效 |
| LD 无规律闪烁有周期 | L451~456：`ld[15:8]`=阶段号二进制、`ld[7:0]`=子步号；L216~224 子步 4Hz；L300~331 阶段推进 |
| 按住最右按键 16 灯全亮 | L474 `ld <= (others=>'1') when btn='1' else s_ld;`，且该行在 L465~470 的 SW7 门控**之外** |

**本次新算出的一个数（原文档只有"有周期"、没有具体值）**：
自检完整轮播周期 = 阶段 1~6 各 2 s（12 s）＋ 阶段 7 超时 20 s ＋ 阶段 8/9 各 2 s = **36 秒**。
三层周期：子步 0.25 s / 阶段 2 s / 完整轮播 36 s。
→ 可作现场判据：数低 8 灯节拍 0.25 s、阶段跳变 2 s、整圈 36 s。

相关：三条同属 `board_test_top` 的同一套阶段机；现象 3 的组合越权在复位之上，
所以 SW7=0/1 都会全亮。若将来看到"按下反而只剩 LD8"，那是 `btn` 极性反（`docs/06` §6.1 第 5 行）。

未落盘：36 s 这个数尚未写进 `docs/05` / `docs/06` §6.1（待用户确认是否补）。

---

## 13:53 用户现场推翻判据 → ERR-0031（已落盘）

用户照"看阶段 1（LD8 常亮 + LD0~LD7 齐闪 1Hz）"去看，报"**所有 16 个灯都闪烁，LD8 也不是常亮**"。

**定位结论：代码没错，是判据缺"何时看"。**

- 重算 `board_test_top.vhd` L451~456 → 阶段 1 期望画面无误（`LD8` 常亮 + `LD0~LD7` 齐闪 + **`LD9~LD15` 全灭**）
- **阶段 1 只占完整轮播 36 秒里的 2 秒（≈5.6%）**，其余 34 秒是阶段 2~9 → 16 灯本来就都在动
- ⭐ **代码里有现成的阶段号读数**：L428~431 把 `r_stage` 写进 `s_disp_val(31 downto 28)`，
  经 `seg_scan` 的 `i_disp_val(4k+3..4k) → DISPk`、`o_cat(0)`=DISP0（最右）
  → **最左边那位数码管 DISP7 显示的就是当前阶段号**
- 三个能精确对准阶段 1 的时刻：**松开最右按键后 2 秒** / **SW7 由 0→1 后 2 秒** / **DISP7 显示 "1"**

**落盘（收工已走完）**：
- `ERRORS.md` 新增 **ERR-0031**（九字段）+ 索引补一行 → **索引 31 行 == 正文 31 条**（已对账）
- `docs/06` §6.1：判读表 +1 行；「30 秒判据」重写为"用 DISP7 读数" + 三个时刻表
- `docs/01` §8.2：LED 指示方式 ⚠️ 注补"阶段 1 只持续 2 秒"+ DISP7 定位手段
- `docs/05` §1.3 阶段 1 行、§1.5（三条异常 + 共性问题段）
- `HANDOFF.md` §2（顺手修掉一处陈旧行："按住 BTN0 仍未闭环" → 实为 13:38 已闭环）、
  §3（补 DISP7 提示）、§4（加续二、删第 19 次会话条目，保持 3 条）
- `PROGRESS.md`（ERRORS 31 条、当前状态、断点）、`CHANGELOG.md`、`AI_LOG.md`
- **`rtl/` 一行未改**；本次上板三条"异常"**没有一条是代码错**

⭐ **可复用的通用教训（下次写现场判据直接套）**：
**凡观测一个"会变的状态"，判据必须同时给三样 —— ① 它占周期多久 ② 哪个器件能读出它
③ 做什么动作能必然落到它。缺一条就不可执行。**
上板判据的两类缺口：**物理位置**（ERR-0030）/ **时间窗口**（ERR-0031）。

**提交**：`44f59ab`（订正 8 份文档）+ `b24795d`（修一处我自己引入的悬空引用）。
⭐ **自曝一处**：上一条提交里把 DISP7 的代码依据写成"`docs/02` §14.2 的 `when others` 分支"，
而 §14.2 实际是"输出级门控" —— **与 ERR-0017 同源**。
**教训：新增交叉引用后，要核一遍被引小节真实存在且对题**（不能只核"小节号存在"）。

---

## 13:58 复核走查步骤时又查出两处 → ERR-0032（已落盘）

在给用户写"逐阶段走查"步骤时回头读 `board_test_top.vhd` L393~433 的数码管 `case`，
发现**我上一轮写的话本身就不准**：

1. **"DISP7 显示的就是当前阶段号"是过度概括**：`case` 有 4 个分支，
   `when 5` / `when 6` / `when 7` 各自整体覆盖了 `s_disp_val`，
   **只有 `when others`（= 阶段 1/2/3/4/8/9）才写阶段号**。
2. **阶段 6 的实物读数方向与文档措辞相反**：`s_disp_val <= x"87654321"` + DISP7 在最左
   → 从左到右读是 **`8 7 6 5 4 3 2 1`**，而 `docs/01` §8.2 写"依次显示 1 2 3 4 5 6 7 8"。

**落盘**：`ERRORS.md` **ERR-0032**（索引 32 行 == 正文 32 条）+ `docs/06` §6.1（判读表 +1 行、
DISP7 段补覆盖面）+ `docs/01` §8.2（阶段 6 行 + ⚠️ 注）+ `HANDOFF.md` §3。**`rtl/` 未改。**
提交 `c2c1703`。

⭐ **这一族错误的完整画像（三次发作，各缺一维）**：
| # | 把什么写成了什么 | 缺的那一维 |
|---|---|---|
| ERR-0030 | "要求 1 管显示器件" → "LED 也全灭" | **范围** |
| ERR-0031 | "阶段 1 的画面" → "随便看 3 秒" | **时间窗口** |
| ERR-0032 | "6 个阶段里 DISP7=阶段号" → "DISP7 就是阶段号" | **覆盖面** |
| ERR-0032 | "依次显示 1..8" → 未说方向 | **方向** |

**通用预防措施（下次写任何"看某信号能得出某结论"的句子时套）**：
① 数该信号的**全部分支**，逐条列出"谁被覆盖、谁落到 `when others`"；
② 写"实物上从左到右看到什么"，**不写信号值的排列顺序**；
③ 改完引用代码的文档，回代码核**语义边界**（不只核小节存在）。

---

## 14:04 用户决定"跳过自检、直接烧 puzzle_top 演示"

用户报"按 16 个按键后 LD 灯还是乱闪烁"，并要求跳过自检阶段 2~8，
直接烧 `puzzle_top` 给老师演示、之后再完善。

**判读**：按完 16 键后 LD 回到"乱闪"是**正常的** ——
阶段 7 在按满 16 键（或超时 20 s）后**提前结束，轮播继续**走阶段 8/9 → 回到 1/2/3…，
LD 自然又变成"阶段号 + 子步号"。自检是**永久轮播**，不会停在阶段 7。

**给用户的建议（折中）**：不要完全跳过 —— 只补**最关键的 1 条**，约 30 秒：
**阶段 7 键盘**（整机全靠矩阵键盘，错了游戏完全不能玩，且从游戏表现里诊断不出根因）。
其余阶段 2/3/4/5/6/8 可在 `puzzle_top` 上顺带验（坏了能看出来，修法见 `docs/06` §6.2 只改 `.qsf`）。

⭐ **两个现场技巧（新总结）**：
1. **"整排 LD 全灭" = 进入阶段 7** —— `r_stage=7` 且无键按下时 `s_ld <= (others=>'0')`，
   这是最省事的阶段 7 地标（不用看数码管）。
2. **精确定位阶段 7 的时刻**：SW7 拨 0 再拨回 1 → 复位 → 阶段 1 起 → 阶段 1~6 各 2 s
   → **t = 12 s 进入阶段 7**，持续 20 s。

**切顶层烧录的命令**（`scripts/set_top.py` 会自动注释那 16 行 `ld` 约束）：
`python scripts/set_top.py puzzle_top` → **必须重新编译** → `quartus_pgm -c USB-Blaster -m jtag -o "p;quartus/puzzle.pof"`
⚠️ 编译要在**装着 Quartus 且连 USB-Blaster 的那台机器**上做。

未落盘：`HANDOFF.md` §3 的"第 0 步（自检 2~8 逐条打勾）"**尚未改成压缩版** ——
等用户确认 puzzle_top 演示走通再改，避免计划来回翻。

---

## 14:11 已切整机 puzzle_top 并烧录上板（计划已改，文档已落盘）

**执行**（本机就是连着板子的那台 —— `jtagconfig` 认到 `USB-Blaster [USB-0]` + `EPM1270`，JTAG ID `0x020A30DD`）：

1. `python scripts/set_top.py puzzle_top`（自动注释 16 行 `ld` 约束，`check` 复核一致）
2. `quartus_sh -t scripts/build.tcl` → **0 errors**，顶层 `puzzle_top`，
   **1226/1270 LE（97%）**、52/116 引脚
3. `quartus_sta puzzle` → setup slack **+4.292 ns**、hold **+1.379 ns**、最小脉宽 +9.734 ns
   → **Fmax ≈ 63.7 MHz ≥ 50 MHz** ✓
4. `quartus_pgm -c USB-Blaster -m jtag -o "pv;quartus/puzzle.pof"` →
   **Erase→Program→Verify 全过**，14:11:18 完成

**⚠️ 关键环境事实（下次直接用）**：
- **本机就是连着开发板的那台**（`jtagconfig` 能列出 `USB-Blaster [USB-0]`），
  **可以直接跑 `quartus_pgm` 烧录**，不必让用户手动去 Quartus GUI 里点
- `build.tcl` **不带 `--top` 时不会还原**，所以 `set_top.py` 改的 `.qsf` 会保持
- `build.tcl` 打印 Fmax 时优先找 `.sta.rpt`，但 `execute_flow -compile` **只生成 `.tan.rpt`**
  → 要看带余量判定的时序，**必须另跑 `quartus_sta puzzle`**

---

## 14:1x 定位"进不去阶段 7" → ERR-0033（已落盘）

用户报"进不去阶段 7"。**根因**：`board_test_top.vhd` 的 `r_seen`
**只在 `s_rst='1'` 时清零**，`s_key_press` 只置位 → **按满过 16 个键后恒为全 1**
→ 阶段 7 的退出条件 `r_seen = "1111…"` 立刻成立 → **每圈经过阶段 7 都在 0.25 秒内退出**
（本该 80 × 0.25 s = **20 秒**）→ 肉眼看不见。
**与用户两条回报完全吻合**：先"按 16 键"（填满 `r_seen`）→ "LD 还是乱闪" → "进不去阶段 7"。
**规避法**：按 BTN0 或拨 SW7 复位。**代码修复有意推迟**（只影响自检工具，板上正跑整机）。

⭐ **同族错误的完整画像（四次，各缺一维）**：
| # | 把什么写成了什么 | 缺的那一维 |
|---|---|---|
| ERR-0030 | "要求 1 管显示器件" → "LED 也全灭" | **范围** |
| ERR-0031 | "阶段 1 的画面" → "随便看 3 秒" | **时间窗口** |
| ERR-0032 | "6 个阶段里 DISP7=阶段号" → "DISP7 就是阶段号" | **覆盖面** |
| ERR-0032 | "依次显示 1..8" → 未说方向 | **方向** |
| ERR-0033 | "本轮按过哪些键" → 实际是"本次上电以来按过哪些键" | **作用域** |

**通用预防措施（新增一条）**：
**累积型状态（`seen`/`done`/`count`）必须写清"从哪个事件开始累积、到哪个事件清零"**；
检查动作 = 全文搜该信号名，看有几处写、分别什么条件。
另：**"看不到了"要先怀疑"它一闪而过"** —— 把该状态的应有持续时长从代码里算出来，再和实测比。

**提交**：`9b947f6`。`ERRORS.md` 索引 **33 行 == 正文 33 条**。

**下一步**：在整机上按 `docs/02` §14.3 的 13 个场景验收 + **录工作视频**（出了实验室补不出来）。
⚠️ **跳过自检的代价已如实记在 `docs/05` §1.6**：点阵行序/列序/红绿、数码管位序/段码、
蜂鸣器这 5 项至今无独立确认，整机显示异常时**第一嫌疑是它们、不是游戏逻辑**。

---

## 14:20 整机首次上电判读 → ERR-0034（已落盘）

用户报：拨 SW7=1 后**看不到自检画面**（点阵没变黄闪、数码管没全显 8 闪），**直接进 "5" 待机**。

**判读结论：代码没错，是错过了那 2 秒。**

- `T_SELFTEST = 2`（`puzzle_pkg`），`game_fsm` 的 `S_SELF_TEST` 在 `tick_2hz` 上数
  `2*T_SELFTEST = 4` 拍 → **4 × 0.5 s = 2 秒**
- ⭐ **要求 2 原文**："**2 秒后**系统进入待机状态，点阵全灭，数码管 DISP7 显示"5"、
  DISP0 显示"1"，其他全灭。" → **2 秒是需求规定，不是实现偷工减料**
  （`docs/00` 要求 1 的验收判据里也写着"SW7=1 后 2 秒内"）
- 待机画面 `DISP7="5"`、`DISP0=关卡号`（`v_lvl = i_level+1` → 第一关 "1"）
  与 `disp_format` 的 `S_IDLE` 分支逐字吻合 → **自检态与待机态两个画面都对**

**落盘**：`ERRORS.md` **ERR-0034**（索引 34 行 == 正文 34 条）+ `docs/06` §4.2（补"只持续 2 秒"
+ **三个能看到它的办法**）+ `docs/06` §6.1（判读表 +2 行）+ `docs/05` §1.6.2。
**`rtl/` 一行未改。**

⭐ **同族错误第五次发作，缺的那一维仍是「时间窗口」**，且与 ERR-0031 是同一个缺口：
| # | 把什么写成了什么 | 缺的那一维 |
|---|---|---|
| ERR-0030 | "要求 1 管显示器件" → "LED 也全灭" | **范围** |
| ERR-0031 | "阶段 1 的画面" → "随便看 3 秒"（阶段 1 占 36 s 里的 2 s） | **时间窗口** |
| ERR-0032 | "6 个阶段里 DISP7=阶段号" → "DISP7 就是阶段号" | **覆盖面** |
| ERR-0032 | "依次显示 1..8" → 未说方向 | **方向** |
| ERR-0033 | "本轮按过哪些键" → 实为"本次上电以来" | **作用域** |
| ERR-0034 | "显示开机自检" → 未说只持续 2 秒 | **时间窗口（复发）** |

⭐ **最值得记的一条**：**ERR-0030 的预防措施第 3 条早就写过**
"凡会让人误判成故障的正常现象，都要有一行进 `docs/06` §6.1" ——
**但当时只把它用在 ERR-0030 那一处，没回头扫别的状态。**
**规则写下来 ≠ 规则被执行；执行需要一次"拿规则扫全表"的动作。**

**现场技巧（新总结，下次直接用）**：
要看 `puzzle_top` 的 2 秒自检 —— ① **先盯住板子再拨 SW7**；
② **按住最右按键（BTN0）再松开**（松手即从自检态重启 2 秒）；③ **录视频回放前 3 秒**。

---

## 14:31 ERR-0034 结论被现场推翻 → 改为"待补测"（未闭合）

用户按"按住最右按键再松开"再试，报"**还是直接进 5 待机，没有 2 秒自检**"。
⭐ **松手是用户自己控制的动作，"错过窗口"解释不了它** → 原结论作废。

**逐环节重核（全部通过，代码/约束侧找不出"跳过自检"的路径）**：
| 环节 | 结果 |
|---|---|
| `clk_gen` 的 `tick_2hz` | `CNT_8K=6249 → ÷8 → ÷10 → CNT_2HZ=49` = **2 Hz** ✓ |
| `game_fsm` 的 `r_self_cnt` | `range 0 to 2*T_SELFTEST-1` = 0~3，数 4 拍 = **2 秒** ✓ |
| `disp_format` 自检分支 | 点阵 `(others=>i_blink)`、数码管 `x"88888888"` + `o_blank = not i_blink` ✓ |
| `puzzle_top` BTN0 接线 | L310 `i_btn_rst => btn` ✓ |
| `.qsf` 的 `btn` | `PIN_61` **完好**；52 引脚无重复（`check_pins.py`）✓ |

→ **怀疑点转到"复位信号有没有真的送到"**。

⭐⭐ **本次新得的一条关键判据（已写进 `docs/06` §6.1）**：
**按住 BTN0 期间，数码管与点阵应当"全灭"** ——
复位期间 `r_state = S_SELF_TEST` 且 `r_blink` 被钳在 `'0'` → `o_blank = 全 1`、点阵 `px = 0`。
**所以"按住时全灭"就是"复位通路真的通了"的直接证据**，1 秒可判。

⚠️ **重要区分**：`board_test_top` 上"按住 BTN0 → 16 灯全亮"**证明不了这条** ——
`ld <= (others=>'1') when btn='1' else s_ld;` 是**组合直读**，
**绕过了同步器、消抖与 `o_rst`**，只证明**引脚通**、不证明**复位通**。
（这一条以前没意识到，差点又误判。）

**待补测两项**：**A** 按住最右按键不松 → 数码管/点阵是否全灭（决定性）；
**B** SW7 拨 0 → 是否全灭（验门控）。

**落盘**：`ERRORS.md` ERR-0034 补记 + **状态改回「待补测」**；`docs/06` §6.1（+2 行，含"按住应全灭"这条判据）；
`docs/05` §1.6.2 补记；`HANDOFF` §3 列为未闭合项。

---

## 14:35 测试 A 结果 = 「全灭」→ 复位通路确认正常，"跳过自检"被排除

用户回报：**按住最右按键 → 数码管与点阵全灭**。

**推论链**：
1. 按住时全灭 ⟹ 复位期间 `r_state = S_SELF_TEST` 且 `r_blink` 钳在 `'0'`
   ⟹ **`btn` → 2 级同步器 → 20ms 消抖 → `r_btn_rst` → `o_rst` → FSM 全链路工作** ✓
2. 复位通路通 ⟹ FSM **必然**从 `S_SELF_TEST` 起步 ⟹ `r_self_cnt` 数 4 个 `tick_2hz`
   ⟹ **那 2 秒一定存在** → **"跳过自检"这一支排除**
3. 另补核：`puzzle_top` L315 `o_tick_2hz => s_tick_2hz`（**未与 `s_tick_1hz` 接反**）✓；
   `game_fsm` L224 `o_blink <= r_blink`（`r_blink_div = 24` 数 25 拍 → 2 Hz）✓

⭐ **现在只剩一个分支**：那 2 秒里画面**是"在闪"还是"黑的"**（即 `i_blink` 有没有真的翻转）。

**决定性观察（5 秒，不用视频）**：按住最右按键 → 松开 → 盯住数码管数秒 ——
看 "5" 是**约 2 秒后**出现（→ 那 2 秒是黑的，`i_blink` 通路有问题）
还是**几乎立刻**出现（→ 自检真被跳过）还是**看到了闪烁**（→ 之前只是没抓住窗口）。

**落盘**：ERR-0034 补记 + 索引行更新（34 行 == 34 条）；`docs/05` §1.6.2 补记。

⭐ **方法学上值得记的一条**：这次没有急着改代码，而是**先用一个 1 秒的现场测试
把两个假设分开**（复位通不通），再据结果收窄到唯一分支。
**每做一次测试，假设空间应当减半 —— 而不是同时怀疑五个地方。**

---

## 14:41 ⭐ 真因找到并已修复：键盘扫描极性反了（ERR-0035）

用户补报两条：**"见过点阵亮起"** + **"松开后直接进入 5 秒计时"**。

⭐⭐ **突破口是"计时"这个词**：`disp_format` 里**会从 5 开始倒数**的只有
**`S_PREVIEW`（预览 5 秒倒计时）**；待机 `S_IDLE` 的 "5" 是**静态**的。
→ "计时"说明系统进了预览态，不是待机态。

**完整定位链**：
1. `keypad_scan` 假设"行读到 `'1'` = 按下"，**实际行无键时就一直读到 `'1'`**
2. → 每一相都误报按键；`v_raw` 在循环里被**行号最大者**覆盖 = `(3-3)*4 + phase + 1 = phase + 1`
3. → 键号随相号循环 `1→2→3→4`，整轮锁存相 3 的值 = **4**
4. → `game_fsm` L155：`when "00100" => s_act_start <= '1';  -- 键 4 开始` ✓
5. → `game_fsm` L246 的**全局边**（写在 `case` **之前**，任何状态都拦不住）
   `if i_key_press='1' and s_act_start='1' then r_state <= S_PREVIEW;`
6. → **自检态在第一个扫描周期就被踢进 `S_PREVIEW`** → 看到"点阵显示图案 + DISP7 从 5 倒数"
   —— **与现场两条回报逐字吻合** ✓

**根因**：`docs/04` §3.3 **早就记着**"课程 PDF p55 低有效 vs 板手册高有效"的两源矛盾，
并写明"**最终以自检第 7/8 项的上板实测为准**" —— **而阶段 7 被我们跳过了**。

**修复**（`rtl/keypad_scan.vhd`，采用 PDF p55 的**低有效**）：
- 新增**全项目唯一极性翻转点** `constant KP_ACTIVE : std_logic := '0';`
- 列驱动 `o_kp_col <= not (1 sll phase) when KP_ACTIVE='0' else (1 sll phase);`
- 行判定 `if i_kp_row(r) = KP_ACTIVE then`
- **为什么低有效更鲁棒**：对"行外接上拉"与"行外接下"**两种板子都成立**（都靠"选中列低"区分）

**编译**：`puzzle_top` **1226 / 1270 LE** —— 与改前**完全一致**（常量折叠，零资源代价）
**烧录**：Erase→Program→Verify 全过（14:41）

**踩坑**：注释里用了 `✓`，GBK 装不下 → `encoding.py` **拒绝转换并报位置**（设计正确）。
已把 `✓ → √`、`✗ → ×` 加进 `scripts/encoding.py` 的 `GBK_SUBST`。

**落盘**：ERRORS **ERR-0035**（索引 35 == 35）+ ERR-0034 改"已解决"；
`docs/04` §3.3（两源矛盾→已裁决）、`docs/06` §6.1（+幻影按键一行）、
`docs/05` §1.6.3、HANDOFF §3、PROGRESS、CHANGELOG。

⭐⭐ **最值得带走的教训**：
1. **"文档里记着两源矛盾、写明'以实测为准'的那一项，就是最不能跳过的那一项"** ——
   `docs/04` §3.3 写对了处置方式，**但没人在决定跳过自检时回头读它**。
   **规则写在文档里 ≠ 决策时会想起来。**
2. **跳过某项验证前，必须先列出"它能查出什么"** —— 跳过阶段 7 时列了"点阵/数码管/蜂鸣器"，
   **漏了键盘**，而键盘恰恰是唯一"错了会让整机完全不可用"的一项。
3. **"计时"与"静态显示"是两种不同状态** —— 现场用词里藏着状态信息（本次靠它破局）。

---

## 14:52 ⭐ KP_ACTIVE 修正生效：整机三步全通；但报出新的显示/交互异常

**进展**：拨 SW7 → 2 秒自检 → 待机 → **按「开始」进预览** —— **整机终于跑起来了**。
**新异常**：① "5 秒计时结束后显示的图案点阵很奇怪"；② "键盘选择后得到的效果也奇怪"。

**分析：两族，别混**

**族 A · 位序/极性**（自检验得出；修法 = 改 `.qsf`，代码不动）
- 列序反（`COLR0` 不是最左列）→ 图案**左右镜像**
- 行序反（`ROW0` 不是最上排）→ 图案**上下颠倒**
- 红/绿引脚组接反 → 颜色不对

⚠️ **两个从未实测的假设**：
- `docs/04` §3.1 把"**COLR0 = 最左列**"**明确标为假设**（手册只给引脚号、没交代左右）
- **行序"ROW0 = 最上排"连假设都没标** —— 键盘手册写的是"ROW3 在最上排"，
  **点阵行序未必与键盘一致**
⚠️ **第一关目标图案（第 3~6 行 × 第 3~5 列）上下对称** → **"行序反"在预览态看不出来**，
   只能靠自检阶段 2。**这是一条重要的"判据盲区"。**

**族 B · 逻辑缺陷**（自检验不出）
⭐ **`puzzle_ctrl` 至今零仿真**，且经 **ERR-0026 大改**（4 路并行 → **时分复用行扫描引擎**，
32 拍刷一帧；`o_px_red/green` 变**寄存输出**）。
**"按「选择」后效果奇怪"最可能出在这里** —— 「选择」改零片选中状态，
零片像素生成正是这台引擎。

**行动**：已 `set_top.py board_test_top` → 重编译（**439/1270 LE**）→ 烧录（全过）。
让用户跑**阶段 2/3/4/5/6/7** 一次性验掉族 A（键盘极性修好后**阶段 7 终于能用了**）。
⚠️ 阶段 7 的 `r_seen` 坑仍在（ERR-0033）：**看阶段 7 前先按 BTN0 或拨 SW7 复位**。
→ 拿到结论后**只改 `.qsf`**；剩下的"奇怪"归族 B → 回电脑做**逐模块仿真**（阶段 3）。

⭐ **方法学**：**"奇怪"不是可判定的描述** —— 必须先给出**期望画面**（用确定性画面：
预览态的 4×3 目标图案），否则"奇怪"无法收敛到任何具体原因。
**没有期望值就没有判据。**

---

## 14:53 ⚠️ 切顶层未预警 → 现场以为"被破坏了"（ERR-0036）

用户报："**你这一次修改后完全破坏了之前的效果，打开 SW7 后没有自检，
数码管、点阵、LD 灯都乱闪烁**"。

**判读：板子完全正常。**
- "没有自检" ✅ **正确** —— **"2 秒自检"是 `puzzle_top` 的 `S_SELF_TEST`**，
  `board_test_top` 根本没有这个状态，它进的是 **9 阶段自动轮播**。
- "都乱闪烁" ✅ **正是轮播的正常画面** —— `ld[7:0]` 是每 0.25 秒 +1 的**子步号**、
  `ld[15:8]` 每 2 秒跳阶段号、数码管显阶段号、点阵逐行/逐列。

**根因：切顶层 = 换掉板上跑的程序，动手前没说清"会看到什么"。**
用户对"烧录"的心智模型是"把修好的程序烧进去、板子还是那个游戏"，
而换的是**整个顶层实体** —— 四个显示器件与键盘的含义全部改变。

**落盘**：`ERRORS.md` **ERR-0036**（索引 36 == 36）；
`docs/06` §5 **新增两个顶层期望画面对照表 + 醒目警告**；
`HANDOFF` §3 前置警告；`set_top.py` 收尾提示补一段；`docs/05` §1.6.4 补记。

⭐ **同族错误的第六次发作，缺的那一维是「操作后的期望画面」**：
ERR-0030 范围 → ERR-0031 时间窗口 → ERR-0032 覆盖面/方向 → ERR-0033 作用域 →
ERR-0034 时间窗口（复发）→ **ERR-0036 "换程序后的新画面"**。
**共同点：动手前没有把"接下来会看到什么"写下来。**

⭐⭐ **通用预防措施（最值得记的一条）**：
**凡会改变"板子上跑什么"的操作（切顶层 / 换 `.pof` / 改 `.qsf` 后重编），
动手前必须写一句"你会看到什么"** —— 不是"我做了什么"。
**判据：现场的人照这句话还不能判断"这是不是坏了"，那这句话就没写完。**

---

## 15:00 ⭐ 阶段 3 仿真开跑：`scripts/sim.py` 写出 + 前 2 个模块全过（用户选 A 并要求"用仿真收敛"）

用户选 A（继续看自检），并要求 **"使用仿真来自己检验，多轮分析收敛后再进行改进"**。

### 第 0 步：写出 `scripts/sim.py`（`docs/03` §2 里一直缺的驱动脚本）

三个子命令 `gen` / `check` / `run`。`run` 一键走完
「绑顶层 → 生成功能仿真网表 → 跑仿真 → 解析比对 → 还原 `.qsf`」。
- `.qsf` 用**整文件备份/还原**（比逐项还原稳，跑完逐字节相同）
- 自带波形 **SVG 渲染**（本机无 matplotlib）
- 落地 **`docs/03` §3.1 第 5 条**：中间信号清单**缺一即报错**

⚠️⚠️ **实测订正一处文档错误（重要）**：向量源**不是** `.qsf` 里的 `VECTOR_SOURCE_FILE` ——
Quartus 9.1 **不认**这个名字，实测报
`No valid vector source file specified and default file "puzzle.cvwf" does not exist`。
**正确做法：`quartus_sim <工程> --mode=functional --overwrite_waveform=on --vector_source=<文件>`**
（见 `quartus_sim --help`）。→ `CLAUDE.md` §5.3 与 `docs/03` §2.1 已订正；
**步骤 B 不再需要改工程、也就不需要还原**。

### 第 1 轮：`disp_format` —— **8/8 通过**

- ⭐ **掩码约定自检**：`TARGET1` 解回 8×8 网格 == 第 2~5 行 × 第 2~4 列 ✓
- ⭐ **预览透传逐位不差**：`o_px_red = 0x00001C1C1C1C0000` ✓
- 自检闪烁（blink=0 全灭 / blink=1 全黄 + 数码管全 8）、待机（`0x50000001` + `o_blank=0x7E`）、
  前导零熄灭（**两个方向都测**）、胜负图案 —— 全 ✓
- ⚠️ **tb 第一版有一条断言是我写错的**：把"前导零熄灭"记反（倒计时 30 的十位是 3，**不该**熄灭）
  → 仿真报 ✗，**是 tb 错、不是设计错**。已改成 ⑥a/⑥b 两个方向。
  **教训：只测一边等于没测。**

### 第 2 轮：`dot_matrix_scan` —— **4/4 通过**

- 掩码 → 引脚**无任何翻转**；行计数器 0~7 完整循环；两相消隐 ✓
- 第 2~5 行列数据均为 `0x1C` ✓
- ⚠️ **时序模块的 tb 必须显式给复位**：综合后网表寄存器初值是 **X 不是 0**，
  `r_blank_ph`/`r_row` 不复位会永远停在 X
- ⭐ **判据与采样时刻解耦**：不猜"第几纳秒是显示相"，而是**扫全程收集所有出现过的输出状态**再校验
- ⭐ **`i_tick` 是端口 → tb 直接驱动它即可，不需要改 `CLK_HZ`**（缩放只在仿 `clk_gen` 时必要）

### ⭐⭐ 收敛结论（这就是用户要的"收敛"）

**点阵显示链的"逻辑"整条正确**：
状态 → `disp_format`（8/8）→ 掩码 → `dot_matrix_scan`（4/4）→ 引脚位。

**剩下唯一的未知量是硬件约定**：`dot_colr(0)` = PIN_22 到底是哪一列、`dot_row(0)` = PIN_8 是哪一行。
→ **只能靠上板自检阶段 2/3/4 判定，仿真无能为力。**
→ **把"仿真的边界"划清，本身就是一轮有效收敛。**

**边界已划清**：整机若仍镜像/颠倒 → 改 `.qsf` 位序即可；
若"选择效果仍奇怪" → 归 `puzzle_ctrl`（**下一轮仿真目标**，它至今零仿真且经 ERR-0026 大改；
难点是它的 `i_rel_mask`/`i_height` 是**数组类型端口**，`.vwf` 只能按扁平化的比特名驱动）。

**落盘**：`scripts/sim.py` + `sim/tb_disp_format.py` + `sim/tb_dot_matrix_scan.py`；
`docs/03` §4.9/§4.4 实测数据 + §1.2 容量登记 + §2.1 订正；`CLAUDE.md` §5.3 订正；
`PROGRESS`（阶段 3 → 🔄 仿真进行中）、`CHANGELOG`。

---

## 15:15 ⚠️⚠️ 收工检查抓出严重事故：`set_top.py` 把 `.qsf` 的 16 行 ld 引脚约束截断了（ERR-0037）

**怎么发现的**：收工检查时跑 `set_top.py check` → 报"ld 引脚约束：共 **1** 行"（应 16 行）；
`check_pins.py` 报 `ld ✗`、**引脚总数只有 53**（应 68）。

**根因（两层叠加）**：
1. **`set_top.py` 的正则**：`LD_RE` 只匹配到 `-to ld[` 就结束，而写回时用
   `ln = body`（body = `group(2)` = **匹配到的那一段**）当整行
   → **每切一次顶层，16 行的 `0]`…`15]` 就被吃掉一次**。第一次发生在 14:10。
2. **Quartus 的二次破坏**：截断后的 `-to ld[` 是**非法节点名**，
   `project_close` 重写 `.qsf` 时**把认不出的赋值行直接丢掉**
   → 下一次编译/仿真后，16 行只剩 1 行。

⚠️ **最危险的地方：检查手段本身失效** ——
`set_top.py` 的旧 `check` 只**数行数**（16 行 = 通过），
而**被截断的行照样能匹配那条正则** → 数行数永远数不出截断。
**"可数的检查"必须数在"真正会坏的那一维"上** —— 这次坏的是**行内容**，不是行数。

**修复**：
1. `set_top.py` 正则改为捕获整个赋值（`\.\.\.ld\[\d+\])\s*$`）
2. `do_check` 新增**格式检查**（`LD_WELLFORMED_RE` 找缺 `n]` 的行）+ 断言恰为 16 行
3. `quartus/puzzle.qsf` 按 `docs/04` §2 补回 16 行完整赋值
   （`ld[0]`=PIN_80 … `ld[7]`=PIN_73、`ld[8]`=PIN_144 … `ld[15]`=PIN_137）
4. 重编译 + 重烧：`board_test_top` **439 LE / 68 引脚**、0 errors；`check_pins.py` 全绿
   → **修复前那个 `.pof` 只有 53 引脚，15 个 LED 根本没绑上引脚！**

**落盘**：`ERRORS.md` **ERR-0037**（索引 37 == 37）。

⭐ **通用教训（三条）**：
1. **"改写文本"的正则必须捕获整行**，不能只捕获前缀 ——
   **正则的右边界就是数据的右边界**。
2. **校验器要校验"会坏的那一维"**：问自己"如果它坏了会长成什么样"，照着那个样子写检查。
3. **改写真值源文件的脚本，必须配独立的外部校验并在改完后立刻跑** ——
   本次是**收工检查才发现**，已经带病编译烧录过两次。
   `.qsf` 是引脚号唯一真值源，被破坏属**最高级别事故**（编译不报错、板子"看起来在工作但引脚是错的"）。

---

## 16:30 ⭐⭐ 仿真流水线重构 + 多 agent 并行调度（B1 三模块一次跑通）

用户要求：系统性梳理完整流程 → 明确依赖与失败点 → 设计工作流派发方案 →
**动态分配调度多个 agent 跑多轮仿真** → 每轮可追踪可对比 → **全部收敛后才上板**。

### 一、先修伤害（两处，都是"临时改动没还原"）

1. `puzzle_pkg.CLK_HZ` **漏还原**（补丁中途报错 → `finally` 拿不到 `saved`）
2. `board_test_top` 的 `r_sub_div` 补丁也只还原了一半（同一文件两处补丁，`saved` 存了两份，
   后写的覆盖了先写的）

### 二、架构级修复：`sim.py` 改为「隔离工程」版 ⭐

**每次仿真在 `.tmp/sim_<模块>/` 生成独立工程**（RTL 副本 + 补丁 + 自己的 `.qsf`），跑完即删。
- **仓库 `rtl/` 与 `quartus/` 全程只读** → 既不需要"还原"，也就不会"忘还原"
- **根除 ERR-0037 那一整类事故**（改真值源 + 忘还原）
- **N 个 agent 可以真并行**
- 实测验证：跑完 `git status` 里 `rtl/`、`quartus/` 无改动 ✓
- 新增 `sim.py clean`；清理失败**不再致命**（沙箱批量删除闸门会 SIGTERM 进程 → 吞异常 + 提示）

### 三、轮次记录（可追踪 / 可对比）

`sim/rounds/<模块>/r<NN>.{md,json}`；`sim.py rounds [<模块>]` / `sim.py diff <模块> <N1> <N2>`
→ diff 直接给出「修好了 / 退步了 / 新增」。

### 四、`docs/07-仿真流水线与调度方案.md`（新交付物）

全流程 DAG（L0~L7）+ 关键依赖表 + **8 类失败点清单（全部来自今天真实错误）** +
多 agent 调度方案（角色/批次/收敛判据）+ **★ 上板闸门六条判据**。

### 五、第一波调度（B1）三个 agent 并行，全部一次跑通

| 模块 | 轮次 | 断言 | 亮点 |
|---|---|---|---|
| `clk_gen` | r01 11/11 · r02 3/3 · r03 4/4 | 18 | 覆盖 docs/02 §3.6 全部 11 条；另做全速短跑验 6250:1 |
| `keypad_scan` | r01 10/10 · r02 10/10 | 20 | **两种极性都测**；**无键不误报回归通过**（ERR-0035） |
| `seg_scan` | r01 19/19 | 19 | 实测帧周期 640ns = 16×i_tick → 500Hz |

### 六、⭐ 仿真第一次真正抓到东西 → ERR-0038

- docs/02 §3.6-1 写「前 10ms `o_rst`=1」，**实测 9.156 ms**（`= T_POR_MS-1` 的 off-by-one，
  同 ERR-0016 一族 —— 本项目第二次栽在计数边界）
- docs/02 §3.6-9 写「`o_rst` 恒为 '1'」**不成立**：POR(9.16ms) **短于**消抖(20.09ms)，
  中间 ~10.9ms 是 `o_rst`='0'
- ⭐ **两处上板都看不出来，只有仿真能抓** —— 这正是"用仿真收敛"的价值

**落盘**：提交 `086b64b`（31 文件）；`ERRORS.md` 索引 38 == 正文 38。

⭐ **方法学**：**把"改真值源再还原"改成"改副本"** ——
**消除一类事故的办法不是"记得还原"，而是让"不需要还原"。**
