# CHANGELOG — 变更日志

本项目所有值得记录的变更都会写在这里。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

**变更类型**：`Added` 新增 · `Changed` 修改 · `Fixed` 修复 · `Removed` 删除 · `Docs` 文档

> **使用约定**：每个 commit 对应一条记录。写完代码就跑仿真、跑完仿真就提交、提交完就登记到这里。

---

## [未发布]

### 2026-09-17 · 第 1 次会话 · 项目启动

**Added**
- 建立仓库目录结构：`rtl/` `sim/` `quartus/` `scripts/` `docs/` `report/`
- 新增 `.gitignore`：排除三份课程 PDF（版权材料）与 Quartus 编译产物
- 新增 `CLAUDE.md`：工作约定、硬约束、硬件速查、构建/仿真命令、Commit 规范、文档同步规则
- 新增 `README.md`：项目总览
- 新增过程文档模板：`PROGRESS.md`（跨会话进度）、`CHANGELOG.md`（本文件）、`ERRORS.md`（错误记录）、`AI_LOG.md`（AI 使用记录）

**Docs**
- 完成需求与硬件资料的解码与核实：
  - 逐像素解码题目4 的图4-1 完整图案：点阵第2~5行 × 第2~4列实心 4×3 矩形（12 格）
  - 逐像素解码三块零片形状：6格 `XXX/XX./X..`、3格 `.X/XX`、3格 `XXX`
  - 程序化穷举验证：三块零片恰好有 **2 种合法铺法** → 确立"并集 == 目标掩码"的成功判定准则
  - 从开发板手册核实全部引脚分配、有效电平、位序（DISP7 最左）
  - 核实键盘实物结构：ROW3 在最上排、丝印仅 KEY1~KEY16
  - 从开发板实物照片读取时钟档位丝印：档位 7 = 50MHz
- 确认工具链：Quartus II 9.1（`C:\QuartusII91`），无 ModelSim/GHDL → 仿真采用内置仿真器 + `.vwf`

**Changed**
- 需求冲突处理：题目基本要求3（关卡号显 DISP0）与要求9（显 DISP2）矛盾，**统一取 DISP0**，在文档中说明理由

---

### 2026-09-17 · 第 2 次会话 · 打通自动仿真链路

**Added**
- 新增 `scripts/vwf.py`：Quartus `.vwf` 向量波形文件读写库（解析 / 生成 / 展开嵌套
  `NODE`·`REPEAT`·`LEVEL` / 参考模型比对）。自带自检 `python scripts/vwf.py`
- 新增 `scripts/verify_tiling.py` 入库（穷举验证拼图分解的合法性）

**Changed**
- **仿真方案落地**：确定并实测跑通全自动仿真链路 ——
  `quartus_map --generate_functional_sim_netlist` →
  Python 生成激励 `.vwf` → `quartus_sim --mode=functional --overwrite_waveform=on`
  → Python 解析回写结果并逐点比对参考模型
- 仿真方案由"计划中的 TCL 脚本"改为 **Python**：实测确认 Quartus II 9.1 的
  `::quartus::simulator` 是空包（无任何 Tcl 命令），`quartus_sim -t` 无法驱动仿真

**Fixed**
- 见 `ERRORS.md` 三条记录（均为工具链/引脚问题，非 RTL 缺陷）：
  - ERR-0001 实体名 `exp` 与 Quartus 原语冲突 → 综合失败
  - ERR-0002 内置仿真器无 Tcl 接口且强制要求 `.vwf` → 仿真一度无法自动化
  - ERR-0003 引脚位序与板上网络名混淆 → 差点抄到镜像的点阵列序

**Docs**
- `CLAUDE.md` §5.3 重写为**已实测的**仿真流程（含命令行、`.vwf` 会被覆盖的警告、三个坑）
- `CLAUDE.md` §4.2 新增"引脚号 vs 位序"小节：手册权威引脚表 + 旧工程踩坑说明
- 交叉验证引脚表：与开发板手册、与桌面上 9 个已跑通的旧 Quartus 工程三方核对，
  六组引脚中五组完全一致，唯一差异（红列位序）已定位为旧工程自身命名习惯

---

### 2026-09-17 · 第 3 次会话 · 完成初步设计方案

**Added**
- 新增 `docs/00-需求分析.md`：11 条基本要求逐条拆解（原文 / 解读 / 验收判据 / 涉及模块）、
  键盘功能映射、提高要求取舍论证、需求追踪矩阵、5 处需求冲突的取舍记录
- 新增 `docs/01-系统设计.md`：**初步设计方案主体** —— 设计思路、总体框图（含内部结构框图）、
  **11 个功能实体**（外加 `puzzle_pkg` 包与 2 个顶层）的划分与理由、全局设计约定（位序 / 极性 / 节拍）、
  完整模块接口定义、关键算法、硬件自检方案、资源预估
- 新增 `docs/02-模块详细设计.md`：12 个 RTL 模块逐个展开（职责 / 端口 / 内部结构 /
  关键代码骨架 / 设计要点 / 仿真验证点），含模块↔文件↔框图↔需求对照表
- 新增 `docs/04-引脚分配表.md`：引脚总表、位序说明、极性总表、资源利用率、
  检查脚本的负向测试记录、上板自检清单、三方交叉验证记录
- 新增 `scripts/check_pins.py`：**引脚一致性检查脚本** —— 把开发板手册的权威引脚表
  与 `quartus/puzzle.qsf` 逐条比对，并检查引脚唯一性；支持 `--markdown` 输出文档表格

**Changed**
- `CLAUDE.md` §4.4：修正 `tools/verify_tiling.py` 路径笔误为 `scripts/verify_tiling.py`
- **第二关图案定稿**：采用向上箭头（18 格，4 块零片 6+6+4+2），
  备用向下箭头；两者均经穷举验证有 **2 种合法铺法**。掩码常量实测写入 `docs/02` §2.5

**Fixed**
- 修正了一处会误导实现的引脚风险表述：明确"引脚号只认手册，旧工程只能用于
  交叉验证用到了哪些引脚"（`ERRORS.md` ERR-0003 的结论已落入 `docs/04` §9）

**Docs**
- 用 `scripts/verify_tiling.py` 生成并核实**全部图案与零片掩码常量**（L1/L2/L2B 目标图案、
  L1_P0~P2、L2_P0~P3），消除 `docs/02` 中所有 `"..."` 占位符
- `scripts/check_pins.py` 通过**负向测试**：注入"抄错单个引脚号 / 整组位序颠倒 /
  漏掉一行"三类典型误配置，**全部被检出**（记录在 `docs/04` §6）
- 识别并记录**三处本质相同的边界回绕陷阱**（掩码逐行移位、零片上移越界、
  倒计时 00→99），作为实验报告"故障及问题分析"的素材
- 确立**时间/空间分离**架构原则：`puzzle_ctrl` 只管坐标掩码（无计数器），
  `game_fsm` 只管状态时间（无坐标运算）

---

### 2026-09-17 · 第 4 次会话 · 图表与资源利用率约定

**Added**
- 新增 `docs/图/系统图.html`：**两张对外图表**（可导入 draw.io 的内联 SVG）
  - DWG-01 系统总体框图 —— 12 个方框按职责三色分区（时序与调度 / 器件驱动 / 数据与规则），
    标出主数据流、反馈路径（橙色虚线）与输出级门控区（灰色虚线，表示代码区而非模块）。
    （第 1 版：当时尚未拆成两层，图号与分层后来有调整）
  - DWG-02 `game_fsm` 状态转移图 —— 6 个状态 + 全部 9 条迁移边，
    含"任意锁定后返回 `S_IDLE`"与"第一关拼对回流 `S_PREVIEW`"两条长回边
- `CLAUDE.md` 新增 §4.6 **独立按键 BTN0~BTN7**：手册原文、引脚、极性（按下=1）、消抖要求，
  并说明它与 4×4 矩阵键盘、SW7 是三套不同的输入
- `CLAUDE.md` 新增 §10.2 **资源利用率约定**（五小节）：`integer` 的 `range` 规则、
  分频器串行级联、位宽按需最小化、其它省面积手法、三条不得逾越的红线
- `CLAUDE.md` 新增 §12 **图表规范**（一个矩形框 = 一个 `.vhd` = 一个实体）
- `CLAUDE.md` 新增 §13 **过程记录要求**（每个会话必须留原始数据与提示词，供报告取材）

**Changed**
- `CLAUDE.md` §2 硬约束新增第 9/10/11 条：`integer` 必须带 `range`、自底向上实现、
  每个模块必须能独立仿真
- `CLAUDE.md` §4.1 引脚速查表新增"独立按键 BTN0~BTN7"一行
- `CLAUDE.md` §0 收工流程新增"记录仿真/编译原始数据"一步

**Fixed**
- **`docs/02` §3.3~§3.5 分频器结构改正**：原写"5 个计数器并行"（需 99 位），
  与 `CLAUDE.md` §10.2 ② 的串行级联要求矛盾 → 改为 5 级串行级联，**33 位**
  （省 66 位；相对裸 `integer` 的 160 位省 127 位）。同时补充：
  只有 `CNT_8K` 依赖 `CLK_HZ`，后四级是纯比例关系（8/10/50/100），
  板上时钟档位若变更**只需改一处**
- `docs/02` §10 与 `docs/01` §5 的 `place()` 形参 `ar`/`ac` 补 `range 0 to 7` ——
  这两个形参参与加法（`r + ar`），不加约束会综合出 32 位加法器
- `docs/02` §2 的 `popcount()` 内 `variable n` 补 `range 0 to 64`

**Docs**
- 修正 `CLAUDE.md` §10.2 ① 的规则表述**过宽**：原文写"不允许出现没有 `range` 的
  `integer` 信号或变量……循环变量一个都不例外"，但 VHDL-93 的 `for` 循环变量是
  隐式声明的、**语法上不允许**写 `range`，且 `constant` 在综合时被折叠、不占资源。
  已改为按位置分类的表格，并补上真正要防的情形：**循环上界不得来自信号**

---

### 2026-09-17 · 第 5 次会话 · 修正设计方法学表述 + 总体框图改为两层

> **本次会话的起因是用户纠正了一处方法学表述错误**：AI 在进度汇报里把项目总体思路
> 说成"**自底向上的依赖分层**"，正确说法是"**自顶向下设计分层**"
> （实现顺序才是自底向上）。两处修正一并落实到了文档与图表。

**Changed**
- **`CLAUDE.md` §2 第 10 条改写**：由"实现顺序自底向上"改为
  "**设计自顶向下，实现自底向上**（两件事，别混为一谈）"
- `CLAUDE.md` §12.1 图表规范改为**两层框图**的表述：DWG-02 才是"一个方框 = 一个实体"，
  DWG-01 一个方框 = 一个功能子系统（框内须列出所含实体名）
- `docs/01-系统设计.md` **§1.1 重写**：由"从需求到结构的推导"改为
  "**自顶向下的三层分解**"（第 0 层需求 / 第 1 层 7 个功能子系统 / 第 2 层 11 个实体）。
  原"三类职责"表重新定位为 §1.1.2"**切分时用的判据**"，并说明它与分层表是两个层次
- `docs/01` **§2 拆为两节**：§2.1 = DWG-01 第一层功能子系统框图（新增 ASCII 图）；
  §2.2 = DWG-02 第二层实体级框图（原 §2.1）
- `docs/01` §3.1 模块清单**新增"所属子系统"与"职责类别"两列**，使两层框图可互查
- `docs/02` §14.1 / §15 的框图引用由 §2.1 改为 **§2.2**，§15 对照表同步新增"所属子系统"列

**Added**
- `CLAUDE.md` 新增 **§2.1「自顶向下设计」与「自底向上实现」是两件事**：
  两者的作用阶段对照表、为什么必须分清、本项目的三层分解结果、报告里该怎么分别表述
- `docs/01` 新增 **§1.1.3 每一层都能追溯到需求**（举例说明"实体 → 子系统 → 需求"的上溯路径），
  这是自顶向下区别于自底向上的本质特征
- `docs/01` §10 一致性约定**新增两层框图的对应规则**：子系统所含实体之和 = 全部 11 个实体，
  不重不漏；并规定模块文件头部注释须写"**所属子系统**"
- `docs/图/系统图.html` 新增 **DWG-01 总体框图**（7 个功能子系统）：
  `clk_gen` 置顶向下引**节拍总线**再扇出；数据流横向贯穿；**全图仅一条反馈**
  （`all_locked` / `solved` 回到 `game_fsm`，橙色虚线标出）
- `ERRORS.md` 新增 **ERR-0005**：校验脚本用硬编码类名白名单，图表改版后反过来误报
- `ERRORS.md` 新增 **ERR-0006**：DWG-01 的 `sound_sel` 走线横穿两个方框 + `pattern_sel` 标签压框

**Fixed**
- **DWG-01 两处图形缺陷**（格式校验全绿也查不出来的那类）：
  ① `sound_sel` 原走 `M530,307 H1230`，y=307 落在方框的 240~328 区间内，
     **横穿「拼图核心」与「显示子系统」两个方框** → 改走最下方空走廊
     `M500,328 V730 H1180 V558`（与 y=690 的 `state·level·cnt` 走廊上下嵌套，互不相交）；
  ② `pattern_sel` 标签原在 `(578,286)`，文字向右延伸约 73px 压到「拼图核心」左边框
     （x=610），且与 `控制` 箭头的箭头尖重叠 → 移到该线水平段上方 `(574,424)`
- **`scripts/check_svg.py` 新增第 ④ 类检查「走线是否横穿方框」**：
  新增 `_segments()`（`<line>` 与轴对齐 `<path>` → 线段）与 `_crosses()`（线段穿矩形内部）。
  **内缩 1 单位再判定**，使"箭头落在方框边上"这种正确画法不误报；
  只判定轴对齐线段，斜线跳过。**已做负向测试**（横穿/竖穿检出，贴边与外侧不误报）
- **`scripts/check_svg.py` 删掉 `FILL_CLASSES` / `STROKE_ONLY_CLASSES` 两张硬编码清单**，
  改为新增 `parse_css_classes()` **直接从 `<style>` 块解析**每个类声明了哪些属性。
  原写法是样式表的手工镜像，图表一改版就失效并产生假警报（详见 ERR-0005）。
  同时补两个检查：`marker-end` 引用不存在的 `id`、同页多张 SVG 的 `id` 重名
- 改动后**重做负向测试**：注入 5 类故障（注释含 `--` / 缺 `fill` / 属性被类覆盖 /
  `marker-end` 悬空引用 / `id` 重名）**全部检出**，并做反例测试确认**不误报**

**Docs**
- `AI_LOG.md` 新增第 5 次会话记录，重点记录**用户对"自顶向下"的方法学纠正**
  —— 这类"用一个正确的观察推出错误结论"的错误，是评分项⑦"人工修正"栏的高价值素材
- `PROGRESS.md` 同步更新阶段状态、模块归属与"上次会话断点"

---

### 2026-09-17 · 第 6 次会话 · 用户手绘总体框图 → DWG-01 定稿

> **本次会话的角色反转**：前五次的图都是 AI 画的，本次**用户自己在飞书白板上画了一张**
> 完善后的总体框图，让 AI 审阅。AI 逐条核对拓扑与接口后，用户裁决"**就定你这张图**"
> ——保留飞书稿的布局，由 AI 把四处问题补/改到矢量图里。

**Added**
- 新增 `docs/图/DWG-01-系统总体框图-飞书原稿.png`：用户手绘定稿的原始导出（留档）
- `docs/01` 新增 **§2.1.1 定稿过程中修正的四处**：四处改动逐条列出「改了什么 / 为什么必须改」，
  其中第 3 处（删除「图案与随机数据」的节拍输入）单独展开论证
- `docs/01` §2.1 新增**全部 11 条连线**的清单表（起止子系统 / 信号名 / 含义），
  使框图与 §5 接口定义表可逐条互查
- `ERRORS.md` 新增 **ERR-0007**：新加的「走线横穿方框」检查把虚线边界框当成了障碍物，
  导致六条合法外部连线全部误报
- `ERRORS.md` 新增 **ERR-0008**：DWG-01 定稿图三处标签被走线从中划开，
  而五类校验全绿

**Changed**
- **`docs/图/系统图.html` 的 DWG-01 整块重排**，采用飞书白板稿的布局
  （椭圆时钟 + 顶部节拍总线 + 横向数据流），并补齐/修正四处：
  1. **补回 `all_locked · solved` 反馈**（走总线与方框之间的 y=395 走廊）——
     全设计唯一的回环，缺了它解释不了 `S_PLAYING` 凭什么走到 `S_WIN`/`S_FAIL`
  2. **补回 `sw7`（系统开关）与 `clk 50 MHz` 两个外部输入** ——
     SW7 是基本要求第 1 条，不能不出现在总体框图上
  3. **删除「图案与随机数据」的节拍输入** —— 见下方"Fixed"第 1 条
  4. **「显示系统」改为「显示子系统」** —— 与 `docs/01` §1.1.1 的子系统命名、
     §3.1 的模块清单对齐，使两层框图能对上号
  另：所有连线加上**端口名标注**、补图例与图号、统一信号名语言
- `docs/01` §2 开头的说明补上"**以矢量版为准，PNG 只作留档**"的约定

**Fixed**
- **删除「图案与随机数据」的节拍分接**（架构性修正，非排版）。
  `pattern_rom` / `piece_rom` 是纯组合常量表，`rng_lfsr` 由 `puzzle_ctrl` 的
  `rnd_step` **请求推进**、接的是原始 `clk`。图上画成"接节拍"，实现时就极易写成
  "每个 `tick` 采一次随机数" → 同一拍内多次落位尝试读到**同一个**随机数 →
  所有零片落点相同 → 必然重叠 → 永远走回退布局。
  **游戏仍能跑，只是永远不随机**（`CLAUDE.md` §10.1 记录的坑）。
  删除后在原位置留了 XML 注释说明"这里为什么没有线"，防止以后被"补"回来
- **DWG-01 三处标签压线**：`target_mask · rel_mask` × x=1240 竖线、
  `rnd_step` × `pattern_sel` 竖线、`像素 / px_red · px_green` × x=1240 竖线。
  **其中两处的肇事竖线就是上面那条被删掉的节拍分接** —— 先删线、再挪标签。
  第三处（`rnd_step` 标签与 `pattern_sel` 竖线的 y 区间重叠）把标签上移到竖线起点之上

**Docs**
- `scripts/check_svg.py` 第 ④ 类检查的判据改为**解析元素最终生效的 `fill` 值**，
  **只把"有填充的 rect"当方框**（`fill:none` 的虚线边界框是"范围"不是"障碍"，
  连线本来就该穿过去）。
  **没有**改成"跳过 `dg-region` 类"——那又是加一张类名白名单，会重犯 ERR-0005。
  判据取自**决定语义的属性**：此后新增任何一种 `fill:none` 的范围框都自动放行
- `scripts/check_svg.py` 新增 `_fill_of()` 辅助函数，按 CSS 规则解析优先级：
  **行内 `style` > CSS 类 > `fill=` 表现属性**（表现属性特异性为 0，ERR-0004 已踩过）
- 负向测试补充**反例**：线穿过 `fill:none` 的范围框**不应报警**（❌ 未误报）
- `AI_LOG.md` 新增第 6 次会话记录。**本次最有价值的一栏不是"AI 产出"而是分工本身**：
  用户画图（对"清不清楚"敏感）× AI 审图（对"自不自洽"敏感），两者恰好互补
- `PROGRESS.md` 同步更新

> **本次会话最值得记的一条**：图上那条多余的节拍线是 AI 为了"保持与手绘稿一致"留下的
> —— **为了像而留，不是因为有用来留**。代价是一条"永远不随机"且几乎查不出来的 bug 的入口。
> **保真度不能凌驾于正确性之上。**

---

### 2026-09-17 · 第 7 次会话 · 收尾准备与交接文档

> **本次会话是一次"关账"**：用户对飞书白板稿做了**第 2 次修订**，AI 同步矢量图、
> 补全文档、建立会话交接单，然后把阶段 1 封版 —— **设计文档到此为止，下一步进入编码**。

**Added**
- **新增 `HANDOFF.md`：一页纸会话交接单**（本次会话的核心交付物）。
  七个小节：①这个项目是什么（含用户最看重的两件事）②现在到哪了 ③你现在该做什么（按顺序）
  ④刚做完什么（别重复劳动）⑤铁律清单（按"踩了会痛"排序；现为指向 `CLAUDE.md` §2 的索引）⑥已打通的工具链 + 仿真器三条硬限制
  ⑦诚实提示（`quartus/` 暂时编译不了是正常的、`scripts/sim.py` 故意暂缓、两个待上板确认的硬件项）。
  **目的**：`PROGRESS.md` 已经很厚（记账用），新会话开工时先读一页纸的 `HANDOFF.md` 更快
- `docs/01` 新增 **§2.1.2 飞书稿第 2 版的两处调整**：逐条记录改了什么、为什么这样改
- `ERRORS.md` 新增 **ERR-0009**：`sw7 系统开关` 标签压过容器边界

**Changed**
- **`docs/图/系统图.html` 的 DWG-01 同步到飞书稿第 2 版**，两处调整：
  1. **50MHz 并入时钟椭圆，不再单画一条输入线**。理由：50MHz 是这一子系统的**属性**
     （分频基准），不是第二路控制输入；真正需要从图外进来的只有 `sw7`
     （它参与复位：`rst = (not SW7) or por_rst`）。椭圆里加了一行
     `50MHz · 5 档单周期节拍` 作副标题
  2. **反馈连线补上中文说明**：`all_locked · solved（反馈游戏是否结束）`。
     信号名是给实现看的，中文说明是给读者看的，两者都要
- `docs/图/DWG-01-系统总体框图-飞书原稿.png` 覆盖为用户第 2 版导出；`.rev` 标记更新为"第 2 版"
- **`CLAUDE.md` §0 开工流程改为 5 步，`HANDOFF.md` 升为第 1 步**（原第 1 步读 `PROGRESS.md` 降为第 2 步）；
  §0 收工流程新增"**第 0 步：更新 `HANDOFF.md` §2/§3/§4**"；§3 目录导航补 `HANDOFF.md` 一行
- **`CLAUDE.md` §12.1 图集表新增「布局来源」列**：DWG-01 = 用户飞书白板稿，DWG-02/03 = AI。
  §12.3 明确"矢量版是权威版本"
- `PROGRESS.md` 阶段 1 状态由"主体已完成，待定稿"改为"**已完成并定稿**"，
  阶段 2 标为"← 现在在这里"；"下一步任务清单"与 `HANDOFF.md` §3 合并为同一份；
  关键决策表补 3 行（会话交接方式 / 图表定稿走清单 / 验收校验器的标准）

**Fixed**
- **ERR-0009 — `sw7 系统开关` 标签压过容器边界**：标签左对齐于 `x=70`、文字宽 79，
  右端落在 `x≈158`，越过容器左边界 `x=150` 约 8 个单位。
  修法：改为**右对齐**（`text-anchor="end" x=132`，留 18 单位余量）。
  **没选"把 x 调小"** —— 它左边只剩 70 单位余量而文字要 79，左对齐这条路根本走不通。
  由此提炼出一条硬规则写进 `CLAUDE.md` §12.4：**文字要朝"余量大的一侧"生长**；
  右边有线/框/边界时用右对齐
- **ERR-0009 的发现过程比缺陷本身更值得记**：它是**照 `CLAUDE.md` §12.4 清单走第 3 步
  （渲染后放大 2.5~3 倍分段看）时第一次放大就抓到的**，不是碰巧。
  ERR-0006 是"缩略图看着没问题、放大才发现"，ERR-0008 是"同类问题第二次出现"，
  而这次 —— **把经验从"文档叙述"改成"固定动作"确实生效了**，这是可验证的流程收益

**Docs**
- `CLAUDE.md` §12.4「校验器查不到什么」表补一行：**标签压边界 / 压虚线框**。
  这条是 ERR-0007 的**副作用**：把 `fill:none` 的虚线边界框从"障碍物"里豁免掉是对的，
  但**豁免之后，"文字压边界"就再没有自动检查兜底了** —— 这是 ERR-0009 的成因。
  由此得出一条通用经验写进文档：**修一个误报时，要问"豁免掉的东西还有没有别的问题需要盯"**
- `CLAUDE.md` §12.4 新增"写标签的一条硬规则"表（按余量方向决定对齐方式）

> **本次会话最值得记的一条**：ERR-0006 之后把教训写进了 `ERRORS.md`（叙述），
> ERR-0008 原样复发；于是把教训升级成 `CLAUDE.md` §12.4 的清单（动作；现为**五步**），
> ERR-0009 就被清单自己抓到了。**叙述会被读过就忘，清单不会。**

---

### 2026-09-17 · 第 8 次会话 · 顶层例化方式定为 `component` 语法

> **本次是一次规范变更**：用户在阶段 2 开始前提出，
> **顶层逻辑组织文件必须用 `component` 语法做模块化**。
> 由于 RTL 一行还没写，这次是把约定**提前**写进规范与设计文档，不是改代码。

**Added**
- **`CLAUDE.md` 新增 §10.3「顶层例化规范：一律用 `component` 语法」**：
  四条理由（一个声明块 = DWG-02 的一个方框 / 端口在顶层文件内全部可见 /
  课程惯例 / VHDL-93 最保守写法）+ 如实写出的代价 + 写法模板 + 五条写法要求
  （声明与例化同序、例化标签 `u_<实体名>`、**一律具名关联**、每个声明块上方一行中文注释、
  端口一律照 `docs/01` §5 抄并**从"线"这一侧反向核一遍**）
- **`CLAUDE.md` §2 硬约束新增第 12 条**：顶层文件一律 `component` 声明 + `port map`，
  不得用 `entity work.xxx` 直接例化
- **`docs/01` 新增 §10.1「为什么顶层必须用 `component` 语法」**：四条理由 + 代价与防法
- **`docs/01` 新增 §5.11 `buzzer_ctrl`** —— **原 §5 接口定义表漏了这一节**（见下方 Fixed）
- **`docs/02` §14.1 重写为三个小节**：14.1.1 例化方式（含"不要在本节抄端口表"的告诫）/
  14.1.2 代码骨架（`component` 声明区 + `port map` 例化区，`clk_gen` 完整展开作样板）/
  14.1.3 五条写法要求
- **`docs/02` §15 对照表新增「接口定义」列**：逐行指向 `01` §5.0~§5.11，
  用于核对"12 节连续覆盖"（ERR-0010 的预防措施）
- `ERRORS.md` 新增 **ERR-0010**：`docs/01` §5 接口定义表漏了 `buzzer_ctrl`

**Changed**
- `CLAUDE.md` §7 文档同步规则的"改了模块接口/端口"一行：
  同步目标由两处扩为**三处**，新增"顶层 `component` 声明"
- `PROGRESS.md` 关键决策表补 2 行（顶层用 `component` / `docs/01` §5 补 §5.11）
- `HANDOFF.md` §5 补入 `component` 语法一条（原为铁律表，第 10 次会话起改为
  指向 `CLAUDE.md` §2 的索引）；§6"改接口要同步三处"同步更新

**Fixed**
- **ERR-0010 — `docs/01` §5 接口定义表漏了 `buzzer_ctrl`**：
  《`CLAUDE.md` 与 `docs/02` 都把 §5 称作"接口的**唯一真值源**"，
  但它只写到 §5.10 `game_fsm`，**11 个实体只定义了 10 个**。
  漏掉的恰是提高项对应的 `buzzer_ctrl` —— 它在 §1.1 / §1.2 / §3.1 各处都露过脸，
  唯独在接口表里没有位置。
  **根因**：§5 一直是"往里查"用的（想问谁的端口就翻谁那节），
  **从来没有人从外面数过它够不够 12 节**。"11 个实体不重不漏"写在 §10 第 1 条里是**叙述**，
  当时只被应用到了框图上，没有被变成一条对每张清单都执行的核对动作。
  修法：补 §5.11 + 在 `docs/02` §15 加「接口定义」列使覆盖度可数
- **`CLAUDE.md` §10.3 模板里的 `clk_gen` 端口写错了**：凭印象写成
  `i_clk` / `i_rst` / `o_por_rst`，真实端口是 `clk` / `sys_en` / `o_rst`
  （且**没有 `o_por_rst`** —— 上电复位是内部信号，由 `o_rst` 一并输出）。
  核对 `docs/01` §5.1 后改正。**这个错本身就是 ERR-0010 的导火索** ——
  凭印象写端口，一核就露馅

> **本次最值得记的一条**：ERR-0010 这个缺口是被"**换一种写法**"逼出来的。
> 直接例化时顶层不需要写端口表，缺口可以一直不被触碰；
> 改成 `component` 之后，每个模块的端口都必须在顶层**显式出现一次**，
> 于是"少一个模块"立刻变得可见。
> **形式改变让隐藏的缺口自己浮了上来** —— 与 §12.4 的经验同源：
> **让正确的做法变成固定动作，错误才会自己浮上来。**

---

### 2026-09-17 · 第 9 次会话 · 接口全量核对与修复（阶段 1 真封版）

> **本次是一次全量交叉核对**：上一轮定为 `component` 语法后，
> 实战演练"照 `docs/01` §5 抄端口"两遍，结果一路撞出 **5 处输入端口没有驱动源**、
> **6 处代码片段照抄编译不过**、**DWG-02 漏画 3 条连线**。
> 用户拍板"**现在一次修完，阶段 1 才算真封版**"，故本次把三处一起改干净。

**Added**

- **`docs/01` §2.1 连接表重写**：由"11 条"改为
  **「2 条系统级 + 12 条子系统间」共 14 条**，每条写清**方向 / 信号名 / 含义**
  （★ 第 10 次会话更正：0 号那行也是一条线，实为 **2 + 13 = 15 条**），
  并补第 4 条 `rnd_step`（**方向极易画反的一条线**，单列说明为什么必须由 `puzzle_ctrl` 发起）
- **`docs/01` §10.2「输入端口必须有驱动源」**：把这次核对固化成**可数动作** ——
  遍历方向**必须从线到模块**（数 §5 里每个 `i_*` 有没有发出方），
  并列出本次查出的 5 处缺口表 + 建议脚本化 `scripts/check_ports.py`
- **`docs/02` §12.2 键号译码表 + `case` 片段**：键 2/4/5/6/7/8/12 → 上/开始/左/下/右/选择/确认
- **`docs/02` §12.9 新增验证点 11~14**：译码表全覆盖 / 方向键在非 `S_PLAYING` 态无效 /
  `o_sound_trig` 脉冲形状 / `i_key_press` 与连发的区别
- **`ERRORS.md` 新增 ERR-0011 / ERR-0012 / ERR-0013**（本次三项发现的完整五段式记录）

**Changed**

- **`docs/01` §5 接口表 6 处补齐**（RTL 一行未写，改动零成本）：

  | 节 | 改动 |
  |---|---|
  | §5.6 `piece_rom` | 补 4 个输出（相对掩码 / 高 / 宽 / 有效） |
  | §5.8 `puzzle_ctrl` | 补 6 个输入（目标掩码 / 零片形状 ×3 / 操作键 ×3）+ 改 1 个输出 |
  | §5.9 `disp_format` | 端口加宽：`i_game_cnt`(6) → `i_game_cnt_bcd`(8，两位 BCD) |
  | §5.10 `game_fsm` | **删 6 增 6** —— 删 `i_game_cnt` / `o_union` 等，增 `o_sound_trig` / `o_sel_out` / `o_conf_out` / `o_dir_out` / `o_seed_load` 等 |

- **`docs/02` 同步 20 个小节**：§8.2~§8.5、§10.2~§10.8、§11.2/§11.4/§11.6、§12.2、§12.6~§12.9、§14.1.2
- **`docs/02` §11.4 重写为「本模块不做二进制 → BCD 转换」**，
  附三行成本对比表；§12.6 新增 ⭐ 说明
  **常量折叠（`T_LEVEL2 / 10`）不生成除法器** —— 除法器只在被除数是**信号**时才综合出来
- **`docs/02` §14.3 提高项对照表补 2 行**（提高② 多图案随机 / 提高① 蜂鸣器音效）

**Fixed**

- **ERR-0011 — 5 处"输入端口没有任何驱动源"**（详见记录）：
  其中最刺眼的是 `buzzer_ctrl.i_trigger` —— 一个 1 位端口没人驱动，
  代价是**整项提高要求静默失效**（蜂鸣器永远不响）；另有
  `puzzle_ctrl` 的 `i_sel`/`i_conf`/`i_dir` 无人驱动（**三个操作键全部失效**）。
  用户拍板：`game_fsm` 增加 `o_sound_trig`；键号译码**放 `game_fsm` 内**
- **ERR-0012 — `docs/02` 的 VHDL 片段 6 处照抄编译不过**：
  ① `process (all_inputs)` 是 **VHDL-2008**；②③④⑤ `PIECE_MASK` / `ANCHOR_ROW/COL` /
  `r_sel = i` / `o_union`（4 处符号不存在或类型错）；
  ⑥ ⭐ **`r_locked and VALID_MASK = VALID_MASK` 运算符优先级错** ——
  VHDL 的关系运算符比逻辑运算符**紧**，实际被解析成 `r_locked and (V=V)`，
  括号里是 boolean → 类型错误。**这一项"看着完全合法"，最贵**
  - §10.5 整段重写：显式敏感表 + `variable` + `for i in 0 to 3 loop`（**循环界是常量**）
  - §10.7 整段重写：由"先无条件加减、再补救"改为 **"先判断、再决定改不改"**
    （原来的"减过头再补救"在 VHDL 里**根本无法表达** —— 下溢已经发生了）
  - §10.8 整段重写：**死循环** `exit when r_sel < 0`（2 位无符号**永远不成立**）
    改为 `for k in 0 to 3 loop … exit when …`，并用**变量**做迭代
    （进程里读信号读到的是旧值，循环每轮会算出同一个数）
- **ERR-0013 — 图与表对不上**：
  - DWG-01 三条连线标签与 §2.1 不符（最常见的一条：`游戏控制 → 拼图核心` 写成
    `level · round_start`，**而 `level` 根本不去 `puzzle_ctrl`**）
  - **DWG-02 漏画 3 条连线**：`game_fsm → puzzle_ctrl`（操作键入口）、
    `puzzle_ctrl → rng_lfsr`（`rnd_step`，**本项目最危险的那根线，实体级图上原本不存在**）、
    `game_fsm → piece_rom`（`level`）
  - `key_press` 标签被 x=320 的竖线压掉最后一个字符 ——
    **ERR-0009 的同类问题第三次出现**，由 `CLAUDE.md` §12.4 第 3 步（渲染放大）抓到

**Docs**

- `HANDOFF.md` §2/§3/§4/§5 更新至第 9 次会话
- `PROGRESS.md` 模块进度表、关键决策表、断点更新
- `CLAUDE.md` §12.4 补充：**对账必须做两遍、方向相反**
  （「表→图」查不一致，「图→表」查漏画）—— 本次的漏画就是只做一遍时漏掉的

> ⭐ **本次最值得记的一条**：ERR-0010 / ERR-0011 / ERR-0013 的根因**是同一个** ——
> **"缺"这个错误，只有在换一个遍历方向时才看得见**
> （通用动作见 `CLAUDE.md` §12.4 第 5 步，三条现象的完整叙述见 `ERRORS.md` ERR-0013）。
>
> 流程收益：§12.4 清单本次又抓到一处（`key_press` 压线）；
> 但它也暴露了自己的盲区 —— 默认"图是完整的、只是可能有瑕疵"，
> **不覆盖"完整性"这一维**，故漏画的 3 条线它管不到。盲区已补为清单的**第 5 步**。

---

### 2026-09-17 · 第 10 次会话 · 全项目复查：系统设计与交接文档（阶段 1 复查封版）

> **起因**（用户原话）："**最后仔细审查系统设计和交接文档，可以分派多个 agent 进行整个项目的核查，
> 文档不要重复堆积而是结构和内容准确清晰，确认无误后提交推送。**"
> 所以这次**不是"再写点什么"，而是专门找矛盾、专门删重复**。
> 结果：`docs/01` 抓到 **14 处**内部不一致（ERR-0014）、图侧修 3 处、
> 5 份过程文档做了一轮去重（**净减约 40 行**）。

**Added**

- **`ERRORS.md` 新增 ERR-0014 / ERR-0015**：前者记录 `docs/01` 的 14 处不一致及其两类根因，
  后者记录无头 Chrome 的两个渲染陷阱（它直接威胁 `CLAUDE.md` §12.4 第 3 步的可信度）
- **`docs/01` §2.2 新增"要查什么 → 去哪里"索引表**（4 行）：
  把"该去哪张图 / 哪张表找什么"固化下来，替代原先那张会漂移的 ASCII 拷贝
- **SVG 裁切 + 放大的看图链路**（`viewBox` 改区域 + `scale(2.6)`）：
  **这是 `CLAUDE.md` §12.4 第 3 步"放大 2.5~3 倍逐段看"的可执行形式**。
  ⚠️ 实现它的三个脚本住在 `.tmp/`，而 `.tmp/` 是 `.gitignore` 明列的临时区（随时可删）——
  **记下来的是参数与做法，不是路径**（理由见 ERR-0015 结尾的 📌）

**Changed**

- **`docs/01` §2.1 / §2.1.1 / §2.1.2 三处关于 `clk` 的叙述统一为"分层画法"**：
  DWG-01 **不画** `clk`（它是 `clk_gen` 子系统的属性，折进椭圆小字），
  DWG-02 用**脚注逐个列出 8 个接收者实体名**。三处原先各说各话（直接打架），现已一致
- **`docs/01` §4.2 的 `tick_2hz` 用途更正**为"开机自检的 **2 秒计时**"，
  并新增 ⚠️：**`o_blink` 是 `tick_100` ÷25 ÷2 得到的方波，不是拿 `tick_2hz` 当方波送**
  （`tick_*` 全是单周期使能，直接当方波送出实际只有 **1Hz**）
- **`docs/01` §6.5 的 `i_rel_mask` 类型更正**：`std_logic_vector(15 downto 0)` → `mask_arr_t`
  （= `array (0 to 3) of std_logic_vector(63 downto 0)`）；
  同时订正 ② —— 真正的坑是**整数与位矢量不能直接比较**，不是"位矢量不能作下标"
- **`docs/01` §9 引脚数**改为 **51（`puzzle_top`）／67（含 LED）**，
  并注明 `ld[15:0]` **只属于 `board_test_top`**，两个顶层不会同时存在
- **`docs/图/系统图.html` 三处**：
  - DWG-01 节拍总线标签去掉 `clk`（改为`节拍总线（不含 clk）`），并加脚注说明 `clk` 画在 DWG-02；
  - DWG-02 **所有时序实体框内小字一律以 `clk` 开头**，脚注逐个列出 8 个 `clk` 接收者
    与 3 个纯组合实体；`game_fsm` 框加宽 **180 → 200**（加 `clk` 前缀后副标题溢出了框）；
  - DWG-03 的"任意状态按开始"两条回边**改画为直接回 `S_PREVIEW`**（不经过 `S_IDLE`），
    与 `docs/00` 要求 11 的判语"立即回到第一关的预览态"对齐
- **`CLAUDE.md` §0 收工流程新增第 0 步**（由用户补入）：**`HANDOFF.md` §4 只保留最近 2~3 次会话**，
  更早的删掉，历史去 `CHANGELOG.md` 查 —— **交接单是"现在"，不是"档案"**。
  据此把 `HANDOFF.md` §4 从 4 条（第 7~10 次）**删到 3 条**（第 7 次那条已在本文件存档）

**Fixed**

- **ERR-0014 — `docs/01` 内部 14 处不一致**（每条都由 `.tmp/fix01.py` 校验命中次数后批量更正）：
  §1.1.3 补要求 6 / §1.2 门控代码块换成顶层真实信号名（原来用的是并不存在的 `dot_row` `seg` `buzz`）/
  §2.1 风险表引用由不存在的"§9"改指 `CLAUDE.md` §9 /
  §3.1 `disp_format` 职责补"**点阵像素多路选择**" /
  §6.3 伪代码标注 ⚠️"不可照抄"（循环上界来自信号，VHDL-93 要求常量）/
  §7 小结图补 `seed` `rnd_step` `target_mask` `pattern_mask`
  —— **其中 10 处属于"同一份信息写了两遍、只改了一处"**
- **`docs/01` §7 的 ASCII 小结图修了一处逻辑矛盾**：该树干有 4 个分支，
  第一个却写成 `└──▶`，而下一行同列又跟着 `│` —— `└` 表示树干终止、`│` 表示它继续，
  **两行自相矛盾**，改为 `├──▶`；另把两行续行的 `◀` 左移 1 格对齐
- **`.tmp/fix01.py` 自身两处报错**（`SyntaxError: 'break' outside loop`、
  `repline() missing 1 required positional argument`）——
  因为**写盘在脚本末尾**，两次失败都**没有污染文件**，修好后一次跑通

**Removed**

- ⭐ **`docs/01` §2.2 那张 60 行的 ASCII 框图整段删除** ——
  它是 §2.1 连接表的**第二份拷贝**，且已经漂移（少 `seed/seed_load`、少 `pattern_mask`、
  把 `puzzle_ctrl` 直连到了 `dot_matrix_scan`）。删掉后替换为一张 4 行的"要查什么 → 去哪里"索引表。
  **这是本次"不要重复堆积"最直接的一处落实。**
- **`PROGRESS.md` 的「下一步任务清单」节**：改为指向 `HANDOFF.md` §3 的一行指针
  （两处都写必然不同步）
- **`HANDOFF.md` §5 抄了 14 行的铁律全文**：改为指向 `CLAUDE.md` §2 的指针 + 只留最容易忘的 4 条
- **`PROGRESS.md` 关键决策表里"仿真方案"/"仿真路线"/"仿真脚本语言"三行合并为一行**
  （三行说的是同一件事，只是写于不同会话）

**Docs**

- ⭐ **本次复查的方法论（最值得带走的一条）**：
  **封版只保证"改完了"，不保证"自洽了"** —— 两者之间还需一次**专门找矛盾的遍历**。
  14 处里 10 处的根因是"同一份信息写两遍、只改了一处"，
  **这类错误在通读时看不见**（相隔两百多行的两句话各自都通顺），
  只有把**同一事实的所有副本并排摆在一起**才现形（见 ERR-0014 的定位过程）。
- **`ERRORS.md` 的模板标题由 `## ERR-0001 <一句话标题>` 改为 `## ERR-XXXX <一句话标题>`** ——
  原先它与真正的 ERR-0001 撞名，用 `grep '^## ERR'` 数记录条数会数出 **14** 条（实为 13 条）。
  **能跑的计数动作必须是准的**（与 `CLAUDE.md` §10.3 同源）
- 5 份过程文档的编号 / 条数订正：`13 个模块` → `11 个功能实体`、`四条写法要求` → `五条`、
  `11 个小节` → `18/20 小节`、连线表 `2 + 12 = 14 条` → `2 + 13 = 15 条`
- `HANDOFF.md` §2/§3/§4 与 `PROGRESS.md` 全部更新至第 10 次会话

---

### 2026-09-18 · 第 11 次会话 · 规划审查与完善（第三根轴：可执行性）

> **起因**：用户要求审查前期规划的合理性、评估模块化结构与接口是否符合行业标准，
> 对可优化处直接完善（当前环境为 macOS，无法跑 Quartus，审查聚焦规划与文档层面）。
> **这是继第 9 次（接口轴：文档↔代码）、第 10 次（一致性轴：文档↔文档）之后的第三根轴
> —— 可执行性轴：规划里的内容能不能直接照抄执行。**

**Added**

- **`scripts/check_ports.py`：接口连通性检查脚本** —— 把 `docs/01` §10.2
  「输入端口必须有驱动源」的人工核对动作脚本化（该项目原计划在阶段 4 前完成，提前落地）。
  解析 `docs/01` §5 的 12 张接口表，集合相减查"悬空输入 / 无接收者输出"；
  改名线用 `ALIAS_IN` 显式登记（与 `docs/01` §5.7 别名一节对应）。
  **当前结果**：11 实体 / 57 输入 / 44 输出全部连通；`--selftest` 注入两类假端口均检出、正例零误报
- **`docs/03-仿真验证方案.md` 框架**（原排在阶段 2 第一步）：仿真策略（已实测链路 +
  `CLK_HZ` 缩放 + 功能/时序仿真分工）、标准验证循环、波形判读五项规范、
  12 张结果记录表（留空待填）与顶层 12 场景表 —— **只索引 `docs/02` 的验证点，不复制内容**
- **`WIN_MASK` / `FAIL_MASK` 位图与掩码定稿**（`docs/02` §2.5/§7.4）：
  对勾 15 格、叉 24 格，笔画 2 格宽；由 `verify_tiling.py` 的
  `cells_to_mask()` / `mask_to_vhdl()` 生成，并做"字面量 → 反解 → 位图"往返自检

**Fixed**

- **ERR-0016 — `docs/02` §13.4 音效骨架两处缺陷**：
  ① 半周期计数 off-by-one —— 装载 N 再数到 0 是 N+1 拍，实际频率 `8000/(2(N+1))`，
  **每种音效全错且不易察觉**；查表改存 N-1；
  ② `HALF_PERIOD` / `DURATION` 查表**只有名字没有定义**、交替音无可综合实现 ——
  补全 `HALF_A` / `HALF_B` / `DUR` / `SEG` 四张模块私有查表、段计数器 `r_seg` + 相位位 `r_alt`、
  全部寄存器声明（含 `range`），并增加**触发时锁存 `i_sound_sel`** 防播放中途音调跳变
- `docs/02` §12.9 验证点编号重复（两个 12）→ 顺次改为 13 / 14 / 15
- `docs/02` §12.5 补注：倒计时 30/40 的**装载点**在状态进程 `S_PREVIEW → S_PLAYING`
  迁移分支（原文只写了"减"没写"装"，阶段 4 易漏，漏了倒计时恒为 00）
- `docs/02` §7.4 原是**两套候选位图并排混写**的编辑残留（哪份是哪个看不出来）→
  重写为定稿版单图 + 设计依据

**Changed**

- `docs/01` §5.0 的 ⚠️ 注记（WIN/FAIL 未定稿）改为 ✅ 已定稿；
  §10.2 的"应当脚本化"注记改为 ✅ 已脚本化（含当前检查结果）
- `README.md` / `PROGRESS.md` / `HANDOFF.md` 同步：
  `docs/03` 状态 ⬜→框架已搭；`check_ports.py` 从未写→已完成；
  下次会话第一件事改为**直接写 `rtl/puzzle_pkg.vhd`**

**Docs**

- ⭐ **本次审查的方法论（最值得带走的一条）**：
  **"能照抄执行"是规划的第三根轴。** 前两次遍历比的是"两份事实是否一致"，
  这次比的是"骨架里的每个符号能不能在本项目里找到定义、每句意图能不能落到计数器语义"。
  ERR-0016 的两处缺陷（off-by-one、空名查表）在前两根轴上**都不可见** ——
  它们各自内部自洽、也没有第二份拷贝可比。
- **三轴至此走完，文档侧不再开新工作**；下一根轴只能是"代码 ↔ 文档"（阶段 2 起由编译器与仿真器执行）

---

### 2026-09-18 · 第 12 次会话 · 课件 PDF 全量解析与合规补全

> **起因**：用户要求"**严格按照老师的 PDF 上的要求完善文档，只修改必要的内容**"，
> 并指出当日早前把 BTN0 复位删掉了（改成仅 POR+SW7 两源），需要按课件恢复。
> **本次是继第 9 次（接口轴）、第 10 次（一致性轴）、第 11 次（可执行性轴）之后的第四根轴
> —— 符合性轴：文档与"课程课件"这个外部权威对不对得上。**

**Added**

- ⭐ **BTN0 按键复位全链路**（课件 p45（一）"复位必须用按键来实现"）：
  `btn` → `clk_gen.i_btn_rst` → **2 级同步器 + 20ms 消抖** → `r_btn_rst`，
  与 `r_por_rst`、`(not sys_en)` **三源或**得到 `o_rst`。
  落点：`CLAUDE.md` §4.6/§10.1/§10；`docs/01` §2.1（系统级连线 **2→3 条**）+
  **新增 §2.1.3** + §4.1/§5.0/§5.1/§5.10/§8.1/§8.2/§9/§10.1；
  `docs/02` §2.6（增 `DEBOUNCE_BTN`）/§3.1~§3.6/§12.4；
  `docs/图/系统图.html` 的 DWG-01 与 DWG-02 **各补一笔**；`.qsf` 加 `PIN_61`；
  `check_pins.py` 加 `btn`；自检工程增**阶段 9**
- ⭐ **`CLAUDE.md` §11 重写为「考核与提交要求对照」**：补入课件 p3 成绩构成
  （平时 20% / 验收答辩 50% / 报告 30%）、p4 报告 8 项**原文**、
  p65 **提交物**（含此前完全没列入计划的 **"工作视频"**）
- ⭐ **`CLAUDE.md` 新增 §11.4「课件设计类要求对照表」**：把课件的通用强制要求
  （p45·p46·p47·p48·p49·p50·p51·p54·p55·p58·p59·p60·p62·p63·p64·p65 + p22·p30-32·p13-19）
  **逐条钉到本项目的落点**，并标出 2 项未闭合的 ⚠️ 待办
- **`.qsf` 新增 `OPTIMIZATION_TECHNIQUE AREA`**（课件 p50 例4）——
  此前**一条都没有**，是本次从纯图页里捞出来的硬指示
- **`CLAUDE.md` §5.3 新增"仿真必须含中间信号"专条**（课件 p59 +
  Quartus `Node Finder` 的 `Registers: post-fit` 过滤器）
- **`CLAUDE.md` 新增 §8.1**：课件 p60/p63/p64 的实验板与开放实验注意事项
  （`USER_BTN` 切档、蜂鸣器 `102` 电位器、预约/签到时间窗、**每班仅 3 块板**）
- **`CLAUDE.md` §10.2 补课件出处**：①~⑥ 与课件 p46~p50 逐条对上，
  并新增 **⑥ Quartus 优化目标 = Area**、补入 p49 的 **`case` 替代长 `if-then-else`** 规则
- **`docs/02` 新增 §12.3.1**：声明 `game_fsm` 为 **Moore 型 + 双进程（形式 1）**
  （课件 p30–32 必问项，此前全项目**一次都没声明过**）

**Fixed**

- **ERR-0017 — 上次改到一半中断留下的 7 处残留**：最严重的是
  `CLAUDE.md` 引用了**不存在的 `ERR-0018`**（悬空引用），以及
  `CLAUDE.md` §4.6（三源）与 §10.1（两源）**自相矛盾**、
  `docs/01`/`docs/02`/图 三处**接口真值源完全没动**
- **ERR-0018 — 复位被按"文档内部一致性"删成两源**，违反课件 p45（一）。
  根因：裁决冲突时按"哪边改动小"选，而不是按权威来源
- **ERR-0019 — DWG-01 时钟椭圆副标题溢出边框**：
  `50MHz · 5 档单周期节拍 · 复位产生` 一行约 190 单位宽，
  而椭圆在副标题基线处可用宽仅 166 → **文字比椭圆本身还宽**。
  `check_svg.py` 全绿（该缺陷属脚本已知盲区）。拆成两行后各 139 / 50 单位，均已复核
- **"异步断言、同步释放"用词错误**（`CLAUDE.md` §4.6 / `docs/02` §3.5）：
  本设计 `o_rst` 是组合输出、**只被各模块在 `rising_edge(clk)` 内采样**，
  断言与释放**都在时钟沿** —— 是纯粹的**同步复位**。已改为统一口径

**Changed**

- **`docs/01` §2.1 系统级连线 2 条 → 3 条**（新增 `btn`），**全图连线数 15 → 16**；
  同步更新 `CLAUDE.md` §12.4 第 5 步的"直接数这个数"
- **`docs/04` 引脚总数 67 → 68**（`puzzle_top` 51 → 52）；§7.1 检查项 9 → **10 条**；
  §7.2 硬件事实 7 → **8 项**、自检阶段 8 → **9 个**；§9 三方交叉验证表补 BTN0 行
- **`docs/04` §3.3 键号布局表补课件 p55 的独立复核**：课件示例表逐条代入公式全部吻合
  → 与上一会话的更正**互为第三方证实**
- **`docs/01` §4.4/§4.6、`docs/04` §4 补课件 p55 键盘极性矛盾的裁决说明**
  （以板手册为准、以上板自检为最终依据）
- `docs/01` §8.2 自检**阶段 9** = 按键复位的唯一实测点（按住 BTN0 全亮、松开后自检重启）

**Docs**

- ⭐ **本次最值钱的一条教训：PDF 里的图形信息不能靠文字提取。**
  65 页里 **13 页**用 `pypdf` 提取出来是**空的**（p1/5/6/19/21/22/37/41/47/48/50/52/53/57），
  而**恰恰有 4 条实质要求只存在于图里**（p50 的 Area 优化、p47 的级联分频、
  p48 的 latch 示例、p46 的资源共享示例）。
  本项目 `AI_LOG.md` **第 1 次会话就记过这条**，本次**原样复发** ——
  已固化为动作：**提取完先数"空页"，空页必须渲染后逐页看。**
- ⭐ **"没读到"和"没有"是两回事。** 第一轮解析后判断"课件正文不含题目 4 的细则"，
  该结论**只对文字层成立**。**报告"某文档没写 X"之前，必须先确认自己读全了。**
- ⭐ **符合性轴的价值**：前三次遍历比的都是"项目内部两份事实是否一致"，
  这一次比的是"项目与**外部权威**是否一致" ——
  ERR-0018 那处错误在**前三根轴上全都看不见**（当时的文档是自洽的，
  甚至可以说是"因为太自洽才错的"）。

---

### 2026-09-18 · 第 13 次会话 · 进代码前的系统性审查（第五根轴：照着做能不能跑）

> **起因**：用户要求"**创建一个多子 agent 协作的 workflow，对整体规划与设计文档进行系统性审查，
> 确保在进入具体代码与硬件实现之前排除严重错误和隐患**"。
> **前四轮比的是"两份事实是否一致"，本轮比的是"照着做能不能跑" —— 两者能看见的东西完全不重叠。**

**Added**

- ⭐ **`docs/审查报告-2026-09-18.md`（729 行）** —— 完整报告：
  §1 十二条已验证的 CRITICAL/HIGH（逐条证据／后果／建议／**复核结论**）、
  §2 六条跨模块与集成问题、§3 四条完整性批评补充、§4 八十条 MEDIUM/LOW、
  §5 **未复核项与覆盖说明**、§6 八步行动清单。
- ⭐ **`ERRORS.md` 新增 ERR-0020** —— 按 ERR-0014 的先例，**一条记录覆盖一批同源缺陷**，
  只记根因与预防，逐条证据不重复（指向审查报告）。

**Changed**

- `HANDOFF.md` **表头**由「第 11 次会话」改为「第 13 次会话」——
  这正是本轮 `CRIT-4` 查出的问题：收工清单（`CLAUDE.md` §0 第 0 条）只要求改 §2/§3/§4，
  **没包括表头**，所以漏更新无人察觉。已把「表头」一并写进收工条目。
- 课件 PDF 复制进 `workbuddy` 工作树（仍被 `.gitignore` 的 `/*.pdf` 挡住，不会入库）——
  修复本轮最大的覆盖盲区：审查时 PDF 不在工作树内，
  "课件符合性"那一路 agent 只能读转述。

**本次审查的规模与结果**

| 项 | 数 |
|---|---|
| 子 agent | **34**（6 顶层 ＋ 13 模块 ＋ 12 复核 ＋ 1 集成 ＋ 1 批评 ＋ 1 汇总） |
| 原始发现 | 96 ＋ 集成 6 ＋ 批评 4 = **106** |
| 对抗性复核 | 12 条（上限 12）；**另有 4 条 HIGH 未复核** |
| 未复核 | 80 条 MEDIUM/LOW |

**五类拦路虎**（详见报告 §1）：① 脉冲宽度契约缺失；② 多重驱动出现在 3 个模块；
③ 顶层端口名无唯一真值源；④ 两处静默失效（种子 bit0 / `i_round_start` 触发时刻）；
⑤ 时序验证手段为零。

**Docs**

- ⭐ **第五根轴的方法论（最值得带走的一条）**：
  ERR-0020 的 12 处缺陷**在前四根轴上全部不可见** —— 当时的文档是自洽的。
  与 ERR-0014 的"不换轴，剩下的错误就是不可见的"完全同源。
- ⭐ **本轮给每一种根因都留了机制**：
  ① "契约从来没写下来" → 跨模块的量必须有唯一真值源；
  ② "改一处忘一处"的**第三种形态**（同一模块内两段代码的隐含约定，
  与前两种 —— 同文件副本 / 跨文件副本 —— 并列）；
  ③ **"修正"本身引入新错误**（`rng_lfsr` 那条：把本原多项式写成了"非本原"）——
  **自洽不等于正确**。
- ⭐ **"默认推翻"的复核是本轮质量的关键**：12 条里有**被降级**的（CRITICAL→HIGH）、
  有**事实被更正**的（图上实为 14 笔而非 15）。
  **没有复核的审查结论不能直接当行动依据。**
- ⚠️ **两个盲区如实记在报告 §5，未掩饰**：① 课件 PDF 当时不在工作树内（已修复）；
  ② 80 条 MEDIUM/LOW 全部未复核。另外本机**无 Quartus/ModelSim**，
  所有"可综合性"判断都是静态推断；**开发板手册未入库**，引脚号转录正确性本轮无法验证。

---

### 2026-09-18 · 第 14 次会话 · 按审查报告修缺陷

> **起因**：用户「继续修复」。按 `docs/审查报告-2026-09-18.md` §6 的顺序执行。

**Fixed —— 报告 §1 的 12 条「已复核」CRITICAL/HIGH，全部清零**

（逐条见 `ERRORS.md` ERR-0020 的进度表，此处不重复。）

**Fixed —— 已复核的其余 4 条**

- `sim-03`：`docs/03` **§2.1 新增**「补两个绑定步骤」——
  ① 把被测模块设为顶层、② 把本次 `.vwf` 设为向量源。**工程只有一个顶层、一个向量源**，
  不改就仿不到被测模块。两者都必须「用完还原」（`set_global_assignment` 会写回 `.qsf`）。
  `CLAUDE.md` §5.3 同步。
- `INT-4`：`puzzle_ctrl` 补散落序列器的 3 个状态声明（`r_seq`/`r_piece`/`r_try`），
  §10.3 触发器总数 **30 → 37**；并写明「**已放置并集**」（组合计算、随序列器增长）
  **不得复用 `s_union`** —— 复用会让求交恒非空 → 8 次尝试全废 → **位置随机静默退化成固定布局**。
- `INT-5`：资源估算改成**一张自洽的分解表**。原先 `docs/02` 单项 `place()` 估 ~300 LE、
  `docs/01` 全芯片估 ~450 LE，**两者相加已超 450**。现总量由三行分量相加得 **~600 LE**，
  并注明是估算、首次编译后必须回填订正。
- `CRIT-2`：**常量值只留 `docs/02` §2.6 一处**（18 个，附分组计数表）；
  `docs/01` §5.0 改为只列符号名。原两处带值且**已不对称**（§5.0 缺 7 个）。

**Fixed —— 从 80 条未复核项里挑出的「照抄编译不过」类**

> 挑这一类的理由：**编译错是客观可确认的**，不必等整轮对抗性复核。

- `seg_scan-01`：`seg_decode` 原先**只被调用、全项目无定义** → 补出函数签名与函数体，
  十个码值**逐条照 §5.4 的表**，含 `when others` 防 latch。
- `buzzer_ctrl-1`：`o_buzz <= not o_buzz` **读了 `out` 模式端口，VHDL-93 不允许** → 编译不过；
  且接上 `sys_en` 门控后读到的是门控值、**逻辑也是错的**。改为内部寄存器 `r_buzz` + 并发输出。
- `buzzer_ctrl-3`：`i_trigger` 的「边沿触发」口径**更正为「单周期脉冲 + 电平装载」**，
  并写明后果：**若脉冲宽于 1 拍会每拍重装、计时器永不推进 → 完全无声**。
- `puzzle_ctrl-3`：明确 `r_anchor`/`r_locked`/`r_sel` **必须单写者**，
  散落序列器落笔时要**并进 §10.7 那个进程**（按 `rst` > 散落 > 移动 排优先级）。

**Docs**

- ⭐ **本轮最值得带走的一条**：**「已复核」和「未复核」必须分开对待。**
  16 条已复核的照单修；80 条未复核的**只挑"客观可确认"的那一类修**
  （编译错、VHDL 语法错 —— 这类不需要对抗性复核就能判定），
  **其余一律不动**。把未复核项当已确认项去改，是拿"审查"当"事实"用。
- `HANDOFF.md` / `PROGRESS.md` 状态更新：由「**修硬伤四步完成前不要写代码**」
  改为「**已复核项已修完，可以进入阶段 2**」，并保留「80 条未复核、不要照着改」的警告。

---

### 2026-09-18 · 第 15 次会话 · 复核并修完报告 §4 的 80 条 MEDIUM/LOW

> ⚠️ **本节为补记**（第 16 次会话收工时补，见 `ERRORS.md` **ERR-0021**）——
> 第 15 次会话没走收工流程，只写了 `ERRORS.md`。以下据
> `ERRORS.md` ERR-0020 的进度表与 `git show db78064` 还原，**不在场的事不编**。

**Fixed —— 报告 §4 的 80 条 MEDIUM/LOW（第 14 次会话时还是"未复核"）**

- 做法：**10 个 agent 分批复核，每批 8 条，复核 agent 的默认立场是「怀疑」**。
  结果 **80 条 → 75 确认 / 1 推翻 / 4 已修 / 0 存疑**（36 MEDIUM ＋ 39 LOW，**无 HIGH**）。
- 75 条确认项按「分组产出编辑指令 → 主循环串行施加并校验唯一性」全部改掉：
  **241 条编辑指令，226 条一次成功**，其余经重新锚定后补完。
- 受影响面（`git show db78064`：**11 个文件、889 增 / 218 删**）：
  `docs/02`（427 行）、`docs/01`（301 行）、`docs/03`、`docs/04`、`docs/00`、
  `docs/图/系统图.html`、`CLAUDE.md`、`ERRORS.md`、`scripts/check_pins.py`、
  `HANDOFF.md`、`PROGRESS.md`。
- 另给 `scripts/check_pins.py` 补了 **`--selftest`** ——
  `docs/04` §6 声称负向测试可复现，此前没有入口。

**Fixed —— `check_ports.py` 自己的一个静默误解析**

- 它原先只认 `### 5.N \`实体名\`` 一种标题写法，于是新增的 §5.12/§5.13
  匹配不上、被并进 §5.11 的 `buzzer_ctrl` —— **脚本照旧"通过"，结论却是错的**
  （与 ERR-0014「能跑的计数动作必须是准的」同源）。
- 同次新增「**顶层端口名 ↔ `.qsf` 的 `-to` 对账**」（报告 `hw-01` 要的护栏）。
  已做负向测试：把 §5.12 的 `seg` 改成 `o_seg`，立刻报
  `✗ 声明了顶层端口 o_seg，但 .qsf 里没有对应约束`。

**Docs**

- ⭐ **本轮最值得带走的一条**：**批量施加编辑会造出一类新错误 ——「同一意图被实现了两遍」。**
  多个 agent 各自独立地修同一条发现的不同侧面，两条编辑落到同一处：
  三条编辑各改各的、都"成功"了，结果却是**重复内容** ——
  `CLAUDE.md` 一行表格出现两次、DWG-02 的 `piece_rom` 标签被两套写法各画一遍
  （且其中一行压住了 x=800/840 的竖线）。
  **防法**：批量施加后必须做一次**重复内容扫描**（相邻重复行 ＋ 同句频次 ＋ 同坐标 `text`），
  并且**改图后仍要走 `CLAUDE.md` §12.4 第 3 步放大看** —— 那处压框正是渲染后才现形的。
- ⚠️ **本次收工未完成**：`HANDOFF.md` 表头 / §2 / §3 与 `CHANGELOG.md` / `AI_LOG.md`
  均未更新，由第 16 次会话代为补齐（`ERRORS.md` ERR-0021）。

---

### 2026-09-20 · 第 16 次会话 · 精简 `CLAUDE.md`

> **起因（用户原话）**：*"先优化 CLAUDE.md 的女人，精简化"*（"女人"是"内容"的笔误）。
> 用户批准 **档 2**，方案里定下判据 ⭐ **删「事实的副本」，留「陷阱的警示」**，
> 并加了一条安全约束：**不删任何小节标题，只压缩节内内容** ——
> 正是这条杜绝了 ERR-0017 那类"引用悬空"。

**Changed —— `CLAUDE.md`：1130 行 → 1009 行**

删掉「**事实的副本**」（别处另有真值源，留着就是第二个会漂移的真值源）：

- **§4.1** 13 行引脚表 → 7 行**纯极性**表 ＋ 抬头「**引脚号不在此列**」
  （引脚号真值源 = `quartus/puzzle.qsf`，可读版 = `docs/04` §2；硬约束 #3 本来就禁止它出现在别处）；
- **§4.3** 键盘位置图 → 6 行紧凑版 ＋ 指向 `docs/02` §12.2；
- **§4.4 / §4.5** 的图案位图与零片形状 → 指针（真值源 `docs/02` §2.5）；**⚠️ 关键结论一字不动**；
- **§10** 17 行模块表 → 2 行清单 ＋ 指向 DWG-02 / `docs/02`；**架构关键分界保留**；
- **§10.3** 那段 **48 行、内含完整 `clk_gen` 端口表**的 VHDL 骨架 → **12 行、不含任何端口的骨架**，
  并写明"这里不列任何具体端口"的理由（`component` 位宽写错可能只出 Warning、`port map` 漏写端口根本不报错）；
- 文末 14 行 `ERRORS.md` 字段模板 → 9 个字段名 ＋ 指向 `ERRORS.md` 开头。

删掉字面重复与零信息行：

- **§12.4** 里**逐字重复的一行表**（"线在、名不全"出现两次）——
  它正是 ERR-0020 记的那个「同一意图被实现了两遍」的物证；
- **§10.2** 里与 §11.4 完全同义的「课件 ↔ 本节」映射表；
- **§11.4** 里三行零信息行（`p47 例1` / `p47 例2` / `p48 例3`，内容都是"同上第 ① 条"）合并为一行。

压缩但**不删内容**：§4.6 的 BTN0 修订史 7 行 → 3 行（**判据原文一字不动**）、
§2.1 的三层 ASCII 块 → 一句散文、§5.3 的状态说明、§0 收工第 0 条、
§10.2 的两张分频位宽表（99 位 vs 33 位）→ 散文、§8 的"优先验证"清单。

术语更正：**四处"五段式" → "九字段"**（`ERRORS.md` 的实际字段是九个，不是五个）。

**Fixed —— `docs/01-系统设计.md` 一处会悬空的反向引用**

- §5.12 原文「引脚表见 `CLAUDE.md` §4.1 与 `docs/04` §2」，而 §4.1 已不再列引脚号
  → 改为「**引脚表见 `docs/04` §2**（唯一可读真值源；`CLAUDE.md` §4.1 只留极性）」。

**Docs —— 结果与不变式（如实记录，未向承诺的 ~800 凑数）**

| | 动刀前 | 实得 |
|---|---|---|
| 行数 | 1130 | **1009（−10.7%）** |
| 字符 | 37 828 | **36 481（−3.6%）** |

- 其中 **6 行是收工时新加的**：按 ERR-0021 的预防措施给 §0 补了
  「收工触发判据 = 这一轮有没有产生提交」——**精简本身到 1003 行**。
- **不变式全部守住**：`⚠️` **36 个一个没少**、`⭐` 原有 **26 个一个没少**（+1 新增）、
  **56 个小节标题全在**、本文件内 `§x` 自引用**零悬空**；
  `check_pins.py`（✓ 68 个引脚无重复）与 `check_ports.py`（✓ 顶层端口逐名吻合）仍为绿。
- ⚠️ **未达方案承诺的 ~800 行，差 209 行** —— 不是手软：按小节量完体量后逐节追问
  「这一行消失了会不会有人踩坑」，六个大节**全部答"会"**，它们不是冗余、是规则本身。
  **如实报数，不凑**（`ERRORS.md` **ERR-0022**）。
- 另：补记第 15 次会话一节 ＋ 修 ERR-0020 的两处过期状态（`ERRORS.md` **ERR-0021**）。

> ⭐ **本轮最值得带走的一条**：**"事实的副本"与"判断本身"必须分开对待。**
> 判据不是"别处有没有"，而是 **"这一行消失了，会不会有人踩坑"** ——
> `CLAUDE.md` 每次会话自动进上下文、`docs/*` 是按需读的，
> 所以"别处也有"**不构成可删的理由**。
> 同一条预防措施还纠出一个更基础的毛病：**数量目标也是"可数动作"，我先估后量了**
> （承诺 ~800、实得 1009）。**能数的事就用数，不要用估。**

### 2026-09-20 · 第 17 次会话 · 交付初步设计方案并补齐图集

**Added**
- **DWG-04 整机工作过程逻辑流程图** 与 **DWG-05 `game_fsm` MDS 助记状态图**：
  并入 `docs/图/系统图.html`（第 4、5 幅，共 3 图 → 5 图，版本 v3 → v4），
  矢量源与位图另存 `report/图/`（`.svg` + `.png`）
- **`report/初步设计方案.docx`**：初步设计方案交付物（6 章 / 7 表 / 3 图，全黑白正式排版，
  含封面与页脚页码）。生成链：Markdown 底稿 → HTML 定稿 → DOCX；
  中间产物在 `output/20260920-prelim-design-v2/`
- `report/图/图2-1-系统总体框图（第一层·功能子系统）.png`：白板稿按定稿口径修正后的交付用图

**Fixed**
- 白板框图（DWG-01 留档稿）与定稿口径的 **3 处冲突**：删去「拼图核心」框上多画的节拍引线、
  键盘消抖由 20 ms 改为 80 ms、显示节拍由 `tick_1k` 改为 `tick_8k`（`ERRORS.md` ERR-0023）
- 文档转换链两个集成缺陷：转换器丢弃 `<caption>` / `<figcaption>`（12 个表题、3 个图题全部缺失）；
  `<img>` 未给宽度时按自然像素插入（14 cm 版心塞进 133 cm 宽的图）（`ERRORS.md` ERR-0024）

**Docs**
- `CLAUDE.md` §12.1 图集表补 DWG-04/05 两行并注明矢量源位置
- `docs/02-模块详细设计.md` §12.3.1 删去「逻辑流程图 / ASM 图**不单独画**」，
  改为指向 DWG-04 / DWG-05
- `README.md` 文档索引补 5 张图与交付物
- `.gitignore` 新增 `output/`（文档生成流水线中间件，与 `.workbuddy/` 同类）

---

### 2026-09-21 · 第 18 次会话 · Typst 重排初步设计方案、文档规范化与敏感信息清理

**Added**
- **`report/初步设计方案.pdf`**：初步设计方案的 Typst 排版版（12 页 A4，正文 6 章 / 3 图 / 7 表），
  与 Word 版内容一致；已逐条核验图表编号、目录页码与全文完整性
- **`report/typst/`** 排版源：`template.typ`（版式模板：封面 / 目录 / 三级标题 / 三线表 /
  图表按章编号 / 页眉页脚）、`main.typ`（正文）、`covers.typ`（5 套封面方案）、
  `cover-compare.typ`（方案对比入口）
- **`report/封面方案对比.pdf`**：5 套风格不同的封面各一页，供挑选；
  正式版采用「方案 A · 北邮式居中」
- **`.gitattributes`**：文本统一以 LF 存储、显式标注二进制类型、修正 GitHub 语言统计

**Changed**
- `README.md` 重写：新增「项目状态」阶段表；「目录结构」补全 `docs/审查报告-2026-09-18.md`、
  `report/typst/` 与交付物；「文档索引」拆为设计文档 / 过程记录 / 交付物三张表；
  新增「许可与声明」（课程作业、课程原始资料不入库、硬件参数以手册为准）
- `HANDOFF.md`：表头更新至第 18 次会话；§2 补 PDF 交付物；§3 删去已完成的「两张图」待办，
  以及随之失效的「三处必须同步改」（图集与 `CLAUDE.md` §12.1 均已改完）—— 消除与 §4 的自相矛盾；
  §4 收敛为最近 3 次会话
- `PROGRESS.md` / `AI_LOG.md`：登记本次会话

**Fixed**
- ⭐ **`docs/审查报告-2026-09-18.md` 中 67 处本机绝对路径**
  （`/Users/<user>/WorkBuddy/Worktrees/<worktree>/`）改为**仓库相对路径** ——
  原路径泄露本机用户名与工作树结构（`ERRORS.md` **ERR-0025**）
- 同上文件中一处被截断的引用残片（`② /Users/…`）一并清除

**Docs**
- 正文去 AI 痕迹：删去 2 处提示框、破折号 `——` 由 10 处降为 0 处、
  「其一/其二/其三/其四」等长列举改写为自然段落；全文保持零 bullet、纯连续段落
- 标题层级按中文课程设计报告通例重定：一级三号黑体居中 / 二级四号黑体顶格 / 三级小四黑体左缩进 2 字；
  段前后留白按层级递增（2.05 / 1.58 / 1.25 em）

> ⭐ **本轮最值得带走的两条**：
> ① **Typst 的 `figure` 默认不可断页** —— 长表放不下时会整张后移，把上一页留出 46% 空白；
> 必须显式 `show figure.where(kind: table): set block(breakable: true)` 才会按行断开并重复表头。
> ② **图表的上下留白要"单一来源"** —— 原先题注自带留白（表题 1.0em / 图题 0.5em）与 figure 块留白两处叠加，
> 难以控制；改为**题注只管「题注↔图表本体」**，与正文的距离统一交给 figure 块的 `above`/`below`。

---


<!--
## [x.y.z] - YYYY-MM-DD
### Added
### Changed
### Fixed
### Removed
### Docs
-->

### 2026-09-23 · 第 19 次会话 · 阶段 2/3/4 编码全部完成，整机编译通过并收敛时序

**Added**
- ⭐ **`report/图/DWG-01-系统总体框图-含信号传递关系-定稿版.png`**（4034×2054）——
  **用户自己在白板工具里重画的定稿版**，已按老师要求标出主要信号名，**这是权威版本**。
  逐条核对 `docs/01` §2.1 后，尚缺 4 处建议补：`key_code[4:0]`、
  `sel · conf · dir[3:0]`、`all_locked · solved`、`sound_sel · sound_trig`（详见 `CLAUDE.md` §12.1.1）
- ⭐ **`report/图/DWG-01-白板版-含信号传递关系.png`**：在上一版白板图上用 `PIL` 叠加 **26 处小字信号名**
  （`sw7` · `btn` · `kp_row/kp_col[3:0]` · `key_code[4:0]/key_press` · `round_start · sel · conf · dir[3:0]` ·
  `all_locked · solved` · `pattern_sel · seed · seed_load` · `state · level · game_cnt_bcd · blink` ·
  `rnd_step` · `target_mask/rel_mask/height/width` · `px_red/px_green[63:0]` · `pattern_mask[63:0]` ·
  `seg/cat[7:0]` · `dot_row/dot_colr/dot_colg` · `sound_sel · sound_trig` · `buzz` ·
  `rst + tick_8k/1k/100/2hz/1hz`），白描边、不覆盖原图内容；生成脚本 `scripts/annotate_dwg01.py`
  （标注位置由**扫描原图暗像素量出**，不是估的；微调改 `L` 表即可）
- ⭐ **`report/图/DWG-01-信号传递关系.svg` + `.png`**：按老师要求重画的**矢量版总体框图** ——
  7 个功能子系统的框内逐条写明"子系统名 / 实体 / 功能"，**每条连线标注信号名**
  （与 `docs/01` §2.1 连线表逐条对应：4 条系统级 + 14 条子系统出线 = 18 条），
  另加"节拍与复位总线"说明框（列出每档 tick 的接收者，以及 S4/S5 不接节拍只收 rst）
  与图例/注释；新增生成脚本 `scripts/gen_dwg01.py`（改完重跑即可，避免图与表漂移）
- `rtl/` 下 **14 个文件全部写出**（此前为空）：`puzzle_pkg` · `clk_gen` · `keypad_scan` ·
  `seg_scan` · `dot_matrix_scan` · `pattern_rom` · `piece_rom` · `rng_lfsr` · `puzzle_ctrl` ·
  `disp_format` · `game_fsm` · `buzzer_ctrl` · `puzzle_top` · `board_test_top`
  - 全部 VHDL-93 子集 + `numeric_std`，注释全中文，文件头标注"所属子系统 / 对应需求"
  - 顶层一律 `component` 声明 + `port map` 具名关联（硬约束 12）
  - `puzzle_pkg` 的常量、位序、状态编码单点定义；图案/零片掩码照 `docs/02` §2.5 抄
- `quartus/puzzle.qsf`：源文件表补全为 14 个（原先只有 6 个）
- `docs/06-Quartus编译与烧录流程.md`：建工程 → 编译 → 引脚分配 → 下载烧录的完整分步操作，
  含实测资源/时序数据与常见报错处置
- `scripts/_gen_fitcheck.py`：按 `.qsf` 生成临时工程，**不动仓库**地验证任一顶层能否编译
- `scripts/encoding.py`：`rtl/*.vhd` 的 **UTF-8 ⇄ GBK** 双向转换（见下方 Fixed 第 1 条）
- `scripts/set_top.py`：一条命令在 `board_test_top` 与 `puzzle_top` 之间切换顶层
  （同时改 `TOP_LEVEL_ENTITY` 与 16 行 `ld` 约束，幂等、不写引脚号）

**Changed**
- ⭐ **`puzzle_ctrl` 的实现方式（面积换时间）**：4 路并行 `place()` 平移 + 掩码求交
  → **行扫描引擎**（4 块 × 8 行 = 32 拍刷一帧）+ **包围盒不相交判据**。
  整机 LE 从 **3461 降到 1226**（见 `ERRORS.md` ERR-0026）
- ⭐ **三处打拍修时序**：判定独立成一拍 + `o_pattern_sel` 寄存 + 图案参数寄存副本/判据寄存
  → Fmax 从 **38.4MHz 提升到 63.66MHz**（见 `ERRORS.md` ERR-0027）
- `clk_gen` 的上电复位改为数 `tick_1k`（19 位 → 4 位计数器），`puzzle_pkg` 相应把
  `CNT_POR` 换成 `T_POR_MS`
- `game_fsm`：`o_pattern_sel` 改为寄存输出；方向键连发到期后**重新装载** `REPEAT_PERIOD`
  （原写法清零后判据 `= 1` 永不再成立，连发只会出一次）

**Fixed**
- ⭐ **修掉 Quartus 里中文注释乱码**（ERR-0028）：Quartus II 9.1 的编辑器固定按系统
  ANSI(GBK) 解码源文件（12.0 起才 UTF-8，且 9.1 没有"以指定编码打开"选项），
  而 `rtl/*.vhd` 是 UTF-8 无 BOM → 中文注释全部乱码（**编译不受影响**）。
  新增 `scripts/encoding.py`（UTF-8 ⇄ GBK 双向转换，带往返安全校验 / 幂等 / 只碰 `rtl/*.vhd`），
  `rtl/` 14 个文件转为 **GBK**（替换 91 处 GBK 装不下的装饰符号）；
  转换后重新编译，两个顶层 **LE 与 Fmax 一字不差**（1226/1270、63.66MHz、439 LE）
- 修掉 4 处"看着合法、编译不过"的写法：`"000" & i_level` 类型不明确（`disp_format`）、
  1 位 `std_logic` 直接转 `unsigned`、`r_free` 用 `std_logic_vector` 却要 `+1`、
  `r_repeat_cnt` 被两个进程写（多重驱动）
- 修掉 `board_test_top` 编译时的两处未使用信号告警来源（保留端口连接、改用 `tick_100` 计时）

**验证结果（Quartus II 9.1 Build 222，EPM1270T144C5）**

| 顶层 | LE | 引脚 | Fmax | 结论 |
|---|---|---|---|---|
| `board_test_top` | 439 / 1270（35%） | 68 / 116 | — | 全流程编译通过 |
| `puzzle_top` | 1226 / 1270（97%） | 52 / 116 | **63.66 MHz** | 全流程编译通过、时序收敛 |

**Docs**
- `ERRORS.md` 新增 ERR-0026（放不进器件）/ ERR-0027（时序不收敛）/ ERR-0028（Quartus 中文乱码），
  三条都按九字段写，定位过程保留实测数值
- ⭐ **订正 `docs/06` 的一个事实错误**：**Quartus II 9.1 的编译产物直接写在工程目录 `quartus/` 下，
  不生成 `output_files/`**（那是 10.x 之后的默认）—— 原稿写成 `quartus/output_files/…`，
  用户实际编译后"找不到产物"。已补 §3.3 产物清单 + §3.3.1「经典时序分析器不读 `.sdc`，
  要看带约束余量请用 TimeQuest」

---
