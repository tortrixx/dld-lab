# CLAUDE.md — 题目4《简易拼图游戏的设计与实现》

> 本文件是给"未来每一次会话的 Claude"看的操作手册。
> 目标：**任何一次新会话，读完本文件 + `PROGRESS.md`，就能无缝续接项目。**

---

## 0. 每次会话的固定流程（最重要，先做这个）

### 开工时（按顺序）
1. 读 **`PROGRESS.md`** —— 弄清：当前阶段 / 模块进度表 / 进行中的工作 / 阻塞项 / **上次会话断点**
2. 读 **`CHANGELOG.md`** 最后一节 —— 弄清最近改了什么
3. 读 **`ERRORS.md`** 未解决的问题 —— 避免重复踩坑
4. 确认本次要做什么 → 需要动哪些模块 → 哪些文档要同步更新

### 收工时（缺一不可）
1. 更新 **`PROGRESS.md`**（含"上次会话断点"一句话，写清楚下次从哪继续）
2. 更新 **`CHANGELOG.md`**（本次新增/修改/修复）
3. 发现或解决了问题 → 写入 **`ERRORS.md`**（五段式，见下）
4. 本次用到 AI → 追加 **`AI_LOG.md`** 记录
5. 本次跑了仿真/编译 → **记下原始数据**（激励条件、实测值 vs 期望值、LE 占用、波形图）
   → 写入 `docs/03-仿真验证方案.md`、图片存 `docs/图/`（**要求见 §13.2，别只写"通过"**）
6. `git add` + commit（规范见 §6）

> ⚠️ 这四份文档不是"事后补写的报告素材"，而是**项目运行的一部分**。
> 报告评分项⑤故障分析 15分、⑦AI使用 10分 直接从 `ERRORS.md` / `AI_LOG.md` 取材，事后补写无效。

---

## 1. 项目一句话

在 MAXII 实验板（**EPM1270T144C5**）上用 **VHDL** 实现一个 8×8 双色点阵拼图游戏，全部开发流程基于 **Quartus II 9.1**。

**课程**：《数字电路与逻辑设计实验（下）》综合课题 · 题目4
**关键截止**：初步设计方案 **2026-09-24 24:00** · 第 9~10 周验收答辩

**已确认的实现范围**：11 条基本要求全部实现 + 三项提高（蜂鸣器音效、多图案随机、零片位置随机）。
**不做**：零片 90°旋转、方向随机。

---

## 2. 硬约束（不可违反）

| # | 约束 |
|---|---|
| 1 | 器件固定 **EPM1270T144C5**（MAX II 家族），工程文件在 `quartus/` |
| 2 | 目标时钟 **50MHz**（板载 USER_BTN 选到档位 7）。**分频常量单点定义**，禁止魔法数字散落各处 |
| 3 | **引脚绑定只允许出现在两个地方**：`rtl/puzzle_top.vhd` 的端口名 + `quartus/*.qsf` 的 `set_location_assignment`。其他任何文件不得出现引脚号 |
| 4 | 所有 **VHDL 注释用中文**，所有**文档用中文** |
| 5 | **模块划分必须与 `docs/01-系统设计.md` 的总体框图严格一致** —— 老师会照着框图随机抽一个模块提问代码和仿真波形 |
| 6 | `puzzle_top.vhd` 必须有**完整中文注释**（报告评分项⑧，10分） |
| 7 | VHDL 统一用 **VHDL-1993 子集 + `numeric_std`**（Quartus II 9.1 兼容性最稳）。不用 `std_logic_unsigned`/`std_logic_arith` |
| 8 | 一个文件一个实体，文件名 = 实体名 |
| 9 | **任何 `integer` 都必须加 `range` 约束**（如 `integer range 0 to 9`）。裸 `integer` 在 Quartus 里综合成 **32 位**，是最大的隐性浪费。详见 §10.2 |
| 10 | **设计自顶向下，实现自底向上**（两件事，别混为一谈，详见 §2.1）：设计时从系统逐层分解到实体；实现时先做并验证叶子模块，再逐层往上集成。**不允许"先写顶层再补子模块"** |
| 11 | **每一个模块都必须能独立仿真**。不能独立仿真的模块说明职责切分有问题，要重切 |

### 2.1 「自顶向下设计」与「自底向上实现」是两件事 ⭐

**这两个方向不是矛盾的，它们作用在项目生命周期的不同阶段：**

| | 方向 | 干什么 | 什么时候 |
|---|---|---|---|
| **设计** | **自顶向下** | 系统 → 功能子系统 → 模块 → 实体，逐层分解 | 阶段 1 已完成 |
| **实现** | **自底向上** | 先写叶子模块（驱动/数据）并逐个仿真通过，再往上集成 | 阶段 2~4 |

**为什么必须分清：**

- 说"**自底向上的依赖分层**"是**错的**——那是在用实现顺序描述设计方法。
  依赖关系确实自底向上（上层依赖下层），但**分层这个动作本身是从顶开始的**：
  先有"这是一个拼图游戏"，才有"要分输入/控制/显示"，最后才有 `seg_scan` 这个实体。
- 设计若真自底向上，就会**由下位模块的便利决定系统结构**，
  最后拼出来的东西没人能保证它满足需求——而需求恰恰在顶上。

**本项目的分层结果（三层，与 `docs/图/系统图.html` 的 DWG-01/02 严格对应）：**

```
第 0 层  需求            11 条基本要求 + 3 项提高        → docs/00
第 1 层  功能子系统      7 个子系统                      → DWG-01
第 2 层  模块（实体）    11 个实体 + 2 个顶层            → DWG-02
```

**报告里怎么写**：设计思路一节写"自顶向下逐层分解"，实现步骤一节写"自底向上逐模块集成"。
两者都出现、且说清各自作用在哪个阶段，才是完整的表述。

---

## 3. 目录导航

| 目录/文件 | 放什么 |
|---|---|
| `CLAUDE.md` | 本文件，工作约定 |
| `README.md` | 项目总览（给人看的） |
| `PROGRESS.md` | **跨会话进度状态**（最重要） |
| `CHANGELOG.md` | 变更日志，Keep a Changelog 格式 |
| `ERRORS.md` | 错误记录，五段式 |
| `AI_LOG.md` | AI 使用记录（工具/提示词/产出/修正） |
| `docs/00-需求分析.md` | 11 条要求逐条拆解 + 需求追踪矩阵 |
| `docs/01-系统设计.md` | 总体框图 + 模块划分 + 接口定义 ← **初步设计方案主体** |
| `docs/02-模块详细设计.md` | 每个模块的内部设计（老师提问主要看这个） |
| `docs/03-仿真验证方案.md` | 每个模块的激励设计 + 波形判读结论 |
| `docs/04-引脚分配表.md` | 引脚对照表 + qsf 约束对照 |
| `docs/05-硬件调试记录.md` | 实验室上板调试记录 |
| `docs/报告/实验报告.md` | 正式报告，按 8 个评分项组织 |
| `docs/图/` | 框图、仿真波形图、编译报告截图 |
| `rtl/` | VHDL 源码 |
| `sim/` | `.vwf` 激励文件与仿真输出 |
| `quartus/` | `.qpf` / `.qsf` 工程与引脚约束 |
| `scripts/` | TCL 与 Python 自动化脚本 |
| `report/` | （预留）导出物 |

> 根目录下三份课程 PDF 已在 `.gitignore` 中，**保留在本地作参考，不入库**。

---

## 4. 硬件速查（高频引用，不必每次翻 PDF）

### 4.1 引脚分配表

| 功能 | 信号 | 引脚 | 有效电平 |
|---|---|---|---|
| 全局时钟 | CLK | 18 | — |
| 点阵行 | ROW0~ROW7 | 8,7,6,5,4,3,2,1 | **低有效** |
| 点阵红列 | COLR0~COLR7 | 22,21,16,15,14,13,12,11 | 高有效 |
| 点阵绿列 | COLG0~COLG7 | 45,44,43,42,41,40,39,38 | 高有效 |
| 数码管段 | AA,AB,AC,AD,AE,AF,AG,AP | 62,59,58,57,55,53,52,51 | 高有效 |
| 数码管位选 | CAT0~CAT7 | 63,66,67,68,69,70,30,31 | **低有效** |
| 键盘列 | COL0~COL3 | 117,118,119,120 | 扫描输出 |
| 键盘行 | ROW0~ROW3 | 111,112,113,114 | 读入，按下=1 |
| 蜂鸣器 | BUZZ | 60 | 音频方波 |
| 系统开关 | SW7 | 125 | 高=系统开 |
| 独立按键 | BTN0~BTN7 | 61,20,89,91,121,122,123,124 | **按下=1，弹起=0** |

### 4.2 关键位序与极性

- **数码管：DISP7 最左 … DISP0 最右**（实物丝印已确认）。`CAT7`→DISP7，`CAT0`→DISP0
- **点阵点亮条件**：
  - 红 = 行低 + COLR高 + COLG低
  - 绿 = 行低 + COLR低 + COLG高
  - 黄 = 行低 + COLR高 + COLG高
- **键盘实物**：丝印只有 `KEY1~KEY16`；**ROW3 在最上排、ROW0 在最下排**；COL0 最左、COL3 最右
  - 换算：`KEY号 = (3 - ROW) * 4 + COL + 1`
  - 列有 10k 上拉、行有 4.7k 下拉 → 扫描时**驱动某列为高，读行，读到 1 表示该键按下**
- **板载时钟档位丝印**：`0:NO CLOCK / 1:1Hz / 2:2Hz / 3:10Hz / 4:100Hz / 5:1KHz / 6:1MHz / 7:50MHz`
  - 开发板上需确认指示灯为档位 **7**

#### ⚠️ 引脚号 vs 位序 —— 别把两者搞混

**引脚号是板上丝印的物理网络名，不随你的信号命名改变。** 本项目一律以手册为准：

| 板上网络名 | 引脚号（手册原文） |
|---|---|
| COLR0…COLR7 | **22、21、16、15、14、13、12、11** |
| COLG0…COLG7 | **45、44、43、42、41、40、39、38** |

也就是说 **`dot_colr(0)` 必须接 PIN_22，`dot_colr(7)` 接 PIN_11**。

曾经踩过的坑：桌面上另一个旧工程 `VHDL/dot_matrix_pwm` 里写的是
`col_r[0] → PIN_11 … col_r[7] → PIN_22`，**与板手册相反**。那个工程能用，
是因为它的扫描代码是按它自己的命名写的——**位序是自洽的，但和手册的标签反了**。
抄它的引脚表会得到"左右镜像"的点阵。

> **规则：引脚号只从手册拿；旧工程只能用来交叉验证"用到了哪些引脚"，不能用来确定位序。**
> 本项目 `quartus/puzzle.qsf` 已按手册写死，并已与手册逐条核对。

### 4.3 键盘功能映射（已定稿）

物理位置（ROW3 上 → ROW0 下）：

```
            COL0    COL1    COL2    COL3
ROW3:        -      上       -      开始
ROW2:       左      下      右      选择
ROW1:        -       -       -      确认
ROW0:        -       -       -       -
```

### 4.4 拼图图案定义（第一关）

**目标图案**：点阵 **第2~5行 × 第2~4列** 实心 4×3 矩形（12 格）

**三块零片形状**（上→下、左→右）：

```
零片1 (6格)     零片2 (3格)     零片3 (3格)
X X X           . X             X X X
X X .           X X
X . .
```

**⚠️ 关键结论（已穷举验证）**：这三块恰好有 **2 种合法铺法**，两种都必须判为**成功**。
因此**成功判定必须是"三块并集 == 目标掩码 且 两两无重叠"**，
**绝不能**写成"每块回到各自初始位置"——那会把其中一种正确铺法误判为失败。

**穷举验证脚本**：`scripts/verify_tiling.py`（**修改任何图案常量前必须先跑它**）

### 4.5 第二关图案（自拟）

**目标图案**：向上箭头，18 格，占第 1~6 行。行掩码（二进制，bit7=最左）：

```
行0: 00000000
行1: 00011000
行2: 00111100
行3: 01111110
行4: 00011000
行5: 00011000
行6: 00011000
行7: 00000000
```

**四块零片**（6+6+4+2 = 18 格，与图案格数一致）：

```
零片0 (6格)   零片1 (6格)   零片2 (4格)   零片3 (2格)
. . X         X . .          X X           X X
. X X         X X .          X X
X X X         X X X
```

另有**向下箭头**作为第二套图案（同形状零片旋转 180°），用于实现"多图案随机选择"这一提高项——几乎零额外成本。

### 4.6 独立按键（BTN0~BTN7）

**手册原文**（`附图16 按键模块`）：

> 实验板上一共有 8 个按键，标示为 **BTN0~BTN7**，依次使用 EPM1270T144C5 芯片的
> **61、20、89、91、121、122、123 和 124** 脚。
> **按键平时输出低电平，按下去输出高电平，放开后自动弹起又输出低电平**，
> 在 BTN7 左下角有一个波形示意图。**按键在使用时需设计防抖动电路。**

| 项 | 值 |
|---|---|
| 引脚 | BTN0=61、BTN1=20、BTN2=89、BTN3=91、BTN4=121、BTN5=122、BTN6=123、BTN7=124 |
| 极性 | **按下 = '1'，弹起 = '0'**（高有效） |
| 消抖 | **必须做**（手册明确要求） |

> **⚠️ 这 8 个按键与 4×4 矩阵键盘（`kp_row`/`kp_col`）是两套完全独立的输入**，
> 不要混淆。游戏操作全部走矩阵键盘；BTN 只用于复位等辅助功能。
>
> **⚠️ 与 SW7 的区别**：SW7 是**拨动开关**（要求 1 规定 SW7='0' 时全部显示熄灭），
> BTN 是**按键**（按一下弹回来）。两者不是一回事。

**约定用法**：BTN0（`PIN_61`）作为**硬件复位键** —— 按下（1）复位，弹起（0）正常运行。

```vhdl
-- 复位是三个来源的"或"：硬件复位键 、 上电复位 、 SW7 关闭
o_rst <= btn_rst or por_rst or (not sys_en);
```

> 若后续不用 BTN 复位，只需删掉 `btn_rst` 一项，其余逻辑不变。

---

## 5. 构建 / 仿真 / 烧录

### 5.1 工具路径

```
QUARTUS_BIN = C:\QuartusII91\QuartusII91\quartus\bin
```

命令行工具：`quartus_sh` `quartus_map` `quartus_fit` `quartus_asm` `quartus_tan` `quartus_sim` `quartus_pgm`

### 5.2 常用命令

```bash
# 全流程编译（综合→布局布线→汇编）
quartus_sh --flow compile quartus/puzzle

# 分步
quartus_map quartus/puzzle --source=rtl/puzzle_top.vhd
quartus_fit quartus/puzzle
quartus_asm quartus/puzzle
quartus_tan quartus/puzzle          # 时序分析

# 仿真（需先在工程里设置好 Simulator 与 .vwf）
quartus_sim quartus/puzzle -c puzzle

# 烧录
quartus_pgm -c USB-Blaster -m jtag -o "p;quartus/output_files/puzzle.pof"
```

### 5.3 仿真方案（已实测跑通，2026-09-17）

**本机没有 ModelSim / GHDL**，只能用 **Quartus II 内置仿真器**。它的三条硬限制
（都已实测确认，不是推测）：

1. 仿真的是**综合后网表**，不是 RTL → **不支持 VHDL testbench、不支持 `assert`**
2. **没有任何 Tcl 命令接口** —— `package require ::quartus::simulator` 能加载，
   但 `info commands ::quartus::simulator::*` 是空的，`quartus_sh` 里也同样为空。
   即**无法用 Tcl 脚本驱动仿真**。
3. **必须有一个 `.vwf` 向量源文件**，否则直接报
   `No valid vector source file specified`。

**突破口（关键结论）：**

- `.vwf` 是**纯文本**格式，格式已从开发板上验证过的真实 `.vwf` 逆向整理完毕
  → 已封装成库 **`scripts/vwf.py`**（读写 / 展开嵌套 REPEAT / 参考模型比对）
- `quartus_sim <工程> --mode=functional --overwrite_waveform=on`
  会把**仿真结果写回输入 `.vwf` 本身**（覆盖激励）→ 结果同样是纯文本，可解析
- 于是可以做**全自动、自校验**的验证：**用 Python 当测试平台**

**标准仿真流程（每个模块都照这个走）：**

```bash
# 1. 生成功能仿真网表（改顶层后必须重跑）
quartus_map <工程> --generate_functional_sim_netlist

# 2. Python 生成激励 .vwf
python scripts/gen_stim.py <模块>

# 3. 跑仿真（结果写回同一个 .vwf）
quartus_sim <工程> --mode=functional --overwrite_waveform=on

# 4. Python 解析结果 + 参考模型逐点比对 + 渲染波形图
python scripts/check_sim.py <模块>
```

第 2/4 步的模块专属激励与断言写在 `sim/tb_<模块>.py` 里。

> ⚠️ **`.vwf` 会被仿真结果覆盖**，所以激励必须**可重新生成**。
> 永远不要手工编辑 `sim/*.vwf` —— 改 `sim/tb_*.py` 然后重新生成。

**其它已实测的坑：**
- `.vwf` 里的 `DISPLAY_LINE` 块如果 `TREE_INDEX` 不连续 / `CHILDREN` 不自洽，
  仿真器会警告 `corrupted display information`（不影响结果，但会让报告难看）
  → `vwf.py` 已保证自洽
- 仿真结果在 `.sim.rpt` 里**看不到波形**（"Waveform report data cannot be output to
  ASCII"），只能从回写的 `.vwf` 里读 → 别再去找 VCD 输出，`--simulation_results_format=VCD`
  **实测不产生任何文件**
- **实体名不得与 Quartus 原语重名**。实测：实体取名 `exp`（LPM 指数原语）会直接
  报 `Entity "exp" will be ignored because it conflicts with Quartus II primitive name`
  → `Top-level design entity "exp" is undefined`。
  高危名字：`exp` `abs` `add` `sub` `mult` `div` `lpm_*` `alt*`。
  **本项目的模块名（`clk_gen` / `seg_scan` / `puzzle_*` …）都不冲突。**

### 5.4 环境备忘

- 从中文 PDF 提取文本：`pdftotext -layout -enc UTF-8`（**必须带 `-enc UTF-8`**，否则中文全丢）
- PDF 转图片：`pdftoppm`（在 MiKTeX 里：`C:\Users\sznnn\AppData\Local\Programs\MiKTeX\miktex\bin\x64\pdftoppm.exe`）
- Python 3.14，已装 `numpy`、`opencv-python`（无 PyPDF/pdfplumber）

---

## 6. Commit 规范

**Conventional Commits + 中文描述。**

```
<type>(<scope>): <中文描述>

[可选的正文]
```

| type | 用途 |
|---|---|
| `feat` | 新增模块或功能 |
| `fix` | 修复缺陷 |
| `docs` | 只改文档 |
| `sim` | 仿真激励或波形相关 |
| `refactor` | 重构，不改行为 |
| `chore` | 构建、工程配置、杂项 |

**scope** 用模块名：`clk_gen` / `seg_scan` / `dot_matrix_scan` / `keypad_scan` / `pattern_rom` / `rng_lfsr` / `puzzle_ctrl` / `game_fsm` / `buzzer_ctrl` / `top`

示例：
```
feat(seg_scan): 实现8位数码管动态扫描与BCD译码
fix(keypad_scan): 修正消抖计数器位宽不足导致的漏键
docs(01): 补充总体框图中各模块间信号定义
```

**规则**：
- **一个模块一次提交**，粒度要小
- 提交前该模块必须**已通过仿真**
- 禁止把多个不相关模块塞进一个 commit

---

## 7. 文档同步规则

| 发生了什么 | 必须同步更新 |
|---|---|
| 改了某个模块的实现 | `docs/02-模块详细设计.md` 对应小节 + `CHANGELOG.md` |
| 改了模块接口/端口 | `docs/01-系统设计.md` 接口定义 + 总体框图 |
| 跑了仿真 | `docs/03-仿真验证方案.md`（激励 + 判读结论）+ 波形图存 `docs/图/` |
| 改了引脚约束 | `docs/04-引脚分配表.md` + `quartus/*.qsf` |
| **发现任何问题**（编译报错、波形不符预期、上板异常） | 立即写 **`ERRORS.md`**（五段式，见下） |
| 用 AI 做了任何事 | 追加 **`AI_LOG.md`** |
| 模块完成情况变化 | 更新 **`PROGRESS.md`** 模块进度表 |

### `ERRORS.md` 五段式模板

```markdown
## ERR-0001 <一句话标题>
- **日期**：2026-09-17
- **阶段**：模块编写 / 仿真 / 综合 / 上板
- **涉及模块**：
- **现象**：观察到什么（原文粘贴报错信息或波形异常描述）
- **定位过程**：怎么一步步缩小范围的（写清楚，这是报告⑤的得分点）
- **根本原因**：
- **修复方法**：具体改了什么
- **预防措施**：以后怎么避免
- **状态**：已解决 / 未解决 / 规避
```

---

## 8. 上板调试注意（板子只在实验室能用）

- **去实验室前**：先列好"本次待验证清单"，带上 `PROGRESS.md`，按清单逐条打勾。时间宝贵，不要到现场才想做什么
- **优先验证**（见 §9 风险）：
  1. 时钟档位指示是否为 **7 (50MHz)** —— 先烧一个时钟自检程序（计数器点亮 LED）
  2. 键盘行列方向与极性 —— 先烧一个"按键点亮 LED"最小程序
- **回来立刻补**：`docs/05-硬件调试记录.md` + `ERRORS.md`
- 每次至少预约 **2 次开放实验**（课程要求），在"Easy 云课堂"小程序预约

---

## 9. 必须上板确认的风险点

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| 1 | 板载时钟档位不确定 | 所有分频都不对 | 分频常量单点定义；先烧时钟自检程序确认档位 7 |
| 2 | 键盘行列方向/极性判断错 | 按键全失效 | `keypad_scan` 的行列方向与极性做成常量，可一处翻转 |
| 3 | 扫描频率不当 → 闪烁/鬼影 | 显示效果差 | 扫描频率 ≥1kHz，切换时消隐；先仿真后上板微调 |
| 4 | 64 位掩码跨行回绕 | 零片贴右边缘时形状错乱 | 掩码运算逐行处理；穷举边界情形仿真 |
| 5 | 无 testbench/assert 可用 | 无法自动判错 | 用 `.vwf` 激励 + Python 脚本解析输出做自动比对 |
| 6 | 初步设计方案 9/24 硬截止 | 直接影响平时成绩 | 文档优先，先交设计再加功能 |

---

## 10. 模块清单（与总体框图一致）

| # | 模块 | 职责 | 类型 |
|---|---|---|---|
| 0 | `puzzle_pkg` | 全局常量、掩码纯函数、位序约定（**唯一真值源**） | 包 |
| 1 | `clk_gen` | 时钟分频与节拍使能产生（含上电复位） | 功能 |
| 2 | `keypad_scan` | 4×4 键盘扫描 + 消抖 + 单次按键脉冲 | 输入 |
| 3 | `seg_scan` | 8 位数码管动态扫描 + BCD 译码 | 显示 |
| 4 | `dot_matrix_scan` | 8×8 双色点阵动态扫描 | 显示 |
| 5 | `pattern_rom` | 图案掩码 ROM（第1/2关图案、胜利/失败图案） | 数据 |
| 6 | `piece_rom` | 零片形状 ROM（相对行掩码 + 宽高） | 数据 |
| 7 | `rng_lfsr` | 8 位伪随机源（含全零保护） | 数据 |
| 8 | `puzzle_ctrl` | 零片锚点/钳位/移动/重叠/锁定/正确性判定 + 随机落位序列器 | 核心逻辑 |
| 9 | `disp_format` | 显示格式化：游戏状态 → 8 位 BCD + 熄灭掩码 | 显示 |
| 10 | `game_fsm` | 游戏主状态机、倒计时、关卡切换、随机选图 | 控制 |
| 11 | `buzzer_ctrl` | 蜂鸣器音效 | 输出 |
| — | `puzzle_top` | 顶层，唯一做引脚绑定的文件 | 顶层 |
| — | `board_test_top` | **上板前的硬件自检顶层**（验证极性与行列顺序，非最终产物但长期保留） | 调试 |

**架构关键分界（答辩要点）：**
- `puzzle_ctrl` 只管**空间**（坐标、掩码），**不含任何计数器**
- `game_fsm` 只管**时间**（状态、倒计时），**不含任何坐标运算**
- 这条分界让两个模块都能独立仿真、独立讲清楚

---

## 10.1 全局设计约定（实现时严格遵守）

| 项 | 约定 |
|---|---|
| 时钟方案 | **单一时钟域 + 多档单周期使能**（`tick_1k` / `tick_100` / `tick_2hz` / `tick_1hz`），不用门控时钟、不用多时钟域 |
| 复位 | **同步、高有效**，全文统一。`rst = (not SW7) or por_rst` |
| 熄灭 | `sys_en = SW7` 组合控制显示驱动与蜂鸣器**输出级** → SW7=0 时即时全灭、无声 |
| SW7 消抖 | **不做消抖**（要求是即时熄灭，消抖反而违背要求）；在文档中说明理由 |
| 库 | `std_logic_1164` + `numeric_std`；**禁用** `std_logic_unsigned`/`std_logic_arith` |
| 点阵位序 | 64 位向量，**`mask(8*行 + 列)`**，列号右增。**bit0 = 左上角** |
| 行内位序 | 第 r 行 = `mask(8*r+7 downto 8*r)`；行内 **bit0 = 最左列**、bit7 = 最右列 |
| 移动方向 | 「向右移一列」= 行向量**左移**：`r(6 downto 0) & '0'` |
| 与驱动的对应 | `dot_matrix_scan` 扫描第 r 行时，直接把 `mask(8*r+7 downto 8*r)` 赋给 `dot_colr` —— 因为 COLR0 在实物上就是最左列，位序天然对齐，**无需任何翻转** |
| 零片表示 | **只存锚点 (row,col)，掩码组合计算**（单一数据源，省 256 个触发器，杜绝锚点与掩码不一致） |
| 边界不变量 | `行 + 高 ≤ 8` 且 `列 + 宽 ≤ 8` —— 由钳位与落位**主动保证**，从根本上排除回绕 |
| 正确性判定 | `所有零片掩码的并集 == 目标掩码`（与拼法无关，自动接受两种合法铺法） |

### ⚠️ 两个必须写进报告的坑（都是设计阶段主动规避的）

1. **LFSR 必须每次尝试推进一次**。若随机数只是每周期采样，同一棋子的多次尝试会读到同一个值 → 所有零片落点相同 → 必然重叠 → 用尽尝试次数 → 每次都走回退布局。
   **游戏仍能运行，只是永远不随机，这个 bug 直到有人数落点分布才会发现。**
2. **回绕靠"锚点不变量"根除，而非靠移位后修补**。落位函数与钳位函数共同保证不变量，移位前的边界判断只是第二道防线。

---

## 10.2 资源利用率约定（EPM1270 只有 1270 个 LE，省着用）

> **总原则**：本项目**对速度没有要求**，但对面积有。任何"用面积换速度"的写法都要反过来评估。
> 但**不能优化到影响功能**（见本节末尾的"三条红线"）。

### ① `integer` 必须加 `range` 约束 ⭐ 最大的一笔

**裸 `integer` 在 Quartus 里综合成 32 位。** 这是 VHDL 里最容易踩、也最容易省的一笔。

```vhdl
-- ✗ 32 位寄存器，绝大部分永远用不到
signal r_cnt : integer;

-- ✓ 只占实际需要的位数
signal r_bcd : integer range 0 to 9;      -- 4 位
signal r_div : integer range 0 to 6249;   -- 13 位
```

**硬性要求**：**凡是会变成硬件对象的 `integer`，必须带 `range`。**

| 位置 | 要不要 `range` | 为什么 |
|---|---|---|
| `signal` | ✅ **必须** | 直接综合成寄存器，裸 `integer` = 32 位触发器 |
| 进程内 `variable` | ✅ **必须** | 同上 |
| 子程序形参，**且参与算术** | ✅ **必须** | `r + ar` 里的 `ar` 若是裸 `integer`，加法器就是 32 位 |
| 子程序形参，**只作查表/比较** | ⭕ 建议 | 不加也能推出来，但加了是文档 |
| `constant` | ❌ 不需要 | 在 elaboration 阶段就被折叠成常量，不占任何资源 |
| `for` 循环变量 | ❌ **加不了** | VHDL-93 里循环变量是隐式声明的，语法上不允许写 `range` |

> ⚠️ **`for` 循环的边界必须是常量。** VHDL-93 的循环变量隐式带 `range`，
> 所以它本身是安全的——**但循环上界如果来自信号**（`for i in 0 to n`，`n` 是信号），
> 综合器只能按该信号的完整位宽展开加法器。**本项目禁止这种写法。**

> **检查方法**：综合后在编译报告里看寄存器数量；
> 或全项目搜 `signal.*: integer;` **和** `variable.*: integer;`，两条都要搜。

### ② 分频器串行级联，不要五个并行 ⭐

**原先的写法（五个独立计数器）**：每个都从 50MHz 直接分频，即使加了 `range` 也要 99 位。

| 节拍 | 分频比 | 位宽 |
|---|---|---|
| 8kHz | 6250 | 13 |
| 1kHz | 50000 | 16 |
| 100Hz | 500000 | 19 |
| 2Hz | 25 000 000 | 25 |
| 1Hz | 50 000 000 | 26 |
| **合计** | | **99 位** |

**改成串行级联**：高频节拍分频低频节拍，每一级只数"上一级来几个脉冲"。

```
50MHz ──6250──▶ tick_8k ──8──▶ tick_1k ──10──▶ tick_100 ─┬─50──▶ tick_2hz
                                                          └─100─▶ tick_1hz
```

| 级 | 数几个 | 位宽 |
|---|---|---|
| 50MHz→8kHz | 6250 | 13 |
| 8kHz→1kHz | 8 | 3 |
| 1kHz→100Hz | 10 | 4 |
| 100Hz→2Hz | 50 | 6 |
| 100Hz→1Hz | 100 | 7 |
| **合计** | | **33 位** |

**节省 66 位触发器**（相比加 `range` 的并行版），
相比裸 `integer` 的并行版（5×32 = 160 位）**节省 127 位**。

> ⚠️ **代价**：级联后各节拍之间有固定的相位关系（`tick_1hz` 一定紧跟某个 `tick_100`）。
> 本项目各节拍用途完全独立（扫描 / 消抖 / 倒计时），**相位关系无影响**。

### ③ 位宽按需最小化

| 对象 | ✗ 浪费写法 | ✓ 写法 |
|---|---|---|
| 状态编码 | 8 位 | **3 位**（6 个状态），不用 one-hot |
| 零片序号 | 8 位 | **2 位**（最多 4 块） |
| 行列坐标 | 各自 8 位 | 打包成**一个 6 位**：`{行[2:0], 列[2:0]}` |
| 点阵行计数器 | 8 位 | **3 位**（0~7） |
| 4×4 键盘键号 | 8 位 | **4 位**（0~16） |
| 倒计时 | 6 位二进制 + 除法器 | **两位 BCD**（各 4 位，共 8 位，**且省掉除法器**） |

### ④ 其余可用的省面积手段

| 手段 | 说明 |
|---|---|
| **共享运算资源** | 4 块零片的 `place()` 若分别展开，逻辑重复 4 次；可由综合器共享，或在设计上串行复用同一套平移逻辑 |
| **`sll` 代替 `case`** | 行译码 `1 sll r`、位选 `1 sll digit` 都是一行，综合成译码器，比手写 `case` 更省 |
| **常量折叠** | 图案/零片掩码直接写成常量，让综合器在编译期折叠，而不是运行时计算 |
| **不用乘法器/除法器** | 本项目**应当完全不出现** `*` `/` `mod`。倒计时用 BCD 就是为了根除 `/10` |
| **降低扫描频率** | 见下方红线②——扫描频率只要能保证不闪即可，不必更高 |

### ⑤ 三条红线（优化不能越过它们）

| # | 红线 | 为什么 |
|---|---|---|
| 1 | **不牺牲可读性与模块边界** | 老师会照框图抽模块提问；为省几个触发器把两个模块合并，是**拿 20 分的设计分换几个 LE**，不划算 |
| 2 | **点阵/数码管扫描频率不得低于 1kHz** | 8 路扫描下刷新率 = 扫描频率/8，1kHz → 125Hz，已经接近下限。**再降就会肉眼可见地闪**。这是"速度无所谓"的**唯一例外** |
| 3 | **不引入组合环、不用 latch** | 组合进程所有分支必须赋值，或开头给默认值 |

> **一句话**：省**寄存器位宽**和**分频器级数**（纯赚），
> 不省**模块划分**和**扫描频率**（会亏）。

---

## 11. 报告评分项对照（随时自查）

| 评分项 | 分值 | 素材来源 |
|---|---|---|
| ① 设计课题的任务要求 | 5 | `docs/00-需求分析.md` |
| ② 系统设计（思路/总体框图/分块设计） | 20 | `docs/01` + `docs/02` |
| ③ 仿真波形及波形分析 | **25** | `docs/03` + `docs/图/` 波形图 |
| ④ 实际实现功能及资源利用（编译报告截图） | 5 | Quartus 编译报告 + `docs/04` |
| ⑤ 故障及问题分析 | 15 | **`ERRORS.md`** |
| ⑥ 总结和结论 | 10 | `CHANGELOG.md` + `PROGRESS.md` |
| ⑦ AI 工具使用情况（工具、提示词） | 10 | **`AI_LOG.md`** |
| ⑧ 顶层文件的 HDL 代码（有注释） | 10 | `rtl/puzzle_top.vhd` |

---

## 12. 图表规范（方框图 / 状态转移图）

> 老师会**照着总体框图随机抽一个模块**提问代码和仿真波形 —— 图与代码必须严格对应。

### 12.1 必须有的两类图

| 图 | 内容 | 约束 |
|---|---|---|
| **DWG-01 总体框图（第一层）** | 7 个功能子系统及其互连 | 每框 = 一个**功能子系统**（含若干实体），**框内要列出所含实体名** |
| **DWG-02 模块框图（第二层）** | DWG-01 展开到实体 | **一个矩形框 = 一个 `.vhd` 文件 = 一个实体** |
| **DWG-03 状态转移图** | `game_fsm` 的状态与迁移 | 每个状态一个圆圈/矩形，箭头标**迁移条件**，箭头上写清触发信号 |

> **为什么分两层**：一张图里平铺 12 个实体，读者（和答辩老师）看不到系统是怎么**分块**的。
> 第一层回答"这个系统由哪几件大事组成"，第二层回答"每件大事由哪几个实体实现"。
> 这**就是自顶向下分解的可视化**（见 §2.1），不是画两张图凑数。

**方框图的硬性要求**：

1. **DWG-02 里一个矩形框 = 一个 `.vhd` 文件 = 一个实体**，框内文字就是实体名；
   **DWG-01 里一个框 = 一个功能子系统**，框内须列出它包含的实体名，
   使两层能对得上（子系统所含实体之和 = 全部 11 个实体，不重不漏）；
2. 框之间的连线必须标**信号名**（与代码里的端口名一致）；
3. 数据流方向用箭头，**不要画成双向**（本设计全是单向数据流 + 一条反馈）；
4. 顶层 `puzzle_top` 单独画一框，**引脚只出现在它这一层**。

**图与代码的对应关系（答辩会被问）**：老师"照着总体框图随机抽一个模块提问"时，
抽到的是 **DWG-02 的实体**——因为那才是 `.vhd` 文件。DWG-01 是它的组织方式。

**当前图集**（均在 `docs/图/系统图.html`，内联 SVG，可导入 draw.io）：

| 图号 | 内容 | 第一层框数 | 第二层实体数 |
|---|---|---|---|
| DWG-01 | 总体框图（功能子系统） | 7 | — |
| DWG-02 | 模块框图（实体级） | — | 11 + 2 顶层 |
| DWG-03 | `game_fsm` 状态转移图 | 6 个状态 | — |

**状态转移图必须覆盖**：`S_SELF_TEST` / `S_IDLE` / `S_PREVIEW` / `S_PLAYING` / `S_WIN` / `S_FAIL`
共 6 个状态，以及**所有**迁移边（含"任意状态按开始键 → 重开"这类边）。

### 12.2 工作方式（分工）

| 步骤 | 谁做 |
|---|---|
| 1. 生成图（HTML + 内联 SVG，浏览器可直接打开） | **Claude** |
| 2. 用 draw.io 等工具美化、调整布局 | **用户** |
| 3. 导出 PNG/SVG 存入 `docs/图/`，插入设计文档与报告 | **用户** |

**存放位置**：`docs/图/`（HTML 源文件与导出的图片都放这里）。

> **为什么先出 HTML+SVG**：SVG 是矢量格式，**draw.io 可以直接导入**，
> 导入后每个矩形框、每条箭头都是**可编辑的独立对象**，
> 比从 PNG 重画快得多。HTML 只是为了让用户能直接在浏览器里看到效果。

### 12.3 图与代码的一致性

**改了模块接口 → 必须同时改框图和 §2.1 的接口定义表。**
图、文档、代码三者不一致，是答辩时最容易被抓的失分点。

---

## 13. 过程记录要求（为报告积累素材）

> 报告评分项③仿真波形 25 分、⑤故障分析 15 分、⑦AI 使用 10 分，**共 50 分**，
> 全部依赖过程记录。**这些分是"当时记下来"的，事后补写拿不到。**

### 13.1 每次会话都要留痕

| 记什么 | 记到哪 | 时机 |
|---|---|---|
| 用了什么提示词、得到了什么、人工改了什么 | `AI_LOG.md` | **实时**，当场记 |
| 遇到的任何异常（编译报错、波形不符、上板异常） | `ERRORS.md` 五段式 | **实时**，解决完立刻写 |
| 本次改了什么 | `CHANGELOG.md` | 收工时 |
| 进度到哪、下次从哪继续 | `PROGRESS.md` | 收工时 |
| **仿真的原始数据**（激励条件、观测点、实测值、期望值） | `docs/03-仿真验证方案.md` | 每次跑完仿真 |
| 波形图 | `docs/图/` | 每次跑完仿真 |

### 13.2 "多记录数据"具体指什么

跑仿真时**不要只记"通过了"**，要留下**可复现、可画进报告的原始数据**：

- **激励条件**：时钟频率、复位时刻、各输入信号在哪些时刻取什么值；
- **观测点**：看了哪些信号，为什么看它们；
- **实测值 vs 期望值**：具体数字或波形截图，**不是"符合预期"四个字**；
- **编译数据**：每次编译的 LE 占用、寄存器数、引脚数 —— **留一条时间线**，
  报告评分项④要用，而且能看出资源优化前后差多少。

> **反面例子**：`docs/03` 里只写"仿真通过"。
> **正确写法**：给出激励波形图 + 观测信号的实测值表 + 与期望值逐项比对的结果。

### 13.3 提示词记录

`AI_LOG.md` 每条任务记四样：**关键提示词（原文）/ AI 产出 / 人工修正 / 结论**。
**"人工修正"这一栏最能得分** —— 它体现了人对 AI 输出的判断，而不是照单全收。
