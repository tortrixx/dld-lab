# CLAUDE.md — 题目4《简易拼图游戏的设计与实现》

> 本文件是给"未来每一次会话的 Claude"看的操作手册。
> 目标：**任何一次新会话，读完本文件 + `PROGRESS.md`，就能无缝续接项目。**

---

## 0. 每次会话的固定流程（最重要，先做这个）

### 开工时（按顺序）
1. 读 **`HANDOFF.md`** —— **一页纸的交接单**：这个项目是什么 / 现在到哪了 / **下一步具体做什么** / 铁律清单
2. 读 **`PROGRESS.md`** —— 弄清：当前阶段 / 模块进度表 / 进行中的工作 / 阻塞项 / **上次会话断点**
3. 读 **`CHANGELOG.md`** 最后一节 —— 弄清最近改了什么
4. 读 **`ERRORS.md`** 未解决的问题 —— 避免重复踩坑
5. 确认本次要做什么 → 需要动哪些模块 → 哪些文档要同步更新

### 收工时（缺一不可）

> ⭐ **触发判据是「这一轮有没有产生提交」，不是「是不是新会话」。**
> 续跑、补做、临时插进来的一轮，**只要落了盘就算一轮**，就要走完整收工 ——
> 第 15 次会话把"接着上次往下干"当成了不用收工，于是 `HANDOFF.md` 表头与 `CHANGELOG.md`
> 双双留空（`ERRORS.md` **ERR-0021**，与 ERR-0020/`CRIT-4` 是同一个坑的**第二次复发**）。
> **完成判据（可数）**：`git status` 干净 **且** `HANDOFF.md`/`PROGRESS.md` 两处「最后更新」都是今天。

0. 更新 **`HANDOFF.md`**：
   ⭐ **a) 表头「最后更新：第 N 次会话 · 日期」** —— **这一项最容易漏**：
   第 13 次审查查出它曾连续两次没更新，此后**又漏过一次**（`ERRORS.md` ERR-0020 `CRIT-4` / ERR-0021）。
   b) §2「现在到哪了」/ §3「你现在该做什么」/ §4「刚做完什么」 —— **给下一个会话看的第一眼，过时了就等于没写**
   c) ⭐ **§4「刚做完什么」只保留最近 2~3 次会话**，更早的**删掉**（历史在 `CHANGELOG.md`）。**交接单是"现在"，不是"档案"**
1. 更新 **`PROGRESS.md`**（含"上次会话断点"一句话，写清楚下次从哪继续）
2. 更新 **`CHANGELOG.md`**（本次新增/修改/修复）
3. 发现或解决了问题 → 写入 **`ERRORS.md`**（九字段，见 §7）
4. 本次用到 AI → 追加 **`AI_LOG.md`** 记录
5. 本次跑了仿真/编译 → **记下原始数据**（激励条件、实测值 vs 期望值、LE 占用、波形图）
   → 写入 `docs/03-仿真验证方案.md`、图片存 `docs/图/`（**要求见 §13.2，别只写"通过"**）
6. `git add` + commit（规范见 §6）

> ⚠️ 这四份文档不是"事后补写的报告素材"，而是**项目运行的一部分**。
> 报告评分项⑤故障分析 15分、⑦AI使用 10分 直接从 `ERRORS.md` / `AI_LOG.md` 取材，事后补写无效。

---

## 1. 项目一句话

在 MAXII 实验板（**EPM1270T144C5**）上用 **VHDL** 实现一个 8×8 双色点阵拼图游戏，全部开发流程基于 **Quartus II 9.1**。

**课程**：《数字电路与逻辑设计实验（下）》综合课题 · 题目4
**关键截止**：初步设计方案 **2026-09-24 24:00** · 第 9~10 周验收答辩

**已确认的实现范围**：11 条基本要求全部实现 + 三项提高（蜂鸣器音效、多图案随机、零片位置随机）。
**不做**：零片 90°旋转、方向随机。

---

## 2. 硬约束（不可违反）

| # | 约束 |
|---|---|
| 1 | 器件固定 **EPM1270T144C5**（MAX II 家族），工程文件在 `quartus/` |
| 2 | 目标时钟 **50MHz**（板载 USER_BTN 选到档位 7）。**分频常量单点定义**，禁止魔法数字散落各处 |
| 3 | **引脚绑定只允许出现在两个地方**：`rtl/puzzle_top.vhd` 的端口名 + `quartus/*.qsf` 的 `set_location_assignment`。其他任何文件不得出现引脚号 |
| 4 | 所有 **VHDL 注释用中文**，所有**文档用中文** |
| 5 | **模块划分必须与 `docs/01-系统设计.md` 的总体框图严格一致** —— 老师会照着框图随机抽一个模块提问代码和仿真波形 |
| 6 | `puzzle_top.vhd` 必须有**完整中文注释**（报告评分项⑧，10分） |
| 7 | VHDL 统一用 **VHDL-1993 子集 + `numeric_std`**（Quartus II 9.1 兼容性最稳）。不用 `std_logic_unsigned`/`std_logic_arith` |
| 8 | 一个文件一个实体，文件名 = 实体名 |
| 9 | **任何 `integer` 都必须加 `range` 约束**（如 `integer range 0 to 9`）。裸 `integer` 在 Quartus 里综合成 **32 位**，是最大的隐性浪费。详见 §10.2 |
| 10 | **设计自顶向下，实现自底向上**（两件事，别混为一谈，详见 §2.1）：设计时从系统逐层分解到实体；实现时先做并验证叶子模块，再逐层往上集成。**不允许"先写顶层再补子模块"** |
| 11 | **每一个模块都必须能独立仿真**。不能独立仿真的模块说明职责切分有问题，要重切 |
| 12 | **顶层文件（`puzzle_top.vhd` / `board_test_top.vhd`）一律用 `component` 声明 + `port map` 例化**，不得用 `entity work.xxx` 直接例化。详见 §10.3 |
| 13 | ⭐ **`and` / `or` 与 `=` / `/=` 混在一行时一律加括号。** VHDL 的关系运算符**比逻辑运算符紧**：`a and b = c` 实际被解析成 `a and (b = c)` → 类型错误。典型场景：**全 1 检测**要写成 `(r_locked and s_valid_mask) = s_valid_mask`。这是"看着完全合法、其实错"的一类，最贵（ERR-0012） |
| 14 | **文档里的代码块，凡是不能直接照抄的，必须显式标注「伪代码，不可照抄」**（符号要么在本项目有定义，要么就标出来 —— 混在完整骨架里的未定义符号会被当真，ERR-0012） |

### 2.1 「自顶向下设计」与「自底向上实现」是两件事 ⭐

**这两个方向不是矛盾的，它们作用在项目生命周期的不同阶段：**

| | 方向 | 干什么 | 什么时候 |
|---|---|---|---|
| **设计** | **自顶向下** | 系统 → 功能子系统 → 模块 → 实体，逐层分解 | 阶段 1 已完成 |
| **实现** | **自底向上** | 先写叶子模块（驱动/数据）并逐个仿真通过，再往上集成 | 阶段 2~4 |

- 说"**自底向上的依赖分层**"是**错的**——那是在用实现顺序描述设计方法。依赖关系确实自底向上
  （上层依赖下层），但**分层这个动作本身是从顶开始的**：先有"这是一个拼图游戏"，
  才有"要分输入/控制/显示"，最后才有 `seg_scan` 这个实体。
- 设计若真自底向上，就会**由下位模块的便利决定系统结构**，
  最后拼出来的东西没人能保证它满足需求——而需求恰恰在顶上。

**本项目的分层结果**（与 `docs/图/系统图.html` 的 DWG-01/02 严格对应）：
`第 0 层 需求`（11 条基本要求 + 3 项提高 → `docs/00`）→ `第 1 层 7 个功能子系统`（DWG-01）
→ `第 2 层 11 个实体 + 2 个顶层`（DWG-02）。

**报告里怎么写**：设计思路一节写"自顶向下逐层分解"，实现步骤一节写"自底向上逐模块集成"。
两者都出现、且说清各自作用在哪个阶段，才是完整的表述。

---

## 3. 目录导航

| 目录/文件 | 放什么 |
|---|---|
| `CLAUDE.md` | 本文件，工作约定 |
| `README.md` | 项目总览（给人看的） |
| `HANDOFF.md` | **会话交接单**（一页纸：现在干什么 / 别踩什么坑）← **开工第一个读** |
| `PROGRESS.md` | **跨会话进度状态**（最全的账本：阶段 / 模块 / 文档 / 阻塞 / 断点） |
| `CHANGELOG.md` | 变更日志，Keep a Changelog 格式 |
| `ERRORS.md` | 错误记录，九字段 |
| `AI_LOG.md` | AI 使用记录（工具/提示词/产出/修正） |
| `docs/00-需求分析.md` | 11 条要求逐条拆解 + 需求追踪矩阵 |
| `docs/01-系统设计.md` | 总体框图 + 模块划分 + 接口定义 ← **初步设计方案主体** |
| `docs/02-模块详细设计.md` | 每个模块的内部设计（老师提问主要看这个） |
| `docs/03-仿真验证方案.md` | 每个模块的激励设计 + 波形判读结论 |
| `docs/04-引脚分配表.md` | 引脚对照表 + qsf 约束对照 |
| `docs/05-硬件调试记录.md` | 实验室上板调试记录 |
| `docs/报告/实验报告.md` | 正式报告，按 8 个评分项组织 |
| `docs/图/` | 框图、仿真波形图、编译报告截图 |
| `rtl/` | VHDL 源码 |
| `sim/` | `.vwf` 激励文件与仿真输出 |
| `quartus/` | `.qpf` / `.qsf` 工程与引脚约束 |
| `scripts/` | TCL 与 Python 自动化脚本 |
| `report/` | （预留）导出物 |

> 根目录下三份课程 PDF 已在 `.gitignore` 中，**保留在本地作参考，不入库**。

---

## 4. 硬件速查（高频引用，不必每次翻 PDF）

### 4.1 引脚极性速查（**引脚号不在此列**）

> ⭐ **引脚号的唯一真值源是 `quartus/puzzle.qsf`**，可读版见 `docs/04-引脚分配表.md` §2
> （硬约束 #3：引脚号只允许出现在 `puzzle_top.vhd` 端口名与 `.qsf` 两处）。
> **本表只留"极性"** —— 极性是陷阱，引脚号只是数据；写在本文档里就多了一个真值源，
> 迟早与 `.qsf` 对不上（ERR-0014）。

| 功能 | 有效电平 |
|---|---|
| 点阵行 ROW0~ROW7 | **低有效** |
| 点阵红列 COLR0~COLR7 / 绿列 COLG0~COLG7 | 两列都**高有效**（红/绿/黄怎么合成见 §4.2） |
| 数码管段 AA~AP / 位选 CAT0~CAT7 | 段**高有效**；位选**低有效** |
| 键盘列 COL0~COL3 / 行 ROW0~ROW3 | 列**驱动输出**、行**读入**；读到 **1 = 该键按下** |
| 蜂鸣器 BUZZ / 系统开关 SW7 | 音频方波；SW7 **高 = 系统开** |
| 独立按键 BTN0~BTN7 | **按下 = 1，弹起 = 0** |
| LED LD0~LD15 | **高电平点亮**。**不属于 `puzzle_top`**，只出现在 `board_test_top` |

### 4.2 关键位序与极性

- **数码管：DISP7 最左 … DISP0 最右**（实物丝印已确认）。`CAT7`→DISP7，`CAT0`→DISP0
- **点阵点亮条件**：
  - 红 = 行低 + COLR高 + COLG低
  - 绿 = 行低 + COLR低 + COLG高
  - 黄 = 行低 + COLR高 + COLG高
- **键盘实物**：丝印只有 `KEY1~KEY16`；**ROW3 在最上排、ROW0 在最下排**；COL0 最左、COL3 最右
  - 换算：`KEY号 = (3 - ROW) * 4 + COL + 1`
  - 列有 10k 上拉、行有 4.7k 下拉 → 扫描时**驱动某列为高，读行，读到 1 表示该键按下**
- **板载时钟档位丝印**：`0:NO CLOCK / 1:1Hz / 2:2Hz / 3:10Hz / 4:100Hz / 5:1KHz / 6:1MHz / 7:50MHz`
  - 开发板上需确认指示灯为档位 **7**

#### ⚠️ 引脚号 vs 位序 —— 别把两者搞混

**引脚号是板上丝印的物理网络名，不随你的信号命名改变。** 本项目一律以手册为准：

| 板上网络名 | 引脚号（手册原文） |
|---|---|
| COLR0…COLR7 | **22、21、16、15、14、13、12、11** |
| COLG0…COLG7 | **45、44、43、42、41、40、39、38** |

也就是说 **`dot_colr(0)` 必须接 PIN_22，`dot_colr(7)` 接 PIN_11**。

曾经踩过的坑：桌面上另一个旧工程 `VHDL/dot_matrix_pwm` 里写的是
`col_r[0] → PIN_11 … col_r[7] → PIN_22`，**与板手册相反**。那个工程能用，
是因为它的扫描代码是按它自己的命名写的——**位序是自洽的，但和手册的标签反了**。
抄它的引脚表会得到"左右镜像"的点阵。

> **规则：引脚号只从手册拿；旧工程只能用来交叉验证"用到了哪些引脚"，不能用来确定位序。**
> 本项目 `quartus/puzzle.qsf` 已按手册写死，并已与手册逐条核对。

### 4.3 键盘功能映射（已定稿）

方向键**居中十字**布局；完整键号↔功能对照表（含 `KEY号` 公式）见 `docs/02` §12.2，不在此重复。

```
ROW3:   -    上     -    开始
ROW2:   左   下    右    选择
ROW1:   -     -     -    确认
ROW0:   -     -     -     -
        COL0  COL1  COL2  COL3        （ROW3 在最上排，见 §4.2）
```

### 4.4 拼图图案定义（第一关）

**目标图案**：点阵 **第2~5行 × 第2~4列** 实心 4×3 矩形（12 格）；**零片 3 块、共 12 格**。
**行掩码与零片形状位图见 `docs/02` §2.5，不在本文档重复。**

**⚠️ 关键结论（已穷举验证）**：这三块恰好有 **2 种合法铺法**，两种都必须判为**成功**。
因此**成功判定必须是"三块并集 == 目标掩码 且 两两无重叠"**，
**绝不能**写成"每块回到各自初始位置"——那会把其中一种正确铺法误判为失败。

**穷举验证脚本**：`scripts/verify_tiling.py`（**修改任何图案常量前必须先跑它**）

### 4.5 第二关图案（自拟）

**目标图案**：向上箭头，**18 格**，占第 1~6 行；**四块零片 6+6+4+2 = 18 格**（与图案格数一致）。
**行掩码与零片形状位图见 `docs/02`，不在本文档重复。**

另有**向下箭头**作为第二套图案（同形状零片旋转 180°），用于实现"多图案随机选择"这一提高项——几乎零额外成本。

> 行掩码每行 8 位，**位序与 §10.1 一致：`bit0` = 该行最左列，`bit7` = 最右列**。

### 4.6 独立按键（BTN0~BTN7）

**手册原文**（`附图16 按键模块`）：

> 实验板上一共有 8 个按键，标示为 **BTN0~BTN7**，依次使用 EPM1270T144C5 芯片的
> **61、20、89、91、121、122、123 和 124** 脚。
> **按键平时输出低电平，按下去输出高电平，放开后自动弹起又输出低电平**，
> 在 BTN7 左下角有一个波形示意图。**按键在使用时需设计防抖动电路。**

→ 结论：**按下 = '1'，弹起 = '0'**（高有效）；**消抖必须做**。

> **⚠️ 这 8 个按键与 4×4 矩阵键盘（`kp_row`/`kp_col`）是两套完全独立的输入**，
> 不要混淆。游戏操作全部走矩阵键盘；BTN 只用于复位等辅助功能。
>
> **⚠️ 与 SW7 的区别**：SW7 是**拨动开关**（要求 1 规定 SW7='0' 时全部显示熄灭），
> BTN 是**按键**（按一下弹回来）。两者不是一回事。

**约定用法**：BTN0 作为**硬件复位键** —— 按下（1）复位，弹起（0）正常运行。

```vhdl
-- 复位是三个来源的"或"：硬件复位键 、 上电复位 、 SW7 关闭
o_rst <= btn_rst or por_rst or (not sys_en);
```

> ⭐ **课程 PDF（`数字系统设计2026.pdf`）p45 明确要求**：
> "开机复位：初值只具有仿真意义，不能实现对系统的复位，**复位必须用按键来实现**。"
> —— 所以 **`por_rst` 只是上电初值，不能替代按键**。（2026-09-18 早前曾按"文档内部一致性"
> 把它删成两源，属**依据不足权威来源的裁决**，已回退并记入 ERR-0018。）
>
> **PDF p54 又要求"按键要注意防抖处理"** —— BTN0 也是按键，故**做消抖**。
> ⚠️ **判据是「器件是不是按键」，不是「抖动有没有害」**：抖动对复位确实无害，
> 但**课件要求的是"按键要消抖"，不是"抖动有害的按键才消抖"**；做了消抖，答辩怎么问都答得死。

**BTN0 进 `clk_gen` 后过两级处理**（骨架见 `docs/02` §3.4）：

```
BTN0 ──▶ 2 级同步器 ──▶ 20ms 消抖（数 tick_1k）──▶ r_btn_rst ──┐
        （消亚稳态）      （课件 p54 + 手册附图16）             ├──▶ o_rst
por_cnt 0→CNT_POR ──▶ r_por_rst ──────────────────────────────┤
sys_en = SW7 ────────▶ (not sys_en) ──────────────────────────┘
```

> **与 SW7 的对比（答辩常问；判据是「器件类型」，不是「信号重不重要」）**：

| 信号 | 器件 | 消抖 | 理由 |
|---|---|---|---|
| **BTN0** | **按键** | ✅ **20ms** | 课件 p54「按键要注意防抖处理」＋手册附图16 |
| 矩阵键盘 | 按键 | ✅ 80ms | 同上（在 `keypad_scan` 内） |
| **SW7** | **拨动开关** | ❌ **不做** | 要求 1 是"**即时**熄灭"，消抖会引入延迟，反而违背要求 |

> ⚠️ **复位仍是全局统一的"同步、高有效"** —— BTN0 没有把它变成异步复位。
> 按键先被**同步进 `clk` 域**，再作为**同步复位**使用（§10.1）。
> 早先版本写的"异步断言、同步释放"是**用词错误**：本设计没有异步复位路径。

---

## 5. 构建 / 仿真 / 烧录

### 5.1 工具路径

```
QUARTUS_BIN = C:\QuartusII91\QuartusII91\quartus\bin
```

命令行工具：`quartus_sh` `quartus_map` `quartus_fit` `quartus_asm` `quartus_tan` `quartus_sim` `quartus_pgm`

### 5.2 常用命令

```bash
# 全流程编译（综合→布局布线→汇编）
quartus_sh --flow compile quartus/puzzle

# 分步
quartus_map quartus/puzzle --source=rtl/puzzle_top.vhd
quartus_fit quartus/puzzle
quartus_asm quartus/puzzle
quartus_tan quartus/puzzle          # 时序分析

# 仿真 —— 两个开关缺一不可（见 §5.3 标准流程；2026-09-18 审查 `sim-06` 订正：
#   旧写法不带开关 → 落到工程默认仿真模式（本项目从未 fit，时序模式跑不起来），
#   且结果不回写 .vwf，Python 侧比对拿不到任何数据）
quartus_sim quartus/puzzle --mode=functional --overwrite_waveform=on

# 烧录
quartus_pgm -c USB-Blaster -m jtag -o "p;quartus/puzzle.pof"
```

### 5.3 仿真方案（**机制**已实测跑通，2026-09-17；第 2/4 步的 `sim.py` 尚待实现）

**本机没有 ModelSim / GHDL**，只能用 **Quartus II 内置仿真器**。它的三条硬限制
（都已实测确认，不是推测）：

1. 仿真的是**综合后网表**，不是 RTL → **不支持 VHDL testbench、不支持 `assert`**
2. **没有任何 Tcl 命令接口** —— `package require ::quartus::simulator` 能加载，
   但 `info commands ::quartus::simulator::*` 是空的，`quartus_sh` 里也同样为空。
   即**无法用 Tcl 脚本驱动仿真**。
3. **必须有一个 `.vwf` 向量源文件**，否则直接报
   `No valid vector source file specified`。

**突破口（关键结论）：**

- `.vwf` 是**纯文本**格式，格式已从开发板上验证过的真实 `.vwf` 逆向整理完毕
  → 已封装成库 **`scripts/vwf.py`**（读写 / 展开嵌套 REPEAT / 参考模型比对）
- `quartus_sim <工程> --mode=functional --overwrite_waveform=on`
  会把**仿真结果写回输入 `.vwf` 本身**（覆盖激励）→ 结果同样是纯文本，可解析
- 于是可以做**全自动、自校验**的验证：**用 Python 当测试平台**

**标准仿真流程（每个模块都照这个走）：**

> ⚠️ **第 2/4 步的 `scripts/sim.py` 尚未写**（`PROGRESS.md`「进行中」）—— **故意等的**：
> 等 `clk_gen` 写出后针对**真实模块**再定接口。本节的"已实测跑通"指的是**闭环机制**
> （往返实验用的是临时最小工程 `simtest`，见 `ERRORS.md` ERR-0002），**不是"脚本已就绪"**。
> **现在照抄第 2 步会得到 `No such file or directory`** —— 先读这条再动手。

```bash
# 1. 生成功能仿真网表（改顶层后必须重跑）
quartus_map <工程> --generate_functional_sim_netlist

# 2. Python 生成激励 .vwf
python scripts/sim.py gen <模块>

# 3. 跑仿真（结果写回同一个 .vwf）
quartus_sim <工程> --mode=functional --overwrite_waveform=on

# 4. Python 解析结果 + 参考模型逐点比对 + 渲染波形图
python scripts/sim.py check <模块>
```

第 2/4 步的模块专属激励与断言写在 `sim/tb_<模块>.py` 里。

> ⚠️ **流程里还缺两个"绑定"步骤**（2026-09-18 审查补，ERR-0020 / 报告 `sim-03`）——
> **工程只有一个顶层、一个向量源设置**，不换就不会真的仿到被测模块：
>
> | 绑定 | 怎么做 |
> |---|---|
> | **把被测模块设为顶层** | `quartus_sh -t scripts/build.tcl --top <模块名>`（在第 1 步**之前**做；脚本自带"用完还原"） |
> | **把本次 `.vwf` 设为向量源** | `set_global_assignment -name VECTOR_SOURCE_FILE sim/<模块>.vwf`（在 `scripts/sim.py` 里做，**同样用完还原**） |
>
> 两者都**必须在关工程前还原** —— `set_global_assignment` 会写回 `.qsf`。
> 细则与后果见 `docs/03-仿真验证方案.md` **§2.1**。

> ⚠️ **`.vwf` 会被仿真结果覆盖**，所以激励必须**可重新生成**。
> 永远不要手工编辑 `sim/*.vwf` —— 改 `sim/tb_*.py` 然后重新生成。

> ⭐ **课件 PDF p59 明确要求：「仿真：加入中间信号，便于查看电路状态变化」**
> （原页截图是 Quartus 的 `Node Finder`，过滤条件圈的是 `Pins: all & Registers: post-fit`）。
> **这不是建议，是课件的做法指示**，直接对应评分项③（仿真波形及波形分析，**25 分**）：
>
> | 要求 | 本项目的落实 |
> |---|---|
> | 波形里要有**中间信号**，不能只有输入输出 | `scripts/vwf.py` 生成的 `.vwf` 除端口外**必须加入**：状态寄存器 `r_state`、倒计时寄存器、扫描计数器、消抖计数器、`r_btn_rst` 等 |
> | 怎么找这些信号 | Quartus `Node Finder`，`Filter` 选 **`Registers: post-synthesis`**（功能仿真用的是综合后网表；课件 p59 截图里的 `Registers: post-fit` 只在全编译后才有 —— 本项目功能仿真阶段工程从未 fit。2026-09-18 审查 `sim-04` 订正，详见 `docs/03` §3.1 第 2 条） |
> | 为什么这条重要 | **只给端口波形，看不出电路"为什么"这么动** —— 例如"零片没动"到底是没按键、还是消抖卡住、还是状态机没进 `S_PLAYING`，三者端口波形**一模一样** |
>
> **每个模块的 `sim/tb_<模块>.py` 里要显式列出本模块的中间信号清单**，
> 与 `docs/02` 各节的"仿真验证点"表一一对应；结果的判读结论写进 `docs/03`。

**其它已实测的坑：**
- `.vwf` 里的 `DISPLAY_LINE` 块如果 `TREE_INDEX` 不连续 / `CHILDREN` 不自洽，
  仿真器会警告 `corrupted display information`（不影响结果，但会让报告难看）
  → `vwf.py` 已保证自洽
- 仿真结果在 `.sim.rpt` 里**看不到波形**（"Waveform report data cannot be output to
  ASCII"），只能从回写的 `.vwf` 里读 → 别再去找 VCD 输出，`--simulation_results_format=VCD`
  **实测不产生任何文件**
- **实体名不得与 Quartus 原语重名**。实测：实体取名 `exp`（LPM 指数原语）会直接
  报 `Entity "exp" will be ignored because it conflicts with Quartus II primitive name`
  → `Top-level design entity "exp" is undefined`。
  高危名字：`exp` `abs` `add` `sub` `mult` `div` `lpm_*` `alt*`。
  **本项目的模块名（`clk_gen` / `seg_scan` / `puzzle_*` …）都不冲突。**

### 5.4 环境备忘

- 从中文 PDF 提取文本：`pdftotext -layout -enc UTF-8`（**必须带 `-enc UTF-8`**，否则中文全丢）
- PDF 转图片：`pdftoppm`（在 MiKTeX 里：`C:\Users\sznnn\AppData\Local\Programs\MiKTeX\miktex\bin\x64\pdftoppm.exe`）
- Python 3.14，已装 `numpy`、`opencv-python`（无 PyPDF/pdfplumber）

> ⚠️ **本仓库所有 `.md` 都是 UTF-8 无 BOM，而 PowerShell 5.1 默认按 ANSI(CP936) 解码。**
> 后果不是报错，而是**静默读出乱码**：CP936 的双字节汉字会把紧跟的 `\n` 吃掉，
> 于是 `Get-Content` / `Select-String` 给出**错误的行号和错误的行内容** ——
> 排查时会被它带到完全无关的地方去。
>
> **改用这两种办法之一**：
> - 读文件用 Read 工具、搜内容用 Grep 工具（都按 UTF-8 处理）；
> - 必须在 PowerShell 里跑 Python 时，一律显式 `io.open(p, encoding='utf-8')`。
>
> **不要用 `Select-String` 在这批文档里定位行号。**

---

## 6. Commit 规范

**Conventional Commits + 中文描述。**

```
<type>(<scope>): <中文描述>

[可选的正文]
```

| type | 用途 |
|---|---|
| `feat` | 新增模块或功能 |
| `fix` | 修复缺陷 |
| `docs` | 只改文档 |
| `sim` | 仿真激励或波形相关 |
| `refactor` | 重构，不改行为 |
| `chore` | 构建、工程配置、杂项 |

**scope** 用模块名：`clk_gen` / `seg_scan` / `dot_matrix_scan` / `keypad_scan` / `pattern_rom` / `rng_lfsr` / `puzzle_ctrl` / `game_fsm` / `buzzer_ctrl` / `top`

示例：
```
feat(seg_scan): 实现8位数码管动态扫描与BCD译码
fix(keypad_scan): 修正消抖计数器位宽不足导致的漏键
docs(01): 补充总体框图中各模块间信号定义
```

**规则**：
- **一个模块一次提交**，粒度要小
- 提交前该模块必须**已通过仿真**
- 禁止把多个不相关模块塞进一个 commit

---

## 7. 文档同步规则

| 发生了什么 | 必须同步更新 |
|---|---|
| 改了某个模块的实现 | `docs/02-模块详细设计.md` 对应小节 + `CHANGELOG.md` |
| 改了模块接口/端口 | **见下方 §7.1 的固定清单** —— 这是全项目落点最多的一类改动 |
| 跑了仿真 | `docs/03-仿真验证方案.md`（激励 + 判读结论）+ 波形图存 `docs/图/` |
| 改了引脚约束 | `docs/04-引脚分配表.md` + `quartus/*.qsf` |
| **发现任何问题**（编译报错、波形不符预期、上板异常） | 立即写 **`ERRORS.md`**（九字段，见下方「记录格式」） |
| 用 AI 做了任何事 | 追加 **`AI_LOG.md`** |
| 模块完成情况变化 | 更新 **`PROGRESS.md`** 模块进度表 |
| **改了 `docs/02` 某模块"仿真验证点"的条数** | `docs/03-仿真验证方案.md` **§4 的索引表 + 对应结果表的行区间**（2026-09-18 审查 `sim-01`：clk_gen / piece_rom / puzzle_ctrl / game_fsm 四处条数曾与 `docs/02` 实际条数对不上） |

### 7.1 ⭐ 「新增 / 修改一个端口」的固定落点清单

> **为什么单列一节**：这是**唯一一类会横跨 8 处**的改动，
> 而各处**单独看全都自洽** —— 矛盾只存在于"改了的"与"没改的"之间。
> ERR-0017 就是漏了其中 4 处（含**接口唯一真值源** `docs/01`）。

**按顺序走，一步不省**（以新增一个输入端口为例）：

| # | 落点 | 改什么 |
|---|---|---|
| 1 | `docs/01` §2.1 连接表 | 有没有新的**线**？系统级连线数要不要变？（**这张表是全图连线数的真值源**） |
| 2 | `docs/01` §5.x 接口表 | **端口逐条定义（唯一真值源）** |
| 3 | `docs/01` §2.1.x / §4.x | 连线表与全局约定的**说明文字**里引用到旧接口的地方 |
| 4 | `docs/01` §8 / §9 | 硬件自检条目、**I/O 引脚数** |
| 5 | `docs/02` 对应 §x.2/§x.3/§x.5/§x.6 | 端口表 · 内部结构图 · 设计要点 · 仿真验证点 |
| 6 | `docs/图/系统图.html` | **两层图都要改**（DWG-01 与 DWG-02），改完走 §12.4 五步 |
| 7 | `puzzle_top.vhd` / `board_test_top.vhd` 的 `component` 声明 | 照 §5 抄，**不许凭印象**（§10.3）；`port map` 漏写端口**不报错、静默悬空** |
| 8 | `quartus/*.qsf` + `docs/04` | 仅当涉及引脚；跑 `python scripts/check_pins.py` |
| 9 | 过程文档 | `CHANGELOG.md` / `ERRORS.md` / `AI_LOG.md` / `PROGRESS.md` / `HANDOFF.md` |

**改完必做两遍对账**（方向相反，**只做一遍必漏**）：

- 按 §10.3「**从线到模块**数一遍每个 `i_*` 的发出方」；
- 按 §12.4 第 5 步「**图 → 表**反向对账，数图上有几笔」。

> ⭐ **收尾动作（一秒钟能跑，专门抓漏）**：
> ```bash
> grep -rn "<新端口名>" --include="*.md" --include="*.html" --include="*.qsf" --include="*.vhd" .
> ```
> **改动的落点是可数的，只改了一半也是可数的。**

### `ERRORS.md` 记录格式

九个字段：**日期 / 阶段 / 涉及模块 / 现象 / 定位过程 / 根本原因 / 修复方法 / 预防措施 / 状态**。

⭐ **`定位过程` 是最重要的字段**——报告评分项⑤（15 分）的得分点在这里，
**不在"我们没出错"，而在"怎么一步步缩小范围"写得清不清楚**。
字段说明与范例见 `ERRORS.md` 开头的「编号规则」，**不再在本文档里抄一遍**。

---

## 8. 上板调试注意（板子只在实验室能用）

- **去实验室前**：先列好"本次待验证清单"，带上 `PROGRESS.md`，按清单逐条打勾。时间宝贵，不要到现场才想做什么
- **优先验证 §9 的前两条**（板载时钟档位是否为 **7 (50MHz)** / 键盘行列方向与极性）——
  各先烧一个最小自检程序：`board_test_top` 一次能验掉它们
- **回来立刻补**：`docs/05-硬件调试记录.md` + `ERRORS.md`

### 8.1 课件 PDF 里的实验板/开放实验注意事项（p60 / p63 / p64）

> **来源：课件《数字系统设计2026》p60（实验板）、p63（开放实验）、p64（器材发放）。**

| # | 事项 | 出处 |
|---|---|---|
| 1 | **输入时钟频率由右上角的 `USER_BTN` 切换选择** | p60 |
| 2 | **蜂鸣器声音小 → 顺时针调节点阵左上角的 `102` 电位器**（注意别太用力） | p60 |
| 3 | 温度传感器在**蜂鸣器的左上角**（本项目不用，仅记录位置） | p60 |
| 4 | **开放实验须在"Easy 云课堂"预约**：可预约时间窗 = **实验开始前 7 天 ～ 开始前 30 分钟** | p63 |
| 5 | **签到时间窗 = 实验开始前 10 分钟 ～ 开始后 30 分钟**，**至少 2 次** | p63 |
| 6 | 签到时系统**自动按顺序分配实验台**，**务必按分配的组号就座** | p63 |
| 7 | ⚠️ **实验板只能在实验室内使用，不要带出实验室** | p63 |
| 8 | **每班只借 3 块实验板 + 下载线**（登记资产编号），**第 8 周验收时由学委收回归还** | p64 |

> ⭐ **第 8 条是排期的硬约束**：全班只有 3 块板，**预约晚一步就排不上**。
> 本项目需要上板确认的风险点有 8 项（§9 与 `docs/01` §8.1），
> **越早把 `board_test_top` 做出来越好** —— 它一次能验掉 8 项。

---

## 9. 必须上板确认的风险点

| # | 风险 | 影响 | 应对 |
|---|---|---|---|
| 1 | 板载时钟档位不确定 | 所有分频都不对 | 分频常量单点定义；先烧时钟自检程序确认档位 7 |
| 2 | 键盘行列方向/极性判断错 | 按键全失效 | `keypad_scan` 的行列方向与极性做成常量，可一处翻转 |
| 3 | 扫描频率不当 → 闪烁/鬼影 | 显示效果差 | 扫描频率 ≥1kHz，切换时消隐；先仿真后上板微调 |
| 4 | 64 位掩码跨行回绕 | 零片贴右边缘时形状错乱 | 掩码运算逐行处理；穷举边界情形仿真 |
| 5 | 无 testbench/assert 可用 | 无法自动判错 | 用 `.vwf` 激励 + Python 脚本解析输出做自动比对 |
| 6 | 初步设计方案 9/24 硬截止 | 直接影响平时成绩 | 文档优先，先交设计再加功能 |

---

## 10. 模块清单（与总体框图一致）

**11 个实体**：`puzzle_pkg`（包）· `clk_gen` · `keypad_scan` · `seg_scan` · `dot_matrix_scan` ·
`pattern_rom` · `piece_rom` · `rng_lfsr` · `puzzle_ctrl` · `disp_format` · `game_fsm` · `buzzer_ctrl`
—— **逐个的职责与类型见 DWG-02 与 `docs/02`，不在本文档重复。**
**另有两个顶层**：`puzzle_top`（唯一做引脚绑定的文件）与 `board_test_top`
（**上板前的硬件自检顶层**，验证极性与行列顺序，非最终产物但长期保留）。

**架构关键分界（答辩要点）：**
- `puzzle_ctrl` 只管**空间**（坐标、掩码），**不含任何计数器**
- `game_fsm` 只管**时间**（状态、倒计时），**不含任何坐标运算**
- 这条分界让两个模块都能独立仿真、独立讲清楚

---

## 10.1 全局设计约定（实现时严格遵守）

| 项 | 约定 |
|---|---|
| 时钟方案 | **单一时钟域 + 多档单周期使能**（`tick_1k` / `tick_100` / `tick_2hz` / `tick_1hz`），不用门控时钟、不用多时钟域 |
| 复位 | **同步、高有效**，全文统一。**三源或**：`rst = btn_rst or por_rst or (not SW7)` |
| 按键复位 ⭐ | **BTN0 → `clk_gen.i_btn_rst`**，经 **2 级同步器 + 20ms 消抖**后并入 `o_rst`。**课件 PDF p45（一）强制"复位必须用按键来实现"** —— `por_rst` 只是上电初值，**不能替代按键**（见 §4.6） |
| 熄灭 | `sys_en = SW7` 组合控制显示驱动与蜂鸣器**输出级** → SW7=0 时即时全灭、无声 |
| SW7 消抖 | **不做消抖**（要求是即时熄灭，消抖反而违背要求）；在文档中说明理由 |
| 按键消抖 | **BTN0 20ms（`clk_gen` 内）＋ 矩阵键盘 80ms（`keypad_scan` 内）** —— 课件 p54「按键要注意防抖处理」＋手册附图16。**SW7 是拨动开关，不在"按键"之列**，故不消抖 |
| 库 | `std_logic_1164` + `numeric_std`；**禁用** `std_logic_unsigned`/`std_logic_arith` |
| ⭐ **顶层端口名** | **顶层器件端口一律不带 `i_`／`o_` 前缀**（`seg`／`cat`／`dot_row`／`buzz`…），**真值源是 `quartus/puzzle.qsf` 的 `-to`**；子模块端口才带前缀（`o_seg`…）。**两个顶层必须同名同义**（`board_test_top` 只多一个 `ld[15:0]`）。表见 `docs/01` §5.12／§5.13。⚠️ **写错不报错、静默悬空**（ERR-0020 / 报告 `hw-01`）|
| ⭐ **脉冲类信号的宽度** | `o_key_press`／`o_sel_out`／`o_dir_out`／`o_conf_out`／`o_round_start`／`o_sound_trig`／`o_rnd_step` 一律为 **1 个 `clk`**。**发出方保证、接收方按此消费** —— 这是跨模块契约，不写下来就会一边给 1ms、一边按 1 拍采（ERR-0020 / 报告 `INT-1`）|
| 点阵位序 | 64 位向量，**`mask(8*行 + 列)`**，列号右增。**bit0 = 左上角** |
| 行内位序 | 第 r 行 = `mask(8*r+7 downto 8*r)`；行内 **bit0 = 最左列**、bit7 = 最右列 |
| 移动方向 | 「向右移一列」= 行向量**左移**：`r(6 downto 0) & '0'` |
| 与驱动的对应 | `dot_matrix_scan` 扫描第 r 行时，直接把 `mask(8*r+7 downto 8*r)` 赋给 `dot_colr` —— 因为 COLR0 在实物上就是最左列，位序天然对齐，**无需任何翻转** |
| 零片表示 | **只存锚点 (row,col)，掩码组合计算**（单一数据源，省 256 个触发器，杜绝锚点与掩码不一致） |
| 边界不变量 | `行 + 高 ≤ 8` 且 `列 + 宽 ≤ 8` —— 由钳位与落位**主动保证**，从根本上排除回绕 |
| 正确性判定 | `所有零片掩码的并集 == 目标掩码`（与拼法无关，自动接受两种合法铺法） |

### ⚠️ 两个必须写进报告的坑（都是设计阶段主动规避的）

1. **LFSR 必须每次尝试推进一次**。若随机数只是每周期采样，同一棋子的多次尝试会读到同一个值 → 所有零片落点相同 → 必然重叠 → 用尽尝试次数 → 每次都走回退布局。
   **游戏仍能运行，只是永远不随机，这个 bug 直到有人数落点分布才会发现。**
2. **回绕靠"锚点不变量"根除，而非靠移位后修补**。落位函数与钳位函数共同保证不变量，移位前的边界判断只是第二道防线。

---

## 10.2 资源利用率约定（EPM1270 只有 1270 个 LE，省着用）

> **总原则**：本项目**对速度没有要求**，但对面积有。任何"用面积换速度"的写法都要反过来评估。
> 但**不能优化到影响功能**（见本节末尾的"三条红线"）。
>
> ⭐ **本节不是自创的偏好，它就是课件 §五（三）「器件资源的优化」的要求**
> （p46 四条 + p47–p50 各一例）。**课件页 ↔ 本节条目的逐条对应见 §11.4 的对照表**，此处不重复。

### ① `integer` 必须加 `range` 约束 ⭐ 最大的一笔

> **课件 p49 原文**：*"定义整数类型的信号和变量时一定要用 **RANGE** 子句指定范围。"*
> 注意课件说的是"**一定要**"——这不是风格建议，是要求。

**裸 `integer` 在 Quartus 里综合成 32 位。** 这是 VHDL 里最容易踩、也最容易省的一笔。

```vhdl
signal r_cnt : integer;                   -- ✗ 32 位寄存器，绝大部分永远用不到
signal r_bcd : integer range 0 to 9;      -- ✓ 4 位
signal r_div : integer range 0 to 6249;   -- ✓ 13 位
```

**硬性要求**：**凡是会变成硬件对象的 `integer`，必须带 `range`。**

| 位置 | 要不要 `range` | 为什么 |
|---|---|---|
| `signal` | ✅ **必须** | 直接综合成寄存器，裸 `integer` = 32 位触发器 |
| 进程内 `variable` | ✅ **必须** | 同上 |
| 子程序形参，**且参与算术** | ✅ **必须** | `r + ar` 里的 `ar` 若是裸 `integer`，加法器就是 32 位 |
| 子程序形参，**只作查表/比较** | ⭕ 建议 | 不加也能推出来，但加了是文档 |
| `constant` | ❌ 不需要 | 在 elaboration 阶段就被折叠成常量，不占任何资源 |
| `for` 循环变量 | ❌ **加不了** | VHDL-93 里循环变量是隐式声明的，语法上不允许写 `range` |

> ⚠️ **`for` 循环的边界必须是常量。** VHDL-93 的循环变量隐式带 `range`，
> 所以它本身是安全的——**但循环上界如果来自信号**（`for i in 0 to n`，`n` 是信号），
> 综合器只能按该信号的完整位宽展开加法器。**本项目禁止这种写法。**

> **检查方法**：综合后在编译报告里看寄存器数量；
> 或全项目搜 `signal.*: integer;` **和** `variable.*: integer;`，两条都要搜。

### ② 分频器串行级联，不要五个并行 ⭐

**✗ 原先的写法（五个独立计数器）**：每个都从 50MHz 直接分频 —— 即使加了 `range` 也要 **99 位**
（分频比 6250 / 50000 / 500000 / 25M / 50M → 位宽 13 / 16 / 19 / 25 / 26）。

**✓ 改成串行级联**：高频节拍分频低频节拍，每一级只数"上一级来几个脉冲"。

```
50MHz ──6250──▶ tick_8k ──8──▶ tick_1k ──10──▶ tick_100 ─┬─50──▶ tick_2hz
                                                          └─100─▶ tick_1hz
```

每级数 6250 / 8 / 10 / 50 / 100 个 → 位宽 13 / 3 / 4 / 6 / 7，**合计 33 位**。

**节省 66 位触发器**（相比加 `range` 的并行版），
相比裸 `integer` 的并行版（5×32 = 160 位）**节省 127 位**。

> ⚠️ **代价**：级联后各节拍之间有固定的相位关系（`tick_1hz` 一定紧跟某个 `tick_100`）。
> 本项目各节拍用途完全独立（扫描 / 消抖 / 倒计时），**相位关系无影响**。

### ③ 位宽按需最小化

| 对象 | ✗ 浪费写法 | ✓ 写法 |
|---|---|---|
| 状态编码 | 8 位 | **3 位**（6 个状态），不用 one-hot |
| 零片序号 | 8 位 | **2 位**（最多 4 块） |
| 行列坐标 | 各自 8 位 | 打包成**一个 6 位**：`{行[2:0], 列[2:0]}` |
| 点阵行计数器 | 8 位 | **3 位**（0~7） |
| 4×4 键盘键号 | 8 位 | **5 位**（1~16，**不是 4 位** —— 键号公式最大取到 16，4 位装不下，会被静默截断成 0 = "没按"） |
| 倒计时 | 6 位二进制 + 除法器 | **两位 BCD**（各 4 位，共 8 位，**且省掉除法器**） |

### ④ 其余可用的省面积手段

| 手段 | 说明 |
|---|---|
| **共享运算资源** | 4 块零片的 `place()` 若分别展开，逻辑重复 4 次；可由综合器共享，或在设计上串行复用同一套平移逻辑 |
| **`sll` 代替 `case`** | 行译码 `1 sll r`、位选 `1 sll digit` 都是一行，综合成译码器，比手写 `case` 更省 |
| **常量折叠** | 图案/零片掩码直接写成常量，让综合器在编译期折叠，而不是运行时计算 |
| **不用乘法器/除法器** | 本项目**应当完全不出现** `*` `/` `mod`。倒计时用 BCD 就是为了根除 `/10` |
| **降低扫描频率** | 见下方红线②——扫描频率只要能保证不闪即可，不必更高 |

> ⚠️ **`case` 与 `if-then-else` 的取舍**（课件 p49 原文：
> *"避免使用长的 if-then-else 语句，采用 case 语句来代替"*，
> 理由是**防止综合出较大的优先编码器**，且提高可读性）：
> **凡"一个信号按取值分派到多个结果"的，一律用 `case`** ——
> 长 `if-then-else` 链综合出来的是**带优先级的级联比较器**，
> 而 `case` 是**并行译码**，面积与延迟都更小。
> 本项目里对应的写法见 `docs/02` §12.2（键号译码）、§11.3（状态 → 显示）、§13.4（音效选择）。

### ⑤ 三条红线（优化不能越过它们）

| # | 红线 | 为什么 |
|---|---|---|
| 1 | **不牺牲可读性与模块边界** | 老师会照框图抽模块提问；为省几个触发器把两个模块合并，是**拿 20 分的设计分换几个 LE**，不划算 |
| 2 | **点阵/数码管扫描频率不得低于 1kHz（以 `i_tick` 计）** | **刷新率 = 扫描频率 ÷（8 路 × 相数）** —— 本项目每路 2 相（消隐 + 显示）→ **÷16**：`tick_1k` 只有 **62.5Hz**（已不可用），故实际用 `tick_8k` → **500Hz**。⚠️ 2026-09-18 审查：原公式漏掉消隐相（按 ÷8 算成 125Hz），与 `docs/01` §4.2 的更正账目冲突。这是"速度无所谓"的**唯一例外** |
| 3 | **不引入组合环、不用 latch** | 组合进程所有分支必须赋值，或开头给默认值 |

> **一句话**：省**寄存器位宽**和**分频器级数**（纯赚），
> 不省**模块划分**和**扫描频率**（会亏）。

### ⑥ Quartus 综合优化目标设为 **Area** ⭐（课件 p50 例4）

**课件 p46 第 ④ 条"软件优化"，在 p50 给了具体做法**：
`Assignments → Settings → Analysis & Synthesis Settings → Optimization Technique`
**选 `Area`**（出厂默认是 `Balanced`）。**本项目已在 `.qsf` 里显式写死**：
`set_global_assignment -name OPTIMIZATION_TECHNIQUE AREA`。

> **为什么必须选 Area**：EPM1270 只有 **1270 个 LE**，而本设计对速度**没有任何要求**
> —— 典型的"用面积换速度"要反过来做。**这是"软件优化"里性价比最高的一条**：
> 一行 `.qsf`，全局生效，不改任何代码。
>
> ⚠️ **编译后要在报告里体现它确实生效了**（评分项④要编译报告截图，见 §11.2）。

---

## 10.3 顶层例化规范：一律用 `component` 语法 ⭐

> **硬约束第 12 条**。适用范围：`puzzle_top.vhd` 与 `board_test_top.vhd`
> —— 本项目中**只有这两个文件例化子模块**，其余 11 个实体都是叶子。
> 若将来某个非顶层模块也要例化子模块，**同样照这一节写**。

### 为什么用 `component` 而不是直接例化

| # | 理由 | 说明 |
|---|---|---|
| 1 | **一个 `component` 声明 = DWG-02 的一个方框** | 顶层文件里数得出来的声明块数，就是图上数得出来的框数。老师照框图抽模块提问时，对应关系是**肉眼可见**的 |
| 2 | **端口名与位宽在顶层文件内全部可见** | 读一个文件就能看全所有连线，不用翻 11 个子文件。**评分项⑧「顶层文件的 HDL 代码（有注释）」10 分直接受益** |
| 3 | **课程惯例** | 数字电路实验课的经典 VHDL 例化风格就是 `component` + `port map`，阅卷时是熟悉的形式 |
| 4 | **VHDL-93 最保守的写法** | Quartus II 9.1 也支持 `entity work.x(rtl)` 直接例化，但 `component` 不可能出兼容问题 |

**代价（要如实认）**：端口列表**写两遍**（子模块实体一遍、顶层 `component` 声明一遍）
→ **改了子模块端口就必须同步改顶层的 `component` 声明**。

> ⚠️ **这是本项目新增的一处"必须两处同步"的地方**，且不一致时的报错**不一定显眼**：
> - 端口名对不上 → 报 `Error: ... has no port named "xxx"`（还好，能看见）
> - **端口名对得上但位宽变了** → 可能只出 Warning，**照样综合出一版错的电路**
> - **`port map` 里漏写一个端口** → 该输入**悬空**，综合器给个默认值，**不报错**
>
> **防法**：`component` 声明的端口**逐条对照 `docs/01-系统设计.md` §5 的接口定义表**
> —— 那张表是唯一真值源，与 §7「改了模块接口/端口 → 同步」的规则是同一件事。

### 写法模板（**骨架；端口列表故意不写**）

> ⭐ **这里不列任何具体端口。** 端口一律照 `docs/01` §5 抄 —— 在这里放一份副本，迟早与接口表对不上，
> 而 `component` 端口**位宽写错可能只出 Warning、`port map` 漏写端口根本不报错**（见上）。
> 这正是下面第 5 条的意思：**凭印象写必错**（ERR-0010 的模板端口就是凭印象写错的）。

```vhdl
architecture rtl of puzzle_top is
    -- ========== 子模块 component 声明 ==========
    -- 顺序 = 按子系统 S1→S7 分组，组内按 docs/图/系统图.html 的 DWG-02 从上到下
    -- 每个子模块一个 component 块（× 11）；端口逐条对照 docs/01 §5 接口定义表抄
    component <子模块名> is port ( … ); end component;

    -- ========== 内部互连信号 ==========  signal s_<线名> : std_logic;  …
begin
    -- ========== 例化（顺序与上面声明同序）==========
    u_<实体名> : <实体名> port map ( <端口名> => <信号名>, … );   -- × 11
end architecture;
```

> ⚠️ 顶层器件端口**不带 `i_`／`o_` 前缀**（`btn` 而不是 `i_btn`），
> 子模块端口才带 —— 真值源是 `quartus/puzzle.qsf` 的 `-to`（§10.1）。

### 五条写法要求

| # | 要求 | 为什么 |
|---|---|---|
| 1 | **`component` 声明块按「子系统 S1→S7 分组、组内按 `docs/图/系统图.html` 的 DWG-02 从上到下」排列**，与 `port map` 例化顺序**同序** | 组号可数（老师按 S1~S7 数框）；组内照图序，代码与图上下对应。⚠️ 原口径"按 DWG-02 的框序"照字面执行不了：矢量图按功能分区布版，严格照几何行序会把同一子系统的实体拆散，且原判据指向的 `docs/01` §2.2 已无图（ERR-0020 / 报告 `顶层与集成-4`） |
| 2 | **例化标签统一 `u_<实体名>`** | 仿真波形与综合报告里的层次名可读 |
| 3 | **端口关联一律用** `名字 => 信号`（具名关联），**不用位置关联** | 位置关联在位宽/顺序变化时会静默连错 |
| 4 | **每个 `component` 声明块上方写一行中文注释**：职责 + 所属子系统 | 同为评分项⑧的素材 |
| 5 | ⭐ **端口一律照 `docs/01` §5 抄，不许凭印象写**；写完后**从"线"这一侧反向核一遍** —— 数每个 `i_*` 端口有没有发出方 | 凭印象写必错（ERR-0010 的模板端口就是错的）；**反向核才查得出"没人驱动"**（ERR-0011，`docs/01` §10.2） |

### ⭐ 「输入端口有没有驱动源」怎么核（一条可数动作）

> **写 `puzzle_top` 之前、以及每次改完接口之后，走一遍这个动作。**

**遍历方向必须是「从线到模块」**：把 `docs/01` §5 里**所有** `i_*` 端口抄成一列，
逐个在 §2.1 的连接表里找它的**发出方**。

**不能按模块翻** —— 每个模块的端口表单独看都是自洽的
（`buzzer_ctrl` 说自己要一个 `i_trigger` 完全合理，它的职责描述也成立），
**缺线这个错误在"按模块看"时根本没有可比对象**。

| 方向 | 查得出 | 查不出 |
|---|---|---|
| 按模块翻（§5 一张张看） | 端口名/位宽写错（有东西可比） | ⚠️ **缺线 —— 空白不引人注意** |
| **从线到模块数**（数每个 `i_*` 的发出方） | ✅ **缺线** | — |

**本项目的人工核对结果在 `docs/01` §10.2**（查出 5 处，已全部修复，ERR-0011）。
**阶段 4 写 `puzzle_top` 之前应脚本化为 `scripts/check_ports.py`** ——
理由与 §12.4 同源：**叙述会被读过就忘，能跑的动作不会。**

---

## 11. 考核与提交要求对照（随时自查）

> **来源：课件《数字系统设计2026》p3（成绩评定方式）/ p4（实验报告要求）/ p65（报告提交要求）。
> 本节钉的是课件原文 —— 解读在各素材文件里，此处不重复。**

### 11.1 成绩构成（课件 p3）

| 项 | 占比 | 课件原文要点 |
|---|---|---|
| 平时 | **20%** | 平时实验情况（含开放）：考勤、纪律、实验台整理。**设计方案**：9/24 24:00 前云课堂提交**初步设计方案**，验收前提交**实际设计方案**，内容包括**总体框图和主要模块的详细设计** |
| 验收答辩 | **50%** | **功能验收**（分必做部分、选做或自拟部分）＋**提问答辩**（随机选择实际设计方案中总体框图的一个模块，进行**代码、仿真波形、设计及实现方法**答辩） |
| 实验报告 | **30%** | 见 §11.2 |

> ⚠️ **"随机抽一个模块"抽的是总体框图里的模块** —— 这正是"图与代码严格一致"（§12.3）
> 与"一个框 = 一个 `.vhd` = 一个实体"（§12.1）两条硬约束的由来，不是洁癖。
>
> ⚠️ **设计方案交两次**：9/24 交"初步设计方案"、验收前交"实际设计方案"。
> 本项目阶段 1 的四份主文档即**初步设计方案**；**验收前需按最终实现重新出一版**。

### 11.2 实验报告评分项（课件 p4 原文）

| 评分项（课件原文） | 分值 | 本项目素材来源 |
|---|---|---|
| ① 设计课题的任务要求 | 5 | `docs/00-需求分析.md` |
| ② 系统设计（包括设计思路、总体框图、分块设计） | 20 | `docs/01` + `docs/02` |
| ③ 仿真波形及波形分析 | **25** | `docs/03` + `docs/图/` 波形图（**做法受课件 p59 约束**，见 §5.3） |
| ④ 实际实现功能**说明**及资源利用情况（**编译报告截图**） | 5 | Quartus 编译报告 + `docs/04` §5.2 |
| ⑤ 故障及问题分析 | 15 | **`ERRORS.md`** |
| ⑥ 总结和结论 | 10 | `CHANGELOG.md` + `PROGRESS.md` |
| ⑦ AI 工具使用情况（包括使用的工具、提示词） | 10 | **`AI_LOG.md`** |
| ⑧ 顶层文件的 HDL 代码（**有注释**，**整体工程单独提交**） | 10 | `rtl/puzzle_top.vhd` ＋ 整个 `quartus/` 工程 |

> ⚠️ **第 ④ 与第 ⑧ 两项各有一个易漏点**：
> - ④ 要"实际实现功能**说明**"＋"资源利用情况（**编译报告截图**）"——
>   **必须附编译报告截图**，不能只写"能放下"。`docs/04` §5.2 已留好填写模板，
>   且 `.qsf` 的优化目标已按课件 p50 设为 **AREA**（§10.2 ④），两件事要对上；
> - ⑧ 括号里是**两件事**："有注释"要求 `puzzle_top.vhd` **本身注释完整**（本项目按 §2 第 6 条做），
>   "**整体工程单独提交**"指**整个 Quartus 工程要一并交**（见 §11.3）。

### 11.3 提交物与命名（课件 p65 原文）

| # | 要求 |
|---|---|
| 1 | 提交含**实验报告、工作视频、完整 project（包括源程序、仿真波形及相关文件）**的压缩文件 |
| 2 | **压缩文件名命名：题目-姓名** |
| 3 | **实验完成后的一周之内**在**云课堂和教学平台**提交压缩文件 |

> ⭐ **"工作视频"是本项目此前完全没列入计划的一项交付物** ——
> 它不在 8 个报告评分项里，但**在提交清单里**，缺了就是材料不齐。
> 应在阶段 5（上板调试）**顺手录**：板子只在实验室能用，出了实验室就补不出来了。
>
> **"相关文件"至少包含**：`rtl/` 全部源码、`quartus/` 工程（`.qpf`/`.qsf`）、
> `sim/` 的 `.vwf` 与激励脚本、`docs/` 的设计文档与波形图、
> 以及本套过程记录（`ERRORS.md` / `AI_LOG.md` / `CHANGELOG.md` / `PROGRESS.md`）。

### 11.4 课件设计类要求对照表 ⭐

> **课件正文不含题目 4 的细则**（p62 明写"题目内容和要求见教学云平台和云课堂"），
> **但它含多条通用强制要求**。本节把它们逐条钉到本项目的落点，
> **防止"改了一处、忘了另一处"**。

| 课件页 | 要求（原文摘） | 本项目落点 | 状态 |
|---|---|---|---|
| **p45（一）** | 开机复位：初值只具有仿真意义，**复位必须用按键来实现** | `btn` → `clk_gen.i_btn_rst`；`docs/01` §2.1.3 / §5.1、`docs/02` §3 | ✅ |
| **p45（二）** | 限流保护：数码管、LED、点阵、**按键**等要加限流保护电阻（100~300Ω） | **硬件层**：板载已含限流/驱动电阻，本项目直接用板，不自行外接 —— 落点 `docs/04` **§4.1**（2026-09-18 审查 `pdf-4` 补：原指向 §4，而 §4 通篇没有"限流"二字，是悬空引用） | ✅ |
| **p46 ①** | **资源共享**：耗资源多的模块用选择/复用方式共享 | `piece_rom` 的 4 块零片共用同一套 `place()` 平移逻辑（§10.2 ④） | ✅ |
| **p46 ②** | **串行化**：把并行逻辑提取成相同模块、时间上复用 | **分频器串行级联**（`docs/02` §3.3）：33 位 vs 99 位 | ✅ |
| **p46 ③** | 不完整 IF 语句的综合结果**会导致时序电路**（锁存器），资源浪费 | §10.2 ⑤ **红线 3**：组合进程全分支赋值或开头给默认值 | ✅ |
| **p46 ④ / p50** | **软件优化**：Quartus `Optimization Technique` 选 **Area** | `quartus/puzzle.qsf` 的 `OPTIMIZATION_TECHNIQUE AREA` | ✅ |
| **p47–p48 例1~3** | 课件给 ①/②/③ 各配的图例（共享一个译码器 / `1MHz ÷5000→200Hz ÷2→100Hz ÷100→1Hz` **级联分频** / 缺 `ELSE` → 4 位锁存器） | **是上面三行的图示版**：① = §10.2 ④；② **与本项目分频方案同构**（§10.2 ②）；③ = 红线 3 | ✅ |
| **p49** | ① 避免长 `if-then-else`，**用 `case` 代替**（防大优先编码器）<br>② 定义整数类型信号/变量**一定要用 `RANGE` 子句** | ① §10.2 ④ + `docs/02` 各 `case`；<br>② §10.2 ① **最大的省面积手段** | ✅ |
| **p51** | 动态扫描：PLD 直接驱动多数码管/点阵**必须动态扫描**（引脚 `7×4=28` → `7+4=11`；共用译码、减少耗电、减少引脚） | `seg_scan` / `dot_matrix_scan` 两个 8 路扫描（≥1kHz，§10.2 ⑤ 红线 2） | ✅ |
| **p54** | 4×4 矩阵键盘扫描减少 I/O；**按键要注意防抖处理** | `keypad_scan` 80ms 消抖；**BTN0 20ms 消抖**（§4.6 / `docs/02` §3） | ✅ |
| **p55** | 键盘扫描示例表（**低有效**，与板手册相反） | **以板手册为准、以上板自检为最终依据**（`docs/04` §3.3 / §4） | ✅ |
| **p58** | 多重驱动：两个以上进程写同一信号 → `Multi Drive` 错误 | 全项目**一个信号只在一个进程里写**。规则表述见 `docs/02` §10.5 的进程规范；**实例**见 §4.4（`r_phase`）、§12.5（倒计时寄存器）、§10.6/§10.7（`r_anchor`）。<br>⚠️ **2026-09-18 审查查出这条曾被 3 个模块违反**（`ERRORS.md` ERR-0020）—— 原先标 ✅ 是**标错了**，现已改真 | ⚠️ **已修正（曾违反）** |
| **p59** | 仿真：**加入中间信号**，便于查看电路状态变化 | §5.3 新增专条：`.vwf` 必须含状态/计数器等中间信号 | ✅ |
| **p60** | 实验板：`USER_BTN` 切时钟档位；蜂鸣器音量调 `102` 电位器 | §8.1 第 1~2 条 | ✅ |
| **p62** | 题目内容和要求见**教学云平台和云课堂**（课件正文不含） | ⚠️ **题目 4 的细则须另从云平台取**，`docs/00` 的依据即来于此 | ⚠️ |
| **p63 / p64** | 开放实验预约与签到时间窗；**实验板不得带出实验室**；每班 3 块板 | §8.1 第 4~8 条 | ✅ |
| **p65** | 提交物含**实验报告 + 工作视频 + 完整 project**；命名"题目-姓名"；一周内提交 | §11.3（**工作视频为新增待办**） | ⚠️ |
| **p30–32** | 有限状态机：Moore/Mealy；**三进程/双进程/单进程**描述方式 | `docs/02` **§12.3.1** 已明确声明本设计为 **Moore 型 + 双进程·形式 2**（课件 p32 原表：进程1 描述次态逻辑+状态寄存器、进程2 描述输出逻辑）。⚠️ 2026-09-18 审查 `pdf-2` 更正：原写"形式 1"，与 §12.6 骨架不符 | ✅ |
| **p13–19** | 自顶向下模块化范例：**顶层用 `COMPONENT` 声明 + `PORT MAP` 例化** | §10.3 顶层例化规范（本项目硬约束第 12 条） | ✅ |
| **p22** | 方框图范例：**`复位` 是独立输入框** | DWG-01 补 `btn` 一笔（`docs/01` §2.1.3） | ✅ |
| **p7 / p9** | **有无控制器是区别数字系统与功能部件的标志** | 本设计有 `game_fsm` 主状态机（控制器）＋ 数据处理器（`puzzle_ctrl` 等） | ✅ |
| **p11–12** | 自顶向下三步：需求分析 → 系统划分 → 逻辑描述 | `docs/01` §1.1 的三层分解（需求 → 7 子系统 → 11 实体） | ✅ |
| **p12 / p20 · p23–29** | 描述方法：方框图（p20）、定时图（p23）、逻辑流程图（p24）、ASM 图（p27）、MDS 图（p28） | 方框图 = DWG-01/02；MDS（≈状态转移图）= DWG-03（状态框内加注输出，照 p28"表示输出"的惯例）；定时图 = `docs/03` 的仿真波形（p23 明说可用 EDA 波形）；逻辑流程图 / ASM 图**不单独画**（行为已由状态输出表 + 迁移边表 + 进程结构确定）—— 逐一对应表见 `docs/02` §12.3.1 末尾（2026-09-18 审查 `pdf-3` 补） | ✅ |

> ⚠️ **两个未闭合的 ⚠️ 待办**：① 题目 4 的细则在云平台（课件正文没有，需要时到"Easy 云课堂"取）；
> ② **"工作视频"尚未排期**（必须在阶段 5 上板时录，见 §11.3）。
>
> 📌 **本表的用法**：课件更新、或老师口头补充要求时，**先往这张表加一行**，再决定落到哪个文件。
> **没有落点的要求 = 将来必漏。**

---

## 12. 图表规范（方框图 / 状态转移图）

> 老师会**照着总体框图随机抽一个模块**提问代码和仿真波形 —— 图与代码必须严格对应。

### 12.1 必须有的两类图

| 图 | 内容 | 约束 |
|---|---|---|
| **DWG-01 总体框图（第一层）** | 7 个功能子系统及其互连 | 每框 = 一个**功能子系统**（含若干实体），**框内要列出所含实体名** |
| **DWG-02 模块框图（第二层）** | DWG-01 展开到实体 | **一个矩形框 = 一个 `.vhd` 文件 = 一个实体** |
| **DWG-03 状态转移图** | `game_fsm` 的状态与迁移 | 每个状态一个圆圈/矩形，箭头标**迁移条件**，箭头上写清触发信号 |

> **为什么分两层**：一张图里平铺 11 个实体，读者（和答辩老师）看不到系统是怎么**分块**的。
> 第一层回答"这个系统由哪几件大事组成"，第二层回答"每件大事由哪几个实体实现"。
> 这**就是自顶向下分解的可视化**（见 §2.1），不是画两张图凑数。

**方框图的硬性要求**：

1. **DWG-02 里一个矩形框 = 一个 `.vhd` 文件 = 一个实体**，框内文字就是实体名；
   **DWG-01 里一个框 = 一个功能子系统**，框内须列出它包含的实体名，
   使两层能对得上（子系统所含实体之和 = 全部 11 个实体，不重不漏）；
2. 框之间的连线必须标**信号名**（与代码里的端口名一致）；
3. 数据流方向用箭头，**不要画成双向**（本设计全是单向数据流 + 一条反馈）；
4. 顶层 `puzzle_top` 单独画一框，**引脚只出现在它这一层**。

**图与代码的对应关系（答辩会被问）**：老师"照着总体框图随机抽一个模块提问"时，
抽到的是 **DWG-02 的实体**——因为那才是 `.vhd` 文件。DWG-01 是它的组织方式。

**当前图集**（均在 `docs/图/系统图.html`，内联 SVG，可导入 draw.io）：

| 图号 | 内容 | 第一层框数 | 第二层实体数 | 布局来源 |
|---|---|---|---|---|
| DWG-01 | 总体框图（功能子系统） | 7 | — | **用户飞书白板稿**（第 6 次会话定稿） |
| DWG-02 | 模块框图（实体级） | — | 11 + 2 顶层 | AI |
| DWG-03 | `game_fsm` 状态转移图 | 6 个状态 | — | AI |

**状态转移图必须覆盖**：`S_SELF_TEST` / `S_IDLE` / `S_PREVIEW` / `S_PLAYING` / `S_WIN` / `S_FAIL`
共 6 个状态，以及**所有**迁移边（含"任意状态按开始键 → 重开"这类边）。

### 12.2 工作方式（分工）

| 步骤 | 谁做 |
|---|---|
| 1. 生成图（HTML + 内联 SVG，浏览器可直接打开） | **Claude** |
| 2. 用 draw.io 等工具美化、调整布局 | **用户** |
| 3. 导出 PNG/SVG 存入 `docs/图/`，插入设计文档与报告 | **用户** |

**存放位置**：`docs/图/`（HTML 源文件与导出的图片都放这里）。

> **为什么先出 HTML+SVG**：SVG 是矢量格式，**draw.io 可以直接导入**，
> 导入后每个矩形框、每条箭头都是**可编辑的独立对象**，
> 比从 PNG 重画快得多。HTML 只是为了让用户能直接在浏览器里看到效果。

### 12.3 图与代码的一致性

**改了模块接口 → 必须同时改框图和 §2.1 的接口定义表。**
图、文档、代码三者不一致，是答辩时最容易被抓的失分点。

**DWG-01 的权威版本是 `docs/图/系统图.html` 的矢量版**（可编辑、与代码同步）；
`docs/图/DWG-01-系统总体框图-飞书原稿.png` 只是用户手绘稿的**留档**。
两者有出入时改矢量版、重新导出 PNG，**不要反过来照着 PNG 改矢量图**。

---

### 12.4 图表定稿检查清单（每张图都要走一遍）

> **为什么单独列一节**：`check_svg.py` 是绿的、图**仍然是错的** —— 这个教训 ERR-0006 记过一次，
> ERR-0008 **又原样复发了一次**。原因是当时只把它写进了 `ERRORS.md`（叙述），没变成固定动作（清单）。
> **叙述会被读过就忘，清单不会。**

| # | 动作 | 为什么这一步不能省 |
|---|---|---|
| 1 | `python scripts/check_svg.py "docs/图/系统图.html"` | 查 XML 合法性 / 填充 / 属性覆盖 / **走线穿框** / id 冲突。**这一步只保证"文件合法"** |
| 2 | **渲染成 PNG** | 用无头 Chrome 或浏览器截图 |
| 3 | **放大 2.5~3 倍，分上/中/下逐段看** ⭐ | **最关键的一步**。标签压线、走线压框这类缺陷**在整图缩略时全都看不出来**，ERR-0006 与 ERR-0008 的六处缺陷无一例外 |
| 4 | 逐条念一遍图上的每一笔：**"它为什么在这儿"** | 本次那条多余的节拍线，就是"为了像手绘稿而留、没有功能理由"的线——**代价是查不出来的 bug 入口** |
| 5 | ⭐ **图与连线表对账要走两遍、方向相反**：「表→图」查不一致，「图→表」查漏画 | **只做一遍必漏** —— ERR-0013 的 3 条漏画线（含 `rnd_step`）就是只做「表→图」时漏掉的。**`docs/01` §2.1 共 18 条连线（4 条系统级 + 14 条子系统出线），改图时直接数这个数**（2026-09-18 由"16 条 = 3 + 13"改口径：原表把键盘的 `kp_row`/`kp_col` 两条漏在两类之外，图↔表两向数出的数不一致） |

> ⚠️ **第 1~4 步默认"图是完整的、只是可能有瑕疵"，不覆盖「完整性」这一维。**
> "图上漏画了一条线"没有任何视觉线索，`check_svg.py` 也没有可比对象
> —— 这正是第 5 步存在的理由（ERR-0013 暴露的清单盲区）。

**校验器查不到什么（必须心里有数，别把"脚本没报错"当成"已经检查过了"）**：

| 查不出的问题 | 为什么 | 只能靠 |
|---|---|---|
| **标签压线 / 压框** | 需要字体度量（字符数 × 字号估宽） | 第 3 步放大看 |
| **标签压边界 / 压虚线框** | 边界框是 `fill:none`，第 ④ 类检查**已豁免它**（ERR-0007）→ 文字压上去无人管 | 第 3 步放大看（ERR-0009） |
| **走线互相重叠** | 脚本只查"线 vs 有填充的方框" | 第 3 步放大看 |
| **语义画错**（多画/漏画/接错信号） | 格式与几何都对 | 第 4 步逐笔追问 + 与 `docs/01` §5 接口表核对 |
| **图上整个少了一条连线** | 没有可比对象 —— 缺口是**空白**，不像"画错"那样有东西可对 | **第 5 步：反向对账（图→表）**（ERR-0013） |
| **线在、名不全**（标签只写了代表信号） | 脚本只算几何、不读标签内容；反向对账若只数线条数也发现不了 | 第 5 步对账时**逐个信号名念过去**，与 `docs/01` §2.1 该条的信号列比对（ERR-0020 / 报告 `顶层与集成-6`：`piece_rom → puzzle_ctrl` 那条表里 4 个信号、图上只标 1 个） |
| **图与代码不一致** | 脚本不看 `rtl/` | 改接口时同步改图（§12.3） |

> ⚠️ **修一个误报时，要顺带问一句"豁免掉的东西，还有没有别的问题需要盯"。**
> ERR-0007 把 `fill:none` 的虚线边界框从"障碍物"里豁免掉是**正确的**
> （外部信号本来就该穿进去），但豁免之后，"**文字压边界**"就再没有自动检查兜底了
> —— 这是 ERR-0009 的成因。**豁免造成的盲区要显式记下来。**

**写标签的一条硬规则**：**文字要朝"余量大的一侧"生长。**

| 标签右边是什么 | 该用什么对齐 |
|---|---|
| 别的线 / 方框 / 图幅边界 | **右对齐**（`text-anchor="end"`），让文字向左长 |
| 一片空地 | 左对齐即可 |
| 两侧都空 | 居中最省心 |

> 反面例子（ERR-0009）：`sw7 系统开关` 左对齐在 `x=70`、文字宽 79 单位 → 右端落到 `x≈158`，
> **压过容器边界（`x=150`）8 个单位**；它左边只剩 70 单位余量，**"把 x 调小"根本走不通** → 只能右对齐。

> **一句话**：脚本管**格式**，眼睛管**对错**。两件事，缺一不可。

---

## 13. 过程记录要求（为报告积累素材）

> 报告评分项③仿真波形 25 分、⑤故障分析 15 分、⑦AI 使用 10 分，**共 50 分**，
> 全部依赖过程记录。**这些分是"当时记下来"的，事后补写拿不到。**

### 13.1 每次会话都要留痕

| 记什么 | 记到哪 | 时机 |
|---|---|---|
| 用了什么提示词、得到了什么、人工改了什么 | `AI_LOG.md` | **实时**，当场记 |
| 遇到的任何异常（编译报错、波形不符、上板异常） | `ERRORS.md`（九字段） | **实时**，解决完立刻写 |
| 本次改了什么 | `CHANGELOG.md` | 收工时 |
| 进度到哪、下次从哪继续 | `PROGRESS.md` | 收工时 |
| **仿真的原始数据**（激励条件、观测点、实测值、期望值） | `docs/03-仿真验证方案.md` | 每次跑完仿真 |
| 波形图 | `docs/图/` | 每次跑完仿真 |

### 13.2 "多记录数据"具体指什么

跑仿真时**不要只记"通过了"**，要留下**可复现、可画进报告的原始数据**：

- **激励条件**：时钟频率、复位时刻、各输入信号在哪些时刻取什么值；
- **观测点**：看了哪些信号，为什么看它们；
- **实测值 vs 期望值**：具体数字或波形截图，**不是"符合预期"四个字**；
- **编译数据**：每次编译的 LE 占用、寄存器数、引脚数 —— **留一条时间线**，
  报告评分项④要用，而且能看出资源优化前后差多少。

> **反面例子**：`docs/03` 里只写"仿真通过"。
> **正确写法**：给出激励波形图 + 观测信号的实测值表 + 与期望值逐项比对的结果。

### 13.3 提示词记录

`AI_LOG.md` 每条任务记四样：**关键提示词（原文）/ AI 产出 / 人工修正 / 结论**。
**"人工修正"这一栏最能得分** —— 它体现了人对 AI 输出的判断，而不是照单全收。
