# HANDOFF — 会话交接单

> **给下一个会话的 Claude：开工按 `CLAUDE.md` §0 的顺序读 ——
> 本页 → `PROGRESS.md` → `CHANGELOG.md` 最后一节 → `ERRORS.md` 未解决的问题。**
> 本文件只回答一件事：**现在该干什么、别踩什么坑**。
> 详细状态在 `PROGRESS.md`，工作约定在 `CLAUDE.md`，本文件不重复它们。

**最后更新**：第 21 次会话 · 2026-09-24

> ⚠️ **这一行是收工必改项之一** —— 第 13 次会话的审查查出，此前收工清单
> （`CLAUDE.md` §0 第 0 条）只要求改本文件的 §2/§3/§4，**没包括这行表头**，
> 于是它曾连续两次会话没更新（见 `ERRORS.md` ERR-0020 / 审查报告 `CRIT-4`）。
> **现已把"表头"并入收工条目。**
>
> ⚠️ **此后又漏过一次**：第 15 次会话**整个收工流程都没走**，这行停在"第 14 次"，
> `CHANGELOG.md` / `AI_LOG.md` 也都没有它的条目（`ERRORS.md` **ERR-0021**）。
> **触发判据因此改了**：不是"是不是新会话"，而是「**这一轮有没有产生提交**」——
> 续跑、补做、临时插进来的一轮，只要落了盘就算一轮。见 `CLAUDE.md` §0 收工时的抬头。

---

## 1. 这个项目是什么

《数字电路与逻辑设计实验（下）》综合课题 **题目4《简易拼图游戏的设计与实现》**。
在 MAXII 实验板（**EPM1270T144C5**）上用 **VHDL** 实现 8×8 双色点阵拼图游戏，
全流程基于 **Quartus II 9.1**（工具路径与命令见 `CLAUDE.md` §5.1 / §5.2）。

**用户最看重的两件事**（都是明确要求过的）：

1. **过程记录就是交付物本身**。报告 8 个评分项里有 50 分（仿真波形 25 / 故障分析 15 / AI 使用 10）
   直接取自 `ERRORS.md` / `AI_LOG.md` / `docs/03`。**这些分是当场记下来的，事后补写拿不到。**
2. **每完成一项工作就停下来汇报**，不要攒着连续推进、不要自行扩大范围。

---

## 2. 现在到哪了

**阶段 1（设计文档）封版**；**阶段 2/3/4（RTL 编写与集成）已完成 —— `rtl/` 下 14 个文件全部写出、
两个顶层都通过 Quartus II 9.1 全流程编译**：

| 顶层 | 逻辑单元 | 引脚 | Fmax | 说明 |
|---|---|---|---|---|
| `board_test_top` | 439 / 1270（35%） | 68 | — | 上板硬件自检（9 阶段） |
| `puzzle_top` | **1226 / 1270（97%）** | 52 | **63.66 MHz** | 整机拼图游戏 |

> ⚠️ **LE 余量只剩约 44 个**。改动任何代码后**先看编译报告的 LE 数**；
> 时序读数用 `quartus_sta`（经典 `quartus_tan` 不读 `.sdc`，Fmax 那一栏会是空的）。

⭐ **实现与原设计文档有三处受器件约束的偏离**（都是先编译、再按实测数字改的）：
① `puzzle_ctrl` 的像素/判定由"4 路并行组合"改为**时分复用行扫描引擎**（32 拍刷一帧），
`o_px_red/green` 变成寄存输出；② 散落的重叠判定由"掩码求交"改为**包围盒不相交**；
③ `clk_gen` 的上电复位改数 `tick_1k`（`puzzle_pkg` 的 `CNT_POR` → `T_POR_MS`）。
完整经过见 `ERRORS.md` ERR-0026 / ERR-0027 与 `CHANGELOG.md` 第 19 次会话一节。

**阶段 5（上板）已第一次开跑**（2026-09-24，第 21 次会话）：`board_test_top` 已烧进真板
（Erase→Program→Verify 全过，JTAG ID `0x020A30DD`）。现场报的两条"异常"**都不是硬件故障**：

| 现场原话 | 真相 |
|---|---|
| 拨 SW7 后"**非常混乱**" | 9 阶段轮播的**正常画面**：`ld[15:8]`=阶段号（每 2 秒跳一次），`ld[7:0]`=**子步号**（每 0.25 秒 +1）→ 整排一直在动 |
| SW7=0 时"只有 LD8 亮黄色" | **预期**：`ld` **不受 SW7 门控**（只门控 seg/cat/dot_*/buzz），SW7=0 时停在阶段 1 |
| 按住 BTN0 没让 16 灯全亮 | ✅ **已闭环（13:38）**：按住**最右边**那个键 → **16 灯全亮**。当时是**按错了键**（`btn` 通路、LED 16 位通路与极性全部正常） |

→ 记 **`ERRORS.md` ERR-0030**；顺手用板手册原文补齐了"**板上位置速查**"（`docs/04` §7.3：
BTN0 在哪 / SW7 往上才是开 / `FREQ` 指示灯看第 7 格 / LD0 在最右端）。
**代码一行未改** —— 结论是"实现对、清单错"。

✅ **13:38「30 秒判据」四步全过（`docs/05` §1.5）**：

| 现场回报 | 判定 |
|---|---|
| "确认了是 50MHz" | ✓ 时钟档位 7，分频系数全部有效 |
| SW7=1 后"所有 LD 灯没有规则地闪烁，**但有周期**" | ✓ = 阶段号 + 子步号的**正常画面** |
| "**按住最右边那个键：16 灯全亮**" | ✓ `btn`/`PIN_61` 通路 + LED 16 位通路 + 极性全通 → 此前"不亮"确系**按错键** |

→ 上板清单第 1/10 条、`docs/06` §6.1 的"档位不对"一行**全部排除**；
**没有发现任何硬件故障**。

⭐ **14:11 已切到整机 `puzzle_top` 并烧进真板**（`set_top.py` → 重编译 → 烧录全过）：
顶层 `puzzle_top`、**1226/1270 LE（97%）**、52 引脚、0 errors；
TimeQuest **setup slack +4.292 ns / hold +1.379 ns** → **Fmax ≈ 63.7 MHz ≥ 50 MHz**。
**自检阶段 2~8 有意跳过**（用户 14:04 决策）—— 代价是点阵/数码管/蜂鸣器那 5 项至今无独立确认，
详见 `docs/05` §1.6。

> ⚠️ **`r_seen` 的坑**（`ERRORS.md` **ERR-0033**，本日第 4 条）：
> **按满 16 个键后 `r_seen` 恒为全 1 → 阶段 7 每圈都在 0.25 秒内退出**，
> 表现为"**进不去阶段 7**"。**必须按 BTN0 或拨 SW7 复位**才能再看到。
> 代码修复有意推迟（只影响自检工具，且当时板上正跑整机）。

**截止**：初步设计方案已完成 · 第 9~10 周验收答辩。

> 完整的阶段表、模块进度表、进行中与阻塞项见 `PROGRESS.md`（唯一账本）。

> ⚠️ **旧笔记作废**：凡引用"工程还不能编译""`rtl/` 是空的""LE 约 600""Fmax 未验证"的，
> 一律以本页与 `PROGRESS.md` 为准。

---

## 3. 你现在该做什么（按顺序，别跳）

> ✅ **代码已经全部写完并编译通过**（第 19 次会话）。
> ⚠️ **板子只在实验室能用 —— 只要人还在实验室，就先做第 0 步**（出实验室就补不出来）；
> 第 1 步（仿真）随时能做。
> ⭐ **计划已于 14:04 变更**（用户拍板）：**不再逐条打勾自检阶段 2~8**，
> 改为**直接烧整机 `puzzle_top` 看效果、之后再完善**。
> **板上现在跑的是 `puzzle_top`**（14:11 烧录成功，1226/1270 LE，Fmax ≈ 63.7 MHz）。

### 第 0 步【优先】：整机 `puzzle_top` 上板演示 + 录视频

**已完成**：切顶层 → 编译（0 errors）→ 时序（setup slack **+4.292 ns**）→ 烧录（Erase→Program→Verify 全过）。

**期望现象（SW7=1）**：`puzzle_top` 的开机自检 `S_SELF_TEST` ——
**点阵全黄 2 Hz 闪烁 + 8 位数码管全显 "8" 且闪烁** → 之后进待机。
⚠️ **它只持续 2 秒**（**要求 2 明文**："2 秒后系统进入待机状态"；`T_SELFTEST = 2`）——
**看不到 ≠ 故障**，多半是那 2 秒过去了（`ERRORS.md` ERR-0034）。
要看它：**先盯住板子再拨 SW7** / **按住最右按键再松开** / **录视频回放前 3 秒**。
之后进**待机**：点阵全灭、**DISP7 显 "5"、DISP0 显关卡号 "1"**（= 要求 2 的原文画面）。

> ⚠️ **未闭合项（`ERRORS.md` ERR-0034，状态 = 待补测）**：现场报"按住最右按键松开后也直接进待机、
> 没有 2 秒自检"。代码/约束侧已**逐环节核完无异常**（`tick_2hz` / `r_self_cnt` / `disp_format`
> 自检分支 / `puzzle_top` 的 `btn` 接线 / `.qsf` 的 `PIN_61`）。
> **先做测试 A（1 秒）**：**按住最右按键不松，看数码管/点阵是否全灭** ——
> **全灭** = 复位通路正常（用录视频复核）；**仍显示 "5"** = **BTN0 没接进复位**（真问题）。
> ⚠️ 注意：`board_test_top` 上"按住 BTN0 → 16 灯全亮"**证明不了**这条 —— 那行是**组合直读**，
> 绕过了同步器/消抖/`o_rst`，只证明**引脚**通。
⚠️ **与 `board_test_top` 的 9 阶段画面完全不同**，别拿自检画面去套。
⚠️ **`puzzle_top` 没有 `ld` 端口**，16 个 LED 不受本设计驱动。

**验收动作**（按 `docs/02` §14.3 的 13 个场景）：第一个交互是**按「开始」** ——
**它同时也是键盘好坏的试金石**：按下去能进预览 = 键盘通；按了没反应 = 键盘有问题。

**键盘映射（演示前记牢，`ROW3` 在最上排）**：
```
ROW3:   -    上     -    开始
ROW2:   左   下    右    选择
ROW1:   -     -     -    确认
```

| 看到 | 大概率原因 | 处置 |
|---|---|---|
| 点阵左右镜像 / 上下颠倒 | `COLR0` 位序是**假设**（手册未交代） | 只改 `.qsf` 位序，**代码不动**（`docs/06` §6.2） |
| 数码管数字乱 / 位序反 | 段码或位选顺序 | 同上 |
| 按键完全没反应 | 键盘行列或极性 | 切回 `board_test_top` 用阶段 7 单独验 |
| 蜂鸣器无声 | 音量电位器 / 无源有源 | 顺时针调点阵左上角 `102` 电位器 |

- ⬜ **录一段工作视频**（课件 p65 的提交物，出了实验室补不出来）。
- ⬜ 顺手看**数码管亮度**（占空恒 1/16，仿真查不出）与点阵/数码管**保护电阻丝印**。
- 若某条不符：把**现象原话**填进 `docs/05` §1.6 + 记 `ERRORS.md`。

> ⚠️ **代价如实记（别忘）**：点阵行序/列序/红绿、数码管位序/段码、蜂鸣器这 **5 项至今没有独立确认过** ——
> 整机若显示异常，**第一嫌疑是它们，不是游戏逻辑**。自检阶段 2~8 的完整表见 `docs/01` §8.2，
> 随时可切回 `board_test_top` 补做（`python scripts/set_top.py board_test_top` → 重编译 → 烧录）。
>
> ⚠️ **`r_seen` 的坑**（`ERRORS.md` ERR-0033）：**按满 16 个键后阶段 7 会变成 0.25 秒一闪而过**
> （表现为"进不去阶段 7"）—— **必须按 BTN0 或拨 SW7 复位**才能再看到。

### 第 1 步：写 `scripts/sim.py` ＋ 第一个模块的 `sim/tb_*.py`

`scripts/vwf.py`（`.vwf` 读写库）已就绪；仿真机制早已实测跑通（`CLAUDE.md` §5.3），
差的是**驱动脚本**。当初暂缓的理由（"等 `clk_gen` 写出来再定接口"）现在已消失。

按 `CLAUDE.md` §5.3 的标准循环走，**每个模块一步不省**：

```
写 sim/tb_<模块>.py  →  Python 生成 .vwf  →  跑 quartus_sim
→  Python 解析回写结果 + 与参考模型逐点比对  →  渲染波形图存 docs/图/
→  把原始数据写进 docs/03  →  单独 commit
```

> ⚠️ **`.vwf` 里必须含"中间信号"**（课件 p59 硬要求，见 `docs/03` §3.1）：
> 状态寄存器、倒计时寄存器、扫描计数器、消抖计数器、`r_btn_rst` … 只给端口波形拿不满 25 分。
> ⚠️ **`.vwf` 会被仿真结果覆盖写回，永远不要手工编辑** —— 改 `sim/tb_*.py` 重新生成。
> ⚠️ 功能仿真要用**综合后网表**，所以每次改顶层前先重跑
> `quartus_map <工程> --generate_functional_sim_netlist`。
> ⚠️ **被测模块要临时设为顶层、本次 `.vwf` 要设为向量源**（两个绑定步骤，用完还原），
> 细则见 `docs/03` §2.1。可用 `quartus_sh -t scripts/build.tcl --top <模块名>`。

### 第 2 步：自检全绿后，切 `puzzle_top` 再进一次实验室

`board_test_top` 的 9 阶段全绿 + SW7/BTN0 两条验掉之后：
`python scripts/set_top.py puzzle_top` → **必须重新编译**（`.pof` 才换得过来）→ 烧录
→ 按 `docs/02` §14.3 的 13 个场景验收。
上板步骤见 **`docs/06-Quartus编译与烧录流程.md`**（建工程 → 编译 → 引脚 → 烧录 → 自检）。

> ⚠️ **改代码前先想 LE**：整机只剩 ~44 个 LE（97%）。新增逻辑前先编译一次看数字。
> ⚠️ 上板若发现**点阵镜像 / 上下颠倒 / 数码管偏暗**，按 `docs/06` §6 与 `docs/02` §5.6 处理。
> ⚠️ **判断任何"异常"之前先查 `docs/06` §6.1 的判读表** —— 第 21 次会话两条"异常"全在里面，
> 都是**正常现象被当成了故障**。

---

## 4. 刚做完什么（别重复劳动）

> **只留最近 2~3 次会话**（`CLAUDE.md` §0 收工第 0 条）。更早的历史在 `CHANGELOG.md`。

- **第 21 次会话（续三）**：**切整机 `puzzle_top` 上板 + 定位"进不去阶段 7"** ——
  ① 用户决定**跳过自检阶段 2~8**、直接烧整机演示（`docs/05` §1.6 记了代价）；
  ② `set_top.py puzzle_top` → 重编译 **0 errors**（1226/1270 LE、52 引脚）→
  TimeQuest **setup slack +4.292 ns** → 烧录 **Erase→Program→Verify 全过**（14:11:18）；
  ③ ⭐ **定位"进不去阶段 7"**：`r_seen` **只在 `s_rst` 时清零** → **按满 16 键后恒为全 1** →
  阶段 7 每圈都在 **0.25 秒**内退出（本该 20 秒）→ 记 **ERR-0033**；
  **规避法：按 BTN0 或拨 SW7 复位**；代码修复有意推迟（只影响自检工具）。
  → 预防措施：**累积型状态必须写清"从哪开始累积、到哪清零"**。
- **第 21 次会话（续二）**：**判据被现场推翻 —— "看阶段 1"这条判据不可执行** ——
  用户照「30 秒判据」去看阶段 1，报回"**所有 16 个灯都闪烁，LD8 也不是常亮**"。
  定位结论：**代码没错**，是判据缺"**何时看**" —— **阶段 1 只占完整轮播 36 秒里的 2 秒**（≈5.6%），
  其余 34 秒是阶段 2~9（`ld[15:8]`=阶段号、`ld[7:0]`=子步号 → 16 个灯本来就都在动）。
  同时找出代码里**现成的阶段号读数**：**最左数码管 DISP7**（`r_stage` → `s_disp_val(31 downto 28)`）。
  → 记 **`ERRORS.md` ERR-0031**（索引 31 行 == 正文 31 条）；订正 `docs/06` §6.1（判读表新增一行 +
  30 秒判据重写为"用 DISP7 读数"并列出**三个能精确对准阶段 1 的时刻**）、`docs/01` §8.2、`docs/05`。
  **`rtl/` 一行未改** —— 本次上板报回的三条"异常"，**没有一条是代码错**。
  → **下一步不变：自检阶段 2~8 逐条打勾（用 DISP7 报阶段号）+ 录工作视频。**
- **第 21 次会话**：**首次上板 + 现场两条"异常"判读 + 手册位置补齐** ——
  ① `quartus/puzzle.pof` 烧进真板成功（Erase→Program→Verify，JTAG ID `0x020A30DD`）；
  ② 用 `pypdf` 抽出**板手册**第 3/4/7/8/10 页原文，逐条核对手册与 `.qsf`（引脚 68/68 一致）
  并在 `docs/04` §4 摘全原文（LED **4 种颜色** / **LD0 在最右端** / 拨码**往上=开** /
  **BTN0 = `PIN_61` = 8 个按键里最右边那个** / **`ld[15:8]` 与 VGA 复用**）；
  ③ 现场两条"异常"判读为**正常现象**（"混乱"= 轮播 + 4Hz 子步号；"SW7=0 只有 LD8 亮"= `ld`
  不受门控），**新增 `docs/04` §7.3「现场位置速查」**，订正 `docs/04` §7.1 第 10/11 条、
  `docs/06` §4.2/§6.1（含**新增 30 秒判据**）、`docs/01` §8.1/§8.2、`docs/05` §1.2~§1.5；
  ④ **代码一行未改** —— 结论是"**实现对、清单错**"。
  → 记 **`ERRORS.md` ERR-0030**（+ 索引同步，30 行 == 30 条）。
  **13:38 现场回证：「30 秒判据」四步全过** —— 时钟确认 50MHz、SW7=1 的 LED"无规则但有周期"（正常）、
  **按住最右 BTN0 → 16 灯全亮** → "按错键"悬案闭合，未发现任何硬件故障。
  → **下一步（人还在实验室就先做）：自检阶段 2~8 逐条打勾填 `docs/05` §1.3 + 录工作视频**。
> 五次早期遍历（接口轴 / 一致性轴 / 可执行性轴 / 符合性轴 / 照着做能不能跑）**到此为止，不要再开新的审查轴** ——
> 剩下的错误只能由编译器与仿真器暴露，而编译器这一关**已经过了**（下一步是仿真）。
> 更早的会话（第 1~17 次）见 `CHANGELOG.md` 对应各节。

---

## 5. 铁律

**铁律全文见 `CLAUDE.md` §2（14 条硬约束），本页不再抄一遍。** 只强调最容易忘的四条：

1. **引脚绑定只允许出现在两处**：`rtl/puzzle_top.vhd` 的端口名 + `quartus/*.qsf` 的
   `set_location_assignment`。其他任何文件不得出现引脚号。
2. **`and` / `or` 与 `=` / `/=` 混在一行，一律加括号。**
   VHDL 的关系运算符比逻辑运算符**紧**，`a and b = c` 实际是 `a and (b = c)` → 类型错误。
   全 1 检测要写成 `(r_locked and s_valid_mask) = s_valid_mask`。**这一项看着完全合法，最贵**（ERR-0012）。
3. **顶层一律 `component` 声明 + `port map`**，不用 `entity work.xxx`；
   端口照 `docs/01` §5 抄，不许凭印象写 —— `port map` 漏写端口**不报错、静默悬空**（`CLAUDE.md` §10.3）。
4. **`rng_lfsr` 不接任何节拍**（由 `puzzle_ctrl.rnd_step` 请求推进）、**`integer` 一律带 `range`**、
   **分频器串行级联** —— 理由见 `CLAUDE.md` §10.1 / §10.2。

---

## 6. 工具链（不必重新摸索）

**工具路径、命令行、仿真流程与 `.vwf` 的实测结论，全部见 `CLAUDE.md` §5**
（§5.1 路径 / §5.2 命令 / §5.3 仿真方案 / §5.4 环境备忘），本页不再列一遍。

> ⚠️ **本机没有 ModelSim / GHDL**，只有 Quartus II 9.1 内置仿真器；它有三条实测确认的硬限制
> （仿的是综合后网表 → 不支持 testbench / `assert`；**没有任何 Tcl 接口**，只能用 Python 驱动；
> **必须有一个 `.vwf`**），**别再重复试探**。

本项目自己写的脚本（均已实测通过；`PROGRESS.md`「文档进度」表有逐个说明）：

- **工程 / 代码**：`scripts/build.tcl`（编译驱动）· `scripts/set_top.py`（切默认顶层，含 16 行 `ld` 约束）·
  `scripts/encoding.py`（`rtl/*.vhd` 的 UTF-8 ⇄ GBK，**打开 Quartus 前必跑 `gbk`**）·
  `scripts/_gen_fitcheck.py`（按 `.qsf` 生成临时工程，**不动仓库**地验证任一顶层能否编译）
- **校验**：`scripts/check_pins.py` · `scripts/check_ports.py` · `scripts/check_svg.py` ·
  `scripts/verify_tiling.py`（**改任何图案常量前必须先跑**）· `scripts/vwf.py`（`.vwf` 读写库）
- **出图**：`scripts/gen_dwg01.py` / `scripts/annotate_dwg01.py`（DWG-01 的生成器与加注器，
  都属**备份版**；对外提交用用户的定稿版，见 `CLAUDE.md` §12.1.1）
- `scripts/sim.py` **尚未写** —— 当初暂缓的理由（"等 `clk_gen` 写出来再定接口"）**现已消失**
  （模块已全部写出），它是阶段 3 的第一步（`CLAUDE.md` §5.3 的标准循环）。

> 图表定稿必须走 `CLAUDE.md` §12.4 的**五步**清单（第 3 步放大看 / 第 5 步图↔表反向对账）；
> 改校验脚本后，负向测试要**同时有正例和反例**。

---

## 7. 诚实提示（本项目的已知薄弱处）

- ⚠️ **整机只剩约 44 个 LE 余量**（`puzzle_top` 1226 / 1270，97%）——
  任何新增逻辑前**先编译一次看 LE 数**；压缩手段见 `CLAUDE.md` §10.2 ④（分时复用）。
- **两个硬件不确定项还没上板确认**（板子只在实验室能用）：
  ① 板载时钟档位是否为 **7 (50MHz)**；② 键盘行列方向与极性。**去实验室前先列好待验证清单。**
- **阶段 3 的逐模块仿真与 `docs/03` 结果表尚未开始** ——
  那是报告评分项③ 的 **25 分**，也是目前最大的一块未动工程量（下一步就是它）。

> 完整的"进行中 / 阻塞 / 待确认"清单（含 `scripts/sim.py` 为何暂缓）
> 见 `PROGRESS.md`，本页不重复。
