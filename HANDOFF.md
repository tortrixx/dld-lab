# HANDOFF — 会话交接单

> **给下一个会话的 Claude：先读这一页，再读 `CLAUDE.md` 与 `PROGRESS.md`。**
> 本文件只回答一件事：**现在该干什么、别踩什么坑**。
> 详细状态在 `PROGRESS.md`，工作约定在 `CLAUDE.md`，本文件不重复它们。

**最后更新**：2026-09-17（第 7 次会话结束）

---

## 1. 这个项目是什么

《数字电路与逻辑设计实验（下）》综合课题 **题目4《简易拼图游戏的设计与实现》**。
在 MAXII 实验板（**EPM1270T144C5**）上用 **VHDL** 实现 8×8 双色点阵拼图游戏，
全流程基于 **Quartus II 9.1**（`C:\QuartusII91\QuartusII91\quartus\bin`）。

**用户最看重的两件事**（都是明确要求过的）：

1. **过程记录就是交付物本身**。报告 8 个评分项里有 50 分（仿真波形 25 / 故障分析 15 / AI 使用 10）
   直接取自 `ERRORS.md` / `AI_LOG.md` / `docs/03`。**这些分是当场记下来的，事后补写拿不到。**
2. **每完成一项工作就停下来汇报**，不要攒着连续推进、不要自行扩大范围。

---

## 2. 现在到哪了（一句话）

**阶段 1「设计文档」已全部完成并定稿（4 份主文档 + 3 张图）。阶段 2 的 RTL 代码一行都还没写。**

| 阶段 | 内容 | 状态 |
|---|---|---|
| 0 | 项目奠基（目录 / 文档体系 / Quartus 工程） | ✅ |
| 1 | 设计文档（初步设计方案） | ✅ **已完成并定稿** |
| 2 | 基础驱动模块（RTL） | ⬜ **← 现在在这里** |
| 3 | 数据与逻辑模块 | ⬜ |
| 4 | 集成（状态机 + 顶层） | ⬜ |
| 5 | 上板调试（实验室） | ⬜ |
| 6 | 验收准备（报告 + 答辩） | ⬜ |

**截止**：初步设计方案 9/24 24:00（文档已提前完成，余量留给编码）· 第 9~10 周验收答辩。

---

## 3. 你现在该做什么（按顺序，别跳）

### 第一步：搭 `docs/03-仿真验证方案.md` 的框架

**不要写满** —— 仿真内容随代码推进逐个模块填充。框架里只要有：
仿真策略（已实测的链路）、波形判读规范、每模块验证点的索引表（先留空行）。

### 第二步：`rtl/puzzle_pkg.vhd`

全局常量 + 掩码纯函数 + 位序约定。**唯一真值源**，后面所有模块都依赖它。

### 第三步起：`rtl/clk_gen.vhd` → `rtl/seg_scan.vhd` → `rtl/dot_matrix_scan.vhd` → `rtl/keypad_scan.vhd`

**每个模块走同一个循环，一步都不能省**：

```
写 rtl/<模块>.vhd  →  写 sim/tb_<模块>.py  →  生成 .vwf  →  跑仿真
→ 解析结果与参考模型逐点比对  →  渲染波形图存 docs/图/
→  把原始数据写进 docs/03  →  单独 commit
```

> ⚠️ **`sim/*.vwf` 会被仿真结果覆盖写回，永远不要手工编辑** —— 改 `sim/tb_*.py` 重新生成。
> ⚠️ **写 `clk_gen` 时直接照 `docs/02` §3.4 的级联骨架抄**，不要自己另发明一套。
> ⚠️ **模块文件头注释必须标「所属子系统」**（S1 时钟与节拍 / S2 键盘输入 / S3 游戏控制 /
> S4 拼图核心 / S5 图案与随机数据 / S6 显示子系统 / S7 音效输出）。

> **`board_test_top` 是第一次进实验室的前置条件** —— 上板前必须先做出来。

---

## 4. 刚做完什么（别重复劳动）

- **第 7 次会话**：DWG-01 总体框图**第 2 版定稿**（用户飞书白板稿的第 2 次修订），
  `docs/01` §2.1 / §2.1.1 / §2.1.2 同步，本交接单建立。
- **第 6 次会话**：DWG-01 第 1 版定稿；`docs/01` §2.1.1 记录定稿时修正的四处。
- **第 5 次会话**：设计方法学表述修正（**设计自顶向下，实现自底向上**），框图改两层。
- **第 1~4 次会话**：项目奠基、打通自动仿真链路、4 份设计文档、图表与资源利用率约定。

**设计文档已经超额完成，不要再往里追加内容。** 现在写代码的边际收益远高于继续写文档。

---

## 5. 铁律（写代码前逐条过一遍）

按「踩了会痛」排序：

| # | 铁律 | 踩了会怎样 |
|---|---|---|
| 1 | **`rng_lfsr` 不接任何节拍**，由 `puzzle_ctrl` 的 `rnd_step` **请求推进** | 同一拍内多次落位尝试读到同一个随机数 → **游戏永远不随机，且几乎查不出来**（`CLAUDE.md` §10.1、`docs/01` §2.1.1） |
| 2 | **任何 `integer` 都必须带 `range`** | 裸 `integer` 综合成 **32 位**，EPM1270 只有 1270 个 LE |
| 3 | **分频器串行级联**，不要五个并行 | 99 位 → 33 位（`CLAUDE.md` §10.2 ②） |
| 4 | **每个模块必须能独立仿真** | 不能独立仿真说明职责切分有问题，要重切 |
| 5 | **引脚绑定只允许出现在** `rtl/puzzle_top.vhd` 端口名 + `quartus/*.qsf` | 其他任何文件不得出现引脚号 |
| 6 | **改模块接口 → 必须同时改** `docs/图/系统图.html`、`docs/01` §5 接口表 | 图/文档/代码不一致是答辩最容易被抓的失分点 |
| 7 | **VHDL-93 子集 + `numeric_std`**，禁 `std_logic_unsigned`/`std_logic_arith` | Quartus II 9.1 兼容性 |
| 8 | **所有注释、所有文档用中文** | 用户明确要求 |
| 9 | **跑完仿真记原始数据**，不要只写"通过" | 评分项③ 25 分的取材（`CLAUDE.md` §13.2：激励条件 / 观测点 / 实测值 vs 期望值 / LE 占用时间线） |
| 10 | **每完成一项就停下汇报** | 用户明确要求"方向不要跑偏" |

---

## 6. 已打通的工具链（可放心依赖，不必重新摸索）

| 能力 | 命令 |
|---|---|
| 综合 + 生成功能仿真网表 | `quartus_map <工程> --generate_functional_sim_netlist` |
| 功能仿真（结果回写 `.vwf`） | `quartus_sim <工程> --mode=functional --overwrite_waveform=on` |
| `.vwf` 读写 / 比对 | `scripts/vwf.py`（`python scripts/vwf.py` 自检） |
| 引脚一致性检查 | `scripts/check_pins.py` |
| 图表格式校验 | `scripts/check_svg.py "docs/图/系统图.html"` |
| 拼图分解穷举验证 | `scripts/verify_tiling.py`（**改任何图案常量前必须先跑**） |
| 编译 + 资源报告 | `scripts/build.tcl` |

**三条已知的仿真器硬限制**（都实测确认过，别再试）：
① 仿真的是综合后网表 → **不支持 VHDL testbench、不支持 `assert`**；
② **没有任何 Tcl 接口**（`::quartus::simulator` 是空包）→ 只能用 Python 驱动；
③ **必须有一个 `.vwf`**，否则直接报 `No valid vector source file specified`。

> **另有两条流程性要求**（都是踩过坑后加的）：
> - **改任何校验脚本后，负向测试必须同时有正例和反例** —— 只测"该报的报了"会把校验器越改越神经质（ERR-0007）。
> - **图表定稿必须走 `CLAUDE.md` §12.4 的四步清单**，其中第 3 步「渲染后放大 2.5~3 倍分上中下逐段看」
>   **最关键**：ERR-0006 / ERR-0008 / ERR-0009 的七处缺陷**全都是这一步抓到的**，校验脚本一处都没报。

---

## 7. 诚实提示（本项目的已知薄弱处）

- **`quartus/` 工程现在还不能编译** —— `.qsf` 里列的 6 个 VHDL 文件在 `rtl/` 下都还不存在。
  **这是正常的**，RTL 要按阶段 2 逐个写出来。**第一个能编译的版本出现在 `board_test_top` 写完时。**
- **`scripts/sim.py` 还没写** —— 故意等的：等 `clk_gen` 出来后针对**真实模块**再写，
  避免凭空设计接口。仿真库 `scripts/vwf.py` 已就绪并验证过。
- **两个硬件不确定项还没上板确认**（板子只在实验室能用）：
  ① 板载时钟档位是否为 **7 (50MHz)**；② 键盘行列方向与极性。
  **去实验室前先列好待验证清单**，第一次去优先烧这两个自检程序。
