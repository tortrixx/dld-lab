# HANDOFF — 会话交接单

> **给下一个会话的 Claude：开工按 `CLAUDE.md` §0 的顺序读 ——
> 本页 → `PROGRESS.md` → `CHANGELOG.md` 最后一节 → `ERRORS.md` 未解决的问题。**
> 本文件只回答一件事：**现在该干什么、别踩什么坑**。
> 详细状态在 `PROGRESS.md`，工作约定在 `CLAUDE.md`，本文件不重复它们。

**最后更新**：第 14 次会话 · 2026-09-18

> ⚠️ **这一行是收工必改项之一** —— 第 13 次会话的审查查出，此前收工清单
> （`CLAUDE.md` §0 第 0 条）只要求改本文件的 §2/§3/§4，**没包括这行表头**，
> 于是它曾连续两次会话没更新（见 `ERRORS.md` ERR-0020 / 审查报告 `CRIT-4`）。
> **现已把"表头"并入收工条目。**

---

## 1. 这个项目是什么

《数字电路与逻辑设计实验（下）》综合课题 **题目4《简易拼图游戏的设计与实现》**。
在 MAXII 实验板（**EPM1270T144C5**）上用 **VHDL** 实现 8×8 双色点阵拼图游戏，
全流程基于 **Quartus II 9.1**（工具路径与命令见 `CLAUDE.md` §5.1 / §5.2）。

**用户最看重的两件事**（都是明确要求过的）：

1. **过程记录就是交付物本身**。报告 8 个评分项里有 50 分（仿真波形 25 / 故障分析 15 / AI 使用 10）
   直接取自 `ERRORS.md` / `AI_LOG.md` / `docs/03`。**这些分是当场记下来的，事后补写拿不到。**
2. **每完成一项工作就停下来汇报**，不要攒着连续推进、不要自行扩大范围。

---

## 2. 现在到哪了

**阶段 1「设计文档」已全部完成并真封版（4 份主文档 + 3 张图），且已走过五轮不同轴的审查**：
接口核对（第 9 次）、"找矛盾删重复"复查（第 10 次）、规划审查（第 11 次）、
课件合规补全（第 12 次）、**进代码前的系统性审查（第 13 次，34 个子 agent）**。

✅ **第 13 次审查查出的 16 条已复核缺陷（12 CRITICAL/HIGH ＋ 4 条其余）已全部修完**，
另从 80 条未复核项里挑出 **4 条「照抄编译不过」类**一并修掉（客观可确认的不必等复核）。
**阶段 1 现在是「审过、已复核项已修完」的状态。**

**阶段 2（RTL）一行代码都还没写 —— `rtl/` 还是空的。**
`quartus/` 工程因此暂时编译不了，这是正常的：第一个能编译的版本出现在 `board_test_top` 写完时。

**截止**：初步设计方案 **2026-09-24 24:00**（文档已提前完成，余量留给编码与修复）· 第 9~10 周验收答辩。

> 完整的阶段表、模块进度表、进行中与阻塞项见 `PROGRESS.md`（唯一账本）；
> 历次审查的详情见 `CHANGELOG.md` 各会话一节；
> **第 13 次审查的完整报告：`docs/审查报告-2026-09-18.md`**（729 行，含逐条证据与复核结论）。

> ⚠️ **第 12 次会话改动过接口**（`clk_gen` 新增 `i_btn_rst`，系统级连线 2→3 条、全图 15→16 笔）——
> **凡引用"连线表 15 条""引脚 67 个""`puzzle_top` 51 个 I/O"的旧笔记，一律作废**，
> 以 `docs/01` §2.1 与 `docs/04` §2 为准（现为 **16 条 / 68 个 / 52 个**）。

---

## 3. 你现在该做什么（按顺序，别跳）

> ✅ **「修硬伤」四步已全部完成**（第 14 次会话，见 `ERRORS.md` ERR-0020 的进度表）。
> **现在可以开始写代码了。**
>
> ⚠️ 但有一条前提：**报告 §4 那 80 条 MEDIUM/LOW 仍未复核** ——
> 它们是「待确认」状态，**不要照着改**。见本节末尾。

### 第一步：读 `docs/审查报告-2026-09-18.md`

至少读 **§0 结论摘要** 与 **§1 十二条已验证缺陷**。这是本次修复的**唯一任务清单**。
修复顺序照报告 **§6**：**先冻结契约 → 再修硬伤 → 再对账 → 再补验证**。

> ⚠️ **报告 §4 那 80 条 MEDIUM/LOW 未经复核**，按"待确认"看待，别当成同等优先级的任务。

### 第二步：冻结五个接口契约（**改动最小、收益最大**）

报告 §6 第 1 条列了五项，核心是：所有**脉冲类信号宽度 = 1 个 clk**、
`o_round_start` 的触发时刻、`i_seed` 必须非零、`i_key_code` 5 位、
**顶层器件端口名以 `.qsf` 的 `-to` 为准**。**这些是跨模块约定，不写下来就一定会错。**

### 第三步：修「照抄编译不过」与「静默失效」

报告 §6 第 2、3 条。**多重驱动出现在 3 个模块**（`keypad_scan` / `game_fsm` / `puzzle_ctrl`），
另有 `piece_rom` 聚合赋值、`buzzer_ctrl` 读自己的 out 端口、`seg_scan` 调用了未定义的 `seg_decode`，
以及 **`clk_gen` 的两处 `component` 模板漏了 `i_btn_rst`**（第 12 次会话的连锁遗漏）。

### 第四步：图↔表对账一轮

报告 §6 第 4 条：DWG-01 补 `pattern_mask`、两图补齐 `height/width/count` 标签、
DWG-02 补 `sw7 → clk_gen`。**全程走 `CLAUDE.md` §12.4 的五步清单。**

### 第五步起：才开始写代码

#### 第一段：`rtl/puzzle_pkg.vhd`

全局常量 + 掩码纯函数 + 位序约定。**唯一真值源**，后面所有模块都依赖它。
全部常量（含已定稿的 `WIN_MASK` / `FAIL_MASK`）照 `docs/02` §2.5 抄。

#### 之后：`rtl/clk_gen.vhd` → `rtl/seg_scan.vhd` → `rtl/dot_matrix_scan.vhd` → `rtl/keypad_scan.vhd`

**每个模块走同一个循环，一步都不能省**：

```
写 rtl/<模块>.vhd  →  写 sim/tb_<模块>.py  →  生成 .vwf  →  跑仿真
→ 解析结果与参考模型逐点比对  →  渲染波形图存 docs/图/
→  把原始数据写进 docs/03  →  单独 commit
```

> ⚠️ **`sim/*.vwf` 会被仿真结果覆盖写回，永远不要手工编辑** —— 改 `sim/tb_*.py` 重新生成（`CLAUDE.md` §5.3）。
> ⚠️ **`.vwf` 里必须加"中间信号"**（课件 p59 硬要求；用 `Node Finder` 的
> `Registers: post-fit` 过滤器找名字）—— 细则见 `docs/03` **§3.1**。
> **只给端口的波形，评分项③ 25 分是拿不满的。**
> ⚠️ **写 `clk_gen` 时直接照 `docs/02` §3.4 的级联骨架抄**，不要自己另发明一套。
> ⚠️ **写 `clk_gen` 的复位部分时照 `docs/02` §3.4 的"按键复位"骨架抄**：
> `i_btn_rst` → 2 级同步 + 20ms 消抖 → `r_btn_rst`，再与另两源相或。
> **消抖计数器不得被 `o_rst` 复位**（否则永远退不出复位）。
> ⚠️ **模块文件头注释必须标「所属子系统」**（S1 时钟与节拍 / S2 键盘输入 / S3 游戏控制 /
> S4 拼图核心 / S5 图案与随机数据 / S6 显示子系统 / S7 音效输出）。

> ⭐ **接口与代码片段现在可以照抄了** —— `docs/01` §5 与 `docs/02` 刚做过全量核对（见 `CHANGELOG.md` 第 9 次会话）。
> 但**抄的时候仍要逐行核**：`docs/02` §10.5 / §10.7 / §10.8 是**整段重写过**的，
> 其中标注 ⚠️ 的段落（运算符优先级、常量折叠、伪代码）**要读，不要跳**。
> ⚠️ **`docs/01` §6.5 的类型在第 10 次会话被更正过**（`i_rel_mask` 实为 `mask_arr_t`）——
> **拿不准的类型一律回 §5 接口定义表对一遍**，那张表才是唯一真值源（ERR-0014）。

> **`board_test_top` 是第一次进实验室的前置条件** —— 上板前必须先做出来。

---

## 4. 刚做完什么（别重复劳动）

> **只留最近 2~3 次会话**（`CLAUDE.md` §0 收工第 0 条）。更早的历史在 `CHANGELOG.md`。

- **第 13 次会话**：**进代码前的系统性审查（第五根轴：照着做能不能跑）** ——
  用户要求"**创建一个多子 agent 协作的 workflow**"做系统性审查。
  ① **34 个子 agent 四阶段**：6 路顶层 → 13 个模块（一个模块一个）→ 12 条对抗性复核 ＋ 跨模块集成 →
  完整性批评 ＋ 汇总；**106 条发现，其中 12 条 CRITICAL/HIGH 经"默认推翻"复核确认**；
  ② ⭐ **12 处缺陷在前四根轴上全部不可见** —— 当时的文档是自洽的，
  **甚至因为太自洽才错**（与 ERR-0014 的"不换轴，剩下的错误就是不可见的"同源）；
  ③ 产出 **`docs/审查报告-2026-09-18.md`（729 行）** 与 **`ERRORS.md` ERR-0020**；
  ④ **结论：四步修复完成前不要开始写代码**（见 §3）。
- **第 12 次会话**：**课件 PDF 全量解析与合规补全**（用户要求"严格按老师的 PDF 完善文档"）——
  这是**第四根轴：符合性轴**（前三次是接口轴 / 一致性轴 / 可执行性轴）。
  ① ⭐ **完整解析 65 页课件**：第一次只用 `pypdf` 提文字，**漏了 13 页纯图幻灯片**，
  而 **4 条实质要求只在图里**（p50 的 Quartus **AREA** 优化、p47 的级联分频、p48 的 latch 示例、
  p46 的资源共享示例）—— **本项目 `AI_LOG.md` 第 1 次会话就记过"PDF 图形信息不能靠文字提取"，本次原样复发**；
  ② **BTN0 按键复位全链路恢复**（课件 p45（一））+ 2 级同步 + 20ms 消抖（课件 p54）；
  ③ `CLAUDE.md` **§11 重写为考核/提交要求对照 + 新增 §11.4 课件要求对照表**（含 p65 的**工作视频**待办）；
  ④ `CLAUDE.md` **§7.1 新增"新增/修改一个端口"的 9 处固定落点清单**；
  ⑤ 修 ERR-0017（半成品残留：**引用悬空** + 自相矛盾）、ERR-0018（复位两源）、
  ERR-0019（**DWG-01 椭圆副标题溢出**，脚本全绿、放大 2.6 倍才现形）。
- **第 11 次会话**：**规划审查与完善（用户要求审查前期规划的合理性并直接优化）** ——
  这是第三根轴的遍历：**可执行性轴**（前两次是接口轴、一致性轴）。做的是"补缺口、修错"，不是追加：
  ① `WIN_MASK` / `FAIL_MASK` 位图与掩码**定稿**（`docs/02` §2.5/§7.4，`render()` 往返自检）；
  ② 修 `docs/02` §13.4 音效骨架两处缺陷：半周期 off-by-one（N→N+1 拍，频率全错）+
     查表空名（ERR-0016）；③ §12.9 验证点重号、§12.5 倒计时装载点补注；
  ④ 新建 `docs/03-仿真验证方案.md` 框架（策略定稿，结果表留空待填）；
  ⑤ 新建 `scripts/check_ports.py` 并通过自检（11 实体 / 57 输入 / 44 输出全部连通）。
**现在的工作不再是"写文档"，也不是"写代码"，而是「按审查报告把 12 处硬伤修掉」。**
第 9~13 次的五次遍历分别沿 **接口轴 / 一致性轴 / 可执行性轴 / 符合性轴 / 照着做能不能跑** 各走了一遍，
**到此为止，不要再开新的审查轴** —— 剩下的错误只能由编译器与仿真器来暴露（阶段 2 起）。

---

## 5. 铁律

**铁律全文见 `CLAUDE.md` §2（14 条硬约束），本页不再抄一遍。** 只强调最容易忘的四条：

1. **引脚绑定只允许出现在两处**：`rtl/puzzle_top.vhd` 的端口名 + `quartus/*.qsf` 的
   `set_location_assignment`。其他任何文件不得出现引脚号。
2. **`and` / `or` 与 `=` / `/=` 混在一行，一律加括号。**
   VHDL 的关系运算符比逻辑运算符**紧**，`a and b = c` 实际是 `a and (b = c)` → 类型错误。
   全 1 检测要写成 `(r_locked and s_valid_mask) = s_valid_mask`。**这一项看着完全合法，最贵**（ERR-0012）。
3. **顶层一律 `component` 声明 + `port map`**，不用 `entity work.xxx`；
   端口照 `docs/01` §5 抄，不许凭印象写 —— `port map` 漏写端口**不报错、静默悬空**（`CLAUDE.md` §10.3）。
4. **`rng_lfsr` 不接任何节拍**（由 `puzzle_ctrl.rnd_step` 请求推进）、**`integer` 一律带 `range`**、
   **分频器串行级联** —— 理由见 `CLAUDE.md` §10.1 / §10.2。

---

## 6. 工具链（不必重新摸索）

**工具路径、命令行、仿真流程与 `.vwf` 的实测结论，全部见 `CLAUDE.md` §5**
（§5.1 路径 / §5.2 命令 / §5.3 仿真方案 / §5.4 环境备忘），本页不再列一遍。

> ⚠️ **本机没有 ModelSim / GHDL**，只有 Quartus II 9.1 内置仿真器；它有三条实测确认的硬限制
> （仿的是综合后网表 → 不支持 testbench / `assert`；**没有任何 Tcl 接口**，只能用 Python 驱动；
> **必须有一个 `.vwf`**），**别再重复试探**。

本项目自己写的脚本（均已实测通过，逐个说明见 `PROGRESS.md`「文档进度」表）：
`scripts/vwf.py` · `scripts/check_pins.py` · `scripts/check_svg.py` · `scripts/check_ports.py` ·
`scripts/verify_tiling.py`（**改任何图案常量前必须先跑**）· `scripts/build.tcl`；
`scripts/sim.py` **尚未写**（故意等的，等 `clk_gen` 写出来后针对真实模块再写，见 `PROGRESS.md`「进行中」）。

> 图表定稿必须走 `CLAUDE.md` §12.4 的**五步**清单（第 3 步放大看 / 第 5 步图↔表反向对账）；
> 改校验脚本后，负向测试要**同时有正例和反例**。

---

## 7. 诚实提示（本项目的已知薄弱处）

- **`quartus/` 工程现在还不能编译**（`.qsf` 里列的 6 个 VHDL 文件在 `rtl/` 下还不存在）—— 正常，
  第一个能编译的版本出现在 `board_test_top` 写完时。
- **两个硬件不确定项还没上板确认**（板子只在实验室能用）：
  ① 板载时钟档位是否为 **7 (50MHz)**；② 键盘行列方向与极性。**去实验室前先列好待验证清单。**

> 完整的"进行中 / 阻塞 / 待确认"清单（含 `scripts/sim.py`、`scripts/check_ports.py` 的暂缓理由）
> 见 `PROGRESS.md`，本页不重复。
