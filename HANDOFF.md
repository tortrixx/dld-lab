# HANDOFF — 会话交接单

> **给下一个会话的 Claude：开工按 `CLAUDE.md` §0 的顺序读 ——
> 本页 → `PROGRESS.md` → `CHANGELOG.md` 最后一节 → `ERRORS.md` 未解决的问题。**
> 本文件只回答一件事：**现在该干什么、别踩什么坑**。
> 详细状态在 `PROGRESS.md`，工作约定在 `CLAUDE.md`，本文件不重复它们。

**最后更新**：第 20 次会话 · 2026-09-24

> ⚠️ **这一行是收工必改项之一** —— 第 13 次会话的审查查出，此前收工清单
> （`CLAUDE.md` §0 第 0 条）只要求改本文件的 §2/§3/§4，**没包括这行表头**，
> 于是它曾连续两次会话没更新（见 `ERRORS.md` ERR-0020 / 审查报告 `CRIT-4`）。
> **现已把"表头"并入收工条目。**
>
> ⚠️ **此后又漏过一次**：第 15 次会话**整个收工流程都没走**，这行停在"第 14 次"，
> `CHANGELOG.md` / `AI_LOG.md` 也都没有它的条目（`ERRORS.md` **ERR-0021**）。
> **触发判据因此改了**：不是"是不是新会话"，而是「**这一轮有没有产生提交**」——
> 续跑、补做、临时插进来的一轮，只要落了盘就算一轮。见 `CLAUDE.md` §0 收工时的抬头。

---

## 1. 这个项目是什么

《数字电路与逻辑设计实验（下）》综合课题 **题目4《简易拼图游戏的设计与实现》**。
在 MAXII 实验板（**EPM1270T144C5**）上用 **VHDL** 实现 8×8 双色点阵拼图游戏，
全流程基于 **Quartus II 9.1**（工具路径与命令见 `CLAUDE.md` §5.1 / §5.2）。

**用户最看重的两件事**（都是明确要求过的）：

1. **过程记录就是交付物本身**。报告 8 个评分项里有 50 分（仿真波形 25 / 故障分析 15 / AI 使用 10）
   直接取自 `ERRORS.md` / `AI_LOG.md` / `docs/03`。**这些分是当场记下来的，事后补写拿不到。**
2. **每完成一项工作就停下来汇报**，不要攒着连续推进、不要自行扩大范围。

---

## 2. 现在到哪了

**阶段 1（设计文档）封版**；**阶段 2/3/4（RTL 编写与集成）已完成 —— `rtl/` 下 14 个文件全部写出、
两个顶层都通过 Quartus II 9.1 全流程编译**：

| 顶层 | 逻辑单元 | 引脚 | Fmax | 说明 |
|---|---|---|---|---|
| `board_test_top` | 439 / 1270（35%） | 68 | — | 上板硬件自检（9 阶段） |
| `puzzle_top` | **1226 / 1270（97%）** | 52 | **63.66 MHz** | 整机拼图游戏 |

> ⚠️ **LE 余量只剩约 44 个**。改动任何代码后**先看编译报告的 LE 数**；
> 时序读数用 `quartus_sta`（经典 `quartus_tan` 不读 `.sdc`，Fmax 那一栏会是空的）。

⭐ **实现与原设计文档有三处受器件约束的偏离**（都是先编译、再按实测数字改的）：
① `puzzle_ctrl` 的像素/判定由"4 路并行组合"改为**时分复用行扫描引擎**（32 拍刷一帧），
`o_px_red/green` 变成寄存输出；② 散落的重叠判定由"掩码求交"改为**包围盒不相交**；
③ `clk_gen` 的上电复位改数 `tick_1k`（`puzzle_pkg` 的 `CNT_POR` → `T_POR_MS`）。
完整经过见 `ERRORS.md` ERR-0026 / ERR-0027 与 `CHANGELOG.md` 第 19 次会话一节。

**截止**：初步设计方案已完成 · 第 9~10 周验收答辩。

> 完整的阶段表、模块进度表、进行中与阻塞项见 `PROGRESS.md`（唯一账本）。

> ⚠️ **旧笔记作废**：凡引用"工程还不能编译""`rtl/` 是空的""LE 约 600""Fmax 未验证"的，
> 一律以本页与 `PROGRESS.md` 为准。

---

## 3. 你现在该做什么（按顺序，别跳）

> ✅ **代码已经全部写完并编译通过**（第 19 次会话）。文档侧与代码侧都不再有"待补的半成品"。
> 下一步是**阶段 3：逐模块仿真验证**（实验报告评分项③ 占 25 分，是目前最大的一块未动工程量）。

### 第一步：写 `scripts/sim.py` ＋ 第一个模块的 `sim/tb_*.py`

`scripts/vwf.py`（`.vwf` 读写库）已就绪；仿真机制早已实测跑通（`CLAUDE.md` §5.3），
差的是**驱动脚本**。当初暂缓的理由（"等 `clk_gen` 写出来再定接口"）现在已消失。

按 `CLAUDE.md` §5.3 的标准循环走，**每个模块一步不省**：

```
写 sim/tb_<模块>.py  →  Python 生成 .vwf  →  跑 quartus_sim
→  Python 解析回写结果 + 与参考模型逐点比对  →  渲染波形图存 docs/图/
→  把原始数据写进 docs/03  →  单独 commit
```

> ⚠️ **`.vwf` 里必须含"中间信号"**（课件 p59 硬要求，见 `docs/03` §3.1）：
> 状态寄存器、倒计时寄存器、扫描计数器、消抖计数器、`r_btn_rst` … 只给端口波形拿不满 25 分。
> ⚠️ **`.vwf` 会被仿真结果覆盖写回，永远不要手工编辑** —— 改 `sim/tb_*.py` 重新生成。
> ⚠️ 功能仿真要用**综合后网表**，所以每次改顶层前先重跑
> `quartus_map <工程> --generate_functional_sim_netlist`。
> ⚠️ **被测模块要临时设为顶层、本次 `.vwf` 要设为向量源**（两个绑定步骤，用完还原），
> 细则见 `docs/03` §2.1。可用 `quartus_sh -t scripts/build.tcl --top <模块名>`。

### 第二步：仿真跑完后，再进实验室（阶段 5）

**`board_test_top` 是第一次进实验室的前置条件**，先烧它、按 9 个阶段逐条打勾
（`docs/01` §8.2），把结果记入 `docs/05-硬件调试记录.md`。
上板步骤见 **`docs/06-Quartus编译与烧录流程.md`**（建工程 → 编译 → 引脚 → 烧录 → 自检）。

> ⚠️ **改代码前先想 LE**：整机只剩 ~44 个 LE（97%）。新增逻辑前先编译一次看数字。
> ⚠️ 上板若发现**点阵镜像 / 上下颠倒 / 数码管偏暗**，按 `docs/06` §6 与 `docs/02` §5.6 处理。

---

## 4. 刚做完什么（别重复劳动）

> **只留最近 2~3 次会话**（`CLAUDE.md` §0 收工第 0 条）。更早的历史在 `CHANGELOG.md`。

- **第 20 次会话**：**全量改动审查 + 文档结构与状态订正** ——
  按用户要求"审查全部改动、检查是否准确 / 有遗漏 / 有冲突"，查出两类缺陷并修完：
  ① `ERRORS.md` **问题索引表缺 6 行**（ERR-0023~0028 全不在表里，其中 4 行被误追加到
  ERR-0025 正文末尾）→ 已补全，现**索引 29 行 == 正文 29 条**；
  ② `README.md` / `HANDOFF.md` / `PROGRESS.md` **多份文档状态停在"编码之前"**（"工程还不能编译"
  "`rtl/` 仍为空""阶段 2/3/4 未开始"），且 `HANDOFF.md` **自身 §2 与 §7 互相矛盾**；
  另统一了 `CLAUDE.md` §12.1.1 / §12.3 关于"DWG-01 权威版本"的口径冲突。
  → 新增 **`ERRORS.md` ERR-0029**；两条通用预防措施：**索引表用可数对账**、
  收工时 `grep -rn "还不能编译\|仍为空\|未开始" *.md` **搜一遍不该留的旧状态**。
- **第 19 次会话**：**RTL 全部写出 + 编译通过 + 面积/时序优化** ——
  ① `rtl/` 下 14 个文件写完（11 实体 + `puzzle_pkg` + 2 顶层，约 2600 行，全中文注释）；
  ② 首次整机编译 **3461 LE / 1270 放不下** → `puzzle_ctrl` 改为**时分复用行扫描引擎**
  （每拍算 1 块零片的 1 个输出行，4×8 = 32 拍刷一帧 ≈ 0.64µs）+ **包围盒不相交判据**
  （纯整数比较，保守但绝不放过真实重叠）→ **1226/1270（97%）**；
  ③ 时序 **38.4 → 63.66 MHz**：判定挪进独立"定型拍"、`game_fsm.o_pattern_sel` 寄存、
  `puzzle_ctrl` 的图案参数寄存副本 + 包围盒判据寄存一拍（散落序列器改三拍制）；
  ④ 新增 **`docs/06-Quartus编译与烧录流程.md`**（建工程 → 编译 → 引脚分配 → 烧录 → 自检，
  含实测数据与常见报错处置）；⑤ 新增 `scripts/_gen_fitcheck.py`（不动仓库地验证任一顶层能否编译）。
  → 两条错误记录：`ERRORS.md` **ERR-0026**（放不进器件）/ **ERR-0027**（时序不收敛）。
  会话末段还修了 **ERR-0028**（Quartus 里中文注释乱码 → `rtl/*.vhd` 转 GBK，
  新增 `scripts/encoding.py`）并新增 `scripts/set_top.py`（切顶层）。
- **第 18 次会话**：① **用 Typst 重排初步设计方案**，产出 `report/初步设计方案.pdf`（12 页，源在 `report/typst/`）——
  封面按北邮毕设论文体例重做，图表编号与目录页码逐条核验，正文做了去 AI 痕迹处理；
  ② 修掉一个排版硬伤：Typst 的 `figure` 默认**不可断页**，长表放不下会整张后移；
  ③ ⭐ **清理仓库中的敏感信息**（`docs/审查报告` 里 67 处本机绝对路径，ERR-0025）；
  ④ 补 `.gitattributes`、重写 `README.md`；⑤ 按逻辑拆成多个提交并推送。

> 五次早期遍历（接口轴 / 一致性轴 / 可执行性轴 / 符合性轴 / 照着做能不能跑）**到此为止，不要再开新的审查轴** ——
> 剩下的错误只能由编译器与仿真器暴露，而编译器这一关**已经过了**（下一步是仿真）。
> 更早的会话（第 1~17 次）见 `CHANGELOG.md` 对应各节。

---

## 5. 铁律

**铁律全文见 `CLAUDE.md` §2（14 条硬约束），本页不再抄一遍。** 只强调最容易忘的四条：

1. **引脚绑定只允许出现在两处**：`rtl/puzzle_top.vhd` 的端口名 + `quartus/*.qsf` 的
   `set_location_assignment`。其他任何文件不得出现引脚号。
2. **`and` / `or` 与 `=` / `/=` 混在一行，一律加括号。**
   VHDL 的关系运算符比逻辑运算符**紧**，`a and b = c` 实际是 `a and (b = c)` → 类型错误。
   全 1 检测要写成 `(r_locked and s_valid_mask) = s_valid_mask`。**这一项看着完全合法，最贵**（ERR-0012）。
3. **顶层一律 `component` 声明 + `port map`**，不用 `entity work.xxx`；
   端口照 `docs/01` §5 抄，不许凭印象写 —— `port map` 漏写端口**不报错、静默悬空**（`CLAUDE.md` §10.3）。
4. **`rng_lfsr` 不接任何节拍**（由 `puzzle_ctrl.rnd_step` 请求推进）、**`integer` 一律带 `range`**、
   **分频器串行级联** —— 理由见 `CLAUDE.md` §10.1 / §10.2。

---

## 6. 工具链（不必重新摸索）

**工具路径、命令行、仿真流程与 `.vwf` 的实测结论，全部见 `CLAUDE.md` §5**
（§5.1 路径 / §5.2 命令 / §5.3 仿真方案 / §5.4 环境备忘），本页不再列一遍。

> ⚠️ **本机没有 ModelSim / GHDL**，只有 Quartus II 9.1 内置仿真器；它有三条实测确认的硬限制
> （仿的是综合后网表 → 不支持 testbench / `assert`；**没有任何 Tcl 接口**，只能用 Python 驱动；
> **必须有一个 `.vwf`**），**别再重复试探**。

本项目自己写的脚本（均已实测通过；`PROGRESS.md`「文档进度」表有逐个说明）：

- **工程 / 代码**：`scripts/build.tcl`（编译驱动）· `scripts/set_top.py`（切默认顶层，含 16 行 `ld` 约束）·
  `scripts/encoding.py`（`rtl/*.vhd` 的 UTF-8 ⇄ GBK，**打开 Quartus 前必跑 `gbk`**）·
  `scripts/_gen_fitcheck.py`（按 `.qsf` 生成临时工程，**不动仓库**地验证任一顶层能否编译）
- **校验**：`scripts/check_pins.py` · `scripts/check_ports.py` · `scripts/check_svg.py` ·
  `scripts/verify_tiling.py`（**改任何图案常量前必须先跑**）· `scripts/vwf.py`（`.vwf` 读写库）
- **出图**：`scripts/gen_dwg01.py` / `scripts/annotate_dwg01.py`（DWG-01 的生成器与加注器，
  都属**备份版**；对外提交用用户的定稿版，见 `CLAUDE.md` §12.1.1）
- `scripts/sim.py` **尚未写** —— 当初暂缓的理由（"等 `clk_gen` 写出来再定接口"）**现已消失**
  （模块已全部写出），它是阶段 3 的第一步（`CLAUDE.md` §5.3 的标准循环）。

> 图表定稿必须走 `CLAUDE.md` §12.4 的**五步**清单（第 3 步放大看 / 第 5 步图↔表反向对账）；
> 改校验脚本后，负向测试要**同时有正例和反例**。

---

## 7. 诚实提示（本项目的已知薄弱处）

- ⚠️ **整机只剩约 44 个 LE 余量**（`puzzle_top` 1226 / 1270，97%）——
  任何新增逻辑前**先编译一次看 LE 数**；压缩手段见 `CLAUDE.md` §10.2 ④（分时复用）。
- **两个硬件不确定项还没上板确认**（板子只在实验室能用）：
  ① 板载时钟档位是否为 **7 (50MHz)**；② 键盘行列方向与极性。**去实验室前先列好待验证清单。**
- **阶段 3 的逐模块仿真与 `docs/03` 结果表尚未开始** ——
  那是报告评分项③ 的 **25 分**，也是目前最大的一块未动工程量（下一步就是它）。

> 完整的"进行中 / 阻塞 / 待确认"清单（含 `scripts/sim.py` 为何暂缓）
> 见 `PROGRESS.md`，本页不重复。
