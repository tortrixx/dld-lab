# HANDOFF — 会话交接单

> **给下一个会话的 Claude：开工按 `CLAUDE.md` §0 的顺序读 ——
> 本页 → `PROGRESS.md` → `CHANGELOG.md` 最后一节 → `ERRORS.md` 未解决的问题。**
> 本文件只回答一件事：**现在该干什么、别踩什么坑**。
> 详细状态在 `PROGRESS.md`，工作约定在 `CLAUDE.md`，本文件不重复它们。

**最后更新**：第 10 次会话 · 2026-09-17

---

## 1. 这个项目是什么

《数字电路与逻辑设计实验（下）》综合课题 **题目4《简易拼图游戏的设计与实现》**。
在 MAXII 实验板（**EPM1270T144C5**）上用 **VHDL** 实现 8×8 双色点阵拼图游戏，
全流程基于 **Quartus II 9.1**（工具路径与命令见 `CLAUDE.md` §5.1 / §5.2）。

**用户最看重的两件事**（都是明确要求过的）：

1. **过程记录就是交付物本身**。报告 8 个评分项里有 50 分（仿真波形 25 / 故障分析 15 / AI 使用 10）
   直接取自 `ERRORS.md` / `AI_LOG.md` / `docs/03`。**这些分是当场记下来的，事后补写拿不到。**
2. **每完成一项工作就停下来汇报**，不要攒着连续推进、不要自行扩大范围。

---

## 2. 现在到哪了

**阶段 1「设计文档」已全部完成并真封版（4 份主文档 + 3 张图）：
接口经过全量核对（第 9 次），之后又做过一次"专门找矛盾、专门删重复"的复查（第 10 次，14 处已改）；
阶段 2（RTL）一行代码都还没写 —— 现在在这里，`rtl/` 还是空的。**
`quartus/` 工程因此暂时编译不了，这是正常的：第一个能编译的版本出现在 `board_test_top` 写完时。

**截止**：初步设计方案 **2026-09-24 24:00**（文档已提前完成，余量留给编码）· 第 9~10 周验收答辩。

> 完整的阶段表、模块进度表、进行中与阻塞项见 `PROGRESS.md`（唯一账本）；
> "真封版"那次接口核对的详情见 `CHANGELOG.md` 第 9 次会话一节与 `ERRORS.md` ERR-0011/0012/0013；
> 第 10 次会话的复查详情见 `CHANGELOG.md` 第 10 次会话一节与 `ERRORS.md` ERR-0014/0015。

---

## 3. 你现在该做什么（按顺序，别跳）

### 第一步：搭 `docs/03-仿真验证方案.md` 的框架

**不要写满** —— 仿真内容随代码推进逐个模块填充。框架里只要有：
仿真策略（已实测的链路）、波形判读规范、每模块验证点的索引表（先留空行）。

### 第二步：`rtl/puzzle_pkg.vhd`

全局常量 + 掩码纯函数 + 位序约定。**唯一真值源**，后面所有模块都依赖它。

### 第三步起：`rtl/clk_gen.vhd` → `rtl/seg_scan.vhd` → `rtl/dot_matrix_scan.vhd` → `rtl/keypad_scan.vhd`

**每个模块走同一个循环，一步都不能省**：

```
写 rtl/<模块>.vhd  →  写 sim/tb_<模块>.py  →  生成 .vwf  →  跑仿真
→ 解析结果与参考模型逐点比对  →  渲染波形图存 docs/图/
→  把原始数据写进 docs/03  →  单独 commit
```

> ⚠️ **`sim/*.vwf` 会被仿真结果覆盖写回，永远不要手工编辑** —— 改 `sim/tb_*.py` 重新生成（`CLAUDE.md` §5.3）。
> ⚠️ **写 `clk_gen` 时直接照 `docs/02` §3.4 的级联骨架抄**，不要自己另发明一套。
> ⚠️ **模块文件头注释必须标「所属子系统」**（S1 时钟与节拍 / S2 键盘输入 / S3 游戏控制 /
> S4 拼图核心 / S5 图案与随机数据 / S6 显示子系统 / S7 音效输出）。

> ⭐ **接口与代码片段现在可以照抄了** —— `docs/01` §5 与 `docs/02` 刚做过全量核对（见 `CHANGELOG.md` 第 9 次会话）。
> 但**抄的时候仍要逐行核**：`docs/02` §10.5 / §10.7 / §10.8 是**整段重写过**的，
> 其中标注 ⚠️ 的段落（运算符优先级、常量折叠、伪代码）**要读，不要跳**。
> ⚠️ **`docs/01` §6.5 的类型在第 10 次会话被更正过**（`i_rel_mask` 实为 `mask_arr_t`）——
> **拿不准的类型一律回 §5 接口定义表对一遍**，那张表才是唯一真值源（ERR-0014）。

> **`board_test_top` 是第一次进实验室的前置条件** —— 上板前必须先做出来。

---

## 4. 刚做完什么（别重复劳动）

> **只留最近 2~3 次会话**（`CLAUDE.md` §0 收工第 0 条）。更早的历史在 `CHANGELOG.md`。

- **第 10 次会话**：**全项目复查（用户原话："审查系统设计和交接文档，不要重复堆积"）** ——
  **这是一次专门找矛盾、专门删重复的遍历，不是"再写点什么"**：
  `docs/01` 抓到 **14 处**内部不一致（含 `tick_2hz` 用途写错、§2.2 一张会漂移的 60 行 ASCII 拷贝）、
  图侧修 3 处（统一 `clk` 的分层画法 / 两处版面溢出）、5 份过程文档去重（**净减约 40 行**）。
  详情见 `CHANGELOG.md` 第 10 次会话一节与 `ERRORS.md` ERR-0014 / ERR-0015。
- **第 9 次会话**：**接口全量核对与修复 —— 阶段 1 到此才真封版**，
  三项发现记入 ERR-0011 / ERR-0012 / ERR-0013（详情见 `CHANGELOG.md` 第 9 次会话一节）。
- **第 8 次会话**：顶层例化方式定为 `component` 语法（`CLAUDE.md` §10.3）；顺带补出 `docs/01` §5.11（ERR-0010）。

**设计文档已经超额完成，不要再往里追加内容。** 现在写代码的边际收益远高于继续写文档。
**第 9 次的核对与第 10 次的复查都属于"修错 / 删重复"而不是"追加"，
两次已经把"接口轴"与"一致性轴"各走了一遍 —— 到此为止，不要再开新的文档工作。**

---

## 5. 铁律

**铁律全文见 `CLAUDE.md` §2（14 条硬约束），本页不再抄一遍。** 只强调最容易忘的四条：

1. **引脚绑定只允许出现在两处**：`rtl/puzzle_top.vhd` 的端口名 + `quartus/*.qsf` 的
   `set_location_assignment`。其他任何文件不得出现引脚号。
2. **`and` / `or` 与 `=` / `/=` 混在一行，一律加括号。**
   VHDL 的关系运算符比逻辑运算符**紧**，`a and b = c` 实际是 `a and (b = c)` → 类型错误。
   全 1 检测要写成 `(r_locked and s_valid_mask) = s_valid_mask`。**这一项看着完全合法，最贵**（ERR-0012）。
3. **顶层一律 `component` 声明 + `port map`**，不用 `entity work.xxx`；
   端口照 `docs/01` §5 抄，不许凭印象写 —— `port map` 漏写端口**不报错、静默悬空**（`CLAUDE.md` §10.3）。
4. **`rng_lfsr` 不接任何节拍**（由 `puzzle_ctrl.rnd_step` 请求推进）、**`integer` 一律带 `range`**、
   **分频器串行级联** —— 理由见 `CLAUDE.md` §10.1 / §10.2。

---

## 6. 工具链（不必重新摸索）

**工具路径、命令行、仿真流程与 `.vwf` 的实测结论，全部见 `CLAUDE.md` §5**
（§5.1 路径 / §5.2 命令 / §5.3 仿真方案 / §5.4 环境备忘），本页不再列一遍。

> ⚠️ **本机没有 ModelSim / GHDL**，只有 Quartus II 9.1 内置仿真器；它有三条实测确认的硬限制
> （仿的是综合后网表 → 不支持 testbench / `assert`；**没有任何 Tcl 接口**，只能用 Python 驱动；
> **必须有一个 `.vwf`**），**别再重复试探**。

本项目自己写的脚本（均已实测通过，逐个说明见 `PROGRESS.md`「文档进度」表）：
`scripts/vwf.py` · `scripts/check_pins.py` · `scripts/check_svg.py` ·
`scripts/verify_tiling.py`（**改任何图案常量前必须先跑**）· `scripts/build.tcl`；
`scripts/sim.py` 与 `scripts/check_ports.py` **尚未写**（故意等的，见 `PROGRESS.md`「进行中」）。

> 图表定稿必须走 `CLAUDE.md` §12.4 的**五步**清单（第 3 步放大看 / 第 5 步图↔表反向对账）；
> 改校验脚本后，负向测试要**同时有正例和反例**。

---

## 7. 诚实提示（本项目的已知薄弱处）

- **`quartus/` 工程现在还不能编译**（`.qsf` 里列的 6 个 VHDL 文件在 `rtl/` 下还不存在）—— 正常，
  第一个能编译的版本出现在 `board_test_top` 写完时。
- **两个硬件不确定项还没上板确认**（板子只在实验室能用）：
  ① 板载时钟档位是否为 **7 (50MHz)**；② 键盘行列方向与极性。**去实验室前先列好待验证清单。**

> 完整的"进行中 / 阻塞 / 待确认"清单（含 `scripts/sim.py`、`scripts/check_ports.py` 的暂缓理由）
> 见 `PROGRESS.md`，本页不重复。
