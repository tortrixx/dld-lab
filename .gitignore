# ============================================================
# 课程原始资料（版权材料，不入库）
# 本地保留作参考，其他同学克隆后代码仍可正常构建
# ============================================================
# ★ 只挡仓库根目录下那 3 份课程 PDF，不挡 report/ 里将来要交的导出 PDF。
#   写成 /*.pdf 而不是 *.pdf —— 后者会把任何位置的 PDF 一起挡掉，
#   而"被 .gitignore 静默挡掉"是最难发现的一类丢文件。
/*.pdf

# ============================================================
# Quartus II 编译产物
# ============================================================
db/
incremental_db/
greybox_tmp/
*.qws
*.qdf
*.bak
*.rpt
*.summary
*.smsg
*.jdi
*.pin
*.sof
*.pof
*.pof.*
*.ttf
*.hexout
*.done
*.eqn
*.vqm
*.vo
*.vho
*.sdo
*.ver
*.syn.rpt
*.fit.rpt
*.map.rpt
*.asm.rpt
*.tan.rpt
*.sta.rpt
*.cdb
*.hdb
*.qmsg
*.ddb
*.rdb
*.tdf
c5_pin_model_dump.txt

# 仿真中间产物
simulation/
sim/*.wlf
sim/*.vcd
sim/*.history
*.vwf.bak

# ============================================================
# 工具状态（WorkBuddy 等工作树工具自己写的记忆文件）
# 属于"运行环境"而不是"项目产物"，不入库
# ============================================================
.workbuddy/

# ============================================================
# 临时实验区（探针工程、逆向试验、一次性脚本）
# 不提交，随时可删
# ============================================================
.tmp/

# ============================================================
# 通用
# ============================================================
Thumbs.db
desktop.ini
.DS_Store
*.tmp
*.log
~$*
.vscode/
.idea/

# Python
__pycache__/
*.pyc
.venv/
venv/
